# Current State

**Branch:** `main` (all prior feature-branch work is merged; commit directly on `main`). **Plan of record:** `Documentation/Plan.v1.md`. **Architecture:** `Documentation/Architecture/Overview.md`. **Per-round loop:** `Scripts/check.sh` (build+test) → `Scripts/log-history.sh "<bullet>"` → update this file → commit (see CLAUDE.md → "Running things").

## Active task

**Sand tracks + ornithopter crash wreck — IMPLEMENTED (2026-06-02).** Both were animation SEAMs. Wired `Animation_Start` at two sites: tracked units now leave **Sand-Track** overlays driving over sand/dune (`UnitMovement.move` → `g_table_animation_unitMove[orient8]`, icon group 5), and a destroyed **ornithopter** paints its **"Flying-Machine Crash"** wreck (`GameState+Explosion` `SET_ANIMATION` → `g_table_animation_map[id]`, icon group 3 — the death script's `Unit_ExplosionSingle(16)` crash explosion was already created; `SET_ANIMATION` was the missing link). Ported `g_table_animation_unitMove` (8) + `g_table_animation_map` (16) + `AnimationKind.unitMove`/`.map`. Both are RNG-free `Animation_Start` calls on the animation/explosion (visual) tick path (off the oracle golden path) ⇒ goldens byte-identical. Tests: `TrackAndCrashTests` (+2). Insight `sim-emc-triggers-types-by-number` (EMC triggers types by raw number, not the C enum name — nearly mis-diagnosed the crash as dead code; the ornithopter is `UnitType 1`, not 11). **608 tests green**, clean release build. (Visual confirmation owed in `scenariolab`/`duneii`/`rendertest`: drive a tank over sand → tracks; shoot down an ornithopter → wreck.)

**Harvester redeploy known-issue — RESOLVED (2026-06-02).** The long-standing `HarvesterCycleTests` `withKnownIssue` ("deployed empty harvester goes STOP instead of resuming HARVEST") is fixed — **it was a test-scenario artifact, not an engine bug.** A per-opcode trace showed the harvester redeploys already in HARVEST, fails `Unit_Harvest`, `SearchSpice(3)` finds nothing, and the *escalating* wider spice search (radius 20/40/60) is **skipped for player harvesters** — UNIT.EMC gates it on `unit house ≠ player house`, i.e. the wide search is AI-only — so it STOPs. Verified faithful against OpenDUNE (`Script_Unit_GetInfo` 0x0E=`Unit_GetHouseID`; `targetLast` cleared on dock in `Unit_Move`; the refinery deploy `RefineSpice`→`FindUnitByType`→`Unknown0C5A`). The test placed the refinery **6 tiles from the spice**, so the redeploy tile was outside radius-3. **Fix: build the refinery adjacent to the spice** (`HarvesterCycleTests`, refinery corner 28,20 → 23,20) so the spice-nearest deploy tile lands on spice and the loop closes; the test asserts `harvestedAgain` directly now. **605 tests green, 0 known issues** (was the lone known-issue). Insight `sim-script-vm-engine-copy` updated (the prior "native-return divergence" hypothesis was wrong — instrument the opcode path + check scenario geometry before assuming an engine bug from a failing scenario test). The prior parallelization summary follows.

**Parallelization experiment — PAUSED + merged (2026-06-02).** Outcome: the experiment is preserved at git tag **`parallel-experiment-pause`** (branch `parallelization-experiment`); its scaffolding (`simbench`, `Simulation+Parallel.swift`, `DUNEII_UNIT_POOL` hatch, `benchInjectUnit`) was **removed**, and only the **golden-safe speedups merged to `main`**: POD `Unit`/`ScriptEngine`/`Structure` via the new `Inline<let N, Element>` wrapper (`DuneIIWorld/Model/Inline.swift`), allocation-free pathfinder (`Pathfinder.Data.buffer` → `Inline<102,UInt8>`), `reserveCapacity` on the find-arrays, and **release builds now strip the RNG/script trace observation** (`#if DEBUG` around the `Random256`/`RandomLCG` `traceSink` + the structure script decision-trace) so release is fast by default. Net measured win: **sequential tick +35–42%** (the tick is allocation-free — 0.05 allocs/tick — and ARC-free on copies). **Conclusion (`Parallelization.md` §8): sequential wins; single-sim parallelism is net-negative and capped by a memory-access/scheduling effect (NOT alloc/ARC, now eliminated) — resuming it needs Instruments + a data-layout (SoA) change.** 605 tests green (1 pre-existing harvester known-issue), clean release build 0 warnings. The original ask follows. The user asked to try the `Documentation/Architecture/Parallelization.md` plan: build a busy scenario, run it sequentially vs parallel, find which is faster (goal: larger maps / massive unit counts). **Built:** `Apps/simbench` (new executable target) — a synthetic rock-map stress scenario (per "group": refinery + heavy factory building a tank + construction yard building a starport + 2 windtraps + a harvester harvesting + a cross-house quad duel), five timed sections (pure-compute sanity, sequential per-phase, parallel-unit end-to-end, divergence-free unit-phase A/B, N independent sims); a **non-golden** parallel unit phase `Frameworks/DuneIISimulation/Simulation+Parallel.swift` (`gameLoopUnitParallel` = snapshot-per-shard via `DispatchQueue.concurrentPerform` + merge; `tickParallel`; `tickTimed`/`PhaseTimings`); `gameLoopUnit` refactored into a slot-parameterised `runUnitPhase` (behaviour-preserving — goldens byte-identical); `Pool.unitIndexMax` reads `DUNEII_UNIT_POOL` (default 102 ⇒ suite unchanged) + `GameState.benchInjectUnit` to inject units past the faithful per-type pool cap (~80 ground units). **Result (16-core, release): sequential wins decisively** — ~123k ticks/s faithful (56 units), ~2500 ticks/s at 2000 injected units; **every parallel variant is slower** — even §4a embarrassingly-parallel independent sims run ~7–10× *slower*, intra-tick snapshot ~78× slower at 2000 units. Pure compute parallelizes 10–12× on the same harness ⇒ the slowdown is **intrinsic**. **Round 2 pinned it** (simbench `[5]`–`[8]` + the `PlayerActions` POD fix): de-sharing `ScriptInfo` and making `ObjectInfo.actionsPlayer` POD both had **no effect**; the memory-streaming sanity `[8]` scales **~10×** while the sim collapses (worker sweep `[7]`: ~1.3× at 2 workers, ~0.13–0.29× at 16). So it is **allocator/ARC-bound** — the malloc global lock + atomic reference-counting serialize across cores — **not** bandwidth- or compute-bound (both scale fine), and **not** any single shared buffer. **Next steps in `Parallelization.md` §8.3/§8.4:** §3 first as a *prerequisite* with a pinned target — eliminate hot-path heap allocation (reusable scratch buffers) **and** ARC traffic (concrete value types over `any …Primitives` existentials on the per-unit path, `borrowing`) — measure success via `simbench [7]` (curve stays ≥1×); the batch-parallel sweet spot is ~2–4 workers, not core count. Then SPEEDUP (§2); re-measure §4a; §4e last. The user's world double-buffer idea is a valid technique but **does not** address this bottleneck (`[6]` ticks in place with no world realloc and still collapses) — see §8.4 note. **Round 3 (done): eliminated per-tick allocation + ARC** — new POD `Inline<let N, Element>` wrapper (`DuneIIWorld/Model/Inline.swift`) replaces the four heap arrays every `Unit` carried (`orientation`/`route` + `ScriptEngine.variables`/`stack`), making `Unit`/`Structure` fully POD (memcpy copies, no ARC); pathfinder `Data.buffer`→`Inline<102,UInt8>` (alloc-free pathfind, routes byte-identical); find-arrays `reserveCapacity` at init. **`simbench [9]` (a `malloc_logger` hook) proves the tick is allocation-free: 0.05 allocs/tick.** Wins: sequential tick **+35–42%** (faithful 123k→175k ticks/s, massive 2533→3392), intra-tick snapshot `[2]` 3034→250ms (~12×). All golden-safe (605 tests green, save round-trip intact). **But parallel single-sim still collapses (~0.1×) — now provably NOT alloc/ARC** (the tick is alloc-free); the residual is a memory-access/cache/scheduling effect (`[8]` sequential streaming scales ~8×, scattered sim access does not) that needs **Instruments** to localize — out of reach in this headless sandbox. The requested goal (eliminate per-tick alloc+ARC) is done + verified; further single-sim parallelism needs a data-layout change (SoA / shrink the hot working set), a separate effort. See `Parallelization.md` §8.5. **No-oracle dev-tool/experiment ⇒ flag-off neutrality bar met:** `runUnitPhase` refactor + `Pool` env-default-102 keep all goldens byte-identical (483 sim/world/scenario + full **605 tests** green, 1 pre-existing harvester known-issue); the parallel path is explicitly non-golden and off every golden path; `simbench` release build is 0-warning. **Run:** `cd Code && swift build -c release --product simbench && .build/release/simbench [groups] [ticks] [targetUnits] [shards]` (massive: `DUNEII_UNIT_POOL=8000 .build/release/simbench 14 100 2000`). **Before this branch:** **No task in flight — clean stopping point (2026-06-02).** Latest: **faithful Pause/Resume for a factory build in `duneii`.** The build panel showed an "On hold" label + Cancel but no way to resume a halted build; OpenDUNE/1.07 has no auto-resume for the *player's* construction (only the repair pad + the AI effectively auto-resume) — the player resumes by clicking the held item (`gui/widget_click.c:107`/`124`, the `STR_ON_HOLD`/`STR_D_DONE` cases). Confirmed with the user we keep this faithful (no QoL auto-resume), and wired the click path: new `Command.pauseBuild`/`.resumeBuild` → `GameState.structurePauseBuild` (sets `.onHold`) / `structureResumeBuild` (clears `repairing`/`onHold`/`upgrading`), dispatched in `UnitOrders.apply`, exposed as `GameModel.pauseBuild()`/`resumeBuild()` and a Pause-while-building / Resume-while-held button pair in `Panels.buildSection`. A resumed build advances only once credits are available (re-holds the same tick if still broke — faithful). Player-GUI seam (no oracle-drivable golden; the parity harness keeps the player powered) ⇒ flag-off neutrality (new commands unused by existing goldens → all byte-identical) + `StructureProductionTests` (+2: pause→resume continues the build; resume-while-broke re-holds until money returns). **605 tests** green (1 pre-existing harvester known-issue), clean `--full` build, 0 warnings. **Manual check owed (live SwiftUI panel): `swift run -c release duneii` from `Code/`** → start a build, Pause it (progress stops), Resume it (continues); spend down to broke mid-build → "On hold" + Resume, which only progresses once credits return. Before that: **`duneii` presentation perf — two CPU-trace-driven throttles.** A CPU Profiler trace (`Traces/trace-1.trace`, 43 s) showed the per-frame cost dominated by (a) the sandworm heat-haze rebuilding a fresh `SKTexture(cgImage:)` every frame per worm (the biggest per-frame texture upload, ~1% of total CPU) and (b) the steady-state HUD derivations driving SwiftUI re-layout every display cycle (~15–19% of total in `CA::Transaction::commit` → NSView layout → AttributeGraph). Fixed both with a new shared **`FrameThrottle`** (`DuneIIContracts/FrameThrottle.swift` — fires one call in every `interval`; interval 1 = neutral/every-frame; the first call always fires so initial state populates immediately): the renderer's `SpriteKitRenderer.shimmerUpdateInterval` (default 1 = byte-identical to before; `GameScene` sets 2) rate-limits the worm shimmer rebuild (skipped frames keep the last patch texture/position), and `GameModel.refreshDerived` runs economy/credits/build/structure-actions/tile-info/hints at ~10 Hz (every 6 frames) but refreshes **immediately** on a selection/order change; radar/selection/order/game-end stay immediate. No-oracle presentation seams ⇒ **flag-off neutrality** bar: the `scena001-worm` render golden stays byte-identical at the default interval, and the HUD change is host-only (touches no sim/render golden path → all goldens byte-identical). Tests: `FrameThrottleTests` (+4), `ShimmerThrottleTests` (+2, install-gated, asserts interval 2 → half the rebuilds, default → every frame). **603 tests** green (1 pre-existing harvester known-issue), clean `--full` build, 0 warnings. **Manual check owed (can't unit-test the live SwiftUI/SpriteKit window): `swift run -c release duneii` from `Code/`** → confirm the worm shimmer still animates smoothly and the credits/economy/build panels update without visible lag. Before that: **authentic AdLib FM music backend via SwiftOPL3 + a MIDI⇄AdLib switch.** Added a second music backend so the user can pick the synthesis path. New `ADLMusicPlayer` (`DuneIIAudio`) synthesises the Westwood `DUNE<file>.ADL` files on an emulated OPL3 chip — the external **SwiftOPL3** package (`OPL3Chip` + `WestwoodADL.ADLPlayer`; git dep `https://github.com/bealex/SwiftOLP3.git` `from: "1.0.0"` — HTTPS because the author's SSH URL is firewalled in this sandbox; transitively pulls `memoirs-ios`, compiled in only under SwiftOPL3's `OPL_TRACE` trait, off here). It streams through an `AVAudioSourceNode` whose render block pulls PCM and ticks the 72 Hz driver between sample boundaries — all cross-thread state in a `Mutex<RenderState>`, every render-path member `nonisolated` (no `nonisolated(unsafe)`; non-`Sendable` chip/driver built *inside* `withLock`; end-of-track hops to main via one guarded `Task`). See insight `swift-audio-render-thread-mutex`. A `MusicEngine` protocol + `MusicBackend` enum (`.adlib` default / `.midi`) let `MusicDirector.backend` swap engines **live** (stop → rebuild → resume the playing track at the same `currentMusicID`); exposed as a Settings ⌘, → Audio picker (`GameModel.musicBackend`, persisted in `UserDefaults` key `musicBackend`). The OpenDUNE `(file, song)` selection table is unchanged — `.adlib` resolves it to `DUNE<file>.ADL` subsong `song` (the §7 "ADL track index == XMIDI sequence index" assumption is confirmed end-to-end by the real-data render test). No-oracle feature ⇒ **flag-off neutrality** bar (music is host-side, touches no sim state — all goldens byte-identical) + `AudioTests/ADLMusicTests` (+4: `MusicBackend` cases, every selectable track resolves to a `.ADL` on disk, the OPL3 path renders non-silent PCM for a selected track, backend-switch preserves the selection). Docs: `Architecture/Music.md` (new backends section) + `Music.OPL2.md` (marked ✅ implemented with an as-built summary); `DuneIIAudio/CLAUDE.md` updated. **594 tests** green (1 pre-existing harvester known-issue), clean `--full` build, 0 warnings. `ADLMusicPlayer` is **producer/consumer** — a background thread synthesises into a ring buffer; the `AVAudioSourceNode` callback only copies out (OPL3 DSP is too heavy for the render thread, and the first cut crashed by capturing `@MainActor self` in the render block). After the upstream SwiftOPL3 speedup (now tracking `branch: "main"`, commit `0cc22ab`), **OPL3 synthesis is ~0.67× real-time in debug and ~0.04× in release** — debug now keeps up (release still recommended for margin). **Manual check still owed (audio can't be unit-tested): `swift run -c release duneii` from `Code/`** → load a scenario (AdLib FM plays by default) → Settings ⌘, → flip Music engine to MIDI and back (track continues in the new timbre). **This session's `duneii` polish batch (all app-side presentation/input unless noted; full suite green at 597 tests, clean `--full`):** OLP3 updated to the optimised synth (now tracking `branch: main`, `0cc22ab`); **gapless AdLib rollover** — the producer keeps looping a finished track until the next supersedes it, so music never goes silent waiting for the main-actor rollover (the old code stopped on end → seconds of silence under a busy main thread); **squish sound** when infantry is crushed (host `VoiceTable` 35→`SQUISH2.VOC`; the sim chain was already faithful — golden `WorldTests/UnitDeathAnimationTests`); **build-progress bar** under the health bar while a factory/CY produces (new `FrameInfo.Structure.buildProgress`, white bar; golden `FrameInfoTests.buildProgress`); **better selection cursor** — pulsating white square (3 s) for buildings, the original `MOUSE.SHP[6]` sprite for units; **radar fixes** — now tracks the live map (rebuild base on a tile-hash change, was frozen at scenario start), obeys fog of war (darken veiled cells + hide blips when `showFog`), right-click issues a unit order, middle-button click/drag pans; **worm shimmer no longer drags the dithered fog edge** into its silhouette under fog (`ShimmerEffect` `veiled` predicate; golden `ShimmerEffectTests.fogSuppression`); **building keyboard shortcuts** r=repair / u=upgrade / s=stop; **upgrade offered only at full HP** (mirrors OpenDUNE's `Structure_BuildObject` factory-window gate `si->o.hitpoints == s->o.hitpoints`); **construction startable underfunded** (matches the original — billed incrementally, auto-pauses `.onHold` out of money); **map no longer spans under the toolbar** (dropped `.ignoresSafeArea()`). Plus the design doc **`Architecture/Parallelization.md`** — the `SPEEDUP` compile mode (strip trace output + `-O`) and the determinism-bounded tick-parallelization plan. Before that: **minimap corner-blob fix (round 2) — clip dots to the playable `mapArea`** — units can faithfully sit in the unplayable border (the Atreides palace Fremen wave scatters via `Tile_MoveByRandom` into tile (0,0); OpenDUNE creates it too, `Unit_Create` wrapped in `g_validateStrictIfZero++`), so the minimap plotted a stray corner blip. Presentation-only fix: plot a blip only when its tile is inside `frame.mapArea` (the same clip the base terrain uses). `MinimapBlobTests` (+2) load real scenarios + run them: `SCENO020` reproduces the border Fremen spawn and that it's clipped, `SCENA020` never plots a corner blip (590 tests). Before that: **Settings window + Save/Load in the File menu (`duneii`)** — a SwiftUI `Settings` scene (⌘,) with Sound-effects + Music toggles (new `EngineAudioSink.enabled` master SFX gate + `GameModel.soundEnabled`; the Music toggle moved out of the Debug panel), and Save/Load added to the File menu (⌘S/⌘O via `CommandGroup`, still in the toolbar too — shared `presentSaveGame`/`presentLoadGame` helpers). Presentation/preferences seam (no oracle): flag-off neutrality + manual verification; 588 tests unchanged. Before that: **single scenario-chooser popover in `duneii`** — house tabs + scenarios grouped by campaign level (replacing the separate scenario menu + the standalone campaign picker, which had a `@Observable` `didSet` self-assign recursion bug); picking a mission now sets `state.campaignID` from the scenario's level via a new **testable `ScenarioID` parser** in `DuneIIWorld` (`SCEN<House><NNN>.INI` → house/mission/campaign, using Dune II's 1-then-3-per-level grouping; `ScenarioCatalogTests` +3, 588 tests). Before that: **campaign-level selector + full greyed-out build menu in `duneii`** — the scenario toolbar gained a Campaign 1–9 picker (`GameModel.campaignLevel` → `state.campaignID`, applied live + on every load; previously pinned to the default 1, which is why Heavy Factory and other late-tech were never buildable), and the factory panel now lists **every** item via the new `Simulation.buildOptions(forStructure:)` — locked items greyed with a "Requires: …" tooltip naming the missing prerequisite structures / campaign level / factory upgrade (`BuildBlocker`). Golden bar: `buildOptions`' available subset asserted == the cross-engine-verified `buildables` (`Structure_GetBuildable`) + blocker-content tests (`StructureBuildTests`, +3; 585 tests). Before that: **minimap top-left phantom blip fixed** — `makeFrameInfo`'s structures loop wasn't filtering `.isNotOnMap`, so a `Structure_Create`d-but-not-yet-placed structure (parked off-map at world (0,0)) leaked into `frame.structures` and the minimap drew it as a cyan square in its top-left corner; added the same `.isNotOnMap` guard the units loop already has (`FrameInfo+Make.swift:73`) + `FrameInfoTests.hiddenStructureOmitted` neutrality test (582 tests). Before that: **spoken "unit destroyed" announcement implemented** (the death-cue feedback voice — see Recently completed; closes the last of the three death-audiovisual gaps). Before that: **sandworm swallow eat animation fixed + lab scenario**, **"Play indefinitely" toggle** (Debug panel disables victory/defeat), **player-palace auto-fire fixed** (the loader didn't set `flags.human`, so the player's castle launched its house missile at scenario start; now set on `Brain=Human`), **base-under-attack alert fixed** (now sim-driven, only on real explosion impact), and **in-game music DONE**. **590 tests** green, clean `--full` build, both apps build, oracle rebuilt+re-signed. **Standing rule (`CLAUDE.md` + memory `feedback_golden_per_feature`): every new feature needs a golden test** — cross-engine scenario golden (gameplay), render golden (renderer), or flag-off neutrality golden (no-oracle debug/UI). **Resume frontier = Phase-6 presentation polish.** **Remaining candidates:** (a) the harvester redeploy bug — ✅ **RESOLVED 2026-06-02** (test-scenario artifact: refinery too far from spice; player harvesters don't escalate-search — see the top of this section); (b) **per-house *announcement* voices** (build-complete/under-attack/deploy — `%c`=house): these need new sim-emitted `SoundEvent`s, so they touch the sim + need golden care (the *acknowledge* voices are DONE — see below); (c) cosmetic seams — all DONE (rubble/destruction animation, crater overlay, distance attenuation, and **music**, 2026-06-01). **(This session: harvester redeploy bug investigated + re-diagnosed (deferred to a trace deep-dive per the trace-align-first rule); acknowledgement voices + player hints DONE — see Recently completed.)** **Periodic self-review done 2026-06-01 at `4438003`** (was 41 commits since `b5caa09`): the session's recurring lessons → two new insights — `host-presentation-gap-not-sim` (a "missing" cue/visual in the client is almost always a host gap — a `VoiceTable` mapping / `FrameInfo` field / overlay — not a sim bug; recurred via the squish sound + build bar) and `render-fog-applies-to-every-overlay` (fog is re-applied per layer — terrain, minimap base+blips, worm shimmer — not inherited; recurred via the radar-fog + worm-shimmer-fog fixes). (Knowledge-base search via retau was unavailable — the sandbox blocks the local MCP server at `127.0.0.1:23900`.) Next self-review trigger: +32 from `4438003` (`git rev-list --count 4438003..HEAD`). (Prior review: 2026-05-31 at `b5caa09` — added `parity-golden-regen-cwd` + `build-test-filter-suite-name`.) The historical breadth-push context is retained below.

**Phases 0–7 all have working first versions (2026-05-31).** Sim closed + cross-engine-verified; renderer world-content gap-free; input, audio, the **`duneii` native-macOS multi-window client**, and the **wired combat/explosion sounds** all landed (see "Recently completed"). **The frontier now is depth, not breadth — pick up here:** (a) ✅ **DONE (2026-05-31)** — the **build/factory dialog** + `Structure_BuildObject` GUI in `duneii` landed (buildable selector + progress + structure placement; see "Recently completed" + `Architecture/BuildGUI.md`); remaining build seams: starport stock, the `available == -1` upgrade-to-unlock listing, hint/voice text; (b) the **Phase-2 save/load + original-save converter** tail; (c) drag-select a group + the original selection-box sprite; (d) the **per-house spoken `%c` voices** (unit-acknowledge/announcement VOCs — the `VoiceTable` first pass covers only the `+` combat effects). **The authoritative remaining-gap list is now `Documentation/Architecture/FeatureParity.md`** (2026-05-31 audit). **Gameplay-seam closing pass: batch 1 = win/lose + score tallies + `[MAP] Bloom`; batch 2 = scenario reinforcements + campaign degrade + `[CHOAM]` stock; batch 3 = palace super-weapons + AI base expansion + starport ordering; batch 4 = `[MAP] Field` spice fields — all DONE.** **Save/load + the original-save converter are now DONE (2026-05-31):** our `SaveGame` (a versioned binary plist of the whole `GameState` — bit-identical deterministic resume) and `SaveConverter` (reads an original OpenDUNE `.SAV`, cross-engine-verified vs the oracle's load). **🎉 No missing gameplay features remain** — the entire four-phase battle sim + persistence is implemented and cross-engine-verified vs OpenDUNE 1.07. The frontier is now presentation/Phase-6 polish (per-house spoken voices, the human palace-launch + starport-buy GUI windows, `duneii`-client polish — all out of `FeatureParity.md`'s scope). The **periodic self-review is done** (2026-05-31, 34 commits since `selfreview-phase4`; nothing new rose to a standing rule — see the self-review tracker). The historical Phase-4 detail below is retained for reference.

**Phase 4 — the `FrameInfo`-driven world renderer (SpriteKit). [DONE — historical detail follows].** The first block — the **`FrameInfo` sim→render seam** — is **done** (2026-05-30): `DuneIIContracts/FrameInfo.swift` + `SpriteLayer.swift` define the presentation-neutral per-frame snapshot, and `Simulation.makeFrameInfo()` (`DuneIISimulation/FrameInfo+Make.swift`) emits it from `GameState`. It carries: the 64×64 terrain tile grid (ground + overlay + fog; **structures are baked into the ground tiles** by `Structure_UpdateMap`, so the renderer draws buildings from the tile layer), units (id/type/effective-house/position + the `UnitSprites`-resolved body/turret `SpriteLayer`s + hp + isSmoking), structures (for the inspector/selection), transient effects (active explosions + smoke clouds, as global sprite indices at world positions), per-house economy (credits/storage/power for the game-info panel), and the viewport origin — all in **world sub-tile units** (256/tile) and **global sprite indices** so the renderer owns pixel size/zoom + SHP-frame mapping. `UnitSpriteLayer` is now a typealias of the canonical Contracts `SpriteLayer`. Design doc: `Documentation/Architecture/FrameInfo.md`. Tests: `FrameInfoTests` (+9).

The second block — the **renderer's FrameInfo-consumer core** (`DuneIIRenderer`, headless-testable) — is also **done** (2026-05-30): `Renderer` protocol + `NullRenderer` (records `lastFrame`); `GlobalSprite` (the canonical `Sprites_Init` load-order global-index→sheet+frame mapping — `UnitSpriteSheet` `.units2`@111/`.units1`@151/`.units`@238 — deduping the apps' copies); `WorldSpriteSource` + `SpriteFrame` (the asset abstraction so compositing stays pure, asset-loading stays app-side); and `FrameComposer` (pure: terrain ground layer → a `side×side` indexed buffer, units body+turret + effects → placed/flipped/house-tinted/z-ordered `ComposedSprite`s; colorization deferred to the leaf). Design doc: `Documentation/Architecture/Renderer.md`. Tests: `FrameComposerTests` (+5). Known gaps (documented, not bugs): sandworm shimmer (`blurTile` units omitted from `FrameInfo`) + tile overlays not yet composited.

The third block — the **`SpriteKitRenderer` visual leaf + hosting it in `mapview`** — is also **done** (2026-05-30): `DuneIIRenderer/SpriteKitRenderer.swift` (`@MainActor`) colorizes the `FrameComposer` output (terrain buffer recomposited on tile change + recoloured on palette-cycle tick → one nearest-filtered `SKSpriteNode`; each `ComposedSprite` house-recoloured via `HouseRemap`, index-0 transparent, rebuilt per frame) in a camera scene. It realizes the `Renderer` seam shape without a formal conformance (so the headless `Renderer`/`NullRenderer` seam stays actor-agnostic). **`mapview` migrated to it:** `MapScene` now advances the `Simulation`, snapshots `makeFrameInfo()`, and renders it each frame — the `GameState`-reaching `MapImageBuilder` is **deleted**, replaced by a value-type `MapSpriteSource: WorldSpriteSource` over `AssetStore`. This proves the seam end-to-end on real `SCEN*.INI` scenarios. (Verification note: pixel-fidelity is now checked **in-environment** by the render-golden harness — `RenderGoldenTests` captures the real renderer off-screen via `SpriteKitRenderer.snapshot` and diffs pixel-exact (the sandboxing now grants an off-screen GPU context). The *live on-screen window* still can't be `screencapture`d here and SIGSEGVs when launched detached, so a final on-screen look is the user's `swift run mapview`; the compositing logic is also unit-tested via `FrameComposerTests`/`FrameInfoTests`.)

**Reported-bug status (the user's mapview-bug list):** #1 carryall tile-by-tile, #2 carryall rotation, #4 windtrap purple start, #5 destroyed-building stays — **all closed** (reported-bug pass, `3401c0e`/`23c7c12`/`8093709`). **#6 rocket/air orientation — closed** (air-unit winger pass, `e41cdaf`). **#3 harvester harvesting overlay — closed** (render/frameinfo pass, 2026-05-30: the `viewport.c:546` third layer now drawn + animated). **The reported-bug list is now empty.** Plus two more fixed in the same pass: **air-unit render order** (wingers were sometimes drawn behind ground units — now z-ordered above ground + explosions, matching `viewport.c`'s separate air pass) and **the carryall-drop phantom** (`makeFrameInfo` was drawing `isNotOnMap`/in-transport units, so a picked-up/carried harvester showed at a stale tile before the carryall delivered it — now skipped, as `viewport.c` never draws hidden units). *(All three are unit-tested; the on-screen confirmation is the user's `swift run mapview`.)*

**Next concrete steps (Phase 4 remainder / Phase 5 — pick up here):**
1. **Visually verify `mapview`** (`swift run mapview` — the GUI SIGSEGVs when launched detached from this harness, so it's the user's run): the harvester overlay/air-z-order/carryall-phantom fixes + the new **Fog toolbar toggle** (overlay + fog compositing).
2. **Dedupe `scenariolab`** onto the same path (it still has its own `globalSprite` + `ScenarioImageBuilder`); migrating it to `GlobalSprite`/`FrameComposer`/`SpriteKitRenderer` (or at least `GlobalSprite`) removes the last copy of the load-order mapping.
3. **Render features + goldens** (`RenderGoldenTests` + `Scripts/gen-render-goldens.sh` run **and diff in this sandbox**; 5 cases — `scena005` multi-structure base, `scena001` soft-fog edge, `scena001-worm` shimmer, base t0/t60). The renderer's world content now has **no outstanding gaps**: overlay transparency, partial-fog soft edges, and the sandworm shimmer are all done. Add more golden cases as visual regressions warrant (a combat/explosion frame, a structure-destruction frame, a harvester-harvesting frame): one line in the `cases` table + a `gen-render-goldens.sh` re-record. Remaining renderer work is **presentation polish / Phase 6 UI**, not world content.
4. **Phase 5 — `DuneIIInput`: first version DONE** (see Recently completed). `InputSource` + `ScriptedInput` + `Selection` + the `InputController` state machine; `mapview` wires mouse/keyboard → pick → `Command` → `Simulation`, outlines the selection, and shows a properties/commands inspector panel. **Remaining:** a reusable `CatalystInput` (the in-game host's event source, Phase 6), drag-select a group, and the original's selection-box `g_sprites[6]` sprite (we draw a plain outline). (Richer commands DONE in `duneii`: a/m/h/r keyboard-armed move/attack/harvest/retreat + s=stop, with a crosshair targeting cursor — `OrderKind`/`Command` `.harvest`/`.retreat`.)
**Remaining documented render gaps (deferred, not bugs):** sandworm shimmer (faithful `DRAWSPRITE_FLAG_BLUR` needs reading terrain-under-worm per pixel → compositor rework) and partial-fog edge sprites (the sim models fog as binary). See `Architecture/Renderer.md` → Known gaps.
Read `Plan.v1.md §4` (4.1/4.6/4.7) + `Architecture/Renderer.md` + `Architecture/FrameInfo.md`.

**Phase 3 (`DuneIISimulation`) — ✅ CLOSED (2026-05-30). The headless simulation core is complete and cross-engine-verified against OpenDUNE.** All behavioral slices (1–6) are ported + tested; the team subsystem is now exercised end-to-end by a **cross-engine `team` golden** (the last DoD item); the parity bar is raised to an **RNG draw-stream golden** (our per-tick `Random256`/`RandomLCG` draws are byte-identical to the oracle's across all 9 scenarios). Slice 7 (campaign degrade + palace special-weapon bodies) + the documented seams (scenario reinforcements, the palace house-missile, the `Structure_BuildObject` GUI factory window, fog/render/audio) are explicitly Phase 4–7 / deferred-per-DoD. **Next: Phase 4 — the `FrameInfo`-driven world renderer (SpriteKit).** A fresh session starts here: read `Plan.v1.md §4` (topology + `DuneIIRenderer`) + `Architecture/Overview.md`. First concrete step — design the **`FrameInfo`** sim→render seam in `DuneIIContracts` (what the renderer needs per frame: visible units/structures with sprite + frame + house + screen position, the viewport, dirty regions) and have `Simulation.tick()` emit it; then build the `Renderer` protocol + `SpriteKitRenderer` (pixel-faithful world content, nearest-neighbour upscaled into a resizable window) consuming it. The asset-drawing services (`DuneIIRenderer` `HouseRemap`/`IndexedImage`) + `SpriteCatalog`/`StructureCatalog` + the `mapview`/`scenariolab`/`rendertest` apps already exist to build on. Done: the pause-aware two-clock loop + the four phases; all native primitives (golden-verified vs OpenDUNE); the EMC script VM + dispatch; unit movement + combat (`attack-close` bit-identical for the full 400 ticks); the pathfinder; **the entire EMC-driven structures subsystem** (`GameLoop_Structure` + the structure script VM, building death, turrets firing, refinery refining, deploy/unload, `Unit_EnterStructure`, carryall summon — *every* `BUILD.EMC` native) **plus the complete `tickStructure` build/repair/upgrade economy** (`structureTickStructure` + `Structure_IsUpgradable`); the **House economy** (`House_CalculatePowerAndCredit` + `Structure_CalculateHitpointsMax` + a live `GameLoop_House`: credit clamp + power upkeep + attack timers); the **complete `GameLoop_Team` + AI** (random cursor + `isAIActive` gate + the team script VM with the **entire `g_scriptFunctionsTeam` table** — getters, recruit/target, order-issuing, `Load`/`Load2` — **plus team creation** `Team_Create` + the `[TEAMS]` scenario section); and the **entire `g_scriptFunctionsUnit` table** (64/64 — movement/combat/harvester/carryall/sandworm/death/MCV-deploy). All four test apps run the live sim (`mapview`/`scenariolab` visually, `duneii-headless` as a CLI demo), now incl. the build/repair/upgrade economy scenarios. Per-slice detail is in "Recently completed" below + `History/` (the daily files); the subsystem plan is `Plan.v1.md §9` + `Architecture/SimulationLoop.md`; the structures detail is `Algorithms/StructureScript.md`, the teams detail `Algorithms/TeamScript.md`.

**Finish Phase 3 — the ordered to-do (status 2026-05-30: behavioral slices 1, 3, 4, 5, 6 DONE + tested; slice 2 exercised end-to-end, cross-engine golden deferred with rationale; slice 7 deferred per DoD).** All three EMC native tables are complete and all four `GameLoop_*` phases are live + their bodies ported. Status of each behavioral slice:

1. ✅ **Carryall winger flight** — DONE (verified). The winger flight motion was delivered by the native-table work (`SetDestinationDirect`/`MoveToTarget`/`MoveToStructure`/`Pickup`/`TransportDeliver`) + the `Unit_Move`/`MovementTick`/`GameLoop_Unit` winger branches; an end-to-end flight test (`CarryallTests.wingerFlight`) confirms a winger actually flies toward its destination.

2. ✅ **End-to-end team AI + cross-engine golden** — DONE. Exercised end-to-end through the live loop (`TeamLoopTests.endToEndTeamAI`: recruit → target → order an attack) **and** by a cross-engine `team` golden: `parity.c` gained `Scen_LoadTeam` (`[TEAMS]` → `Team_Create` + `isAIActive`) + `TEAM.EMC`; the scenario unit dump gained the `team` field; the `teams` golden matches the oracle full 400 ticks on both unit state (the team field `0→1` as `Team_AddClosestUnit` recruits) and the RNG draw stream. (Targeting is fog-gated off, `seenByHouses 0`, matching the harness; the full chain incl. targeting is covered by the Swift test.) The earlier "stochastic AI → byte-exact golden impossible" worry was wrong — after the `GameLoop_Team` cursor + animation RNG fixes, the stream aligns and it's a full match.

3. ✅ **Area-effect combat seams** — DONE. Sonic-blast area damage (sonicProtection immunity + wall RNG), the death-hand 17-point blast, saboteur arrival detonation, the `range != 0` IMPACT crater, sand-burst on empty sand (`AreaEffectCombatTests`).

4. ✅ **Deviator gas + map blooms** — DONE. `Map_DeviateArea`, `Map_FillCircleWithSpice`, `Map_Bloom_ExplodeSpice` (+ harvester-death spice). `Map_Bloom_ExplodeSpecial` left unported (unreachable in 1.07 — `isSpecialBloom` never set) (`MapEffectTests`).

5. ✅ **`GameLoop_House` tails** — DONE. `Map_FindLocationTile`, `Unit_CreateWrapper`, `House_EnsureHarvesterAvailable` + the harvester-incoming spawn, the starport stock bump, the starport frigate delivery, the `playerCreditsNoSilo` reset (`GameLoopHouseTailsTests`). Scenario reinforcements (`[REINFORCEMENTS]` data not loaded) + the palace house-missile (slice 7) remain documented seams.

6. ✅ **`Structure_BuildObject` headless state-setup** — DONE. The concrete-objectType build path (`UnitCombat.structureBuildObject`) + `Structure_CancelBuild` (`BuildObjectTests`). The player factory-window GUI (sentinels, `Structure_GetBuildable` + `available` flags, starport stock, placement hints) is a documented Phase-6 seam.

7. ⏸️ **Campaign degrade + palace** special-weapon countdown bodies + `g_scenario` kill/harvested tallies — deferred per the DoD (cursors advance; bodies are SEAMs).

**Definition of done for Phase 3 — ✅ ALL MET (Phase 3 closed):** slices 1–6 ported + tested ✅; the team subsystem exercised end-to-end by a cross-engine golden ✅; `Structure_BuildObject` headless portable parts ✅; full suite green + clean `--full` build ✅ (404 tests). Slice 7 + the render/audio/GUI seams (`Unit_UpdateMap(2)`, `Voice_PlayAtTile`, `Sound_Output_Feedback`, `Unit_Select`, GUI hints/text, fog reveal, scenario reinforcements, palace house-missile, the `Structure_BuildObject` factory window) are **Phase 4–7 / deferred-per-DoD**, not Phase 3.

*(Done: the entire `tickStructure` body; the whole team subsystem (loop + every native + creation); the **entire unit native table** (64/64); the structure-placement chain `structureCreate`/`Place`/`IsValidBuildLocation` — see "Recently completed".)*

**Verification frontier (the "even-better harness").** The scenario golden dumps **houses + structures + units** per tick. `attack-structure` is a full 400-tick match (it caught the `world-structure-corner-position` bug); the new **`economy`** golden is a full 60-tick **house-aggregate** match (credits/power/storage) via a `[HOUSES]` scenario section — validating `House_CalculatePowerAndCredit` + the credit clamp + power upkeep cross-engine (see Recently completed). **Tier-2a structure decision-trace is now done** — the oracle's per-opcode `--parity-script-trace` is wired into the scenario mode + our `StructureScriptRunner` emits the identical line (`StructureScriptTracer`/`ScriptTraceLine`), diffed by `StructureTraceTests`; the `attack-structure` windtrap matches the oracle's 24-opcode trace exactly (opcode-identical EMC execution). Next levels (`ScenarioHarness.md`): broaden the decision-trace (a **unit** trace via `--parity-script-unit`; a structure running its **death** branch) + **Tier-3i semantic event-trace alignment** (RNG-robust `(tick,event,entity,payload)`). (The per-tick house credit/power golden — the `economy` scenario — is also done.)

**Per-round checks: use `Scripts/`.** `Scripts/check.sh` (build + full suite → concise BUILD/TESTS/VERDICT; `--full` = clean-build zero-warnings audit, `--filter <Suite>` = inner loop), `Scripts/build-oracle.sh` (rebuild + re-sign the oracle, sandbox disabled), `Scripts/gen-scenario-goldens.sh [--only <name>]` (regenerate goldens), `Scripts/log-history.sh` (append a History bullet). **Investigation helpers (new):** `Scripts/odfn.sh <Fn>` (print an OpenDUNE C function + file:line), `Scripts/emc.sh <unit|build|team> [N|--linear|all]` (disassemble an EMC script / its shared-subroutine region via `--linear`), `Scripts/golden.sh <name>` (a golden's per-tick "what changed" timeline). Fold any new repeating action into them (CLAUDE.md → "Running things").

**Build/oracle quirk in this environment:** direct `exec` of `/usr/bin/clang` and of freshly-built binaries returns `EPERM`. Rebuild the oracle via `Scripts/build-oracle.sh` (a `.shim/gcc` shim routes `gcc`→`xcrun clang`; it re-signs `bin/opendune`, since a relinked binary is SIGKILLed on exec — "killed", no output — until re-signed). Build/test Swift with `Scripts/check.sh` (or raw: `cd Code && mkdir -p .build/tmp && TMPDIR="$PWD/.build/tmp" xcrun swift build --disable-sandbox`; recreate `.build/tmp` after `swift package clean`). The oracle build + golden regen need the sandbox disabled. See insight `build-exec-eperm`.

## Next up (queue)

- **Tick speedup — plan ready, not implemented.** `Documentation/Architecture/Parallelization.md` (added 2026-06-01) lays out: a **`SPEEDUP` build mode** (env-driven `.define` + `#if !SPEEDUP`, built `-c release`) that strips all non-essential debug output (the `RngTraceSink` hooks in `Random256.next`/`RandomLCG.range`; the script decision-trace decode in `StructureScriptRunner`) to zero footprint; single-thread copy/alloc cuts (kill the `var u = state.units[slot]; …; state.units[slot] = u` double-copy in `gameLoopUnit`); and determinism-safe parallelism (whole independent sims, off-path `makeFrameInfo`) — the shared-RNG stream order caps single-tick parallelism. **Phase 1 = the SPEEDUP flag + strip tracing + `Scripts/bench.sh` + the flag-on/off final-state equivalence golden.** Every change keeps the scenario + RNG-stream goldens byte-identical; any parallel change needs the mandatory identical-hash stress test.
- **Phase 3 — `DuneIISimulation`** ✅ **CLOSED** (see Active task). The headless sim core is complete + cross-engine-verified (9 scenario goldens + an RNG draw-stream golden, 404 tests). Deferred to Phase 4–7: slice 7 (campaign degrade + palace special-weapon bodies), scenario reinforcements, the palace house-missile, the `Structure_BuildObject` GUI factory window, and the render/audio/GUI seams.
- **Phase 4 (NOW) — the `FrameInfo`-driven world renderer (SpriteKit)** + **Phase 5 `DuneIIInput`.** The asset-render services (`DuneIIRenderer`) + the `mapview`/`scenariolab`/`rendertest` apps already exist; the renderer here is the in-game `FrameInfo` consumer.
- **Phase 6 — hosts + multi-window UI: first version DONE** (see Recently completed). The `duneii` **native macOS** client (pivoted from Catalyst) exists: a SwiftUI map window + 4 floating `NSPanel` tool windows (minimap/selection/economy/debug), with combat/explosion sounds wired, health/state HUD, smooth selection, and continuous fog. **Remaining:** the build dialog / factory window, drag-select a group, and a proper game-info/objectives panel.
- **Phase 7 — `DuneIIAudio`: first version DONE** (see Recently completed). The `SoundEvent` seam + `AudioSink`/`NullAudio`/`EngineAudioSink` (low-latency, polyphonic AVAudioEngine playback) exist; the sim now **emits `SoundEvent`s** for combat fire + explosions (`emitSound`/`GameState.soundEvents`) and `duneii` plays them via a ported `VoiceTable`. **Remaining:** the per-house **spoken `%c` voices** (unit-acknowledge/announcement VOCs — `VoiceTable` covers only the `+` combat effects), distance attenuation from `SoundEvent.position`, and music — **all DONE** (2026-06-01; music: see Recently completed + `Architecture/Music.md`).

## Recently completed

- **Minimap obeys radar + STATIC.WSA tuning animation + debug toggle — DONE (2026-06-01).** The minimap gates on the player's radar: blank until `House.radarActivated` (outpost built + powered, toggled by the ported `House_UpdateRadarState`), exposed via a new `FrameInfo.House.radarActivated`. A **"Minimap (force on)"** Debug-panel toggle (`GameModel.forceMinimap`) overrides for debugging. On a radar on/off transition the host plays the decoded **STATIC.WSA** noise frames (`Minimap.radarStaticFrames` via the `Wsa` decoder; forward=coming online, backward=going dark) driven by `GameModel.updateRadar`; the **announcer voice** (feedback 28/29) was already emitted by the sim. **Also: `House.structuresBuilt` is now computed at scenario load** (`Game_Prepare`) so the radar + build prerequisites are correct from turn 1 — golden-neutral (not dumped, RNG-free; goldens byte-identical). `+2 tests` (`FrameInfoTests` radar exposure, `ScenarioLoaderTests` structuresBuilt-at-load). **581 tests**, clean `--full`.
- **All remaining gameplay sound announcements wired — DONE (2026-06-01).** Closed the audio-feedback seams from the audit. **Sim emits** (golden-neutral — RNG-free + not dumped, all goldens byte-identical): structure-destroyed `pendingFeedback` 21-24 + CRUMBLE `Voice_PlayAtTile(44)`; **threat warnings** 1-12/37 + battle music (`Unit_HouseUnitCount_Add` player-alert block ported — directional "enemy approaching" from the player CY, saboteur=12, sandworm=37, late-campaign house-specific); unit/harvester deployed 30-35/68-73; starport frigate 38; spice bloom 36; AI palace house-missile 39; carryall drop `Voice(24)`→DROPEQ2P; **radar on/off 28/29** (`House_UpdateRadarState` core ported in `structureCalculateHitpointsMax`, player-only — outpost+power toggles `radarActivated`); animation + script `VoicePlay` natives (unit 0x26 / structure 0x0E). **Host:** `FeedbackVoice` is now data-driven over the `g_feedback` sequences + `g_table_voices` fragments (registered per house, played in order, rate-limited, battle music on threat indices); `VoiceTable` +24/44/54 (DROPEQ2P/CRUMBLE/EXTINY). `+5 FeedbackAnnouncementTests`. **580 tests**, clean `--full`. **Two index spaces:** `Sound_Output_Feedback`→`pendingFeedback`→`FeedbackVoice` (direct g_table_voices); `Voice_PlayAtTile`→`emitSound`→`VoiceTable` (g_table_voiceMapping).
- **Spoken "unit destroyed" announcement — DONE (2026-06-01).** Implemented OpenDUNE's `Sound_Output_Feedback` death cue (`Unit_Damage`, `unit.c:1558`) — the last of the three death-audiovisual sources (see the sandworm note below). **Sim:** `UnitImpact.damage`'s lethal branch appends the feedback index to `pendingFeedback` — saboteur→20; else (unless the unit is `noMessageOnDeath` — bullets/worm/frigate — or wasn't alive) `houseID+14` for the player's own loss / `campaignID>3`, else `13` ("enemy unit destroyed"). RNG-free ⇒ golden-neutral (the append isn't dumped + draws no RNG; all goldens stay byte-identical). **Host:** new `FeedbackVoice` table (feedback index → a `%c`-prefixed `g_table_voices` fragment sequence — e.g. 13→[ENEMY,UNIT,DESTROY]) + `GameModel.playDeathFeedback`, which registers the fragments per player house (durations stored) and plays the sequence in order scheduled by each clip's length, **rate-limited to one announcement at a time** (`speaking`) so a busy battle doesn't pile up speech. `+1 UnitCombatTests` (enemy→13, player→14, saboteur→20, sandworm/`noMessageOnDeath`→silent). The feedback `voiceId[]` are **direct `g_table_voices` indices**, not `voiceMapping` ids. **575 tests**, clean `--full` build.
- **Sandworm "swallow" eat animation + lab scenario — DONE (2026-06-01).** Filled the `Map_MakeExplosion(ui->explosionType, pos, 0, 0)` seam in `UnitCombat.fire` (the `bulletType==sandworm` eat case, `script/unit.c`): eating a unit now starts the `EXPLOSION_SANDWORM_SWALLOW` gulp animation (type 13, sprites 218-222) at the worm; the WORMET3P voice (`emitSound(63)`) was already wired. `hitpoints: 0` ⇒ visual-only, RNG-free ⇒ golden-neutral (no worm in any golden). `+1 SandwormNativesTests` (eat starts a type-13 explosion + emits voice 63 + decrements `amount`). New `scenariolab` **`sandwormEating`** scenario (`DuneIIScenarios` `ScenarioKind` + builder forcing unit1=sandworm vs a pickable prey + `ScenarioOutcome`) — pick it in scenariolab to watch the eat. **This also fixes one "destruction with no animation/sound" case: a worm-eaten unit was removed silently** (it's swallowed, not DIE-scripted, so it never ran `ExplosionSingle`). Other death audio is multi-source (see note). **574 tests**, clean `--full` build. **Note — why unit destruction is sometimes silent/animation-less:** death audiovisual comes from THREE distinct sources — (1) the unit's DIE EMC script `ExplosionSingle` → an explosion sprite whose table may carry a `playVoice` boom (vehicles: tankExplode/wheeled/flames all do); (2) **foot units** die via a silent corpse-overlay animation (no explosion ⇒ no boom); (3) the spoken "unit destroyed" cue is OpenDUNE's `Sound_Output_Feedback` in `Unit_Damage` (saboteur→20, else `houseID+14`/13 gated on `!noMessageOnDeath && wasAlive`) — **still a SEAM in our engine** (`UnitImpact.damage` line ~47), so that announcement never plays. So vehicle kills boom, foot kills are quiet, eaten units were silent (now fixed). Implementing the spoken death announcement is a separate follow-up (the multi-word feedback-voice system).
- **"Play indefinitely" toggle (disable victory/defeat) — DONE (2026-06-01).** A Debug-panel toggle so the game never ends. New golden-neutral `GameState.disableLevelEnd` (default `false`) short-circuits `Simulation.evaluateLevelEnd`; `GameModel.playIndefinitely` applies it to the live sim (`didSet`) and to every load (`finishLoad`, so it survives a save/restore), and clears any already-latched `gameEndState` (dismisses the Victory/Defeat banner) when turned on mid-/post-game. UI: `Panels.DebugPanel` toggle. Debug/UI seam (no OpenDUNE oracle behaviour) ⇒ the golden bar is **flag-off neutrality** (default off ⇒ `evaluateLevelEnd` unchanged ⇒ all scenario/render goldens byte-identical) + unit coverage (`LevelEndTests.playIndefinitely`: the toggle keeps `gameEndState == .playing` past a met win condition; the control still wins). **573 tests**, clean `--full` build.
- **Fix: the player's palace auto-launched a rocket at scenario start — DONE (2026-06-01).** The palace special-weapon gate (`!flags.human && isAIActive`, `structure.c:65` / `Simulation` palace tick) is faithful, but our `ScenarioLoader` never set `flags.human` on the `Brain=Human` house — so the *player's* castle fired its own house missile on the palace's first tick (a fresh palace's `countDown` starts at 0). Set `flags.human` in `ScenarioLoader.loadHouses` for `Brain=Human` (mirrors `Scenario_Load_House`, `scenario.c:74`) and on the `duneii` app's chosen player house (it picks the player via `AssetStore.playerHouse`, which can differ from the loader's fallback). **Verification:** `ScenarioLoaderTests` now asserts SCENA001's Atreides player is `.human` (CPU Ordos isn't); `PalaceTests.loadedHumanPalaceDoesNotAutoFire` is an end-to-end repro — load a scenario with a human palace + `isAIActive` (via `[TEAMS]`), run the palace cursor, assert no fire (countDown 0, no missile), and the negative control (clear `.human`) fires. A cross-engine golden is **impossible** here: the headless oracle SIGSEGVs on *any* ticked player-house palace (the player-house GUI/economy path) and on flying any house missile (the documented `palace-launch` 0-tick limit) — confirmed empirically (exit 139), so the bar is unit tests + golden neutrality (existing goldens byte-untouched; no fixture uses `Brain=`). Reverted the briefly-added unused `[BASIC] PlayerHuman` harness knob (`parity.c`), keeping only the `house-under-attack` dump fields. **572 tests**, clean `--full` build, oracle rebuilt (house-under-attack golden re-verified identical).
- **Fix: "base under attack" false-fires + the voice never played — DONE (2026-06-01).** The 2026-06-01 cosmetics batch had wired the alert as a **host heuristic** in `GameModel.refreshHints`: it summed all player-structure HP each frame and fired on *any* drop vs last frame (rate-limited 600f) — so building **placement** (partial-slab HP), **campaign degrade**, and **power-shortfall** HP clamping all triggered "your base is under attack." Root-caused against OpenDUNE: the alert is `Structure_HouseUnderAttack` (`structure.c:1933`), called **only** from `Map_MakeExplosion` (`map.c:500`) on a real combat impact, gated by `timerStructureAttack`. **Ported it** as `GameState.structureHouseUnderAttack` (`GameState+Lifecycle.swift`, beside `structureDamage`): human-player (≡`playerHouseID`) → raise feedback `48` + arm the 8-tick timer; AI → flip `doneFullScaleAttack`; the enhanced-only counter-attack loop is omitted (un-enhanced parity). Called from `mapMakeExplosion` right before `structureDamage` (mirrors `map.c:500`). New transient `GameState.pendingFeedback: [UInt16]` (the global `Sound_Output_Feedback` seam, distinct from positional `soundEvents`; cleared each tick). `GameModel` now drains `pendingFeedback` per tick → message + `%cWARNING.VOC` voice (un-attenuated) + `music.enterBattle()`; the cumulative-HP block + its `lastPlayerStructureHP`/`underAttackCooldown` fields are deleted. RNG-free ⇒ **golden-neutral** (the oracle dump carries neither timer nor flag; all scenario/render goldens stay byte-identical). `+8 HouseUnderAttackTests` (player raises 48+timer; timer gates a 2nd hit; AI no feedback; 0xFF no-op; via-explosion player/AI/visual-blast; **direct `structureDamage` raises nothing** — the degrade/power/placement regression pin). **Cross-engine `house-under-attack` scenario golden** (a Harkonnen tank hits an Ordos windtrap; the Ordos house's `doneFullScaleAttack` flips 0→1 at the tick-82 impact, tick-for-tick vs the oracle): extended `parity.c` `Parity_DumpHouses` with `timerStructureAttack`+`doneFullScaleAttack` + an opt-in `[BASIC] PlayerHuman`; a `checkUnderAttack`-gated optional house comparison in `ScenarioGoldenTests` keeps every other committed golden byte-untouched. Negative-tested (disabling the call fails the golden at tick 82, isolating the change). The player-human feedback (timer + voice/message) can't run headless — the oracle SIGSEGVs in `Sound_Output_Feedback` (no string table) — so it's a UI seam, unit-tested only (stated per the workflow). `FeatureParity.md` §H row added. **571 tests**, clean `--full` build, oracle rebuilt+re-signed. Note: a clean (`swift package clean`) build was required — the `GameState` field addition changed its layout and an incremental build SIGSEGV'd via a stale `.swiftmodule` (insight `build-gamestate-layout-stale-module`). **Manual confirm (user):** place a building / let buildings degrade ⇒ **no** alert; take real weapons fire on a building ⇒ alert + spoken "your base is under attack."
- **In-game music — DONE (2026-06-01).** The last "honest exception" (XMI/AdLib) is closed without an XMI decoder: macOS *is* the synth. `MusicPlayer` (`DuneIIAudio`) plays the already-extracted Standard-MIDI songs (`Resources/Audio/Music/DUNE<file>.<song:02d>.mid` — the `.NN` suffix **is** OpenDUNE's `g_table_musics` song index) through **`AVMIDIPlayer`** + the system DLS bank (or a drop-in `Resources/Audio/music.sf2`; same role as OpenDUNE's FluidSynth, not OPL2 — which OpenDUNE doesn't do either). `MusicDirector` is the selection policy, a verbatim transcription of `g_table_musics` + the per-house `musicWin/Lose/Briefing` IDs. `GameModel` drives it host-side: scenario load → random map theme (rolling into the next at its end), player-base-under-attack → attack theme, win/lose → house stinger, pause → pause. Music never touches the sim/`GameState`/sim-RNG (host RNG, non-deterministic, explicitly outside the determinism contract), so the **golden bar is a flag-off neutrality golden** — all scenario+render goldens stay byte-identical. `+7 MusicDirectorTests` (table verbatim, per-house IDs, filename mapping, random-pool ranges seeded, every selectable track exists on disk). Design doc `Architecture/Music.md`. **563** tests, clean `--full` build. **Manual confirm (user):** load a scenario → music plays + cycles; get attacked → faster track; win/lose → stinger; pause → pauses. (FeatureParity untouched — presentation seam, out of its scope.)
- **Presentation seams + cosmetics batch — DONE (2026-06-01; `2db82b8` audio, `732c033` crater).** Worked through the FeatureParity presentation/cosmetic TODOs. **(1) Distance attenuation** — `AudioSink.setListener` + `EngineAudioSink` fade a positioned `SoundEvent` by distance (full ≤2 tiles → ~0.12 ≥18 tiles); `GameModel.advance` now plays the full event (was discarding position) + sets the listener from the viewport. **(2) Per-house announcement voices** — `registerHouseVoices` loads the player-house `%c`-prefixed VOCs (`HCONST`/`AWARNING`/…, prefix from `HouseInfo.prefixChar`), played on the client hint events: construction-complete → `%cCONST`, base-under-attack (player-building HP drop, rate-limited) → `%cWARNING`. **(3) Crater overlay** (§257, last seam) — `Explosion_Func_TileDamage` tail: World VM records the impact tile (`pendingCraters`), `Simulation.drainCraters` stamps the sand/rock crater (grow-or-random `Random_256`) + spice deduct + bloom pop; gated to `tickExplosions` (off goldens) ⇒ parity-neutral; `+1` `ExplosionTests`. **Already done (verified):** destruction-rubble (§231 — the death animation's ground-tile rubble cycle) and partial-fog soft edges (§269 — `FrameComposer.cell` composites `fogEdgeSpriteIndex`) → both flipped ✅. **Music — now DONE** (2026-06-01, see the next bullet). All duneii/render/audio presentation → neutrality (**556** tests, clean `--full` build). Docs `DuneiiClient.md` (Audio), `Algorithms/Explosion.md`, `FeatureParity.md` §206/§231/§257/§350. **Visual confirm (user):** distant combat is quieter; build a structure / get attacked → house voice; shoot sand → crater.
- **Wire pause + win/lose into duneii (2026-06-01; `f62256d`).** Two sim features the engine had but the client never surfaced (audited the `Command` cases — all reachable — and `Simulation`/`GameState` for unexposed state). **(1) Pause:** `GameModel.paused` → `GameState.paused` (the two-clock pause; `Simulation.tick` no-ops while paused, camera/selection/orders keep working) — a toolbar play/pause button + the **space** key, speed picker disabled while paused. **(2) Win/lose:** `GameModel` reads `GameState.gameEndState` (latched each tick by `evaluateLevelEnd`/`GameLoop_IsLevelFinished`) → a centred **Victory/Defeat** banner + auto-pause. Both reset on load (`finishLoad` syncs `paused`/`gameEnd` from the new state — a fresh scenario is unpaused/playing; a restored save keeps its pause). App-only wiring, no sim change → neutrality (**555** tests, clean `--full` build). Doc `DuneiiClient.md`. **Visual confirm (user):** space pauses; reach a win/lose condition → banner.
- **Fix: fog over-reveal — the REAL cause of "I see all enemy troopers" + "AI fog does nothing" (2026-06-01; `73a478c`).** A genuine **parity bug**, found by tracing fog with a new cross-engine golden. `Unit_RemoveFog` (`unit.c:1217`) reveals the player's fog **only for player-allied units**; our `GameState.unitRemoveFog` was missing the `House_AreAllied` check, so **every enemy unit self-revealed the player's fog around its own tile** → (a) the player saw all enemies through the fog, (b) `Unit_UpdateMap` then treated each now-unveiled enemy as *player contact* → `aiFogReveal` fired for every house instantly, defeating the toggle. Added the allied + off-map/`0xFFFF`/radius-0 guards. **New `fog` scenario golden**: added `veil` (isUnveiled) to the `[DUMPTILES]` dump (`parity.c` + `ScenarioGoldenTests`) + `fog.ini` — our `veil` now matches the oracle every tick (before: we unveiled tiles 5–10 away the oracle kept fogged). Gated at tick 6 (**residual**: the guard soldier *adjacent* to the base picks a different target encoding than the oracle — a stock targeting wobble, unrelated to fog; follow-up). Re-recorded the scena001 fog render golden; fixed `StructureScriptTests.turretFiresAtEnemy` (a "seen" enemy must be on an unveiled tile). Insight `sim-unit-removefog-allied-only`. **555** tests, clean `--full` build, oracle rebuilt. **(Supersedes the earlier `reapplyPlayerVisibility` fix `7c2bc16` — real, but not the main cause.)** **Visual confirm (user):** scena001 + fog on ⇒ enemies hidden; aiFogOfWar on + do nothing ⇒ AI passive.
- **Three duneii requests — dominant-type drag-select, save/load, all-action shortcuts (2026-06-01).** (1) **Drag-select the dominant type** (`e95350b`): a mixed box (trike+harvester) can't share one order, so on release the box is reduced to its most-numerous unit type (ties → lowest type id) via the testable `InputController.dominantGroup`; `+1` `InputControllerTests`. (2) **Save / load** (`06ac5ba`): a toolbar **File menu** wires the existing `SaveGame` (whole-`GameState` binary, bit-identical resume) through `NSSavePanel`/`NSOpenPanel` → `GameModel.saveGame(to:)`/`loadGame(from:)`; a shared `finishLoad` tail builds the sim for both scenario-load and save-restore (`structureScript` hoisted to a stored prop). `SaveGame` round-trip covered by `SaveGameTests`. (3) **Keyboard shortcuts for all actions + `r`=Return** (`186c655`): `GameModel.issueAction(ActionType)` fires only if the action is in the unit's `actionsPlayer` (a=Attack ignored for a harvester, r=Return for a tank); a/m/h arm a click, r/e/g/d/b/x apply at once, s=Stop; fixed the old Retreat-armed-as-targeted bug (it's `selectionType .unit`); immediate actions + inspector buttons now hit the whole group; inspector labels show the letters. **555** tests, clean `--full` build, both apps build. Doc `Architecture/DuneiiClient.md` (input model). **Visual confirm (user):** drag mixed units (only majority selected), File→Save/Load, select a harvester + press `r`.
- **Fix: AI fog of war "doesn't work, they attack anyway" (2026-06-01; `7c2bc16`).** Root cause: enabling the duneii toggle **after** a scenario was already loaded (or relying on the default-off load) left the base placed with stock `seenByHouses=0xFF`, and the live toggle only affected *future* objects — so the AI kept seeing/attacking the base. New `GameState.reapplyPlayerVisibility()` re-applies the current flag to every existing player object (on ⇒ re-hide: reset to the player bit + clear `housesFoundPlayer`; off ⇒ restore `0xFF`); duneii's `aiFogOfWar` didSet now calls it, so the toggle works **immediately, mid-game, no reload**. A test confirmed set-before-load already worked (the loader's `unitUpdateMap` of far enemies in fog doesn't trigger `aiFogReveal`). `+2` `AIFogTests`, **554**, clean `--full` build. Note: "contact" still includes the player's units/scouts sighting an enemy (by design), so the AI engages once you actually meet — but no longer rushes a hidden base. Docs `AIFogOfWar.md` + panel text.
- **Player hints + unit acknowledgement voices + harvester-bug re-diagnosis (2026-06-01).** Three items from the "do 1/2/3" pass. **(1, deferred) Harvester redeploy bug** (`4813322`): investigated the deployed-empty-harvester-goes-STOP defect; *ruled out* the script-VM reconciliation (traced `UnitScriptRunner.run` — `setActionDefault` loads STOP into the VM engine + writes it back consistently; the suspected `setAction` write-back is a no-op). It's a **native-return divergence in the post-deploy RETURN-action EMC** (ACTION_HARVEST is never set by C — resume-HARVEST is an EMC `SetAction` gated by a native); needs a `--parity-script-unit` cross-engine trace. Updated insight `sim-script-vm-engine-copy`; **not** hand-patched (trace-align-first rule). **(2) Unit acknowledgement voices** (`1666550`): `g_table_voices` 17–22 (the `%c`=Z English set) — select → REPORT1 (foot)/REPORT2 (vehicle) (`unit.c:1730`); order → the action voice for a foot unit (move→MOVEOUT, attack/retreat→OVEROUT, harvest→REPORT3) or a host-random REPORT3/AFFIRM for a vehicle (`viewport.c:182`). New `SoundID`s + `playSelectVoice`/`playOrderVoice`/`isFootUnit`; the vehicle random uses a **host** RNG (never the sim's), so goldens are untouched. **(3) Player hints** (`960f4cf`): a transient bottom-of-map banner (`GameModel.notice`/`refreshHints`) on construction-complete / low-power / insufficient-funds, all derived from the player economy + factory state (no new sim events). Both (2)+(3) are duneii presentation only → **neutrality** (552 tests, sim untouched) + manual verify. Doc `Architecture/DuneiiClient.md` (Audio + Hints). **Visual confirm (user):** `swift run duneii` → select/order units (voices), build/lose-power (hint banners).
- **Drag-select a group (2026-05-31; committed `0ff566e`).** Box-select multiple units (a verification-client convenience — the original Dune II selects one unit at a time). `InputController` gains `selectedUnits` + `selectGroup`; **left-click is now resolved on mouse-UP** so a press-drag-release rubber-bands a green box (`GameScene.mouseDown/Dragged/Up` + `dragBoxNode`, 4px threshold, suppressed during placement/missile/armed-order modes). `GameModel.dragSelect` collects every player-owned, on-map, **normal** unit in the tile box → `selectGroup`. Orders (move/attack/stop, armed `a`/`m`/`r`, right-click) issue **per selected unit**; a *single* selected harvester still defaults to Harvest, a group never does (`isSelectedHarvester` now requires `selectedUnits.count == 1`). Each selected unit gets a white outline (a pooled `selectionNodes` + `selectionBoxes()` replacing the single `selectionNode`/`selectionBox()`); inspector header shows `×N`. Input/UI only, no OpenDUNE oracle → **neutrality** (full golden suite byte-identical, sim untouched) + `+2` `InputControllerTests` (group selection/orders). **552**, clean `--full` build, both apps build. Doc `Architecture/DuneiiClient.md`. **Visual confirm (user):** `swift run duneii` → drag a box over your units, then right-click to move them as a group. See History.
- **Starport CHOAM pricing + the order charge (2026-05-31; committed `b5caa09`).** Fixes "starport units were free." Ported `GUI_FactoryWindow_CalculateStarportPrice` (`gui.c:2726`) as `GameState.starportPrice(buildCredits:)` — `(c/10)*4 + (c/10)*(lcg(0,6)+lcg(0,6))`, capped 999, **two LCG draws/item** — with a **cross-engine golden** (new `Golden_StarportPrice` → `choamprice-golden.jsonl` + `StarportPriceTests`, value-for-value vs the oracle). `UnitCombat.structureStarportOrder` gains a `price` (default 0 ⇒ neutral, existing starport golden/tests unchanged) and **charges** it, refunding on pool-full; `Command.starportOrder` carries it. duneii rolls a CHOAM price per in-stock unit **once per starport selection** (drawing the live sim LCG, as window-open does in 1.07 — `GameModel.pricedStarport`), shows + charges it, disables unaffordable orders. `+2` tests (`StarportPriceTests`, `StarportOrderTests.chargesPrice`), **550**, clean `--full` build, duneii builds, oracle rebuilt+re-signed. Docs: `Algorithms/StarportPrice.md`, `FeatureParity.md` §J. See History.
- **Unit-limit toggle — `enforceUnitLimit` (2026-05-31; committed `126284d`).** New duneii Debug-panel **"Follow unit limit"** toggle + the `GameState.enforceUnitLimit` flag it drives. Default **true** = faithful (the `Unit_Allocate` per-house cap check, `GameState+Pools.swift:134`, is now gated on it — a house at its cap can't make ground units, wingers/slithers still bypass). Off (debug) = build past the cap. **duneii now keeps each house's real scenario `MaxUnit`** (default 39) instead of pinning `1000`, so "follow" actually enforces the scenario limit; the toggle flips enforcement live + at load. Debug toggle with no OpenDUNE oracle for the off-mode (like `aiFogOfWar`) → golden bar = **neutrality** (full suite byte-identical with default-true) + `GameStateTests.unitCapDisabled`. `+1` test, **548**, clean `--full` build, duneii builds. **Visual confirm (user):** `swift run duneii` → Debug → "Follow unit limit" off ⇒ build past 39. See History.
- **duneii tool-window polish — 4 UI requests (2026-05-31; committed `d2fd4a9`).** (1) **Tile inspector** — left-clicking a bare tile (no unit/structure) now shows its parameters in the Selection inspector: landscape name, packed id, ground/overlay tile ids, spice, owner (concrete/wall/structure), fog, buildable. New `TileInfo` + `GameModel.inspectedTile`/`refreshTileInfo` (derives via `DefaultMapPrimitives.landscapeType` + `LandscapeType.displayName` + `LandscapeInfo.isValidForStructure`); clears when an object is selected. (2) **Debug window** — removed the "State chip" legend row + "Idle units show no chip" text (and the now-unused `legend`/`RightTriangle`/`Diamond`). (3) **Selection window** — removed the "Deselect" button (Esc still deselects; clicking bare ground now inspects). (4) **Economy window** — lists only houses **on the map** (`housesOnMap` = ≥1 used unit/structure), dropping merely-`[HOUSES]`-activated empty houses. All duneii-only UI (no OpenDUNE oracle) → neutrality bar: the full golden suite stays byte-identical (sim untouched). **547** tests, clean `--full` build, duneii builds. **Visual confirm (user):** `swift run duneii` → click bare tiles, check the three panels. See History.
- **Two duneii input-bug fixes — harvester right-click + middle-drag pan (2026-05-31; committed `e2c1166`).** (1) Right-clicking a selected **harvester** now issues its default **Harvest** order (`actionsPlayer[0]`) instead of Move — it harvests/seeks spice at the clicked tile. `InputController.rightClick` gains a `harvester:` fact (resolved by the host's `isSelectedHarvester` in both `GameModel` + mapview `MapScene`); harvester wins over the enemy flag (it can't attack). (2) **Middle-mouse drag/recentre** now pans the main map — `SKView` relays `mouseDown`/`rightMouseDown` to its scene but **not** `otherMouse*`, so the scene's pan handlers never fired; `FirstMouseSKView` now forwards `otherMouseDown/Dragged/Up`. Both are duneii/mapview **UI-input fixes with no OpenDUNE oracle** (the original has neither input) → golden bar = neutrality (the full golden suite stays byte-identical, sim untouched) + a unit test; the harvest *behaviour* itself is already golden/unit-covered. `+1` `InputControllerTests` (harvester right-click), **547**, clean `--full` build, both apps build. **Visual confirm (user):** `swift run duneii` → right-click a harvester onto spice (harvests), middle-drag the map (pans). See History.
- **New standing rule + `palace-launch` cross-engine golden (2026-05-31; committed `3b009a0`).** User: **every new feature requires a golden test**, not just unit tests — codified in `CLAUDE.md` (Feature workflow step 3 now mandatory + "What counts as tested") and memory `feedback_golden_per_feature`. Applied it retroactively to the palace launch: new oracle `--parity-launch` / `Scen_LaunchMissile` (`Repositories/OpenDUNE/src/parity.c`), a `launch:`/`LaunchCmd` `Spec` in `ScenarioGoldenTests`, a `launch,` case in `gen-scenario-goldens.sh`, and the `palace-launch` fixture (Harkonnen player palace fires the death-hand). **Frame-0 golden** (like `refinery-harvester`): the spawned `missileHouse` bullet's spawn tile + jittered `targetAttack` (encodes the 3 launch RNG draws) + the palace's re-armed countDown match the oracle. The oracle **segfaults flying a `missileHouse` bullet headless** (a harness limit — our engine flies it fine, covered by unit tests + the §G blast), so the bullet isn't ticked. The `aiFogOfWar` debug toggle's golden requirement is met by neutrality (the full golden suite stays byte-identical with the flag off). Oracle rebuilt+re-signed. **546** tests, clean `--full` build. See History.
- **Human palace super-weapon launch (2026-05-31, Phase-6 polish; committed `4e352e7`).** The AI already auto-fires its house weapon (`tickPalace` → `structureActivateSpecial`); now the **human** can launch from a selected ready palace. `structureActivateSpecial(_ slot, missileTarget: UInt16? = nil)` — for the death-hand MISSILE the human passes the clicked tile (jittered ±160 via `Unit_LaunchHouseMissile`) instead of the AI's "first non-allied structure" scan; `nil` (AI + Fremen/saboteur) is unchanged ⇒ **golden-neutral**. New `Command.activateSuperWeapon(structure:)` (Fremen/saboteur, no target) + `Command.launchHouseMissile(structure:tile:)` (death-hand), consumed by **`Simulation.applyPalaceCommand`** (returns false ⇒ caller falls back to `UnitOrders`), gated to a **ready** (`countDown==0`), `used`, player-owned palace. duneii: `GameModel.superWeapon`/`missileTargeting` + a **Launch button** in the inspector (Fremen/saboteur fire in place; death-hand arms a target-select map click — left-click fires, right-click/Esc cancels, crosshair cursor). `+3` tests (`PalaceTests` human launch/gating), **546**, clean `--full` build, duneii app builds. Docs: `Algorithms/PalaceSuperWeapon.md`, `FeatureParity.md` §P (House-missile launch ◐→✅). **Visual confirm (user):** `swift run duneii` → select a ready player palace → Launch. See History.
- **`aiFogOfWar` debug toggle — give the AI a fog of war (2026-05-31, Phase-6 polish; committed `fd35e4d`).** Answers "the AI rushes my base from turn one." Stock Dune II (faithfully ported) reveals every player-owned object to *all* houses the instant it's placed (`Structure_Place`/`Unit_HouseUnitCount_Add` set `seenByHouses = 0xFF`) — the render "Fog of war" toggle is player/render-only and never touched the AI. New **debug** flag `GameState.aiFogOfWar` (default **off** ⇒ exact stock behaviour, golden/RNG-neutral, draws no RNG): `playerObjectVisibilityMask()` (returns `0xFF` off, `player|housesFoundPlayer` on) replaces the blanket `0xFF` in `UnitCombat.structurePlace` + `GameState.unitHouseUnitCountAdd`; the player **making contact** (their fog sighting an enemy unit/structure via `mapUnveilTile`/`unitHouseUnitCountAdd`) fires `aiFogReveal(toEnemyHouse:)`, which flips that AI house's bit in `housesFoundPlayer` and back-fills the whole player base/army's `seenByHouses` — the discover-then-rush arc. Wired into **duneii**: `GameModel.aiFogOfWar` (set on the live sim + every scenario load) + a Debug-panel toggle. `+5` tests (`AIFogTests` ×4, `StructureBuildTests.placedStructureVisibility`), **543**, clean `--full` build, duneii app builds. Doc: `Architecture/AIFogOfWar.md`. Out of `FeatureParity.md`'s scope (debug/test aid). **Visual confirm (user):** `swift run duneii` → Debug panel → "AI fog of war" (set before loading a scenario) → quiet early game until you scout the enemy out. See History.
- **Scenario map boundary — don't draw the unused border (2026-05-31, Phase-6 polish).** Each scenario uses only a sub-rectangle of the fixed 64×64 grid (`[BASIC] MapScale` → `g_mapInfos[mapScale]`; the first scenarios use a small centre window). Gameplay already respected it (`Map_IsValidPosition`/`MapInfo.scales`); the gap was presentation. **`FrameInfo.mapArea`** now carries the playable rectangle (filled by `makeFrameInfo` from `GameState.mapScale`, default full 64×64); **`FrameComposer`** blacks out terrain cells outside it (`borderColourIndex`, unconditional of fog) + **culls** unit/effect sprites outside it; `SpriteKitRenderer`'s dynamic path skips border cells; the **duneii camera** (`Viewport.area`) clamps pan/zoom to the rect + recenters on load; the **minimap** blacks its border too. Render-golden-neutral (every committed crop sits inside its scenario's playable area — SCENA001 `MapScale=1`, SCENA005 `MapScale=0` — verified). `+3` tests (`FrameComposerTests` ×2, `FrameInfoTests.mapArea`), **538**, clean `--full` build, render goldens green; the `duneii` app builds. Doc: `Algorithms/MapBounds.md`. See History.
- **duneii map taps work while a tool window is focused (2026-05-31, Phase-6 polish).** Replaced SwiftUI's `SpriteView` (whose `SKView` returns `acceptsFirstMouse == false`, swallowing the first click on the unfocused main window) with a `FirstMouseSKView` (`acceptsFirstMouse → true`), so a tap on the map reaches `GameScene` even when a floating tool panel holds focus; widened the scene tracking area to `.activeAlways` so hover/placement-preview tracks unfocused too. App-only (AppKit), 535 tests unchanged. See History.
- **Shoot-the-bloom + full per-unit action menu (2026-05-31, Phase-6 polish).** **(1) Attacking a spice bloom now pops it** — wired the explosion VM's `BLOOM_EXPLOSION` command (`Explosion_Func_BloomExplosion`, `explosion.c:157`): the World-layer VM records a still-live bloom tile under a blast in `GameState.pendingBloomDetonations`, drained by `Simulation.drainBloomDetonations` right after `explosionTick` into `Map_Bloom_ExplodeSpice` (spice-fill + tremor — a Simulation primitive). The impact explosions all carry the command, so a bullet landing on a bloom detonates it. Explosion-tick (visual-app) path only → golden-neutral. **(2) The `duneii` inspector now renders the original per-unit player action menu** (`UnitInfo.o.actionsPlayer`) instead of a fixed Move/Attack — Harvest/Return/Deploy/Guard/Retreat/Destruct/…; a `.target` action arms a click, a `.unit` action applies immediately via the new `Command.setAction(unit:action:)` → `UnitOrders.setUnitAction` (`stop` now delegates to it). `+4` tests (`ExplosionTests` ×2, `BloomInteractionTests.tickDrainsBloomDetonation`, `UnitOrdersTests.setActionOrder`), **535**, clean `--full` build; the `duneii` app builds. Docs: `Algorithms/Explosion.md`, `FeatureParity.md` (bloom row). See History.
- **`duneii` structure commands — repair / upgrade / starport-order (2026-05-31, Phase-6 polish).** New `Command.repair`/`.upgrade`/`.starportOrder`, routed through `UnitOrders` to `structureSetRepairingState`/`structureSetUpgradingState` (toggle) + `structureStarportOrder`. The inspector gains Repair + Upgrade toggle buttons for a selected player building + a CHOAM order list for a starport (the starport no longer shows the empty build section). `+1` test (`StructureBuildTests.structureCommandSeam`), 531. See History.
- **Save/load + the original-`.SAV` converter — the last gameplay feature (2026-05-31).** Two deliverables. **(1) Our format** — `DuneIIWorld/State/SaveGame.swift`: `save`/`load` a versioned binary plist of the **whole** `GameState` (pools, map, both RNGs' internal state via custom `Codable` `rawState`, clocks/cursors, scenario, animations/explosions, `iconMap`). Made the model types + `IconMap` + `SoundEvent`/`SoundID` `Codable` (synthesized) so `GameState` synthesizes. A loaded game resumes **bit-identically** (`SaveGameTests.deterministicResume`: save mid-game SCENA001, load, run both 80 ticks, tick-for-tick agreement). **(2) The converter** — `DuneIIWorld/State/SaveConverter.swift`: reads an original OpenDUNE `.SAV` (the IFF/FORM chunks INFO/PLYR/UNIT/BLDG/`MAP `/TEAM), seed-regenerates the landscape + applies the sparse tile overrides, and rebuilds the pools (skipping the 55-byte embedded `ScriptEngine` — semantic ingest, re-seeded on resume, per `Plan.v1.md` §2). Cross-engine-verified: extended the oracle harness with `--parity-save` (`SaveGame_SaveToPath` + `Scen_*`) to write a real `.SAV` + dump it; `SaveConverterTests` converts the `.SAV` and matches the oracle's houses/structures/on-map units exactly. The agent's format report had wrong byte *sizes* (Object is 71 B not 38, since `ScriptEngine` is 55 not 22) — corrected by parsing field-by-field. `+4` tests, **530**, clean `--full` build. Docs: `Formats/Save.md`, insight `parity-savegame-record-sizes`. See History + `FeatureParity.md` §T (now ✅).
- **Cross-engine golden for the per-refinery harvester spawn (2026-05-31).** New `refinery-harvester` scenario golden (a Harkonnen CY builds + places 2 refineries on a concrete pad; each placement spawns its own ferried harvester — the `viewport.c:210` per-refinery spawn fixed earlier). Extended the oracle harness with a **`--parity-place`** command + `Scen_BuildPlace` (`parity.c`) and `ScenarioGoldenTests` with **`place:` specs** (the real `structureBuildObject` + `structurePlaceReady` path). Frame 0 matches the oracle on the CY + windtrap + 2 refineries (incl. the refinery's incoming-BUSY state) + the 2 spawned carryalls + `unitCount==4`. The dump snapshot now skips in-transport units (matching the oracle's `Unit_Find`). Headless gotchas (insight `parity-structure-placement-golden`): the player-house `GUI_DisplayText` paths crash (replicate only headless state-setup; keep the player powered with a windtrap). **Found + fixed a latent bug:** `UnitCombat.unitCreateWrapper` returned the **carryall**, but OpenDUNE's `Unit_CreateWrapper` returns the **cargo** — so a placed refinery's harvester got its `originEncoded` set on the carryall instead. Now returns the cargo (fixed 2026-05-31; the winger path + `!=nil`-check callers are unaffected). **525 tests**, clean `--full` build. See History + `Architecture/ScenarioHarness.md`.
- **Reported-issue pass — spice blooms, concrete, duneii HUD (2026-05-31).** Eight user-reported items: **(1) spice bloom not disappearing after detonation** — `ScenarioLoader.loadMapBlooms` no longer overwrites `mapBaseTileID` with the bloom sprite (only `groundTileID`), so `Map_Bloom_ExplodeSpice`'s revert (`groundTileID = mapBaseTileID & 0x1FF`) reverts to sand+spice instead of restoring the bloom (`BloomInteractionTests` — detonation + drive-onto-bloom; "attacking" and "moving into" a bloom are the same 1.07 arrival-detonation path). **(2) concrete dark grid lines + (3) bogus "Constructing" state** — one root cause: `UnitCombat.structurePlace` had no slab/wall case, so player-built concrete kept a slab *structure* (baking its structure sprite, selectable). Now it dispatches `slab1x1/slab2x2/wall` → `placeSlab`/`placeWall` + `Structure_Free` (`structure.c:456`/`:476`), painting the `builtSlab` concrete tile and leaving nothing selectable (`StructureBuildTests.placeConcrete`). **(4–9, duneii presentation):** the placement footprint projection now shows immediately on the **Place** button (seeded to the viewport-centre tile, then follows the cursor); **middle-mouse drag pans** the map (hand-tool; a plain middle-click recentres on the clicked point); **health-bar length = the exact health fraction** of the full width (full→100%, dead→0%, set via `size.width`, not `xScale`); **unit bars sit closer to the sprite**, **building bars draw over the building's top row**; **unit state icons are 2× smaller**. Insight `world-slab-wall-bloom-are-map-tiles`. `+3` tests, **521**, clean `--full` build; the `duneii` app builds. The presentation items (4–9) need the user's `swift run duneii` to confirm visually. See History + `FeatureParity.md` (§I/§M).
- **Gameplay-seam closing pass — batch 4 (2026-05-31).** **`[MAP] Field` spice fields** — `Scenario_Load_Map_Field` (`scenario.c:328`) detonates a `Map_Bloom_ExplodeSpice` (radius-5 spice circle) at each hand-placed field tile *at load*. The World loader can't reach the sim's bloom primitive, so `ScenarioLoader.loadMapBlooms` now stashes the `[MAP] Field` tiles in `Scenario.spiceFields`, and `Simulation.applyScenarioSpiceFields` (`Simulation+SpiceFields.swift`) drains+fills them once, driven idempotently from the top of `tick()` while the list is non-empty (so the fill + its `Map_FillCircleWithSpice` RNG land before any GameLoop draws — matching the oracle's load-time fill ahead of `g_tickScenarioStart`). Re-recorded the two SCENA001 t60 render-goldens (`Field=1300,1510` — tile 1510=(38,23)'s circle overlaps the render rect, so the now-correct spice renders); the t0/worm/scena005 goldens re-recorded byte-identical, confirming the change is isolated. `+4` tests (`SpiceFieldTests`, `ScenarioLoaderTests.loadMapField`), **518**, clean `--full` build. **This closes the gameplay-seam pass — save/load + the original-save converter (Phase-2 tail) is the only remaining gameplay gap.** See `FeatureParity.md` + History.
- **Gameplay-seam closing pass — batch 3 (2026-05-31).** **Batch 3:** (1) **palace super-weapons** — `Simulation.structureActivateSpecial` (`structure.c:822`), fired by the palace cursor in `gameLoopStructure` (now a live `tickPalace`: count the countdown down; an AI palace, fresh at `countDown 0`, fires on its first palace tick — the human's launch UI is a Phase-6 seam): MISSILE (Harkonnen/Sardaukar) launches a death-hand at the first non-allied structure via `Unit_LaunchHouseMissile` (jitter ±160, free the carrier, spawn the bullet from the palace); FREMEN (Atreides/Fremen) calls 5 hunting troopers at a random map tile; SABOTEUR (Ordos/Mercenary) deploys a saboteur next to the palace. (2) **AI base expansion** — `Simulation.aiStructureMaintenance` (run right after `structureTickStructure` per structure): auto-repair below 50% HP, auto-build idle factories via `structureAIPickNextToBuild` (`structure.c:1980` — CY rebuilds from the queue, unit factory runs the 25%-random/priorityBuild tournament), and AI construction-yard **auto-place** of a finished product at its remembered spot; `structureRemove` now records the loss in the house's `aiStructureRebuild` queue (`structure.c:1336`). (3) **starport CHOAM ordering** — `UnitCombat.structureStarportOrder` (the per-unit `FACTORY_BUY` body, `structure.c:1577`): create the unit off-map, chain it onto `house.starportLinkedID`, arm `starportTimeLeft`, decrement stock (the existing `tickStarport` frigate dispatch drains it; the factory-window GUI + CHOAM pricing stays a Phase-6 seam). All golden-safe — the new bodies are dormant in the 400-tick goldens (no `isAIActive` AI palace/factory; the parity harness only flags the team house `isAIActive`, matching `ScenarioLoader.loadTeam`). `+17` tests (`PalaceTests`, `AIMaintenanceTests`, `StarportOrderTests`), **514**, clean `--full` build. Remaining gameplay gaps: `[MAP] Field` fill (sim post-load hook) + save/load + original-save converter (Phase-2 tail). See `FeatureParity.md` + History.
- **Gameplay-seam closing pass — batch 2 (2026-05-31).** **Batch 2:** (1) **scenario reinforcements** — `[REINFORCEMENTS]` parse in `ScenarioLoader.loadReinforcement` → new `Scenario.reinforcements[16]` (`Reinforcement` recipe: type/house/location/timeBetween/timeLeft; `timeBetween = atoi·6+1`; 1.07 never repeats, the `+` is dropped); `Simulation+Reinforcements.tickReinforcements()` runs in the 600-tick house cursor — an edge entry (loc 0-3) places the unit at a random map edge (`unitCreate`), an air entry (4-7) flies it in on a carryall (`unitCreateWrapper`); each fires once. **Unit creation is lazy at deploy** (not at load — World loader can't reach the sim's `unitCreate`), observably identical bar pre-deploy pool occupancy. (2) **campaign degrade body** in `gameLoopStructure` — on `campaignID > 1`, a `degrades` structure above half its **base** HP loses its house's `degradingAmount` each ~10800-tick degrade cursor (added `tickDegrade` to the loop guard). (3) **`[CHOAM]` starport-stock seed** — `ScenarioLoader` seeds `starportAvailable[type]` from the scenario. `+9` tests (`ReinforcementTests`, `DegradeTests`, loader test), **497**, clean `--full` build. Remaining gameplay seams (next batches): palace super-weapons (death-hand/Fremen/saboteur), AI base rebuild/auto-build, starport ordering (CHOAM buy flow), `[MAP] Field` fill (needs a sim post-load hook), save/load + original-save converter, crater overlay, the harvester-STOP known issue. See `FeatureParity.md`.
- **Gameplay-seam closing pass — batch 1 (2026-05-31).** Working through the `FeatureParity.md` gameplay gaps in batches of 3 (commit+push each). **Batch 1:** (1) **win/lose conditions** — new `Scenario` model (`WinFlags`/`LoseFlags` from `[BASIC]`) + `GameState.gameEndState`; `Simulation.evaluateLevelEnd()` ports `GameLoop_IsLevelFinished`/`Won` (structure-count + quota, 7200-tick minimum), latched RNG-free in `tick()`; (2) **`g_scenario` score/kill/destroyed/harvested tallies** wired at `Unit_Die`/`Structure_Damage`/`RefineSpice`; (3) **`[MAP] Bloom`/`Special`** spice-bloom placement in the loader. `+6` tests, **487**, clean build.
- **Harvester economy — scenario test + the script-VM engine-copy root-cause fix (2026-05-31).** Created **`HarvesterCycleTests`** (a real-`UNIT.EMC`/`BUILD.EMC` scenario driving harvest→return→dock→refine→redeploy) to chase the reported "harvesters don't redeploy" bug. It reproduced a hard stall and pinned the root cause: **the unit/structure script runners execute the EMC VM on a value-type *copy* of `o.script`**, so natives that mutate the **live** script via `state` (`Object_Script_Variable4_Clear` in the refinery deploy path, `Unit_SetAction`) were clobbered on write-back — the refinery never deployed its docked harvester (stuck inside forever). Fixed with a **compare-and-adopt reconciliation** around each native dispatch (`StructureScriptRunner`/`UnitScriptRunner`): the harvester now **docks, refines, and redeploys** with **no golden regression** (476 tests, the 400-tick goldens never ran a ~9000-tick cycle). **Remaining (known issue):** the deployed *empty* harvester goes STOP instead of resuming HARVEST (`setActionDefault` writes both engine + state, breaking the reconciliation's disjointness) — `withKnownIssue` + insight `sim-script-vm-engine-copy`. The other user-reported client items (slab rendering/state, HUD tweaks, placement preview, middle-drag, per-refinery harvester, harvest command) are **still queued** — see Active task. See History.
- **Gameplay feature-parity audit + fix "place buildings anywhere" (2026-05-31).** New **`Documentation/Architecture/FeatureParity.md`** — a full gameplay parity table (Swift vs OpenDUNE 1.07), audited from the `// SEAM:` markers + source read. **The four-phase battle-sim core is done + cross-engine-verified** (movement/pathfinding/combat, full unit+structure+team script tables, build/repair/upgrade economy, placement, house power/credit, spice/harvest/blooms, fog, AI behaviour). **Remaining *gameplay* gaps** (presentation seams excluded): **win/lose conditions ✗** (quota/flags loaded but never checked — a match can't end); **palace super-weapons** (death-hand launch / Fremen / saboteur deploy bodies — slice-7 seam); **scenario reinforcements ✗** (`[REINFORCEMENTS]` unparsed); **AI base rebuild/auto-build** (`ai_structureRebuild` seam — AI doesn't expand); **campaign degrade** (slice-7 seam); **starport ordering** (buy-from-CHOAM returns early); **save/load + original-save converter ✗** (Phase-2 tail); score/kill tallies (feeds the quota check). Also **fixed the placement bug**: ported the `Structure_IsValidBuildLocation` **adjacency rule** (`structure.c:786` — a non-CY building must touch a player structure / slab / wall in its `tilesAround` ring; CY exempt). `+1` test (`StructureBuildTests.placementAdjacency`). **475 tests**, clean `--full` build. See History.
- **Fix structure health-bar length + the `400/250` outpost readout (2026-05-31).** An under-powered structure showed an over-full / wrong-length health bar and a nonsensical inspector readout (outpost `400/250`). Cause: the display used the **power-degraded** `Structure.hitpointsMax` (rescaled each house tick by `Structure_CalculateHitpointsMax` to `base·power/256`, floored at `base/2`) as the denominator, but a structure keeps its full `o.hitpoints` until it bleeds down — so HP legitimately exceeds the degraded cap. OpenDUNE divides by the **base** HP (`widget_draw.c:725` `GUI_DrawProgressbar(o->hitpoints, si->o.hitpoints)`). Fixed: `FrameInfo.Structure.hitpointsMax` + the inspector now report `StructureInfo[type].o.hitpoints` (base); the engine's degraded cap is unchanged (still the repair ceiling / degradation target). Units already used their base HP (no degrade). Insight `render-structure-healthbar-base-hp`; `FrameInfoTests.structure` now uses a degraded cap (120) to prove the base (200) is reported. **474 tests**, clean `--full` build. See History.
- **Fix incremental fog-frontier rendering (2026-05-31).** Fog was right on toggle-on but went wrong as a unit moved (toggling off/on fixed it). Cause: `SpriteKitRenderer.TileKey` (the per-cell texture cache key) **omitted the fog-edge sprite** — `FrameComposer.cell` composites it, so cells sharing ground/overlay/house but with different fog-edge masks collided in the cache, and a re-textured dirty cell reused another cell's stale edge. (The full-rebuild path goes through `terrainBuffer`, bypassing this cache — hence the toggle "fix".) Added `fogEdge` to `TileKey`. This is the *cache-key* twin of the `CellAppearance` dirty-key bug — both layers must carry any field that changes the composited pixels. `+1` regression test (`IncrementalFogTests.fogEdgeCacheKey`, confirmed red without the fix); extended insight `render-incremental-appearance-key`. **474 tests**, clean `--full` build. See History.
- **Scenario starting credits + player house from the real per-house sections (2026-05-31).** The `duneii` build UI showed **0 credits** (couldn't build): `loadScenario` only read a flat `[HOUSES]` section, which the real install scenarios don't have — they use **per-house sections** (`[Atreides]` with `Brain`/`Credits`/`Quota`/`MaxUnit`). Ported `Scenario_Load_House` (`scenario.c:50`): `loadHouses` now activates each house whose `Brain` is HUMAN/CPU, seeds `credits`/`creditsQuota`/`unitCountMax` (default 39), and for the `Brain=Human` house sets `playerHouseID` **and `playerCreditsNoSilo = credits`** (so the house-tick credit clamp `max(creditsStorage, playerCreditsNoSilo)` keeps the player's starting credits without a silo — otherwise they're clamped to ~0). The flat `[HOUSES]` parity-fixture path is kept unchanged. Re-recorded the `scena001-base-fog-t60` render-golden: `loadScenario` now sets `playerHouseID` to the scenario's real Human house (Atreides), so fog reveals around the **player's** units (the harness previously ran with the default Harkonnen). `+1` test (`ScenarioLoaderTests.loadHouses`). **473 tests**, clean `--full` build. See History.
- **Build GUI — factory build/place in `duneii` (2026-05-31).** The last big gameplay-UI piece: the player can now **build units and structures** and **place** the latter. New sim seam (`Documentation/Architecture/BuildGUI.md`): **`Simulation.buildables(forStructure:)`** ports `Structure_GetBuildable` (`structure.c:1834`) — the available list filtered by `structuresBuilt`/`structuresRequired`/`availableHouse`/`availableCampaign`/`campaignID`/`upgradeLevel` (Ordos trike→raider + siege-tank quirks); **`Simulation.buildState(structureSlot:)`** reports `(objectType, isStructure, progress, isReady, onHold)`; **`UnitCombat.structurePlaceReady(factory:position:)`** fuses the GUI place flow (`Structure_Place` + factory reset + a placed refinery's first harvester via `houseEnsureHarvesterAvailable`); and **`Command.build/.cancelBuild/.placeStructure`** route through `UnitOrders` (which builds a `UnitCombat` from its primitives). App: `InspectorPanel` gains a **Build section** for a selected player factory — a buildable selector (name + credit cost, greyed when unaffordable) → a labelled **progress bar** → **Cancel** + (a ready construction-yard structure) **Place it**; `GameScene` shows a **green/red footprint placement preview** following the cursor (`NSTrackingArea` `mouseMoved`) and places on a valid click (Esc/right-click cancels). Unit factories auto-deploy via the existing `BUILD.EMC` deploy native (no placement UI); only the construction yard needs manual placement. `+8` tests (`StructureBuildTests`), insight `build-getbuildable-upgradelevel-gate`. **472 tests**, clean `--full` build, zero warnings. Visual confirm is the user's `swift run duneii` (select a construction yard / factory). Deferred seams: starport stock, the `available == -1` "upgrade to unlock" listing, build/place hint+voice text, per-refinery (vs first) harvester. See History.
- **duneii Selection window shows the entity's state (2026-05-31).** The inspector now has a prominent "what it's doing" status line: a **unit's current action** (`ActionInfo[ActionType(actionID)].name` — Move / Attack / Guard / Area Guard / Harvest / Return / Hunt / Stop / Deploy …) or a **structure's activity** (`Building` / `Upgrading` / `Working` / `Ready` / `Idle` / `Constructing`, from `objectType`/`countDown`/`upgrade*`/`StructureState`). New `SelectionInfo.state` (derived in `GameModel.currentInfo` + `structureState(_:)`); `InspectorPanel` renders it as a tinted `Label` under the title. Republished live (the per-tick derived-info diff includes `state`), so it tracks as the order changes. App-only; **465 tests**, clean `--full` build. See History.
- **Fix fog-of-war entity masking (2026-05-31).** With `showFog` on, the terrain blackened correctly but **every unit/effect/sandworm/health-bar still drew over the veil** — enemies floated on the black map. Root cause: `viewport.c` masks fog in **two independent passes** — a terrain black-fill *and* a per-entity `isUnveiled` skip on every later pass (units `421`/`484`, explosions/smoke `644`/`681`, sandworm) — and we only had the terrain one. New **`FrameComposer.isHiddenByFog(_:worldX:worldY:showFog:)`** (`showFog && !tile.isUnveiled`); `FrameComposer.sprites` filters units + effects, `SpriteKitRenderer.updateSprites`/`updateBlurs` pass `showFog`, and `duneii`'s `GameScene.updateHealth` skips hidden entities' bars. Structures need no extra skip (baked into the ground tiles, so the terrain pass blacks them). `+1` test (`FrameComposerTests.fogMasksEntities`), insight `render-fog-two-passes`. **465 tests**, clean `--full` build. Visual confirm is the user's `swift run duneii` with fog on. See History.
- **`duneii` client UX pass — window persistence, speed control, minimap interaction (2026-05-31).** Seven user-reported client improvements: **(1)** the **main window + all four tool windows now persist their position/size between runs** via `NSWindow` frame autosave (`DuneII.main` + `DuneII.tool.<kind>`; `WindowAccessor` reaches the SwiftUI window, `ToolWindowManager.open` restores/saves each panel, cascading only on first-ever open). **(2)** health bars are now **depletion-proportional** — width tracks health exactly (full→full, half→half, 0 hp→0 width/hidden), dropping the old 5% floor. **(3)** a **0.5×–4× game-speed picker** in the toolbar (`GameModel.gameSpeed`); `GameScene.update` now **paces sim ticks against wall-clock time** (a fractional-tick accumulator at a 60-ticks/s base × speed, capped at 16 steps/frame to avoid catch-up spirals) instead of one tick per drawn frame — `advance()` became `advance(ticks:)`. **(4+5)** the minimap **recentres the map on click and follows the cursor while dragging**, and **works even when the panel isn't focused** — a transparent first-mouse AppKit overlay (`MinimapMouse`/`MinimapMouseView`, `acceptsFirstMouse`, `isFlipped`) replaces the SwiftUI tap gesture. **(6)** the minimap **redraws live** (units **and** structures now plotted each tick; the observation dependency is read in `body` so the Canvas re-evaluates). **(7)** the tool windows are now **child windows** of the map window at `.normal` level (was `.floating`): they sit **above the map always but below other applications** when ours is inactive. App-only change; **464 tests** green, clean `--full` build, zero warnings. Visual confirm is the user's `swift run duneii`. See History.
- **`duneii` review-pass fixes — health bars, state-chip shapes, smooth selection, animating-tile jitter, continuous fog (2026-05-31).** Five user-reported issues from the running client: **(1+2)** health bars now show over **real units + buildings** (never projectiles — bullets/rockets/sonic blasts), **on by default**, **halved** to 7×1 px; the unit state chips are now **distinct shapes** (move ▶ green / attack ◆ red / guard ■ blue / harvest ● orange via `SKShapeNode`) with a matching Debug-panel legend. **(3)** the selection outline **follows a unit smoothly** (sub-tile `GameModel.selectionBox`) instead of jumping tile-to-tile. **(4)** `SpriteKitRenderer` **latches** an animating cell's overlay node instead of toggling it back to the static-background render path (the two don't land on identical pixels under a fractional camera at zoom — the construction-yard corner jittered). **(5)** fog of war: ported the `Unit_UpdateMap(1)` → `Tile_RemoveFogInRadius(pos,1)` SEAM so a player-allied non-sandworm unit lifts fog **continuously as it moves** (not only at stop), and the renderer's `CellAppearance` now tracks the **fog-edge sprite** so the soft edges repaint as neighbours reveal instead of going stale. `+1` fog test. **464 tests**, clean `--full` build, scenario goldens green (the reveal is observably neutral — the oracle harness already reveals the same as Harkonnen=player). Visual confirm is the user's `swift run duneii`. See History.
- **Wire combat/explosion sounds through the `SoundEvent` seam (2026-05-31).** The sim's inline `Voice_PlayAtTile` SEAMs are now real sounds. New **`GameState.soundEvents`** queue + **`emitSound(voiceID, at:)`** (RNG-free → golden-neutral; ignores the `0xFFFF`/out-of-range sentinels), cleared at the **top of `Simulation.tick()`** so a non-draining golden run never accumulates. `Script_Unit_Fire` emits **per-case faithfully** (sandworm → fixed voiceID 63 `WORMET3P`; bullet path → `ui.bulletSound`), and `Explosion_Func_PlayVoice` emits the explosion VM's voiceID. Host **`duneii`**: new **`VoiceTable`** ports `g_table_voiceMapping`→`g_table_voices` for the combat **`+` effects** (gun/cannon/rocket/explosion VOCs), registered under `SoundID(voiceID)` at load; `GameModel.advance()` drains `sim.state.soundEvents` and plays each via `EngineAudioSink`. UI feedback `SoundID`s moved to 1000/1001 so they never collide with game voice ids 0–119. `+3` tests (fire emits `bulletSound`, structure-explosion `playVoice` emits, `emitSound` bounds). **463 tests**, clean `--full` build, zero warnings. The per-house spoken `%c` voices remain a follow-up. See History.
- **duneii — unit state chip in front of the health bar (2026-05-31).** New Contracts `FrameInfo.UnitActivity` (idle/moving/attacking/guarding/harvesting) on `FrameInfo.Unit`; `makeFrameInfo` collapses the unit's `actionID` via `Simulation.activity(forActionID:)`. `GameScene` draws a 4 px colour chip just ahead of each health bar — green move / red attack / blue guard / orange harvest, none for idle (legend in the Debug panel). `+1` test. **461 tests**. See History.
- **Phase 6 — the `duneii` native macOS client, first version (2026-05-31).** Pivoted the host from **Mac Catalyst → native AppKit + SwiftUI** (docs updated: `Plan.v1.md` §3/§4/§6/§8, `Overview.md`, `CLAUDE.md`). New **`duneii`** executable: a SwiftUI **map window** (pixel-perfect, fills the window, black margins) driven by a `Viewport` — **zoom (+/- keys)**, **scroll (arrow keys)** via the `SKCameraNode` (scale `1/zoom`, position = centre) over `SpriteKitRenderer`; **click** select / right-click move-or-attack (enemy via the player house) / Esc deselect, reusing `DuneIIInput`'s `InputController` + `UnitOrders`; `EngineAudioSink` feedback. **Four floating tool windows** as AppKit `NSPanel`s (`.floating` + `.fullScreenAuxiliary`, so they stay **over the fullscreened map**), managed by `ToolWindowManager` + toggled from the map toolbar (which also has the load-scenario menu): **Minimap** (downscaled terrain + live unit dots + the viewport rectangle; **click recentres** the map), **Selection** (the inspector: type/house/HP/tile + Move/Attack/Stop), **Economy** (per-house credits/power/storage, all or only-mine), **Debug** (toggle fog · all-economies · per-unit health bars). Player house = the scenario's **`Brain=Human`** house (fallback: first allocated). `GameModel` (`@Observable`) is the shared state; derived panel info is republished only on change (no 60 Hz SwiftUI churn), `lastFrame` is observed for the live minimap. Builds clean, **460 tests**. Visual confirm is the user's `swift run duneii`; `mapview` stays as the simple single-window viewer. See History.
- **Phase 7 — basic audio: low-latency, polyphonic playback (2026-05-31).** Multiple sounds play **at once** and **exactly when called**. New Contracts **`SoundEvent`** + **`SoundID`** (the sim→audio seam, finally defined). New **`DuneIIAudio`**: `AudioSink` protocol (`@MainActor`) + `NullAudio` (no-op) + **`EngineAudioSink`** (`AVAudioEngine`) — sounds are **pre-decoded** into `AVAudioPCMBuffer`s (all resampled to one canonical float32-mono rate via manual linear interpolation, so any buffer plays on any node) and a **pool of 24 player nodes** is kept running, so `play()` is a single `scheduleBuffer(at: nil, options: .interrupts)`: it starts at the next render quantum (no perceptible delay) and **mixes** with other voices (round-robin; steals the oldest only past 24 overlaps). Degrades to silent on a box with no audio device (`start()` fails quietly). **`mapview` wires it:** decode install VOCs (`CLICK.VOC` select, `AFFIRM.VOC` acknowledge), register + start on launch, and play instantly on select/order — rapid actions overlap into audible mixing. `+4` tests (`AudioTests`: the 8-bit→float `Pcm8`, `makeBuffer` same-rate + resample, `NullAudio`, graceful no-device). **460 tests**, clean `--full` build, zero warnings. Audible confirm is the user's `swift run mapview`. See History.
- **Phase 5 — player input (first version) + a selection inspector panel (2026-05-31).** The verification UI is now interactive. New **`DuneIIInput`** (Contracts-only): `InputSource` protocol + `ScriptedInput` (fixed command list) + `Selection` (`.unit`/`.structure` by pool slot — the sim has no "selected" concept) + **`InputController`**, the pure, unit-tested selection/order state machine. Contracts gained **`Command.stop(unit:)`** + `displayName` on `UnitType`/`StructureType`/`HouseID`; `UnitOrders` gained the stop path (clear targets/route → GUARD). **`mapview` is now interactive:** `MapScene` handles **mouse** (left-click selects the unit/building under the cursor via `unitGetByPackedTile`/`structureGetByPackedTile`; right-click orders the selected unit — attack an enemy tile, else move) + **keyboard** (Esc deselect, S stop, A arm-attack), applies the queued `Command`s through `UnitOrders` each tick, outlines the selection (an `SKShapeNode` over its footprint), drops a dead selection, and publishes the live `SelectionInfo`. New **`InspectorView`** (a SwiftUI `.inspector` panel): the selected unit/building's type / house / HP bar / tile + **Move / Attack / Stop** command buttons (Move/Attack arm a pending order that the next map click targets). `InputControllerTests` (5) + a `UnitOrders` stop test. **456 tests**, clean `--full` build, zero warnings. Visual confirm is the user's `swift run mapview` (the GUI SIGSEGVs when launched detached from this harness). See History.
- **Sim — built concrete slabs ARE destructible (correcting a wrong claim) (2026-05-31).** A prior entry said "1.07 has no slab destruction" — **wrong.** `Explosion_Func_TileDamage` (`explosion.c:49`) reverts a concrete slab to the base landscape tile, just like a wall. It lives in the **GUI-clocked explosion VM** (the `TILE_DAMAGE` command, only on the larger `IMPACT_LARGE`/`IMPACT_EXPLODE` explosions — a tank's `IMPACT_SMALL` has none), which the golden harness gates off, so the slab survived only because explosions weren't ticked. Ported the slab/structure/destroyed-wall/veiled handling into `GameState.explosionTileDamage` (replacing the SEAM mislabelled "cosmetic"); the crater overlay (needs the full landscape classification + draws `Random_256`) stays a documented SEAM. Verified RNG-free by **`ExplosionTests`** (direct + via the real `IMPACT_EXPLODE` command sequence). A cross-engine *destruction* scenario golden is impractical (no accurate `TILE_DAMAGE` weapon; the harness fires once; rockets scatter/overshoot the exact tile), so **`slab-indestructible` was reframed**: a new oracle `[BASIC] TickExplosions=1` flag ticks `Explosion_Tick` in the scenario loop, and the golden proves a tank's `IMPACT_SMALL` carries no `TILE_DAMAGE` (slab survives even with explosions ticking). Oracle rebuilt + re-signed. **450 tests**, clean `--full` build. See History.
- **Parity — wall-destruction + slab-indestructible scenario goldens + a map-tile dump (2026-05-31).** Closed the wall-destruction SEAM and added two cross-engine **map-tile** goldens. Ported **`Map_UpdateWall`** (`GameState+WallSlab.swift`: revert the wall's ground to the seed base tile, set the destroyed-wall overlay marker when unveiled, re-connect neighbours) + wired **`Map_MakeExplosion`'s wall branch** into `mapMakeExplosion` (`UnitImpact.swift`): a wall tile is destroyed if blast HP ≥ the wall's HP (deterministic, the `||` draws no RNG) else a `Random_256` roll. New **tile-dump infrastructure** (walls/slabs aren't in the structure find-array, so they need map-tile comparison): the oracle `parity.c` gains a **`[DUMPTILES]`** section + a per-tick `"tiles":[{packed,ground,overlay,lst}]` dump; `ScenarioGoldenTests` decodes + compares each cell's `groundTileID`. **`wall-destruction`** (seed 42): a tank's 25-dmg bullet's roll destroys the 50-HP wall at tick 67 (ground reverts 34→base terrain) — and it **rides the RNG-stream golden** (our destroy draw lands at the oracle's stream position). **`slab-indestructible`**: a tank fires at a concrete slab. Oracle rebuilt + re-signed. **11 scenario goldens, 448 tests**, clean `--full` build. *(Correction, same day: built concrete slabs **are** destructible — see the top "Recently completed" entry; this golden was reframed.)* Insight `world-fog-veil-overlay-off-by-one` (the tile golden compares ground, not the fog-veil overlay — a latent gameplay-neutral off-by-one). See History.
- **Phase 4 — sandworm shimmer (`DRAWSPRITE_FLAG_BLUR`) as a CoreGraphics effect — the last render gap is closed (2026-05-31).** A `blurTile` unit isn't a sprite draw: OpenDUNE writes `*buf = buf[blurOffset]` for each opaque worm pixel (`gui.c:1289`), so within the worm's silhouette the terrain is **displaced horizontally** by a cycling 1–5 px (`blurOffsets {1,3,2,5,4,3,2,1}`) — an animated heat-haze. `makeFrameInfo` now emits sandworms to a **new `FrameInfo.blurs`** (silhouette sprite + position, kept out of `units`); **new `ShimmerEffect.patch`** (CoreGraphics) builds a worm-sized `CGImage` sampling the colorized terrain `offset` columns to the right within the worm mask (transparent outside); `SpriteKitRenderer` lays it on a blur layer above the terrain and advances the offset each frame. `+3` tests (`ShimmerEffectTests` displacement/offsets, `FrameInfoTests` `sandwormBlur`) + a `scena001-worm-t0` render-golden; `rendercap`/`RenderHarness` gained a `--worm`/`worm:` inject. Subtle in a still (the original shimmers in motion) — confirmed it alters the rendered pixels (worm vs no-worm differ). **448 tests**, clean `--full` build, zero warnings. **The renderer's world content has no outstanding gaps.** See History.
- **Phase 4 — partial fog-of-war soft edges (closes the documented gap) (2026-05-31).** The renderer now draws the original's dithered fog frontier instead of a hard tile-aligned veil. The sim keeps fog **binary** (`isUnveiled` per tile — parity untouched); `makeFrameInfo` *derives* the soft edge: for each revealed tile it computes the 4-neighbour veil bitmask (`Simulation.fogEdgeMask`, mirroring `Map_UnveilTile_Neighbour` `map.c:1293`; bit 0 = N, 1 = E, 2 = S, 3 = W) and picks the fog-edge sprite `TileIDs.fogEdges[mask]` (the FOG_OF_WAR icon-group tiles 0–15, now hoisted into `GameState` like `tileIDs.veiled`). It rides in a **new, separate `FrameInfo.Tile.fogEdgeSpriteIndex`** — separate from `overlaySpriteIndex` because that slot carries walls (always shown) while fog edges are gated by the renderer's `showFog` toggle. `FrameComposer.cell` composites the fog edge transparently over the ground (the overlay-transparency path) only when `showFog` is on; a fully-veiled tile stays a solid black square. `+3` tests (`FrameInfoTests` `fogEdgeMask`/`fogEdges`, `FrameComposerTests` `fogEdgeComposition`); the `scena001-base-fog-t60` render-golden re-recorded to the dithered edge. **445 tests**, clean `--full` build, zero warnings. **Only the sandworm shimmer (`DRAWSPRITE_FLAG_BLUR`) remains as a render gap.** See History.
- **Phase 4 — faithful tile-overlay transparency + a multi-structure render-golden (2026-05-31).** `FrameComposer.cell` now composites a non-veil overlay (a wall — `overlayTileID == g_wallTileID`) **over** the ground with **index-0 transparency** (only its non-zero pixels overwrite), matching `GFX_DrawTile` (`gfx.c:210`), instead of opaque-replacing the cell — so the ground shows through a wall's transparent pixels (it was a no-op where overlay tiles are fully opaque, e.g. walls on concrete, but is the correct/faithful path and the one the deferred fog edges will ride on). Reworked the `cellComposition` test (a half-transparent wall tile shows ground through its lower half). Added render-golden case **`scena005-base-t40`** — a richer multi-structure enemy base (house-recoloured Harkonnen vs the Atreides player, concrete slabs, walls, infantry + vehicles), the renderer's most complex visual content, now a pixel-exact regression lock. Insight `render-tile-overlay-transparency`; sharpened the partial-fog deferral in `Renderer.md` (walls + fog share the one `overlayTileID` slot, so faithful fog edges need a **separate `FrameInfo` fog field** + the fog icon-group ids in `GameState`, gated by `showFog`). **442 tests** (4 render-golden cases), clean `--full` build, zero warnings. See History.
- **Phase 4 — render-golden harness (`RenderGoldenTests`) (2026-05-31).** The Phase-4 DoD *"diff a rendered frame pixel-exact against a reference PNG"* is now an automated test. `RenderGoldenTests` runs a scenario to a tick through the **real** `Simulation`, captures a tile-space region through the **real** `SpriteKitRenderer.snapshot`, and diffs it byte-for-byte against a committed reference (`Tests/RenderGoldenTests/Fixtures/<name>.png`). One capture path produces both the reference (`DUNEII_RENDER_RECORD=1` → write) and the run-time image (→ diff), so they match exactly; `Scripts/gen-render-goldens.sh` records. New `PngImage` (RGBA8 decode/encode/diff with first-mismatch + max-channel-Δ reporting) + `RenderHarness` (install loader mirroring `rendercap`). 3 SCENA001 base-crop cases (t0 / t60 / fog) — verified visually (Const Yard + trikes + soldiers + spice; the fog case shows the binary fog-reveal circles). **Capture now runs *and diffs* in this sandbox** (the updated sandboxing grants an off-screen GPU context — `snapshot` returns a real texture under `swift test`, where it previously returned `nil`); short-circuits (passes) when the install or GPU is absent. **Fixed a real `snapshot` crop bug:** `SKView.texture(from:)` rasterizes at the host backing scale (2× on Retina), so the CGImage is larger than the logical world; the crop rect (logical points) is now scaled to pixels before `cropping(to:)` — previously a cropped capture grabbed the wrong region at half size (this also fixes `rendercap --rect`). `+1` test (the parameterized suite), **442 total**, clean `--full` build, zero warnings. See History.
- **Phase 4 — headless render capture (`SpriteKitRenderer.snapshot`) (2026-05-31).** A headless run can now **pause at any tick and capture any part of the map as an image**, using the **real renderer** (not a parallel rasterizer): `SpriteKitRenderer.snapshot(_ frame:, crop:)` renders a `FrameInfo` into an off-screen `SKScene` and rasterizes it (`SKView.texture(from:)` → `CGImage`), cropped to an image-space rect. Intended flow: keep a `NullRenderer` for the run, swap in a dedicated `SpriteKitRenderer` only for the capture (a node has one parent, so a capture renderer isn't also `attach`ed to a live scene), then back. **GPU-backed** — runs under `swift test` / a tool on a real Mac (returns `nil` on a no-GPU box like this sandbox); the per-pixel composition it captures is already covered headlessly by `FrameComposer`/`FrameInfo` tests. Also extracted **`DecodedSpriteSource`** (reusable value-type `WorldSpriteSource` over decoded `ICON.ICN` + UNITS SHPs via `GlobalSprite`); `mapview`'s `MapSpriteSource` now just adapts its `AssetStore` onto it (dedup). `+1` test, **441 total**, clean `--full` build. **Next (the auto-test payoff):** a render-golden harness — load the install, run a scenario to chosen ticks, `snapshot` a region, diff vs a committed reference PNG; reference capture runs on a Mac (a `gen-` script, like the scenario goldens). See History.
- **Phase 4 — tile-overlay + fog-of-war compositing (2026-05-30).** The renderer now composites the **overlay tile layer** (was carried but ignored): `FrameComposer.cell` draws ground → an opaque non-veil overlay tile (walls) on top → or a **black veil** (colour 12, `viewport.c:390`) for a cell whose `overlaySpriteIndex == FrameInfo.veiledTileIndex` (the new `g_veiledTileID` field, set by `makeFrameInfo`). **Fog is a `SpriteKitRenderer.showFog` toggle, default off** — the verification UI shows the whole landscape (OpenDUNE's debug-scenario view); flipping it on blacks out veiled cells so the sim's fog-reveal can be verified. `mapview` gained a **Fog toolbar toggle** (`MapScene.showFog` → `renderer.rebuildTerrain`). The renderer's per-cell identity + tile-texture cache now key on `(ground, overlay, house, wind, fog)`; the dynamic-terrain diff tracks the full `(ground, overlay, house)` appearance. **Documented limitation:** our fog model is binary (veiled vs clear) — no partial-fog edge sprites. `+3` tests (`FrameComposerTests` cell + fog buffer; `FrameInfoTests` veiledTileIndex). **440 tests, clean `--full` build, zero warnings.** (Adding `veiledTileIndex` to the `FrameInfo` Contracts struct tripped the stale-`swiftmodule` layout trap again — incremental test run failed on misaligned fields; clean `--full` build green. Insight `build-gamestate-layout-stale-module`.) See History.
- **Phase 4 — harvester overlay + air-unit z-order + carryall-drop phantom (2026-05-30).** Three render/seam fixes from the user's latest mapview report. **(A) Harvester "harvesting" overlay (closes reported #3):** `UnitSprites.info` now resolves the `viewport.c:546` third layer (sprite `(spriteOffset % 3) + 0xDF + values_32A4[o8].offset*3`, offset by `values_334E[o8]`, body H-flip, **house-neutral** — the `GetSprite_HousePalette` call is commented out in OpenDUNE), gated on `type == harvester && actionID == HARVEST && spriteOffset >= 0` **and on a spice tile**. It threads through a new `UnitSpriteInfo.overlay` → `FrameInfo.Unit.overlay` → `FrameComposer` (new `ZOrder.overlay`, between body and turret). `makeFrameInfo` resolves the spice gate via `mapPrimitives.landscapeType` and passes `onSpice:`. The frame animates because `tickUnknown5` already advances a harvesting harvester's `spriteOffset` (ungated). **(B) Air-unit render order:** wingers (carryall/ornithopter/frigate/missiles) were interleaved with ground units at the same z; they now z-order **above** ground units + explosions (`FrameInfo.Unit.isAirUnit` → `FrameComposer.ZOrder.airBody/airTurret`), matching `viewport.c`'s separate air pass drawn last. **(C) Carryall-drop phantom:** `makeFrameInfo` drew every `.used` unit incl. `isNotOnMap` (in-transport / inside a structure / off-map) ones — so a picked-up or carried harvester showed frozen at its stale position before the carryall delivered it; now skipped (`viewport.c` never draws hidden units). `+6` tests (`UnitSpriteTests` harvesting overlay; `FrameComposerTests` air z-order + overlay; `FrameInfoTests` hidden-unit skip + air flag + overlay). **437 tests, clean `--full` build, zero warnings.** Visual confirm is the user's `swift run mapview`. See History.
- **Phase 4 — air-unit (winger) sprite drawing fixed (2026-05-30).** Rockets/aircraft flying **south** were drawn pointing the wrong way (reported #6). Root cause: `UnitSprites.info` ran *every* unit through `viewport.c`'s **ground** pass (`values_32A4`, horizontal-mirror W half), but air units (`movementType == .winger`: carryall, ornithopter, frigate, the missiles) are drawn by a **separate AIR pass** with their own tables where **southern facings flip VERTICALLY** — `values_32E4` (air UNIT, 8-dir), `values_3304` (air ROCKET, **16**-orientation via `Orientation.to16`), `values_33AE` (ornithopter rotor), plus the `hasAnimationSet` wing-beat (+5) and carryall-`inTransport` (+3). Proven against the **OpenDUNE bitmap draw tool** (`parity.c --parity-draw`, re-edited to branch ground/air): a south rocket = the **N frame + vertical flip** = points down. Threaded a `flippedV` mirror across the seam — `Contracts/SpriteLayer.flippedV` → `UnitSprites` air branch → `FrameComposer.ComposedSprite.flippedV` → `SpriteKitRenderer` (generalized `mirror(horizontal:vertical:)` baked into the cached texture; `SpriteKey` keyed on both flips). `UnitSpriteTests.everyUnitEightDirections` now re-derives **both** passes for every type×8 dirs; `rocketDirections` asserts south = N-frame + V-flip. 431 tests, clean `--full` build. (Visual confirm is the user's `swift run mapview` — the GUI SIGSEGVs when launched detached from this harness.) See History. **#6 now CLOSED; only #3 harvester-overlay remains open.**
- **Phase 4 — reported mapview-bug pass (2026-05-30).** Six visual issues triaged against OpenDUNE: **(#5) destroyed buildings stayed on the map** — `structureRemove` never reset `groundTileID`; now starts the destruction animation (`Animation_Start(g_table_animation_structure[0])`, `structure.c:1305`) so the building collapses (headless stays oracle-faithful — animations aren't ticked there; visual apps play it). **(#4) windtrap light started purple** — the animated palette indices (223/255/239) were the raw IBM.PAL magenta placeholders; `PaletteAnimator.seedAnimatedColours` seeds them to their `GUI_PaletteAnimate` references at renderer init. **(#1 carryall tile-by-tile + #2 rotation one-way)** — **diagnosed sim-faithful** (new `CarryallTests`: sub-tile per-step motion + shortest-path rotation both ways); render-side. **Still open (render-side): (#3)** harvester "harvesting" overlay not drawn (`UnitSprites` lacks the `viewport.c:546` overlay layer at sprite base `0xDF` + the `actionID==HARVEST` gate). **(#6) rocket orientation — now FIXED** (see the air-unit entry above: air units use `viewport.c`'s separate AIR pass with vertical flips, not the ground table). Commits `3401c0e`/`23c7c12`/`8093709`. 422 tests.
- **Phase 4 — renderer perf fix (2026-05-30).** mapview ran slower the longer it played: `PaletteAnimator.animatedPalette` replayed ticks 0→N **every frame** (O(tick), so O(tick²) over a session) and `SpriteKitRenderer` recolourised the full 1024×1024 terrain **every frame** unconditionally. Added an incremental `PaletteAnimator.stepTick`/`CycleState` (O(1)/tick; `animatedPalette` now shares it) — the renderer advances the cycling palette one tick at a time — and gated terrain recolour on its **tiles or the wind-trap colour (223) actually changing**. `+1 RendererTests` proving `stepTick` ≡ `animatedPalette` at every checkpoint. 419 tests, clean `--full` build. See History.
- **Phase 4 — the `SpriteKitRenderer` visual leaf + `mapview` migration (2026-05-30).** `DuneIIRenderer/SpriteKitRenderer.swift` (`@MainActor`) colorizes the `FrameComposer` output into a camera SpriteKit scene (terrain node recomposited on tile change/recoloured on palette tick; per-frame house-recoloured sprite nodes). `mapview`'s `MapScene` now drives it from `Simulation.makeFrameInfo()` (deleted `MapImageBuilder`; new `MapSpriteSource: WorldSpriteSource` over `AssetStore`) — the seam proven end-to-end on real `SCEN*.INI`. Doc `Architecture/Renderer.md`. No new tests (visual leaf; composer logic already covered). 418 tests, clean `--full` build; mapview launches + renders (sandbox blocks the screenshot). See History. **Next: visually verify + dedupe `scenariolab`; then Phase 5 `DuneIIInput`.**
- **Phase 4 — the renderer's `FrameInfo`-consumer core (2026-05-30).** `DuneIIRenderer` (headless-testable): `Renderer` protocol + `NullRenderer` (records last frame); `GlobalSprite` (canonical `Sprites_Init` global-index→sheet+frame mapping, deduping the apps); `WorldSpriteSource` + `SpriteFrame` (asset abstraction); `FrameComposer` (pure terrain-buffer + unit/effect `ComposedSprite` compositing from a `FrameInfo`). Colorization deferred to the SpriteKit leaf. Doc `Architecture/Renderer.md`. `FrameComposerTests` +5. 418 tests, clean `--full` build. See History. **Next: the `SpriteKitRenderer` visual leaf + host it in an app.**
- **Phase 4 — the `FrameInfo` sim→render seam (2026-05-30).** Defined `DuneIIContracts/FrameInfo.swift` + `SpriteLayer.swift` (the presentation-neutral per-frame snapshot: terrain tile grid, units with `UnitSprites`-resolved body/turret layers + effective house + hp/smoke, structures, transient effects = explosions + smoke as global sprite indices, per-house economy, viewport; positions in world sub-tile units, sprites as global indices) + `Simulation.makeFrameInfo()` (`DuneIISimulation/FrameInfo+Make.swift`) building it from `GameState`. `UnitSpriteLayer` → typealias of the canonical Contracts `SpriteLayer`. Design doc `Architecture/FrameInfo.md`. `FrameInfoTests` +9. 413 tests, clean `--full` build. See History. **Next: the `Renderer` protocol + `SpriteKitRenderer`.**
- **Phase-3 final-stretch behavioral slices 1–6 (2026-05-30).** A six-slice sweep closing the bounded behavioral to-do (see "Finish Phase 3" above): **(1)** verified carryall winger flight end-to-end (`CarryallTests.wingerFlight`); **(3)** wired the area-effect combat seams in `Unit_Move`/`Unit_Damage` — sonic-blast area damage, the death-hand 17-point blast, saboteur detonation, the `range!=0` crater, sand-burst (`AreaEffectCombatTests` +6); **(4)** deviator gas + spice bloom — `Map_DeviateArea` + the relocated `Unit_Deviate` (now on `UnitMovement`, `UnitCombat` delegates), `Map_FillCircleWithSpice`, `Map_Bloom_ExplodeSpice` + harvester-death spice (`MapEffectTests` +5); **(5)** the `GameLoop_House` tails — `Map_FindLocationTile`, `Unit_CreateWrapper`, `House_EnsureHarvesterAvailable` + harvester-incoming spawn, starport stock bump, frigate delivery, the `playerCreditsNoSilo` reset; added `GameState.starportAvailable[27]`+`minimapPosition` (`GameLoopHouseTailsTests` +7); **(6)** `Structure_BuildObject` headless state-setup + `Structure_CancelBuild` (`BuildObjectTests` +6); **(2)** team AI exercised end-to-end through the live loop (`TeamLoopTests.endToEndTeamAI`) — the cross-engine `team` golden is **deferred** (stochastic AI, see "Finish Phase 3"). 401 tests, clean `--full` build, scenario goldens unchanged. See History.
- **Finished the last 3 unit SEAMs — the `g_scriptFunctionsUnit` table is now 64/64; all three EMC native tables are complete.** `RandomSoldier` (0x21 — `spawnChance`-gated `SOLDIER` spray), `CallUnitByType` (0x23 — summon a same-house carryall, reusing `Unit_CallUnitByType`), and **`MCVDeploy`** (0x09) via a new **structure-placement chain** on `UnitCombat`: `structureCreate` (`structure.c:373`) + `structurePlace` (`:442`, general-building case — the `BUILD.EMC` script-load is **deferred to `GameLoop_Structure`'s auto-load**, sidestepping the layering issue) + `structureIsValidBuildLocation` (`:734` — footprint `isValidForStructure`/`2` + occupancy; structures build on **rock**, not sand). Seams: `Structure_BuildObject` (GUI build-menu), the wall/slab placement specials, fog reveal. Marker retargeted to a **non-native `0x40`** (stable — past the 64-entry table). `+5 MCVDeployTests`. 375 tests. See History.
- **Sandworm + link-validity natives — `Sandworm_GetBestTarget` 0x36, `Unknown2BD5` 0x37, `Unknown0288` 0x3B.** `0x36` → new `TargetFinder.sandwormFindBestTarget` + `sandwormGetTargetPriority` (`unit.c:1020`/`987`: prey on revealed **sand**, weighted by `movementType`/motion/distance); `0x37` validates the unit's `variables[4]` link (clears it unless mutual + same-house); `0x3B` reports whether an encoded index is a stale/empty reference. Marker retarget `0x37`→`0x09` (`MCVDeploy`, blocked on `Structure_Create`). `+3 SandwormNativesTests`. 370 tests. **Unit native table ~61/64 live** — remaining: `09 MCVDeploy` (needs the GUI-heavy `Structure_Create`), `21 RandomSoldier`, `23 CallUnitByType` (unit variant). See History.
- **Carryall transport natives — `MoveToStructure` 0x1E, `Pickup` 0x22, `TransportDeliver` 0x14 (all on `UnitCombat`).** A summoned carryall can now link to a structure, pick a unit up (from a READY factory or off the ground — routing it to its refinery/repair pad, with the harvester spice-field bookkeeping), and deliver it (starport busy→ready / refinery+repair via `Unit_EnterStructure` / ground via `Unit_SetPosition`). Pairs with the already-wired `FindUnitByType` summon. Marker retarget 0x14→0x37 in `UnitScriptRunnerTests`. `+5 CarryallTests`. 367 tests. **Unit native table: ~58/64 live**; remaining SEAMs: `09 MCVDeploy` (needs `Structure_Create`), `21 RandomSoldier`, `23 CallUnitByType` (unit variant), `36 Sandworm_GetBestTarget`, `37 Unknown2BD5`, `3B Unknown0288`. See History.
- **More unit-script natives — `ExplosionMultiple` 0x12, `FindIdle` 0x18, `GetTargetPriority` 0x1D, `IsValidDestination` 0x2F.** `ExplosionMultiple` (`UnitMovement`, the death-hand 8-blast: one at the unit + 7 random-offset, LCG damage); `FindIdle` (`UnitScriptFunctions`, an idle structure of a type / a given encoded structure for the house); `GetTargetPriority` (`TargetFinder.targetPriority`, the priority of an encoded unit/structure target); `IsValidDestination` (`UnitCombat`, can the carrier head to a tile/structure — temp-relocates its passenger to test occupancy). Retargeted the `UnitScriptRunnerTests` "unported native" marker (0x12→0x14) — porting the marker opcode broke + crashed those tests (insight `sim-unported-native-marker-test`). `+4 UnitNativesTests`. 362 tests. See History + the coverage table.
- **Common unit-script natives — `MoveToTarget` 0x16, `SetActionDefault` 0x0A, `SetSprite` 0x13, `Blink` 0x0B, `DisplayText` 0x02.** `MoveToTarget` (`UnitMovement`, `unit.c:427`) is the fine-approach homing onto `targetMove`: **far** (≥128 sub-units) aims + sets a distance/turn-scaled speed, **close** (<128) stops + steps ±16/axis, arrived (<32) returns 1, else re-runs via a **script-PC rewind** (`engine.scriptPC -= 1`) + a short `delay`. `SetActionDefault` → the type's `actionsPlayer[3]`; `SetSprite` sets the **negative** `spriteOffset` (death frames, `-(value & 0xFF)`); `Blink` sets `blinkCounter`; `DisplayText` is a GUI no-op. `+7 UnitNativesTests`. 358 tests. See History + the unit-native coverage table.
- **Harvester loop natives (`Harvest` 0x2A / `SearchSpice` 0x29 / `GoToClosestStructure` 0x33).** A harvester now gathers: `Script_Unit_Harvest` (gather a random 0/1 of spice on a spice tile, flag `inTransport`, ~1/32 deplete the tile — 2 `Random256` draws), `Script_General_SearchSpice` (nearest harvestable spice within `radius` via `Map_SearchSpice` → encoded tile), `Script_Unit_GoToClosestStructure` (Move to the nearest idle/unlinked/un-busy structure of a type — a harvester → an empty refinery). `harvest`/`searchSpice` on `UnitMovement` (has `map`); `goToClosestStructure` on `UnitScriptFunctions` (takes the action layer + live `engine`). Closes the harvester's own side of the harvester→refinery→`RefineSpice`→credits loop. `+6 HarvesterTests`. 351 tests. See History.
- **Structure production triggers — `Structure_SetRepairingState` + `Structure_SetUpgradingState`.** Ported the two setters (`GameState+Lifecycle`, `structure.c:1735`/`:1691`) that start/stop/toggle (`state` 1/0/−1) a structure's self-repair / upgrade by flipping `.repairing`/`.upgrading`/`.onHold` — the flags the `tickStructure` repair/upgrade branches already act on. Faithful: repair needs allocated + below-max HP; upgrade needs `upgradeTimeLeft != 0` and supersedes repair; the `Widget`/`GUI_DisplayText` feedback is a seam and `g_dune2_enhanced` is pinned false (1.07). Refactored the `repairBuilding`/`upgradeBuilding` `ScenarioKind`s to call the real setters instead of inserting flags by hand. (`Structure_BuildObject` itself stays a SEAM — it's the GUI build-menu handler: factory window + `available` flags + starport; not a clean headless port.) `+8 StructureStateTests`. 345 tests. See History.
- **Team creation — `Team_Create` + the `[TEAMS]` scenario section.** `GameState.teamCreate(houseID:teamActionType:movementType:minMembers:maxMembers:scriptPC:)` (`GameState+Pools`, port of `team.c:75`): allocate + set identity/`action`(==`actionStart`)/movement/membership bounds + `Script_Reset`/load to a passed-in `scriptPC` + `delay=0`. World-layer, so the **caller resolves `offsets[teamActionType]`** (the team script program is Simulation-side); `scriptNull` ⇒ inert. The PC is set once at scenario-load and persists in `state.teams` (must **not** be reloaded in the per-tick `Simulation.init`). `ScenarioLoader.loadScenario` gains a `teamScriptOffsets` param + parses `[TEAMS]` (`<House>,<TeamAction>,<MovementType>,<min>,<max>`) via new `MovementType.named`/`TeamActionType.named` parsers (`g_table_movementTypeName`/`teamActionName`; INI "Winged"→`.winger`). **The whole team subsystem (loop + every native + creation) is now ported**; remaining is wiring `TEAM.EMC` + offsets + an AI-active house into the lab/golden so teams run end-to-end. `+4 TeamCreateTests`. 337 tests. See History + `Algorithms/TeamScript.md`.
- **Verified movement start-speed ← terrain + scenariolab finish-grace.** Confirmed (code review + a new test + the existing goldens) that `Unit_StartMovement` (`UnitMovement.startMovement`) takes terrain into account **at the start of each step** (incl. the first), not only mid-move: it reads the landscape of the **tile being entered** (`Tile_MoveByOrientation` → `landscapeType` → `LandscapeInfo.movementSpeed[movementType]`, minus the half-HP −25%). New `MovementTerrainTests` (+2): a swap test proves the **entered** tile — not the tile the unit sits on — drives the start speed (wheeled sand 160 > rock 64); the `moving`/`move-trike`/`trooper` goldens confirm it end-to-end over real `Map_CreateLandscape` terrain. `scenariolab`: after a scenario is detected finished, the sim keeps ticking a **5-second grace period** (`ScenarioScene.finishedAt`/`finishGraceSeconds`) so death/destruction explosions + animations finish, then auto-pauses. 333 tests. See History.
- **Troopers — walk animation + death (reported) + a new oracle golden.** Ported **`tickUnknown5`** (`gameLoopUnit`, `unit.c:239`) — advance `spriteOffset` for a moving **foot** unit (the walk cycle), a smoking unit, the ornithopter rotor, and a harvesting harvester, throttled by `timer = animationSpeed/5`; **RNG-free** (the `Unit_UpdateMap(2)` render redraw is a SEAM) — so infantry now animate (they were stuck at frame 0). Ported the unit DIE-branch natives the **trooper** death path needs: `Script_Unit_StartAnimation` (0x04 — the deterministic state effects + `return 1`; the overlay corpse animation is a render SEAM, consistent with our overlay-not-rendered seam) and `General_VoicePlay` (0x26 — an audio no-op), so a trooper reaches `Unit_Die` and is removed (its branch is `Stop → RandomRange → VoicePlay → StartAnimation → Die`). **New `trooper` scenario golden**: a foot trooper walks 400 ticks, **full match vs the oracle** — and the golden now compares a new **`spriteOffset`** dump field (added to the oracle's `Scen_DumpState` + the `UnitState` struct), verifying the walk animation **tick-for-tick** (`spriteOffset` cycles 0→12 as it walks 2600→2535). All goldens now also compare `spriteOffset` (0 for non-foot units). Oracle rebuilt + all goldens regenerated. `+6 tests` (`TrooperTests` ×2 walk/die, the `trooper` golden, +2 stale `UnitScriptRunnerTests` retargeted to op 0x12). 331 tests. See History.
- **`scenariolab` reported-bug fixes — units now die + correct damage smoke.** The unit **DIE branch** (`Stop → ExplosionSingle → DisplayDestroyedText → Die`) was clean-halting at unported natives, so a 0-HP unit sat in `ACTION_DIE` forever (drawn, never removed — "units don't die"). Ported `Script_Unit_ExplosionSingle` (0x0E — a single explosion at the unit, full-HP blast) + `Script_Unit_Die` (0x0F — `Unit_Remove`, **resets the running engine** so the freed unit's script stops, + the saboteur death blast; the `g_scenario` kill/score tally is a SEAM) in `UnitImpact.swift`, and made `Script_Unit_DisplayDestroyedText` (0x27) a **GUI no-op** (was halting the branch before `Die`). The unit death path now completes everywhere (lab/mapview/headless), with the real death explosion. Also fixed the lab's **damage smoke** to OpenDUNE's draw (`viewport.c:615`: frame `180 + (spriteOffset & 3)` with 183→181, 14px above the unit) — it had a wrong frame source + offset. (Explosion/spark *positioning* is rendered identically to OpenDUNE — centered on the impact via `DRAWSPRITE_FLAG_CENTER` — so it was already faithful; the missing death explosions were the visible gap.) `+4 tests` (`UnitCombatTests` death natives ×3; `ScenarioExplosionTests` end-to-end unit death ×1). 329 tests. See History.
- **Team subsystem — the order-issuing + script-switch brain natives (the `g_scriptFunctionsTeam` table is now COMPLETE).** `moveOrGuardMembers` (0x05, `team.c:196` — keep members massed: a strayed member → Move to a random tile within `distance` of the team centroid, else Guard), `issueAttackOrders` (0x07, `team.c:350` — set each member to Attack the team `target` at a random firing position a fire-range out around it, falling back to the target tile if occupied), and `load`/`load2` (0x08/0x09 — switch the team's action script via `interpreter.load` on the **passed-in live engine**, not `state.teams[slot].script`, so the runner's write-back doesn't clobber the reload) in `TeamScriptFunctions`; wired into `TeamScriptRunner.dispatch`. The two order natives take the unit-action layer (`UnitActions.setAction` + `UnitScriptFunctions.unitSetDestination`/`unitSetTarget`) explicitly, so they clean-halt only when the runner has no `UnitScriptRunner`. `+5 TeamScriptFunctionsTests` (decision-trace). 325 tests. Doc `Algorithms/TeamScript.md`. **Next team slice:** team *creation* (scenario `[TEAMS]` + `Team_Create`/`Team_Add`).
- **Explosion subsystem (port of `src/explosion.c`) + real damage/death visuals in `scenariolab`.** New `DuneIIWorld/Model/Explosion.swift` (the `Explosion` POD, `ExplosionType`/`ExplosionCommand`, all 20 command tables verbatim) + `State/GameState+Explosion.swift` (`explosions` 32-slot pool + `explosionStart`/`explosionTick`/`explosionStopAtPosition`, the command VM: SET_SPRITE/SET_TIMEOUT/SET_RANDOM_TIMEOUT(LCG)/MOVE_Y/STOP live; TILE_DAMAGE/PLAY_VOICE/SCREEN_SHAKE/SET_ANIMATION/BLOOM documented seams). Wired into `mapMakeExplosion` (`UnitImpact.swift`) — `explosionStart` is **RNG-free → golden-neutral** (and matches the oracle, which also starts but never ticks explosions in the scenario harness). The per-tick `explosionTick` **draws RNG**, so it's **gated**: `Simulation(tickExplosions:)` defaults `false` (goldens/oracle path unchanged) and `scenariolab` sets it `true`. `scenariolab` now renders the real impact/death/**building-destruction** explosion sprite animations + a smoke cloud over `.isSmoking` vehicles (`ScenarioImageBuilder.effectSprites`, reusing the already-loaded UNITS SHPs via `globalSprite`); the earlier synthetic burst/scorch markers were removed. The "✓ <label>" **finished banner** + `ScenarioWorld.outcome()` (+`kind`/`structureSlots`) from the prior slice are kept. `+9 tests` (`ExplosionTests` ×5 incl. the structure LCG-draw decision-trace; `ScenarioExplosionTests` ×2 — end-to-end: a destroyed windtrap animates a structure explosion when ticked, stays sprite-0 when gated off; `ScenarioOutcomeTests` ×4 from the prior slice). 320 tests. Docs `Algorithms/Explosion.md` (new) + `Architecture/ScenarioHarness.md` + `DuneIIWorld/CLAUDE.md`. See History.
- **Parity harness — Tier-2a structure decision-trace (opcode-by-opcode vs the oracle).** Wired the oracle's `--parity-script-trace` (per-opcode `pc/op/param/delay/SP/FP/return/current`, `script/script.c`) into the **scenario** mode (`parity.c` — it was only in savegame parity). Our `StructureScriptRunner` emits the identical line via an injected `StructureScriptTracer` + `ScriptTraceLine.decode` (the same opcode/flag decode + pre-execution engine state) — `DuneIISimulation/Script/ScriptTrace.swift`; `Simulation.init` gains a `structureTracer:`. `StructureTraceTests` runs `attack-structure` tracing the windtrap (index 0) and diffs line-by-line: a **24-opcode exact match** — the placement-animation shared subroutine (`FP` 17↔14), `RemoveFog`/`GetState`/`SetState`, settle into `Delay(120)`. This proves the structure EMC runs **opcode-identical** (not just state-identical) and localizes any future divergence to the exact PC. Fixture `attack-structure-struct0-trace.txt`, regenerated by `gen-scenario-goldens.sh`. Oracle rebuilt+re-signed. 309 tests. Docs `Architecture/ScenarioHarness.md`.
- **Parity harness — the house-economy golden (a new `[HOUSES]` scenario section + per-tick house comparison).** Both engines now activate scenario houses: oracle `Scen_LoadHouse` → `House_Allocate` (`parity.c`), ours `loadScenario` → `houseAllocate` (`ScenarioLoader.swift`), each seeding starting credits + `House_CalculatePowerAndCredit` after load. New `economy.ini` (an Ordos windtrap+silo base, no units) → a **full 60-tick** house-aggregate match: `credits` 2000 → `GameLoop_House` clamp-to-storage 1000 → power-maintenance upkeep 999, `powerProduction` 100, `powerUsage` 5, `creditsStorage` 1000 — cross-validating `House_CalculatePowerAndCredit` + the credit clamp + power upkeep against the oracle. `ScenarioGoldenTests` now decodes + compares `houses` (`HouseGolden`: credits/storage/power) per tick; `Spec.cmd` gates the player order (off for the unit-less economy scenario). Scenarios without `[HOUSES]` are unchanged (both house dumps empty), so the unit/combat goldens still match. Oracle rebuilt + re-signed; `gen-scenario-goldens.sh` gains the `economy` entry (60 ticks). 7 golden scenarios. 308 tests. Docs `Architecture/ScenarioHarness.md` + the `ScenarioGoldenTests` header.
- **Team subsystem — the recruit + target brain natives.** `Script_Team_AddClosestUnit` (0x03, recruit the nearest eligible same-house scenario unit — skips saboteurs/wrong-movementType/non-scenario, can poach from under-strength teams), `GetAverageDistance` (0x04, set the team `position` to its members' centroid + return their mean distance, flip `targetTile`→2 near the target), `FindBestTarget` (0x06, a member's `Unit_FindBestTargetEncoded` → team `target` + a `targetTile` in its direction) in `TeamScriptFunctions`. New World primitives: `unitAddToTeam`, `mapIsValidPosition`, `tileGetTileInDirectionOf` (a random valid tile toward the target — draws `Random256`) in `GameState+Lifecycle`. `TeamScriptRunner` now carries the optional `UnitScriptRunner` so `FindBestTarget` reuses its `TargetFinder`. `TeamScriptFunctionsTests` +7 (decision-trace). 308 tests. Doc `Algorithms/TeamScript.md`. **Next team slice:** the **order-issuing** natives `Unknown0543` (0x05, move/guard members) + `Unknown0788` (0x07, attack orders — `Unit_SetAction`/`SetDestination`/`SetTarget`, needs the unit-action layer wired into the team runner) + `Load`/`Load2` (0x08/0x09, switch script), then **team creation** (scenario `[TEAMS]` / `Team_Create`).
- **Test apps — surfaced the `tickStructure` economy.** 3 new `ScenarioKind`s in `DuneIIScenarios` exercise the build/repair/upgrade state machine live: **`factoryProduce`** (a Light Factory builds a Trike → credits drain + countdown → `READY`), **`repairBuilding`** (a damaged windtrap self-repairs, HP climbing), **`upgradeBuilding`** (a barracks upgrades, level 0→1). The builder sets the player's no-silo credit allowance (else the House loop's storage clamp wipes the demo credits on tick 1) + **settles** the structure (a long `script.delay` suspends its BUILD.EMC placement-animation `SetState` sequence, which would otherwise overwrite the production `state`). `duneii-headless` prints each demo's per-run economy progression (verified: factory `countDown 1536→0`, repair `hp 100→150`, upgrade `level 0→1`); `scenariolab` lists them via `allCases` (renders the building); `mapview` already runs the economy on real `.INI` scenarios (it passes `BUILD.EMC`). `ScenarioBuilderTests` +3. 301 tests. See History. **Two non-obvious setup gotchas found:** the no-silo credit clamp wiping demo credits, and the placement-animation script clobbering a manually-set structure `state`.
- **Team subsystem — `GameLoop_Team` goes live (the team-script bring-up).** `gameLoopTeam` (`Simulation.swift`, `team.c:22`) now fires on a single random-period cursor (`GameState.teamLoopTick`, re-arms to `timerGame + (Random256()&7) + 5` = 5–12 ticks; one RNG draw/fire), walks every team, gates on the owning house's `isAIActive`, decrements a suspended team's `script.delay`, and runs **one** opcode of its `TEAM.EMC` script via the new `TeamScriptRunner` (mirrors `StructureScriptRunner` — same VM, `g_scriptFunctionsTeam` dispatch). The 1.07 "a halted team aborts the rest this fire" quirk is reproduced. Live natives: `General_Delay`/`DelayRandom`/`NoOperation` + the getters `Team_GetMembers`/`GetVariable6`/`GetTarget` (new `TeamScriptFunctions`); `DisplayText`/`DisplayModalMessage` are GUI no-ops. **SEAMs (the team "brain", next slices):** `AddClosestUnit`/`GetAverageDistance`/`Unknown0543`/`FindBestTarget`/`Unknown0788`/`Load`/`Load2` (clean-halt) + team *creation* (scenario `[TEAMS]`). `Simulation` gained a `teamScriptInfo` param. No effect on the scenario goldens (no team EMC passed there → the team phase is inert, draws no RNG). `TeamLoopTests` +5. 298 tests. Doc `Algorithms/TeamScript.md`. See History. **Note:** adding `teamScriptInfo` to the `Simulation.init` signature + `teamLoopTick` to `GameState` tripped the same stale-build class as before (a linker miss on the old init mangling) — clean `--full` build is green (insight `build-gamestate-layout-stale-module`).
- **Structures subsystem — upgrade branch + `Structure_IsUpgradable` (`structure.c:1102`): the `tickStructure` body is now COMPLETE.** The `.upgrading` branch in `structureTickStructure` (pay `buildCredits/40`/tick, step `upgradeTimeLeft` by 5, finish → `upgradeLevel++` with the Ordos Heavy-Vehicle 1→3 free jump, re-arm to 100/0 via `Structure_IsUpgradable`); out of money cancels. `structureIsUpgradable` ports the per-house/-type exclusions (Harkonnen Hi-Tech, Ordos Heavy-Vehicle gate, Harkonnen WOR late unlock) + the generic `upgradeCampaign[level] ≤ campaignID+1` test + the construction-yard 2nd-upgrade rocket-turret prerequisite. Only the AI maintenance/auto-place + GUI/sound remain seams in `tickStructure`. `StructureProductionTests` +7. 293 tests. Doc `Algorithms/StructureScript.md`. See History.
- **Structures subsystem — `structureTickStructure`: the `GameLoop_Structure` `tickStructure` body is now LIVE** (was a seam). `structureTickStructure` (`GameState+Lifecycle.swift`, `structure.c:53`'s `if (tickStructure)` block): structure self-repair (1.07 repair cost, +5/+3 heal, finish-at-full), factory production (HP-scaled `buildSpeed`, `buildCost` billing, completion→`READY`, out-of-money `onHold`), and the repair-pad unit-repair countdown. `gameLoopStructure` restructured to run it on the **structure** cursor (every `AdjustToGameSpeed(30,15,60)`) before the script cursor, with the faithful slab/wall skip. New `GameState.campaignID` (default 1 = first campaign) feeds the repair heal + AI build-speed cap exactly (replaces the old "campaigns aren't modeled" pins). **SEAMs:** the upgrade branch, the AI-maintenance block (auto-repair + `Structure_AI_PickNextToBuild`/`BuildObject`), the AI construction-yard auto-place, GUI/sound. No effect on the scenario goldens. `StructureProductionTests` +10 (decision-trace). 286 tests. Doc `Algorithms/StructureScript.md`. See History. **Note:** adding `campaignID` (a `GameState` stored property) tripped a stale-`swiftmodule` incremental-build false failure in `SearchSpiceTests` — a clean `--full` build was green; layout changes to `GameState` warrant a clean build (CLAUDE.md step 5 catches this). **Next:** the **upgrade branch** (`Structure_IsUpgradable`) is the small remaining `tickStructure` piece; then **teams/AI** (`GameLoop_Team`).
- **House subsystem — `Structure_CalculateHitpointsMax` (the tickHouse power→HP seam is now LIVE).** `structureCalculateHitpointsMax` (`GameState+Lifecycle.swift`, `structure.c:623`): power ratio = `256` at full supply (`powerUsage==0`) else `min(powerProduction*256/powerUsage, 256)`; each structure (slabs/walls excluded) gets `hitpointsMax = info.hitpoints * ratio / 256` floored at half its default HP, and bleeds 1 HP (`Structure_Damage(s,1,0)`) when the cap drops below current HP — so under-powered bases decay. Wired into `Simulation.gameLoopHouse` `tickHouse`, right after `houseCalculatePowerAndCredit`. `House_UpdateRadarState` (player radar/GUI) is the one remaining seam. No effect on the scenario goldens. `HouseEconomyTests` +3. 276 tests. See History. **Next House/structure slice:** the **BUILD/REPAIR/factory production state machine** (the `GameLoop_Structure` `tickStructure` cursor body).
- **Test apps — surfaced the structure + economy features.** **scenariolab**: the builder/runner now take `BUILD.EMC` (structures run their scripts) + two new scenario kinds — `attackStructure` (a tank destroys a pre-weakened windtrap → the death branch runs live) and `turretDefense` (a gun-turret acquires/aims/fires). **mapview**: loads both EMCs + allocates houses + Scen-prepares units + runs the scenario LIVE (units/bullets/soldiers follow the sim each tick; a destroyed building disappears). **duneii-headless**: a real deterministic demo driver (runs the two scenarios, prints the events). **rendertest**: asset-only, unchanged. `structureRemove` now sets `mapDirty`. `ScenarioBuilderTests` +2. 273 tests. See History.
- **House subsystem — `GameLoop_House` core: the House phase is now LIVE** (was a stub). `HouseTickCursors` (6 cadences) + `GameState.playerCreditsNoSilo`; `Simulation.gameLoopHouse` runs `tickHouse` (every 900: clamp credits to storage + `House_CalculatePowerAndCredit` recompute + decrement the attack timers) and `tickPowerMaintenance` (every 10800: deduct `powerUsage/32+1`). Starport/reinforcement/missile/harvester-incoming/`Structure_CalculateHitpointsMax`/GUI are seams. No effect on the scenario goldens (houses aren't allocated there). `HouseLoopTests` (4). 271 tests. See History. **Next House slices:** the **BUILD/REPAIR/factory production state machine** (the `GameLoop_Structure` `tickStructure` cursor body — building construction, repair, unit/structure production), and `Structure_CalculateHitpointsMax` (power→structure HP). With houses now allocated in a parity scenario (a small `[HOUSE]`/allocation add to the oracle harness), the per-tick **house credit/power golden** becomes available.
- **House subsystem — started: `House_CalculatePowerAndCredit`** (`house.c:470`, `GameState+Lifecycle`). Sums a house's power usage/production + credit storage over its structures (consumers add `powerUsage`; power plants produce `-powerUsage`, hitpoint-scaled when damaged; storage adds `creditsStorage`). **Closed the two `Unit_EnterStructure` takeover SEAMs.** `HouseEconomyTests` (3). 267 tests. See History.
- **Tooling — extracted recurring investigation commands** (`Scripts/odfn.sh` = print an OpenDUNE C function; `Scripts/emc.sh` = disassemble a script type / shared region via `--linear`; `Scripts/golden.sh` = a golden's per-tick timeline) + a permanent `assetgen emc-disasm --linear` mode; refined `gen-scenario-goldens.sh` (DRY via `build-oracle.sh` + `--only <name>`). See History / CLAUDE.md "Running things".
- **Parity harness — structures + houses in the scenario golden, + a real bug found & fixed.** Extended the oracle scenario mode (`parity.c`): `Scen_DumpState` dumps `{tick,houses,structures,units}`; `Scen_LoadStructure` + `[STRUCTURES]` loading + `BUILD.EMC`. New `attack-structure` scenario (tank vs Ordos windtrap) — **full 400-tick match** (units + structures, inc. the bullet-impact `Structure_Damage` 200→175). The golden immediately caught a real bug: **structures store the tile corner, not the centred sub-tile** (`Structure_Place: position &= 0xFF00`) — `loadStructure` mispositioned every structure 128px, shifting unit-vs-structure aim. Fixed; insight `world-structure-corner-position`. **Even-better harness recorded** (`ScenarioHarness.md`): Tier-2a structure decision-trace (oracle per-opcode hooks already exist) → Tier-3i event-trace. `ScenarioGoldenTests` now 6 scenarios. 264 tests. See History.
- **Structures subsystem — `FindUnitByType` (0x03) + `Unit_CallUnitByType`: the structure script native table is COMPLETE.** Every native `BUILD.EMC` calls is ported → a structure runs its full script (no clean-halts); the refinery cycle runs end to end. `Unit_CallUnitByType` (`UnitCombat`) finds the last idle carryall (or spawns an off-map scenario one) and orders it to a target. `StructureScriptTests` (+3). 264 tests. See History.
- **Structures subsystem — `Unit_EnterStructure` (+ `Unit_Hide` + `Structure_GetStructuresBuilt`): harvester economy loop CLOSED.** A unit arriving inside a structure: allied → READY/BUSY + link the hidden unit (repair heals/times it); saboteur → detonate + remove; enemy → capture low-HP structure (houseID swap + structures-built rebuild) else damage. All `GameState`/World; **wired into the `Unit_Move` SEAM** so arrival enters the structure. The harvester→refinery→`RefineSpice`→credits loop now runs end-to-end (integration test). Seams: `g_unitSelected` deselect, `House_CalculatePowerAndCredit`. `StructureScriptTests` (+4). 261 tests. See History.
- **Structures subsystem — deploy cluster (`Unit_SetPosition` + `Structure_FindFreePosition` + unit-unload `Unknown0C5A` 0x07).** A structure deploys its linked unit to a free adjacent tile (or a winger lifts off its own tile), unlinks it, shifts the next queued unit in (or SetState IDLE), clears var-4. `Unit_SetPosition` (`UnitCombat`) places a unit centred + default-action + map-stamp (fails if occupied); `Structure_FindFreePosition` finds a free ring tile (spice-nearest for a harvester). `StructureScriptTests` (+3). Doc `Algorithms/StructureScript.md`. 257 tests. See History.
- **Structures subsystem — `Script_Structure_RefineSpice` (0x15): refinery economy core.** A refinery converts a linked harvester's spice → owner credits (hitpoint-scaled step clamped to the harvester's `amount`; 7/unit, enemy ±RNG bonus; `delay=6` throttle; emptied harvester drops `inTransport`; unlinked → SetState(IDLE)). `g_scenario` harvested tally is a SEAM. `StructureScriptTests` (+3). Doc `Algorithms/StructureScript.md`. 254 tests. See History.
- **Structures subsystem — turret-firing natives (a defensive turret now shoots).** `Script_Structure_FindTargetUnit` (0x08, 1.07 last-in-range pick) / `RotateTurret` (0x09, ICM base-turret sprite step) / `GetDirection` (0x0A) / `Fire` (0x0B, reuses `Unit_CreateBullet`) in `StructureScriptFunctions` + dispatch. A gun turret acquires → aims → fires at a seen enemy end-to-end via `GameLoop_Structure`. `StructureScriptTests` (+4 incl. the turret-fires integration). Doc `Algorithms/StructureScript.md`. 251 tests. See History.
- **Structures subsystem — the structure script system + death-path closure (the big next foundation).** Bridged `BUILD.EMC` → a structure `ScriptInfo`; new `GameLoop_Structure` (`Simulation.swift`) — `StructureTickCursors` (script path live; degrade/structure/palace seams), `StructureScriptRunner` (op-14 dispatch over `g_scriptFunctionsStructure`, fixed 3-opcode batch, the 1.07 `i!=3`-aborts-remaining quirk), `StructureScriptFunctions` (SetState/GetState/RemoveFogAroundTile/var-4 scrubs/Explode/Destroy; turret/FindUnitByType/RefineSpice deferred → clean-halt). The death-script `Script_Load` lives in `GameLoop_Structure`'s not-loaded else-branch (`Structure_Destroy` only `Script_Reset`s), so a building damaged to 0 HP reloads its death branch (Explode→Delay→Destroy→`Structure_Remove`) — verified end-to-end. Added `GameState.structureRemoveFog`. `StructureScriptTests` (3). Doc `Algorithms/StructureScript.md`; insight `world-map-occupancy-index-invariant`. 247 tests. See History.
- **Periodic self-review (overdue, 94 commits).** Extracted one recurring lesson → the RNG-stream-desync misdiagnosis (the `attack-rocket` tick-69 residual cost three wrong diagnoses) into a standing `CLAUDE.md → "What not to do"` rule (align the RNG draw stream via `--parity-random-trace` before calling a scenario-golden divergence a bug; gate at the deterministic horizon). Re-tagged baseline `selfreview-phase3`, reset the count. See History + the self-review tracker below.
- **Structures subsystem — started (4 World-layer primitives + wiring).** `Structure_UntargetMe` (ref-scrub), `Structure_Remove` (map-clear + free teardown), `Structure_Destroy` (begin destruction: mark + credits + linked-unit removal + windtrap), `Structure_Damage` (HP drain → death) in `GameState+Lifecycle.swift`, all decision-trace tested; **wired into `Map_MakeExplosion`** (`UnitImpact.swift`) so a bullet now damages/destroys a building. Seam: the structure death-script `Script_Load` (needs the structure script system — the big next foundation). 244 tests. See History.
- **Missile homing fix + `attack-rocket` residual closed.** Fixed `GameLoop_Unit` to re-aim a flying missile at `currentDestination` unless chasing an aircraft (was: any unit target). The remaining 1-unit `attack-rocket` divergence was root-caused (instrumented the RNG stream) as **stochastic idle-rotation spread, not a bug** — gates at `compared: 69`, not chased. See History.
- **Tooling — `Scripts/` per-round scripts** (`check.sh` build+test→concise verdict, `build-oracle.sh`, `log-history.sh`) + `scenariolab` Speed control (1×–10×). See History / CLAUDE.md "Running things".
- **Combat-integration slice — `attack-rocket` golden (Launcher duel) + fog flip.** Wired `Script_Unit_RemoveFog` (0x28) live (all goldens stay green). Added a 2nd combat golden: two stationary Launchers fire `notAccurate` rockets — verifies the missile/scatter SPAWN path (matches to tick 68), and **confirms `sim-rng-stream-unpinned-wobble`** (both stationary ⇒ no wobble ⇒ the stream + both units' idle-twitches align byte-for-byte). Gated `compared: 69` (diverges at the Rocket unit's own in-flight EMC script — the next gap). 239 tests. See History.
- **Combat-integration slice step 2c — bullet impact (`Map_MakeExplosion` + `Unit_Damage`): FULL 400-tick unit-vs-unit combat parity.** Ported `Map_MakeExplosion` + relocated `Unit_Damage` to the movement layer (`UnitImpact.swift`, `extension UnitMovement` — breaks the Move↔Combat cycle; `UnitCombat.damage` delegates); wired the three `Unit_Move` bullet-impact points (mid-flight wall/building/mountain hit, arrival detonation, the now-unblocked `degrades` self-damage). **`attack-close` regated `compared: 67 → 0`:** fire→bullet→impact (defender 200→188, GUARD→HUNT)→retaliation (attacker 200→188), all 401 frames bit-identical (no RNG-spread — stationary combatants don't draw the render-only wobble that desyncs `guard`). `UnitCombatTests` (+1: mapMakeExplosion). 239 tests. See History.
- **Combat-integration slice step 2b — `Script_Unit_Fire` projectile path (a unit fires; the bullet spawns + flies bit-identical to the oracle).** Ported `Unit_Create` + `Unit_IsTileOccupied` + `Unit_CreateBullet` + the full `Script_Unit_Fire` in `UnitCombat.swift` (dispatch `0x08` → `combat.fire`; new `combat` member on `UnitScriptRunner`); extracted `Unit_SetTarget` → `UnitScriptFunctions.unitSetTarget` (UnitOrders delegates). **`attack-close` matches the oracle through tick 66** (`compared: 67`): attacker fires at tick 61, the bullet (unit 12, type 23, hp 25, orient 64 @ 1040) is identical and flies 6 ticks; diverges at tick 67 = impact damage (next SEAM). Golden comparison now sorts by `index` (slot-order vs find-array-order). `UnitCombatTests` (+4). 238 tests. See History.
- **Combat-integration slice step 2a — `Script_General_GetDistanceToObject` (0x3E) unblocks the ATTACK script.** Dispatch-traced the attacker (it halted at tick 0 on 0x3E), ported it in `GeneralScriptFunctions` (→ `Object_GetDistanceToEncoded`; structure target → facing edge tile via the existing `StructureLayoutInfo.edgeTiles`, matching the oracle's un-gated structure-layout index). **`attack-close` now matches the oracle through tick 60** (regated `compared 5 → 61`); both tanks do setup→aim→acquire identically and diverge only at the first shot (the projectile = `Fire` SEAM). `GeneralScriptFunctionsTests` (+1). 234 tests. See History.
- **Combat-integration slice step 1 — GUARD-loop natives `Script_Unit_SetDestination` (0x05) / `FindStructure` (0x25) / `IdleAction` (0x31).** Dispatch-traced the real GUARD script to find what it clean-halted on (a chain, not one native), ported all three in `UnitScriptFunctions` + wired `UnitScriptRunner`. `unitSetDestination` is the shared `Unit_SetDestination` home (`UnitOrders.setDestination` delegates). The GUARD loop now runs to `Delay` and reaches `FindBestTarget` each cycle. `IdleAction`'s stochastic idle rotation drifts our `random256` stream off the oracle's unpinned render-only `wobble` draws → `guard` golden regated `compared: 6` (deterministic prefix). Added `--parity-lcg-trace=`/`--parity-random-trace=` to `Parity_RunScenario` (`Repositories/OpenDUNE/src/parity.c`) to diagnose (gated → goldens unchanged). `UnitScriptFunctionsTests` (+3 decision-trace). Insight `sim-rng-stream-unpinned-wobble`. 233 tests. See History.
- **Fog of war primitives** (`DuneIIWorld/State/GameState+Fog.swift`): `Map_UnveilTile` / `Tile_RemoveFogInRadius` / `Unit_RemoveFog` — player-only `isUnveiled` + the `seenByHouses` reveal. `FogTests` (3, after a fix: the already-unveiled guard + a centre/candidate sub-tile mismatch). **Ported but NOT wired into the `RemoveFog` native (0x28)** — wiring it diverges `attack-close` (revealing the enemy changes `FindBestTarget` inputs mid-combat before the rest of combat matches the oracle); flip it on with the combat slice. 231 tests.
- **Tier-E `Unit_FindBestTarget`** (`DuneIISimulation/TargetFinder.swift`, native op-`0x1C`): deterministic auto-target — find/priority chain scoring seen, non-allied, priority enemies by distance. `TargetFinderTests` (4); exercised by the guard/attack scenario goldens (no regression). **Finding (corrected): fog is NOT the attack-close blocker** — a per-tick trace shows the defender's GUARD script *clean-halts* (PC nulled) at tick 1 on an unported native, so it never reaches acquisition; identifying that native is the real next step (see Active task).
- **Scenario goldens — multi-unit setup fidelity + attack/guard per-tick coverage.** `loadScenario` parses the unit action (`ActionType.named` = `Unit_ActionStringToType`); the golden test adds a Scen-style prepare (per unit: `Unit_SetAction` script-load + `Unit_UpdateMap(1)` placement, mirroring the oracle) so multi-unit target resolution + occupancy match. New goldens: **`guard`** (guard sits while a trike crosses the map — full 400-tick match) and **`attack-close`** (deterministic prefix to tick 5, where the defender's combat AI reacts; combat RNG isn't chased byte-exact). `Spec.cmdUnit` selects the commanded unit. All 4 scenario goldens green. See History.
- **Scenario goldens — viewport script-throttle + table-driven per-tick harness.** Ported `Map_IsPositionInViewport` (`Tile32.isPositionInViewport`) + `GameState.viewportPosition`; `GameLoop_Unit` throttles an off-viewport unit's script to 3 opcodes/tick (vs 52) — closing the pinned-52 SEAM, so movement matches the oracle for any placement. `ScenarioGoldenTests` is now table-driven (`Spec` + `@Test(arguments:)`), pinning `viewportPosition=(12,12)`. Added **`move-trike`** golden (off-viewport trike, seed 777) — full 400-tick match; `moving` tank still full. Attack/guard goldens deferred (need Simulation-layer Scen-style setup — per-unit `Unit_SetAction` + `Unit_UpdateMap` placement — + combat natives). See History.
- **`scenariolab` — natural terrain, real loop, Deviate case.** Terrain via real `Map_CreateLandscape` (partial sand/dune/rock/mountain/spice; `ScenarioTerrain.apply(iconMap:)`); `ScenarioRunner.tick()` drives the full `Simulation.tick()` so units actually cross the terrain in the lab; fixed `ScenarioBuilder.move()` to set `targetMove` (real `Unit_SetDestination`). New **`deviate`** `ScenarioKind` demoing `Unit_Deviate`; `ScenarioImageBuilder` colours units by the effective house (`unitHouseID`) so deviation is visible. Lab viewport points at the region (full-speed). `DuneIIScenarios` CLAUDE.md updated.
- **Tier-E #17 `Unit_Deviate`** (`UnitCombat.deviate`): eligibility guards + `toughness`-probability + one RNG draw → `deviated=120`/action-flip/untarget. `UnitCombatTests` (+4).
- **Tier-E #18 `Unit_Damage`** (`DuneIISimulation/UnitCombat.swift`): HP drain → deviation wear → death (`GameState.unitRemovePlayer` + `Unit_SetAction(DIE)`), ambush→attack, sandworm/half-HP death, infantry→soldier downgrade (RNG retreat), tracked/wheeled/harvester smoke. Seams: explosions (#15, `range != 0`), harvester-death spice, audio, render. `UnitCombatTests` (6, decision-trace). Not yet wired into `Unit_Move`'s degrades branch (combat-integration slice). 219 tests green. See History.
- **EMC VM — unported natives halt the script cleanly (null the PC) instead of suspending.** Faithful to OpenDUNE's fully-populated table (only `Delay` suspends); removes the silent per-tick timing skew that stalled movement. `ScriptVMTests`/`UnitScriptRunnerTests` assert PC nulled + stays stopped. See History.
- **Unit movement cluster — a tank moves end-to-end, bit-identical vs OpenDUNE over the full 400-tick trajectory.** `Tile_MoveByOrientation` (World leaf) + `DuneIISimulation/UnitMovement.swift` (`Unit_MovementTick`/`Unit_Move`/`Unit_StartMovement`/`Unit_Deviation_Decrease` + the op-`0x0C` native `Script_Unit_CalculateRoute`), composing the ported primitives + `Pathfinder`. Faithful `GameLoop_Unit` per-unit body wired into `Simulation` (turret-aim → movement → rotation → deviation → tile-reclaim → script(52 opcodes) → nextAction-promote); `Simulation(scriptInfo:)` now builds a `UnitScriptRunner`. Added the MOVE-routine no-op natives `Script_Unit_Fire` (`0x08`, projectile spawn SEAM), `Unknown2552` (`0x24`), `RemoveFog` (`0x28`, fog SEAM). `Unit_Move`'s bullet/sonic/saboteur/bloom branches transcribed with SEAMs (Unit_Damage #18, explosions #15, EnterStructure, bloom). **`ScenarioGoldenTests.moving` now asserts the whole `moving-golden.jsonl` (400 ticks)** — tank rotates then steps `(16,16)→(19,19)` matching the oracle tile-for-tile. Doc `Algorithms/UnitMovement.md`; `TileMoveByOrientationTests`. 213 tests green. See History.
- Reset to a core-engine rebuild; wrote `Documentation/Plan.v1.md` (commit `9b8ee1f`). See History.
- Phase 0 docs: CLAUDE.md rewrite + Architecture/{Overview,Testing,ParityHarness}.md + this file + History/Insights (commit `68301f4`).
- Phase 0 scaffold: `Code` SwiftPM graph — 7 libraries + `assetgen` + `duneii-headless` + per-module CLAUDE.md. Clean build, zero warnings, 4 scaffold tests green.
- Phase 1: Format80 (LCW) decode ported from OpenDUNE `src/codec/format80.c` + `Documentation/Formats/Format80.md` + 11 decode tests. See History; insight `codec-format80-overlap.md`.
- Phase 1 (rest): Format40, PAK, Palette, IFF, SHP, CPS, ICN, WSA, FNT, VOC, INI, EMC ports + `emc-disasm`; per-format docs; real-data decodes from install PAKs; insights. Fixed the Phase-0 SwiftPM unhandled-files warnings (per-module CLAUDE.md excluded). See History.
- Phase 1 (asset export): `DuneIIExport` (`PngWriter`/`WavWriter`) + `assetgen extract` → PNG/WAV/EMC-listing output; verified on the real install (350 assets, 0 failures; CPS 320×200, valid WAVs). Closes the Phase-1 asset-regeneration deferral. 60 tests green.
- Renderer + render-test app (Phase 4 pulled forward): `DuneIIRenderer` asset services (`HouseRemap`, `IndexedImage`) + `rendertest` SwiftUI inspector (`swift run rendertest`) — asset hierarchy, animated playback, house recolor, 1×–16× scale, VOC playback. Pixel logic test-verified + OpenDUNE-faithful; on-screen look to be confirmed by running it. 64 tests green.
- rendertest refinements: frame animation off by default (Play toggle + frame stepper; thumbnails show index + size); `PaletteAnimator` palette cycling (wind-trap index 223 etc.) with a live "Palette cycling" toggle; colorize-on-display so house + palette changes apply live. 65 tests green.
- rendertest logical sprite grouping: `SpriteCatalog` (DuneIIRenderer) splits the unit SHPs (UNITS/UNITS1/UNITS2) into per-unit groups (directional vs animation), surfaced as a "Units" category; directional groups offer a Facing stepper (no auto-animate), animation groups a Play. Scale picker now also scales the grid thumbnails. 66 tests green.
- rendertest building/terrain grouping: `IconMap` decoder (DuneIIFormats) + "Buildings"/"Terrain" categories — each ICON.MAP icon group lists its ICON.ICN tiles (subitems) with house remap + palette cycling. 68 tests green.
- rendertest MENSHPM palette fix: mentat face sprites colored by their `MENTAT<house>.CPS` palette (not IBM.PAL) via `AssetLibrary.cpsPalette` + `AssetDetailView.mentatPalette` (OpenDUNE `gui/mentat.c:494`). Build clean. Insight `render-contextual-palette`.
- rendertest assembled buildings: `StructureCatalog` (DuneIIRenderer) maps structure icon groups → `(w,h)` tile layout (OpenDUNE `structureinfo.c`); the `.iconGroup` decode assembles a building's tiles (consecutive `w*h`-tile states, row-major per `Structure_UpdateMap`) into whole-building frames. Buildings category now shows whole buildings, not separate tiles. 69 tests green. Insight `render-structure-layout`.
- rendertest palette-cycling correctness fix: cycling (`GUI_PaletteAnimate`) now applies **only** to IBM.PAL-rendered content — `AssetDetailView.decode` gates `paletteAnimatable = !rawFrames.isEmpty && displayPalette == nil`. Assembled structures default to the built state (2) so the windtrap power light (index 223, present only in built states) actually pulses. Insight `render-palette-animation` (extended).
- rendertest context palettes: discovered the mentat CPS/SHP and the intro/finale WSAs **embed no palette** (verified on the install) — so they were wrongly drawn with IBM.PAL. Added `AssetLibrary.palette(named:)` + `AssetDetailView.contextPalette(for:)` reproducing the runtime loads: mercenary mentat (`MENSHPM.SHP`/`MENTATM.CPS`) → `BENE.PAL`; cutscene WSAs (`INTRO*`/`?FINAL*`) → `INTRO.PAL`; others → IBM.PAL. Corrected the earlier (non-working) MENSHPM "fix". Insight `render-contextual-palette` (rewritten). (Committed `ad67e38`.)
- *(earlier Phase 0–2 + foundational Phase-3 primitive/EMC/stat-table slices — all in `Documentation/History/`; summarised in the Active task above.)*

## Test status

`Scripts/check.sh` (or `--full` for the clean-build zero-warnings audit): **538 tests, all green** (clean `--full` build, zero warnings; 1 known issue — the deployed-empty-harvester STOP-vs-HARVEST `withKnownIssue`, insight `sim-script-vm-engine-copy`). The render goldens (`RenderGoldenTests`, pixel-exact) are green and unchanged by the map-bounds clip. (The count grew through the Phase-5/6/7 work: save/load + the original-`.SAV` converter, the carryall/cargo fix, repair/upgrade/starport-order commands, the shoot-the-bloom explosion path, the full per-unit action menu, and the scenario map-boundary clip — see "Recently completed".)

**RNG draw-stream golden (new).** `ScenarioGoldenTests` now also asserts our per-tick `Random256`/`RandomLCG` draw stream is **byte-identical to the oracle's** (`--parity-random-trace`/`--parity-lcg-trace`, committed as `<name>-r256.txt`/`-lcg.txt` fixtures; recorded on our side by `RngTraceSink`). This catches a missing/extra/reordered draw the instant it happens — even when it doesn't yet move a compared field. It already caught two hidden bugs: the `GameLoop_Team` cursor draw (fixed) and `animationTick` drawing `Random256` in the parity path (fixed — animations are now gated behind `Simulation.tickAnimations`, off for the golden path / on for the visual apps, mirroring `tickExplosions`). To regenerate after a real RNG change: `Scripts/gen-scenario-goldens.sh` (dumps the traces alongside the state goldens).

**Golden parity vs OpenDUNE (the scenario harness, 11 scenarios + a decision-trace + an RNG draw-stream golden):** the full 400-tick `moving`/`move-trike`/`attack-close`/**`attack-structure`**/**`trooper`**/**`guard`**/**`attack-rocket`**/**`teams`**/**`missile-duel`** trajectories — all full matches (per tick: units — incl. `spriteOffset` for the walk animation + `team` for AI recruiting — + structures + houses). **Map-tile goldens** (a new `[DUMPTILES]` section → per-tick `groundTileID` dump): **`wall-destruction`** (a tank's bullet's `Random_256` roll destroys a wall at tick 67 — `Map_MakeExplosion`'s wall branch + the ported `Map_UpdateWall` revert the ground; rides the RNG-stream golden) and **`slab-indestructible`** (explosion VM ticked via `[BASIC] TickExplosions=1`: a tank's `IMPACT_SMALL` carries no `TILE_DAMAGE`, so the slab survives — built concrete **is** destructible by a `TILE_DAMAGE` explosion (IMPACT_LARGE/EXPLODE), verified RNG-free in `ExplosionTests`). See `Architecture/ScenarioHarness.md` + insight `world-fog-veil-overlay-off-by-one` (why the tile golden compares ground, not the veil overlay). **`guard`/`attack-rocket` became full matches** once the `GameLoop_Team` unconditional-cursor-draw bug was fixed; **`teams`** is the cross-engine AI golden (an Ordos `Normal`-brain team recruits its tanks, `team` field `0→1`, matching the oracle). Plus the **RNG draw-stream golden** (`RngTraceSink`): our per-tick `Random256`/`RandomLCG` draws are byte-identical to the oracle's `--parity-random-trace`/`-lcg-trace` for all full-match scenarios (caught the team-cursor + animation-tick draw bugs). **`economy`** — a full 60-tick **house-aggregate** golden (credits/power/storage via a `[HOUSES]` section) cross-validating `House_CalculatePowerAndCredit` + the credit clamp + power upkeep; **Tier-2a structure decision-trace** (`StructureTraceTests`) — the `attack-structure` windtrap's per-opcode trace matches the oracle's 24 lines exactly (opcode-identical EMC execution). Plus the Tier-1 function goldens (RNG, tile geometry, game-speed, index, stat tables incl. unit/structure info + structure layout + actionsAI, ObjectFlags/MapTile bit layouts, createLandscape, pathfinder, spice/search).

**Decision-trace + integration coverage:** the four `GameLoop_*` phases (Team→Unit→Structure→House); unit movement/rotation/combat (fire→bullet→impact→retaliation) + `Unit_Damage`/`Deviate`/`FindBestTarget`/fog; the structure subsystem (every `BUILD.EMC` native — turret fire, refinery spice→credits, unit-unload deploy, carryall summon, the death-path removal) + the **`tickStructure` economy** (factory production/repair/upgrade + `Structure_IsUpgradable`); the **House economy** (`House_CalculatePowerAndCredit` + `Structure_CalculateHitpointsMax` + the live `GameLoop_House`); the **team subsystem** (`GameLoop_Team` loop + the getter + recruit/target brain natives); the harvester→refinery→credits loop. Plus model-POD defaults, pool/index/map queries, clock/cadence, tile motion, landscape type, sprite tile-ids, scenario load (synthetic + real-data). Clean build: zero warnings (full output audited, incl. the `rendertest` product). OpenDUNE oracle: `Scripts/build-oracle.sh` (the `.shim/gcc`→`xcrun clang` shim + re-sign; see the build/oracle quirk note above).

## Open decisions (from Plan.v1.md §8)

1. Package layout — proceeding with `Code` single-package multi-target as planned (swift-tools 6.3, macOS 26). Adjust if desired.
2. `assetgen` + `Resources/` regenerate fresh in Phase 1 (planned).
3. OpenDUNE oracle tooling stood up in Phase 1–2.
4. ~~Multi-window UI tech~~ — **resolved (2026-05-31): native macOS AppKit + SwiftUI** (non-Catalyst); `duneii` host with floating `NSPanel` tool windows.

## Self-review tracker

Baseline tag: `selfreview-phase4` (re-tagged 2026-05-31 after the breadth-push review). Commits since = `git rev-list --count selfreview-phase4..HEAD`. Trigger: each completed phase, or every 32 commits. See CLAUDE.md → "Periodic self-review." Phase 0 review: nothing recurrent (first phase). Phase 1 review (done): recurring issue = build warnings hidden by `tail`; strengthened CLAUDE.md step 5 (read full build output) and captured insight `build-swiftpm-unhandled-files`. **Phase 2 review (done, at commit `e1e3a65`):** recurring friction = the sandbox EPERM-on-exec / out-of-repo-write problem rediscovered each session; captured insight `build-exec-eperm` (commands also in this file's task header). **Phase-3 mid-phase review (done 2026-05-30, at `04dfc17`, the overdue 94-commit one):** one genuine recurring lesson extracted → the RNG-stream-desync misdiagnosis (the `attack-rocket` tick-69 residual cost **three** wrong diagnoses before being root-caused as stochastic idle/`wobble` spread). Added a standing rule to CLAUDE.md → "What not to do": align the RNG draw stream (`--parity-random-trace=`/`--parity-lcg-trace=`) before treating any scenario-golden divergence as a bug; gate at the deterministic horizon if it traces to unpinned idle/wobble draws. Everything else recurrent was already mitigated — build/exec EPERM (`build-exec-eperm` + scripts), stale-signature SIGKILL (`build-oracle.sh`), "read the test result before committing" (`Scripts/check.sh`'s concise verdict). Re-tagged `selfreview-phase3` at HEAD; count reset to 0. **Phase-3 COMPLETION review (done 2026-05-30, at Phase-3 close):** the one big recurring lesson this span was the RNG-stream-divergence misdiagnosis — but it's now **structurally resolved**, not just documented: the new **RNG draw-stream golden** (`RngTraceSink` + `ScenarioGoldenTests`) asserts our per-tick draws match the oracle's byte-for-byte, so a missing/extra/reordered draw fails immediately with a pointer to the site. It found two real bugs the state goldens had hidden (the `GameLoop_Team` cursor draw; `animationTick` drawing in the parity path). The CLAUDE.md "What not to do" rule + insight `sim-rng-stream-unpinned-wobble` were corrected (a fully-seeded divergence is a real draw-count bug, not "spread"). The recurring stale-`swiftmodule` GameState-layout trap recurred once more (slice 5) and is already covered by `build-gamestate-layout-stale-module` + step-5 `--full`. **Nothing new rose to a standing rule** — the recurring problems are fixed or now auto-caught. Re-tagged baseline → `selfreview-phase4` at HEAD; count reset to 0. **Phase-3 review #2 (done 2026-05-30, at `5faba93`, the 32-commit trigger — the explosion/team/unit-natives span):** the one genuinely recurring friction was the `UnitScriptRunnerTests` "unported native" **marker opcode** breaking (once crashing) the suite on *every* slice that ported it (0x04→0x12→0x14→0x37→0x09) — now **structurally resolved** by pointing the marker at a non-native `0x40` (past the 64-entry table), so it never needs retargeting again. The span's other recurrences already have insights: `swift-expect-no-mutating-call` (hit ×2), `build-gamestate-layout-stale-module` (the stale-build linker miss, `--full` catches it), `parity-oracle-harness-untick-visual-subsystems`, `sim-movement-speed-entered-tile`. Nothing rose to a new standing CLAUDE.md rule (the frictions are fixed or captured as insights). Re-tagged `selfreview-phase3` at HEAD; count reset to 0. **Phases-4–7 breadth-push review (done 2026-05-31, 34 commits since `selfreview-phase4` — the render harness/goldens, fog/shimmer/transparency, wall/slab, input, audio, the `duneii` native client, the state chip, and wire-sounds).** Each subsystem landed cleanly with its own insight captured (`render-snapshot-backing-scale`, `render-tile-overlay-transparency`, `render-sandworm-blur-displacement`, `world-fog-veil-overlay-off-by-one`, …). **Nothing new rose to a standing rule.** The one user-corrected miss — claiming "1.07 has no slab destruction" without an OpenDUNE citation — was a **violation of existing core principle #6** (ground behavior in OpenDUNE; don't invent — including a *negative*), not a gap a new rule would fix. The recurring stale-`swiftmodule` GameState-layout trap recurred once more (wire-sounds) and is already covered by `build-gamestate-layout-stale-module` + the step-5 `--full`. Re-tagged baseline `selfreview-phase4` at HEAD; count reset to 0.
