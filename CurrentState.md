# Current State

**Branch:** `main`. **Plan of record:** `Documentation/Plan.v1.md`. **Architecture:** `Documentation/Architecture/Overview.md`.

## Active task

**Phase 2/3 — port the native functions the EMC scripts call, golden-tested against OpenDUNE, then transcribe the EMC files.** Approach + status in `Documentation/Architecture/FunctionParityHarness.md` (the 3-step plan: port primitives → match-test each vs OpenDUNE → port EMC). The OpenDUNE oracle builds headlessly here and has a `--parity-golden` function-dump mode.

**Done so far (Tier-0 primitives, all golden-verified):** both RNGs (`DuneIIWorld/Rng/`); tile geometry + orientation (`DuneIIWorld/Tile/` — `Tile32` pack/unpack/distance/direction, `Orientation`); `Tools_AdjustToGameSpeed` + `Tools_Index_GetType`/`Decode` (`DuneIIWorld/Tools/`). Per-category fixtures under `WorldTests/Fixtures/` (`rng-`, `tile-`, `gamespeed-`, `index-golden.jsonl`) via `opendune --parity-golden=<dir>` + the shared `GoldenFixture` loader. Tier-0 pure primitives are essentially complete.

**World model started — stat tables (golden-verified):** `HouseID` (Contracts) + `HouseInfo`; `LandscapeType`/`MovementType` + `LandscapeInfo`; `ActionType`/`SelectionType` + `ActionInfo`; **the big tables `ObjectInfo`+`UnitInfo` (27 units) and `StructureInfo` (19 structures)** with seam enums `UnitType`/`StructureType` (Contracts) + `DisplayMode`/`StructureLayout` (World); **`StructureLayoutInfo`** (the 7 layout geometries, consolidating `g_table_structure_layout*`) + `XYSize`; **`ActionInfo.actionsAI`** (`g_table_actionsAI`) — all in `DuneIIWorld/Stats/`. The unit/structure `table` literals are generated from the golden dumps by `Tests/WorldTests/Fixtures/.gen_tables.py` and re-checked by `UnitStructureInfoGoldenTests`; layout + actionsAI by `StatTableGoldenTests`.

**Build/oracle quirk in this environment:** direct `exec` of `/usr/bin/clang` and of freshly-built binaries returns `EPERM`. Rebuild the oracle via `cd Repositories/OpenDUNE && PATH="$PWD/.shim:$PATH" make -j4` (a `.shim/gcc` shim routes `gcc`→`xcrun clang`); regenerate fixtures with `./bin/opendune --parity-golden=Code/Tests/WorldTests/Fixtures`. Build/test Swift with `cd Code && mkdir -p .build/tmp && TMPDIR="$PWD/.build/tmp" xcrun swift build --disable-sandbox` (same for `swift test`); recreate `.build/tmp` after `swift package clean`.

**Model PODs done (`DuneIIWorld/Model/`):** `Object`/`ObjectFlags`/`ScriptEngine`/`Dir24`/`Unit`/`Structure`/`StructureState`/`House`/`HouseFlags`/`Team`/`TeamFlags`/`TeamActionType`/`MapTile`/`MapInfo` — faithful value-type ports of the OpenDUNE structs (design in `Architecture/DataModel.md`). Bit layouts of `ObjectFlags` + `MapTile` golden-pinned (`objectflags-`/`maptile-golden.jsonl`); tests in `ModelTests`.

**Pools + `GameState` done (`DuneIIWorld/State/`, `Pool/`):** `GameState` is the single owned value-type aggregate (unit/structure/house/team slot arrays at `*_INDEX_MAX` + find arrays, `MapTile[64*64]`, both RNGs, the two clocks, `validateStrictIfZero`). `Unit`/`Structure`/`House`/`Team` `_Find`/`_Allocate` + `Unit_Recount` ported faithfully (band allocation, house cap with winger/slither bypass, the 3 special structure slots). The deferred `Tools_Index_Encode`/`IsValid`/`GetUnit`/`GetStructure`/`GetObject`/`GetTile` now live on `GameState` (`GameState+Index.swift`, `ObjectRef` for the C `Object*`); `Tile32.addDiff` added. Tests in `GameStateTests`. **Still not in `GameState`:** the speed/pause model, the per-subsystem tick cursors, and the hoisted `ObjectInfo.available` — those land with the loop (Phase 3). `Unit`/`Structure`/etc. `_Free`/`_Remove` also deferred to the loop.

**Phase 3 started (`DuneIISimulation`) — loop foundation done:** `Simulation` driver with the pause-aware two-clock `tick()` and the four-phase order (Team → Unit → Structure → House); `gameSpeed`/`paused` + the `GameLoop_Unit` tick cursors live in `GameState`; `GameLoop_Unit`'s cadence is ported faithfully (`advanceUnitCadence`/`UnitTickFlags`). The per-unit work + the Team/Structure/House phases are order-preserving stubs. Design in `Architecture/SimulationLoop.md`; tests `SimulationTests/LoopTests`.

**Native primitives — dependency-ordered plan in `Plan.v1.md` §9** (Tier 0 done → A tile helpers → B rotation → C movement → D map → E combat → F lifecycle/pathfinding). Implementing bottom-up; each must match its OpenDUNE function by result/effect (golden where pure, state-diff/Tier-2a where stateful). **Done so far:** Tier A (1–4) — `Tile32.centered`/`isOutOfMap`/`moveByDirection`/`moveByRandom`, golden-tested (`tilemotion-golden.jsonl`, `TileMotionGoldenTests`); Tier B.5 `setOrientation` + B.7 `rotate`; Tier C.8 `setSpeed` — as the **replaceable** `UnitPrimitives` protocol + `DefaultUnitPrimitives` (injected into `Simulation` as `unitPrimitives`; **no static functions** for sim primitives — leaf math stays static), behaviour-tested (`UnitLogicTests`). **Dependency correction (now in Plan §9):** `Unit_UpdateMap`/`Unit_Move`/`Unit_MovementTick` actually need the Tier-D map cluster + lifecycle/combat, so they're resequenced after Tier D (D′1–D′3) — not done yet.

**Tier D/E — primitives done:** `MapPrimitives.isValidPosition`/`landscapeType`/`tileIsUnveiled`/`isPositionUnveiled`/`changeSpiceAmount`/`searchSpice`; `HousePrimitives.areAllied`; and **Tier-E #16 `UnitPrimitives.tileEnterScore`** + `isValidMovementIntoStructure` (+ World `GameState.structureGetByPackedTile`) — all replaceable protocols injected into `Simulation`, golden- and decision-trace-tested. **Tier-F lifecycle in progress:** the pool `_Free` ops, the reference-clearing cluster (`unitUntargetMe` + helpers), and **`Unit_Remove` (#20)** + its map/visibility cluster (`unitUpdateMap`/`unitRemoveFromTile`/`unitHouseUnitCount*`) are **done** (with audio/GUI/fog/`Unit_SetAction` seams noted). The **EMC VM core + the first `Script_General_*` native batch are done** (`DuneIISimulation/Script/`). Natives are **clean explicit-parameter functions**, each heavily tested (memory `feedback-script-natives-clean-functions`). **Recommended next: (a) continue the natives — the `Script_Unit_*` batch (`UnitScriptFunctions`, simplest first: GetInfo/GetAmount/GetOrientation/SetSpeed/SetOrientation/Rotate, then SetAction/SetDestination/…), same clean+well-tested style; (b) the op-14 dispatch glue per category (peek stack args → call the clean function) + the real EMC→`ScriptInfo` bridge so a unit script runs end-to-end (decision-trace golden vs the oracle's `g_parityScriptTrace`); (c) wire `Unit_SetAction` (#19), closing the two `Unit_Remove` seams.** Recorded in Plan §9.

**Scenario/map/sprite-init layer — design + sub-deliverable 1 done.** Design of record: `Architecture/ScenarioMapLoading.md` (resource access → sprite tile-ids → `Map_GetLandscapeType` → original-save conversion → mission `.INI` loading → `Map_CreateLandscape` → tile→sprite render → the map app, in dependency order). **Done:** `TileIDs` (`DuneIIWorld/Map/`) = runtime tile-id bases from `ICON.MAP` (`Sprites_Init`), in `GameState.tileIDs`; `IconMap.tileID(group:offset:)` flat lookup; verified vs the real `ICON.MAP` (`TileIDsTests`).

**Sub-deliverable 2 done — map-from-seed.** `GameState.createLandscape(seed:iconMap:)` = `Map_CreateLandscape` (+ `Map_AddSpiceOnTile`), bit-exact vs the oracle for every tile of every seed (`createlandscape-golden.jsonl`, `MapGeneratorTests`). **We can now generate the exact Dune II map from any seed** → the keystone for drawing.

**Scenario loader + `mapview` app done.** `GameState.loadScenario(ini:iconMap:)` (`DuneIIWorld/Scenario/`) loads a scenario `.INI` → `TileIDs` + `createLandscape([MAP]Seed)` + `[BASIC]MapScale` + `[UNITS]`/`[STRUCTURES]` into the pools (`ScenarioLoaderTests`). New macOS SpriteKit app `Apps/mapview` (`swift run mapview`): picks any install scenario, draws terrain+structures (composited `ICON.ICN` tile layer via `MapImageBuilder`) + units (house-recoloured SHP sprites), resizable window + toolbar (scenario picker + 1×–16× camera-zoom scale). Builds/launches clean.

**`mapview` rendering complete (all five items, proper ports).** Scene `update(_:)` runs `Simulation.tick()` (clocks + `animationTick`), re-blits terrain when `mapDirty`, recolorizes via `PaletteAnimator` each tick. (1) game loop ✓; (2) windtrap palette (223) ✓; (3) **building frame animation** via the ported structure **Animation system** (`Animation`/`AnimationTables.structure` in World, `GameState+Animation` engine = `Animation_Start`/`Tick` + `Structure_UpdateMap`; structures live in `g_map.groundTileID` and cycle states) ✓; (4) **orientation-correct** unit body + **turret** frames (viewport `values_32A4`/`values_32C4` + flip) ✓; (5) **sandworm** sand-distortion via the `DRAWSPRITE_FLAG_BLUR` pixel-displace ✓. Tests: `AnimationTableTests` (table golden), `AnimationTests` (a building's tiles cycle). The unit sprite-index logic is extracted into `DuneIISimulation.UnitSprites.info(for:)` (body + turret global indices/flip/offset, per `viewport.c`); `mapview` maps the global index → SHP via load-order bases. **`AllScenariosTests`** loads every `SCEN*.INI` and verifies unit/structure positions (vs the INI) + sprite-index resolution; `UnitSpriteTests` spot-checks indices/offsets vs OpenDUNE.

**Next on the app:** load original **saves** (`_SAVE00x.DAT` `FORM` via `Iff` → `GameState`, MAP-chunk overlay on `createLandscape`) so the toolbar can "load anything"; refine unit sprites (orientation-correct frame + turret overlay), draw fog/spice; visual polish (confirm tile y-orientation + palette on a display). `Map_GetLandscapeType` (Tier D) pairs with the save loader (golden vs `--parity-load` `Parity_DumpLandscape`).

**Earlier immediate-next note (superseded by the above):** (a) a **tile→sprite render mapping** — `MapTile.groundTileID` → `ICON.ICN` tile (we have `Resources/Tiles/ICON.{png,json}`) → an `IndexedImage`/`CGImage` for the 64×64 grid (in `DuneIIRenderer`); (b) a **macOS-only test app** (`mapview` or fold into a target): resizable window, toolbar to load a save/scenario + a 1×/2×/4×/8×/16× scale picker, nearest-neighbour scaled map filling the window. Drive it first from a hardcoded/scenario seed via `createLandscape`, then wire **mission `.INI` loading** (`BASIC/Seed` + `MapScale` + objects) and **original-save conversion** (`_SAVE00x.DAT` `FORM` via `Iff` → `GameState`, incl. the sparse MAP-chunk overlay on top of `createLandscape`) so "load anything" works and units/structures draw too. `Map_GetLandscapeType` (Tier D #11) lands alongside (golden vs the oracle's `--parity-load` `Parity_DumpLandscape`). Small follow-ups (anytime): `ICM_ICONGROUP_*`/`EXPLOSION_*` enums; `StructureCatalog`↔`StructureLayoutInfo` cross-test.

## Next up (queue)

- **Phase 3 — `DuneIISimulation`** (in progress; loop foundation done). Loop + clocks ✓ → primitives → one unit type end-to-end → economy → structures → houses/teams/AI → projectiles/explosions. State machines = exact EMC transcription, verified by Tier-2a decision traces.
- **Phases 4–5 — `DuneIIRenderer` + `rendertest` + `DuneIIInput`.**
- **Phase 6 — hosts + multi-window UI.**
- **Phase 7 — `DuneIIAudio`.**

## Recently completed

- Reset to a core-engine rebuild; wrote `Documentation/Plan.v1.md` (commit `9b8ee1f`). See History 2026-05.
- Phase 0 docs: CLAUDE.md rewrite + Architecture/{Overview,Testing,ParityHarness}.md + this file + History/Insights (commit `68301f4`).
- Phase 0 scaffold: `Code` SwiftPM graph — 7 libraries + `assetgen` + `duneii-headless` + per-module CLAUDE.md. Clean build, zero warnings, 4 scaffold tests green.
- Phase 1: Format80 (LCW) decode ported from OpenDUNE `src/codec/format80.c` + `Documentation/Formats/Format80.md` + 11 decode tests. See History 2026-05; insight `codec-format80-overlap.md`.
- Phase 1 (rest): Format40, PAK, Palette, IFF, SHP, CPS, ICN, WSA, FNT, VOC, INI, EMC ports + `emc-disasm`; per-format docs; real-data decodes from install PAKs; insights. Fixed the Phase-0 SwiftPM unhandled-files warnings (per-module CLAUDE.md excluded). See History 2026-05.
- Phase 1 (asset export): `DuneIIExport` (`PngWriter`/`WavWriter`) + `assetgen extract` → PNG/WAV/EMC-listing output; verified on the real install (350 assets, 0 failures; CPS 320×200, valid WAVs). Closes the Phase-1 asset-regeneration deferral. 60 tests green.
- Renderer + render-test app (Phase 4 pulled forward): `DuneIIRenderer` asset services (`HouseRemap`, `IndexedImage`) + `rendertest` SwiftUI inspector (`swift run rendertest`) — asset hierarchy, animated playback, house recolor, 1×–16× scale, VOC playback. Pixel logic test-verified + OpenDUNE-faithful; on-screen look to be confirmed by running it. 64 tests green.
- rendertest refinements: frame animation off by default (Play toggle + frame stepper; thumbnails show index + size); `PaletteAnimator` palette cycling (wind-trap index 223 etc.) with a live "Palette cycling" toggle; colorize-on-display so house + palette changes apply live. 65 tests green.
- rendertest logical sprite grouping: `SpriteCatalog` (DuneIIRenderer) splits the unit SHPs (UNITS/UNITS1/UNITS2) into per-unit groups (directional vs animation), surfaced as a "Units" category; directional groups offer a Facing stepper (no auto-animate), animation groups a Play. Scale picker now also scales the grid thumbnails. 66 tests green.
- rendertest building/terrain grouping: `IconMap` decoder (DuneIIFormats) + "Buildings"/"Terrain" categories — each ICON.MAP icon group lists its ICON.ICN tiles (subitems) with house remap + palette cycling. 68 tests green.
- rendertest MENSHPM palette fix: mentat face sprites colored by their `MENTAT<house>.CPS` palette (not IBM.PAL) via `AssetLibrary.cpsPalette` + `AssetDetailView.mentatPalette` (OpenDUNE `gui/mentat.c:494`). Build clean. Insight `render-contextual-palette`.
- rendertest assembled buildings: `StructureCatalog` (DuneIIRenderer) maps structure icon groups → `(w,h)` tile layout (OpenDUNE `structureinfo.c`); the `.iconGroup` decode assembles a building's tiles (consecutive `w*h`-tile states, row-major per `Structure_UpdateMap`) into whole-building frames. Buildings category now shows whole buildings, not separate tiles. 69 tests green. Insight `render-structure-layout`.
- rendertest palette-cycling correctness fix: cycling (`GUI_PaletteAnimate`) now applies **only** to IBM.PAL-rendered content — `AssetDetailView.decode` gates `paletteAnimatable = !rawFrames.isEmpty && displayPalette == nil`. Assembled structures default to the built state (2) so the windtrap power light (index 223, present only in built states) actually pulses. Insight `render-palette-animation` (extended).
- rendertest context palettes: discovered the mentat CPS/SHP and the intro/finale WSAs **embed no palette** (verified on the install) — so they were wrongly drawn with IBM.PAL. Added `AssetLibrary.palette(named:)` + `AssetDetailView.contextPalette(for:)` reproducing the runtime loads: mercenary mentat (`MENSHPM.SHP`/`MENTATM.CPS`) → `BENE.PAL`; cutscene WSAs (`INTRO*`/`?FINAL*`) → `INTRO.PAL`; others → IBM.PAL. Corrected the earlier (non-working) MENSHPM "fix". Insight `render-contextual-palette` (rewritten). (Committed `ad67e38`.)
- **Function-parity harness + RNGs (Phase 2/3 start).** Headless OpenDUNE oracle builds here; added `--parity-golden` function-dump mode. Ported `Random256` + `RandomLCG` to `DuneIIWorld/Rng/`, golden-verified bit-for-bit (3 tests). Docs: `Architecture/FunctionParityHarness.md`, `Algorithms/Rng.md`. (Committed `2addd51`.)
- **Tile geometry + orientation.** `Tile32` (pack/unpack, distance/direction) + `Orientation` in `DuneIIWorld/Tile/`, golden-verified (`TileGoldenTests`, 7 tests). Per-category golden fixtures (`--parity-golden=<dir>`) + shared `GoldenFixture` loader. Doc `Algorithms/Tile.md`. See History 2026-05.
- **`Tools_AdjustToGameSpeed` + index helpers.** `Tools.adjustToGameSpeed` + `Tools.indexType`/`indexDecode` (`IndexType`) in `DuneIIWorld/Tools/`, golden-verified (`ToolsGoldenTests`, 3 tests; `gamespeed-`/`index-golden.jsonl`). `Tools_Index_Encode`/`IsValid`/`Get*` deferred to the World model. Doc `Algorithms/Tools.md`. See History 2026-05.
- **Stat tables (World model start).** `HouseInfo` + `HouseID` (Contracts); `LandscapeInfo` + `LandscapeType`/`MovementType`; `ActionInfo` + `ActionType`/`SelectionType` — all in `DuneIIWorld/Stats/`, golden-verified field-for-field (`HouseInfoGoldenTests` 1 + `StatTableGoldenTests` 2; `houseinfo-`/`landscapeinfo-`/`actioninfo-golden.jsonl`). Doc `Algorithms/StatTables.md`. See History 2026-05.
- **Big stat tables — `ObjectInfo`+`UnitInfo`+`StructureInfo`.** Seam enums `UnitType`/`StructureType` (Contracts) + `DisplayMode`/`StructureLayout` (World); `ObjectInfo` shared embed with `ObjectInfo.Flags` OptionSet (13-bit bitfield); `UnitInfo` (27, `UnitInfo.Flags`) + `StructureInfo` (19), all `DuneIIWorld/Stats/`. Oracle dumper extended (`Golden_DumpObjectInfo`/`Golden_UnitInfo`/`Golden_StructureInfo` in `src/parity.c`) → `unitinfo-`/`structureinfo-golden.jsonl`; Swift tables generated by `Fixtures/.gen_tables.py`, verified by `UnitStructureInfoGoldenTests` (3, incl. hand-known spot-checks). 88 tests green. See History 2026-05.
- **Structure layout geometry + AI action table.** `StructureLayoutInfo` (7 layouts, consolidating `g_table_structure_layout*`) + `XYSize` + `ActionInfo.actionsAI` (`g_table_actionsAI`), `DuneIIWorld/Stats/`. Oracle dumper `Golden_StructureLayout`/`Golden_ActionsAI` → `structurelayout-`/`actionsai-golden.jsonl`; verified by `StatTableGoldenTests` (2 new). 90 tests green. See History 2026-05.
- **Model PODs.** `Object`/`ObjectFlags`/`ScriptEngine`/`Dir24`/`Unit`/`Structure`/`House`/`Team`/`MapTile` + enums/flags, `DuneIIWorld/Model/`, faithful value-type ports. `ObjectFlags`+`MapTile` bit layouts golden-pinned (`Golden_ObjectFlags`/`Golden_MapTile` → `objectflags-`/`maptile-golden.jsonl`); `ModelTests` (4). Design doc `Architecture/DataModel.md`. 94 tests green. See History 2026-05.
- **Object pools + `GameState` + pool-dependent `Tools_Index_*`.** `GameState` aggregate (`DuneIIWorld/State/`) + `Pool`/`PoolFind` (`Pool/`); faithful `_Find`/`_Allocate`/`Unit_Recount` ports and `Tools_Index_Encode`/`IsValid`/`Get*` on `GameState`. `GameStateTests` (7). 101 tests green. See History 2026-05.
- **Phase 3 loop foundation.** `Simulation` driver (`DuneIISimulation`): pause-aware two-clock `tick()` + four-phase order; `gameSpeed`/`paused`/unit tick cursors in `GameState`; `GameLoop_Unit` cadence (`advanceUnitCadence`/`UnitTickFlags`). Design `Architecture/SimulationLoop.md`; `LoopTests` (5). 105 tests green. See History 2026-05.
- **Primitive plan + Tier A tile motion.** Dependency-ordered native-primitive list added to `Plan.v1.md` §9; implemented `Tile32.centered`/`isOutOfMap`/`moveByDirection` (+ `_stepX` table), golden-tested (`Golden_TileMotion` → `tilemotion-golden.jsonl`; `TileMotionGoldenTests`, 3). 108 tests green. See History 2026-05.
- **Tier A.4 + Tier B/C unit primitives.** `Tile32.moveByRandom` (golden per-seed) + `DuneIISimulation` `UnitPrimitives`/`DefaultUnitPrimitives` (`setOrientation`/`rotate`/`setSpeed`, behaviour-tested). Resequenced `Unit_UpdateMap`/`Unit_Move`/`Unit_MovementTick` after Tier D in Plan §9. `UnitLogicTests` (3) + moveByRandom. 112 tests green. See History 2026-05.
- **Replaceable sim primitives + Tier D/E ready slice.** Converted primitives to injected protocols (`UnitPrimitives`/`MapPrimitives`/`HousePrimitives` + `Default*`, no static funcs — see memory `feedback-replaceable-primitives`); implemented `Map_IsValidPosition` + `House_AreAllied`, golden-tested (`maphouse-golden.jsonl`, `MapHouseTests`). 114 tests green. See History 2026-05.
- **Scenario/map/sprite-init design + sprite tile-ids.** `Architecture/ScenarioMapLoading.md` (full layer, dependency-ordered); `TileIDs` from `ICON.MAP` (`Sprites_Init`) in `GameState.tileIDs` + `IconMap.tileID(group:offset:)`; verified vs real `ICON.MAP` (`TileIDsTests`). 117 tests green. See History 2026-05.
- **Map-from-seed (`Map_CreateLandscape`).** `GameState.createLandscape(seed:iconMap:)` generates the exact landscape from a seed, bit-exact vs the oracle for every tile of every seed (`createlandscape-golden.jsonl`, `MapGeneratorTests`). The keystone for drawing. 118 tests green. See History 2026-05.
- **Scenario loader + `mapview` SpriteKit app.** `GameState.loadScenario` (`.INI` → GameState; `ScenarioLoaderTests`); `Apps/mapview` (`swift run mapview`) draws a scenario's terrain/structures/units via SpriteKit with a scenario picker + 1×–16× scale toolbar. 119 tests green. See History 2026-05.
- **Tier D #11 `Map_GetLandscapeType`.** `MapPrimitives.landscapeType` + `_landscapeSpriteMap`; golden vs the oracle over every generated tile (`LandscapeTypeTests`, via the `lst` grid in `createlandscape-golden.jsonl`). 120 tests green. See History 2026-05.
- **Tier D #13/#14 fog + spice.** `MapPrimitives.tileIsUnveiled` (`Tile_IsUnveiled`) + `isPositionUnveiled` (`Map_IsPositionUnveiled`) + `changeSpiceAmount` (`Map_ChangeSpiceAmount` + private `fixupSpiceEdges`), mutating `state.map`/`state.mapBaseTileID`. New oracle dumper `Golden_Spice` → `spice-golden.jsonl`; `SpiceTests` reproduces a whole-map ±1 pass bit-for-bit, `UnveilTests` spot-checks the fog boundary. 130 tests green. See History 2026-05.
- **Tier-F pool queries + Tier D `Map_SearchSpice`.** `GameState.unitGetByPackedTile` (`Unit_Get_ByPackedTile`) + `unitIsTypeOnMap` (`Unit_IsTypeOnMap`) on the World pool layer (`GameStateTests.mapQueries`); `MapPrimitives.searchSpice` (`Map_SearchSpice`) golden vs the oracle (`Golden_SearchSpice` → `searchspice-golden.jsonl`, found + not-found cases; `SearchSpiceTests`). 132 tests green. See History 2026-05.
- **Tier-E #16 `Unit_GetTileEnterScore`** (the pathfinder's per-tile cost function). `UnitPrimitives.tileEnterScore` + `isValidMovementIntoStructure` in `DefaultUnitPrimitives` (`unit.c:2335`/`:660`), composing the existing map/house primitives + the new World pool query `GameState.structureGetByPackedTile` (`Structure_Get_ByPackedTile`). Pinned `g_dune2_enhanced = false` in the oracle's `Parity_DumpGolden` (no-op for existing fixtures) to match the 1.07 target. Golden over the landscape path (`tileenterscore-golden.jsonl`, 648 rows; `TileEnterScoreGoldenTests`), decision-trace over the occupant/structure branches (`UnitMovementDecisionTests`, 10 cases). Doc `Algorithms/UnitTileEnterScore.md`. 143 tests green. See History 2026-05.
- **Tier-F pool `_Free`.** `GameState.unitFree`/`structureFree`/`teamFree` (`pool/*.c`) + `ScriptEngine.reset` (`Script_Reset`): flags-clear, find-array compaction, house-count decrement, slab/wall specials skip the find array. `GameStateTests` (3). 146 tests green. See History 2026-05.
- **Tier-F reference-clearing cluster.** `GameState.unitUntargetMe` (`unit.c`) + helpers `objectScriptVariable4Set`/`Clear` (`object.c`, incl. the incoming-busy structure state flip), `structureSetState`/`structureGetLinkedUnit` (`structure.c`), `unitRemoveFromTeam` (`unit.c`) — World-layer bookkeeping in `GameState+Lifecycle.swift`. `GameStateLifecycleTests` (4). 150 tests green. See History 2026-05.
- **Behavioural scenario harness — foundation `DuneIIScenarios`** (new lib; design `Architecture/ScenarioHarness.md`). Deterministic 8×8 sand/rock terrain (seeded), the 5 scenario definitions (moving/close-/far-attack/guarding/move-around-building), `ScenarioBuilder` (lays out terrain + 2 units + actions + optional building into a `GameState`), `ScenarioRunner` (ticks scripts, captures per-tick `UnitSnapshot`s). `ScenariosTests` (7). Units stay static until movement/combat natives land. **Next: the `scenariolab` macOS app (visual, zoom), then the OpenDUNE scenario-golden harness.** 203 tests green. See History 2026-05.
- **EMC end-to-end on real data.** `RealUnitScriptTests` bridges the committed `UNIT.EMC` → `ScriptInfo`, `SetAction`s a tank (loads its real script), and runs it under `UnitScriptRunner` — **a real Dune II unit script now executes under the engine** (halts gracefully at the first unported native). 196 tests green. See History 2026-05.
- **EMC integration — op-14 dispatch glue + script run loop (`UnitScriptRunner`).** `dispatch` routes a function index → ported native (args via `ScriptEngine.peek`); `run(slot:in:budget:)` loops `Script_Run` (copy-out/write-back to avoid `GameState` inout-aliasing), stopping on delay/halt/unported. `UnitActions.setAction` refactored to take the engine separately (+ standalone overload). **VM + dispatch + natives now run a unit script end-to-end** (`UnitScriptRunnerTests`, 4). 195 tests green. See History 2026-05.
- **Tier-F #19 core — `Unit_SetAction`.** `DuneIISimulation/UnitActions.swift` (`UnitActions`, holds injected `ScriptInterpreter`): `setAction` switches a unit + (re)loads its EMC script (switchType 0 queue/immediate, 1 immediate, 2 subroutine; dying/INVALID guards). `UnitActionsTests` (4, synthetic per-type `ScriptInfo`). 191 tests green. See History 2026-05.
- **EMC native script functions — `Script_Unit_*` (11) + `getInfo` + `Unit_FindClosestRefinery`.** `DuneIISimulation/Script/Natives/UnitScriptFunctions.swift` (holds injected `UnitPrimitives`): `getAmount`, `getOrientation`, `setSpeed`, `setOrientation`, `rotate`, `stop`, `isInTransport`, `getRandomTile`, `setTarget`, `setDestinationDirect`, and the 20-field `getInfo`; plus the World helper `GameState.unitFindClosestRefinery`. `UnitScriptFunctionsTests` (12 multi-case). 187 tests green. See History 2026-05.
- **EMC native script functions — first batch (`Script_General_*`).** `DuneIISimulation/Script/Natives/GeneralScriptFunctions.swift`: 12 category-independent natives as **clean explicit-parameter functions** (no stack-poking in the logic — per memory `feedback-script-natives-clean-functions`; a thin VM-glue will pass stack args). Each heavily tested (`GeneralScriptFunctionsTests`, 11 multi-case tests; RNG/distance cross-checked vs the golden primitives). 175 tests green. See History 2026-05.
- **EMC script VM core** (big Phase-3 unlock). `DuneIISimulation/Script/`: `ScriptInfo` (program words + per-type offsets, bridged from `Emc.Program`) + replaceable `ScriptInterpreter`/`DefaultScriptInterpreter` = faithful `Script_Run` (one opcode/step) + stack ops + `Load`/`LoadAsSubroutine`/`Reset`/`IsLoaded` (`script.c`). All 19 opcodes, 3 param encodings, call/return frame, signed-16 wrapping, op-14 FUNCTION via injected closure. NULL `script` ptr → `ScriptEngine.scriptPC == scriptNull` (`0xFFFF`); `reset()` nulls the PC, default engine not-loaded. Doc `Algorithms/ScriptVM.md`. `ScriptVMTests` (11). 164 tests green. See History 2026-05.
- **Tier-F #20 `Unit_Remove`** (+ the map/visibility cluster). `GameState.unitRemove` (`unit.c`): untarget → `unitUpdateMap(0)` → `unitHouseUnitCountRemove` → script reset → `unitFree`. Brought in `unitRemoveFromTile`, `unitHouseUnitCountAdd`/`Remove`, and `unitUpdateMap(type:slot:)` (headless state subset of `Unit_UpdateMap`). Refactored `House_AreAllied` → canonical `House.areAllied(_:_:playerHouseID:)` (World) that the Simulation primitive + lifecycle delegate to. **Seams:** `unitHouseUnitCountAdd`'s player-alert block (audio/GUI/`selectionType`) + ambush→`Unit_SetAction(HUNT)` (needs EMC VM); air-redraw/render-dirty/fog-radius in `unitUpdateMap`; `Unit_Select` deselect. `GameStateLifecycleTests` (+3). 153 tests green. See History 2026-05.

## Test status

`cd Code && TMPDIR="$PWD/.build/tmp" xcrun swift test --disable-sandbox`: **203 tests, all green** (format/codec/EMC/IconMap + PNG/WAV export + renderer services + **golden parity vs OpenDUNE** (RNG, tile geometry, game-speed, index, stat tables incl. unit/structure info + structure layout + actionsAI, ObjectFlags/MapTile bit layouts) + model-POD defaults + pool/index/map-query behaviour + sim loop/clock/cadence + tile motion + unit rotation/speed + map/house primitives + landscape type + fog unveil + spice change/search + tile-enter score + sprite tile-ids + map-from-seed + scenario load, synthetic + real-data). Clean build (`swift package clean` then recreate `.build/tmp`, then `swift build`): zero warnings (full output audited, incl. the `rendertest` product). OpenDUNE oracle: `cd Repositories/OpenDUNE && PATH="$PWD/.shim:$PATH" make -j4` → `./bin/opendune` (the `.shim/gcc`→`xcrun clang` shim is required here; see the build/oracle quirk note above).

## Open decisions (from Plan.v1.md §8)

1. Package layout — proceeding with `Code` single-package multi-target as planned (swift-tools 6.3, macOS 26). Adjust if desired.
2. `assetgen` + `Resources/` regenerate fresh in Phase 1 (planned).
3. OpenDUNE oracle tooling stood up in Phase 1–2.
4. Multi-window UI tech (Catalyst `UIScene` vs AppKit) — decide at Phase 6.

## Self-review tracker

Baseline tag: `selfreview-phase1` (set at the Phase 1 commit). Commits since = `git rev-list --count selfreview-phase1..HEAD`. Trigger: each completed phase, or every 32 commits. See CLAUDE.md → "Periodic self-review." Phase 0 review: nothing recurrent (first phase). Phase 1 review (done): recurring issue = build warnings hidden by `tail`; strengthened CLAUDE.md step 5 (read full build output) and captured insight `build-swiftpm-unhandled-files`. **Phase 2 review (done, at commit `e1e3a65`):** recurring friction = the sandbox EPERM-on-exec / out-of-repo-write problem rediscovered each session; captured insight `build-exec-eperm` (commands also in this file's task header). Nothing else recurrent — the golden-dump→generated-table and oracle-bitfield-pinning patterns are working and already documented in `Algorithms/StatTables.md` / `Architecture/DataModel.md`.
