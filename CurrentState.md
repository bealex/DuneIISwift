# Current state

This file is the single source of truth for "what's happening right now." Read it first when a session starts — it tells you where the previous session stopped, what to do next, and what's already done so you don't redo it. Update it at the end of every task so the next session can pick up cleanly.

## How to use

1. **At session start:** read this file before anything else. It tells you the active task and the immediate next step.
2. **Before each task:** put the task under "Active task" with enough detail that a cold reader can continue it.
3. **After each task completes:** move it to "Recently completed" (with a test count and a pointer to the history entry), update "Active task" to the next thing, refresh "Next up" if priorities shifted, and update "Test status."
4. **Never use this file for long-term design notes.** Those belong in `Documentation/Plans/`, `Documentation/Architecture/`, or the appropriate algorithm doc. This file is short, operational, and lossy — rewritten often.

## Active task

**🎯🎯 SAVE007 achieves FULL 1000-tick byte-for-byte parity with OpenDUNE under real UNIT.EMC / BUILD.EMC / TEAM.EMC, plus `wobbleIndex` in the diff schema.** This is the §6 closure milestone, plus one parity-schema widening on top. Five fixes shipped today: (1) inline per-unit `fireDelay--`; (2) `Script_General_SearchSpice` slot 0x29; (3) `findSpiceNear` tie-break `<` → `<=`; (4) `Unit_Move` `targetMove` auto-clear on arrival; (5) `compareUnit` widened with `wobbleIndex`. Bonus: SAVE001 also matches its full 200-tick golden now.

**Three follow-up frontiers surfaced while scouting deeper**, all deferred — the §6 milestone holds:

1. **Tick 3011 `u39.targetMove=54699 vs 54441`** (5000-tick golden, not committed). Both engines have u39 at the same position; SearchSpice returns different tiles. Spice maps differ at u39's current tile (20, 20) — OpenDUNE has drained it, Swift hasn't. Almost certainly accumulated spice-map state divergence over the 3000-tick horizon that the current schema can't see. Investigation needs a map-tile parity dump.

2. **Tick 11 `u37.orientation1Target=32 vs 0`** (SOLDIER, non-turreted). RESOLVED at the schema level by gating `orientation1` compare on `hasTurret` — `Unit_Rotate` only advances `orientation[1]` for turreted units (`src/unit.c:221`), so non-turreted unit orientation[1] is dead state. The underlying `Script_Unit_SetTarget`-on-non-turreted SetOrientation(level: 1) nuance remains for future investigation if anything else surfaces.

3. **Widen `compareHouse` with `unitCount` / `unitCountMax` / `harvestersIncoming`.** These fields exist in the save record's `Players.Slot` decoder but aren't on `HouseSlot` yet. Adding them needs full `Unit_HouseUnitCount_Add/Remove` ports + maintenance from `Unit_Allocate` / `Unit_Free` / `Unit_Recount`. Larger surface area than the other two follow-ups.

**Next step**: pick (1) — write a `Map_GetLandscapeType` parity-dump extension to OpenDUNE's `parity.c` so we can diff per-tile spice levels. Then walk back from tick 3011 to find the first cell that drifts.

**Earlier this session — RNG / LCG trace diagnostics shipped (preserved for context)**: three new fixes closed the tick-551 / tick-581 drifts: (1) `LST_STRUCTURE → LST_CONCRETE_SLAB` remap in `makeCalculateRouteUnit` (`src/unit.c:1088..1089`) — foot units walking across structure tiles now read concrete-slab speed 255 instead of 0; (2) OpenDUNE's `Object_GetDistanceToEncoded` edge-tile distance semantics ported as `Pos32.distance(from:toEncoded:host:)` + `Tools_Index_GetTile` layout-centre semantics as `Pos32.targetTile(encoded:host:)` — a SOLDIER firing at a 2x2 CYARD now reads the edge-adjusted distance (491 vs 555 to centre), and Fire's orientation-diff gate sees the layout-adjusted centre (7936, 6656) not the raw anchor (7680, 6400); (3) `isWobbling = letUnitWobble` enhanced clear (`src/unit.c:1097..1101`, `g_dune2_enhanced=true` default path) — canWobble units that step off rock now stop wobbling, eliminating the ~1-byte-per-step Tools_Random_256 drift.

**Tick 586 frontier** — enemy SOLDIER u37 fired bullet u12 at tick 581. Bullet travels NE toward the player CYARD. At tick 586 OpenDUNE's bullet detonates and CYARD takes `damage=3` (hp 400 → 397). Swift's CYARD still shows hp=400 — either the bullet didn't arrive, arrived at the wrong tile, or the detonation pass skipped the structure-damage path.

**Next step**: trace u12's per-tick position + currentDestination 581..586 and compare to OpenDUNE. Likely the bullet's `currentDestination` targets the structure's raw anchor (7680, 6400) rather than the layout-adjusted centre (7936, 6656) — so when it arrives it lands on a tile adjacent to (but not overlapping) the CYARD and either misses the impact-radius check or doesn't call `applyStructureDamage`. Candidate fix: `Units.createBullet` (BULLET arm, lines 308..) writes `bullet.currentDestinationX/Y = targetPos.x/y` — confirm `targetPos` is `Pos32.targetTile` (layout-adjusted), not `Pos32.of` (raw). If already correct, the issue is in the arrival / detonation pass in `Scheduler.tickMovement`.

**LCG trace parity shipped earlier this session**:
- `Scripting.RandomSource.onLCGDraw((UInt16, String) -> Void)?` + `lcgRange(_:_:)` helper + all 4 LCG call sites tagged (`RandomRange`, `IdleAction.gate`, `ExplosionMultiple.central` / `.scatter`).
- `Simulation.ParityHarness.LCGTrace` writer + `saveSevenParityDump200TickLCGStream` test at `tmp/swift_lcg_trace_200.txt`.
- OpenDUNE side: `--parity-lcg-trace=<path>` flag in `parity.c` + `Tools_RandomLCG_Range` write hook in `src/tools.c`, tagged with `g_scriptCurrent{Unit,Structure,Team}`.
- **First divergence discovered**: Swift missed OpenDUNE's tick=1 `ctx=s2 value=0` draw. Root cause `tickStarportAvailability`: used `Tools_Random_256` (via `harvestRNG`) instead of OpenDUNE's `Tools_RandomLCG_Range(0, UNIT_MAX-1)` per `src/house.c:105`; modulo-counter cadence (300-tick) never fired at tick 1, while OpenDUNE's `s_tickHouseStarportAvailability = 0` gate fires immediately at tick 1 then every 1800 ticks.
- **Fix**: `Scheduler.lcgRange: ((UInt16, UInt16) -> UInt16)?` closure (parity harness wires to `source.lcgRange`; gameplay falls back to `harvestRNG`); new `nextStarportAvailabilityGate: UInt32 = 0` + `openDUNEStarportAvailabilityReschedule = 1800` activated under `perTickCadenceGatesEnabled`.
- **Result**: Swift + OpenDUNE LCG traces across 200 ticks are byte-identical (same tick, idx, value — only ctx labels differ since they're engine-specific). Tick 166 u0 `orientation0Target=43 vs 0` closed; RandomRange(0,128) at pc=1977 now returns 43 matching OpenDUNE.

Earlier history (RNG byte-stream work — pre-interleaving — preserved for cross-reference): `saveSevenParityDump200TickRNGStream` test writes `tmp/swift_rng_trace_200.txt` for the first 151 ticks; OpenDUNE binary re-dumped to `tmp/opendune_rng_trace_200.txt` (200 ticks, via `Repositories/OpenDUNE/bin/opendune --parity-random-trace=... --parity-ticks=200`). Byte-stream diff shows: **Swift and OpenDUNE produce the SAME byte sequence** (deterministic RNG) — but the **CALLERS attributing each byte differ**. E.g., at tick 1:

- OpenDUNE idx=0..9 attributed to: NULL, u22/u23/u24/u29/u32/u33/u34/u35 (DelayRandom from each unit's script), then idx=9 to u36 wobble.
- Swift idx=0..9 attributed to: wobble u36 (FIRST), then an untagged draw, then DelayRandom u22..u35.

This is because **OpenDUNE's `GameLoop_Unit` interleaves per-unit { movement + wobble, rotation, script }** inside a single `Unit_Find` loop (`src/unit.c:175..306`), while **Swift's `Scheduler.tick()` runs `tickMovement()` as a batch over all units BEFORE `tickUnits()` dispatches any script**. So Swift's u36 wobble draw (from the movement pass) fires before any unit script's DelayRandom, inverting the order.

The byte-stream is still identical, so most ticks happen to produce the correct per-unit state. At tick 151 the accumulated ordering shift causes u39's Harvest.bump to read the byte OpenDUNE's `ctx=u39 Harvest.drainRoll` would read (0x14, LSB=0 → no bump), producing `amount=11` instead of 12.

Fix requires Swift's scheduler to adopt OpenDUNE's per-unit-interleaved structure — a significant refactor flagged as queue item "(b) rewrite as a single per-unit loop with inline script/movement/rotation branches" earlier in CurrentState. Next session's shape: introduce `tickPerUnit(findIndex:)` in `Scheduler` that runs movement/rotation/sprite/script for one unit, then replace `tickMovement()` + `tickRotation()` + `tickSpriteOffsets()` + `tickUnits()` call chain with a single findArray loop invoking it. Around 150+ tests depend on the batch ordering — expect broad test adjustments.

Diagnostic infrastructure shipped this session:
- `ParityHarness.ScriptTrace` — opcode-level trace writer per unit.
- `saveSevenParityDumpU0ScriptTrace` + `saveSevenParityDump200TickRNGStream` — regenerate `tmp/swift_*_trace_*.txt` for diffing against OpenDUNE.
- `movementRNG` closure signature changed to `((Int) -> UInt8)?` so the parity harness tags each wobble draw with its owning unit (e.g. `ctx=wobble u36`).

**Prior (shipped later 2026-04-24 session):**
- Added `ParityHarness.ScriptTrace` — opcode-level trace writer mirroring OpenDUNE's `--parity-script-trace` line format. `saveSevenParityDumpU0ScriptTrace` test regenerates `tmp/swift_u0_script.txt` on demand.
- `Script_Unit_GetInfo` now covers cases 0x09 (movingSpeed), 0x0A (body rotation delta), 0x0C (fireDelay==0), 0x0F (byScenario), 0x11 (turret rotation delta, falls back to body for now), 0x13 (seenByHouses). Was missing 6 cases; u0's carryall flight script called GetInfo(0x0F) at pc~1957 and got 0 where OpenDUNE returned 1, shifting the carryall's whole downstream state by tick 101.

**Historical diagnostic narrowing (preserved for context; was the map that led to the GetInfo fix):**

The tick-151 drift *before* the GetInfo fix cascaded via u0 — diagnostic narrowing via temporary skip-list in `ParityHarness.compareUnit` showed:

- skip `amount` → next drift tick 166 `u0.orientation0Target=43 vs -15`
- skip `orientation0Target/Speed` too → tick 167 `u0.orientation0Current=-3 vs -15`
- skip all u0 orientation0 → tick 171 `u0.movingSpeed=255 vs 200`
- skip u0 speed fields → tick 172 `u0.positionX=5813 vs 5720`
- skip u0 position → tick 196 `u26.fireDelay=50 vs 51` (tiny off-by-one, clean far-field)

Conclusion: **every drift between ticks 151 and ~195 is the same u0 carryall script divergence** — Swift's carryall reached `orientationTarget=-15` by tick ~140 (rotation completed, `orientationSpeed=0`), but OpenDUNE's script at tick 166 fires a new `Unit_SetOrientation(u, 43, false, 0)` + a jump to pc=1981. Swift's script never executes that opcode, so the carryall stays facing -15, never accelerates to speed 255 (OpenDUNE did this at tick 96), and lags behind. The RNG stream differential from u0 running different opcodes cascades into u39's tick-151 harvest bump.

OpenDUNE's u0 trace (from golden):
- tick 80..95: pc=1117 (loop, speed=200/9), `orient0=(126,126,0)`, flying south-east.
- tick 96: pc=1968 (+851 jump), speed bumped to 255/12.
- tick 96..100: pc=1968.
- tick 101: pc=2014, `orient0=(126,-15,12)` (new target via SetOrientation or SetDestinationDirect), `delay=12`.
- tick 101..165: pc=2014, delay counts down, rotation walks current 126 → -15.
- tick 166: pc=1981 (back-jump), `orient0=(-15,43,12)` (new target fired again).

Candidate roots for next session: (a) a script function we haven't ported that normally advances pc from 1117 → 1968 at tick 96 — likely `Script_Unit_MoveToTarget` (slot 0x16) which currently only returns 0/1 instead of running OpenDUNE's smooth-close logic. (b) `Script_Unit_Rotate` or `Unit_SetOrientation` pc flow. Next concrete step: add diagnostic printouts to `ParityHarness.runAgainst` that log `(tick, unit[0].scriptPc, scriptDelay, orientation0*)` every tick, then compare against the golden's tick 80..170 u0 dump (already captured above). First tick where Swift's pc trajectory deviates is the root cause.

**Prior (shipped 2026-04-24 session):**
- Tick 101 `unit[0].orientation0Current: expected=126 actual=-15` closed by rewriting `Script_Unit_SetDestinationDirect` byte-exact against `src/script/unit.c:918..936`. Removed the spurious `targetMove` write, switched from `orientationCurrent = ...` snap to `Units.setOrientation(rotateInstantly: false)`, and gated the `currentDestination` write on `currentDestination == (0, 0)` (carryall semantics — `isNormalUnit` flag not yet tracked but no non-normal non-carryall caller hits this slot today).
- Tick 76 `unit[37].spriteOffset: expected=7 actual=8` closed by reordering `Scheduler.tick()` — moved `tickSpriteOffsets()` to run AFTER `tickMovement()` + `tickRotation()`, matching OpenDUNE's per-unit `src/unit.c:199..287` ordering. Previously Swift bumped sprite pre-movement, so on arrival ticks it saw `speed != 0` (not yet cleared) and added one extra frame.
- Tick 70 `house[1].credits: expected=304 actual=305` closed by porting OpenDUNE's `tickPowerMaintenance` cadence. Added `powerProduction` + `powerUsage` to `HouseSlot`, loaded from save via `WorldSnapshot`, wired `Scheduler.nextHousePowerMaintenanceGate = 70` (initial fire seeded by `opendune.c:1503`'s `max(g_timerGame + 70, saved)`), implemented `tickHousePowerMaintenance()` per-house drain `min(credits, (powerUsage / 32) + 1)` from `src/house.c:270..273`. Flipped `ParityHarness.compareHouse` to assert `powerProduction` + `powerUsage`. Two new scheduler tests pin the cadence and the drain formula.
- Tick 56 `unit[28].spriteOffset: expected=11 actual=6` closed by rewriting `makeIdleActionUnit` byte-exact against `src/script/unit.c:1748..1776`. Our earlier port drew the gate roll from Tools_Random_256 instead of `Tools_RandomLCG_Range(0, 10)`, fabricated a sandworm branch that doesn't exist in OpenDUNE, and wrote `spriteOffset` unconditionally for GUARD infantry instead of only when the LCG rolls `> 8` (2/11 chance). Deleted the invented `idleActionSpawnChance` helper. Two new IdleActionTests (`footSpriteOffset`, `footSpriteOffsetGateClosed`).
- Tick 51 `unit[12].actionID: expected=3 actual=7` closed by fixing `Units.createBullet` to match `Unit_Create`'s `Unit_SetAction(u, player ? actionsPlayer[3] : actionAI)` branch with the `ACTION_INVALID` early-return. Three pieces: added `ActionID.invalid = 0xFF`; flipped `UnitInfo.table` rows 18..26 (bullets / missiles / sandworm / frigate) to `actionAI = .invalid`; introduced `Units.resolvedInitialAction(info:houseID:host:)` helper + `host.playerHouseID` auto-wire in `ParityHarness.runAgainst` (CYARD-owner heuristic). Fresh AI-spawned bullets now keep the `ACTION_GUARD` default from `Unit_Create`'s `src/unit.c:425`; fresh player-spawned bullets get `actionsPlayer[3] = STOP`.
- Tick 41 `unit[38].orientation0Target: expected=15 actual=0` closed by wiring `host.tileEnterScore` in `ParityHarness.runAgainst` with a `Unit_GetTileEnterScore` port (playable-rect check from `game.info.scenario.mapScale`; landscape-speed lookup; diagonal penalty; res^0xFF invert). u38 (SOLDIER, HUNT) was stranded at tile (17, 62) outside the playable rect; with a proper scoreFn every adjacent tile returns 256, pathfinder bails, route[0] stays 0xFF, `Script_Unit_CalculateRoute` clears targetMove without hitting the orientation gate.
- Tick 41 `unit[38].route1: expected=0 actual=255` (follow-up on the same unit, same tick) closed by matching OpenDUNE's `memcpy(u->route, res.buffer, min(res.routeSize, 14))` — only overwrite `route[0..copyCount-1]`; leave the tail at its saved values. Was previously writing 0xFF to every route slot beyond the pathfinder's result.
- `retargetImpassableDst: Bool = true` added to `Pathfinder.findRoute` + `Scripting.Host`. Default preserves gameplay (HUNT units slide to the nearest passable neighbour so they don't halt one tile short of a CYARD); parity harness flips it false to match OpenDUNE's `Script_Unit_Pathfinder`, which never retargets. New `PathfinderTests.findRouteParityNoRetarget` pins the behaviour.

**Prior (shipped 2026-04-23 sessions):**
- Tick 31 `unit[36].movingSpeed: expected=0 actual=112` closed by moving `setSpeed` to AFTER the orientation gate in `makeCalculateRouteUnit` (matches `Unit_StartMovement` order — setSpeed is *inside* StartMovement, which only runs after orientation aligns). The old eager setSpeed was clobbering `movingSpeed=0` to `movingSpeed=112` on every CalcRoute call while the unit was still rotating. Fixed PathfinderTests (two) that depended on the eager-speed behaviour by pre-orienting the test unit to route[0] direction.
- Tick 31 `unit[36].orientation0Current: expected=0 actual=-32` closed by replacing the direct `orientationCurrent = desired` snap in `makeCalculateRouteUnit` with `Simulation.Units.setOrientation(rotateInstantly: false)` — mirrors `Script_Unit_CalculateRoute` (`src/script/unit.c:1331..1334`). The old snap was instantly rotating u36 NW (-32) when OpenDUNE gradually rotated across multiple ticks.
- Tick 31 `unit[13].speedRemainder: expected=255 actual=0` closed by adding wobble-byte draw to `tickMovement`: new `UnitSlot` fields `wobbleIndex: UInt8` + `isWobbling: Bool` + `UnitInfo.canWobble(type:)` lookup; `makeCalculateRouteUnit` sets `isWobbling=true` when entering a `letUnitWobble` landscape (port of `src/unit.c:1098..1100`); `tickMovement` step block draws `Tools_Random_256() & 7` into `wobbleIndex` when `canWobble && isWobbling` via a new `Scheduler.movementRNG` closure wired from the parity harness.
- `makeIdleActionUnit` rewritten to match OpenDUNE byte-exactly (was drawing from `lcg.range(0,10)` instead of `Tools_Random_256`). First byte always drawn; sandworm/INFANTRY-GUARD branches; `spawnChance` gate via `idleActionSpawnChance(type:)` (0 for most, 64 for ground vehicles, 128 for harvester). Updated `IdleActionTests.swift:footSpriteOffset` to set `actionID=GUARD` and pick a `toolsSeed` where the second draw has `& 0x3F != 0`.

### Session progress (2026-04-23 evening)

Advanced frontier tick 31 → 34 with five fixes on top of the earlier tick 25 → 31 batch:

1. Catch-up movement/rotation pass for mid-tick-allocated bullets (separate commit `b0d3eaf`).
2. `Script_Unit_IdleAction` byte-exact RNG port.
3. Wobble-byte tracking + draw (`isWobbling`, `wobbleIndex`, `canWobble`, landscape trigger, `movementRNG` closure).
4. `Script_Unit_CalculateRoute` orientation gate now delegates to `Units.setOrientation(rotateInstantly: false)` instead of snapping `orientationCurrent`.
5. `setSpeed` moved after the orientation gate (matches `Unit_StartMovement` order).

**Test status**: 983/97 green, 0 warnings. Two `PathfinderTests` pre-orient their unit to route[0] direction now (post-fix the test scheduler doesn't run `tickRotation`, so orientation stays until tickRotation completes — tests need the route direction to match initial orientation).

**Uncommitted since `b0d3eaf`**. Ready for a single commit capturing all five fixes.

**Prior (shipped this session):**
- Tick 31 `unit[13].speedRemainder: expected=255 actual=0` (new bullet) closed via `tickMovement/tickRotation(fromFindArrayIndex:)` catch-up pass after `tickUnits`. In OpenDUNE, `Unit_Find` iterates `g_unitFindArray` — a dynamic array that `Unit_Allocate` appends to — so newly-created bullets get their first `Unit_MovementTick` in the same `GameLoop_Unit` call. Swift's separate passes didn't replicate this; the catch-up pass snapshots `findArray.count` pre-`tickUnits` and processes the tail post-`tickUnits`. `Scheduler.swift` lines ~804-822.

**Root cause**: per-unit single-pass ordering mismatch. OpenDUNE: per-unit { script; movement; rotation; … } in one `Unit_Find` loop. Swift: three separate passes (`tickUnits`, `tickMovement`, `tickRotation`) over a pre-computed `findArray`. Solutions: (a) recount after `tickUnits` and have `tickMovement` pick up new units — quick fix but doesn't match OpenDUNE's per-unit ordering; (b) rewrite as a single per-unit loop with inline script/movement/rotation branches — correct but large refactor; (c) manually run `tickMovement` for any unit allocated during script dispatch, within the same tick. **Recommended next step**: try (a) first — after `tickUnits`, call `host.units.recount()` so `tickMovement` sees the new bullets.

Post tick-31 queue: `SAVE001 tick-1 harvester drift (unit[22].actionID=6 vs 5)` still open (same root as earlier — harvester transitions RETURN prematurely).

### Session progress (2026-04-23 PM)

Advanced frontier tick 1 → tick 28 → tick 31 with three wins:

1. **Script_Unit_Rotate ported (slot 0x3D)** — was nil (returned 0). u30 TRIKE has `orientation[0].speed=-8` at tick 26; without Rotate's "already rotating" gate (return 1), the script fell through to Script_Unit_Fire and fired. Port at `Functions.swift` `makeRotateUnit(host:)` mirrors `src/script/unit.c:726`. Wired in `FunctionTables.swift:127`. Closed tick 28 `unit[30].fireDelay=2 vs 0`.

2. **Turret orientation tracking** — `UnitSlot` got `turretOrientationCurrent/Target/Speed: Int8`. `WorldSnapshot` loads `slot.orientation[1]` when `slot.orientation.count > 1`. `Units.setOrientation` now handles `level=1` (was no-op). `Scheduler.tickRotation` runs turret rotation when `hasTurret == true` (mirrors `src/unit.c:220-221`). `makeSetTargetUnit` `setOrientation(level:1)` call now actually writes.

3. **Arrival logic rewrite** — `UnitSlot.distanceToDestination: UInt16 = 0x7FFF`, loaded from save (already in `SaveUnits.swift`). `Script_Unit_CalculateRoute` resets it to `0x7FFF` when setting `currentDestination` (port of `src/unit.c:1082` `Unit_StartMovement`). `tickMovement` post-step arrival is now gated on `perTickCadenceGatesEnabled`:
   - **Parity mode** (`true`): `slot.distanceToDestination < distAfter || distAfter < 16` (mirrors `src/unit.c:1419`). On arrival: `slot.speed = 0; slot.speedPerTick = 0; slot.movingSpeed = 0` (NOT `speedRemainder` — `Unit_MovementTick` overwrites that AFTER `Unit_Move` returns); clear `currentDestination`; do NOT pop route (script's CalculateRoute pops on next dispatch).
   - **Gameplay mode** (`false`, tests): old logic — `distAfter >= distBefore || distAfter < 16`, inline-pop route, optional targetMove clear. Keeps movement tests passing.
   The pre-move arrival (`distToGoal <= 16` before step) is also gated: projectile detonation always; ground-unit arrival only in gameplay mode. `slot.distanceToDestination = distAfterU16` is written at end of step (parity mode uses it next tick).
   Closed tick 28 `unit[36].movingSpeed=160 vs 0`.

4. **Test touch-up** — `FireTests.swift:90` assertion flipped from `bullet.targetAttack == unit(30).raw` to `== 0`. BULLET arm in OpenDUNE (`src/unit.c:2003`) doesn't set `targetAttack` — the previous session's `createBullet` fix removed the write but left the old test assertion.

**Key file changes this session**:
- `Code/Core/Sources/DuneIICore/Simulation/UnitPool.swift` — added 3 turret fields + `distanceToDestination`.
- `Code/Core/Sources/DuneIICore/Simulation/WorldSnapshot.swift` — loads orientation[1] + distanceToDestination from save.
- `Code/Core/Sources/DuneIICore/Simulation/Units.swift` — `setOrientation` handles level=1.
- `Code/Core/Sources/DuneIICore/Simulation/Scheduler.swift` — `tickRotation` handles turret; `tickMovement` arrival gated on `perTickCadenceGatesEnabled`.
- `Code/Core/Sources/DuneIICore/Scripting/Functions.swift` — `makeRotateUnit` added; `makeCalculateRouteUnit` resets `distanceToDestination = 0x7FFF`.
- `Code/Core/Sources/DuneIICore/Scripting/FunctionTables.swift` — wired 0x3D.
- `Code/Core/Tests/DuneIICoreTests/FireTests.swift` — targetAttack=0 for BULLET.

**Uncommitted. Test status: 983/97 green, 0 warnings on clean build.**

### Next step for session resume

Try the quickest fix for tick 31: call `host.units.recount()` after `tickUnits` in `Scheduler.tick()` so the new `findArray` includes bullets allocated during script dispatch, then `tickMovement` picks them up. If that changes too much behavior (bullets process movement immediately vs. via the per-unit `Unit_Find` sequence OpenDUNE uses), fall back to explicitly running `tickMovement` for just the newly-allocated bullets. The allocation points are in `Units.createBullet` (called from `makeFireUnit`). Consider tracking `host.units.allocatedThisTick: [Int]` to run movement for only those.

**Before investigating tick 31 drift further**: verify by dumping u13 state in Swift at tick 31 to confirm it IS allocated (just with `speedRemainder=0`). If it's NOT allocated, the root cause is different (Swift's u26 didn't fire — check Script_Unit_Rotate orientation convergence for u26 across ticks 28-31).

Trace files (gitignored in `tmp/`):
- `tmp/opendune_rng_trace.txt` — regenerated via rebuilt OpenDUNE binary at `Repositories/OpenDUNE/bin/opendune --parity-load=_SAVE007.DAT --parity-dump=tmp/save007_dump.jsonl --parity-ticks=1 --parity-data-dir=Repositories/patched_107_unofficial --parity-random-trace=tmp/opendune_rng_trace.txt`.
- `tmp/swift_rng_trace.txt` — regenerated by running `swift test --filter saveSevenParityTickOneDumpRandomStream`.

Swift trace currently shows (contexts added):
- idx 0–2: `DelayRandom u22 / u23 / u24`
- idx 3–4: `Harvest.bump u39` / `Harvest.drainRoll u39`  ← fires here
- idx 5–9: `DelayRandom u29 / u32 / u33 / u34 / u35`

Findings / open questions:
- OpenDUNE must process u25–u28 *before* u39 reaches Harvest, drawing 4 bytes their Swift counterparts don't.
- Candidate causes: (a) off-viewport 3-opcode cap mis-firing for units with a saved mid-script PC; (b) our script engine resume state differs from OpenDUNE's post-save-load state in a way that skips the pre-Harvest RNG-drawing opcodes; (c) some unit on OpenDUNE's side calls multiple RNG-consuming opcodes (IdleAction = up to 3 bytes) that Swift's port short-circuits.
- Next step: tag `IdleAction.sprite / turret / newOrient`, `ExplosionMultiple`'s `Pos32.movedRandomly`, and any other `source.toolsNext()` sites with unit index (IdleAction already done); rerun Swift trace and see which units contribute which functions. Then script-trace OpenDUNE for u25 on tick 1 (`--parity-script-trace=... --parity-script-unit=25`) and compare against Swift's opcode trace for u25.

**Prior (shipped 2026-04-23):**
- Harvester return-path "diagonal not 45°" bug fixed via octant-snap in `tickMovement` fallback-slide orientation write — matches OpenDUNE's `Unit_StartMovement` `(current + 16) & 0xE0`. 1 new `MovementSelfHealTests` regression.
- Player orders rejected outside `playableRect` — `ScenarioRuntime.isInPlayableRect(tileX:tileY:)` gates 4 move-style order sites.

---

**Prior (tick-parity): Script_Unit_Harvest ported + Team-first pass order; drift at u39.amount (RNG off-by-one).** SAVE007 tick-1 real-EMC drift progressed:
1. Was `unit[39].actionID=6 RETURN` (script wrong-branch drift — closed by porting slot 0x2A).
2. Now `unit[39].amount=8` (expected 9) — our `(tools.next() & 1)` bumps by 0, OpenDUNE bumps by 1. **Single-byte RNG stream offset** somewhere in the tick-1 execution.

Changes landed:
- `Scripting.Functions.makeHarvestUnit(host:source:)` ported from `src/script/unit.c:1640..1670`. Slot 0x2A (was nil / default `noOperation`). Consumes exactly 2 `tools.next()` bytes, gates on `spiceMap != nil` + current tile being `.thin` or `.thick` spice, sets `inTransport=true`, clamps amount to 100, 1/32 drain branch decrements spice via `SpiceMap.apply`.
- Parity harness builds real `spiceMap` from save via `Map.Generator.generate(seed:resolver:)` + `TileResolver.landscapeType(...)`. Previous `Map.empty()` baseline left every tile `.notSand`, short-circuiting Script_Unit_Harvest.
- `Scheduler.tick()` pass order reordered from `Unit → Structure → Team` to **`Team → Unit → Structure`** to match OpenDUNE's `GameLoop_Main` at `src/opendune.c:1117..1120`. RNG consumed by Team scripts now sequenced correctly vs OpenDUNE.

The single-byte RNG offset is likely a subtle per-unit script divergence or a scheduler pass that consumes a byte OpenDUNE doesn't (or vice versa). Needs per-call RNG trace instrumentation on both sides (extend `parity.c` to dump every `Tools_Random_256` call; Swift wraps `RandomSource.tools` with a counter) then diff. Deferred.

Next concrete step: add `Tools_Random_256` call tracing to the parity patch + `Scripting.RandomSource` tools-wrapper in Swift; diff the byte-call streams until a divergence appears; fix the upstream cause. The big win: **off-viewport units in OpenDUNE get a 3-opcode script cap** (`src/unit.c:292..294`), not 52. Without this our Swift VM was running 10-15× more opcodes per tick for every unit whose `scriptNoSlowdown=false` and position fell outside the viewport rect — causing every downstream field to drift. Fix: `parity.c` dumps `viewportPosition` per tick; Swift `Scheduler` gains `viewportPackedPosition` + `isInViewport(pos32X:pos32Y:)` ported from `Map_IsPositionInViewport` (`src/map.c:363`); `Scheduler.offViewportSlowdownEnabled` gates per-unit budget to `min(budget, 3)` when unit has `scriptNoSlowdown=false` AND is off-viewport. Parity harness flips it on. Also added `Simulation.UnitInfo.scriptNoSlowdown(type:)` as a type-id lookup helper (11 of 27 unit types return true: CARRYALL, ORNITHOPTER, LAUNCHER, HARVESTER, MCV, bullets 18..24, SANDWORM, FRIGATE).

To diagnose this I added opcode-level script tracing to both VMs: `parity.c` grew `--parity-script-trace=<path>` + `--parity-script-unit=<idx>` flags; OpenDUNE's `Script_Run` writes one line per opcode when enabled (`src/script/script.c` modified — design-doc no-touch list violation, but the hook is no-op when g_parityScriptTrace==NULL). Swift's `Scripting.VM` grew `trace: TraceHook?` called right before each `execute`. Running both traces for SAVE007 u37 tick 1 showed Swift executed 15+ opcodes vs OpenDUNE's 3 — directly pointing at the off-viewport cap. Patch grew 585 lines (up from 503).

**Parity status now:**
- SAVE001 & SAVE007 tick 0: byte-identical.
- SAVE001 tick 1 (real EMC): `unit[22].actionID` expected `5 HARVEST`, actual `6 RETURN`. Harvester transitions a tick early.
- **SAVE007 tick 1 (real EMC): `unit[39].actionID` expected `5 HARVEST`, actual `6 RETURN`.** Same class — harvester HARVEST → RETURN premature. `Scheduler.tickHarvesting`'s coherence-pin or a SetAction call in our harvester branches is triggering vs OpenDUNE's HARVEST script staying in HARVEST.
- SAVE001 / SAVE007 tick 1 empty-EMC: unchanged.

Both open drifts converge on the same root. Next step: trace the exact SetAction call for u22/u39 to see whether it's our `tickHarvesting` pin fighting EMC, or a `Script_Unit_SetAction(RETURN)` opcode firing that OpenDUNE doesn't reach. The central finding: **OpenDUNE's install has `OPTIONS.CFG` pinned at `gameSpeed=4` (Fastest)**, which makes `Tools_AdjustToGameSpeed(144, 1, 255, false)` return `255` instead of `144`. That's why u36's `speedRemainder` at tick 1 was `241` (= `(242 + 255) & 0xFF`) while our Swift (default `gameSpeed=2`) computed `130`. The parity dump now serialises `gameSpeed` per tick and the Swift harness reads it from tick 0 before running.

Fixes landed this slice:

- **`parity.c` dump widened** with `gameSpeed`, `scriptDelay / scriptPc / scriptSP / scriptFP`. Swift `ParityHarness` reads `goldenTicks[0].gameSpeed` to seed `scheduler.gameSpeed`. Patch at 503 lines.
- **`Scheduler.seedFromSave` zeroes `engine.delay`**: `src/saveload/unit.c:84` explicitly resets delay on load. Unlocked every script-driven drift that had been "delayed indefinitely."
- **`Script_Unit_SetTarget` orientation write reverted**: OpenDUNE's `Unit_SetOrientation(u, dir, rotateInstantly=false, 0)` writes `orientation[0].target + .speed` which we don't track; writing `.current` directly diverged the comparator. Now writes only `targetMove` for non-turreted units.
- **`Script_Unit_CalculateRoute` early-return**: ported OpenDUNE's `src/script/unit.c:1306` guard — returns 1 early when `currentDestination != 0` (unit mid-step) or encoded destination invalid. Previously we'd re-run target-rewrite logic for mid-step units.
- **`tickMovement` auto-populate currentDest gated on `speed != 0`**: OpenDUNE only populates `currentDestination` via `Unit_StartMovement` (tied to setting speed). Our unconditional auto-populate was overwriting parked units' currentDest from their queued route step.
- **`makeIdleActionUnit` orient write removed**: same class as SetTarget — consumes 2 RNG bytes but no longer writes `.current` (OpenDUNE's Unit_SetOrientation doesn't either). Gameplay-visible: idle ground units no longer randomly rotate until we port target/speed + rotation-tick.

Parity status now:
- SAVE001 & SAVE007 tick 0: byte-identical.
- SAVE001 tick 1 (real EMC): `unit[22].actionID` expected `5 HARVEST`, actual `6 RETURN` — harvester transitions to RETURN a tick early. Unchanged from last slice.
- **SAVE007 tick 1 (real EMC): `unit[37].route0` expected `0`, actual `1`** — our `Script_Unit_CalculateRoute` consumes a route step via memmove-left even when OpenDUNE's `Unit_StartMovement` would return false (tile score > 255 or == -1). Need to port the `Unit_GetTileEnterScore` check + `route[0] = 0xFF` / early-return path.
- SAVE001 / SAVE007 tick 1 empty-EMC: unchanged.

Next concrete step: port `Unit_StartMovement`'s tile-enter-score check into our `makeCalculateRouteUnit` — on `score > 255 || score == -1`, set `route[0] = 0xFF` and return without consuming the step. See `src/unit.c:1084-1086`.

**Known Swift gaps the harness will surface in rough order** (from the `compareUnit` skip-list): `nextActionID`, `orientation0Target`, `orientation0Speed`, `orientation1*` turret track, `wobbleIndex`, `timer`, structure `upgradeTimeLeft`, house `powerProduction`/`powerUsage`/`unitCount`/`unitCountMax`/`harvestersIncoming`. Each is a `public var` addition to the relevant slot + save/scenario load paths + a flipped `compareX` line. RNG globals stay out of the schema by design (no `src/tools.c` exposure). Map-tile parity is deferred — current test uses `Map.empty()` for the baseline so tile grid isn't compared yet; the harness's pool-state diff is the load-bearing part.

Previous STARPORT + CHOAM work is fully wrapped. Residual micro-follow-ups that can slip in opportunistically: briefing-music playback; frigate descent animation (needs FRIGATE.EMC host-fns).

What works now:
- Player CY + sidebar build panel + slab / windtrap / refinery chain.
- Harvester loop: seek → drain → dock → deposit → repeat. Parallelises across refineries; full harvester + all-refineries-busy triggers carryall ferry (spawn → fly → drop → re-dock). User `H`-ordered harvest correctly loops — pin flips any non-HARVEST idle back to HARVEST.
- Spice tiles visibly deplete (thick → thin → bare).
- Spice-bloom walk-into: tremor explosion + radius-5 spice spread + walker dies.
- Right-click attack: unit approaches (around other units, not through), stops at fire range, snaps orientation to target, fires bullets → bullet arrives → explosion + HP damage → on kill, house-coloured dead-body sprite persists ~20s then fades, attacker reverts to GUARD.
- Click any unit / structure → info panel shows name / house / HP bar (capped at max, correct percent) / state / action.
- Green halo around friendly selection; red halo around enemy. Blue (Atreides) structure outlines use the same code path so they sit exactly on the footprint.
- Per-house ICN palette remap applied in both `ScreenshotRenderer` + live scene; corpses use `.index0Transparent` + per-house remap so they show on sand in the owner's colour.
- Windtrap vanes render grey (palette index 223 statically resolved to target 12 — animated vanes TODO).
- Map trim: playable rect (`g_mapInfos[mapScale]`) renders with fully-veiled border around the scenario area.
- Camera: scene `.resizeFill` so the window can be any size (sidebar pinned right). `mapContainer` scaled 4× by default, centred on player CYARD at load. Arrow keys pan (clamped to playable rect). `=` / `-` zoom [1..16]×.
- Minimap cropped to playable rect + viewport overlay (white outline tracks the camera view).
- Keyboard: Escape (deselect + unstage), Tab (cycle player units), A/M/H/R (stage action), `,` / `.` (sim speed 1..16×), `=` / `-` (zoom), arrows (pan).
- Headless: `screenshot x y w h path`; `dump icntile / iconmap / explosions`; `stage <name>`; `fire-gate` + `damage` + `attack-hold` + `carryall` + `bloom` + `harvest-pin` + `scene` tracers all emit to stderr.
- Unit sprites render at native pixel size (harvester > trike > infantry). Units pathfind around each other.
- Attack visuals: muzzle flash + impact dimmed to 30% alpha so they're a hint, not a clutter.

Test status: **993 / 97** tests in suites green, zero warnings on clean build. 5 golden screenshot fixtures (`Fixtures/Screenshots/`), 2 golden parity JSONLs (`Fixtures/ParityGoldens/save001_200ticks.jsonl` + `save007_200ticks.jsonl`).

Latest session (2026-04-23) shipped, across two merged lines of work: (1) movement polish + STARPORT slices 5a/5b/5c-sim (fallback-slide per-tile gate, OpenDUNE crush parity, `[CHOAM]` INI loader + buildable mask, order commit + frigate delivery + availability bump, `StarportController` + pricing + runtime stock seeding); (2) harvester RETURN replan + adjacency dock + coherence pin + `Map_FixupSpiceEdges` port + CLAUDE.md "consult OpenDUNE" rule. Previous session (2026-04-22) shipped 27 commits. Details in today's `Documentation/History/2026-04.md` bullets.

## Next up (queued)

Ordered by value. Each one follows the `CLAUDE.md` feature workflow (design doc → implement → tests → full suite green → history entry → insight if non-obvious → update this file).

1. **Tick-parity harness — widen / extend.** SAVE007 now passes byte-for-byte across all 200 golden ticks under real UNIT.EMC / BUILD.EMC / TEAM.EMC (closed 2026-04-24). Remaining follow-ups: (a) regenerate OpenDUNE golden with `--parity-ticks=500` or `1000` to push the frontier deeper and flush out any longer-horizon divergences before declaring §6 complete; (b) fix SAVE001 tick-1 `unit[22].actionID=5 HARVEST vs 6 RETURN` (pre-existing harvester-transition drift, no SAVE007 overlap); (c) widen the compare schema with map-tile state + any structure fields still in the skip-list. Once (a) + (b) close, §6 of `Documentation/Plans/01.Initial.md` is done.
2. **Frigate landing animation + cargo drop** — STARPORT follow-up. Script-driven via FRIGATE unit's EMC entry; can land opportunistically after Mentat. (CHOAM sim-side + cart UI shipped 2026-04-23 across slices 5a / 5b / 5c-sim / 5c-ui.)
3. **`BULLET.EMC` script wiring** — bullets detonate via a scheduler shortcut; the real script gives proper flight frames + sonic-beam propagation. Cosmetic.
5. **Save-chunk TEAM decoder** — when we ship save compat (P6), TEAM chunk needs a body decoder.
6. **Sandworm `GetBestTarget`** — separate `Unit_Sandworm_GetTargetPriority`; slot 0x36.
7. **HP-bar visual on world markers** — small bar above each unit/structure SKNode for at-a-glance status. Info panel already shows HP for the selection; this would make every entity show it.
8. **Carryall reuse** — when a drop completes, fly the empty carryall back to a map-edge origin instead of freeing it; next ferry can reuse the existing slot. Cosmetic + matches OpenDUNE's persistent-carryall behaviour.
9. **Spice-bloom on damage** — port `Map_Bloom_ExplodeSpecial` + the explosion-callback path so blooms also detonate when attacked / caught in other explosions. Walk-into already shipped.

## Recently completed

Reverse-chronological; link to the day's history bullet for detail.

- **2026-04-24 — Parity schema widening: `orientation1{*}` added to `compareUnit`, gated on `hasTurret`.** Turret-equipped units (u26 TANK in SAVE007) now have their full turret orientation track diffed against OpenDUNE across 1000 ticks. Non-turreted units skip the compare since `Unit_Rotate` only advances `orientation[1]` when `hasTurret` (`src/unit.c:221`). **1032 / 101 / zero warnings.**

- **2026-04-24 — Parity schema widening: `wobbleIndex` added to `compareUnit`.** SAVE007 now also matches every unit's `wobbleIndex` byte across all 1000 ticks. Scouted deeper too: 2000-tick parity holds, 3010-tick parity holds, but tick 3011 `u39.targetMove=54699 vs 54441` surfaces a NEW frontier — spice-map state divergence (Swift sees tile (20, 20) as spice; OpenDUNE has drained it). Reverted golden to 1000 ticks (5000-tick dump = 53MB, too large to commit). Tried adding `orientation1{*}` to compare schema — surfaces tick 11 `u37.orientation1Target=32 vs 0` for non-turreted SOLDIER, deferred. **1032 / 101 / zero warnings.**

- **2026-04-24 — 🎯 SAVE007 achieves FULL 1000-tick parity with OpenDUNE.** Three fixes stacked on top of the morning's inline-fireDelay close: (1) `Script_General_SearchSpice` slot 0x29 ported (was NoOp stub); (2) `findSpiceNear` tie-break flipped `<` → `<=` to match OpenDUNE's last-wins; (3) `Unit_Move` `targetMove` auto-clear on arrival (port of `src/unit.c:1484..1486`). Also closed SAVE001 tick-1 harvester transition drift as a side-effect (same SearchSpice path); converted the SAVE001 "expected to diverge" test to `expectFullParity(tickLimit: 200)`. New regression tests: `tieBreakLastWins` (SpiceSeekTests), `searchSpiceReturnsEncodedTile` / `searchSpiceNilHostHook` / `searchSpiceReturnsZero` (EmcHostBatch67Tests). **1032 / 101 / zero warnings.**

- **2026-04-24 — SAVE007 parity extended 690 → 800 ticks via inline per-unit `fireDelay--` port.** Closed tick 691 by inlining OpenDUNE's `u->fireDelay--` (`src/unit.c:202..216`) into the interleaved per-unit loop, right after `tickMovement(fromFindArrayIndex:singleUnit:)`. Old batched `tickFireCooldowns()` was visiting every findArray entry post-shift, so when u25 died mid-tick and `Unit_Free`'s memmove dragged u26 into u25's cursor position, Swift incorrectly decremented u26.fireDelay (47) where OpenDUNE had skipped u26 entirely (48). Gameplay (non-interleaved) path keeps the batched call; only `perUnitInterleavedTickOrder` mode skips it. New `interleavedFireDelayDecrementSkipsShiftedUnit` regression test in `Simulation.Scheduler` synthesizes the exact mid-loop free + shift via a `FUNCTION 0x0F (Die)` program. Next frontier: tick 801 `u39.actionID=5 HARVEST vs 6 RETURN` — same class as the open SAVE001 tick-1 harvester transition drift. **1028 / 101 / zero warnings.**

- **2026-04-24 — SAVE007 parity extended 621 → 690 ticks via `Unit_EnterStructure` hostile-entry port.** `Simulation.Units.enterStructure` applies `min(unit.hp * 2, structure.hp / 2)` damage + consumes the attacker when a ground unit arrives on a structure tile owned by a different, non-allied house. Wired into `Scheduler.tickMovement`'s ground-unit arrival block. Closed tick 622 CYARD 40 hp drop. New `UnitEnterStructureTests` suite (5 tests). Next frontier: tick 691 `u26.fireDelay=48 vs 47` — off-by-1 RNG drift between ticks 622..690. **1025 / 101 / zero warnings.**

- **2026-04-24 — SAVE007 parity extended 585 → 621 ticks via bullet-vs-structure mid-flight detonation port.** `Scheduler.tickMovement`'s bullet step block now ports OpenDUNE's `Unit_Move` at `src/unit.c:1409..1416` — a bullet landing on LST_WALL / LST_STRUCTURE / LST_ENTIRELY_MOUNTAIN tile explodes + frees immediately, independent of `distanceToDestination` arrival. Closed tick 586 `structure[0].hitpoints=397 vs 400`. New test `bulletDetonatesOnStructureTile` in `BulletFlightTests` pins the behaviour. Next frontier: tick 622 CYARD 40 hp damage (13+ SOLDIER shots) missing in Swift. **1020 / 100 / zero warnings.**

- **2026-04-24 — SAVE007 parity extended 550 → 585 ticks via structure-target + wobble fixes.** `LST_STRUCTURE → LST_CONCRETE_SLAB` speed remap in `makeCalculateRouteUnit`; `Pos32.distance(from:toEncoded:)` + `Pos32.targetTile` for edge-tile / layout-centre structure semantics (ports of `Object_GetDistanceToEncoded` + `Tools_Index_GetTile`); `isWobbling = letUnitWobble` enhanced clear; Fire's orientation-diff + `Units.createBullet` now use layout-adjusted target centre. `StructureLayout.edgeTileOffsets` + `tileDiff` added with verbatim OpenDUNE table values. 12 new tests across `StructureLayoutTablesTests` (new), `CalculateRouteStartMovementTests` (new), `TileMathTests`, and `EmcHostBatch67Tests`. Next frontier: tick 586 `structure[0].hitpoints=397 vs 400` — bullet impact damage missing. **1018 / 100 / zero warnings.**

- **2026-04-24 — Parity-fix test backfill.** 11 new synthetic tests covering every behavior change shipped in this parity sprint: `deferFreeOnDeath` paths (`ExplosionTests`), `untargetUnit` sweep (new `UnitUntargetTests` suite, 5 tests), `tickStarportAvailability` LCG + tick-1 gate (`StarportDeliveryTests`), `RandomSource.lcgRange` hook (`EmcFunctionsTests`). **1005 / 98 / zero warnings.**

- **2026-04-24 — SAVE007 parity extended 200 → 550 ticks via `Unit_UntargetMe` port + 1000-tick golden.** Regenerated OpenDUNE dump with `--parity-ticks=1000`; renamed committed fixture `save007_200ticks.jsonl` → `save007_ticks.jsonl`. Ported `Unit_UntargetMe` (`src/unit.c:1611`) as `Simulation.Units.untargetUnit(poolIndex:host:)` and wired into `makeDieUnit` — clears stale `targetMove/targetAttack` references on every other unit before the slot is freed, matching OpenDUNE's `Unit_Remove` sequence. Closed tick 201 `u30.targetMove=0 vs 16420` (player tank's target was an infantry u36 that got freed one tick prior). Next drift at tick 551 `u37.movingSpeed=255 vs 0` — HUNT trooper's rotation arrival hits target orientation one tick after OpenDUNE, delaying the `Unit_StartMovement` branch. **994 / 97 / zero warnings.**

- **2026-04-24 — 🎯 SAVE007 achieves FULL 200-tick byte-for-byte parity with OpenDUNE.** Last drift closed was tick 199 `u36.isUsed` via `Host.deferFreeOnDeath` — `applyUnitDamage` transitions to ACTION_DIE on hp=0 instead of freeing the slot outright, matching `src/unit.c:1567`. New helper `expectFullParity` replaces the "expect *some* drift" `expectDivergenceUpTo` flow for SAVE007. SAVE001 still diverges at tick 1 (pre-existing harvester HARVEST→RETURN drift; separate fix). **994 / 97 / zero warnings.**

- **2026-04-24 — SAVE007 parity frontier tick 166 → tick 199 via byte-identical Borland LCG streams.** Added `Scripting.RandomSource.onLCGDraw` + `lcgRange` helper, `ParityHarness.LCGTrace`, OpenDUNE `--parity-lcg-trace` flag + `src/tools.c` hook. First LCG-byte diff revealed Swift missed OpenDUNE's tick=1 `ctx=s2` draw — `tickStarportAvailability` used `Tools_Random_256` (should be LCG per `src/house.c:105`) and its modulo cadence never fired at tick 1 (OpenDUNE's zero-init gate fires immediately). Fixed both with `Scheduler.lcgRange` closure + `nextStarportAvailabilityGate` / `openDUNEStarportAvailabilityReschedule=1800`. Swift + OpenDUNE LCG traces byte-identical across 200 ticks; tick 166 u0 orientation0Target drift closed. New frontier at tick 199 `u36.isUsed=true vs false` — Swift frees an infantry slot on hp=0 one tick earlier than OpenDUNE's DIE-animation state. **994 / 97 / zero warnings.**

- **2026-04-24 — SAVE007 parity frontier tick 109 → tick 166 via per-unit rotation in the interleaved loop + `Script_Unit_SetSpeed` return-value fix.** Flipped `perUnitInterleavedTickOrder = true` in the parity harness. Closed tick 109 u0 `positionX=7355 vs 7325` (30-pixel carryall drift) by adding `tickRotationForUnit(idx:)` to the interleaved loop between movement and sprite — matches OpenDUNE's `src/unit.c:220..221` per-unit ordering. Fixed `Script_Unit_SetSpeed` (slot 0x1B) to return `u->speed` after `Units.setSpeed` instead of the input percent (port of `src/script/unit.c:393`). New frontier at tick 166 u0 `orientation0Target=43 vs 0` is LCG state drift — u0 script trace at tick 166 diverges at pc=1977 `RandomRange(0,128)` with Swift=0 vs OpenDUNE=43. **993 / 97 / zero warnings.**

- **2026-04-24 — SAVE007 tick-151 `u39.amount` drift root-caused to per-tick RNG draw order divergence.** RNG byte stream matches Swift / OpenDUNE (same seed, deterministic), but callers attributed to each byte differ. Swift batches `tickMovement` / `tickRotation` / `tickSpriteOffsets` over all units before running any unit scripts; OpenDUNE interleaves all of them per-unit inside `Unit_Find`. By tick 151 the accumulated ordering shift makes u39's Harvest.bump read OpenDUNE's drainRoll byte. Fix is a scheduler-structure refactor — see the active task. New insight `simulation-per-tick-rng-order-matters`. Infrastructure: `saveSevenParityDump200TickRNGStream` diagnostic test, `movementRNG: ((Int) -> UInt8)?` signature for per-wobble tagging. **993 / 97 / zero warnings.**

- **2026-04-24 — SAVE007 u0 carryall script byte-exact for OpenDUNE's tick-1 window via `Script_Unit_GetInfo` case 0x0F (byScenario) + 5 other missing cases.** ScriptTrace diagnostic infrastructure landed (`ParityHarness.ScriptTrace` + `saveSevenParityDumpU0ScriptTrace` test regenerating `tmp/swift_u0_script.txt`). Per-opcode diff against `tmp/opendune_u0_script.txt` pinned the divergence at opcode 41 — GetInfo(0x0F) fetches `byScenario`, Swift's `default: return 0` branch fired where OpenDUNE returned 1. Added 0x09/0x0A/0x0C/0x0F/0x11/0x13 cases to `makeGetInfoUnit`. Tick-151 u39.amount drift is now an independent issue, no longer a u0 cascade. **992 / 97 / zero warnings.**

- **2026-04-24 — SAVE007 parity frontier tick 101 → tick 151 via `Script_Unit_SetDestinationDirect` byte-exact rewrite.** Removed the spurious `targetMove` write, switched to `Units.setOrientation(rotateInstantly: false)` for gradual turn, and gated the `currentDestination` write on `currentDestination == (0, 0)` — matching `src/script/unit.c:918..936`. **989 / 97 / zero warnings.**

- **2026-04-24 — SAVE007 parity frontier tick 76 → tick 101 via `tickSpriteOffsets` reorder.** Moved the sprite-animation pass in `Scheduler.tick()` to sit after `tickMovement` + `tickRotation`, matching OpenDUNE's per-unit `src/unit.c:239..287` ordering. For foot units arriving this tick, `Unit_MovementTick` clears `speed = 0` first, so the sprite gate `FOOT && speed != 0` correctly refuses the extra bump. **989 / 97 / zero warnings.**

- **2026-04-24 — SAVE007 parity frontier tick 70 → tick 76 via `tickHousePowerMaintenance` port.** OpenDUNE's `src/house.c:270..273` drains `min(credits, (powerUsage / 32) + 1)` every 10 800 ticks, seeded by `opendune.c:1503`'s `max(g_timerGame + 70, saved)` — for Atreides in SAVE007 that's 1 credit at tick 70. Added `powerProduction` + `powerUsage` fields to `HouseSlot` (loaded from save), wired `Scheduler.tickHousePowerMaintenance()` at tick-tail, flipped parity harness to assert both power fields. Two new `SimulationSchedulerTests` (`houseMaintenanceDrain`, `houseMaintenanceDrainScale`). Frontier: tick 76 `unit[37].spriteOffset=7 vs 8`. **989 / 97 / zero warnings.**

- **2026-04-24 — SAVE007 parity frontier tick 56 → tick 70 via `Script_Unit_IdleAction` byte-exact rewrite.** Our port had four divergences from OpenDUNE's `src/script/unit.c:1748..1776`: wrong RNG (Tools instead of LCG) for the gate roll, a fabricated sandworm branch, an unconditional GUARD-infantry sprite write (instead of FOOT + `random > 8`), and a `spawnChance`-style gate replacing the fixed `random > 2` threshold. Rewritten line-for-line: one LCG draw; movementType ∈ {foot,tracked,wheeled} filter; FOOT-only `random > 8` sprite write; `random > 2` early-return; else 2 Tools bytes for `Unit_SetOrientation` level-select + target. `IdleActionTests` updated (`footSpriteOffset` now picks an LCG seed that rolls > 8; new `footSpriteOffsetGateClosed` complement). Frontier: tick 70 `house[1].credits=304 vs 305`. **987 / 97 / zero warnings.**

- **2026-04-24 — SAVE007 parity frontier tick 51 → tick 56 via `Unit_Create` initial-action port for bullets.** `Units.createBullet` was writing `info.actionAI` (= STOP) unconditionally; OpenDUNE's `Unit_Create` instead picks `(houseID == g_playerHouseID) ? actionsPlayer[3] : actionAI` via `Unit_SetAction`, and for bullets / missiles / sandworm / frigate `actionAI = ACTION_INVALID` triggers the `Unit_SetAction` early-return path (leaving `actionID = ACTION_GUARD` from `Unit_Create`'s `src/unit.c:425`). Added `ActionID.invalid = 0xFF`, flipped `UnitInfo.table` rows 18..26 to `actionAI = .invalid`, introduced `Units.resolvedInitialAction(info:houseID:host:)`, and wired `host.playerHouseID` in `ParityHarness.runAgainst` via a CYARD-owner heuristic. Two new `UnitInfoTests` (`bulletActionAIInvalid`, `resolvedInitialActionBranches`). Frontier: tick 56 `unit[28].spriteOffset=11 vs 6`. **986 / 97 / zero warnings.**

- **2026-04-24 — SAVE007 parity frontier tick 41 → tick 51 via `Unit_GetTileEnterScore` port in parity harness.** Three stacked fixes: (1) `ParityHarness.runAgainst` wires `host.tileEnterScore` with a `Unit_GetTileEnterScore` port — playable-rect gate from `game.info.scenario.mapScale`, landscape-speed lookup from `LandscapeInfo.table`, diagonal penalty, `res ^ 0xFF` invert. Without it, the fallback `return 128` let our pathfinder find bogus routes for units stranded outside the rect (u38 at (17, 62)). (2) `retargetImpassableDst: Bool = true` flag added to `Pathfinder.findRoute` + `Scripting.Host` (gameplay default preserved); parity harness flips false to match OpenDUNE's `Script_Unit_Pathfinder` which never retargets. (3) `makeCalculateRouteUnit` now only overwrites `route[0..copyCount-1]` (port of `memcpy(u->route, res.buffer, min(res.routeSize, 14))`), leaving the tail at its saved bytes — was previously clobbering the whole 14-entry `route[]` with 0xFF on empty-route, which drifted u38's `route1..3 = 0` to 255. New `PathfinderTests.findRouteParityNoRetarget` test. Frontier: tick 51 `unit[12].actionID=3 vs 7`. **984 / 97 / zero warnings.**

- **2026-04-23 — SAVE007 parity frontier tick 11 → tick 25 via `Unit_StartMovement` port + landscape-oracle fix.** Five stacked fixes: (1) `ParityHarness.runAgainst` landscape oracle was reading `SpiceMap.Level` rawValue as `LandscapeType` rawValue — fixed by adding `snapshotLandscape: [LandscapeType]` parameter built from `resolver.landscapeType(...)`. (2) Removed spurious `byScenario * 192/256` scaling from `makeCalculateRouteUnit`'s SetSpeed block (OpenDUNE's Unit_StartMovement doesn't apply it). (3) Fixed HP-half check to `(info.hitpoints / 2) > hp` matching OpenDUNE. (4) Added `Host.gameSpeed` + plumbed through all `setSpeed` / `Stop` / `CalculateRoute` call sites — parity harness syncs both `scheduler.gameSpeed` and `host.gameSpeed`. (5) Ported `Unit_StartMovement`'s `currentDestination = Tile_MoveByOrientation(pos, orientation)` via new `Pos32.movedByOrientation(_:orientation:)` (exact 256-pixel octant offsets, unlike the sin-approx `Pos32.moved`). Frontier: tick 25 `unit[36].positionY=7552` (expected 7568) — subpixel step math off by one tile. **982 / 96 / zero warnings.**

- **2026-04-23 — SAVE007 parity frontier tick 6 → tick 11 via sprite-animation `timer` gate.** Ported OpenDUNE's `tickUnknown5` pacing from `src/unit.c:240..286`: `timer: UInt16` on `UnitSlot`, `animationSpeed: UInt16` on `UnitInfo` (27 table values from `src/table/unitinfo.c`), `tickSpriteOffsets` now decrements `timer` when non-zero and only animates + resets timer when timer hits 0. `compareUnit` flipped `timer` out of the skip-list. Frontier: `unit[37].movingSpeed=0` (expected 112) — trooper off-viewport (3-opcode cap), Script_Unit_SetSpeed investigation next. **982 / 96 / zero warnings.**

- **2026-04-23 — SAVE007 parity frontier walks tick 2 → tick 6 via per-unit cadence gates + `Unit_Rotate` port.** Three fixes stacked: (1) cadence gates for `tickScript` (every 5 ticks), `tickMovement` (every 3), `tickRotation` (2..8 gameSpeed-adjusted) — gated behind `perTickCadenceGatesEnabled: Bool = false` default, parity harness flips on; (2) `Unit_Rotate` port as `Scheduler.tickRotation`, new `orientationTarget` + `orientationSpeed` fields on `UnitSlot`, `Unit_SetOrientation(rotateInstantly:)` helper, updated `Script_Unit_SetOrientation` + `Script_Unit_SetTarget` to seed target+speed; (3) `tickHarvesting` gated by `tickHarvestingEnabled: Bool = true` (parity off) to avoid double-bumping `amount` with real `Script_Unit_Harvest`. Frontier: `unit[36].spriteOffset=64` (expected 63) — sprite-animation `timer` gate not yet ported. **982 / 96 / zero warnings.**

- **2026-04-23 — SAVE007 tick 1 byte-identical with OpenDUNE under real EMC; parity frontier moved to tick 2.** Closed the final tick-1 drift (`unit[39].spriteOffset`) by porting OpenDUNE's harvester-specific animation branch at `src/unit.c:268..283` into `Scheduler.tickSpriteOffsets` — when `type == HARVESTER && action == HARVEST`, bump the 6-bit counter; otherwise reset to 0. Test `saveSevenParityTickOneRealEmc` → `saveSevenParityRealEmcFrontier` with a new `expectDivergenceUpTo(tickLimit:)` helper. Tick-2 drift: `unit[0].orientation0Current=106` (expected 96) — carryall rotation needs `Unit_Rotate` + orientation-target/speed fields. **982 / 96 / zero warnings.**

- **2026-04-23 — SAVE007 tick-1 `unit[39].amount` drift CLOSED via three RNG-stream fixes.** (1) `tickTeams` now consumes 1 byte per outer-gate fire (matching OpenDUNE `src/team.c:27`), using new `timerGame` + `nextTeamTickGate` fields. (2) `UnitPool.recount()` ported from OpenDUNE's `Unit_Recount` (`src/pool/unit.c:75`) + called at the end of `WorldSnapshot(loading:baseline:)` so findArray is in pool-index order regardless of save-chunk order. (3) `makeFireUnit(host:source:)` now draws the trailing `fireDelay += Tools_Random_256() & 1` byte from `src/script/unit.c:692`. Drift moved to `unit[39].spriteOffset=24` (expected 25) — different class, sprite-cycle cadence, RNG stream now aligned. Also extended OpenDUNE's `Tools_Random_256` trace hook with per-draw context tagging (`ctx=u39` / `ctx=s3` / `ctx=t0` / `ctx=NULL`) for future per-unit diffing. **982 / 96 / zero warnings.**

- **2026-04-23 — Tick-parity RNG-byte trace on both sides (Swift + OpenDUNE) — u39.amount root cause identified.** Added `Scripting.RandomSource.onToolsDraw((UInt8, String) -> Void)?` hook + `currentTraceContext` tag + `Simulation.ParityHarness.RNGTrace` class (Swift) + `--parity-random-trace=<path>` flag / `Tools_Random_256` write hook in OpenDUNE's `parity.c` + `src/tools.c`. Diffing the two SAVE007 tick-1 traces showed OpenDUNE consumes 4 extra pre-Harvest bytes that Swift doesn't — so Swift's `Harvest.bump u39` reads byte 0xF0 (bump=0) while OpenDUNE reads 0x3F (bump=1). Root cause located; fix deferred to a follow-up slice since the 4 missing byte consumers need per-unit script-trace comparison (candidates: u25–u28 dispatching an EMC opcode path Swift shortcuts). New `saveSevenParityTickOneDumpRandomStream` diagnostic test regenerates the Swift trace on demand. **982 / 96 / zero warnings.**

- **2026-04-23 — Harvester return path octant-snapped (gameplay visual fix).** User-reported: harvester return path rendered as a non-octant diagonal. Root cause: `Scheduler.tickMovement`'s fallback-slide branch wrote the *continuous* `Pos32.direction(...)` result to `orientationCurrent`, so `Pos32.moved` stepped along arbitrary angles. Fix: octant-snap via `(raw + 16) & 0xE0` matching OpenDUNE's `Unit_StartMovement` at `src/unit.c:1073`. Route-locked motion unaffected. 1 new regression test pinning `orientation & 0x1F == 0` for a knight's-move fallback slide. **981 / 96 / zero warnings.**

- **2026-04-23 — Player move orders clamped to `playableRect`.** User-reported: units could be right-clicked into the veiled border (outside the scenario's playable rect) and would happily walk off the scenario area. Fix in `ScenarioRuntime`: new `isInPlayableRect(tileX:tileY:)` helper; all four move-style order sites (`rightClick .orderMove`, staged-left-click `.orderMove` / `.orderHarvest`, A-key force-attack-on-empty fallback) now early-return `ClickOutcome.orderMove(ok: false)` / `.orderHarvest(ok: false)` with a logged `REJECTED out-of-playable` line. Target-based orders (`orderAttack`, `orderAttackStructure`) untouched. 1 new test pinning the rejection + 1 existing pathfinder test retargeted (50,50) → (45,45) so it still forces routing around CYARD while staying inside mission-1's `(16,16,32,32)` rect. **980 / 96 / zero warnings.**

- **2026-04-23 — Script_Unit_Harvest ported + tick-pass reorder to match OpenDUNE (Team → Unit → Structure).** Wired slot 0x2A (was nil / `noOperation`). Port of `src/script/unit.c:1640..1670` — guards on HARVESTER type / `amount < 100` / `host.spiceMap != nil` + current tile being `.thin` or `.thick` spice; consumes exactly 2 `tools.next()` bytes (amount bump & 1, keep/drain roll & 0x1F), sets `inTransport = true`, clamps amount to 100, and decrements spice via `SpiceMap.apply(delta:-1, at:)` on the 1/32 drain branch. Parity harness builds real `spiceMap` from `AssetLoader.tileResolver` + `Map.Generator.generate(seed:resolver:)` so `landscapeType` reports spice tiles correctly (previous `Map.empty()` baseline left every tile `.notSand`, short-circuiting Harvest to return 0). Also reordered `Scheduler.tick()` pass sequence: `tickTeams` now runs BEFORE `tickUnits` / `tickStructures`, matching OpenDUNE's `GameLoop_Main` at `src/opendune.c:1117..1120`. TEAM.EMC's RNG consumption now happens before unit-tick RNG consumption (no-op on saves without teams, but structurally correct). Drift advanced from `unit[39].actionID=6 RETURN` (script wrong-branch — closed) to `unit[39].amount=8` (expected 9 — 1-byte RNG sequence offset, needs per-call `Tools_Random_256` tracing on both sides to isolate). **979 / 96 / zero warnings.**

- **2026-04-23 — Tick-parity: off-viewport 3-opcode cap (the biggest parity find yet) via opcode-level VM tracing.** Dropped opcode-level script tracing into both VMs (parity.c + src/script/script.c hooks in OpenDUNE; `Scripting.VM.trace: TraceHook?` in Swift) to diff per-opcode execution. Running the trace for SAVE007 u37 tick 1 revealed OpenDUNE executed 3 opcodes while Swift ran 15+. Root cause: OpenDUNE `src/unit.c:292..294` caps unit scripts at 3 opcodes per tick for off-viewport units (position outside the viewport rect) whose type has `scriptNoSlowdown=false`. Our Swift scheduler was running the full 52 (or 7 gameplay) regardless. Ported `Map_IsPositionInViewport` (`src/map.c:363`) as `Scheduler.isInViewport(pos32X:pos32Y:)`, added `Scheduler.viewportPackedPosition` + `offViewportSlowdownEnabled` toggle, added `Simulation.UnitInfo.scriptNoSlowdown(type:)` lookup. Parity harness reads viewport from golden's tick 0 + flips toggle on. Drift advanced from `unit[37].route0` to `unit[39].actionID` (harvester RETURN premature — same class as SAVE001's u22). Patch grew to 585 lines (added src/script/script.c diagnostic hooks + viewport dump in parity.c). **980 / 96 / zero warnings.**

- **2026-04-23 — Tick-parity: gameSpeed from golden + mid-step CalculateRoute + speed-zero movement gates.** Five closures pushed SAVE007 tick-1 real-EMC drift from `unit[30]` → `unit[36]` → `unit[37]` (movement / route state, ~3 levels deeper into the sim). Key discovery: **our install's `OPTIONS.CFG` pins `gameSpeed=4` (Fastest)**, not the default 2. `Tools_AdjustToGameSpeed(144, 1, 255, false)` returns `255` at gameSpeed=4 but `144` at gameSpeed=2 — so u36's tick-1 `speedRemainder` was `241` in the golden vs `130` on our side. Now serialised per-tick in the golden + read by the Swift harness. Other fixes: (a) `Script_Unit_CalculateRoute` early-returns when unit is mid-step (currentDest != 0) — OpenDUNE `src/script/unit.c:1306`; (b) `tickMovement` auto-populate of currentDest gated on `speed != 0` since OpenDUNE only writes currentDest via `Unit_StartMovement`; (c) `makeIdleActionUnit` no longer writes `orientationCurrent` (consumes 2 RNG bytes but orientation stays put — OpenDUNE sets target+speed which we don't track); (d) idle-action + SetTarget tests updated to pin new OpenDUNE-correct behavior. Parity dump schema widened with `gameSpeed / scriptDelay / scriptPc / scriptSP / scriptFP`; both goldens refreshed; patch at 503 lines. **979 / 96 / zero warnings.**

- **2026-04-23 — Tick-parity: script delay zeroed on save-load, SetTarget orientation reverted, dump schema widened.** Three closures moved SAVE007 tick-1 real-EMC drift deep into second-order state (u36 position off-by-11) and advanced SAVE001 drift from `unit[22].targetMove` → `unit[22].actionID` (harvester now runs its RETURN transition). Key finding: **OpenDUNE's `Unit_Load` at `src/saveload/unit.c:84` explicitly zeroes `script.delay = 0` for every loaded unit** — so post-load, all unit engines are immediately runnable regardless of the saved delay. Our `Scheduler.seedFromSave(_:)` now zeroes `engine.delay` to match. This single change unlocked every script-driven drift that was previously "delayed indefinitely" on our side. Reverted the previous-slice `orientationCurrent = Pos32.direction(...)` write in `makeSetTargetUnit`: OpenDUNE's `Unit_SetOrientation(u, dir, rotateInstantly=false, 0)` writes `orientation[0].target + .speed` (not `.current`), with `.current` updated gradually by `tickRotation`. We don't track target/speed yet, so writing current directly diverged the comparator. Kept the targetMove write. Updated `setTargetWritesAndFaces` test to pin the new behavior (targetMove set, body orientation unchanged). Parity dump schema extended with `scriptDelay / scriptPc / scriptSP / scriptFP` for future debugging; patch at 501 lines, both goldens refreshed. **979 / 96 / zero warnings.**

- **2026-04-23 — SAVE007 tick-1 parity: spriteOffset + SetTarget + AttackHold gated for real EMC.** Three closures moved the SAVE007 tick-1 real-EMC drift from `unit[25]` → `unit[26]` → `unit[30]`. (1) `tickSpriteOffsets` gate tightened to match OpenDUNE `src/unit.c:241`: animate only when `speed != 0` (parked units with stale targetMove shouldn't tick the walk cycle). (2) `Script_Unit_SetTarget` (slot 0x3A) — our port wrote only `targetAttack`; added the OpenDUNE `if (!hasTurret)` block at `src/script/unit.c:855-858` that also writes `targetMove = target` + snaps body orientation for non-turreted units (infantry / trike / quad). Turreted units (tank / siege) keep targetMove untouched. (3) `Scheduler.tickAttackHoldEnabled: Bool` — new toggle, default true for gameplay, false for parity. Our `tickAttackHold` manually clears `targetMove` when an attacker is in fire range — a stopgap for stubbed EMC that pre-empts OpenDUNE's `Script_Unit_MoveToTarget` + `Script_Unit_Fire` decisions once real EMC is loaded. ParityHarness flips the toggle. New drift at `unit[30].targetMove` (expected encoded u36 via `FindBestTarget`, actual 0) needs target-priority audit next. **979 / 96 / zero warnings.**

- **2026-04-23 — Closed SAVE007 tick-1 orientation drift: `tickMovement` orientation-recompute gated on `speed != 0`.** OpenDUNE's `Unit_MovementTick` early-returns when `speed == 0` (`src/unit.c:98`) and never touches orientation for parked units. Our `tickMovement` unconditionally recomputed orientation for any unit with a targetMove/currentDestination/route — even parked ones. SAVE007 unit[25] (parked enemy trooper, actionID=HUNT, `speed=0`) had `targetMove` encoding unit 26 (a trike), blocked by a refinery footprint at tile (25, 23). `nearestPassableNeighbor` redirected the goal NORTH to (25, 21) since the south-side adjacencies of the trike tile were structure-blocked. Our orientation recompute then rotated unit 25 north (`direction=0`) while OpenDUNE kept it stored east (`64`). Fix: wrap the orientation-recompute block in `if slot.speed != 0 { ... }`. Three-line change. SAVE007 tick-1 drift advanced to `unit[25].spriteOffset` (off-by-one in `tickSpriteOffsets` — next slice). **979 / 96 / zero warnings.**

- **2026-04-23 — Real-EMC parity path: save-script seeding + opcode budget override + SAVE007 movingSpeed closed.** Wired real `UNIT.EMC` / `BUILD.EMC` / `TEAM.EMC` into `ParityHarnessTests` via `AssetLoader.loadEmc` (2 new install-gated tests, `saveOneParityTickOneRealEmc` + `saveSevenParityTickOneRealEmc`). Hit two roadblocks: (a) our `Scheduler` started every tick by resetting the engine via `VM.load(engine:typeID:)` when `loadedUnitAction[idx] != action` (which is always true on the first post-seed tick since `loadedUnitAction` init to -1), clobbering the save's `ScriptState`. Added `Scheduler.seedFromSave(_:)` — reads each unit/structure's saved script state (pc, delay, stack, framePointer, variables, isSubroutine) via new `Scripting.Engine.fromSave(_:)` helper, pairs it with `loadedUnitAction[idx] = slot.actionID` so the delta-check passes. (b) OpenDUNE runs 52 opcodes per unit tick (`SCRIPT_UNIT_OPCODES_PER_TICK + 2`, `src/unit.c:292`), we ran 7 — scripts halted on delays or natural stopping points well before `Script_Unit_SetSpeed(u, 255)` fired. Made `unitOpcodeBudget / structureOpcodeBudget / teamOpcodeBudget` instance-mutable; parity harness bumps to 52. With both fixes, SAVE007 `unit[0].movingSpeed` closed (carryall goes 76 → 255 correctly on tick 1), drift advanced to `unit[25].orientation0Current` (enemy trooper, HUNT action). SAVE001 still diverges at `unit[22].targetMove` — deeper harvester-script path needs more investigation (likely script gating cadence `s_tickUnitScript` every 5 ticks vs. our per-tick dispatch). 2 new tests. **979 / 96 / zero warnings.**

- **2026-04-23 — Closed SAVE007 tick-1 position drift: winger off-script movement bugs in `tickMovement`.** Two bugs uncovered by the parity harness, both in `Scheduler.swift`'s `tickMovement`: (1) the "orientation-first" block recomputed `orientationCurrent` from `Pos32.direction(pos → currentDestination)` whenever `goalSource == "route"` but `route[0] == 0xFF` — i.e. whenever currentDestination was set but no pathfinder route existed. For a script-set fly-through winger (SAVE007 unit[0], carryall, orientation 96, currentDest pointing NW) this flipped heading from SE to NW, causing the unit to drift in the wrong direction. Fix: only recompute orientation in the `targetMove(fallback)` branch; route-locked steps keep `route[0] * 32`; everyone else keeps the stored orientation. (2) The post-move overshoot-snap block snapped `positionX/Y` to `goal` whenever `distAfter >= distBefore`. For wingers whose script sets currentDestination to a fly-through point and orientation to something unrelated, post-move distance legitimately grows every tick — snap-to-goal teleported them onto their destination. OpenDUNE's `Unit_Move` (`src/unit.c:1451`) only snaps to `currentDestination` when `ui->flags.isGroundUnit`; wingers fall through to `unit->o.position = newPosition` at line 1512. Fix: skip the overshoot-snap for `movementType == .winger && !isProjectileType(slot.type)`. Bullets (also `movementType == .winger`) continue to snap + detonate on arrival via the existing path — 3 `BulletFlightTests` that regressed on the first pass were the signal that got the scoping right. Next harness drift is now `unit[0].movingSpeed` (script-driven, needs `UNIT.EMC` wired into the harness). **977 / 96 / zero warnings** (no test count change — tests got stricter, not more numerous).

- **2026-04-23 — SAVE007 golden fixture + OpenDUNE audio-off hardening.** User added a richer mid-mission save (`_SAVE007.DAT`: active harvester, attacking trike, 4 hunting infantry) to exercise more of the sim per tick. First attempt to dump it segfaulted at tick 25 — skipping `Drivers_All_Init` in parity mode had left `g_driverVoice->index = 0` (not `0xFFFF`), so `Driver_Voice_Play`'s early-return gate didn't trigger and `DSP_Play` ran against an uninitialised audio device the instant something on-map fired. Fix in `parity.c`: force `g_gameConfig.music = g_gameConfig.sounds = 0` *and* pin `g_driverVoice / g_driverMusic / g_driverSound ->index = 0xFFFF` at the top of `Parity_Run`. Re-verified SAVE001's committed fixture unchanged after the fix (no sim-behaviour regression), then captured `save007_200ticks.jsonl` (2.4 MB, 201 lines, deterministic across two runs). Regenerated `tick_parity_dump.patch` (494 lines, up from 480) against vanilla OpenDUNE `60019e8` — round-tripped via a fresh clone-and-rebuild to confirm. 2 new `ParityHarnessTests` (`saveSevenParityTickZero` — byte-identical; `saveSevenParityTickOneDiverges` — captures the first drift). Tick 1 divergence on SAVE007 is a **movement bug**, not a script-gap: `unit[0].positionX` expected `6496`, actual `6340` — same magnitude (±78), opposite sign. Unit 0 at orientation 96 (SE) with `speedPerTick=255 / speedRemainder=248` moves in the wrong direction — off-script, independent of EMC, so closes purely by auditing our `tickMovement` against `src/unit.c:107 Unit_MovementTick`. **977 / 96 / zero warnings.**

- **2026-04-23 — Tick-parity Swift replay skeleton + first divergence.** `Simulation.ParityHarness.runAgainst(snapshot:golden:tickLimit:unitProgram:structureProgram:teamProgram:rngSeed:)` at `Code/Core/Sources/DuneIICore/Simulation/ParityHarness.swift` parses the newline-delimited JSONL golden into typed `GoldenTick` / `GoldenHouse` / `GoldenStructure` / `GoldenUnit` records, spins up a headless `Scheduler` from the passed `WorldSnapshot` with empty EMC programs by default, and field-diffs pool state at each tick. First mismatch throws `Divergence(tick, kind, slot, field, expected, actual)` and halts. First use exposed a real gap: `WorldSnapshot(loading:baseline:)` wasn't propagating `speedPerTick` / `speedRemainder` / `movingSpeed` / `currentDestinationX` / `currentDestinationY` from the save record into the live `UnitSlot` — fields the save decoder already read but never wired. Four-line fix brings tick 0 to byte-identical parity with OpenDUNE's golden. Tick 1 diverges at `unit[22].targetMove` (expected 54957, actual 0) because our scheduler runs with empty UNIT.EMC by default and OpenDUNE's harvester script writes targetMove on its first tick after load — the next slice wires real EMC. 4 new `ParityHarnessTests` (parse happy, parse malformed, tick-0 parity real save, tick-1 currently-diverges doc). **975 / 96 / zero warnings.**

- **2026-04-23 — Tick-parity OpenDUNE capture side.** Patch `tick_parity_dump.patch` (480 lines, touches `src/opendune.c` / `src/file.c` / `src/file.h` / `source.list`; adds `src/parity.{c,h}`) gives vanilla OpenDUNE four CLI flags — `--parity-data-dir` / `--parity-load` / `--parity-dump` / `--parity-ticks` — and a deterministic fast-forward mode. In parity mode: `SDL_VIDEODRIVER=dummy`, audio driver init skipped, `Timer_Add(Timer_Tick)` + `Timer_Add(Video_Tick)` registrations skipped so SIGALRM doesn't drift counters, both data dirs pinned via new `File_SetDataDir` / `File_SetPersonalDataDir` helpers (suppresses the ini/HOME/bundle fallbacks in `File_Init`), `Tools_RandomLCG_Seed(0)` + `Tools_Random_Seed(0)` before *and* after `SaveGame_LoadFile` so the LCG doesn't inherit `time(NULL)` from `OpenDune_Init`, then `GameLoop_Main` short-circuits to `Parity_Run` which loads the save, emits tick 0, and runs a tight `g_timerGame++ / g_timerGUI++ / GameLoop_{Team,Unit,Structure,House}()` loop for N ticks writing one JSON line per tick. 200 ticks finish in ~1 s wall-time on M-series. Deterministic: two runs produce byte-identical output. Golden fixture committed at `Code/Core/Tests/DuneIICoreTests/Fixtures/ParityGoldens/save001_200ticks.jsonl` (1.3 MB, 201 lines). RNG globals deliberately not in the schema — `src/tools.c` is in the design-doc no-touch list, and both engines seed from the save bytes by construction. Swift `ParityHarness` remains for the next session. **971 / 95 / zero warnings** (no Swift-side changes).

- **2026-04-23 — Tick-parity harness design doc + `Unit_SetSpeed` gameSpeed pipeline.** Groundwork for §6 closure. Design doc at `Documentation/Architecture/TickParityHarness.md` pins the approach: start state is a mid-mission save (both engines load it natively — no input injection), golden capture is a per-tick `.jsonl` dump from a patched OpenDUNE, replay is a new `Simulation.ParityHarness` that diffs pool state field-by-field. `Simulation.Tools.adjustToGameSpeed` ports `Tools_AdjustToGameSpeed` (`src/tools.c:20`); `Units.setSpeed` + `Scheduler.tickMovement` apply it for non-winger units with a `Scheduler.gameSpeed: UInt8 = 2` knob. At default speed the adjustment is identity so nothing regresses — machinery is there so the harness can drive OpenDUNE at other speeds. 13 new `ToolsGameSpeedTests`. **971 / 95 / zero warnings.**

- **2026-04-23 — STARPORT cargo drop (`tickFrigateUnload`).** Closes the CHOAM follow-up. New scheduler pass walks a spawned FRIGATE's `linkedID` chain, places each off-map cargo unit on a passable tile in the 12-tile ring around the 3×3 STARPORT, clears `inTransport`, and frees the frigate when the chain drains. Partial drops (ring-full) preserve the remaining chain for a later tick. House-ownership-gated. Cosmetic descent animation stays queued (needs FRIGATE.EMC host-fns). 5 new `FrigateUnloadTests`. **958 / 94 / zero warnings.**
- **2026-04-23 — Mentat briefing slice 3: `.ENG` decoder + briefing text.** New `Formats.Strings.decode(_:compressed:)` ports OpenDUNE's string-table + byte-pair decompression (`src/string.c:42..81`) — uint16 LE offset table, couples-based 2-char expansion, 0x1B escape for extended literals, Latin-1 output. `MentatScene` loads `TEXT{H,A,O}.ENG` for the player house and renders the briefing string (local index `campaignID * 4 + 4`) as a word-wrapped label. No voice — OpenDUNE explicitly stops voice for briefings (`src/gui/mentat.c:102`); Dune II briefings are text + music only. 11 new tests (9 decoder + 2 resolver). Briefing-music playback deferred. **958 / 94 / zero warnings.**
- **2026-04-23 — Mentat briefing slice 2: `GUI_Mentat_Animation` port.** Mentat face comes alive: eyes blink + drift, mouth cycles 0..4 on amplitude-driven timers, Atreides' book / Ordos' ring alternate on house-specific reschedules. New `MentatAnimator` pure struct (14 unit tests covering every branch) + scene wiring that loads `MENSHP{H,A,O,M}.SHP`, slices the 15-frame atlas into eyes/mouth/shoulder/other, and positions each sprite at the per-house offset from `s_mentatSpritePositions`. Animator ticks every render frame (60 Hz = 1 GUI tick) against a `BorlandLCG` seeded off the scenario name. `speakingMode` pinned to `.speaking` until slice 3 brings real voice playback. **958 / 94 / zero warnings.**
- **2026-04-23 — Mentat briefing slice 1.** Boot flow re-routes via a proper Mentat screen: `Intro → Mentat(scenarioName) → Scenario(scenarioName)`. `MentatScene` now picks `MENTAT{A,H,O,M,S,F}.CPS` for the player's house (inferred from the scenario filename's 5th char) and loops the scenario's `BriefPicture` WSA in the sub-screen at ~10 fps. Click advances to the scenario. Text / voice / mouth animation land in slices 2–3. 5 new tests + manual-verification checklist in `Documentation/Algorithms/MentatBriefing.md`. **958 / 94 / zero warnings.**
- **2026-04-23 — Movement jumps fixed: per-tile stall + 60 Hz render interp.** User reported visible jumps while units moved. Headless screenshots + per-tick pos dumps pinned two causes: (1) sim-side Δ=0 stall at each tile boundary (overshoot-snap tick moved Δ=32, next tick popped route with Δ=0) — fixed by inline route-pop in the overshoot-snap branch; (2) render-side stepping (scene at 60 Hz, sim at 12 Hz, no interpolation) — fixed by snapshotting prev/curr unit positions per sim tick and lerping markers every frame via `interpolateUnitMarkers`. Teleports (|Δ| ≥ 1 tile) skip the lerp. Interpolation disabled at speedMultiplier > 1. 1 new regression test pins "no more than 3 stalls across a 13-tile route-driven move" (pre-fix was ~13). **958 / 94 / zero warnings.**
- **2026-04-23 — Harvester UX batch: bounded seek + playable-rect + crush + manual-halt.** Closes four user-reported gaps: (1/2) `findNearestSpiceTile` replaced by `findSpiceNear(from:radius:playableRect:...)` — port of `Map_SearchSpice` (`src/map.c:1117`). Clamps to playable rect, caps at `autoHarvestSpiceSearchRadius = 20`, skips structure- / unit-occupied tiles, prefers thick spice within distance 4. `ScenarioRuntime.load` seeds `Scheduler.playableRect`. (3) Crush damage in `tickMovement` post-step — tracked + harvester movers kill foot units on tile entry via `applyUnitDamage` (port of `Unit_Move`'s `isTracked && d<48` branch at `src/unit.c:1328..1349`). (4) Coherence pin respects `.stop` as organic so a user-ordered `orderMove` stops the harvester on arrival instead of auto-resuming seek. Full harvester in `.stop` still flips to `.returnAction`. 14 new tests across 2 new suites + `SpiceSeekTests` rewritten. **958 / 94 / zero warnings.**
- **2026-04-23 — STARPORT slice 5c-ui: CHOAM cart panel in `ScenarioScene`.** Closes the CHOAM arc. Left-click a friendly STARPORT → sidebar flips from BUILD to CHOAM (yellow header, one row per stocked unit with sprite + name + price + stock + in-cart count). Left half of a row = decrement, right half = increment; SEND commits via `Simulation.Structures.commitStarportOrder(...)` and drains `house.credits`, CANCEL drops the controller (credits auto-refund because the drain was virtual). Stray map clicks or non-STARPORT structure clicks auto-cancel. 4 new `ClickOutcome` cases, `runtime.starportController: StarportController?`, `renderStarportPanel()` + `starportHitTest(atY:x:)` in scene. 2 new `ScenarioRuntimeTests` for end-to-end flow. Manual-verification checklist at `Documentation/Algorithms/Starport.md`. Frigate landing animation stays deferred. **958 / 94 / zero warnings.**
- **2026-04-23 — STARPORT slice 5c-sim: `StarportController` + CHOAM pricing + runtime wiring.** `ScenarioRuntime.load` now seeds `Scheduler.starportStock` from `scenario.choamInventory`. New `DuneIIRendering.StarportController` is the pure cart state machine: `open(...)` builds one row per house-available listed unit type, computes a deterministic per-session `unitPrice` via a `BorlandLCG`-seeded draw, and exposes `increment` / `decrement` that atomically track `inCart`, `stockRemaining`, `availableCredits`, and `cartTotal`. `pendingOrders()` → `[Order]` flows into `Structures.commitStarportOrder`. Pricing is an exact port of `src/gui/gui.c:2726` (`(bc/10)*4 + (bc/10)*(rng(0..6)+rng(0..6))`, cap 999, exactly two LCG draws per call). 14 new tests in `StarportControllerTests`. Scene rendering stays for slice 5c-ui.
- **2026-04-23 — STARPORT slice 5b: order commit + frigate delivery + availability bump.** `Scheduler.starportStock: [Int16]` + new `HouseSlot.starportTimeLeft` complete the sim side of CHOAM. `Simulation.Structures.commitStarportOrder(...)` ports the cart-commit block (chains units off-map on `HouseSlot.starportLinkedID`, decrements stock to `-1` on drain, seeds the countdown). Two new scheduler passes: `tickStarportDelivery` counts down per house and spawns a FRIGATE when zero (moving the linked chain onto `frigate.linkedID` with `inTransport=true`); `tickStarportAvailability` draws a random type and bumps stock (`-1→1`, else `+1` cap 10). `WorldSnapshot` reads `starportTimeLeft` from the save chunk. 12 new tests (StarportDeliveryTests). UI + credit drain remain for slice 5c.
- **2026-04-23 — STARPORT slice 5a: `[CHOAM]` loader + starport buildable mask.** `Scenario.choamInventory` (27-entry `Int16` array indexed by `UnitType.typeID`) now populates from the scenario INI's `[CHOAM]` section via `loadChoam` — port of OpenDUNE's `Scenario_Load_Choam` (`src/scenario.c:473`). New `Simulation.Structures.starportBuildableUnits(inventory:houseID:)` returns a `UInt32` bitmask of currently-orderable units after honouring `UnitInfo.availableHouse` gating. 9 new tests (4 INI + 5 buildable-mask). Runtime mutation + UI stay in slices 5b/5c.
- **2026-04-23 — Fallback slide per-tile gate + OpenDUNE crush semantics.** `tickMovement`'s fallback slide now samples `isTilePassable` on each tile-boundary entry and halts (keeping `targetMove`) when the next tile is blocked — closes the post-`move-halt` clip-through-structure gap. Separately, `isTilePassable` + `CalculateRoute`'s scorer gained the crush rule from `Unit_GetTileEnterScore` (`src/unit.c:2354`): tracked + harvester movers may enter foot-occupied tiles. 2 new tests (crush parity, wheeled still detours), 1 flipped test (`fallbackSlideHaltsAtStructure`), 1 updated (`routeStepBlockedByUnitKeepsTargetMove` — blocker switched TROOPER→TANK).
- **2026-04-23 — Movement long-run tracking suite.** New `MovementLongRunTests` drives `Scheduler.tick()` across hundreds of ticks and pins: 8-direction fallback-slide reachability, per-tick delta ≤ 1 tile (no teleport), octant-locked route-driven orientation, and pathfinder detours around refineries + mountain blocks without ever entering a blocked tile. A `KNOWN GAP` test documents that after a `move-halt`, the fallback slide walks straight through intermediate structure tiles — flip the assertion when the scheduler grows per-tile passability in the slide branch or an on-halt pathfinder replan. 13 new tests (19 with the 8-octant parameterisation).
- **2026-04-23 (merged from main) — Harvester post-drain stall fixed.** User reported: harvester reaches spice, harvests a tile's worth, then stops. Root cause: with nil slot 0x2A `Script_Unit_Harvest` the real UNIT.EMC halts the engine mid-HARVEST loop, but one branch still flips action to RETURN prematurely. RETURN branch then had nothing to dock (no targetMove) and the harvester sat on spice forever. Fixes: (a) RETURN branch re-issues `orderMove` to nearest refinery when idle (ports `Script_Unit_GoToClosestStructure`). (b) Coherence pin keeps `{harvest, move, returnAction}` trio organic while overriding EMC drift to `stop`/`guard`/`attack`/etc; idle RETURN at amount<100 demotes to HARVEST. New `longRunCompletesHarvestCycle` test drives the full cycle over 4000 ticks with real UNIT.EMC + realistic RNG.
- **2026-04-23 (merged from main) — Harvester adjacency dock + `Map_FixupSpiceEdges` port + CLAUDE.md OpenDUNE rule.** (a) RETURN-action harvesters were stuck next to the refinery because the dock probe matched only footprint tiles, but the pathfinder halts them on an adjacent tile. New `Scheduler.refineryAtOrAdjacent` checks same-house footprint OR 4-cardinal neighbour; RETURN branch uses it. (b) Drained spice showed hard blocky edges — repaint was always assigning the "isolated" variant (offsets 49 / 65). Ported `Map_FixupSpiceEdges` (`src/map.c:725..764`) via new `SpiceMap.edgeBitfield(at:)`; repaint now picks one of 16 edge variants per tile AND refreshes the 4 cardinal neighbours. `spiceLevelDidChange` signature grew a `SpiceMap` param. (c) CLAUDE.md: added "always consult OpenDUNE for game logic" rule to Conventions. 8 new tests total.
- **2026-04-23 — Harvester continuous loop + fullness bar.** `tickHarvesting` used `!inTransport` as the anti-reharvest gate, but `harvestSpiceStep` sets `inTransport=true` on the first pickup — so ordered harvesters picked up 1 unit then froze. New `Scheduler.isHarvesterDocked` walks the refinery → harvester `linkedID` chain; the real "don't harvest" gate. Added small cyan fullness bar above every harvester marker (`amount / 100`). New `HarvesterContinuousLoopTests` (6 tests) pin the invariants.
- **2026-04-22 — Movement keeps targetMove on halt + move-track + regression tests.** `tickMovement` now clears only `route` + `currentDestination` when a planned step becomes impassable, keeping `targetMove` live so the next tick's fallback slide (or the script's next `CalculateRoute`) rescues the unit. Hunt enemies no longer wedge in concave building angles; user-ordered moves re-route around transient blockers instead of stalling. Two `MovementPassabilityTests` updated + new `MovementSelfHealTests` suite (6 tests). New `move-track` tracer streams per-tick tile / pos / orient / goal for every moving ground unit. (An earlier in-flight attempt that ran `Pathfinder.findRoute` inline inside `tickMovement` was reverted — it pre-empted the script's `CalculateRoute` + `setSpeed` and stalled fresh user-ordered moves.) **838 / 84 / zero warnings.**
- **2026-04-22 — Harvester pin widens.** Any non-HARVEST idle harvester flips back to HARVEST so user-ordered harvests don't strand in STOP / GUARD after arrival. Test updated.
- **2026-04-22 — Pathfind around units.** `isTilePassable` + CalculateRoute scorer treat unit-occupied tiles as blocked (OpenDUNE `Unit_GetTileEnterScore`). Mover's own tile + projectiles + wingers skip.
- **2026-04-22 — Scene fills window.** `.resizeFill` scaleMode; sidebar pinned right via dynamic `sidebarX` / `mapAreaWidth` / `mapAreaHeight`; `didChangeSize` re-lays everything and logs.
- **2026-04-22 — Attacker flips to default on target death.** `tickAttackHold` now reverts to `actionsPlayer[3]` (GUARD/STOP) when targetAttack's slot has been freed. Logs under `attack-hold`.
- **2026-04-22 — Corpse transparency + per-house palette + muzzle alpha.** `corpseTexture(houseID:)` builds per-house `.index0Transparent` CGImages so corpses show on sand without the black box and render in the owner's colour. IMPACT_SMALL alpha capped at 0.3 in the per-tick recompute.
- **2026-04-22 — Regression pair (placement + corpse).** Stale `selectedStructureIndex` short-circuit blocked placement clicks when placementType was set. `stopAtPosition` wiped corpses on follow-up bullet impacts — now skips `corpseInfantry` entries. New headless `dump explosions` verb for diagnosing.
- **2026-04-22 — Infantry corpse + dim muzzle flash.** `ExplosionType.corpseInfantry` drops a `sandDeadBodies` sprite on infantry death (240-tick lifetime, fades in final frames). `IMPACT_SMALL` alpha lowered from 0.9 → 0.3 so muzzle flashes stop cluttering the view.
- **2026-04-22 — Playtest batch 3.** (a) HP percent conversion (`u->hitpoints = max * percent / 256`) — no more `HP: 256/100`. (b) Camera clamped to playable rect via `clampMapPan()`. (c) Minimap cropped to playable rect. (d) Minimap viewport overlay — white outline shows camera view.
- **2026-04-22 — Unify structure marker / halo.** `syncStructures` switched to `SKShapeNode(rect:)` with the same `screenPosition(ax, ay+dims-1)` + explicit size the halo uses — single source of truth for the footprint rect.
- **2026-04-22 — Attack chain end-to-end.** Fix 1: `WorldSnapshot.init(scenario:)` now allocates via `UnitInfo.indexStart..indexEnd` (Unit_Allocate port) — scenario units land in 22..101, bullet range 12..15 stays free. Fix 2: new `Scheduler.tickAttackHold` — stops ACTION_ATTACK units at fire range and snaps orientation toward target so `Script_Unit_Fire`'s gate can pass. Projectiles skipped so bullets still fly. End-to-end verified in headless: bullet arrives, damage lands, HP decreases.
- **2026-04-22 — Camera.** `mapContainer` parent node holds all map content; `mapZoom=4` default, `mapPan` centred on player yard at load, arrow keys pan, `=` / `-` zoom [1..16]×. Click classifier inverse-transforms through scale+pan.
- **2026-04-22 — Map trim.** `ScenarioRuntime.load` stamps `veiledTileID` (fog-of-war offset 16) on every cell outside `scenario.playableRect` (port of `g_mapInfos[mapScale]`).
- **2026-04-22 — Windtrap vanes.** Palette index 223 (the animated "windtrap colour" slot per `GUI_PaletteAnimate`) statically resolved to index 12 at load. Pink → grey.
- **2026-04-22 — Unit sprite scaling.** Render at native SHP pixel size (matches `Sprite_GetWidth` / `Sprite_GetHeight`), not a forced 16×16 fit — harvester > trike > infantry hierarchy restored.
- **2026-04-22 — Bug batch 2.** `tickSpriteOffsets` now gates on active movement (guard-state infantry stop animating). Added `fire-gate` / `damage` / `scene-halo` tracers.
- **2026-04-22 — Attack muzzle flash.** `Script_Unit_Fire` spawns a cosmetic `IMPACT_SMALL` explosion at the shooter's tile on every successful fire so attack intent is always visible. Extended `FireTests.fireSuccess`. 832 green / 83 suites.
- **2026-04-22 — Spice-bloom walk-into detonation.** `Simulation.Bloom.explodeSpice` ports `Map_Bloom_ExplodeSpice` + `Map_FillCircleWithSpice` (radius-5, edge 50% via rng, centre gets +2). `Scheduler.tickBloomDetonation` sweeps non-sandworm units on bloom tiles between `tickMovement` and `tickUnits`. New `Host.groundTileOverride` closure resets the bloom cell to sand. 6 new tests. 832 green / 83 suites.
- **2026-04-22 — Action shortcut keys (A/M/H/R).** `UnitCommandController.stage(action:pool:)` primes a staged intent; next left-click resolves (Harvest/Return harvester-only; Attack falls back to move on empty). New `.orderHarvest` / `.orderReturn` runtime outcomes; scene keybindings + HUD `STAGE X (click target)` indicator. Headless `stage <name>` verb. 11 new tests. 826 green / 82 suites.
- **2026-04-22 — Slice 9: scene repaint on SpiceMap.apply.** New `Host.spiceLevelDidChange` callback; scheduler fires on level transitions; runtime rewrites tileGrid groundTileID per `Map_ChangeSpiceAmount`'s offsets. 3 new tests. 815 green / 81 suites / zero warnings.
- **2026-04-22 — Scene tile rendering: per-house palette remap.** `ScenarioScene` lazy-caches house-remapped `SKTexture`s via the same `(tileID, houseID)` path used by `ScreenshotRenderer`; `syncGroundTiles` runs right after `runtime.load` so Atreides CY renders in blue on first frame.
- **2026-04-22 — Structure ICN tiles get per-house palette remap (renderer).** New `Formats.Icn.TileSet.pixels(forTile:houseID:)` applies OpenDUNE vanilla's `(c & 0xF0) == 0x90` 0x90..0x9F remap. `ScreenshotRenderer` routes structure cells through a lazy `(tileID, houseID) → CGImage` cache. New headless `dump icntile` / `dump iconmap` debug commands.
- **2026-04-22 — Screenshot test suite + shared `ScreenshotRenderer`.** 5 golden tests (initial, unit halo, structure halo, slab placement, windtrap placement). `DUNEII_REGENERATE_GOLDENS=1` rewrites fixtures. Shared renderer consumed by both the headless `screenshot` command and the tests. Also fixed a gap where `ScenarioRuntime.load` didn't stamp iconGroup tiles for scenario-placed structures.
- **2026-04-22 — Headless screenshot command.** `screenshot x y w h path` on `duneii-headless` writes a PNG of a tile region (16 px/tile, ground layer only; pre-renderer extraction).
- **2026-04-22 — Playtest bug-fix batch (5 issues).** Sidebar click Y offset, speed multiplier (`,`/`.`), iconGroup frame pick (`Structure_UpdateMap` offset), structure house-colour outline centre, harvester STOP→HARVEST pin.
- **2026-04-22 — Carryall pickup 8c.** `Simulation.Units.dropCarryall` detaches the ferried harvester + frees the carryall on arrival. New `Scheduler.tickCarryallFerry` pass (between tickMovement and tickUnits) picks up carryalls whose `targetMove` just cleared, invokes drop. Closes the carryall pickup loop. 5 new tests. 807 green / 79 suites / zero warnings.
- **2026-04-22 — Carryall pickup 8b.** `Simulation.Units.callCarryall` spawns a CARRYALL in-pool at the harvester's tile with `inTransport=true` + `linkedID=harvester` + `targetMove=encoded(refinery)`. `tickHarvesting` picks between free-refinery (8a), carryall-ferry (new), and on-foot-only-option branches; `countRefineries(houseID:structures:)` gates ferry on ≥ 2 refineries. 4 new tests, 1 superseded. 802 green / 79 suites / zero warnings.
- **2026-04-22 — Carryall pickup 8a.** `findFreeRefinery` + tickHarvesting preference for free refineries over nearest. Observable: two harvesters parallelise across two refineries. 9 new tests. 798 green / 79 suites / zero warnings.
- **2026-04-22 — Sidebar minimap.** New `DuneIIRendering.Minimap` produces a 64×64 RGBA buffer from live tile grid + pools. `ScenarioScene.refreshMinimap()` rebuilds the `SKTexture` every tick; 120×120 pt node sits above the info panel. Terrain colours per `LandscapeType`; units + structure footprints overlaid in house colour; projectiles (types 18..24), slabs + walls skipped. 12 new tests. **789 green / 78 suites / zero warnings.**
- **2026-04-22 — Task 9: Pathfinder + scheduler redirect to passable neighbour.** `findRoute.resolveReachable` + `Scheduler.nearestPassableNeighbor` stop hunt-action enemies from halting at the player CYARD. Mission 1's 3 Ordos hunters now march toward the base. 3 new tests.
- **2026-04-22 — Task 8: Keyboard shortcuts (Escape / Tab).** Deselect all + cycle through friendly units. 2 new tests.
- **2026-04-22 — Task 7: Right-click attack on enemy structures.** New `Simulation.Units.orderAttackStructure`; `UnitCommandController.handle` takes a structures pool + scans for enemy structures; right-click promotes to `.orderAttackStructure` when the click lands on one. Non-turret attackers drive to the building, turreted rotate in place. 4 new tests. 772 green.
- **2026-04-22 — Task 6: Spawn harvester on first refinery.** `ensureHarvesterAvailable` ports OpenDUNE's same-named helper (simplified; no carryall ferry). Triggers on REFINERY placement. New `harvester-spawn` tracer. 1 new test. 768 green.
- **2026-04-22 — Selection UI tasks 1-5: enemy unit selection, runtime structure selection, info panel, structure halo, ground tile repaint.** Live info panel + halo for any selected entity; enemies show info only; ground tiles now repaint as slabs + buildings get placed. 5 commits + 3 new tests.
- **2026-04-22 — Movement port + infantry sprite fix.** `Unit_SetSpeed` + `Unit_MovementTick` ported with the subpixel accumulator (`speedPerTick` / `speedRemainder`). `Tile_MoveByDirection` drives actual motion via `Pos32.moved`. Overshoot detection mirrors `unit.c:1419`. Infantry resolver now uses `values_32C4` buckets + `values_334A` phase with an animated `spriteOffset` that advances every 5 ticks for moving infantry. 9 test-file updates to match new slot-field semantics (`movingSpeed` carries the percent, `speed` the tile-hop clamp). 763 green / 77 suites / zero warnings.
- **2026-04-22 — Mission-1 bug hunt pass 1.** Scenario-structures stamp `hasStructure` on tileGrid at load; runtime-placed slabs/structures stamp live; CYARD flips IDLE after commit; landscape closures capture a TileGridRef class box so mutations propagate. 14 new install-gated runtime tests cover the full click flow + build chain. 763 green / 77 suites / zero warnings.
- **2026-04-21 — Headless harness + ScenarioRuntime extraction.** New `ScenarioRuntime` owns all non-visual sim state + intent methods (`load`, `tick`, `leftClick`, `rightClick`, `sidebarClick`, `selectYard`). `ScenarioScene` becomes a thin SpriteKit renderer. New `duneii-headless` executable drives the runtime from stdin (commands: load, tick, click, rclick, sidebar, yard, dump [units/structures/houses/tile/build/spice/scene], validity, enqueue, build, quit) — Log facade to stderr. First bug caught: runtime `tick()` wasn't refreshing BuildPanelController surface state so headless callers saw stale yardState. Fixed.
- **2026-04-21 — Harvester auto-seek spice (slice 7).** Idle HARVEST-action harvester off spice auto-issues `orderMove` to nearest spice via new `Scheduler.findNearestSpiceTile`. Completes the self-sustaining harvest cycle. 5 new tests. 749 green / 76 suites / zero warnings.
- **2026-04-21 — Placement UX + movement passability gate.** (1) Building placement gained a hover ghost footprint (green/yellow/red by validity), persistent placement mode across rejections, "PLACING X" HUD indicator, red toast on rejection; `commitPlacement` exits mode only on success. (2) New `Scheduler.isTilePassable(tileX:tileY:movementType:)` combines live structure footprint check + `LandscapeInfo.movementSpeed` gate; wired into both `tickMovement` paths (route step + targetMove fallback). Winger/slither/projectiles bypass. Halts unit + clears route on impassable step with `move-halt` log. 7 new tests. 744 green / 75 suites / zero warnings.
- **2026-04-21 — Harvester AI loop (slice 6).** `tickHarvesting` auto-undocks after empty (6a) + seeks nearest same-house refinery when full / docks on arrival (6b). `Scheduler.findNearestRefinery` (squared-distance) + `refineryAt` (footprint hit) helpers. Every transition logs under `harvest-tick`. 5 new tests. 737 green / 74 suites / zero warnings.
- **2026-04-21 — Scheduler + scene wiring for harvesting (slice 5).** `Host.spiceMap`, `Scheduler.harvestRNG`, new `Scheduler.tickHarvesting()` at cadence=3, scene seeding via `TileResolver.landscapeType`, shared RandomSource. `harvest-tick` tracer summarises entry. 5 new tests. 732 green / 73 suites / zero warnings.
- **2026-04-21 — Runtime spice map (slice 4 of spice income).** `Simulation.SpiceMap` (64×64 Level enum) + `apply(delta:at:)` ports OpenDUNE's `Map_ChangeSpiceAmount` transition rules. `init(landscapeAt:)` seeds from a closure; `landscapeByte(at:)` bridges to the `harvestSpiceStep` closure shape. Each level transition logs under `spicemap` with before→after. 7 new tests in `SpiceMapTests`. 727 green / 72 suites / zero warnings.
- **2026-04-21 — Harvester on-spice pickup (slice 3 of spice income) + Package.swift CLAUDE.md excludes.** `Simulation.Units.harvestSpiceStep` ports `Script_Unit_Harvest` as a pure function with tile reader / writer / RNG closures; each step logged under `harvest`. Package.swift now `exclude: ["CLAUDE.md"]` on every target to silence 5 unhandled-resource warnings from the per-module CLAUDE.md files added earlier today. 10 new tests in `HarvestSpiceStepTests`. 720 green / 71 suites / zero warnings.
- **2026-04-21 — Harvester dock / undock primitives (slice 2) + log retrofit.** `Simulation.Structures.dockHarvester` (chain-link + state=READY) and `undockHarvester` (unlink + exit-tile reposition + state=IDLE when chain empty) — pure-sim ports of `Unit_EnterStructure` + `Script_Structure_FindAndLeaveUnit`. Step-by-step logs under the `dock` tracer. Retrofitted logs across `refineSpiceStep`, `startConstruction`/`cancelConstruction`/`completeConstruction`, `createUnit`, `setRallyPoint`/rally-fire. 10 new tests in `HarvesterDockTests`. 710 green / 70 suites / zero warnings.
- **2026-04-21 — Harvester → refinery spice deposit (slice 1 — pure-sim refine step).** `Simulation.Structures.refineSpiceStep` ports OpenDUNE's `Script_Structure_RefineSpice` deposit math as a pure function: `harvesterStep = (refineryHP × 256 / hpMax) × 3 / 256` capped by amount; `creditsStep = 7` flat (+ optional −1..+2 enemy jitter via closure); credits += step × harvesterStep; amount decrements; `inTransport` clears on empty. No script / dock / tile wiring yet. Credits saturate at `UInt16.max`. 13 new tests in `HarvesterSpiceDepositTests`. 700 green / 69 suites / zero warnings. Design: `Algorithms/HarvesterSpiceDeposit.md`.
- **2026-04-21 — Rally-point click on factories.** New `StructureSlot.rallyPointPacked: UInt16` (`0xFFFF` = unset) + `Simulation.Structures.setRallyPoint(yardIndex:tile:pool:)` writer (factory-kind + bounds validated; `tile: nil` clears). `completeConstruction` reads the rally on success and fires `Simulation.Units.orderMove` on the freshly-spawned unit. Scene: `rightMouseDown` branches — unit-command first (selection → move/attack), then rally-point when a player factory is selected. Yellow diamond `SKShapeNode` marker at the rally tile, refreshed on yard-switch + rally updates. This is our own feature — OpenDUNE has no rally system. 11 new tests in `FactoryRallyPointTests`. 687 green / 68 suites / zero warnings on clean build. Design: `Algorithms/FactoryRallyPoint.md`.
- **2026-04-21 — Per-module CLAUDE.md + dropped strict TDD.** Each SwiftPM target (`DuneIICore`, `AssetExport`, `DuneIIRendering`, `assetgen`, `duneii`) now carries its own `CLAUDE.md` with local layout / conventions / entry points. Root `CLAUDE.md` points at them. Feature workflow relaxed: tests still mandatory but now written after the implementation in the same commit. No code changes.
- **2026-04-21 — Wire EMC-driven SetSpeed for ground units.** Two-part port fix. (1) `Script_Unit_GetInfo` subcase `0x0B` now reads `currentDestinationX/Y` instead of `targetMove` (OpenDUNE `src/script/unit.c:968`) — the misport made UNIT.EMC's MOVE handler loop at the "already moving" wait on tick 1 and never reach `CalculateRoute`. (2) `makeCalculateRouteUnit` now ports the landscape-speed slice of `Unit_StartMovement` (`src/unit.c:1088..1105`): looks up landscape via the new `Scripting.Host.landscapeAt` closure, reads `LandscapeInfo.movementSpeed[movementType]`, applies HP<half 1/4 slowdown (non-winger only), scales by `byScenario`, writes `slot.speed`. `ScenarioScene.setUpScheduler` wires the closure from the snapshot tile grid. We bypass `Unit_SetSpeed`'s `movingSpeedFactor` / gameSpeed / speed-vs-speedPerTick split — sub-pixel parity is queued for the tick-parity golden harness. 4 new tests (GetInfo 0x0B + 3 on CalcRoute: landscape-set, HP<half, nil-closure guard). 675 green / 67 suites / zero warnings on clean build. Design: `Algorithms/EmcDrivenSetSpeed.md`. Insight: `scripting-unit-getinfo-0b-is-currentdestination.md`.
- **2026-04-21 — P5 slice 6d: HUD credits label.** "Credits: N" SKLabelNode pinned at top of the right-hand sidebar; refreshed every tick from new pure-sim helper `Simulation.House.credits(for:in:)` (returns `UInt16?` — `nil` for unallocated / out-of-range house IDs, `0` for allocated-but-empty, distinguishing "—" from "0" in the label). Mission 1 Atreides starts at 1000 and ticks down as BUSY yards drain. 5 new tests in `HouseCreditsLookupTests`. 671 green / 67 suites / zero warnings on clean build. Design: `Algorithms/HudCreditsLabel.md`.
- **2026-04-21 — Right-click-attack on enemy units (slice 2).** New `Simulation.Units.orderAttack(poolIndex:targetUnitIndex:units:)` composes `Unit_SetAction(ACTION_ATTACK)` + `Unit_SetTarget` (`src/unit.c:497, 1131`) into one pure-sim write. Always: `targetAttack = EncodedIndex.unit(targetUnitIndex)`, `actionID = attack` (0), `currentDestination{X,Y} = 0`. Non-turret attackers (TROOPER, TRIKE, QUAD, DEVASTATOR) also get `targetMove = targetAttack` + `route[0] = 0xFF` so the chassis chases; turreted units leave move state alone (rotates turret in place — `unit.c:1161`). Controller side: new `Action.orderAttack` + `enemyUnitAtTile` scan; the right-click handler discriminates empty / friendly / enemy under the click and promotes to attack on enemy. Slice-1 right-click semantics regression-guarded. `ScenarioScene.applyCommandAction` gets the new case; logs `unit-order-attack unit=N target=M ok=true`. 14 new tests (8 in `UnitOrderAttackTests`, 6 added to `UnitCommandControllerTests`). 666 green / 66 suites / zero warnings on clean build. Design: `Algorithms/UnitOrderAttack.md`. Deferred: enemy-structure attack (slice 3), target blink + voice cue, alliance check.
- **2026-04-21 — Movement bug triage: fix scheduler entry-point, port SetActionDefault, fix IdleAction turret-skip.** Three pre-existing bugs surfaced by player-ordered MOVE actions. Scheduler now loads `unitVM` at `entryPoints[slot.type]` and sets `variables[0] = actionID` separately (OpenDUNE's split: `src/unit.c:520`). `Script_Unit_SetActionDefault` (slot 0x0A) ported so MOVE→GUARD transition works on arrival. `IdleAction` split restored: 50% body, 50% turret — non-turret units stop excess rotation. 3 new tests + 1 rewrite. 652 green / 65 suites / zero warnings on clean build. Insight: `scripting-unit-entry-point-by-type-not-action.md`.
- **2026-04-21 — Unit selection + move orders (slice 1).** `Simulation.Units.orderMove` (4-field pure-sim write). `DuneIIRendering.UnitCommandController` (pure state machine, parallel to `BuildPanelController`). `ScenarioScene` wiring: left-click routes to command controller first (consumes on `.selectUnit`, falls through on `.deselect` / `.none`); new `rightMouseDown` for move orders; green halo `SKShapeNode` parented to selected unit's marker; per-tick `validateSelectionHalo` clears stale selections. 18 new tests (7 orderMove + 11 controller). 649 green / 65 suites / zero warnings. Design: `Algorithms/UnitSelectionAndOrders.md`. Insight: `simulation-action-id-drives-script-reload.md`.
- **2026-04-21 — Boot flow: dropped mentat screens + fixed map-click scene-cycle bug.** Two UI-layer fixes in response to user report. `ScenarioScene.mouseDown`'s `.none` fallback replaced with `break` so unclassified map clicks don't route to mainMenu. `GameController` boots Intro → Scenario directly, skipping `MainMenuScene` + `MentatScene`; jukebox kickoff moved to scenario entry. Wrote design doc `Algorithms/UnitSelectionAndOrders.md` for the next slice. No sim changes; 631 green / 63 suites / zero warnings.
- **2026-04-21 — P5 slice 6c: factory credit drain + refund.** `UnitInfo.buildCredits` added (27 rows; 17 non-zero). `Structures.producedCost(slot:)` private helper dispatches CY→StructureInfo, factory→UnitInfo; `tickConstruction` + `cancelConstruction` share it. Drain + refund symmetric across both yard kinds. 5 new tests. 631 green / 63 suites / zero warnings. Design: `Algorithms/CreditDrainFactory.md`.
- **2026-04-21 — P5 slice 6b: credit drain + cancel refund (CY path).** `tickConstruction` and `cancelConstruction` gained `houses: inout HousePool`. CY drain = `buildCredits/buildTime` per tick; pauses when house can't pay. Cancel refunds proportional to progress. `Scripting.Host` grew `houses`. Factory drain deferred to 6c. 6 new tests. 626 green / 63 suites / zero warnings. Design: `Algorithms/CreditDrainAndRefund.md`.
- **2026-04-21 — P5 slice 6a: HouseSlot credits plumbing.** `credits` / `creditsStorage` / `creditsQuota` added to `Simulation.HouseSlot`; both `WorldSnapshot` init paths seed from `Scenario.HouseLayout` / `Formats.Save.Players.Slot`. Pure-sim groundwork for 6b's drain + 6c's HUD. 3 new tests. 620 green / 63 suites / zero warnings. Design: `Algorithms/HouseCredits.md`.
- **2026-04-21 — P5 slice 5c: cancel + queue-swap + rally tile.** `Structures.cancelConstruction` flips BUSY/READY back to IDLE. `Structures.factorySpawnTile` returns south-of-footprint for unit spawn; `completeConstruction` routes through it. Controller gains `.cancelConstruction(type:)` action; BUSY + queued-type cancels; READY + different type queue-swaps via `.enqueue`. 11 new tests. 617 green / 63 suites / zero warnings. Design: `Algorithms/BuildPanelTightening.md`.
- **2026-04-21 — P5 slice 5b-build: UnitInfo.buildTime + unit spawn on factory completion.** Factory loop closes end-to-end. `UnitInfo.buildTime` 27 rows pinned. `startConstruction` dispatches countdown by yard kind (CY→structure, factory→unit). New `Simulation.Units.createUnit` + `Simulation.Structures.completeConstruction`. Scene routes READY factory clicks to unit spawn. 11 new tests. 607 green / 63 suites / zero warnings. Design: `Algorithms/FactoryUnitSpawn.md`.
- **2026-04-21 — P5 slice 5b-select+units: factory UI + yard switching.** Click player-owned CY/factory on map → selectedYardIndex switches → sidebar re-populates. Factory rows show unit icons via `UnitSpriteAtlas.texture` + abbreviated labels. `Structures.selectableYardAt` pure helper; `startConstruction` relaxed to accept factories. `UnitInfo.buildableUnitTypes(from:)` helper. READY factory clicks gate placement (unit spawn deferred to 5b-build). 13 new tests. 596 green / 63 suites / zero warnings. Design: `Algorithms/BuildPanelFactoryUI.md`.
- **2026-04-21 — P5 slice 5a: factory buildable units (pure sim).** `UnitInfo` gained availableHouse / structuresRequired / upgradeLevelRequired (17 of 27 rows customised). `StructureInfo.buildableUnits[8]` populated for 5 factory rows. New `Structures.buildableUnitsFromFactory` dispatcher with Ordos TRIKE→RAIDER_TRIKE + SIEGE_TANK upgrade-1 quirks. 22 new tests. 583 green / 63 suites / zero warnings. Design: `Algorithms/FactoryBuildable.md`.
- **2026-04-21 — P5 slice 4d-ui: build-panel surfaces BUSY/READY + progress bar + gates clicks.** Controller gains yardState / queuedType / countDown / buildTime / progress; handle(click:) branches on yardState (IDLE → enqueue, BUSY → no-op, READY on queued → enterPlacement). Sidebar renders yellow progress bar on BUSY + green outline on READY. Scene refreshes every tick so progress animates. 7 new tests + 3 slice-3 tests removed (superseded semantics). 561 green / 62 suites / zero warnings. Design: `Algorithms/BuildPanelProgress.md`.
- **2026-04-21 — P5 slice 4d-sim: construction countdown state machine.** `StructureInfo.buildTime` added (19 rows, OpenDUNE values). `Simulation.StructureState` enum mirrors the 5 OpenDUNE state constants. `Structures.startConstruction` ports `Structure_BuildObject` tail (IDLE yard → BUSY + countDown set). `Structures.tickConstruction` drains countdown by 256 per tick; flips READY at zero. `Scheduler.tick` runs the pass. No UI changes yet — yards go BUSY invisibly. 14 new tests. 557 green / 62 suites / zero warnings. Design: `Algorithms/StructureConstruction.md`.
- **2026-04-21 — P5 slice 4c: HP degradation + adjacent-to-player-base gate.** `StructureSlot.degrades: Bool` added + plumbed from save's ObjectFlags.degrades. `Structures.create(..., tilesWithoutSlab:)` applies the OpenDUNE HP math + sets degrades=true on negative validity. `StructureLayout.adjacentOffsets` ports the 7-layout × up-to-16 tile ring. `Structures.isValidBuildLocation` gained `playerHouseID` + `tileHouseIDAt` parameters; runs the adjacency gate for non-CY placements when both are non-nil. Scene wires both. 13 new tests. 543 green / 61 suites / zero warnings. Design: `Algorithms/BuildValidationAdjacency.md`.
- **2026-04-21 — P5 slice 4b (landscape): landscape gate + slab count + `isValidForStructure2` + `notOnConcrete`.** `LandscapeInfo.isValidForStructure2` added (rock-family only). `StructureInfo.notOnConcrete` added (CYARD only). `isValidBuildLocation` gained optional `landscapeAt` closure; non-nil enables gate + slab count. `ScenarioScene.commitPlacement` wires closure from snapshot tileGrid + resolver. Return `-neededSlabs` logged as degraded; HP still max (4c applies degradation). Fixed 3 misports in `LandscapeInfo` (`5 MOSTLY_ROCK`, `13 DESTROYED_WALL`, `letUnitWobble` on rows 4/5/9). 10 new tests. 530 green / 61 suites / zero warnings. Design: `Algorithms/BuildValidationLandscape.md`.
- **2026-04-21 — P5 slice 4a: `sortPriority` ordering + minimal `isValidBuildLocation`.** 19 sortPriority values populated. `buildableTypesByPriority(from:)` helper. `StructureLayout.footprintOffsets` + `Structures.footprintTiles`. `Structures.isValidBuildLocation` checks bounds + no overlap with existing pool structures + no unit on footprint tile. Commit path in `ScenarioScene` gated on validity; invalid clicks log + reset placement state. Landscape/slab/adjacency gates deferred to 4b. 18 new tests. 520 green / 61 suites / zero warnings. Design: `Algorithms/BuildValidation.md`.
- **2026-04-21 — P5 slice 3: build panel UI on `ScenarioScene` + click → instant `Structure_Create`.** First user-visible P5 slice. New `DuneIIRendering.BuildPanelController` is a pure state machine (testable without SpriteKit); `Simulation.StructureInfo.buildableTypes(from:)` decodes the slice-1 bitmask. `ScenarioScene` widened by 128 pt for a right-hand sidebar listing buildable types (last ICN tile of each iconGroup + short label); `mouseDown` translates clicks via controller. Auto-selects the player's first construction yard. 13 new tests. 502 green / 60 suites / zero warnings. Manual-verification checklist in `Algorithms/BuildPanel.md` §8 (sandbox can't drive SpriteKit, so the user must visually confirm via `swift run duneii`).
- **2026-04-21 — P5 slice 2: `Structure_Create` + `Structure_Allocate` (pure-sim allocator + field seed).** Three new `StructureSlot` fields (`hitpointsMax`, `upgradeLevel`, `objectType`) plumbed through both `WorldSnapshot` init paths. `Simulation.Structures.allocate` routes slabs/walls to reserved slots 81/80/79 and normal structures to the first free in `[0, 79)`. `Simulation.Structures.create` seeds `state=JUSTBUILT`, `hitpoints=hitpointsMax=StructureInfo.hitpoints`, tile-aligns position via `& 0xFF00`, sets `upgradeLevel=1` for Harkonnen LIGHT_VEHICLE. AI auto-upgrade loop + `Structure_BuildObject` + wall/slab placement deferred. 17 new tests. 489 green / 59 suites / zero warnings. Design: `Algorithms/StructureCreate.md`.
- **2026-04-21 — P5 kickoff slice 1: `Structure_GetBuildable` (yard case) + `Structure_GetStructuresBuilt`.** Four new fields on `Simulation.StructureInfo` (availableCampaign / availableHouse / structuresRequired / upgradeLevelRequired), three new house bitmask constants, new `Simulation.Structures` namespace with the two pure functions. Harkonnen WOR exception + non-Harkonnen LIGHT_VEHICLE pin ported verbatim. Signed-int campaign gate (ROCKET_TURRET's `availableCampaign=0` passes the gate; upgrade-level is what locks it). 18 new tests. 472 green / 58 suites / zero warnings. Factory/starport cases deferred to slice 5. Design: `Algorithms/StructureBuildable.md`. Insight: `simulation-getbuildable-signed-int-campaign-gate.md`.
- **2026-04-21 — Worktree adaptation.** Updated hardcoded log path for project rename (`DuneIIRemake` → `DuneIISwift`). Dropped stale Fixtures resource declaration in `Package.swift`. `TestInstall.locate()` now gates on directory readability so install-gated tests skip cleanly when the sandbox can't reach main-repo `Repositories/`.
- **2026-04-21 — Team AI live hookup + log filter + INI robustness.** `Scenario.[TEAMS]` parser + `TeamAction` enum. `WorldSnapshot.teams` exposes a populated `TeamPool`. `ScenarioScene.setUpScheduler` loads `TEAM.EMC` + passes a real `teamVM` to Scheduler. Fixed INI loader to accept 2-field generated structure rows + `Concrete` / `Spice Silo` aliases. `FilteringMemoir` drops VM verbose spam by default; `DUNEII_LOG_VERBOSE=1` widens. 7 new tests. 454 green / 57 suites / zero warnings. Mission 2+ enemies now have real team AI.
- **2026-04-21 — Motion bug: `VM.load` seeds `variables[0] = typeID` + `seenByHouses = 0xFF` at scenario spawn.** Units now reach IdleAction and FindBestTarget returns real targets. User confirmed visible motion.
- **2026-04-20 — Logging (memoirs-ios + rotating file).** New `Log` facade wraps any `Memoirs.Memoir`, gated at compile time by `#if DEBUG` so release builds compile every call to a no-op. `FileMemoir` writes line-formatted items through a serial queue; `LogRotator` keeps `Logs/run-*.log` under the repo root, trimming to 10 newest. `duneii` executable installs both on boot. Log sites wired at install open, WorldSnapshot spawn, Scheduler action-load + halt + bullet arrival, Fire bullet creation, applyUnitDamage death, makeExplosion, ScenarioScene.build. 5 new tests. 447 green / 56 suites / zero warnings. memoirs-ios 2.1.2 added as SwiftPM dep.
- **2026-04-20 — Motion bug second pass: actionID seeding + NoOp default for unwired slots.** `Scenario.UnitAction.typeID` maps to OpenDUNE's `ActionType`. `WorldSnapshot.init(scenario:)` seeds `slot.actionID = spawn.action.typeID` instead of hardcoding 0; wingers get `speed = 255`. Function-table factories default unwired slots to `noOperation` so scripts run past unported opcodes and reach wired slots like `IdleAction`. New install-gated integration test runs UNIT.EMC 30 ticks and confirms units update state. 442 green / 55 suites / zero warnings.
- **2026-04-20 — Visual fidelity pass: motion bug + per-house palette + structure sprites.** Fixed the three user-visible regressions in `swift run duneii`. Scheduler now loads each engine at its action's entry point (per-slot `loadedAction` tracking detects changes; `VM.load` is a no-op when `entryPoints.isEmpty`, preserving test state). `Formats.Palette.applyHouseColors` + `CGImageFactory.makeImage(subPalette:)` + `AssetLoader.loadShp(houseID:)` + `UnitSpriteAtlas` lazy per-house cache → units now show real house colours via the `[0x90, 0x98]` palette remap, no more blend-factor tint. `Simulation.StructureInfo.iconGroupRawValue` + `ScenarioWorld(iconMap:)` stamp the last `w × h` tiles of each structure's iconGroup into the baseline map so buildings render as real ICN art. 7 new tests. 438 green / 55 suites / zero warnings.
- **2026-04-20 — P4 Phase 4 ninth slice: Team AI dynamic behaviour.** `Simulation.Units.addToTeam` / `removeFromTeam` helpers. Team slot 0x03 `AddClosestUnit` ported with byScenario + movementType gating + steal-from-weaker-team fallback. `Scheduler` grew a `teamVM` + `teamEngines` + `tickTeams` (5-opcode budget). `Scheduler.init` defaults `teamVM` to the unit VM when not supplied (safe no-op until TEAM.EMC wiring lands). 7 new tests. 431 green / 54 suites / zero warnings. Team AI is now feature-complete at infrastructure — awaiting `[TEAMS]` INI parse + `ScenarioScene.setUpScheduler` loading TEAM.EMC.
- **2026-04-20 — P4 Phase 4 eighth slice: Team AI infrastructure.** New `Simulation.TeamPool` (16 slots) + `TeamSlot` + `TeamAction` enum. `UnitSlot.team` field + WorldSnapshot plumbing. `Host.teams` + `ObjectRef.team(poolIndex:)`. Seven team script slots wired (0x01 DisplayText, 0x02 GetMembers, 0x06 FindBestTarget, 0x08 Load, 0x09 Load2, 0x0C GetVariable6, 0x0D GetTarget) via the new `teamTable(host:source:)` factory. FindBestTarget mode 4 for KAMIKAZE else 0; reuses `TargetAcquisition.findBestTargetEncoded`. Deferred: slot 0x03 AddClosestUnit, scheduler team-tick, TEAM save chunk, `TEAMS=` INI section. 11 new tests. 424 green / 54 suites / zero warnings.
- **2026-04-20 — P4 Phase 4 seventh slice: sandworm + ExplosionMultiple + Tile_MoveByRandom.** `Pos32.movedRandomly` ports `Tile_MoveByRandom` (takes `() -> UInt8` closure for RNG abstraction, consumes 2 bytes). `Simulation.TargetAcquisition.sandwormTargetPriority` + `sandwormFindBestTarget` port movement-weighted worm priority (wheeled 5000, tracked 1000, foot 100; ×4 if moving/firing; ×2 within 2 tiles). Slot 0x36 `Sandworm_GetBestTarget` + slot 0x12 `ExplosionMultiple` (1 central + 7 scatter DEATH_HAND) wired. 11 new tests. 413 green / 53 suites / zero warnings.
- **2026-04-20 — P4 Phase 4 sixth slice: sim-to-visual sync on ScenarioScene.** `Scheduler.tick()` grew a `tickExplosions` pre-pass (decrement + free on zero). `ScenarioScene.syncVisualsFromPool` is now lazy — markers created on first `isUsed` observation, so bullets spawned mid-combat get proper sprites. Explosion visuals render as coloured `SKShapeNode` discs per `ExplosionPool` slot, fading with `remainingFrames`. Scenario host now wires `explosions: Simulation.ExplosionPool` + `playerHouseID: Atreides` (so alliance checks pick the right enemies). 2 new tests. 402 green / 52 suites / zero warnings. `swift run duneii` shows live combat end-to-end.
- **2026-04-20 — P4 Phase 4 fifth slice: explodeOnDeath + structure combat.** `applyUnitDamage` now spawns `UnitInfo.explosionType` on zero HP for `explodeOnDeath` units (TANK/SIEGE_TANK/DEVASTATOR/LAUNCHER visibly detonate). Ported four structure-combat script slots: 0x08 `FindTargetUnitStructure` (nearest non-ally in range, 3× for ornithopters), 0x09 `RotateTurret` (8-step rotation, one step per call), 0x0A `GetDirection` (octant-shifted orientation), 0x0B `FireStructure` (reads `engine.variables[2]` target; ROCKET_TURRET ≥ 0x300 pos32 → MISSILE_TURRET, else BULLET). `StructureSlot` gained `rotationSpriteDiff`. 3 + 11 new tests. 400 green / 52 suites / zero warnings. Turrets defend bases; tanks explode on death — mission-1 combat is fully live.
- **2026-04-20 — P2 slice 2.1: intro WSA + audio (Jukebox + Voice).** New `DuneIIRendering/Audio/Jukebox` wraps `AVMIDIPlayer` for XMI playback (system DLS default; user can drop `.sf2` at `~/Library/Audio/Sounds/Banks/`). New `Audio/Voice` on `AVAudioEngine` + `AVAudioPlayerNode` with a `u8 → float = (byte - 128) / 128.0` PCM remap. New `IntroScene` plays `INTRO.WSA` at 15 fps via swap-texture; click / end-of-animation routes to main menu. `GameController` now owns a `Jukebox` + optional `Voice`, starts on `.intro` when asset present, plays `DUNE0.XMI` on main-menu entry. `AssetLoader` gained `loadWsa` / `loadXmiAsSMF` / `loadVoc` helpers. 6 new tests (`AudioIntroTests`) — all install-gated. 386 green / 51 suites / zero warnings. P2 closed (mentat briefing text + scenario pan/zoom belong with P5's full HUD).
- **2026-04-20 — P4 Phase 4 fourth slice: bullet flight + arrival detonation.** End-to-end combat loop now works. `UnitInfo` gained `explosionType: UInt16?` with all 27 values from `src/table/unitinfo.c`. `createBullet` sets `speed = 255` (matches OpenDUNE's `Unit_Create` winger path) so the scheduler's `step = max(4, speed/4) = 63` crosses the map quickly. `Scheduler.tickMovement` detonates bullets (types 18..24) on arrival via `Simulation.Explosions.makeExplosion` using `UnitInfo.explosionType` for the visual + `slot.hitpoints` for damage + `slot.originEncoded` for attacker credit; shortcuts OpenDUNE's `BULLET.EMC` → `Script_Unit_ExplosionSingle` chain until bullet scripts are wired. 4 new tests in `BulletFlightTests` (advances per tick, detonates + damages target, MISSILE_ROCKET uses IMPACT_EXPLODE, SONIC_BLAST with nil explosionType still frees). 380 green / 50 suites / zero warnings.
- **2026-04-20 — P4 Phase 4 third slice: explosion pool + damage + slot 0x0E.** New `Simulation.ExplosionPool` (32 slots) + `ExplosionType` enum (20 cases mirroring OpenDUNE). `Simulation.Explosions` namespace: `applyUnitDamage` / `applyStructureDamage` (simplified `Unit_Damage` / `Structure_Damage` — HP reduction + free-on-zero; bullets invulnerable), `makeExplosion` (radius `>> 4` scan with `hitpoints >> (d >> 2)` falloff, SANDWORM_SWALLOW exemption, FRIGATE skip, IMPACT_LARGE→SMOKE_PLUME half-HP downgrade, structure-at-point damage, pool entry with stopAtPosition dedup). `makeExplosionSingleUnit` wires slot 0x0E. `Host.explosions` field added. Deferred: slot 0x12 (needs `Tile_MoveByRandom`), explosion command-stream animation, `explodeOnDeath` + `ACTION_DIE`, AI retaliation, wall decay. 14 new tests in `ExplosionTests`. 376 green / 49 suites / zero warnings. Algorithm doc: `Explosion.md`.
- **2026-04-20 — P4 Phase 4 second slice: `Script_Unit_Fire` + bullet plumbing.** `UnitInfo` grew `indexStart` / `indexEnd` / `bulletType` / `firesTwice`. `UnitSlot` grew runtime `fireDelay` + `fireTwiceFlip`. `UnitPool.allocateForType` walks the per-type index range (port of `Unit_Allocate` with `UNIT_INDEX_INVALID`). New `Simulation.Units.createBullet` ports `Unit_CreateBullet` for MISSILE_* + BULLET + SONIC_BLAST (spawns in slots 12..15 with target stamped in `currentDestination`). `Pos32.moved(_:orientation:distance:)` ports `Tile_MoveByDirection` with the full 256-entry `_stepX` table. `makeFireUnit` wires slot 0x08 with range + orientation + cooldown gates, long-range trooper MISSILE_TROOPER substitution, and firesTwice flip-flop reload (5 ticks quick / `fireDelay*2` normal). `Scheduler.tick()` grew a `tickFireCooldowns` pre-pass. `WorldSnapshot` plumbs both new slot fields from save records. Deferred: sandworm eat branch, game-speed scaling, RNG jitter, voice/fog side-effects. 14 new tests in `FireTests` (allocator ranges / invalid inputs, createBullet success + invalid target, slot 0x08 gates + success + firesTwice double-tap + scheduler tick decrement). 362 green / 48 suites / zero warnings. Algorithm doc: `Fire.md`.
- **2026-04-20 — P4 Phase 4 first slice: target acquisition.** New `Simulation.TargetAcquisition` namespace ports five OpenDUNE functions — `Unit_GetTargetUnitPriority`, `Unit_GetTargetStructurePriority`, `Unit_FindBestTargetUnit` (with origin-stamp side-effect), `Unit_FindBestTargetStructure` (slab/wall skip; `>=` tie-break), and the dispatcher `Unit_FindBestTargetEncoded` (mode-4 prefers structure; deviators skip structures). `Simulation.UnitInfo` grew `priority` / `targetAir` / `priorityBuild` / `priorityTarget`; `StructureInfo` grew `priorityBuild` / `priorityTarget` + `layoutTileDiff` table. `Simulation.House.areAllied` ports `House_AreAllied` (Fremen↔Atreides exception + non-player implicit alliance). `Scripting.Host` gained `playerHouseID` + optional `isValidPosition` / `isPositionUnveiled` callbacks, defaulting to "always valid" / "always unveiled" for unwired callers. `Pos32.distanceRoundedUp` + `EncodedIndex.tile(packed:)` helpers added. `makeFindBestTargetUnit` (slot 0x1C) + `makeGetTargetPriorityUnit` (slot 0x1D) wired in `unitTable`. 27 new tests across priority math, gate checks (self/unallocated/unseen/allied/priority-off/winger/off-map), distance gating (mode 0/1/2), origin-stamping, tie-breaks, dispatcher modes, and VM slot integration. 348 tests / 47 suites / zero warnings. Algorithm doc: `TargetAcquisition.md`.
- **2026-04-20 — Unit SHP sprites on the scenario scene.** `Simulation.UnitInfo` gained `groundSpriteID` + `displayMode` (new `DisplayMode` enum) for all 27 entries. New `Orientation` helper (`to8(_:)` + 8-entry `octantFrame` table mirroring OpenDUNE's `values_32A4`). New `DuneIIRendering.UnitSpriteAtlas` loads UNITS2/UNITS1/UNITS.SHP into a 355-slot `[SKTexture?]` matching OpenDUNE's global sprite numbering (`src/sprites.c:487..494`); `resolveFrame(info:orientation:)` composes `(spriteID, flipHorizontal)` per displayMode. `ScenarioScene` replaces unit `SKShapeNode` circles with `SKSpriteNode`s whose texture + flip update every tick from pool orientation. Per-house tint (blend 0.35) until the palette remap lands. 11 new tests. 321 green / 46 suites / zero warnings.
- **2026-04-20 — Warning cleanup + no-warning policy + IdleAction (slot 0x31).** One pathfinder `var direction` that was never mutated → `let`. `CLAUDE.md` feature-workflow step 5 now mandates **zero warnings on a clean build** (`swift package clean && swift build`); don't silence with `_ =` or pragmas. `Script_Unit_IdleAction` ports `src/script/unit.c:1748` — foot/tracked/wheeled units fidget (spriteOffset + orientation nudges via `Tools_RandomLCG_Range` + `Tools_Random_256`); wingers/harvesters/sandworms skip. 3 tests. 311 green / 43 suites / 0 warnings.
- **2026-04-20 — P4 Phase 3 (pathfinder + route-follower).** `Simulation.LandscapeInfo` ports 15 rows from `src/table/landscapeinfo.c` (`movementSpeed[6]` + flags). `Simulation.Pathfinder` is the faithful port of `Script_Unit_Pathfinder` / `Pathfinder_Connect` / `Pathfinder_Smoothen` (`src/script/unit.c:1012..1286`) plus tile-packed helpers (`packedTile`, `packedDistance`, `directionPacked`). `UnitSlot` grew `route: [UInt8]` (14-entry, `0xFF`-terminated) and `currentDestination{X,Y}: UInt16`. `Scripting.Host.tileEnterScore` closure lets callers (ScenarioScene) inject a map-backed scorer; when nil, pathfinder treats all tiles walkable at cost 128. `Script_Unit_CalculateRoute` (slot 0x0C) fully ported. `Script_Unit_MoveToTarget` (slot 0x16) wired as minimal "arrived within 32 px" stub. Scheduler `tickMovement` replaces Phase 2's naive pass with a `currentDestination`-based follower — bugfix vs. Phase 2 was mid-step sub-goal shift when units crossed tile boundaries; `currentDestination` locks the goal. 7 new tests in `PathfinderTests` (packedTile / packedDistance / directionPacked cardinals / open-map / impassable / wall-routing / scheduler follow). Plan: `09.P4-Phase3.md`.
- **2026-04-20 — P4 Phase 2 (data tables + naive movement + sim-to-visual).** `Simulation.UnitInfo` ports 27 entries from `src/table/unitinfo.c` (hitpoints, fireDistance, fireDelay, damage, movementType, hasTurret, explodeOnDeath, movingSpeedFactor, turningSpeed, actionsPlayer[4], actionAI); `Simulation.StructureInfo` ports 19 entries (hitpoints, buildCredits, fogUncoverRadius, layout with dimensions). New `MovementType` / `StructureLayout` enums + `ActionID` constants. `Script_Unit_GetInfo` now covers 6 more subcases (0x00 / 0x02 / 0x0B / 0x0D / 0x10 / 0x12). `SetSpeed` applies OpenDUNE's `byScenario` 192/256 down-scale via new `UnitSlot.byScenario` flag (plumbed through both WorldSnapshot paths). `SetDestination` (slot 0x05) and `SetDestinationDirect` (slot 0x19) wired. `Scheduler.tick()` grew a `tickNaiveMovement()` pre-script pass — non-faithful placeholder that slides units by `max(4, speed/4)` px toward `targetMove` per tick, clearing on arrival (|dx|+|dy| ≤ 32 px). `ScenarioScene` now drives markers from live pools (keyed by pool index), syncs positions every tick from pos32, honours real structure footprints (3×2 refineries), hides freed slots. 14 new tests. Plan: `08.P4-Phase2.md` with updated remaining-work list.
- **2026-04-20 — P4 Phase 1 (host-function wiring + live scripts).** `Scripting.Functions.unitTable(host:source:)` / `structureTable(host:source:)` produce 64-slot tables mirroring OpenDUNE's `g_scriptFunctionsUnit` / `g_scriptFunctionsStructure`; 28 + 11 slots wired, unported slots nil with blocker-specific comments. `Simulation.UnitSlot` grew 7 fields (`positionX/Y`, `hitpoints`, `seenByHouses`, `speed`, `spriteOffset`, `blinkCounter`, `inTransport`); `StructureSlot` grew 3 (`positionX/Y`, `hitpoints`). All plumbed through both `WorldSnapshot` init paths. New `Pos32` value type + `centered(at:)` / `distance` / `direction(from:to:)` / `of(_:host:)` — full ports of `Tile_GetDistance` / `Tile_GetDirection` from `src/tile.c:342`. `Scripting.Host.voiceLog` sink added. 12 new host-function factories: batch 6 (`GetDistanceToTile`, `GetDistanceToObject`, `VoicePlay`) + batch 7 (`SetOrientation`, `SetSpeed`, `Stop`, `Blink`, `Die`, `SetSprite`, `SetTarget` with direction-aim, `IsInTransport`, `DestroyStructure`). `ScenarioScene.setUpScheduler` now wires the real tables — scripts no longer halt on first `SCRIPT_FUNCTION`. 18 new tests. Plan: `07.P4-Phase1.md` with a per-slot coverage table and a 6-10-session estimate for remaining P4 (UnitInfo, pathfinding, projectiles, explosions, team AI, tick parity harness).
- **2026-04-20 — P3 complete + P4 tick loop kickoff.** `ScenarioScene` now draws unit/structure markers (circles / 2-tile rectangles) in canonical house colours on top of the ground grid. `Simulation.WorldSnapshot.init(scenario:resolver:)` builds live pools from scenario spawns (no save file). `ScenarioScene.update(_:)` drives `Simulation.Scheduler.tick()` every 5 frames; HUD shows `Tick N · units M · structures K` live. Unit/structure VMs load `UNIT.EMC`/`BUILD.EMC` from the install with empty function tables — scripts halt on first `SCRIPT_FUNCTION`, but the tick loop itself runs. `Formats.Emc.Program.empty` is a new canonical zero-instruction fallback. `Scenario.{House,UnitType,StructureType}` gained `typeID` extension properties porting OpenDUNE's numeric IDs. 2 new tests (`scenarioRoster`, `scenarioSnapshotPoolAllocation`). Plan doc: `06.P3-Complete-P4-Kickoff.md` with manual-verification checklist.
- **2026-04-20 — P2 slice 2 + P3 scaffold (`duneii` executable).** New `DuneIIRendering` library + `duneii` executable targets in the SwiftPM package. `Installation` discovers the install and builds a case-insensitive PAK catalog; `AssetLoader` caches palette + iconmap + `TileResolver` and exposes CPS / SHP / ICN → `CGImage` + scenario decoders. Three SKScenes: `MainMenuScene` (MENTATA.CPS), `MentatScene` (tinted mentat backdrop stub), `ScenarioScene` (P3 — SCENA001.INI → 64×64 tile grid as `SKSpriteNode`s). `GameController` is the `SceneCoordinator`; `GameWindow` is the AppKit host. 6 install-gated tests in `RenderingInstallationTests`. Ergonomic `WorldSnapshot.init(loading:resolver:)` added (install-gated test). **Deviation from Initial plan**: AppKit host instead of Mac Catalyst — xcodegen isn't installed; SpriteKit rendering is host-agnostic so the migration path is a project regeneration, not a logic rewrite. P2 slice 1 formally closed with `04.P2-S1-Complete.md`; `05.P2-S2-P3.md` documents the scaffold with a manual-verification checklist (visual correctness can't be asserted from CI).
- **2026-04-20 — EMC host batch 5 (further generics).** Five new `Scripting.Functions` factories that require no new pool state: `makeDelayRandom(source:)` (draws from `RandomSource.tools`, writes `engine.delay`), `makeGetIndexType(host:)` (returns `IT_*` int or `0xFFFF`), `makeDecodeIndex(host:)` (returns `EncodedIndex.decoded` or `0xFFFF`), `makeGetLinkedUnitType(host:)` (returns the linked unit's `type` via current-object `linkedID`), `makeFindIdle(host:)` (two-mode: structure-index direct check vs. type-search via `PoolQuery`). `RandomSource` now carries both `BorlandLCG` and `ToolsRandom256`; new `init(lcgSeed:toolsSeed:)` constructor. 13 tests in a new `Scripting host batch 5 (further generics)` suite covering the function matrix plus stream-advancement for `DelayRandom`, freed-slot `0xFFFF` for `GetIndexType`, tile-packing for `DecodeIndex`, structure-origin currentObject for `GetLinkedUnitType`, and FindIdle positive/negative in both modes. Algorithm doc extended: `EmcHostFunctions.md` §10.
- **2026-04-20 — EMC host batch 4 (structure-specific, first pair).** Two new factories on `Scripting.Functions`: `makeGetStateStructure` (returns `s->state` as `UInt16(bitPattern:)`) and `makeSetStateStructure` (writes `peek(1)` to `s->state`, with the `-2` DETECT sentinel resolving to IDLE / READY / BUSY based on `linkedID == 0xFF` / `countDown == 0` / else). `Structure_UpdateMap` side-effect deferred. `Simulation.StructureSlot` gained two fields — `state: Int16` and `countDown: UInt16` — plumbed through `Simulation.WorldSnapshot` from `Formats.Save.Structures.Slot` for soft-range records (reserved slots 79-81 untouched). Private `currentStructure(host:)` helper mirrors `currentUnit(host:)`. 8 tests in a new `Scripting host batch 4 (structure-specific)` suite (GetState signed round-trip, GetState no-current, SetState explicit, SetState DETECT IDLE / READY / BUSY, SetState no-current no-op, `_SAVE001.DAT` round-trip on both fields). Algorithm doc extended: `EmcHostFunctions.md` §9.
- **2026-04-20 — EMC host batch 3 (unit-specific).** Three new factories on `Scripting.Functions`: `makeGetInfoUnit` (ten of twenty `Script_Unit_GetInfo` subcases wired — `targetMove` with live validity, `index`, `orientationCurrent`, `targetAttack`, `originEncoded`, `type`, `EncodedIndex.unit(index)`, `houseID`; UnitInfo-table and `g_playerHouseID`-backed subcases return 0), `makeSetActionUnit` (writes `actionID` on the current unit), `makeGetAmountUnit` (own `amount`, or linked unit's `amount` when `linkedID != 0xFF`). `Simulation.UnitSlot` grew five fields — `actionID`, `amount`, `targetAttack`, `targetMove`, `originEncoded` — all plumbed through `Simulation.WorldSnapshot` from the save record verbatim. Private `isValid(encoded:host:)` helper ports `Tools_Index_IsValid` against live pool state. 9 tests in a new `Scripting host batch 3 (unit-specific)` suite (GetInfo subcase sweep, no-current-object, 0x01 valid targetMove; SetAction write + no-current no-op; GetAmount unlinked / linked / no-current; WorldSnapshot real-save round-trip asserting all five new fields). Algorithm doc extended: `EmcHostFunctions.md` §8.
- **2026-04-20 — `Simulation.Scheduler`.** Per-tick driver for EMC script dispatch. Single `tick()` call walks the unit pool in findArray order (setting `host.currentObject` per entity, decrementing `engine.delay` or running up to 7 opcodes via `VM.step(_:)`), then the structure pool with budget 3 and the slab/wall skip set `{0, 1, 14}`, then clears `host.currentObject`. Per-slot engines stored on the scheduler as parallel `[Scripting.Engine]` arrays — keeps pool slots minimal. Host-function `delay` writes end the step loop early. 6 tests (delay countdown, opcode budget cap, mid-loop delay exit, walk order, currentObject clearing, structure slab/wall skip). Algorithm doc: `TickScheduler.md`.
- **2026-04-20 — `Scripting.EncodedIndex` + `Scripting.Host` + EMC host batch 2.** `EncodedIndex` is the value-type port of OpenDUNE's `Tools_Index_*` 16-bit `{kind, index}` encoding (`.none`/`.tile`/`.unit`/`.structure` via `raw & 0xC000`; `decoded` returns pool index or `Tile_PackXY(x,y)`). `Host` is a reference-typed context bundle (pools, `currentObject` ref, texts table, text log) with convenience queries (`currentHouseID`, `houseID(of:)`, `unitSlot(for:)`) — all of which handle `Tools_Index_IsValid` against live pool state. Five new factory host functions: `makeDisplayText(host:)`, `makeUnitCount(host:)`, `makeGetOrientation(host:)`, `makeIsEnemy(host:)`, `makeIsFriendly(host:)` — all peek-never-pop, matching the existing insight. `UnitSlot` grew one field (`orientationCurrent: Int8`) plumbed through `WorldSnapshot` from the save record. 13 tests in a new suite. Algorithm doc extended: `EmcHostFunctions.md` §7.
- **2026-04-20 — `Simulation.WorldSnapshot`.** Bridge from a decoded `Formats.Save.Game` + `Core.Map.Map` baseline into live `HousePool` / `UnitPool` / `StructurePool` + a dense 4096-cell tile grid with the save's sparse `MAP ` overrides layered on top. Single `throws` initializer: allocates each pool slot by the record's own `index`, restores `linkedID` / `starportLinkedID`, routes reserved structure slots (`[79, 81]`) through `allocateReserved` vs. normal `allocate(at:)`. Tile grid preserves the off-by-one `objectRef` (pool-index-plus-one, `0` = none). Cross-chunk invariants enforced: duplicate indices in any pool, out-of-range indices. 6 tests (pinned single-of-each synthetic, sparse-override layering at cells 10 and 4095, duplicate-house rejection, out-of-range unit / structure indices, real `_SAVE001.DAT` end-to-end with pool-slot fidelity and every sparse override verified in the dense grid). Architecture doc: `Documentation/Architecture/WorldSnapshot.md` (contract, Tile field table, scope exclusions — no EMC resumption, no fog reconstruction, no tile↔pool cross-link validation).
- **2026-04-20 — `Formats.Save.Game` (save-format coverage closed).** Top-level aggregator composing `Container` / `Info` / `Player` / `Units` / `Structures` / `TileMap` behind a single `decode(_:)` entry point. Surfaces the `NAME` chunk as Swift `String` (NUL-terminated ASCII), optional `TEAM` / `ODUN` as raw `Data?` (both always `nil` in vanilla saves). Every failure maps to a distinct `DecodeError` case wrapping the underlying body error, plus two aggregator-only cases (`missingRequiredChunk(tag:)`, `nameNotAscii`). Unknown chunks tolerated. 7 tests (real-data `_SAVE001.DAT` end-to-end, synthetic minimal composition, missing-chunk, malformed container, non-ASCII NAME, legacy version propagation, unknown-chunk tolerance). Format doc extended: `SAVE.md` §12 (contract + failure-mode table + "what Game does NOT do" callouts — no pool allocation, no map reconstruction, no script resumption). Every chunk in a vanilla 1.07 save now round-trips into typed Swift values.
- **2026-04-20 — `Formats.Save.TileMap`.** Body decoder for the `MAP ` chunk (trailing space). **Sparse**, not fixed-size: each 6-byte record is `u16 LE cellIndex` + 4 packed tile bytes, emitted only for cells differing from the map-seed baseline or carrying dynamic state. Packed tile unpacks 9-bit `groundTileID` (spans bytes 0-1), 7-bit `overlayTileID` (byte 1 bits 1-7), 3-bit `houseID`, five boolean flag bits, and an off-by-one pool reference `tileIndex` (`0` = no object; `n` = pool index `n - 1`). 9 tests including real-data `_SAVE001.DAT` decode with ascending cellIndex validation. Format doc extended: `SAVE.md` §11. Insight: `format-save-map-is-sparse-not-fixed.md` — reconstructing the full grid requires layering sparse overrides on top of `Core.Map.Generator(mapSeed)` output, not reading 4096 records from the chunk.
- **2026-04-20 — `Formats.Save.Structures`.** Body decoder for the `BLDG` chunk (88-byte packed records). Layered shape: shared 71-byte `ObjectHeader` (from UNIT work) + 17-byte structure tail (`creatorHouseID`, `rotationSpriteDiff`, `SLD_EMPTY(UINT8)` pad, `objectType` being produced, `upgradeLevel` + `upgradeTimeLeft`, `countDown`, `buildCostRemainder` fractional accumulator, signed `state` `i16` where `-1` = invalid, `hitpointsMax`). Reserved pool slots 79-81 (walls/slabs aggregates) never appear in the chunk — OpenDUNE's writer walks `Structure_Find` which visits `[0, 78]` only. 6 tests including real-data `_SAVE001.DAT` decode. Format doc extended: `SAVE.md` §10.
- **2026-04-20 — `Formats.Save.Units` + shared `ObjectHeader`.** Body decoder for the `UNIT` chunk (128-byte packed records). Each record is a 71-byte `ObjectHeader` (index / type / linkedID / `ObjectFlags` 32-bit bitfield / houseID / seenByHouses / position x,y / hitpoints / nested 55-byte `ScriptState` with delay / scriptOffset / returnValue / frame + stack pointers / variables[5] / stack[15] / isSubroutine) followed by a 57-byte unit tail (SLD_EMPTY pad / currentDestination x,y / originEncoded / action IDs / narrowed `fireDelay` u8 / distanceToDestination / target attack/move / amount / deviated / targetLast + targetPreLast tile pairs / two `dir24` orientation tracks / speed quintet + sprite/blink/team / timer / route[14]). `ObjectHeader` + `ScriptState` + `ObjectFlags` live in a shared file for `BLDG` reuse. 6 tests. Real `_SAVE001.DAT` UNIT chunk decodes cleanly; no `ODUN` present. Format doc extended: `SAVE.md` §9. Insight: `format-save-odun-repairs-narrowed-fields.md` — `ODUN` patches truncated fields (fireDelay u16→u8 narrowing), it isn't additive state.
- **2026-04-20 — `Formats.Save.Player`.** Body decoder for the `PLYR` chunk: a packed array of 66-byte House records. Every allocated slot round-trips across `index` / `harvestersIncoming` / `flags` / 4 unit counters / `structuresBuilt` (u32) / 4 economy fields / `palacePositionX,Y` / `SLD_EMPTY` pad / 3 alert cooldowns / 2 starport fields / `aiStructureRebuild[10]`. `HouseFlags` is a value-type wrapper exposing `used` / `human` / `doneFullScaleAttack` / `isAIActive` / `radarActivated` via the OpenDUNE bit layout, plus `rawWord` for round-trip. `humanSlot` picks the first `flags.human == true` slot (matches `House_Load`'s `g_playerHouseID` wiring, which uses the flag rather than the index). 6 tests. `_SAVE001.DAT` has a 132-byte PLYR (2 houses only — mission 1 is player + single enemy, not the pool cap). Format doc extended: `Documentation/Formats/SAVE.md` §8.
- **2026-04-20 — `Formats.Save.Info`.** Body decoder for the `INFO` chunk in modern (0x0290) saves. Flat port of OpenDUNE's `s_saveInfo` table nesting `g_saveScenario`: a 228-byte scenario block (score, win/lose flags, mapSeed, mapScale, timeOut, three 14-byte picture filenames, counter u16s, sixteen 10-byte Reinforcement records) plus 100 bytes of top-level state (playerCreditsNoSilo, minimap / selection / structure-active positions, scenarioID, campaignID, two hintsShown dwords, elapsed ticks, duplicate credits field, starportAvailable[27], house-missile fields, structureIndex). Modern body = 330 bytes (2-byte version + 328 payload); legacy versions are rejected via `DecodeError.legacyVersion(version:)`. 5 tests (pinned synthetic, duplicate-second-wins, legacy rejection, truncated rejection, real-data decode of `_SAVE001.DAT` over plausibility ranges). Format doc extended: `Documentation/Formats/SAVE.md` §7. Insight: `format-save-info-duplicate-credits.md` (OpenDUNE's table references `g_playerCreditsNoSilo` twice — second write wins; not a separate field).
- **2026-04-20 — `Formats.Save.Container`.** Top-level IFF walker for `_SAVE00?.DAT`: asserts `FORM` / size / `SCEN`, then indexes chunks by 4CC into a `[String: Data]`, preserving whitespace in tags (critical for `MAP ` — trailing space). Surfaces the `INFO` savegame version (`0x0290` for modern 1.07) via a convenience accessor. Per-chunk bodies are deferred to follow-up work. 8 tests (synthetic round-trip, `MAP `-vs-`MAP` key distinction, odd-length pad, four failure modes, real-data pass over `_SAVE001.DAT`). Format doc: `Documentation/Formats/SAVE.md`. Insight: `format-save-odun-is-opendune-only.md` (vanilla 1.07 saves never emit `ODUN`, and `TEAM` is conditional — our reader must not require the 8-chunk OpenDUNE superset).
- **2026-04-20 — `Simulation.PoolQuery` + `next(_:)` on Unit/Structure pools.** Filter-and-resume iteration mirroring OpenDUNE's `Unit_Find` / `Structure_Find`, including the structure-pool tail-trio walk over the three reserved aggregate slots. Value-type cursor with `nil` for "any" filter, opaque `position` for resumability. 7 tests; `Documentation/Architecture/Pools.md` extended with §6.
- **2026-04-20 — EMC host-function table (first batch).** `Engine` grew a `delay: UInt16` field; public `push/pop/peek` stack helpers replaced the internal `popForTest` shim. `Scripting.Functions` namespace holds the first three `Script_General_*` ports: `noOperation`, `delay` (`engine.delay = peek(1) / 5`), and a `makeRandomRange(source:)` factory that closes over a shared `Scripting.RandomSource` (reference-typed wrapper around `RNG.BorlandLCG`). All three use `peek`, never `pop` — critical for stack parity with the EMC compiler's `STACK_REWIND` convention. 6 tests. Algorithm doc: `Documentation/Algorithms/EmcHostFunctions.md`. Insight: `scripting-host-fn-peek-not-pop.md`.
- **2026-04-20 — Pool abstractions.** `Simulation.UnitPool` (102 slots), `Simulation.StructurePool` (82 hard / 79 soft + 79/80/81 reserved for walls and slabs), `Simulation.HousePool` (6 slots). Slot value types are minimal (`isUsed`, `isAllocated`, `index`, `type`, `houseID`, `linkedID` / `starportLinkedID`) and grow as systems land. Mirrors OpenDUNE byte-for-byte including the `House_Free` "leaves isUsed set" quirk. 16 tests. Architecture doc: `Documentation/Architecture/Pools.md`. Insight: `simulation-house-free-leaves-used.md`.
- **2026-04-20 — `Scripting.Engine` + `Scripting.VM`.** Stack-based EMC virtual machine; `Engine` is a Sendable value type, `VM.step(_:)` mirrors OpenDUNE `Script_Run` byte-for-byte across all 19 opcodes including the peek-before-pop guards. 12 tests (reset, push/pop, BINARY arithmetic, UNARY ops, JUMP_NE, SETRETURNVALUE, stack alloc/dealloc, FUNCTION dispatch, three halt paths, pinned 5-instruction program). Algorithm doc: `Documentation/Algorithms/EmcVM.md`. Insight: `scripting-emc-saved-location-plus-one.md`.
- **2026-04-20 — `Core.Map.Generator`.** Bit-for-bit port of OpenDUNE `Map_CreateLandscape`: noise pool → blob spread → 16×16 stamp → bilinear fill → 9-neighbour smoothing → terrain thresholding → spice sprinkling → sprite-index assignment → ICN tile resolution. 6 tests (determinism, distinct seeds, range invariant, terrain-type histogram, per-cell pin and grid checksum for seed `0x16BD`). Closes the §3 risk in `01.Initial.md`. Algorithm doc: `Documentation/Algorithms/MapGenerator.md`. Insight: `format-tile-packxy-uint16-bounds.md`.
- **2026-04-20 — Markdown reflow pass.** Removed paragraph-internal hard wraps from all 41 repo `.md` files via `.tools/reflow_md.swift`. Fixed a nested-list bug in the script; added a `CLAUDE.md` rule forbidding paragraph hard-wraps in future edits. See today's history bullet.
- **2026-04-20 — `Core.RNG.ToolsRandom256` + `Core.RNG.BorlandLCG`.** Bit-for-bit ports of OpenDUNE's PRNGs with pinned baselines. 6 tests. Algorithm doc: `Documentation/Algorithms/RNG.md`.
- **2026-04-20 — `Core.ScenarioWorld`.** Typed world snapshot pairing `Scenario` with a stamped `Map`. Handles structure placement, `GEN`-keyed slabs/walls, `units(at:)`, `structure(at:)`, `isBlocked(at:)`. 5 tests. Algorithm doc: `Documentation/Algorithms/ScenarioWorld.md`.
- **2026-04-20 — `Core.Map.Map` + `LandscapeType` + `TileResolver`.** 64×64 grid + OpenDUNE's 81-entry landscape-sprite lookup. 4 tests. Algorithm doc: `Documentation/Algorithms/Map.md`.
- **2026-04-20 — `Core.Scenario`.** Typed model over `Formats.Ini.Document` (House / UnitType / StructureType / UnitAction / UnitSpawn / StructureSpawn / HouseLayout / MapField / Briefing) including `GEN`-prefix slab/wall rows. 6 tests. Algorithm doc: `Documentation/Algorithms/Scenario.md`.
- **2026-04-20 — `Formats.Xmi.Song`.** Miles XMIDI decoder with SMF converter. `assetgen` now emits playable `.mid` files. Insight: `format-xmi-delay-and-duration.md`.
- **2026-04-20 — `Formats.Emc.Program`.** 19-opcode byte-level decoder with 1/2-word instructions. `assetgen` emits raw `.emc` + JSON disassembly. Insight: `format-emc-variable-instruction-width.md`.
- **2026-04-20 — Plans `02.P1-Complete.md` and `03.P2-Rendering.md`.** P1 "format foundation" closed.
- **2026-04-19 — `Formats.Ini.Document` + `Formats.IconMap`.** Plus the whole P1 format foundation (PAK, PAL, CPS, SHP, WSA, ICN, FNT, VOC, Format80, Format40) and `assetgen` CLI.

## Test status

`cd Code/Core && swift test` — **1032 tests across 101 suites, all green** as of 2026-04-24 (🎯 FULL 1000-tick SAVE007 parity, plus full 200-tick SAVE001 parity, all under real UNIT.EMC / BUILD.EMC / TEAM.EMC). `swift package clean && swift build` reports **zero warnings** (library + tests). `swift build` also builds the `duneii` executable (< 11 s clean, < 5 s incremental).

## Open questions / risks (pointers)

These live in full in `Documentation/Plans/01.Initial.md` §10. Copied here only as reminders:

- ADL / OPL playback — still deferred. Decide in P2.
- MT-32 `.C55` timbres — deferred. GM SoundFont is good enough for a first pass.
- `SavedGame` opaque chunks — preserve verbatim when we get there.
