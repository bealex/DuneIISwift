# The headless oracle can't tick a player-house palace or fly any house missile — no cross-engine golden there

**Finding:** the OpenDUNE parity oracle (`--parity-scenario`, minimal headless init: pools + ICON.MAP, no GFX/strings) **SIGSEGVs** on two palace-related paths, so neither can be a scenario golden: (1) running *any* tick with a **player-house palace** present — the player-house `House_CalculatePowerAndCredit` / `GUI_Widget_ActionPanel_Draw` path dereferences GUI/string state the harness never loads (crashes even at 1 tick); (2) **flying any `missileHouse` bullet** — the missile's GUI/sprite path segfaults (this is why `palace-launch` is a 0-tick, frame-0-only golden). The same root cause makes `Sound_Output_Feedback` (the "base under attack" voice/message) crash — `String_Get_ByIndex` has no string table headless.

**Why it matters:** you can burn real time trying to build a cross-engine golden for player-palace behaviour (e.g. "the human palace must NOT auto-fire", or the player-human base-under-attack feedback). It is **impossible** — the oracle dies before dumping. These are genuinely headless-unobservable, so per the feature workflow they fall to the "no-oracle UI/economy seam" bar: unit/integration tests in our engine (which *can* tick player palaces and fly missiles) + golden neutrality, stated explicitly in History.

**Evidence:** confirmed empirically — `opendune --parity-scenario=<player Harkonnen palace + windtrap, PlayerHuman> --parity-ticks=1` exits 139 (SIGSEGV); same for a player-structure hit reaching `Sound_Output_Feedback(48)`. The `palace-launch` golden (`Tests/ScenariosTests/ScenarioGoldenTests.swift` + `gen-scenario-goldens.sh`) runs 0 ticks for exactly this reason; `parity.c Scen_BuildPlace` notes the player-house `GUI_DisplayText` headless crash. Our-side coverage: `PalaceTests.loadedHumanPalaceDoesNotAutoFire` (load → tick the palace cursor → no fire; negative control fires) and `HouseUnderAttackTests`.

**How to apply:** for player-palace / player-feedback behaviour, don't attempt a cross-engine golden — verify in our headless engine (it ticks player palaces + flies missiles fine) with an integration test plus a negative control, and assert golden neutrality. A cross-engine golden is only viable for the **AI-house** half of these primitives (e.g. `house-under-attack` flips an *AI* house's `doneFullScaleAttack` with no feedback/missile, so it ticks cleanly). See [build-gamestate-layout-stale-module](build-gamestate-layout-stale-module.md) for the related "clean-build before trusting a red" rule when a `GameState` field is added.
