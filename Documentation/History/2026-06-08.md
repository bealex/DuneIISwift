# History — 2026-06-08

- Add an original app icon for both clients — a single setting sun (Arrakis orbits one star, Canopus) over layered dunes with sunlit crests, dusk sky, stars, spice glints. Procedurally generated (no game assets) by `Scripts/gen-app-icon.swift` (CoreGraphics). iOS: `Code/Apps/duneii-ios/Assets.xcassets/AppIcon.appiconset` (full-bleed 1024, wired via the existing ASSETCATALOG_COMPILER_APPICON_NAME). macOS: `Code/Apps/duneii/AppIcon.icns` + new `Scripts/bundle-mac.sh` that wraps the SwiftPM `duneii` executable into a branded `duneii.app` (the bare binary had no bundle/icon); `Package.swift` excludes the .icns from the target. Verified on device + clean --full build (0 warnings, 648 tests).
- fix(client): tile(atScenePoint:) rejected off-map margin clicks via toward-zero integer-division truncation (a click in the scrollable border, scene x∈[-borderPx,0), snapped to edge tile 0 past the 0..<64 guard); now bounds-check the raw scene point before dividing (GameScene.swift:533). Presentation seam ⇒ new ClientTests/TileMappingTests (2); scenario/render goldens byte-identical.
- Make GameModel audio opt-in (init audioEnabled, default off) so headless/test scenario loads are silent — skip setupAudio() + keep both master gates shut + gate music.startInGame(); the two game clients pass audioEnabled:true (GameModel.swift, duneii/App.swift, duneii-ios/App.swift); add AudioDisableTests (ClientTests).
- Refactor SwiftUI/UI architecture per review: convert injected `@State var model` re-wraps to plain `var`/`@Bindable` across the shared DuneIIClient views + both app-host ContentViews + the viewer ContentViews (fixes a latent model-swap bug); replace hand-rolled `Binding(get:set:)` with `@Bindable` $model bindings; delete the dead InspectorPanel/EconomyPanel/DebugPanel (folded into GameSidebar long ago), keeping DebugToggleRows + the ActionType/OrderKind extensions; make GameModel.lastFrame @ObservationIgnored behind a cheap minimapVersion redraw token so the large FrameInfo never drives 60Hz SwiftUI invalidation; resolve WindowAccessor's window once instead of re-dispatching every update + drop ToolWindowManager's unused model param; defer the viewer apps' synchronous PAK-decode model init out of @State defaults into .task; and split rendertest's ~500-line AssetDetailView god-view, extracting all decode/assembly into a new AssetDecoder + DecodedAsset value type. Full suite (653 tests, incl. render/scenario goldens — neutrality golden) green, 0 warnings, iOS sources compile.
- Add heavy-scenario profiling: `duneii-headless profile [SCEN] [ticks]` (full live sim, per-phase wall-clock via new parity-neutral `Simulation.tickProfiled()`/`runTick(profile:)`) + `RenderProfilingTests` timing makeFrameInfo/render/snapshot; findings in `Documentation/Architecture/Profiling.md` — tick ~155k ticks/s (unit phase ~70%), live render ~1800fps, only the snapshot read-back (~84ms) is costly (capture-only). Neutral: scenario/RNG/render goldens byte-identical (654 tests).
- Client UI: remove the `Show all economies` debug toggle (+ dead `GameModel.showAllEconomies`, economy list now player-only); draw the concrete map-boundary ring on the minimap in every radar state (`AssetStore.concreteMinimapColor` + `Minimap.borderRing`); make the minimap clickable regardless of radar (only the tuning animation blocks it) and let a primary click confirm an armed Move/Attack/Harvest at the tile (`GameModel.minimapPrimaryClick`/`isOrderArmed`) so the minimap doubles as a target picker. Presentation/input seam ⇒ new `MinimapBorderTests` (2) + `MinimapTargetingTests` (2); macOS+iOS compile, 658 tests green.
