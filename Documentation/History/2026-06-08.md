# History — 2026-06-08

- Add an original app icon for both clients — a single setting sun (Arrakis orbits one star, Canopus) over layered dunes with sunlit crests, dusk sky, stars, spice glints. Procedurally generated (no game assets) by `Scripts/gen-app-icon.swift` (CoreGraphics). iOS: `Code/Apps/duneii-ios/Assets.xcassets/AppIcon.appiconset` (full-bleed 1024, wired via the existing ASSETCATALOG_COMPILER_APPICON_NAME). macOS: `Code/Apps/duneii/AppIcon.icns` + new `Scripts/bundle-mac.sh` that wraps the SwiftPM `duneii` executable into a branded `duneii.app` (the bare binary had no bundle/icon); `Package.swift` excludes the .icns from the target. Verified on device + clean --full build (0 warnings, 648 tests).
