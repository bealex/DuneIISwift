# History — 2026-05-31

- render/headless-capture: add SpriteKitRenderer.snapshot(_:crop:) — render a FrameInfo off-screen through the real pipeline (SKView.texture→CGImage) and crop to an image-space region, so a headless run can pause at any tick and capture any part of the map as an image (swap NullRenderer→a dedicated SpriteKitRenderer for the capture, then back). GPU-backed (runs on a Mac; the per-pixel composition is covered headlessly by FrameComposer tests). Extract DecodedSpriteSource (reusable value-type WorldSpriteSource over decoded ICON.ICN + UNITS SHPs via GlobalSprite); mapview's MapSpriteSource now adapts AssetStore onto it. +1 test, 441 total.
- render/golden-harness: add RenderGoldenTests — run a scenario through the real sim + SpriteKitRenderer.snapshot, diff the captured tile-region pixel-exact against a committed reference PNG (Tests/RenderGoldenTests/Fixtures/<name>.png); DUNEII_RENDER_RECORD=1 records via the same path (Scripts/gen-render-goldens.sh). 3 SCENA001 base-crop cases (t0/t60/fog). New PngImage (RGBA8 decode/encode/diff) + RenderHarness (install loader mirroring rendercap). Fixed SpriteKitRenderer.snapshot crop: texture(from:) rasterizes at the host backing scale (2x Retina), so scale the logical-point crop rect to pixels before cropping(to:) — previously a crop captured the wrong region at half size (also fixes rendercap --rect). 442 tests.
- docs/history: move from a monthly changelog (History/YYYY-MM.md) to per-day files (History/YYYY-MM-DD.md) — split 2026-05.md into 2026-05-{28,29,30,31}.md (bullets verbatim, date prefix dropped since the filename carries it), add History/README.md as the newest-first day index. Rewrote Scripts/log-history.sh to append to today's day file + insert a new day's link atop the index. Updated CLAUDE.md (layout/workflow step 6/self-review/scripts) + CurrentState pointers.
- render: composite a non-veil tile overlay (a wall) OVER the ground with index-0 transparency in FrameComposer.cell (faithful GFX_DrawTile gfx.c:210 — overlay/wall tiles skip colour 0), instead of opaque-replacing the cell; ground now shows through a wall's transparent pixels. +reworked the cellComposition test (half-transparent wall tile shows ground through its lower half). Added a richer render-golden case scena005-base-t40 (multi-structure enemy base: house-recoloured Harkonnen vs Atreides, concrete slabs, walls, infantry+vehicles). Insight render-tile-overlay-transparency. Sharpened the partial-fog-edge deferral in Renderer.md (walls+fog share overlayTileID → needs a separate FrameInfo fog field). 442 tests.
- render: partial fog-of-war soft edges (closes the documented gap). makeFrameInfo now derives each revealed tile's 4-neighbour veil bitmask (Simulation.fogEdgeMask, mirroring Map_UnveilTile_Neighbour map.c:1293; bit 0=N/1=E/2=S/3=W) and picks the fog-edge sprite TileIDs.fogEdges[mask] (FOG_OF_WAR icon-group tiles 0-15, hoisted into GameState like tileIDs.veiled), carried in a new separate FrameInfo.Tile.fogEdgeSpriteIndex (separate from overlaySpriteIndex, which holds walls — always shown — vs fog edges gated by showFog). FrameComposer.cell composites the fog edge transparently over the ground only when showFog is on. +3 tests (FrameInfoTests fogEdgeMask/fogEdges, FrameComposerTests fogEdgeComposition); re-recorded the scena001-base-fog-t60 render-golden (now a dithered soft edge). 445 tests. Only sandworm shimmer remains as a render gap.
- render: sandworm shimmer (DRAWSPRITE_FLAG_BLUR) as a CoreGraphics effect — closes the last render gap. A blurTile unit isn't a sprite draw: OpenDUNE writes *buf=buf[blurOffset] per opaque worm pixel (gui.c:1289), displacing the terrain under the worm silhouette horizontally by a cycling 1-5px (blurOffsets {1,3,2,5,4,3,2,1}). makeFrameInfo now emits sandworms to a new FrameInfo.blurs (silhouette + position, not units); new ShimmerEffect.patch builds a worm-sized CGImage sampling the colorized terrain offset-cols to the right within the worm mask (transparent outside); SpriteKitRenderer lays it on a blur layer above the terrain, advancing the offset each frame. +3 tests (ShimmerEffectTests displacement/offsets, FrameInfoTests sandwormBlur) + a scena001-worm-t0 render-golden; rendercap/RenderHarness gain a --worm/worm: inject. Subtle in a still (the original shimmers in motion); confirmed it alters pixels. 448 tests.
- parity/wall-slab: add wall-destruction + slab-indestructible scenario goldens (map-tile goldens). Ported Map_UpdateWall into GameState+WallSlab (revert ground to the base landscape tile + destroyed-wall overlay marker + re-connect neighbours) and wired Map_MakeExplosion's wall branch into mapMakeExplosion (UnitImpact.swift): a wall tile is destroyed if blast HP >= wall HP (deterministic, no draw) else a Random_256 roll. New tile-dump infrastructure: oracle parity.c gains a [DUMPTILES] section + per-tick "tiles":[{packed,ground,overlay,lst}] dump (walls/slabs aren't in the structure find-array); ScenarioGoldenTests decodes + compares the dumped cells' groundTileID. wall-destruction (seed 42): a tank's 25-dmg bullet's Random_256 roll destroys the 50-HP wall at tick 67 — ground reverts 34->base-terrain; rides the RNG-stream golden (the destroy draw aligns). slab-indestructible: a tank fires at a concrete slab — slabs have no destruction in 1.07 (Map_MakeExplosion only special-cases LST_WALL), so the slab ground stays. Oracle rebuilt+re-signed. 11 scenario goldens, 448 tests. (Overlay excluded from the tile golden: a latent fog-veil off-by-one, ours 124 vs oracle 123, gameplay-neutral.)
- sim/correction: built concrete slabs ARE destructible (correcting the prior 'no slab destruction in 1.07' claim). Ported Explosion_Func_TileDamage's LST_CONCRETE_SLAB branch (GameState+Explosion.explosionTileDamage: a built slab reverts to the seed base landscape tile; structure/destroyed-wall/veiled tiles skipped) — replacing the SEAM mislabelled 'cosmetic'. Slab destruction lives in the GUI-clocked explosion VM (the TILE_DAMAGE command, only on IMPACT_LARGE/EXPLODE explosions — a tank's IMPACT_SMALL has none), gated off the golden path, so it's verified RNG-free by ExplosionTests (direct + via the real IMPACT_EXPLODE sequence). A cross-engine destruction scenario golden is impractical (no accurate TILE_DAMAGE weapon; the harness fires once; rockets scatter/overshoot), so slab-indestructible is reframed with a new oracle [BASIC] TickExplosions=1 flag (calls Explosion_Tick in the scenario loop): a tank's IMPACT_SMALL has no TILE_DAMAGE, so the slab survives even with the explosion VM ticking. Oracle rebuilt+re-signed. 450 tests.
- input/ui: implement Phase-5 player input (first version) + a selection inspector panel in mapview. New DuneIIInput: InputSource protocol + ScriptedInput + Selection (.unit/.structure by pool slot) + InputController — the interactive selection/order state machine (pure value type, InputTests). Contracts: Command.stop(unit:) + displayName on UnitType/StructureType/HouseID. UnitOrders gains the stop path (clear targets/route → GUARD). mapview: MapScene now handles mouse (left-click select a unit/building via unitGetByPackedTile/structureGetByPackedTile, right-click order the selected unit — attack an enemy tile, else move) + keyboard (Esc deselect, S stop, A arm-attack), applies queued Commands via UnitOrders each tick, outlines the selection (SKShapeNode over the footprint), and publishes the live SelectionInfo. New InspectorView (.inspector panel): the selected unit/building's type/house/HP/tile + Move/Attack/Stop command buttons (Move/Attack arm a pending order targeted by the next map click). +6 tests, 456 total. Visual confirm is the user's swift run mapview.
- audio: implement Phase-7 basic audio — low-latency, polyphonic playback. New Contracts SoundEvent + SoundID (the sim→audio seam, finally defined). New DuneIIAudio: AudioSink protocol (@MainActor) + NullAudio (no-op) + EngineAudioSink (AVAudioEngine) — sounds are pre-decoded into AVAudioPCMBuffers (resampled to one canonical float32-mono rate via manual linear interpolation) and a pool of 24 player nodes is kept running, so play() is a single scheduleBuffer(at:nil, options:.interrupts): it starts at the next render quantum (no perceptible delay) and mixes with other voices (round-robin, steals the oldest only past 24 overlaps). Degrades to silent on a box with no audio device (start() fails quietly). mapview wires it: decode install VOCs (CLICK.VOC select, AFFIRM.VOC acknowledge), register + start on launch, play instantly on select/order (rapid actions overlap → audible mixing). +4 tests (AudioTests: 8-bit→float Pcm8, makeBuffer same-rate + resample, NullAudio, graceful no-device). 460 tests. Audible confirm is the user's swift run mapview.
- app/duneii: start the native macOS game client (Phase 6) — pivoted the host from Mac Catalyst to native AppKit + SwiftUI (docs updated: Plan §3/§4/§6/§8, Overview, CLAUDE.md). New 'duneii' executable target: a SwiftUI main MAP WINDOW (pixel-perfect, fills the window with black margins) driven by a Viewport (zoom +/- keys, scroll arrow keys; SKCameraNode scale=1/zoom, position=center) over the SpriteKitRenderer; left-click select / right-click move-or-attack (enemy via the player house) / Esc deselect — reusing DuneIIInput's InputController + UnitOrders; EngineAudioSink feedback on actions. Four FLOATING TOOL WINDOWS as AppKit NSPanels (level .floating, collectionBehavior .fullScreenAuxiliary so they stay over the fullscreened map), managed by ToolWindowManager + toggled from the map toolbar (load-scenario menu too): Minimap (downscaled terrain image + live unit dots + the viewport rectangle; click recentres the map), Selection (the inspector: type/house/HP/tile + Move/Attack/Stop), Economy (per-house credits/power/storage, all or only-mine), Debug (toggle fog, all-economies, per-unit health bars). Player house = the scenario's Brain=Human house (fallback first allocated). GameModel (@Observable) is the shared state; derived panel info republished only on change (no 60fps churn), lastFrame observed for the live minimap. Builds clean; 460 tests. Visual confirm is the user's swift run duneii. mapview kept as the simple viewer.
- duneii/render: add a unit state chip to the debug health overlay. New Contracts FrameInfo.UnitActivity (idle/moving/attacking/guarding/harvesting) on FrameInfo.Unit; makeFrameInfo collapses the unit's actionID via Simulation.activity(forActionID:) (attack/hunt/ambush/sabotage→attacking, move/retreat→moving, guard/areaGuard→guarding, harvest/return→harvesting, else idle). GameScene draws a 4px colour chip just in front of each health bar — green move / red attack / blue guard / orange harvest, no chip for idle; the Debug panel shows the legend. +1 test (FrameInfoTests activityMapping). 461 tests.
