# History — 2026-05

Append-only changelog. One bullet per change, imperative mood, with file references.

- 2026-05-28: Archived the first attempt and reset the project to a core-engine rebuild; wrote `Documentation/Plan.v1.md` — behavioral parity (validated against OpenDUNE as oracle), state machines as exact EMC transcription, the package topology, the parity verification pyramid, the multi-window verification UI, the render-test app, and the phased build.
- 2026-05-28: Phase 0 — rewrote `CLAUDE.md` for the rebuild (engine-first goals, package topology, core principles, the periodic self-review rule); added `Documentation/Architecture/{Overview,Testing,ParityHarness}.md`; recreated `CurrentState.md`; started `History/2026-05.md` and `Insights/README.md`.
- 2026-05-28: Phase 0 — scaffolded the SwiftPM package under `Code/`, organized by kind: `Frameworks/` (7 `DuneII*` libraries — Contracts/Formats/World/Simulation/Renderer/Input/Audio, each with its own `CLAUDE.md`), `Tools/` (`assetgen`), `Apps/` (`duneii-headless`), and `Tests/` (`ContractsTests`/`FormatsTests`/`WorldTests`/`SimulationTests`), with each target's directory set explicitly in `Package.swift`. Clean build, zero warnings, 4 scaffold link-tests green.
- 2026-05-28: Phase 1 — ported Format80 (LCW) decompression from OpenDUNE `src/codec/format80.c` to `Code/Frameworks/DuneIIFormats/Codec/Format80.swift` (decode-only, byte-by-byte back-references, throws on truncated/corrupt input); documented in `Documentation/Formats/Format80.md`; 11 decode tests covering every command, size clamping, and both error cases (suite: 14 green). Insight: `codec-format80-overlap.md`.
- 2026-05-28: Phase 1 — ported the rest of the format/codec layer to `DuneIIFormats`: Format40 (XOR-delta codec), PAK (archive), Palette (VGA 6-bit), IFF/FORM chunk reader (also serves the savegame container), SHP (sprites), CPS (images), ICN (tiles), WSA (animations), FNT (font), VOC (sound), INI (scenario text), and EMC (script bytecode parser + disassembler). Added `Format80.decodeToEnd` (unbounded) and a shared `ImageBlock` (raw/Format80) helper. Each format has an own-words doc under `Documentation/Formats/` citing its OpenDUNE source, synthetic per-branch tests, and a real-data test against install PAKs (SHP/CPS/ICN/WSA/FNT/VOC/INI/EMC all decode real DUNE.PAK/SCENARIO.PAK/VOC.PAK assets). Wired `assetgen emc-disasm`, which renders a real `UNIT.EMC` to a readable opcode listing with native-function names. Suite: 57 green, zero warnings.
- 2026-05-28: Correction to the Phase 0 scaffold entry above — the per-module `CLAUDE.md` files triggered SwiftPM "unhandled files" warnings that a `tail`-ed build hid; they are now `exclude:`d per target in `Code/Package.swift`, restoring a genuinely zero-warning clean build. Insight: `build-swiftpm-unhandled-files.md`.
- 2026-05-28: Phase 1 — note: full `Resources/` image/audio regeneration (PNG/WAV writers via ImageIO) is deferred — it is the presentation/writer layer, not format decoding, and `Resources/` is already committed and not needed for Phases 2–3. The Foundation-only decoder layer + `emc-disasm` are complete. (Closed below.)
- 2026-05-28: Phase 1 (asset export) — added the `DuneIIExport` writer library (`PngWriter` via ImageIO/CoreGraphics, `WavWriter` via hand-written RIFF) and `assetgen extract <installDir> <outputDir>`, which decodes the install PAKs to viewable PNG (SHP frames, CPS images, ICN tile sheets, WSA frames, FNT glyph sheets) and playable WAV (VOC) plus EMC listings. Verified on the real install: 350 assets extracted, 0 failures; CPS images are 320×200, the ICN sheet is a 256×400 grid, WAVs are valid RIFF/WAVE 8-bit mono. 60 tests green, zero warnings. Closes the deferral above. Insight: `swift-toplevel-mainactor-globals`.
- 2026-05-28: Renderer + render-test app (Phase-4 work pulled forward for asset verification). Added asset rendering services to `DuneIIRenderer` — `HouseRemap` (house color recolor ported from OpenDUNE `gui/viewport.c:300` sprites / `gfx.c:224` tiles, 1.07 path) and `IndexedImage` (indexed pixels + palette → `CGImage` with optional house remap + transparent index, unsmoothed), unit-tested for the remap and image sizing. Built `rendertest`, a native macOS SwiftUI asset inspector (SPM executable, `swift run rendertest [installDir]`): browsable PAK asset hierarchy; per-asset animated playback (`TimelineView`, adjustable fps) + all-frames grid; a house picker (correct per-house recolor); 1×/2×/4×/8×/16× nearest-neighbor scale; VOC sound playback (AVAudioPlayer); ICN tile sheets, FNT glyph sheets, CPS images, EMC listings. 64 tests green, zero warnings. Pixel logic is test-verified and faithful to OpenDUNE; on-screen appearance is confirmed by running the app. Insight: `swift-spm-macos-gui`.
- 2026-05-28: rendertest refinements + palette cycling. Frame animation no longer auto-plays — the preview defaults to a static frame with a Play toggle and a frame stepper, and each grid thumbnail shows its index + W×H. Implemented Dune II palette cycling: `PaletteAnimator` (DuneIIRenderer, ported from OpenDUNE `gui/gui.c:643`) cycles indices 223 (wind-trap, entry 12↔10 every 5 ticks), 255 (selection), 239 (repair) — the IBM.PAL magenta placeholders are meant to be animated. Refactored the asset detail view to store raw indices and colorize on display, so house remap + palette cycling apply live; added a "Palette cycling" toggle (default on). The wind-trap tile's purple pixels now pulse correctly. 65 tests green, zero warnings. Insight: `render-palette-animation`. Splitting a SHP into logical sprites and labeling directional-vs-animation frames (the other request) needs the unit-info sprite tables (displayMode + base sprite IDs + orientation tables) — not in the SHP itself; spec'd and queued as a focused unit-sprite-layout port.
- 2026-05-28: rendertest — logical unit sprite grouping + scale fix. Added `SpriteCatalog` (DuneIIRenderer), transcribed from OpenDUNE `g_table_unitInfo` + the `Sprites_Init` load-order base offsets (UNITS2=111, UNITS1=151, UNITS=238, verified against `src/sprites.c:485`): it splits the flat unit-SHP frame lists into logical per-unit groups (Quad, Tank body, Tank turret, Infantry, …) labeled directional vs animation. The app gained a "Units" category listing each group; selecting one shows just that unit's frames, with directional groups offering a Facing stepper (no auto-animate) and animation groups a Play toggle. Scale fix: the all-frames grid thumbnails now honor the 1×–16× scale picker (previously capped at 2×). 66 tests green, zero warnings. Insight: `sprite-global-indices`. Building grouping (on-map structures via ICN/ICON.MAP) is the remaining piece if wanted.
- 2026-05-28: rendertest — building & terrain grouping via ICON.MAP. Added the `IconMap` decoder (DuneIIFormats, ported from `src/sprites.h:8`): parses `ICON.MAP` into named icon groups (1–10 terrain/effects, 11–26 structures). The app gained "Buildings" and "Terrain" categories — each icon group (Windtrap, Refinery, Construction Yard, …) lists its `ICON.ICN` tiles as subitems, with tile house-remap + palette cycling (so the Windtrap pulses). 68 tests green (incl. real-data: ICON.MAP groups reference valid ICON.ICN tiles), zero warnings. Doc: `Documentation/Formats/IconMap.md`. Tiles shown individually; assembling them into the full multi-tile building shape (needs `structureInfo.layout`) is an optional refinement.
- 2026-05-28: rendertest — fixed the MENSHPM.SHP palette. Mentat face sprites (`MENSHP[H/A/O/M].SHP`) are colored by their matching `MENTAT<house>.CPS` embedded palette, not IBM.PAL (OpenDUNE `gui/mentat.c:494`); rendering them with IBM.PAL scrambled the reds/blues/whites. Added `AssetLibrary.cpsPalette(_:)` (finds a CPS across PAKs, returns its palette) and `AssetDetailView.mentatPalette(for:)` which maps a MENSHP sprite to its `MENTAT<letter>.CPS` palette in the `.sprite` decode path. Build clean, zero warnings. Insight: `render-contextual-palette`.
- 2026-05-28: rendertest — assembled whole buildings instead of separate tiles. Added `StructureCatalog` (DuneIIRenderer), transcribed from OpenDUNE `g_table_structureInfo[].layout` → `g_table_structure_layoutSize` (`src/table/structureinfo.c`): maps each structure icon group (11–26) to its `(width, height)` in tiles. The `.iconGroup` decode now assembles a building's `ICON.MAP` tiles — consecutive build/animation states of `w*h` tiles, row-major per `g_table_structure_layoutTiles` (`Structure_UpdateMap`, `src/structure.c:1779`) — into one `(w*16)×(h*16)` image per state, so the Buildings category shows whole buildings (Windtrap 2×2→4 states, Palace 3×3, Refinery 3×2, Starport 3×3→10 states, …). 1×1 turrets stay per-tile (their group tiles are facings). 69 tests green (added a `StructureCatalog.layout` test), zero warnings (full clean build incl. the `rendertest` product). Insight: `render-structure-layout`.
- 2026-05-28: rendertest — fixed palette cycling being applied to embedded-palette assets. `GUI_PaletteAnimate` (`gui/gui.c:643`) only cycles indices 223/239/255 of the active IBM.PAL game palette, where they are magenta placeholders; CPS images, WSA animations, and mentat-face SHPs carry their own palettes that use those indices for real colors, so cycling corrupted them (reported: MENTATM.CPS, MENSHPM.SHP, INTRO7B/8B/8C.WSA, OFINALB/C.WSA). Replaced the per-case `paletteAnimatable = true` assignments in `AssetDetailView.decode` with a single post-switch gate `paletteAnimatable = !rawFrames.isEmpty && displayPalette == nil` — animate only IBM.PAL-rendered content (tiles, icon groups, in-game unit sprites). Also fixed the assembled-windtrap "no cycling" report: index 223 lives only in the built states (2–3), not the foundation; assembled structures now default to state 2 (`Structure_UpdateMap`, `structure.c:1779`) so the pulsing power light is visible. 69 tests green, clean build zero warnings (rendertest is app-only, untested). Insight: `render-palette-animation` (extended).
- 2026-05-28: rendertest — fixed the palettes of assets that embed none. Real-install inspection showed `MENTAT[H/A/O/M].CPS` (paletteSize 0), `MENSHP*.SHP`, and the intro/finale WSAs (`INTRO*`, `?FINAL*`, hasPalette 0) carry no palette — so falling back to IBM.PAL scrambled them. Reproduced the runtime loads: mercenary mentat (`MENSHPM.SHP`/`MENTATM.CPS`) → `BENE.PAL` (`gui/mentat.c:500`); cutscene WSAs → `INTRO.PAL` (`GameLoop_PrepareAnimation`, `cutscene.c:82`); other mentats keep IBM.PAL. Replaced `AssetLibrary.cpsPalette` (built on the false premise that MENTAT*.CPS embed a palette) with `palette(named:)` (loads a raw `.PAL` from any PAK), and `AssetDetailView.mentatPalette` with `contextPalette(for:)`, applied as `image.palette ?? contextPalette(...)` / `animation.palette ?? contextPalette(...)`. Corrected insight `render-contextual-palette` (the earlier MENSHPM "fix" never worked — the CPS has no embedded palette). 69 tests green, clean build zero warnings.
- 2026-05-28: Phase 2/3 start — function-parity harness + first primitives (RNGs). Confirmed the OpenDUNE oracle builds headlessly here (configure with the `.shim/sdl2-config`, SDL `dummy` driver) and added a self-contained `--parity-golden` mode (`src/parity.c:Parity_DumpGolden`, wired in `opendune.c main()`) that dumps bit-exact native-primitive outputs to JSONL before any install/PAK init. Ported both RNGs to `DuneIIWorld/Rng/` — `Random256` (`Tools_Random_256`, tools.c:268) and `RandomLCG` (Borland LCG + `_Range`, tools.c:327/341) — and verified them bit-for-bit against the committed golden fixture (`WorldTests/Fixtures/rng-golden.jsonl`) in `WorldTests/RngGoldenTests` (3 tests). Docs: `Architecture/FunctionParityHarness.md` (the 3-step port plan + oracle build), `Algorithms/Rng.md`. 72 tests green, clean build zero warnings.
- 2026-05-28: Function-parity — ported the tile geometry + orientation primitives. Extended the OpenDUNE `--parity-golden` dump with `Tile_UnpackTile`, `Tile_GetDistance`/`GetDistancePacked`/`GetDistanceRoundedUp`, `Tile_GetDirection`/`GetDirectionPacked`, and `Orientation_Orientation256ToOrientation8`/`16` (over packed grids, an 8×8 tile32 point grid incl. sub-tile offsets, and all 256 orientation inputs). Ported them to `DuneIIWorld/Tile/` — `Tile32` (a bit-exact `tile32`: pack/unpack + the distance/direction math, wrapping `&+`, signed `Int8` direction) and `Orientation`. Consolidated the golden harness onto one growing fixture `WorldTests/Fixtures/primitives-golden.jsonl` with a shared `WorldTests/GoldenFixture` loader (migrated `RngGoldenTests`); excluded `Fixtures/` from the `WorldTests` build graph (read via `#filePath`, not bundled). Added `WorldTests/TileGoldenTests` (7 tests). Doc `Algorithms/Tile.md`. 79 tests green, clean build zero warnings.
- 2026-05-28: Function-parity harness — split the golden dump into per-category fixtures (preferred over one growing file). `--parity-golden=<dir>` now writes one JSONL per category (`rng-golden.jsonl`, `tile-golden.jsonl`) via a `categories[]` table in `Parity_DumpGolden`; `GoldenFixture.records(file:fn:)` loads the named category file. Adding a category = a `Golden_<Category>(FILE*)` fn + one table entry. 79 tests green, zero warnings. Docs updated.
- 2026-05-28: Function-parity — ported `Tools_AdjustToGameSpeed` and the pure encoded-index helpers `Tools_Index_GetType`/`Decode` to `DuneIIWorld/Tools/` (`Tools` enum + `IndexType`), with `Tile32.packXY` added for the tile-index decode. New golden categories `gamespeed-golden.jsonl` (every game speed × inverse × 6 cases) and `index-golden.jsonl` (14 encoded values), dumped by `Golden_GameSpeed`/`Golden_Index` in parity.c; asserted by `WorldTests/ToolsGoldenTests` (3 tests). `Tools_Index_Encode`/`IsValid`/`Get*` deferred (need the object pools). Doc `Algorithms/Tools.md`. 82 tests green, clean build zero warnings.
- 2026-05-28: World model — ported the first static stat tables (`DuneIIWorld/Stats/`), golden-verified field-for-field against OpenDUNE `g_table_*`. `HouseID` (Contracts) + `HouseInfo` (`houseinfo.c`); `LandscapeType`/`MovementType` + `LandscapeInfo` (`landscapeinfo.c`); `ActionType`/`SelectionType` + `ActionInfo` (`actioninfo.c`). New golden categories `houseinfo-`/`landscapeinfo-`/`actioninfo-golden.jsonl` dumped by `Golden_HouseInfo`/`Golden_Landscape`/`Golden_Action`; `GoldenFixture.decode(_:as:)` generic loader added; suites `HouseInfoGoldenTests` (1) + `StatTableGoldenTests` (2). Doc `Algorithms/StatTables.md`. 85 tests green, clean build zero warnings.
- 2026-05-28: World model — ported the two big stat tables, `ObjectInfo`+`UnitInfo` (`unit.h`/`unitinfo.c`, 27 units) and `StructureInfo` (`structure.h`/`structureinfo.c`, 19 structures), golden-verified field-for-field. Added the seam enums `UnitType`/`StructureType` (Contracts, alongside `HouseID`) and `DisplayMode`/`StructureLayout` (World). `ObjectInfo` is the shared embed (`DuneIIWorld/Stats/ObjectInfo.swift`) with its 13-bit flag bitfield as an `OptionSet` (`ObjectInfo.Flags`); `UnitInfo`/`StructureInfo` each embed it and add their own fields (+ `UnitInfo.Flags` OptionSet). Extended the OpenDUNE `--parity-golden` dumper with `Golden_DumpObjectInfo`/`Golden_UnitInfo`/`Golden_StructureInfo` (`src/parity.c`), emitting `unitinfo-`/`structureinfo-golden.jsonl`. The Swift `table` literals are generated from those dumps by the committed `Tests/WorldTests/Fixtures/.gen_tables.py` and re-checked by `WorldTests/UnitStructureInfoGoldenTests` (3 tests: per-type unit + structure parity, plus hand-known spot-checks as an independent anchor against a dumper bug). 88 tests green, clean build zero warnings. Note: in this environment direct `exec` of `/usr/bin/clang` and freshly-built binaries is `EPERM`, so the oracle rebuilds via a new `Repositories/OpenDUNE/.shim/gcc` (→ `xcrun clang`), and `swift build`/`test` need `TMPDIR=$PWD/.build/tmp` + `--disable-sandbox`.
- 2026-05-28: World model — ported the structure layout geometry + the AI action table. `StructureLayoutInfo` (`DuneIIWorld/Stats/`) consolidates OpenDUNE's six `g_table_structure_layout*` arrays (`src/table/structureinfo.c`) into one struct keyed by `StructureLayout`: `tiles[9]`/`edgeTiles[8]`/`tileCount`/`tileDiff` (`Tile32`)/`size` (`XYSize`)/`tilesAround[16]` (signed `Int16`). Added `XYSize` (World) and `ActionInfo.actionsAI` (`g_table_actionsAI[4]` = hunt/areaGuard/ambush/guard, `unitinfo.c:12`). Extended the oracle dumper with `Golden_StructureLayout`/`Golden_ActionsAI` (`src/parity.c`) → `structurelayout-`/`actionsai-golden.jsonl`; asserted by `StatTableGoldenTests` (2 new tests). The renderer's `StructureCatalog` keeps its independent layout mirror (it can't import World). 90 tests green, clean build zero warnings.
- 2026-05-28: World model — ported the runtime data-model PODs (`DuneIIWorld/Model/`), faithful to OpenDUNE's structs for the eventual save-format port. `ObjectFlags` (`OptionSet<UInt32>`, golden-pinned bit layout) + `ScriptEngine` (the C `uint16 *script` stored as the `scriptPC` offset) + `Object`; `Dir24` + `Unit`; `StructureState` + `Structure`; `HouseFlags` + `House`; `TeamActionType` + `TeamFlags` + `Team`; `MapTile` (the packed 4-byte `Tile`, with `init(packed:)`/`packed` round-trip, golden-pinned) + `MapInfo`. Extended the oracle dumper with `Golden_ObjectFlags`/`Golden_MapTile` (`src/parity.c`) → `objectflags-`/`maptile-golden.jsonl`. Tests in `WorldTests/ModelTests` (4): both bit layouts vs the oracle, default-construction faithfulness, flag set/contain/clear. Design doc `Architecture/DataModel.md` (also covers the pending pools + `GameState`). 94 tests green, clean build zero warnings.
- 2026-05-28: World model — built the object pools + the `GameState` aggregate + the deferred pool-dependent `Tools_Index_*`. `GameState` (`DuneIIWorld/State/`) is the single owned value-type aggregate: the unit/structure/house/team slot arrays (sized to OpenDUNE's `*_INDEX_MAX`) + their insertion-order find arrays, the `MapTile[64*64]` grid, both RNGs, the two clocks, and `validateStrictIfZero`. Ported `Unit`/`Structure`/`House`/`Team` `_Find`/`_Allocate` + `Unit_Recount` (`src/pool/*.c`) faithfully — including the unit index-band allocation, the house unit cap (winger/slither bypass), and the 3 special never-in-find-array structure slots (wall/2×2/1×1). Ported `Tools_Index_Encode`/`IsValid`/`GetUnit`/`GetStructure`/`GetObject`/`GetTile` (`src/tools.c`) onto `GameState` (`GameState+Index.swift`), with `ObjectRef` standing in for the C `Object*` return and `Tile32.addDiff` (`Tile_AddTileDiff`). New `Pool` constants + `PoolFind`. Tests in `WorldTests/GameStateTests` (7) cover band allocation, find iteration/filtering, special slots, recount, and the index round-trips. 101 tests green, clean build zero warnings.
- 2026-05-28: Phase 3 kickoff (`DuneIISimulation`) — the loop foundation. Added the `Simulation` driver (replacing the scaffold enum): `tick()` advances the two clocks pause-aware (`timerGUI` always; `timerGame` + the four `GameLoop_*` phases only when not paused), mirroring `parity.c`'s headless tick. Moved `gameSpeed`/`paused` + the seven `GameLoop_Unit` tick cursors (`UnitTickCursors`) into `GameState` (it owns mutable state). Ported `GameLoop_Unit`'s cadence faithfully (`advanceUnitCadence` → `UnitTickFlags`: movement +3, rotation +`AdjustToGameSpeed(4,2,8,inverse)`, blinking +3, unknown4 +20, script +5, unknown5 +5, deviation +60); the per-unit work + the Team/Structure/House phases are order-preserving stubs awaiting their per-type state-machine slices. Design doc `Architecture/SimulationLoop.md`. Tests `SimulationTests/LoopTests` (5: clocks, pause, cadence schedule, speed-scaled rotation, adjust wrapper). 105 tests green, clean build zero warnings.
- 2026-05-28: Phase 3 — added the dependency-ordered native-primitive plan (Plan.v1.md §9: Tier 0 done → A tile helpers → B rotation → C movement → D map → E combat → F lifecycle/pathfinding; each must match its OpenDUNE function by result/effect) and implemented Tier A items 1–3 (pure tile motion), golden-tested. `Tile32.centered` (`Tile_Center`), `Tile32.isOutOfMap` (`Tile_IsOutOfMap`), and `Tile32.moveByDirection` (`Tile_MoveByDirection`, `tile.c:276`) with the ported `_stepX` step table (`_stepY[i] = stepX[(i+64)&0xFF]`). New oracle dump `Golden_TileMotion` → `tilemotion-golden.jsonl` (Center + MoveByDirection over a 5×10×7 grid + IsOutOfMap); asserted by `WorldTests/TileMotionGoldenTests` (3). `Tile_MoveByRandom` (Tier A.4, RNG-coupled) deferred. 108 tests green, clean build zero warnings.
- 2026-05-28: Phase 3 primitives — Tier A.4 + the dependency-ready parts of Tiers B/C. `Tile32.moveByRandom` (`Tile_MoveByRandom`) golden-tested per-seed against the oracle (our `Random256` is bit-exact, so an isolated call matches byte-for-byte; `Golden_TileMotion` extended → 250 `Tile_MoveByRandom` rows). New `DuneIISimulation.UnitLogic` with `setOrientation` (`Unit_SetOrientation` `unit.c:1671`), `rotate` (`Unit_Rotate` `unit.c:65`, orientation state effect — its `Unit_UpdateMap(2,…)` is render/visibility, deferred), and `setSpeed` (`Unit_SetSpeed` `unit.c:1902`), behaviour-tested against hand-derived values over the golden `UnitInfo` (`UnitLogicTests`, 3). **Dependency correction:** found that `Unit_UpdateMap`/`Unit_Move`/`Unit_MovementTick` actually depend on the Tier-D map cluster + lifecycle/combat, so resequenced them after Tier D in Plan §9 (they are not "small"). 112 tests green, clean build zero warnings.
- 2026-05-28: Made the sim primitives replaceable (user directive: no static functions). Converted `UnitLogic` (static-func enum) into a `UnitPrimitives` protocol + OpenDUNE-faithful `DefaultUnitPrimitives` struct, injected into `Simulation` as `var unitPrimitives: any UnitPrimitives` (default = the faithful port) so it can be swapped (reference/optimized/instrumented/test). Scope: simulation primitives only — leaf value-type math (`Tile32`/`Tools`/`Orientation`/stat tables) stays static. Recorded the convention in `DuneIISimulation/CLAUDE.md` + `Architecture/SimulationLoop.md`. Tests updated; behaviour unchanged. 112 tests green, clean build zero warnings.
- 2026-05-28: Phase 3 Tier D/E — implemented the two dependency-free primitives + found the prerequisite wall. `MapPrimitives.isValidPosition` (`Map_IsValidPosition`, + `MapInfo.scales`=`g_mapInfos` and `GameState.mapScale`) and `HousePrimitives.areAllied` (`House_AreAllied`, + `GameState.playerHouseID`) — replaceable protocols + `Default*` impls injected into `Simulation`, golden-tested (`Golden_MapHouse` → `maphouse-golden.jsonl`: 30 valid-position + 147 allied rows; `SimulationTests/MapHouseTests`). **Finding:** the rest of Tier D (`Map_GetLandscapeType`/spice/fog) needs the runtime tile-id bases that `Sprites_Init` (`sprites.c:274`) derives from `ICON.MAP` — the deferred scenario/map/sprite-init layer — and Tier E (`Unit_Deviate`/`Unit_Damage`) needs Tier-F lifecycle (`Unit_Remove`/`Unit_SetAction`). Recorded in Plan §9; recommended next = port the scenario/map/sprite init. 114 tests green, clean build zero warnings.
- 2026-05-28: Scenario/map/sprite-init layer — design + sub-deliverable 1 (sprite tile-id init). Wrote `Architecture/ScenarioMapLoading.md` (the whole layer: resource access, sprite tile-ids, scenario `.INI` loading, `Map_CreateLandscape`, original-save conversion, the tile→sprite render mapping, and the map app — in dependency order). Implemented `TileIDs` (`DuneIIWorld/Map/`) = the runtime tile-id bases (`veiled`/`bloom`/`builtSlab`/`landscape`/`wall`) derived from `ICON.MAP` per `Sprites_Init` (`sprites.c:274`), stored in `GameState.tileIDs`. Added `IconMap.tileID(group:offset:)` — the flat `g_iconMap[g_iconMap[group]+offset]` lookup (indexes across group bounds, as the fog base needs). Verified against the real committed `ICON.MAP` (`TileIDsTests`: veiled 124, bloom 208, builtSlab 126, landscape 127, wall 33) + a synthetic `IconMap.tileID` test. This unblocks `Map_GetLandscapeType` (next). 117 tests green, clean build zero warnings.
- 2026-05-29: Map-from-seed — ported `Map_CreateLandscape` (`map.c`) as `GameState.createLandscape(seed:iconMap:)` (+ the recursive `Map_AddSpiceOnTile`, the `_offsetTable`/`around` tables). Reseeds the game RNG to the seed and generates the whole landscape (4×4 random grid → averaging passes → landscape-type filter → spice blobs via `Tile_MoveByRandom` → smoothing/sprite-index → final landscape-icon-group mapping). Purely RNG-driven, so bit-exact vs OpenDUNE: new oracle dump `Golden_CreateLandscape` loads the committed `ICON.MAP` directly and dumps the 4096 ground sprite IDs per seed (`createlandscape-golden.jsonl`, 4 seeds); `MapGeneratorTests` reproduces every tile of every seed. This is the keystone for drawing any map. 118 tests green, clean build zero warnings.
- 2026-05-29: Scenario loader + the `mapview` SpriteKit test app. World: `GameState.loadScenario(ini:iconMap:)` (+ `HouseID/UnitType/StructureType.named(_:)`) parses a scenario `.INI` — derives `TileIDs`, generates the landscape from `[MAP] Seed`, sets `[BASIC] MapScale`, and places `[UNITS]`/`[STRUCTURES]` into the pools (loading bypasses the unit cap). Verified loading real `SCENA001.INI` (`ScenarioLoaderTests`). New macOS app `Apps/mapview` (`swift run mapview`): loads any install scenario into a `GameState` and draws it with SpriteKit — terrain+structures composited as one `ICON.ICN` tile layer (`MapImageBuilder`), units as house-recoloured SHP sprites (resolved via `SpriteCatalog`); resizable window, toolbar with a scenario picker + a 1×/2×/4×/8×/16× scale (camera zoom). Builds + launches clean (live rendering verified on a display by the user). 119 tests green, clean build zero warnings.
- 2026-05-29: Tier D #11 — `Map_GetLandscapeType` (now unblocked by `TileIDs`). `MapPrimitives.landscapeType(_ tile:tileIDs:)` + the `_landscapeSpriteMap[81]` table (`DefaultMapPrimitives`); classifies a tile from its ground/overlay ids + the tile-id bases (structure case = `tile.hasStructure`). Golden-verified: extended `Golden_CreateLandscape` to set all 5 tile-id bases and dump the per-tile landscape-type grid (`lst`) after `Map_CreateLandscape`; `LandscapeTypeTests` reproduces every tile of every seed. 120 tests green, clean build zero warnings.
- 2026-05-29: `mapview` — basic game loop + palette animation + tank turrets (proper, no synthetics). The scene's `update(_:)` now runs `Simulation.tick()` (advances the clocks; no unit logic) and re-colorizes the cached terrain index buffer through the existing faithful `PaletteAnimator` (`GUI_PaletteAnimate` port) each tick, so the windtrap power light (index 223) pulses from real state. Refactored `MapImageBuilder` to expose `terrainIndices` + `colorize` (build-once, recolour-per-tick). Tank/siege turrets now draw: `unitSprites` emits the body plus a separate turret sprite (z-ordered above) for `hasTurret` units via the `SpriteCatalog` turret groups. Sandworm/sonic-blast (`blurTile`) are skipped rather than drawn as the wrong white blob — the proper sand-distortion BLUR draw (viewport.c) is a pending render port. 120 tests green, clean build zero warnings. **Deferred as proper engine ports (not hacks):** the structure Animation system (`g_table_animation_structure` + `Animation_Start`/`Tick`) for building frame animation; orientation-correct unit/turret frame selection (the viewport orientation tables); the sandworm BLUR draw.
- 2026-05-29: Structure Animation system — proper port (no synthetics) so buildings frame-animate from real state. `Animation`/`AnimationCommand`/`AnimationCommandStruct` PODs + `AnimationTables.structure` (29 rows, generated from the oracle dump `animationstructure-golden.jsonl`, golden-checked by `AnimationTableTests`). `GameState+Animation.swift` ports `Animation_Start`/`Animation_Tick` + the command handlers (`SetGroundTile` cycles a building's icon-group states into `g_map.groundTileID`; `Pause`/`Rewind`/`Forward`/`Abort`/`Stop`) and `Structure_UpdateMap` (stamps the built state + starts the per-state animation from `StructureInfo.animationIndex`). `GameState` gained the `animations` pool, `mapBaseTileID`, `iconMap`, `animationTimer`, `mapDirty`; `IconMap` made `Sendable`. The scenario loader stamps + animates each structure; `Simulation.tick()` calls `animationTick()`. `mapview` now draws structures straight from `g_map` (one pass) and re-blits the terrain only when `mapDirty`, so factories/refineries/etc. animate. `AnimationTests` verifies a building's tiles cycle over ticks. **Rendering system complete** for the five requested items (game loop, windtrap palette, building animation, tank turrets + orientation, sandworm distortion). 122 tests green, clean build zero warnings.
- 2026-05-29: `mapview` turret rendering corrected per OpenDUNE `viewport.c`. The turret is drawn for any unit with `turretSpriteID != 0xFFFF` (not just `hasTurret`) — so launcher, deviator, sonic tank, and devastator now show turrets too, not only tank/siege. The turret sprite is `UNITS2.SHP` frame `turretSpriteID − 111 + values_32A4[o8]`, oriented by `orientation[hasTurret ? 1 : 0]`. Centering fixed with the per-type pixel offsets: siege `values_336E`, devastator `values_338E` (per-orientation), sonic `−2y`, launcher/deviator `−3y`, tank none. clean build zero warnings, 122 tests green.
- 2026-05-29: Extracted the unit sprite-index logic into a testable engine type + added all-scenario tests. New `DuneIISimulation.UnitSprites.info(for:)` returns the body + (optional) turret `UnitSpriteLayer`s (global sprite index, flip, pixel offset) — a faithful port of OpenDUNE `viewport.c`'s per-unit drawing (`groundSpriteID`/`turretSpriteID` + the `values_32A4`/`values_32C4`/`values_336E`/`values_338E` tables; turret oriented by `orientation[hasTurret ? 1 : 0]`). `mapview` now uses it (maps the global index → SHP via the load-order bases) instead of its own copy. Tests: `UnitSpriteTests` (hand-derived body/turret indices, orientations, per-type turret offsets, turret-presence), and **`AllScenariosTests`** — loads every committed `SCEN*.INI` and verifies each unit/structure sits at its INI-declared position and every unit's sprites resolve with turret presence matching `turretSpriteID`. 127 tests green, clean build zero warnings.
- 2026-05-29: Tier D fog + spice primitives. Added to `MapPrimitives`/`DefaultMapPrimitives`: `tileIsUnveiled` (`Tile_IsUnveiled`, `sprites.c:477` — the 16-frame veil run ending at `veiledTileID`), `isPositionUnveiled` (`Map_IsPositionUnveiled`, `map.c:341` — `isUnveiled` flag ∧ non-veil overlay), and `changeSpiceAmount` (`Map_ChangeSpiceAmount`, `map.c:771` + the private `fixupSpiceEdges`, `map.c:725`) which grows/depletes spice on a tile (sand↔spice↔thick-spice), retiles via the LANDSCAPE icon group, and fixes the spice-edge sprites on the tile + its four `g_table_mapDiff` neighbours (mutating `state.map`/`state.mapBaseTileID`; the `Map_Update` render dirty-marking is a skipped seam). Golden harness extended (`Golden_Spice` → `spice-golden.jsonl`, 2 seeds × ±1, dumping the full ground grid after a packed-order whole-map pass); `SpiceTests` reproduces the oracle bit-for-bit, `UnveilTests` spot-checks the fog boundaries. 130 tests green, clean build zero warnings.
- 2026-05-29: Tier-F pool queries + Tier-D `Map_SearchSpice`. Added two faithful pool/map queries to `GameState` (World, alongside `unitFind`): `unitGetByPackedTile` (`Unit_Get_ByPackedTile` — slot of the unit on a packed tile, off-map/empty → nil; `g_map.index` is 1-based) and `unitIsTypeOnMap` (`Unit_IsTypeOnMap`, `unit.c:474` — any on-map unit matching house/type, with `HOUSE_INVALID`/`UNIT_INVALID 0xFF` wildcards and the `isNotOnMap` skip); `GameStateTests.mapQueries` pins both. With `unitGetByPackedTile` in place, ported `MapPrimitives.searchSpice` (`Map_SearchSpice`, `map.c:1117`): nearest harvestable spice within a radius (thick spice closer than 4 preferred, else any spice), skipping structure/unit tiles, with the clamped bounds computed signed (C int-promotion in `max`/`min`). New oracle dumper `Golden_SearchSpice` → `searchspice-golden.jsonl` (2 seeds × 6 query tiles × 3 radii, covering found + not-found); `SearchSpiceTests` reproduces it. `Unit_HouseUnitCount_Add` deferred — entangled with sound/GUI/music/teams/`Unit_SetAction`. 132 tests green, clean build zero warnings.
- 2026-05-29: Tier-E #16 `Unit_GetTileEnterScore` (the pathfinder's per-tile cost function). Added `UnitPrimitives.tileEnterScore` + `isValidMovementIntoStructure` to `DefaultUnitPrimitives` (`unit.c:2335`/`unit.c:660`) — faithful 1.07-path transcriptions composing the existing `MapPrimitives` (`isValidPosition`/`landscapeType`) + `HousePrimitives` (`areAllied`), and the new World pool query `GameState.structureGetByPackedTile` (`Structure_Get_ByPackedTile`, mirror of `unitGetByPackedTile`). Pinned `g_dune2_enhanced = false` in the oracle's `Parity_DumpGolden` (golden mode short-circuits config load) so dumps match the project's 1.07 target — verified a no-op for all existing fixtures. New oracle dumper `Golden_TileEnterScore` → `tileenterscore-golden.jsonl` (648 rows: 2 seeds × 27 unit types × 6 tiles × orthogonal/diagonal, no objects placed) reproduced bit-for-bit by `TileEnterScoreGoldenTests`; the occupant/structure branches the no-objects map can't reach are covered by `UnitMovementDecisionTests` (10 decision-trace cases). Doc `Algorithms/UnitTileEnterScore.md`. 143 tests green, clean build zero warnings.
- 2026-05-29: Tier-F pool lifecycle — the `_Free` operations (deferred with the loop). `ScriptEngine.reset()` = `Script_Reset` (`script/script.c`; the modeled numeric subset — frame/stack pointers 17/15, isSubroutine 0; the NULL-PC/scriptInfo pointers aren't stored). `GameState.unitFree`/`structureFree`/`teamFree` (`pool/{unit,structure,team}.c`): clear `o.flags`, reset the script (units/structures), remove the slot from the find array closing the gap, and (units) decrement the house's `unitCount` (wrapping to match the C `uint16`); the structure specials (slab1x1/slab2x2/wall) reset without touching the find array. `GameStateTests` (3 new: unit/structure/team free, gap-close + count). 146 tests green. Prerequisite for `Unit_Remove` (Tier F).
- 2026-05-29: Tier-F reference-clearing cluster — the bookkeeping `Unit_Remove`/`Unit_Deviate` need. On `GameState` (World, `GameState+Lifecycle.swift`): `structureGetLinkedUnit` + `structureSetState` (`structure.c`; sets `state` then re-stamps via the existing `structureUpdateMap`); `objectScriptVariable4Set`/`objectScriptVariable4Clear` (`object.c`; the two-way script-var-4 link, with the incoming-busy structure state flip IDLE↔BUSY when unlinked); `unitRemoveFromTeam` (`unit.c`; member decrement, returns free slots); and `unitUntargetMe` (`unit.c`) which scrubs every reference to a unit before removal — its own var-4 link, other units' `targetMove`/`targetAttack`/var-4, turret structures' `variables[2]`, team membership, and team `target`. Operate over `ObjectRef`/pool slots (the C `Object*` aliasing a unit or structure). `GameStateLifecycleTests` (4). 150 tests green, clean build zero warnings.
- 2026-05-29: Tier-F #20 `Unit_Remove` + the map/visibility cluster it needs (`unit.c`). On `GameState` (`GameState+Lifecycle.swift`): `unitRemoveFromTile` (tile-occupancy clear with the dest/`bulletIsBig` guard), `unitHouseUnitCountRemove`/`unitHouseUnitCountAdd` (per-house "units I can see" tallies + the AI-awake flip + `seenByHouses`), `unitUpdateMap(type:slot:)` (headless game-state subset of `Unit_UpdateMap` — visibility counts + tile occupancy for remove/place; the air-unit redraw, render dirty-marking, and fog-unveil radius are render/fog **seams**), and `unitRemove` itself (untarget → updateMap(0) → houseUnitCount remove → script reset → free). Two documented seams in `unitHouseUnitCountAdd`: the player-alert block (audio/GUI/music + suppression timers, gated on `selectionType` we don't model) and the ambush→`Unit_SetAction(HUNT)` reaction (needs the EMC VM); `Unit_Select(NULL)` deselect is a render seam. Refactored `House_AreAllied` into a canonical `House.areAllied(_:_:playerHouseID:)` (World) that both `DefaultHousePrimitives.areAllied` (Simulation) and the lifecycle bookkeeping delegate to (removed the duplicate). `GameStateLifecycleTests` (+3: count add/remove, full `unitRemove`). 153 tests green, clean build zero warnings.
- 2026-05-29: EMC script VM core (the big Phase-3 unlock). New `DuneIISimulation/Script/`: `ScriptInfo` (the loaded program — `DATA` words + `ORDR` per-type offsets, bridged from the Formats `Emc.Program`) and the replaceable `ScriptInterpreter` protocol + `DefaultScriptInterpreter` — a faithful port of OpenDUNE `Script_Run` (one opcode/step), the stack ops (push/pop/peek, fill-from-end, over/underflow → NULL-PC error), and `Script_Load`/`LoadAsSubroutine`/`IsLoaded` (`src/script/script.c`). All 19 opcodes incl. the three parameter encodings (13-bit GOTO, sign-extended inline byte, next-word), the call/return frame protocol, signed-16-bit wrapping arithmetic, and op-14 FUNCTION dispatch via an injected `(Int, inout ScriptEngine) -> UInt16?` closure (the per-category native tables wire through it later). Modeled OpenDUNE's NULL `script` pointer as `ScriptEngine.scriptPC == scriptNull` (`0xFFFF`); `ScriptEngine.reset()` (= `Script_Reset`) now nulls the PC and the default engine starts not-loaded. Doc `Algorithms/ScriptVM.md`. Tests `ScriptVMTests` (11: each opcode/encoding, subroutine frame, FUNCTION dispatch + unknown, not-loaded, overflow). **Next: bridge real EMC → `ScriptInfo` + start the per-category native function tables (`Script_Unit_*`).** 164 tests green, clean build zero warnings.
- 2026-05-29: EMC native script functions — first batch (`Script_General_*`). New `DuneIISimulation/Script/Natives/GeneralScriptFunctions.swift`: 12 category-independent natives ported as **clean, explicit-parameter functions** (per the new convention — no `STACK_PEEK` in the logic; a thin VM-glue will pass stack args in): `noOperation`, `delay`, `delayRandom`, `randomRange`, `getDistanceToTile`, `isEnemy`, `isFriendly`, `getIndexType`, `decodeIndex`, `getOrientation`, `getLinkedUnitType`, `unitCount` (`src/script/general.c`). Each heavily tested (`GeneralScriptFunctionsTests`, 11 tests with many cases each): RNG natives cross-checked against the golden `Random256`/`RandomLCG`, distance against golden `Tile_GetDistance`, the rest as exhaustive case tables (house/deviation/validity/sentinels) matching the source. 175 tests green, clean build zero warnings. Next: the op-14 dispatch glue (peek stack → call) + the unit-specific natives (`Script_Unit_*`).
- 2026-05-29: EMC native script functions — `Script_Unit_*` first batch. New `DuneIISimulation/Script/Natives/UnitScriptFunctions.swift` (holds the injected `UnitPrimitives`): 10 unit natives as clean explicit-parameter functions (`src/script/unit.c`) — `getAmount`, `getOrientation`, `setSpeed` (192/256 scale unless scenario-placed), `setOrientation`, `rotate` (turret-toward-target), `stop`, `isInTransport`, `getRandomTile`, `setTarget`, `setDestinationDirect`. Mutating natives route through `DefaultUnitPrimitives` (`Unit_SetSpeed`/`SetOrientation`), so they're cross-checked against direct primitive calls; tile bits reuse golden `Tile_GetDirection`/`Tile_MoveByRandom`. `UnitScriptFunctionsTests` (10 multi-case). 185 tests green, clean build zero warnings. Next: the op-14 dispatch glue (peek stack → call these) + the real EMC→`ScriptInfo` bridge to run a unit script end-to-end.
- 2026-05-29: EMC natives — `Script_Unit_GetInfo` + `Unit_FindClosestRefinery`. `UnitScriptFunctions.getInfo(slot:field:)` ports the 20-case info switch (`src/script/unit.c`) — HP%, targetMove/targetAttack, fire distance/delay, orientations (body + turret, incl. |target−current|), origin refinery (field 0x06), type/index/encoded, movingSpeed, destination/byScenario/explodeOnDeath/seenByHouses, deviation-aware houseID; field 0x12 transcribed literally (`movementType & 0x40`, always 0 in 1.07). Its one dependency, `GameState.unitFindClosestRefinery` (`unit.c` — nearest same-house BUSY refinery else any; non-harvester stamps its tile), ported as a World helper. Tests: `getInfo` over every field + `findClosestRefinery` (busy/fallback/non-harvester). 187 tests green, clean build zero warnings.
- 2026-05-29: Tier-F #19 core — `Unit_SetAction` (`unit.c:497`). New `DuneIISimulation/UnitActions.swift` (`UnitActions`, holds the injected `ScriptInterpreter`): `setAction(slot:action:scriptInfo:in:)` switches a unit to an action and (re)loads its EMC script for the unit's type — early-returns for a dying/destructing unit or `ACTION_INVALID` (0xFF); `switchType` 0 queues behind an in-progress move (`nextActionID`) when the unit has a destination else falls through to an immediate switch; 1 switches immediately (`Script_Reset` + `variables[0]=action` + `Script_Load`); 2 loads as a subroutine (unreachable via the 1.07 action table, but ported). `UnitActionsTests` (4: switchType 0 with/without destination, switchType 1, the dying/INVALID guards) with a synthetic per-type `ScriptInfo`. 191 tests green, clean build zero warnings. Remaining for #19: the thin `Script_Unit_SetAction` native wrapper (+ harvest guard) + the op-14 dispatch.
- 2026-05-29: EMC integration — the op-14 dispatch glue + script run loop (`UnitScriptRunner`). `dispatch(index:engine:state:slot:)` routes a function index to the ported native, reading args off the engine stack via the new `ScriptEngine.peek(_:)` (`STACK_PEEK`) — wiring `g_scriptFunctionsUnit` (the ~25 ported unit/general natives + the `Script_Unit_SetAction` harvest-guard wrapper); unported indices return nil (VM halts). `run(slot:in:budget:)` loops `Script_Run` up to a budget, stopping on `delay`/halt — copying the engine out and back so the dispatch can mutate `GameState` (incl. the unit) without overlapping `inout` access. Refactored `UnitActions.setAction` to take the engine separately (with a `slot`-only standalone overload) for the same reason. Tests `UnitScriptRunnerTests` (4): index routing, op-01 SetAction script-load, an end-to-end PUSH+Delay run (suspends on delay), halt-on-unported. **The VM + dispatch + natives now run a unit script end-to-end.** 195 tests green, clean build zero warnings. Next: bridge the real `UNIT.EMC` → `ScriptInfo` + a decision-trace golden vs the oracle's `g_parityScriptTrace`, and port the remaining `Script_Unit_*` natives.
- 2026-05-29: EMC end-to-end on real data. Added `RealUnitScriptTests` — bridges the committed `Resources/Scripts/UNIT/UNIT.emc` (`Emc.Program` → `ScriptInfo`), switches a tank to an action (`Unit_SetAction` loads its real script at the ORDR offset, `variables[0]=action`), and runs it under `UnitScriptRunner` (VM + op-14 dispatch). Confirms the whole chain decodes **actual game bytecode** and halts gracefully at the first unported native. **A real Dune II unit script now executes under our engine.** 196 tests green, clean build zero warnings. Next: port the remaining `Script_Unit_*` natives until a full action runs, then a decision-trace golden vs the oracle's `g_parityScriptTrace`.
- 2026-05-29: Behavioural scenario harness — foundation library `DuneIIScenarios` (new target; design `Architecture/ScenarioHarness.md`). `ScenarioTerrain` (deterministic 8×8 sand/rock from a seed, mapped into the scale-0 playable rect; `sandTileID = landscape+0`, `rockTileID = landscape+16`, both passable). `ScenarioKind` (moving / close- & far-attack / guarding / move-around-building) + `TestScenario`. `ScenarioBuilder` lays out a `GameState` — terrain + two houses + the units (positions, `Unit_SetAction` + destination/target) + an optional 2×2 building. `ScenarioRunner` (`ScenarioWorld.run(ticks:)`) ticks each unit's script and captures per-tick `UnitSnapshot`s (until movement/combat natives land the units stay put). Tests `ScenariosTests` (7: terrain determinism/mapping/apply + each scenario's layout built from the real `ICON.MAP`/`UNIT.EMC`). 203 tests green, clean build zero warnings. Next: the `scenariolab` macOS app (visual, 1×–16× zoom), then the OpenDUNE scenario-golden oracle mode + per-tick comparison.
- 2026-05-29: Scenario harness part 1 — the `scenariolab` macOS app (`swift run scenariolab`). SpriteKit window over the 8×8 region with a toolbar: scenario picker (the 5 kinds), two unit-type pickers, Regenerate (new terrain seed), Run/Pause, and a 1×–16× zoom. Builds a `ScenarioWorld` via `DuneIIScenarios.ScenarioBuilder` and renders terrain (+ building) as one `ICON.ICN` layer plus house-recoloured unit sprites (`UnitSprites` + `ScenarioImageBuilder`), ticking the world each frame so behaviours animate as the natives land. New target + `ScenarioImageBuilder`/`ScenarioScene`/`ScenarioAssets`/`ScenarioLabModel`/`ContentView`/`App`. Builds clean (full audit incl. the new product), 203 tests green. Next: part 2 — the OpenDUNE scenario-golden oracle mode + per-tick comparison.
- 2026-05-29: Scenario harness re-architected onto real machinery (per user direction): a shared scenario `.INI` loaded by both engines, terrain from `[MAP] Seed → Map_CreateLandscape` (real generator, partial transition tiles), and actions driven by simulated user `Command`s — built on the real scenario/command pipeline since the harness doubles as the integration bed. Revised `Architecture/ScenarioHarness.md`. Brick 1: the committed bootstrap `.INI` (`Tests/ScenariosTests/Fixtures/bootstrap.ini`) + `BootstrapScenarioTests` proving our `GameState.loadScenario` generates the createLandscape terrain (varied, with partial sand/rock tiles) and places the `[UNITS]`. 204 tests green, clean build zero warnings. Next bricks: the Command/input application pipeline (sim + oracle replay), the oracle `Scenario_Load` parity mode, the per-tick golden.
- 2026-05-29: Scenario harness brick 2 — the command/input order path. New `DuneIIContracts.Command` (`.move`/`.attack` by unit index + packed tile — the input→sim seam). `DuneIISimulation.UnitOrders` ports OpenDUNE's viewport click→order (`gui/viewport.c`): clear targets/route → resolve the tile → `Unit_SetAction` → `Unit_SetDestination` (move) / `Unit_SetTarget` (attack), with `Unit_FindTargetAround` (snap an attack to a nearby object). Brought in the supporting primitives `Unit_SetDestination`/`Unit_SetTarget`/`Unit_FindTargetAround` + the World helper `GameState.objectScriptVariable4Link`. `apply(_ command:)` drives it from a `Command`. `UnitOrdersTests` (5: move to empty/structure tile, attack with/without turret, findTargetAround). 209 tests green, clean build zero warnings. Next: wire commands into the scenario harness + the oracle `Scenario_Load` parity mode, then the per-tick golden.
- 2026-05-29: Scenario harness bricks 3–4 — OpenDUNE scenario golden, end-to-end. Oracle: new `--parity-scenario=<id>` mode in `parity.c` (self-contained — dispatched right after `File_Init`, since the full `GameLoop_Main` GFX/sprite/string init doesn't run headless): minimal init (pools + ICON.MAP tile-ids + `UNIT.EMC` script), reads the shared `SCEN<H><id>.INI` (`[MAP] Seed → Map_CreateLandscape`, `[UNITS]` via `Scen_LoadUnit` mirroring `Scenario_Load_Unit`), replays a `--parity-cmd=move|attack,<unit>,<tile>` stream (`Scen_Order`, mirroring `gui/viewport.c` + our `UnitOrders`), and dumps per-tick unit state. Swift `ScenarioGoldenTests` loads the same `bootstrap.ini`, applies the same `Command`, and asserts our unit state equals the oracle's **frame 0** for the moving scenario (load + move order) — bit-for-bit (position/orient/hp/action/targetMove/…). Committed fixture `moving-golden.jsonl`. The shared-scenario golden pipeline is proven; per-tick trajectory + the other scenarios follow as the movement/combat natives land. 210 tests green, clean build zero warnings.
- 2026-05-29: Scenario golden — prepared the whole-run **per-tick** comparison framework. `ScenarioGoldenTests` now loads the shared INI, applies the command, runs our engine for the whole trajectory (a frame per `Simulation.tick()`), and asserts our per-tick frames equal the oracle's leading `comparedTicks` (=1 today). Raising `comparedTicks` + regenerating the fixture with `--parity-ticks=N` extends it — the run + compare loop already handles the full sequence. **Headless-oracle limit recorded:** OpenDUNE's movement/placement path (`Unit_UpdateMap`/`Game_Prepare`) crashes/hangs in this sandbox, so committed fixtures carry frame 0 only; live trajectories need a display-capable env (or our `GameLoop_Unit` running scripts, the next phase). 210 tests green, clean build zero warnings. Ready to return to porting game-logic functions.
- 2026-05-29: Scenario golden — out-of-sandbox trajectory generation. The oracle's real scenario run needs the full game init (GFX/sprites) + `Game_Prepare`, which don't run in the headless sandbox (the freshly-built binary is also exec-blocked here). Added a second oracle mode `--parity-scenario-run=<id>` (`parity.c`): the real `Game_LoadScenario` + command replay + `GameLoop` ticking → the true per-tick trajectory; dispatched from `GameLoop_Main` after full init (reuses `Scen_Order`/`Scen_DumpUnits`). The existing `--parity-scenario` (self-contained, frame-0) stays for in-sandbox/setup parity. New `Scripts/gen-scenario-goldens.sh [INSTALL_DIR]` — run OUTSIDE the sandbox to build the oracle, stage the data dir (install + scenario INIs), run each scenario, and write `*-golden.jsonl` fixtures; then re-run the Swift golden + raise `comparedTicks`. Doc `Architecture/ScenarioHarness.md` updated (two modes + the script). Oracle links clean; 210 Swift tests green (read the committed fixtures, no oracle exec).
- 2026-05-29: `scenariolab` app polish. Rendering is now a fixed **square** (the 8×8 region = `sidePx` game pixels) centred on black — never squished by window resize; removed the SpriteKit camera so the scene maps square→square. Zoom is **point-to-pixel**: at `scale` the square is `sidePx × scale` screen points (1× = one game pixel per screen point, 2× = 2, …), driven by the view's frame (nearest-neighbour textures keep it crisp). Any selection change now **pauses** (rebuild starts paused; default `running=false`) so the setup can be assessed before running. Regenerate + Run/Pause toolbar buttons are SF Symbols (`arrow.clockwise`, `play.fill`/`pause.fill`). Builds clean, 210 tests green.
- 2026-05-29: Scenario golden — **real per-tick trajectories, headless**. Discovered the earlier "crashes/hangs" were the macOS stale-signature SIGKILL (fixed by `codesign --force --sign -` after build, now in the script + the OpenDUNE build step here). With the binary running, made the self-contained `--parity-scenario` mode actually move units: place each unit on the map (`Unit_UpdateMap(1)`, the bit of `Game_Prepare` movement needs), pin `gameSpeed=2` (matching our `GameState`), and point `g_viewportPosition` at the region — without that, `GameLoop_Unit` throttles an off-viewport unit's script to 3 opcodes/tick so it never sets up a move. A tank ordered `(16,16)→(40,40)` now rotates then steps diagonally (verified: `(16,16)…(19,19)` over 400 ticks). Removed the broken full-init `--parity-scenario-run` path (it segfaulted/hung — the full GFX/string init doesn't run headless). Regenerated `moving-golden.jsonl` as the real 401-frame trajectory; `gen-scenario-goldens.sh` uses `--parity-scenario` (runs anywhere, no display). Swift golden stays green at `comparedTicks=1` (frame 0 unchanged); raise it as our `GameLoop_Unit` learns to move. 210 tests green, clean build zero warnings.
- 2026-05-29: Tier-F #21 — the unit **pathfinder** (`Script_Unit_Pathfinder` + `_Connect` + `_Smoothen`, `src/script/unit.c`). New `DuneIISimulation/Pathfinder.swift`: faithful port — try the direct line toward the destination, and where blocked wall-follow clockwise/counter-clockwise (`connect`) then smooth out redundant turns (`smoothen`), with per-tile cost from `Unit_GetTileEnterScore` (the `score` wrapper = `tileEnterScore(unit, packed, dir<<5)`, structure sentinel −1→256, the 1.07 path). Holds the injected map/house/unit primitives. Golden-verified bit-for-bit vs the oracle: exposed the file-local pathfinder via `Script_Pathfinder_Parity` (`script/unit.c`) + new `Golden_Pathfinder` dumper (a tank routing over generated maps) → `pathfinder-golden.jsonl`; `PathfinderTests` reproduces every route/score/routeSize (2 seeds × 5 src→dst). 211 tests green, clean build zero warnings. Next toward movement: `Script_Unit_CalculateRoute` (wraps the pathfinder → unit.route + orient toward route[0]) + `Unit_StartMovement`/`Unit_MovementTick`/`Unit_Move`, then wire `GameLoop_Unit`.
- 2026-05-29: **Unit movement cluster — a tank moves end-to-end, bit-identical to OpenDUNE over the whole 400-tick trajectory.** Ported `Tile_MoveByOrientation` (`tile.c:405`, leaf → `Tile32`) + the new `DuneIISimulation/UnitMovement.swift`: `Unit_MovementTick` (`unit.c:98`), `Unit_Move` (`unit.c:1286`), `Unit_StartMovement` (`unit.c:1059`), `Unit_Deviation_Decrease` (`unit.c:1174`), and the script native `Script_Unit_CalculateRoute` (`script/unit.c:1308`, op-`0x0C`) — composing the already-ported primitives (set-speed/-orientation/tile-enter-score, `Pathfinder`) + the World pool/map ops. `Unit_Move`'s bullet/sonic/saboteur/spice-bloom branches are transcribed with `SEAM` markers for their unported deps (`Unit_Damage` #18, `Map_MakeExplosion` #15, `Unit_EnterStructure`, `Map_Bloom_Explode*`); inert for a ground unit on sand. Wired the faithful `GameLoop_Unit` per-unit body into `Simulation` (`gameLoopUnit`: turret-aim/movement/rotation/deviation/tile-reclaim/script(52 opcodes)/nextAction-promote) gated by the tick cadence; `Simulation` now takes an optional `scriptInfo` and builds a `UnitScriptRunner`. Also ported the no-target/no-fog natives the MOVE routine calls before CalculateRoute — `Script_Unit_Fire` (`0x08`, early-outs faithful, projectile spawn SEAM), `Script_Unit_Unknown2552` (`0x24`), `Script_Unit_RemoveFog` (`0x28`, fog SEAM). `ScenarioGoldenTests.moving` now asserts the **whole** committed `moving-golden.jsonl` (400 ticks) — the tank rotates then steps diagonally `(16,16)→(19,19)`, matching the oracle tile-for-tile + orientation-for-orientation. Doc `Algorithms/UnitMovement.md`; `TileMoveByOrientationTests` (2). 213 tests green, clean build zero warnings.
- 2026-05-29: EMC VM — unported natives now **halt the script cleanly** instead of suspending it. OpenDUNE's native table is fully populated (unused slots = `NoOperation`), so a function call never suspends a script — only `Delay` does. The interpreter's op-14 path previously returned `false` on an unknown/unported native **without nulling the PC**, so the runner stopped for that tick then resumed *past* the call next tick — silently skewing tick timing (this was what stalled unit movement before the needed natives were ported). Now a `nil` dispatch nulls the PC (`ScriptInterpreter` case 14): `Script_IsLoaded` goes false and the unit freezes — the gap is loud and visible (principle: surface the gap, don't invent behavior), and timing is never silently altered. Updated `ScriptVMTests`/`UnitScriptRunnerTests` (assert PC nulled + stays stopped) and docs (`Algorithms/ScriptVM.md`, interpreter/runner comments). 213 tests green, clean build zero warnings.
- 2026-05-29: Tier-E #18 — `Unit_Damage` (`unit.c:1530`). New `DuneIISimulation/UnitCombat.swift` (`UnitCombat`, sibling of `UnitMovement`, reusing its `actions`/`scriptInfo`/`deviationDecrease`): HP drain → `Unit_Deviation_Decrease(0)`; on death `Unit_RemovePlayer` (new `GameState.unitRemovePlayer` — clears `allocated` + `Unit_RemoveFromTeam` for a player unit; deselect/GUI seam) + `Unit_SetAction(DIE)`; survivors: small-arms hit on an ambushing enemy → `ACTION_ATTACK`, sandworm/below-half → `ACTION_DIE`, infantry/troopers → +2 type with HP reset (RNG `RETREAT` chance), tracked/wheeled/harvester → `isSmoking`. Seams: `Map_MakeExplosion` (`range != 0`, #15), `Map_FillCircleWithSpice` (harvester death, Tier-D), `Sound_Output_Feedback` (audio), `Unit_UpdateMap(2)` (render). Added a standalone `UnitMovement.deviationDecrease(slot:amount:in:)` overload. Decision-trace tests `UnitCombatTests` (6: non-lethal/lethal/enemy-not-unallocated/smoke/infantry-downgrade/sandworm). Not yet wired into `Unit_Move`'s degrades branch (combat-integration slice, to avoid a Move↔Combat cycle). Plan §9 #18 marked done. 219 tests green, clean build zero warnings.
- 2026-05-29: `scenariolab` / `ScenarioRunner` now drive the **real `Simulation.tick()`** (the full four-phase loop + `GameLoop_Unit` cadence) instead of a simplified per-script stand-in — so the movement cluster runs and a `moving` unit actually crosses the 8×8 terrain in the lab. Fixed `ScenarioBuilder.move()`: it set `currentDestination` directly (which makes `Script_Unit_CalculateRoute` early-return → no route), now it sets `targetMove` + resets `route[0]` (the real `Unit_SetDestination`), so the move script routes to the destination. `ScenarioRunner.swift` imports `DuneIISimulation`. Updated `ScenarioBuilderTests` (moving now asserts `targetMove` + that the unit advances over 300 ticks; guarding asserts `targetMove`) and `DuneIIScenarios` CLAUDE.md. Attack/guard units run until an unported combat native then halt cleanly (aim/rotate, no fire). 219 tests green, clean build zero warnings.
- 2026-05-29: `scenariolab` terrain now uses real `Map_CreateLandscape` instead of the flat 8×8 sand/rock LCG — natural sand/dune/rock/mountain/spice with proper transition tiles (the same bit-exact generator the bootstrap golden uses), rendered through the existing `ICON.ICN` tile layer. `ScenarioTerrain` is now a thin placement-window descriptor (seed + origin + `mapPacked`); its `apply(to:iconMap:)` calls `state.createLandscape`. Dropped the `TerrainKind`/`kinds` LCG. `ScenarioTerrainTests` rewritten (reproducible + varied tiles via the real ICON.MAP). The mover still crosses the (now natural) terrain. 218 tests green, clean build zero warnings.
- 2026-05-29: Tier-E #17 — `Unit_Deviate` (`unit.c:1241`), in `UnitCombat.deviate`: guards (isNormalUnit / not already deviated / not isNotDeviatable), probability defaults to the owner house's `toughness` (−⅛ for non-player units), one `Random256` draw; on success sets `deviated=120` + `deviatedHouse`, flips to the new owner's default action (`actionsPlayer[3]` if the player gained it, else `actionAI`), and untargets (clears `targetAttack`/`targetMove`). `Unit_UpdateMap(2)` render redraw is a seam. `UnitCombatTests` (+4: success/already-deviated/immune/RNG-fail). Plan §9 #17 marked done. 222 tests green, clean build zero warnings.
- 2026-05-29: `scenariolab` — surfaced the new combat work: a **`deviate` scenario case** (`ScenarioKind.deviate`) where an enemy deviator mind-controls the player unit via the just-ported `Unit_Deviate`; the lab renders the victim in the captor's colours because `ScenarioImageBuilder` now colours units by the effective house (`GameState.unitHouseID` / `Unit_GetHouseID`) so deviation is visible. New case auto-appears in the picker (`ScenarioKind.allCases`). `ScenarioBuilderTests` +1 (deviate: victim deviated, deviatedHouse=Ordos, renders as Ordos, targets cleared). 223 tests green, clean build zero warnings.
- 2026-05-29: Scenario goldens — **viewport-script-throttle ported + table-driven per-tick harness with a second movement scenario**. Ported `Map_IsPositionInViewport` (`map.c:363`, as `Tile32.isPositionInViewport`) + `GameState.viewportPosition`; `GameLoop_Unit` now runs an off-viewport unit's script at 3 opcodes/tick (vs 52 in-view), unless `scriptNoSlowdown` — closing the pinned-52 SEAM. This makes movement match the oracle for **any** unit placement, not just near the parity viewport. `ScenarioGoldenTests` is now table-driven (`Spec` list, `@Test(arguments:)`): the golden test pins `viewportPosition=(12,12)` (matching the oracle's parity pin) and runs each scenario per-tick. Added **`move-trike`** (a Harkonnen trike on seed 777, off the parity viewport at tile 40:40 → oracle-throttled) — matches the full 400-tick trajectory; the `bootstrap` `moving` tank still matches fully. `gen-scenario-goldens.sh` generates both (`move-trike.ini` + golden committed). `scenariolab`/`ScenarioBuilder` point the viewport at the region so the lab runs full-speed. Multi-unit attack/guard goldens deferred (need the Simulation-layer Scen-style setup — per-unit `Unit_SetAction` + `Unit_UpdateMap` placement — and combat natives). 223 tests green, clean build zero warnings.
- 2026-05-29: Scenario goldens — **multi-unit setup fidelity + attack/guard per-tick goldens** (fixes the deferral). `loadScenario` now parses the unit's 6th `.INI` field (action) into `actionID` (`ActionType.named` = `Unit_ActionStringToType`); the golden test adds a Scen-style prepare (per used unit: `Unit_SetAction` to load its script + `Unit_UpdateMap(1)` placement — mirroring the oracle's `Scen_LoadUnit` + `Game_Prepare`), so multi-unit target resolution (`Unit_Get_ByPackedTile`) and occupancy match before the command. `Spec` gained `cmdUnit` (the order's unit index). New goldens: **`guard`** (a Harkonnen tank guards while an Ordos trike approaches from tile 40:40 — never reaches range, so the guard sits and the trike crosses the map: **full 400-tick match**) and **`attack-close`** (Harkonnen tank attacks an adjacent Ordos tank — the deterministic prefix matches to tick 5, where the defender's combat AI reacts; combat draws RNG, which the parity bar doesn't chase byte-exact, so it gates there). `gen-scenario-goldens.sh` generates all four. 223 tests green, clean build zero warnings.
- 2026-05-29: Tier-E — `Unit_FindBestTargetEncoded` + the find/priority chain (`DuneIISimulation/TargetFinder.swift`, native `Script_Unit_FindBestTarget` op-`0x1C`). Faithful, deterministic auto-target: `Unit_FindBestTargetUnit`/`Structure` scan the pools, `Unit_GetTargetUnitPriority`/`StructurePriority` score by the target's `priorityTarget+priorityBuild` over distance, gated by `seenByHouses` visibility, alliance, the `priority`/`targetAir` flags, and valid-position. Composes the injected map/house primitives. Wired into the unit dispatch; the guard/attack scenario goldens now run it (no regression — guard still full-match, attack-close still gates at tick 5). `TargetFinderTests` (4: picks seen enemy / unseen-ignored / allied-ignored / nearer-wins). **Note:** extending the attack goldens past the deterministic prefix is gated on the fog model — our `seenByHouses` for an enemy unit differs from the oracle's (which reveals fog around units), so the AI's acquisition timing diverges; the fog/visibility port is the next step. 227 tests green, clean build zero warnings.
- 2026-05-29: Fog of war primitives — `Map_UnveilTile` (`map.c:1129`), `Tile_RemoveFogInRadius` (`map.c:1196`), `Unit_RemoveFog` (`unit.c:1217`) in `GameState+Fog.swift`. Player-only fog: `Map_UnveilTile` lifts a tile's `isUnveiled` for the player (no-op for AI houses) and reveals any unit/structure on it (`seenByHouses` gains the player, via `Unit_HouseUnitCount_Add`); `Tile_RemoveFogInRadius` unveils the rounded-distance disc; `Unit_RemoveFog` reveals by the type's `fogUncoverRadius` (skipping an unplaced 0:0 unit). Render dirty-marking (`Map_MarkTileDirty`/`Map_Update`, the partial-fog overlay) is a seam. **Found + recorded:** wiring this into the `Script_Unit_RemoveFog` native (0x28) makes `attack-close` diverge *earlier* (revealing the adjacent enemy changes `FindBestTarget`'s visibility mid-combat, and the rest of combat isn't ported to match the oracle) — so the native stays a no-op for now and fog is verified by its own tests; flip it on with the combat slice. `FogTests` (3). 231 tests green, clean build zero warnings.
- 2026-05-29: Fix — the fog primitives shipped with `FogTests` red (committed in error). Two bugs: `Map_UnveilTile`'s already-unveiled guard compared `overlayTileID != tileIDs.veiled` (never fires when `veiled==0`, e.g. in tests) — replaced with a plain `isUnveiled` guard (we don't model the partial-fog overlay); and `Tile_RemoveFogInRadius` built the centre tile centred (`unpack`) but each candidate at the corner (`<<8`), skewing `distanceRoundedUp` asymmetrically — now both via `unpack`. `FogTests` (3) + full suite green (231). Process lesson recorded in CurrentState self-review: read the test result before committing (output was misread under a flaky shell this session).
- 2026-05-29: Combat-integration slice (step 1) — **unblocked the GUARD script loop by porting the three natives it clean-halted on.** Dispatch-traced the real `UNIT.EMC` GUARD routine (the blocker was *not* the guessed `0x1D/0x37`): it halts in sequence on `Script_Unit_SetDestination` (`0x05`), `Script_Unit_FindStructure` (`0x25`), `Script_Unit_IdleAction` (`0x31`). Ported all three in `UnitScriptFunctions` (+ dispatch in `UnitScriptRunner`): `setDestination` (`script/unit.c:796` — clears on 0/invalid, harvester-refinery special, else the shared `Unit_SetDestination` primitive, which now has a single home `unitSetDestination` that `UnitOrders.setDestination` delegates to); `findStructure` (`:1572` — first idle/unlinked/unbusied structure of the unit's house+type via `Structure_Find`); `idleAction` (`:1760` — ground-only stochastic idle twitch: `Tools_RandomLCG_Range(0,10)` then, on a low roll, a turret/body rotation chosen by `Tools_Random_256()&1`, faithful draw order). The GUARD loop now runs end-to-end (RemoveFog→SetDestination→SetTarget→FindStructure→**FindBestTarget**→SetTarget→IdleAction→Delay) and suspends cleanly — so a guarding unit now acquires targets via `FindBestTarget` each cycle. `UnitScriptFunctionsTests` (+3 decision-trace: setDestination/findStructure/idleAction). **`guard` scenario regated `compared: 0 → 6`** (its deterministic prefix): `IdleAction`'s idle twitch is RNG-driven, and our `random256` stream isn't byte-aligned with the oracle's through the unpinned render-only `wobble` draws — which we do not chase (Principle 1). Added LCG/256 tracing to `Parity_RunScenario` (`Repositories/OpenDUNE/src/parity.c`, `--parity-lcg-trace=`/`--parity-random-trace=`, gated → goldens unchanged) to diagnose it. Insight `sim-rng-stream-unpinned-wobble`. 233 tests green, clean build zero warnings.
- 2026-05-29: Combat-integration slice (step 2a) — **ported `Script_General_GetDistanceToObject` (native `0x3E`), unblocking the ATTACK script.** Dispatch-traced the `attack-close` attacker (action ATTACK): it clean-halted at tick 0 on `0x3E`. Ported it in `GeneralScriptFunctions.getDistanceToObject` (→ `Object_GetDistanceToEncoded`, `object.c:114`): a unit/tile target measures to its tile; a *structure* target measures to the edge tile facing the source (`StructureLayoutInfo.edgeTiles[(orient8+4)&7]`, the `g_table_structure_layoutEdgeTiles` port we already had) — matching OpenDUNE's un-gated `s->o.type` layout index (an enhancement over 1.07; we follow the oracle). Wired into `UnitScriptRunner` dispatch. **`attack-close` now matches the oracle through tick 60** (was 5): both tanks complete setup → aim → target-acquire identically; it diverges only at the first *shot* (tick 61, where the oracle spawns a type-23 projectile our `Script_Unit_Fire` SEAM doesn't yet create). Regated `attack-close` `compared: 5 → 61`. `GeneralScriptFunctionsTests` (+1: unit/tile/structure-edge/invalid). Next (step 2b): `Script_Unit_Fire`'s projectile path (`Unit_CreateBullet`). 234 tests green, clean build zero warnings.
- 2026-05-29: Combat-integration slice (step 2b) — **the `Script_Unit_Fire` projectile path: a unit now fires and spawns a bullet bit-identical to the oracle.** Ported `Unit_Create` (`unit.c:1380`, the general unit-spawn primitive: allocate → orientation/speed/hitpoints/flags init → `Unit_FindClosestRefinery` → tracked-degrade RNG → winger-speed/occupancy → `Unit_UpdateMap(1)` + default `Unit_SetAction`), `Unit_IsTileOccupied` (`unit.c:1789`), and `Unit_CreateBullet` (`unit.c:1310`, BULLET/SONIC + MISSILE cases incl. the `notAccurate` scatter + unseen-bullet `Tile_RemoveFogInRadius`) — all in `DuneIISimulation/UnitCombat.swift` (composing `movement`'s primitives/actions). Replaced the `Script_Unit_Fire` SEAM with the full firing path (target/self/turret/retarget/fireDelay/range/aim-diff<8 checks → `Unit_CreateBullet` or the sandworm devour → `fireDelay = AdjustToGameSpeed(fireDelay*2, inverse) + (Random256&1)` + the `firesTwice` 2nd-shot short delay); dispatch `0x08` now routes to `UnitCombat.fire` (new `combat` member on `UnitScriptRunner`). Extracted `Unit_SetTarget` into `UnitScriptFunctions.unitSetTarget` (single home; `UnitOrders.setTarget` delegates), mirroring `unitSetDestination`. **`attack-close` now matches the oracle through tick 66** (was 60): the attacker fires at tick 61, spawning the bullet (unit 12, type 23, hp 25, orient 64 @ packed 1040) **exactly** as the oracle, and the bullet flies identically for 6 ticks; it diverges at tick 67 = the bullet's `Unit_Move` impact → `Unit_Damage` (oracle: defender 200→188 hp, action→HUNT), the next SEAM. Made the scenario-golden comparison order-independent (sort by `index`): our snapshot enumerates pool-slot order, the oracle find-array/allocation order, which differ once a bullet is spawned mid-run. `UnitCombatTests` (+4: unitCreate winger/off-map, unitCreateBullet, fire early-outs). Next (step 2c): the bullet's `Unit_Move` impact → `Unit_Damage` (done) / `Map_MakeExplosion` (#15). 238 tests green, clean build zero warnings.
- 2026-05-30: Combat-integration slice (step 2c) — **the bullet's impact: full 400-tick unit-vs-unit combat now matches OpenDUNE bit-for-bit.** Ported `Map_MakeExplosion` (`map.c:403`) + relocated `Unit_Damage` (`unit.c:1530`) onto the movement layer (`DuneIISimulation/UnitImpact.swift`, `extension UnitMovement`) — `Unit_Move` triggers a bullet's explosion which damages nearby units, a genuine Move↔Combat cycle, so both live where `Unit_Move` can reach them with no construction cycle (`UnitCombat.damage` delegates). `Map_MakeExplosion`: the distance-scaled `Unit_Damage` to every unit in the reaction radius (`hitpoints >> (distance>>2)`), plus the non-allied-survivor retaliation (team-staging→HUNT, harvester-flee, guard-by-scenario→HUNT, else `Unit_SetTarget` toward the firer); structure damage / wall destruction / `Explosion_Start` are seams. Wired the three `Unit_Move` bullet-impact points (mid-flight wall/building/mountain hit; arrival detonation; the now-unblocked `degrades` self-`Unit_Damage(1)`). **`attack-close` regated `compared: 67 → 0` (FULL match):** the attacker fires at tick 61 → bullet flies → impacts the defender at tick 67 (200→188 hp, GUARD→HUNT) → the defender retaliates and hits the attacker back (200→188) — all 401 frames bit-identical to the oracle. (No RNG-spread divergence: the combatants are stationary, so they never draw the render-only `wobble` that desyncs `guard` — see `sim-rng-stream-unpinned-wobble`.) `UnitCombatTests` (+1: mapMakeExplosion radius/scaling/retaliation). Doc `Algorithms/UnitFire.md` updated. 239 tests green, clean build zero warnings.
- 2026-05-30: Combat-integration slice — **wired `Script_Unit_RemoveFog` (`0x28`) live** (`UnitScriptFunctions.removeFog` → `state.unitRemoveFog`, the long-standing fog SEAM). Held off earlier because revealing an enemy shifts `Unit_FindBestTarget`'s `seenByHouses` visibility mid-combat and diverged the attack goldens before combat was ported; now that `attack-close` matches the oracle for the full 400 ticks, flipping fog on keeps **all four scenario goldens green** — fog + combat-AI visibility finally come up together. No code beyond the one-native flip. 239 tests green, clean build zero warnings.
- 2026-05-30: Combat-integration slice — **second combat golden `attack-rocket` (Launcher duel), exercising the missile / `notAccurate`-scatter spawn path.** Two stationary Launchers (turreted, fireDistance 9) five tiles apart fire rockets at each other. New `attack-rocket.ini` + oracle golden (`Scripts/gen-scenario-goldens.sh`, scenario 95, 401 frames). Matches the oracle through tick 68: setup → aim → fire → the `Rocket` (type 19, `notAccurate`) spawning bit-identical, its scatter draws aligned. **Strong confirmation of `sim-rng-stream-unpinned-wobble`:** with *both* units stationary (no `wobble`), the `random256` stream stays aligned and even both GUARD units' `IdleAction` twitches match byte-for-byte — the desync `guard` hit comes specifically from its *moving* trike's render-only wobble. Gated `compared: 69`: it diverges at the **rocket unit's own in-flight EMC script** (the projectile draws `Tools_Random_256` at tick 69, ctx `u12`) — the rocket-script natives are the next gap, separate from the firing path. 239 tests green (5 scenario specs), clean build zero warnings.
- 2026-05-30: Tooling — **`Scripts/check.sh`** (the per-round verification: build + tests → a concise BUILD/TESTS/VERDICT summary that names the specific warnings, failing tests, and scenario divergences; `--full` for the clean-build zero-warnings audit, `--filter <Suite>` for the inner loop) and **`Scripts/build-oracle.sh`** (rebuild + ad-hoc re-sign the OpenDUNE oracle, wrapping the `.shim` + stale-signature gotchas). Both wrap this repo's env quirks (repo-local `TMPDIR`, `xcrun`, `--disable-sandbox`). CLAUDE.md "Running things" now points at `Scripts/` as the per-round entry point with a standing instruction to fold any new repeating action into them; CurrentState pointer added.
- 2026-05-30: `scenariolab` — added a **Speed control (1×–10×)** to the toolbar: a `speed` picker (`ContentView`) bound to `ScenarioLabModel.speed`, which drives `ScenarioScene.ticksPerFrame`. The scene's `update(_:)` now runs `tick()` that many times per rendered frame (rendering once afterwards), fast-forwarding the simulation without redrawing each tick — useful for watching long combat/movement scenarios play out. App-only/visual (no sim-logic change); `scenariolab` builds clean, full suite still 239 green.
- 2026-05-30: Combat — **fixed a missile-homing bug in `GameLoop_Unit`.** A flying winger non-normal-unit (a missile) re-aims each tick while `fireDelay != 0`; OpenDUNE (`unit.c:203`) re-aims at `currentDestination` *unless* it is chasing an **aircraft** (a winger-type `targetAttack`), in which case it tracks the moving target. Ours re-aimed at *any* unit `targetAttack`, so a rocket fired at a ground unit flew straight at the target (orient 64) instead of toward its scattered `currentDestination`. Now gated on the target being a winger (`Simulation.swift`). Dispatch-traced the Rocket (type 19) script first to rule it out — it just `Delay(1200)`s (no unported native), so this was a movement-loop bug, not a script gap. Cuts `attack-rocket`'s tick-69 divergence from 7 orientation units (64 vs 57) to **1** (58 vs 57); the residual is a sub-tile difference in `Tile_MoveByRandom`'s `center: false` scatter (the path `getRandomTile`'s golden never exercised) — flagged for a focused golden, not chased (stochastic scatter under the parity bar). `attack-rocket` stays `compared: 69`. 239 tests green, clean build zero warnings.
- 2026-05-30: Correction to the previous entry — the `attack-rocket` tick-69 residual is **not** a `Tile_MoveByRandom(center:false)` bug (that math checks out). Back-computing the scatter shows our `newDistance` resolves to `0x18` = the byte the oracle draws ~3 positions later, i.e. our `Script_Unit_Fire`/`Unit_CreateBullet` **missile** path makes ~3 extra `Tools_Random_256` draws the oracle doesn't. The fix is to find + remove those draws (diff our random256 sequence vs `--parity-random-trace` around the tick-66 fire), not to golden `moveByRandom`. Re-flagged in CurrentState.
- 2026-05-30: Closing the `attack-rocket` tick-69 residual investigation — **it is stochastic spread, not a bug** (correcting the two prior entries' "extra draws in the missile path" lead, which had the direction wrong). Instrumented our `random256` draw stream: over 70 ticks our engine draws only `13 c2 f6 55` vs the oracle's 13 bytes — *fewer*, not more. The defender launcher (`u23`, a GUARD unit) reaches `Script_Unit_IdleAction` on a slightly different cadence, so its idle-twitch RNG-draw count diverges; the twitches rotate the *turret* (level 1), so the body-orient snapshots still match through tick 68, but the diverged stream makes `u22`'s `notAccurate` rocket scatter consume different bytes → the 1-unit sub-tile `currentDestination`. Same class as `sim-rng-stream-unpinned-wobble` (idle/wobble RNG we don't byte-chase). `attack-rocket` stays `compared: 69`; not chased further. The missile-homing fix it surfaced stands. 239 tests green.
- 2026-05-30: Structures subsystem — first primitive: **`Structure_UntargetMe`** (`structure.c`) in `GameState+Lifecycle.swift`. Mirrors the ported `Unit_UntargetMe`: clears the structure's own two-way script-var-4 link, then zeroes any unit `targetMove`/`targetAttack`/`variables[4]` and any team `target` pointing at it (minus the turret-`variables[2]` + team-membership bits, which are unit-only). Needed by `Structure_Damage` (the `Map_MakeExplosion` structure SEAM). `GameStateLifecycleTests` (+1: scrubs unit targets + var4 link + team target). 240 tests green, clean build zero warnings.
- 2026-05-30: Structures subsystem — **`Structure_Remove`** (`structure.c:1305`, the map-clear + free teardown, analog of `Unit_Remove`) in `GameState+Lifecycle.swift`: clears `hasStructure` occupancy on each of the structure's layout tiles, frees the slot (`Structure_Free`, already ported), and scrubs references (`Structure_UntargetMe`). The destruction animation (`Animation_Start`/`Animation_Stop_ByTile`) and the House AI rebuild queue (`ai_structureRebuild`) are seams (render / AI, no headless consumer). `GameStateLifecycleTests` (+1: tile occupancy cleared, unit refs scrubbed, slot freed + dropped from the find array). 241 tests green, clean build zero warnings.
- 2026-05-30: Structures subsystem — **`Structure_Damage` + `Structure_Destroy`, wired into `Map_MakeExplosion`** (so a bullet/explosion now damages a building). `GameState.structureDamage` (`structure.c:1037`): HP drain; on 0 HP → `Structure_Destroy` + `Structure_UntargetMe` (returns destroyed); the score tally + sound + the survivor `range != 0` `Map_MakeExplosion(IMPACT_LARGE)` are seams. `GameState.structureDestroy` (`structure.c:979`): mark destroyed (`variables[0]=1`, un-`allocated`), reset script, remove linked units (or destroy a construction-yard's linked structure), apply the House credit penalty (storage-share loss + enemy build-cost refund) + windtrap decrement. **Seam:** the `Script_Load` of the structure death script (needs `BUILD.EMC` + the structure `ScriptInfo`, a Simulation concern) — so a destroyed structure stays unallocated-but-unremoved here until the script layer drives `structureRemove`. `UnitImpact.mapMakeExplosion`'s structure SEAM now calls `structureDamage`. `GameStateLifecycleTests` (+3: non-lethal drain / no-op guards, lethal destroy + enemy refund + ref-scrub, windtrap decrement). 244 tests green, clean build zero warnings.
- 2026-05-30: Tooling — `Scripts/log-history.sh`: append a dated bullet to `History/YYYY-MM.md` (workflow step 6), creating the month file with its header when new; flattens to one line, strips a pre-formatted `-`/date prefix, takes text as args or stdin, `--date` override. Wired into CLAUDE.md "Running things".
- 2026-05-30: docs: handoff refresh — CurrentState corrected for restart (branch `main` not `phase2-world-stat-tables`; test count 239→244; structures-subsystem + missile-homing + tooling added to Recently completed; self-review flagged OVERDUE at 93 commits).
- 2026-05-30: Periodic self-review (overdue, 94 commits since selfreview-phase1): reread History/2026-05.md + Insights; extracted one recurring lesson — RNG-stream-desync misdiagnosis (attack-rocket tick-69 cost three wrong diagnoses) — into a standing CLAUDE.md "What not to do" rule (align the RNG draw stream via --parity-random-trace before calling a scenario-golden divergence a bug; gate at the deterministic horizon). Re-tagged baseline selfreview-phase3, reset the count; CurrentState tracker updated.
- 2026-05-30: Structures subsystem — the structure script system + death-path closure (the big next foundation). Bridged BUILD.EMC → a structure ScriptInfo and ran structure EMC scripts via a new GameLoop_Structure (Simulation.swift): StructureTickCursors (degrade/structure/script/palace; only the script path is live, the rest seams), a StructureScriptRunner (op-14 dispatch over g_scriptFunctionsStructure, fixed 3-opcode batch, the 1.07 'i!=3 aborts remaining structures' quirk), and StructureScriptFunctions (SetState/GetState/RemoveFogAroundTile/the two var-4 scrubs/Explode/Destroy; FindUnitByType/turret/Fire/RefineSpice deferred → clean-halt). Key faithfulness point: the death-script Script_Load lives in GameLoop_Structure's not-loaded else-branch (Structure_Destroy only Script_Resets at the World layer), so a building damaged to 0 HP reloads its death branch (Explode→Delay→Destroy→Structure_Remove) — confirmed by decoding BUILD.EMC's shared @0 subroutine. Added GameState.structureRemoveFog (Structure_RemoveFog) + StructureTickCursors. Tests StructureScriptTests (3: healthy idle, full death-path removal, SetState DETECT resolution). Doc Algorithms/StructureScript.md. 247 tests, clean build zero warnings.
- 2026-05-30: Structures subsystem — turret-firing natives (Script_Structure_FindTargetUnit 0x08 / RotateTurret 0x09 / GetDirection 0x0A / Fire 0x0B) in StructureScriptFunctions, wired into StructureScriptRunner dispatch. FindTargetUnit is 1.07-faithful (distanceCurrent never updates → returns the LAST in-range seen non-allied unit, not the closest; the original swapped-assignment no-op). RotateTurret steps the turret groundTileID toward the target via the ICM base-turret sprite (group 23/24 through the iconMap). Fire reuses the ported Unit_CreateBullet (rocket turret ≥0x300 → missile/launcher delay, else bullet/tank delay), stamping originEncoded. A gun turret now acquires → aims → fires at a seen enemy end-to-end via GameLoop_Structure. StructureScriptTests (+4: FindTargetUnit last-pick/ignores, GetDirection, Fire spawn, the full turret-fires integration over ticks). Doc Algorithms/StructureScript.md (turret-firing section). 251 tests, clean build zero warnings.
- 2026-05-30: Structures subsystem — Script_Structure_RefineSpice (0x15): a refinery converts a linked harvester's spice into the owner House's credits. harvesterStep = (hitpoints*256/maxHitpoints)*3/256 per call (a damaged refinery refines slower), clamped to the harvester's remaining amount; 7 credits/unit (enemy refineries get a ±RNG bonus); script.delay=6 throttle; an emptied harvester drops inTransport; no linked unit → SetState(IDLE). The g_scenario allied/enemy harvested tally is a SEAM (scenario score). Ported in StructureScriptFunctions + StructureScriptRunner dispatch. StructureScriptTests (+3: player exact credits/amount/delay, empties→inTransport cleared, enemy RNG-bonus bounds). Doc Algorithms/StructureScript.md. Next deploy slice (FindUnitByType 0x03 / unit-unload 0x07 / Unit_EnterStructure) needs Structure_FindFreePosition + Unit_SetPosition (+ Unit_CallUnitByType). 254 tests, clean build zero warnings.
- 2026-05-30: Structures subsystem — deploy cluster: Unit_SetPosition + Structure_FindFreePosition + the unit-unload native (Script_Structure_Unknown0C5A, 0x07). Unit_SetPosition (UnitCombat.unitSetPosition, unit.c:1107) places an off-map unit (centred) at a tile, failing+re-flagging off-map if occupied, else clears destination/targets, refreshes visibility on an unveiled tile, sets the default action (AI/harvester/saboteur→actionAI else player[3]), and stamps the map. Structure_FindFreePosition (StructureScriptFunctions, structure.c:1101) picks a free non-wall/mountain/occupied tile from the ring around a structure (random start; spice-nearest for a harvester). unloadLinkedUnit (0x07) deploys the structure's linked unit — a winger lifts off the structure's own tile, a ground unit deploys to a free adjacent tile — then unlinks it, shifts the next queued unit in (or SetState IDLE), clears var-4. Wired into StructureScriptRunner dispatch. StructureScriptTests (+3: unitSetPosition place/occupied, findFreePosition ring/full, unload deploy+unlink). Doc Algorithms/StructureScript.md. Next: FindUnitByType (0x03, needs Unit_CallUnitByType) + Unit_EnterStructure to close the harvester loop. 257 tests, clean build zero warnings.
- 2026-05-30: Structures subsystem — Unit_EnterStructure (unit.c:2177) + Unit_Hide + Structure_GetStructuresBuilt (all GameState/World), closing the harvester economy loop. A unit arriving inside a structure: allied (harvester→refinery / unit→repair) → structure goes READY/BUSY (busyStateIsIncoming), a repair pad heals+times the unit, and the unit links into the structure's chain and stays hidden (Unit_Hide: off-map, script reset, untargeted, dropped from visibility counts); saboteur → Structure_Damage(500)+remove; enemy unit → capture a low-HP enemy structure (houseID swap + Structure_GetStructuresBuilt rebuild) else damage it. Seams: g_unitSelected deselect (render) + House_CalculatePowerAndCredit (House subsystem); 1.07-enhanced takeover untarget/unveil off. **Wired the Unit_Move SEAM** (UnitMovement.swift) so a unit arriving on a structure tile now enters it. StructureScriptTests (+4: enter-refinery link/hide, saboteur detonate, dead-structure remove, and the harvester→refinery→credits integration over GameLoop_Structure ticks). 261 tests, clean build zero warnings.
- 2026-05-30: Structures subsystem — Script_Structure_FindUnitByType (0x03) + Unit_CallUnitByType: the last BUILD.EMC native. Unit_CallUnitByType (UnitCombat, unit.c:2017) finds the last idle (linkedID 0xFF, not moving) unit of a type/house and orders it to a target (var-4 set); with createCarryall + none free + type==CARRYALL it spawns a fresh off-map scenario carryall (validateStrictIfZero bypass). FindUnitByType (StructureScriptFunctions, structure.c:195) summons a carryall to collect a READY structure's linked unit — a player harvester with no last-target + a free deploy spot is left to walk out (returns 0), else links the carryall via the structure's var-4. Wired into StructureScriptRunner dispatch. **All natives BUILD.EMC actually calls are now ported** — a structure runs its full script with no clean-halts; the refinery cycle (enter→refine→summon carryall/walk out→unload) runs end to end. StructureScriptTests (+3: summon idle carryall, player-harvester walk-out, create-carryall). Remaining structure work = the BUILD/REPAIR/factory production state machine + House_CalculatePowerAndCredit (House subsystem) + the carryall flight AI + the oracle [STRUCTURES] golden. 264 tests, clean build zero warnings.
- 2026-05-30: Parity harness — structure + house golden (oracle Scen_DumpState) + a structure scenario, and FOUND+FIXED a real bug. Extended the oracle scenario mode (parity.c): Scen_Init loads BUILD.EMC; new Scen_LoadStructure (Structure_Create placement, mirroring Scenario_Load_Structure); Scen_DumpUnits→Scen_DumpState dumps {tick,houses,structures,units} (reusing Parity_DumpHouses/DumpStructures); Parity_RunScenario parses [STRUCTURES]. New attack-structure.ini (a Harkonnen tank attacks an Ordos windtrap) + gen-scenario-goldens.sh entry; rebuilt+re-signed the oracle, regenerated all 6 goldens in the new format. ScenarioGoldenTests now compares structures (hp/state/linkedID) per tick. **The new golden immediately caught a real bug:** structures store the tile CORNER, not the centred sub-tile (Structure_Place: position &= 0xFF00) — our loadStructure used the centred Tile32.unpack, mispositioning every structure 128px and shifting unit-vs-structure aim (the tank fired ~10 ticks late). Fixed in ScenarioLoader.loadStructure; attack-structure now matches the oracle the FULL 400 ticks (units + structures, inc. the bullet-impact Structure_Damage 200→175). Insight world-structure-corner-position. Investigation of the better harness (Tier-2a structure decision-trace — oracle hooks already exist; Tier-3i event-trace) recorded in ScenarioHarness.md/ParityHarness.md. 264 tests, clean build zero warnings.
- 2026-05-30: Tooling — extracted the recurring investigation commands into Scripts/, and refined the golden generator. New: odfn.sh (print an OpenDUNE C function body + file:line — the single most re-typed command when porting), emc.sh (disassemble an EMC script: one type N / the shared region via --linear / all), golden.sh (a scenario golden JSONL as a per-tick 'what changed' timeline — replaces the python one-liners). Added a permanent --linear whole-program mode to assetgen emc-disasm (refactored Emc.disassemble into a from/to range overload) so the shared subroutines below the lowest type entry — a structure's death/turret/refine common code, reached only via Jump — are disassemblable (previously needed a temp tool hack). Refined gen-scenario-goldens.sh: DRY'd the oracle build through build-oracle.sh + added --only <name> (regenerate one scenario instead of all 6). CLAUDE.md 'Running things' lists the new helpers. 264 tests, clean build zero warnings.
- 2026-05-30: House subsystem — started: House_CalculatePowerAndCredit (house.c:470, GameState+Lifecycle). Recomputes a house's powerUsage/powerProduction/creditsStorage by summing over its structures — consumers add powerUsage, power plants produce -powerUsage scaled by hitpoints (1.07: ≤ half HP → half output, else -powerUsage*hp/maxHP), storage adds creditsStorage. Player low-power GUI warning + the g_playerCreditsNoSilo reset are seams. Closed the two Unit_EnterStructure takeover SEAMs (the captured/losing houses now recompute power+credit). HouseEconomyTests (3: sum producers/consumers/storage + other-house exclusion, damaged-plant proportional output, idempotent recompute). 267 tests, clean build zero warnings.
- 2026-05-30: House subsystem — GameLoop_House core (house.c:51): the House phase is now live (was an order-preserving stub). Added HouseTickCursors (6 cadences) + GameState.playerCreditsNoSilo; Simulation.gameLoopHouse advances the cursors and runs the core economy — tickHouse (every 900: clamp credits to creditsStorage, the player keeping a no-silo allowance; House_CalculatePowerAndCredit recompute; decrement timerUnit/Sandworm/StructureAttack) and tickPowerMaintenance (every 10800: deduct powerUsage/32+1 from credits). Seams: starport frigate delivery, scenario reinforcements, the house missile, starport stock, House_EnsureHarvesterAvailable, Structure_CalculateHitpointsMax, harvestersIncoming spawns, all GUI/sound. No effect on the scenario goldens (houses aren't allocated there → houseFind is empty → no-op, matching the oracle). HouseLoopTests (4: clamp+recompute, power maintenance, attack-timer decrement, paused freezes the economy). 271 tests, clean build zero warnings.
- 2026-05-30: Test apps — surfaced the structure + economy features across the apps. DuneIIScenarios/scenariolab: ScenarioBuilder + runner now take BUILD.EMC (structureScript) so structures run their scripts; added two scenario kinds — attackStructure (a tank destroys a pre-weakened windtrap → the death branch Explode→Destroy→Remove + soldiers runs live) and turretDefense (a gun-turret script acquires→aims→fires at an approaching enemy); fixed placeStructure to corner-position structures (insight world-structure-corner-position); ScenarioBuilderTests +2 (building damaged/destroyed, turret fires). mapview: loads UNIT.EMC + BUILD.EMC + allocates houses + Scen-style unit prepare + viewport, so the scenario runs LIVE; MapScene now rebuilds unit sprites each tick (units/bullets/soldiers follow the sim) and re-blits on structure changes (structureRemove now sets mapDirty). duneii-headless: replaced the stub with a real deterministic headless demo driver (loads scripts from Resources, runs attackStructure + turretDefense via ScenarioBuilder, prints the events — e.g. building destroyed+removed at tick 106). rendertest: asset-only, no change. 273 tests, clean build zero warnings.
- 2026-05-30: docs: session-reset handoff — rewrote CurrentState.md's stale Active task (it still described Phase-2-era stat-table work) into a concise current frontier (Phase 3 late-stage: structures + house economy done; remaining = production state machine / teams-AI / carryall AI; the verification frontier = Tier-2a structure decision-trace), preserved the env-quirk + Scripts notes, trimmed 32 foundational Phase-0/1/2 entries from Recently completed (left a History breadcrumb), refreshed the queue; 160→87 lines. Updated SimulationLoop.md (Structure + House phases are live, only Team is a stub). 273 tests, all green.
- 2026-05-30: world+sim: Structure_CalculateHitpointsMax — power ratio rescales each structure's max HP (floored at half), bleeds 1 HP when the cap drops below current HP; wired into tickHouse (GameState+Lifecycle.swift, Simulation.gameLoopHouse), HouseEconomyTests +3 (276).
- 2026-05-30: world+sim: structureTickStructure — the GameLoop_Structure tickStructure body goes live (structure-repair + factory production progress/completion + onHold + repair-pad unit-repair countdown); restructured gameLoopStructure to run it on the structure cursor + the faithful slab/wall skip; added GameState.campaignID (default 1). Upgrade branch + AI maintenance/auto-place remain seams. StructureProductionTests +10 (286).
- 2026-05-30: world: structureTickStructure upgrade branch + Structure_IsUpgradable (structure.c:1102) — upgrade progress (buildCredits/40/tick, upgradeTimeLeft step, level bump, Ordos Heavy-Vehicle 1→3 free, campaign-gated re-arm). Closes the tickStructure body (AI maintenance/auto-place + GUI remain seams). StructureProductionTests +7 (293).
- 2026-05-30: insight: build-gamestate-layout-stale-module — adding a GameState stored property tripped a stale-swiftmodule false failure in an unrelated test (SearchSpice); clean-build before trusting the red.
- 2026-05-30: world+sim: GameLoop_Team goes live — the team script subsystem bring-up (team.c:22). TeamScriptRunner over a bridged TEAM.EMC + the g_scriptFunctionsTeam dispatch (getters + delays + no-ops live; the recruit/target/order brain natives clean-halt as seams); single random-period cursor GameState.teamLoopTick (5-12 ticks), isAIActive gate, per-team delay, the 1.07 abort-rest quirk. New TeamScriptFunctions + Simulation.teamScriptInfo. TeamLoopTests +5 (298). Doc Algorithms/TeamScript.md.
- 2026-05-30: apps: surface the tickStructure economy in the test apps — 3 new ScenarioKinds (factoryProduce/repairBuilding/upgradeBuilding) exercise the build/repair/upgrade state machine live; the builder sets the player no-silo allowance + settles the structure (suspends the placement-animation script so it doesn't clobber the production state). duneii-headless prints each demo's economy progression; scenariolab lists them via allCases. ScenarioBuilderTests +3 (301).
- 2026-05-30: world+sim: team brain natives — recruit + target. Script_Team_AddClosestUnit (0x03), GetAverageDistance (0x04), FindBestTarget (0x06) in TeamScriptFunctions (script/team.c); new World primitives Unit_AddToTeam + Tile_GetTileInDirectionOf + Map_IsValidPosition (GameState+Lifecycle). TeamScriptRunner now carries the UnitScriptRunner so FindBestTarget reuses its TargetFinder. TeamScriptFunctionsTests +7. 308 tests. Order-issuing natives + Load are the next slice.
- 2026-05-30: parity: house-economy golden — a new [HOUSES] scenario section activates a house (Scen_LoadHouse/House_Allocate in parity.c; loadScenario+houseAllocate on our side) + both House_CalculatePowerAndCredit after load. economy.ini (Ordos windtrap+silo) gives a full 60-tick house-aggregate match (credits 2000→clamp 1000→power-maint 999, power 100/5, storage 1000) — validates House_CalculatePowerAndCredit + the credit clamp + power upkeep cross-engine. ScenarioGoldenTests compares houses per tick (HouseGolden); 7 scenarios. Oracle rebuilt+re-signed. 308 tests.
- 2026-05-30: parity: Tier-2a structure decision-trace harness. Wired --parity-script-trace into the oracle scenario mode (parity.c); our StructureScriptRunner emits the identical per-opcode line (pc/op/param/delay/SP/FP/return/current) via an injected StructureScriptTracer + ScriptTraceLine.decode (DuneIISimulation/Script/ScriptTrace.swift). StructureTraceTests diffs line-by-line — attack-structure windtrap (index 0) matches the oracle's 24-opcode trace exactly (placement-anim subroutine FP flex + Delay settle). Proves opcode-identical EMC execution. Fixture attack-structure-struct0-trace.txt + gen entry. 309 tests.
