# History — 2026-05

Append-only changelog. One bullet per change, imperative mood, with file references.

- 2026-05-28: Archived the first attempt and reset the project to a core-engine rebuild; wrote `Documentation/Plan.v1.md` — behavioral parity (validated against OpenDUNE as oracle), state machines as exact EMC transcription, the package topology, the parity verification pyramid, the multi-window verification UI, the render-test app, and the phased build.
- 2026-05-28: Phase 0 — rewrote `CLAUDE.md` for the rebuild (engine-first goals, package topology, core principles, the periodic self-review rule); added `Documentation/Architecture/{Overview,Testing,ParityHarness}.md`; recreated `CurrentState.md`; started `History/2026-05.md` and `Insights/README.md`.
- 2026-05-28: Phase 0 — scaffolded the SwiftPM package under `Code/`, organized by kind: `Frameworks/` (7 `DuneII*` libraries — Contracts/Formats/World/Simulation/Renderer/Input/Audio, each with its own `CLAUDE.md`), `Tools/` (`assetgen`), `Apps/` (`duneii-headless`), and `Tests/` (`ContractsTests`/`FormatsTests`/`WorldTests`/`SimulationTests`), with each target's directory set explicitly in `Package.swift`. Clean build, zero warnings, 4 scaffold link-tests green.
- 2026-05-28: Phase 1 — ported Format80 (LCW) decompression from OpenDUNE `src/codec/format80.c` to `Code/Frameworks/DuneIIFormats/Codec/Format80.swift` (decode-only, byte-by-byte back-references, throws on truncated/corrupt input); documented in `Documentation/Formats/Format80.md`; 11 decode tests covering every command, size clamping, and both error cases (suite: 14 green). Insight: `codec-format80-overlap.md`.
- 2026-05-28: Phase 1 — ported the rest of the format/codec layer to `DuneIIFormats`: Format40 (XOR-delta codec), PAK (archive), Palette (VGA 6-bit), IFF/FORM chunk reader (also serves the savegame container), SHP (sprites), CPS (images), ICN (tiles), WSA (animations), FNT (font), VOC (sound), INI (scenario text), and EMC (script bytecode parser + disassembler). Added `Format80.decodeToEnd` (unbounded) and a shared `ImageBlock` (raw/Format80) helper. Each format has an own-words doc under `Documentation/Formats/` citing its OpenDUNE source, synthetic per-branch tests, and a real-data test against install PAKs (SHP/CPS/ICN/WSA/FNT/VOC/INI/EMC all decode real DUNE.PAK/SCENARIO.PAK/VOC.PAK assets). Wired `assetgen emc-disasm`, which renders a real `UNIT.EMC` to a readable opcode listing with native-function names. Suite: 57 green, zero warnings.
- 2026-05-28: Correction to the Phase 0 scaffold entry above — the per-module `CLAUDE.md` files triggered SwiftPM "unhandled files" warnings that a `tail`-ed build hid; they are now `exclude:`d per target in `Code/Package.swift`, restoring a genuinely zero-warning clean build. Insight: `build-swiftpm-unhandled-files.md`.
- 2026-05-28: Phase 1 — note: full `Resources/` image/audio regeneration (PNG/WAV writers via ImageIO) is deferred — it is the presentation/writer layer, not format decoding, and `Resources/` is already committed and not needed for Phases 2–3. The Foundation-only decoder layer + `emc-disasm` are complete. (Closed below.)
- 2026-05-28: Phase 1 (asset export) — added the `DuneIIExport` writer library (`PngWriter` via ImageIO/CoreGraphics, `WavWriter` via hand-written RIFF) and `assetgen extract <installDir> <outputDir>`, which decodes the install PAKs to viewable PNG (SHP frames, CPS images, ICN tile sheets, WSA frames, FNT glyph sheets) and playable WAV (VOC) plus EMC listings. Verified on the real install: 350 assets extracted, 0 failures; CPS images are 320×200, the ICN sheet is a 256×400 grid, WAVs are valid RIFF/WAVE 8-bit mono. 60 tests green, zero warnings. Closes the deferral above. Insight: `swift-toplevel-mainactor-globals`.
- 2026-05-28: Renderer + render-test app (Phase-4 work pulled forward for asset verification). Added asset rendering services to `DuneIIRenderer` — `HouseRemap` (house color recolor ported from OpenDUNE `gui/viewport.c:300` sprites / `gfx.c:224` tiles, 1.07 path) and `IndexedImage` (indexed pixels + palette → `CGImage` with optional house remap + transparent index, unsmoothed), unit-tested for the remap and image sizing. Built `rendertest`, a native macOS SwiftUI asset inspector (SPM executable, `swift run rendertest [installDir]`): browsable PAK asset hierarchy; per-asset animated playback (`TimelineView`, adjustable fps) + all-frames grid; a house picker (correct per-house recolor); 1×/2×/4×/8×/16× nearest-neighbor scale; VOC sound playback (AVAudioPlayer); ICN tile sheets, FNT glyph sheets, CPS images, EMC listings. 64 tests green, zero warnings. Pixel logic is test-verified and faithful to OpenDUNE; on-screen appearance is confirmed by running the app. Insight: `swift-spm-macos-gui`.
- 2026-05-28: rendertest refinements + palette cycling. Frame animation no longer auto-plays — the preview defaults to a static frame with a Play toggle and a frame stepper, and each grid thumbnail shows its index + W×H. Implemented Dune II palette cycling: `PaletteAnimator` (DuneIIRenderer, ported from OpenDUNE `gui/gui.c:643`) cycles indices 223 (wind-trap, entry 12↔10 every 5 ticks), 255 (selection), 239 (repair) — the IBM.PAL magenta placeholders are meant to be animated. Refactored the asset detail view to store raw indices and colorize on display, so house remap + palette cycling apply live; added a "Palette cycling" toggle (default on). The wind-trap tile's purple pixels now pulse correctly. 65 tests green, zero warnings. Insight: `render-palette-animation`. Splitting a SHP into logical sprites and labeling directional-vs-animation frames (the other request) needs the unit-info sprite tables (displayMode + base sprite IDs + orientation tables) — not in the SHP itself; spec'd and queued as a focused unit-sprite-layout port.
- 2026-05-28: rendertest — logical unit sprite grouping + scale fix. Added `SpriteCatalog` (DuneIIRenderer), transcribed from OpenDUNE `g_table_unitInfo` + the `Sprites_Init` load-order base offsets (UNITS2=111, UNITS1=151, UNITS=238, verified against `src/sprites.c:485`): it splits the flat unit-SHP frame lists into logical per-unit groups (Quad, Tank body, Tank turret, Infantry, …) labeled directional vs animation. The app gained a "Units" category listing each group; selecting one shows just that unit's frames, with directional groups offering a Facing stepper (no auto-animate) and animation groups a Play toggle. Scale fix: the all-frames grid thumbnails now honor the 1×–16× scale picker (previously capped at 2×). 66 tests green, zero warnings. Insight: `sprite-global-indices`. Building grouping (on-map structures via ICN/ICON.MAP) is the remaining piece if wanted.
- 2026-05-28: rendertest — building & terrain grouping via ICON.MAP. Added the `IconMap` decoder (DuneIIFormats, ported from `src/sprites.h:8`): parses `ICON.MAP` into named icon groups (1–10 terrain/effects, 11–26 structures). The app gained "Buildings" and "Terrain" categories — each icon group (Windtrap, Refinery, Construction Yard, …) lists its `ICON.ICN` tiles as subitems, with tile house-remap + palette cycling (so the Windtrap pulses). 68 tests green (incl. real-data: ICON.MAP groups reference valid ICON.ICN tiles), zero warnings. Doc: `Documentation/Formats/IconMap.md`. Tiles shown individually; assembling them into the full multi-tile building shape (needs `structureInfo.layout`) is an optional refinement.
- 2026-05-28: rendertest — fixed the MENSHPM.SHP palette. Mentat face sprites (`MENSHP[H/A/O/M].SHP`) are colored by their matching `MENTAT<house>.CPS` embedded palette, not IBM.PAL (OpenDUNE `gui/mentat.c:494`); rendering them with IBM.PAL scrambled the reds/blues/whites. Added `AssetLibrary.cpsPalette(_:)` (finds a CPS across PAKs, returns its palette) and `AssetDetailView.mentatPalette(for:)` which maps a MENSHP sprite to its `MENTAT<letter>.CPS` palette in the `.sprite` decode path. Build clean, zero warnings. Insight: `render-contextual-palette`.
- 2026-05-28: rendertest — assembled whole buildings instead of separate tiles. Added `StructureCatalog` (DuneIIRenderer), transcribed from OpenDUNE `g_table_structureInfo[].layout` → `g_table_structure_layoutSize` (`src/table/structureinfo.c`): maps each structure icon group (11–26) to its `(width, height)` in tiles. The `.iconGroup` decode now assembles a building's `ICON.MAP` tiles — consecutive build/animation states of `w*h` tiles, row-major per `g_table_structure_layoutTiles` (`Structure_UpdateMap`, `src/structure.c:1779`) — into one `(w*16)×(h*16)` image per state, so the Buildings category shows whole buildings (Windtrap 2×2→4 states, Palace 3×3, Refinery 3×2, Starport 3×3→10 states, …). 1×1 turrets stay per-tile (their group tiles are facings). 69 tests green (added a `StructureCatalog.layout` test), zero warnings (full clean build incl. the `rendertest` product). Insight: `render-structure-layout`.
- 2026-05-28: rendertest — fixed palette cycling being applied to embedded-palette assets. `GUI_PaletteAnimate` (`gui/gui.c:643`) only cycles indices 223/239/255 of the active IBM.PAL game palette, where they are magenta placeholders; CPS images, WSA animations, and mentat-face SHPs carry their own palettes that use those indices for real colors, so cycling corrupted them (reported: MENTATM.CPS, MENSHPM.SHP, INTRO7B/8B/8C.WSA, OFINALB/C.WSA). Replaced the per-case `paletteAnimatable = true` assignments in `AssetDetailView.decode` with a single post-switch gate `paletteAnimatable = !rawFrames.isEmpty && displayPalette == nil` — animate only IBM.PAL-rendered content (tiles, icon groups, in-game unit sprites). Also fixed the assembled-windtrap "no cycling" report: index 223 lives only in the built states (2–3), not the foundation; assembled structures now default to state 2 (`Structure_UpdateMap`, `structure.c:1779`) so the pulsing power light is visible. 69 tests green, clean build zero warnings (rendertest is app-only, untested). Insight: `render-palette-animation` (extended).
- 2026-05-28: rendertest — fixed the palettes of assets that embed none. Real-install inspection showed `MENTAT[H/A/O/M].CPS` (paletteSize 0), `MENSHP*.SHP`, and the intro/finale WSAs (`INTRO*`, `?FINAL*`, hasPalette 0) carry no palette — so falling back to IBM.PAL scrambled them. Reproduced the runtime loads: mercenary mentat (`MENSHPM.SHP`/`MENTATM.CPS`) → `BENE.PAL` (`gui/mentat.c:500`); cutscene WSAs → `INTRO.PAL` (`GameLoop_PrepareAnimation`, `cutscene.c:82`); other mentats keep IBM.PAL. Replaced `AssetLibrary.cpsPalette` (built on the false premise that MENTAT*.CPS embed a palette) with `palette(named:)` (loads a raw `.PAL` from any PAK), and `AssetDetailView.mentatPalette` with `contextPalette(for:)`, applied as `image.palette ?? contextPalette(...)` / `animation.palette ?? contextPalette(...)`. Corrected insight `render-contextual-palette` (the earlier MENSHPM "fix" never worked — the CPS has no embedded palette). 69 tests green, clean build zero warnings.
- 2026-05-28: Phase 2/3 start — function-parity harness + first primitives (RNGs). Confirmed the OpenDUNE oracle builds headlessly here (configure with the `.shim/sdl2-config`, SDL `dummy` driver) and added a self-contained `--parity-golden` mode (`src/parity.c:Parity_DumpGolden`, wired in `opendune.c main()`) that dumps bit-exact native-primitive outputs to JSONL before any install/PAK init. Ported both RNGs to `DuneIIWorld/Rng/` — `Random256` (`Tools_Random_256`, tools.c:268) and `RandomLCG` (Borland LCG + `_Range`, tools.c:327/341) — and verified them bit-for-bit against the committed golden fixture (`WorldTests/Fixtures/rng-golden.jsonl`) in `WorldTests/RngGoldenTests` (3 tests). Docs: `Architecture/FunctionParityHarness.md` (the 3-step port plan + oracle build), `Algorithms/Rng.md`. 72 tests green, clean build zero warnings.
- 2026-05-28: Function-parity — ported the tile geometry + orientation primitives. Extended the OpenDUNE `--parity-golden` dump with `Tile_UnpackTile`, `Tile_GetDistance`/`GetDistancePacked`/`GetDistanceRoundedUp`, `Tile_GetDirection`/`GetDirectionPacked`, and `Orientation_Orientation256ToOrientation8`/`16` (over packed grids, an 8×8 tile32 point grid incl. sub-tile offsets, and all 256 orientation inputs). Ported them to `DuneIIWorld/Tile/` — `Tile32` (a bit-exact `tile32`: pack/unpack + the distance/direction math, wrapping `&+`, signed `Int8` direction) and `Orientation`. Consolidated the golden harness onto one growing fixture `WorldTests/Fixtures/primitives-golden.jsonl` with a shared `WorldTests/GoldenFixture` loader (migrated `RngGoldenTests`); excluded `Fixtures/` from the `WorldTests` build graph (read via `#filePath`, not bundled). Added `WorldTests/TileGoldenTests` (7 tests). Doc `Algorithms/Tile.md`. 79 tests green, clean build zero warnings.
- 2026-05-28: Function-parity harness — split the golden dump into per-category fixtures (preferred over one growing file). `--parity-golden=<dir>` now writes one JSONL per category (`rng-golden.jsonl`, `tile-golden.jsonl`) via a `categories[]` table in `Parity_DumpGolden`; `GoldenFixture.records(file:fn:)` loads the named category file. Adding a category = a `Golden_<Category>(FILE*)` fn + one table entry. 79 tests green, zero warnings. Docs updated.
- 2026-05-28: Function-parity — ported `Tools_AdjustToGameSpeed` and the pure encoded-index helpers `Tools_Index_GetType`/`Decode` to `DuneIIWorld/Tools/` (`Tools` enum + `IndexType`), with `Tile32.packXY` added for the tile-index decode. New golden categories `gamespeed-golden.jsonl` (every game speed × inverse × 6 cases) and `index-golden.jsonl` (14 encoded values), dumped by `Golden_GameSpeed`/`Golden_Index` in parity.c; asserted by `WorldTests/ToolsGoldenTests` (3 tests). `Tools_Index_Encode`/`IsValid`/`Get*` deferred (need the object pools). Doc `Algorithms/Tools.md`. 82 tests green, clean build zero warnings.
- 2026-05-28: World model — ported the first static stat tables (`DuneIIWorld/Stats/`), golden-verified field-for-field against OpenDUNE `g_table_*`. `HouseID` (Contracts) + `HouseInfo` (`houseinfo.c`); `LandscapeType`/`MovementType` + `LandscapeInfo` (`landscapeinfo.c`); `ActionType`/`SelectionType` + `ActionInfo` (`actioninfo.c`). New golden categories `houseinfo-`/`landscapeinfo-`/`actioninfo-golden.jsonl` dumped by `Golden_HouseInfo`/`Golden_Landscape`/`Golden_Action`; `GoldenFixture.decode(_:as:)` generic loader added; suites `HouseInfoGoldenTests` (1) + `StatTableGoldenTests` (2). Doc `Algorithms/StatTables.md`. 85 tests green, clean build zero warnings.
- 2026-05-28: World model — ported the two big stat tables, `ObjectInfo`+`UnitInfo` (`unit.h`/`unitinfo.c`, 27 units) and `StructureInfo` (`structure.h`/`structureinfo.c`, 19 structures), golden-verified field-for-field. Added the seam enums `UnitType`/`StructureType` (Contracts, alongside `HouseID`) and `DisplayMode`/`StructureLayout` (World). `ObjectInfo` is the shared embed (`DuneIIWorld/Stats/ObjectInfo.swift`) with its 13-bit flag bitfield as an `OptionSet` (`ObjectInfo.Flags`); `UnitInfo`/`StructureInfo` each embed it and add their own fields (+ `UnitInfo.Flags` OptionSet). Extended the OpenDUNE `--parity-golden` dumper with `Golden_DumpObjectInfo`/`Golden_UnitInfo`/`Golden_StructureInfo` (`src/parity.c`), emitting `unitinfo-`/`structureinfo-golden.jsonl`. The Swift `table` literals are generated from those dumps by the committed `Tests/WorldTests/Fixtures/.gen_tables.py` and re-checked by `WorldTests/UnitStructureInfoGoldenTests` (3 tests: per-type unit + structure parity, plus hand-known spot-checks as an independent anchor against a dumper bug). 88 tests green, clean build zero warnings. Note: in this environment direct `exec` of `/usr/bin/clang` and freshly-built binaries is `EPERM`, so the oracle rebuilds via a new `Repositories/OpenDUNE/.shim/gcc` (→ `xcrun clang`), and `swift build`/`test` need `TMPDIR=$PWD/.build/tmp` + `--disable-sandbox`.
- 2026-05-28: World model — ported the structure layout geometry + the AI action table. `StructureLayoutInfo` (`DuneIIWorld/Stats/`) consolidates OpenDUNE's six `g_table_structure_layout*` arrays (`src/table/structureinfo.c`) into one struct keyed by `StructureLayout`: `tiles[9]`/`edgeTiles[8]`/`tileCount`/`tileDiff` (`Tile32`)/`size` (`XYSize`)/`tilesAround[16]` (signed `Int16`). Added `XYSize` (World) and `ActionInfo.actionsAI` (`g_table_actionsAI[4]` = hunt/areaGuard/ambush/guard, `unitinfo.c:12`). Extended the oracle dumper with `Golden_StructureLayout`/`Golden_ActionsAI` (`src/parity.c`) → `structurelayout-`/`actionsai-golden.jsonl`; asserted by `StatTableGoldenTests` (2 new tests). The renderer's `StructureCatalog` keeps its independent layout mirror (it can't import World). 90 tests green, clean build zero warnings.
- 2026-05-28: World model — ported the runtime data-model PODs (`DuneIIWorld/Model/`), faithful to OpenDUNE's structs for the eventual save-format port. `ObjectFlags` (`OptionSet<UInt32>`, golden-pinned bit layout) + `ScriptEngine` (the C `uint16 *script` stored as the `scriptPC` offset) + `Object`; `Dir24` + `Unit`; `StructureState` + `Structure`; `HouseFlags` + `House`; `TeamActionType` + `TeamFlags` + `Team`; `MapTile` (the packed 4-byte `Tile`, with `init(packed:)`/`packed` round-trip, golden-pinned) + `MapInfo`. Extended the oracle dumper with `Golden_ObjectFlags`/`Golden_MapTile` (`src/parity.c`) → `objectflags-`/`maptile-golden.jsonl`. Tests in `WorldTests/ModelTests` (4): both bit layouts vs the oracle, default-construction faithfulness, flag set/contain/clear. Design doc `Architecture/DataModel.md` (also covers the pending pools + `GameState`). 94 tests green, clean build zero warnings.
- 2026-05-28: World model — built the object pools + the `GameState` aggregate + the deferred pool-dependent `Tools_Index_*`. `GameState` (`DuneIIWorld/State/`) is the single owned value-type aggregate: the unit/structure/house/team slot arrays (sized to OpenDUNE's `*_INDEX_MAX`) + their insertion-order find arrays, the `MapTile[64*64]` grid, both RNGs, the two clocks, and `validateStrictIfZero`. Ported `Unit`/`Structure`/`House`/`Team` `_Find`/`_Allocate` + `Unit_Recount` (`src/pool/*.c`) faithfully — including the unit index-band allocation, the house unit cap (winger/slither bypass), and the 3 special never-in-find-array structure slots (wall/2×2/1×1). Ported `Tools_Index_Encode`/`IsValid`/`GetUnit`/`GetStructure`/`GetObject`/`GetTile` (`src/tools.c`) onto `GameState` (`GameState+Index.swift`), with `ObjectRef` standing in for the C `Object*` return and `Tile32.addDiff` (`Tile_AddTileDiff`). New `Pool` constants + `PoolFind`. Tests in `WorldTests/GameStateTests` (7) cover band allocation, find iteration/filtering, special slots, recount, and the index round-trips. 101 tests green, clean build zero warnings.
- 2026-05-28: Phase 3 kickoff (`DuneIISimulation`) — the loop foundation. Added the `Simulation` driver (replacing the scaffold enum): `tick()` advances the two clocks pause-aware (`timerGUI` always; `timerGame` + the four `GameLoop_*` phases only when not paused), mirroring `parity.c`'s headless tick. Moved `gameSpeed`/`paused` + the seven `GameLoop_Unit` tick cursors (`UnitTickCursors`) into `GameState` (it owns mutable state). Ported `GameLoop_Unit`'s cadence faithfully (`advanceUnitCadence` → `UnitTickFlags`: movement +3, rotation +`AdjustToGameSpeed(4,2,8,inverse)`, blinking +3, unknown4 +20, script +5, unknown5 +5, deviation +60); the per-unit work + the Team/Structure/House phases are order-preserving stubs awaiting their per-type state-machine slices. Design doc `Architecture/SimulationLoop.md`. Tests `SimulationTests/LoopTests` (5: clocks, pause, cadence schedule, speed-scaled rotation, adjust wrapper). 105 tests green, clean build zero warnings.
- 2026-05-28: Phase 3 — added the dependency-ordered native-primitive plan (Plan.v1.md §9: Tier 0 done → A tile helpers → B rotation → C movement → D map → E combat → F lifecycle/pathfinding; each must match its OpenDUNE function by result/effect) and implemented Tier A items 1–3 (pure tile motion), golden-tested. `Tile32.centered` (`Tile_Center`), `Tile32.isOutOfMap` (`Tile_IsOutOfMap`), and `Tile32.moveByDirection` (`Tile_MoveByDirection`, `tile.c:276`) with the ported `_stepX` step table (`_stepY[i] = stepX[(i+64)&0xFF]`). New oracle dump `Golden_TileMotion` → `tilemotion-golden.jsonl` (Center + MoveByDirection over a 5×10×7 grid + IsOutOfMap); asserted by `WorldTests/TileMotionGoldenTests` (3). `Tile_MoveByRandom` (Tier A.4, RNG-coupled) deferred. 108 tests green, clean build zero warnings.
- 2026-05-28: Phase 3 primitives — Tier A.4 + the dependency-ready parts of Tiers B/C. `Tile32.moveByRandom` (`Tile_MoveByRandom`) golden-tested per-seed against the oracle (our `Random256` is bit-exact, so an isolated call matches byte-for-byte; `Golden_TileMotion` extended → 250 `Tile_MoveByRandom` rows). New `DuneIISimulation.UnitLogic` with `setOrientation` (`Unit_SetOrientation` `unit.c:1671`), `rotate` (`Unit_Rotate` `unit.c:65`, orientation state effect — its `Unit_UpdateMap(2,…)` is render/visibility, deferred), and `setSpeed` (`Unit_SetSpeed` `unit.c:1902`), behaviour-tested against hand-derived values over the golden `UnitInfo` (`UnitLogicTests`, 3). **Dependency correction:** found that `Unit_UpdateMap`/`Unit_Move`/`Unit_MovementTick` actually depend on the Tier-D map cluster + lifecycle/combat, so resequenced them after Tier D in Plan §9 (they are not "small"). 112 tests green, clean build zero warnings.
- 2026-05-28: Made the sim primitives replaceable (user directive: no static functions). Converted `UnitLogic` (static-func enum) into a `UnitPrimitives` protocol + OpenDUNE-faithful `DefaultUnitPrimitives` struct, injected into `Simulation` as `var unitPrimitives: any UnitPrimitives` (default = the faithful port) so it can be swapped (reference/optimized/instrumented/test). Scope: simulation primitives only — leaf value-type math (`Tile32`/`Tools`/`Orientation`/stat tables) stays static. Recorded the convention in `DuneIISimulation/CLAUDE.md` + `Architecture/SimulationLoop.md`. Tests updated; behaviour unchanged. 112 tests green, clean build zero warnings.
- 2026-05-28: Phase 3 Tier D/E — implemented the two dependency-free primitives + found the prerequisite wall. `MapPrimitives.isValidPosition` (`Map_IsValidPosition`, + `MapInfo.scales`=`g_mapInfos` and `GameState.mapScale`) and `HousePrimitives.areAllied` (`House_AreAllied`, + `GameState.playerHouseID`) — replaceable protocols + `Default*` impls injected into `Simulation`, golden-tested (`Golden_MapHouse` → `maphouse-golden.jsonl`: 30 valid-position + 147 allied rows; `SimulationTests/MapHouseTests`). **Finding:** the rest of Tier D (`Map_GetLandscapeType`/spice/fog) needs the runtime tile-id bases that `Sprites_Init` (`sprites.c:274`) derives from `ICON.MAP` — the deferred scenario/map/sprite-init layer — and Tier E (`Unit_Deviate`/`Unit_Damage`) needs Tier-F lifecycle (`Unit_Remove`/`Unit_SetAction`). Recorded in Plan §9; recommended next = port the scenario/map/sprite init. 114 tests green, clean build zero warnings.
- 2026-05-28: Scenario/map/sprite-init layer — design + sub-deliverable 1 (sprite tile-id init). Wrote `Architecture/ScenarioMapLoading.md` (the whole layer: resource access, sprite tile-ids, scenario `.INI` loading, `Map_CreateLandscape`, original-save conversion, the tile→sprite render mapping, and the map app — in dependency order). Implemented `TileIDs` (`DuneIIWorld/Map/`) = the runtime tile-id bases (`veiled`/`bloom`/`builtSlab`/`landscape`/`wall`) derived from `ICON.MAP` per `Sprites_Init` (`sprites.c:274`), stored in `GameState.tileIDs`. Added `IconMap.tileID(group:offset:)` — the flat `g_iconMap[g_iconMap[group]+offset]` lookup (indexes across group bounds, as the fog base needs). Verified against the real committed `ICON.MAP` (`TileIDsTests`: veiled 124, bloom 208, builtSlab 126, landscape 127, wall 33) + a synthetic `IconMap.tileID` test. This unblocks `Map_GetLandscapeType` (next). 117 tests green, clean build zero warnings.
- 2026-05-29: Map-from-seed — ported `Map_CreateLandscape` (`map.c`) as `GameState.createLandscape(seed:iconMap:)` (+ the recursive `Map_AddSpiceOnTile`, the `_offsetTable`/`around` tables). Reseeds the game RNG to the seed and generates the whole landscape (4×4 random grid → averaging passes → landscape-type filter → spice blobs via `Tile_MoveByRandom` → smoothing/sprite-index → final landscape-icon-group mapping). Purely RNG-driven, so bit-exact vs OpenDUNE: new oracle dump `Golden_CreateLandscape` loads the committed `ICON.MAP` directly and dumps the 4096 ground sprite IDs per seed (`createlandscape-golden.jsonl`, 4 seeds); `MapGeneratorTests` reproduces every tile of every seed. This is the keystone for drawing any map. 118 tests green, clean build zero warnings.
- 2026-05-29: Scenario loader + the `mapview` SpriteKit test app. World: `GameState.loadScenario(ini:iconMap:)` (+ `HouseID/UnitType/StructureType.named(_:)`) parses a scenario `.INI` — derives `TileIDs`, generates the landscape from `[MAP] Seed`, sets `[BASIC] MapScale`, and places `[UNITS]`/`[STRUCTURES]` into the pools (loading bypasses the unit cap). Verified loading real `SCENA001.INI` (`ScenarioLoaderTests`). New macOS app `Apps/mapview` (`swift run mapview`): loads any install scenario into a `GameState` and draws it with SpriteKit — terrain+structures composited as one `ICON.ICN` tile layer (`MapImageBuilder`), units as house-recoloured SHP sprites (resolved via `SpriteCatalog`); resizable window, toolbar with a scenario picker + a 1×/2×/4×/8×/16× scale (camera zoom). Builds + launches clean (live rendering verified on a display by the user). 119 tests green, clean build zero warnings.
- 2026-05-29: Tier D #11 — `Map_GetLandscapeType` (now unblocked by `TileIDs`). `MapPrimitives.landscapeType(_ tile:tileIDs:)` + the `_landscapeSpriteMap[81]` table (`DefaultMapPrimitives`); classifies a tile from its ground/overlay ids + the tile-id bases (structure case = `tile.hasStructure`). Golden-verified: extended `Golden_CreateLandscape` to set all 5 tile-id bases and dump the per-tile landscape-type grid (`lst`) after `Map_CreateLandscape`; `LandscapeTypeTests` reproduces every tile of every seed. 120 tests green, clean build zero warnings.
- 2026-05-29: `mapview` — basic game loop + palette animation + tank turrets (proper, no synthetics). The scene's `update(_:)` now runs `Simulation.tick()` (advances the clocks; no unit logic) and re-colorizes the cached terrain index buffer through the existing faithful `PaletteAnimator` (`GUI_PaletteAnimate` port) each tick, so the windtrap power light (index 223) pulses from real state. Refactored `MapImageBuilder` to expose `terrainIndices` + `colorize` (build-once, recolour-per-tick). Tank/siege turrets now draw: `unitSprites` emits the body plus a separate turret sprite (z-ordered above) for `hasTurret` units via the `SpriteCatalog` turret groups. Sandworm/sonic-blast (`blurTile`) are skipped rather than drawn as the wrong white blob — the proper sand-distortion BLUR draw (viewport.c) is a pending render port. 120 tests green, clean build zero warnings. **Deferred as proper engine ports (not hacks):** the structure Animation system (`g_table_animation_structure` + `Animation_Start`/`Tick`) for building frame animation; orientation-correct unit/turret frame selection (the viewport orientation tables); the sandworm BLUR draw.
- 2026-05-29: Structure Animation system — proper port (no synthetics) so buildings frame-animate from real state. `Animation`/`AnimationCommand`/`AnimationCommandStruct` PODs + `AnimationTables.structure` (29 rows, generated from the oracle dump `animationstructure-golden.jsonl`, golden-checked by `AnimationTableTests`). `GameState+Animation.swift` ports `Animation_Start`/`Animation_Tick` + the command handlers (`SetGroundTile` cycles a building's icon-group states into `g_map.groundTileID`; `Pause`/`Rewind`/`Forward`/`Abort`/`Stop`) and `Structure_UpdateMap` (stamps the built state + starts the per-state animation from `StructureInfo.animationIndex`). `GameState` gained the `animations` pool, `mapBaseTileID`, `iconMap`, `animationTimer`, `mapDirty`; `IconMap` made `Sendable`. The scenario loader stamps + animates each structure; `Simulation.tick()` calls `animationTick()`. `mapview` now draws structures straight from `g_map` (one pass) and re-blits the terrain only when `mapDirty`, so factories/refineries/etc. animate. `AnimationTests` verifies a building's tiles cycle over ticks. **Rendering system complete** for the five requested items (game loop, windtrap palette, building animation, tank turrets + orientation, sandworm distortion). 122 tests green, clean build zero warnings.
