# History — 2026-06-09

- client input QoL batch: (1) iOS map pans immediately on touch-down (dropped the 0.18s pan-settle delay) and switches to pinch-zoom when a second finger lands, fixing sluggish scroll (GameScene touchesBegan/Moved); (2) double-tap/click now selects the connected same-type *cluster* via InputController.clusterGroup (≤3-tile BFS hops, so a whole row selects), not just units within 3 tiles of the tap; (3) new triple-tap/click selects every same-type unit on the map (GameModel.tripleClickSelectAllSameType); (4) selections capture a centroid-relative formation (InputController.formation/TileOffset) replayed on move orders so groups keep their shape (GameModel.formationOffsets); (5) macOS Cmd+digit saves the selection (slots+formation) to a control group, plain digit recalls live units (GameModel.saveControlGroup/recallControlGroup, GameScene.keyDown). Input seam ⇒ neutrality (scenario/render goldens byte-identical) + InputControllerTests (clusterGroup, formationMove) + ClientTests/ControlGroupFormationTests (2).
- formation refinement: anchor a group's formation on its **leader** (the first/clicked unit) instead of the centroid — a move sends the leader to the clicked tile and the rest to leader-tile + their leader-relative offset, preserving the arrangement (GameModel.formationOffsets now leader-relative; double/triple-click put the clicked unit first via leaderFirst). The leader is exposed (InputController.leaderSlot / GameModel.leaderSlot) and drawn with a small yellow pennant over its tile (GameScene.makeLeaderFlag + selectionBoxes isLeader). Input/presentation seam ⇒ goldens byte-identical; tests updated (formationOffsetsAreLeaderRelative, clickedUnitBecomesLeader, leaderSlot assertions).
