# History — 2026-06-02

- feat(duneii): faithful Pause/Resume for a factory build — the client now shows a Pause button while building and a Resume button when on-hold (out-of-money or player-paused), mirroring OpenDUNE's `STR_D_DONE`/`STR_ON_HOLD` GUI clicks (`gui/widget_click.c:107/124`). New `Command.pauseBuild`/`.resumeBuild` → `GameState.structurePauseBuild` (set `.onHold`) / `structureResumeBuild` (clear `repairing`/`onHold`/`upgrading`); wired in `UnitOrders.apply` + `GameModel.pauseBuild/resumeBuild` + `Panels.buildSection`. A build only advances once the house can afford it (re-holds the same tick if still broke — faithful, NOT auto-resume). Player-GUI seam (no oracle-drivable golden — harness keeps the player powered): flag-off neutrality (new commands unused by existing goldens → all byte-identical) + `StructureProductionTests` (+2: pause-then-resume continues the build; resume-while-broke re-holds until credits return).
- docs: add SPEEDUP_UNORDERED key to Parallelization.md — an opt-in, explicitly non-golden build tier that drops RNG draw-order parity to enable full intra-tick parallelism (§2.6, §4e, §5.2), while the default + plain-SPEEDUP parity builds keep the golden bar
- docs: drop -Ounchecked from Parallelization.md — keep bounds/overflow checks in all builds (incl. SPEEDUP/SPEEDUP_UNORDERED); -O is the optimizer ceiling, no safety/trap-elision trade
- docs: spec the SPEEDUP_UNORDERED parallelization algorithm in Parallelization.md §4e — confirm cross-entity write deps are dense+data-determined (combat AoE damages other units, UnitImpact.swift:151), so no static partition; design a lock-free snapshot-read + deferred-effects fork-join (per-phase, concurrentPerform barrier, per-worker RNG + effect buffers, serial O(#effects) apply) with granularity/threshold guards
- docs: evaluate the work-queue/continuation parallelization model in Parallelization.md §4e.5 — it's task-graph/actor dataflow that relocates (not removes) the write conflict, gives the same independent-items parallelism as fork-join (no intra-chain speedup, only small pipeline-overlap), and its costs (queue contention + fine-grain task overhead) are the user's stated fears; land on a bulk-synchronous wavefront layered on deferred effects as the version that pays off
- Parallelization experiment (branch `parallelization-experiment`): added `Apps/simbench` (busy rock-map scenario — harvesters, factories building tanks, CYs building starports, quad duels) + a non-golden parallel unit phase (`Simulation+Parallel.swift`: `gameLoopUnitParallel`/`tickParallel`/`tickTimed`, snapshot-per-shard via `concurrentPerform`) + `DUNEII_UNIT_POOL` pool override + `benchInjectUnit`; measured sequential vs parallel — sequential wins decisively (~123k ticks/s faithful, ~2500 at 2000 units), parallel is ~7–10× SLOWER even for independent sims (cross-core ARC/COW refcount contention on shared stat tables + ScriptInfo, plus per-tick allocation), see Parallelization.md §8; refactored `gameLoopUnit`→`runUnitPhase` behaviour-preserving (605 tests green, goldens byte-identical).
- Parallelization experiment, round 2 (branch `parallelization-experiment`): pinned the parallel bottleneck. Made `ObjectInfo.actionsPlayer` POD (`PlayerActions`, was a heap `[ActionType]` copied per stat-table lookup) and added simbench isolation runs [5] de-shared ScriptInfo, [6] fully-independent build+tick, [7] worker-count scaling sweep, [8] memory-streaming sanity. Findings: de-sharing ScriptInfo and POD actionsPlayer both had NO effect; [8] memory streaming scales ~10× and [0] compute ~12×, but the sim collapses (2 workers ~1.3×, 16 workers ~0.13–0.29×) ⇒ the sim is allocator/ARC-bound (malloc global lock + atomic refcount traffic), not bandwidth/compute-bound. §3 target sharpened in Parallelization.md §8.3/§8.4 (kill hot-path heap alloc + ARC; batch sweet spot ~2–4 workers). 605 tests green, goldens byte-identical, duneii compiles.
- Parallelization round 3 — eliminated per-tick allocation + ARC (branch `parallelization-experiment`): new POD `Inline<let N,Element>` wrapper (`DuneIIWorld/Model/Inline.swift`) replaces the 4 heap arrays every Unit carried (orientation/route + ScriptEngine variables/stack) so a Unit/Structure is fully POD (memcpy copies, no ARC); pathfinder `Data.buffer`→`Inline<102,UInt8>` (alloc-free pathfind, routes byte-identical); find-arrays `reserveCapacity` at init. simbench `[9]` (malloc_logger hook) proves the tick is allocation-free: 0.05 allocs/tick. Sequential tick +35–42% (faithful 123k→175k ticks/s, massive 2533→3392), intra-tick snapshot [2] 3034→250ms (~12×). All golden-safe — 605 tests green, save round-trip intact. Parallel single-sim still collapses but now provably NOT alloc/ARC (tick is alloc-free) — residual is memory-access/scheduling, needs Instruments. See Parallelization.md §8.5.
- Parallelization experiment paused + merged: tagged `parallel-experiment-pause` (full experiment preserved on branch), then removed the parallel scaffolding (`Apps/simbench`, `Simulation+Parallel.swift`, `DUNEII_UNIT_POOL` env hatch in Pool.swift, `GameState+Bench.swift`, the `runUnitPhase` slot-split) and kept the golden-safe speedups on main: POD `Unit`/`ScriptEngine`/`Structure` via `Inline<let N,Element>`, allocation-free pathfinder, reserved find-arrays; and gated the RNG/script trace hooks behind `#if DEBUG` so release strips them (fast by default). 605 tests green (debug), clean release build 0 warnings; sequential tick ~35–42% faster. See Parallelization.md §8.
- Resolved the harvester redeploy known-issue — it was a test-scenario artifact, not an engine bug. A per-opcode trace showed the redeployed player harvester faithfully STOPs: `Unit_Harvest` fails, `SearchSpice(3)` finds nothing, and the escalating wide spice search (radius 20/40/60) is skipped for PLAYER harvesters (UNIT.EMC gates it on unit-house≠player-house — AI-only). Verified vs OpenDUNE (GetInfo 0x0E=Unit_GetHouseID, targetLast cleared on dock in Unit_Move, the refinery RefineSpice→FindUnitByType→Unknown0C5A deploy). The test put the refinery 6 tiles from spice → out of radius-3; fixed by building it adjacent to spice (HarvesterCycleTests refinery 28,20→23,20). The loop now closes; test asserts harvestedAgain directly (dropped withKnownIssue). 605 tests green, 0 known issues. Insight sim-script-vm-engine-copy updated (prior native-return-divergence hypothesis was wrong).
- Bumped the SwiftOPL3 (AdLib OPL3 synth) dependency on `main` from `0cc22ab` to `956d440` (Package.resolved; `swift package update`'s manifest step hits the sandbox EPERM quirk, so pinned the revision by hand and resolved via `swift build --disable-sandbox`). Builds clean (release, 0 warnings); AudioTests/ADLMusicTests (+0, all 4) green — the OPL3 path still renders non-silent PCM, tracks resolve, backend-switch preserves selection; full suite 605 green. Note: the new commit now compiles in `Memoirs`/`MemoirsWorkaroundC` (previously only under the OPL_TRACE trait). Live audio still needs a manual check: `swift run -c release duneii`.
- rendertest: collapsible sidebar groups + ADL music preview. Sidebar categories are now collapsible (`Section(isExpanded:)` with per-category state). Added a Music category listing every `g_table_musics` track whose `DUNE<file>.ADL` is present, with a play/stop/loop preview through the authentic OPL3 AdLib synth (`ADLMusicPlayer`); rendertest now depends on `DuneIIAudio`, and locates `Resources/Audio/Music`. New public `MusicDirector.previewTracks` (id/file/song/name) exposes the track list. Tests: `MusicDirectorTests.previewTracks` (+1, 606 total). rendertest builds clean (release, 0 warnings). Run: `swift run rendertest` from `Code/`.
- rendertest: fix the 'All frames' grid to span the full detail-pane width. It was the only non-greedy block in the detail view, so the LazyVGrid collapsed to its content width; added `.frame(maxWidth: .infinity, alignment: .leading)` (+ leading/top grid alignment) so `.adaptive` packs as many thumbnails per row as fit.
- Implemented sand tracks + the ornithopter crash wreck (both were SEAMs). Wired `Animation_Start(g_table_animation_unitMove[orient8], iconGroup 5)` at the tracked-unit SEAM in `UnitMovement` (a tank now leaves Sand-Track overlays on sand/dune), and `Explosion_Func_SetAnimation` → `animationStart(g_table_animation_map[id], iconGroup 3, kind:.map)` in `GameState+Explosion` (the ornithopter death's `Unit_ExplosionSingle(16)` crash explosion now paints the 'Flying-Machine Crash' wreck). Ported `g_table_animation_unitMove` (8 rows) + `g_table_animation_map` (16 rows) + AnimationKinds `.unitMove`/`.map`. Both are RNG-free starts on the animation/explosion (visual) path, so goldens stay byte-identical. Tests: new `TrackAndCrashTests` (+2: tank leaves a sand-track overlay; dying ornithopter creates explosion 16 + the crash wreck overlay). Insight `sim-emc-triggers-types-by-number` (EMC triggers types by number, not the C enum name — nearly mis-called the crash dead code; ornithopter is UnitType 1 not 11). 608 tests green, clean release build.
- fix(duneii): minimap blips were placed at 1/16 scale (used world-pixel scale on tile-space positions) — crammed into the top-left corner; now positioned + sized in tile space (side/64), scale with the panel, and structures span their full footprint (Minimap.swift).
- feat(duneii): in-game right sidebar (200pt) — radar + house/economy + selection sprite/HP/command-icons + factory/starport build-order list + Mentat/Options/Save/Load row; retires the floating Minimap/Inspector/Economy panels (folded into the sidebar + an Options popover). New Sidebar.swift with a cached UnitType/StructureType→CGImage SpriteImageProvider; SelectionInfo gains typeRaw/houseID (GameModel.swift); ContentView HStack(map, sidebar); App minWidth 1000.
- chore(ios): add .iOS("26.0") to Package platforms — groundwork for the duneii-ios app. Engine frameworks verified AppKit-free (only CoreGraphics/SpriteKit/AVFoundation/ImageIO), so they're iOS-portable; macOS suite stays green (608). iOS build verification happens in Xcode (this env's sandbox blocks xcodebuild for iOS destinations).
- refactor(client): extract DuneIIClient shared library — move GameModel/AssetStore/GameScene/Viewport/SpriteSource/Panels/Sidebar/Minimap/ScenarioPicker/Settings/audio glue out of the macOS app into a cross-platform target both clients share. GameScene rendering is cross-platform (SKColor); its NSEvent/NSTrackingArea/NSCursor input is now behind #if os(macOS) with shared tile(atScenePoint:)/tile(fromView:)/worldPoint(fromView:) helpers for the iOS gesture layer. Public seams: GameSidebar takes injected onSave/onLoad (NSSavePanel↔UIDocumentPicker). duneii is now a thin AppKit shell (App/ContentView + gutted ToolWindows frame-autosave). macOS suite green (608), 0 warnings.
- fix(client): sidebar polish — (1) full-screen → black background + forced-dark (white) text, windowed → system window background + adaptive text (GameSidebar gains a fullScreen flag; ContentView tracks NSWindow enter/exit-fullscreen); (2) build/order list buttons stretched to full width, leading-aligned; (3) bring back Debug controls as a ladybug toolbar-button popover (DebugPanel made public).
- fix(client): build-list structure icons — render the *built* state for every structure by selecting group tiles at OpenDUNE's 2*layoutSize offset (Structure_UpdateMap), via the structure's actual StructureLayoutInfo footprint (covers the special 1×1 groups: walls=6, concrete=8, turrets=23/24, not just 11–26). Fixes turrets now showing their gun, walls intact (not rubble), concrete clean (no placement-grid lines). Cycling-placeholder indices (223 windtrap/239 repair/255 selection, magenta in IBM.PAL) remapped to black. SpriteThumbnail now fills a square frame (scaledToFill + clip).
- fix(client): unit build/order icons now composite the turret — units with a turretSpriteID (combat/siege/devastator tanks, launcher, sonic, deviator) draw body (groundSpriteID, north) + turret (turretSpriteID, north) on top at the orientation-0 pixel offset (UnitSprites.turretOffset / viewport.c: siege -5y, devastator -4y, launcher/deviator -3y, sonic -2y, default 0), bounding-boxed so the gun isn't clipped. Turretless units (quad/trike/harvester/infantry/air) unchanged.
