# History — 2026-06-07

- Add full-sight-while-moving toggle: GameState.fullSightMovementReveal (engine default off = OpenDUNE-faithful radius-1 trail; golden-neutral) makes unitUpdateMap(1) re-stamp a moving player unit's full fogUncoverRadius every tile so the sight disc tracks it (the original look). Client toggle (GameModel, default on; debug-panel 'Full sight while moving'). MovementFogRevealTests (5). NOTE: completes commit 943d105, which committed the GameModel side of this flag without the GameState definition (a git-add race during concurrent editing left that intermediate commit non-compiling); this commit restores a consistent, building tip.
- Double-click select-same-type now only groups units within 3 tiles (Chebyshev) of the clicked unit (GameModel.doubleClickSelectSameType), so it selects the local cluster instead of every same-type unit across the whole map.
- Fix the Concrete 2x2 build-list icon: Concrete (1x1) and Concrete4 (2x2) share icon group 8, so the generic per-state offset (2*perState=8, needs >=12 tiles) overran the group and the 2x2 fell back to tileIDs[0] (the gridded foundation frame = black lines). SpriteImageProvider.structureImage now special-cases slabs: tile the clean built concrete tile (group offset 2) across the w*h footprint (correct 2x2; unchanged 1x1).
- Building production-progress bar is now blue and drawn at the bottom edge of the building footprint (was white, just under the top health bar): GameScene.updateHealth uses the structure footprint height to place it, placeBuildBar uses .systemBlue. Presentation-only (SpriteKit overlay) — manual verification.
- README: add links to OpenDUNE, dunepak (PAK packer/unpacker, asset-container reference) and SwiftOPL3 (LGPL-2.1 FM-music backend); add a License section — the project is a faithful port of OpenDUNE (GPL-2.0) so it is itself GPL-2.0; game data excluded (rights holders').
- Add GPL-2.0 LICENSE file (verbatim GPL v2, matching the OpenDUNE upstream this project ports) and reference it from the README; add an 'Other Dune II reimplementations' section to the README (OpenDUNE, Dune Dynasty, Dune Legacy, Dune II — The Maker, Dune2JS).
- Fix: loading a saved game no longer stays paused. A save is written while the save/load UI pauses the game, so state.paused is baked in true; GameModel.finishLoad now starts a loaded game playing (userPaused=false) and syncs the fresh sim to the effective pause, instead of inheriting the saved flag. ClientTests/SaveLoadPauseTests.
- Mentat: describe all units/buildings. MentatView no longer hides topics by campaign level (it's a full reference now), and maps the per-house name spellings (Wind Trap/Windtrap, Deviator/Ordos Deviator) so the sprite+stats panel populates for all. ClientTests/MentatCoverageTests asserts every unit/building has a non-empty description in all three house files.
- Fix: Atreides palace Fremen now actually spawn. The Fremen house (3) isn't a scenario house (unitCountMax 0), so Unit_Allocate refused the foot soldiers; Simulation+Palace now brackets the Fremen unitCreate with validateStrictIfZero (the unit-cap bypass OpenDUNE uses). PalaceTests.fremenWithoutAllocatedFremenHouse covers the real (unallocated-house) condition the prior tests masked.
