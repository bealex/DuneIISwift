# History — 2026-05-30

- Combat-integration slice (step 2c) — **the bullet's impact: full 400-tick unit-vs-unit combat now matches OpenDUNE bit-for-bit.** Ported `Map_MakeExplosion` (`map.c:403`) + relocated `Unit_Damage` (`unit.c:1530`) onto the movement layer (`DuneIISimulation/UnitImpact.swift`, `extension UnitMovement`) — `Unit_Move` triggers a bullet's explosion which damages nearby units, a genuine Move↔Combat cycle, so both live where `Unit_Move` can reach them with no construction cycle (`UnitCombat.damage` delegates). `Map_MakeExplosion`: the distance-scaled `Unit_Damage` to every unit in the reaction radius (`hitpoints >> (distance>>2)`), plus the non-allied-survivor retaliation (team-staging→HUNT, harvester-flee, guard-by-scenario→HUNT, else `Unit_SetTarget` toward the firer); structure damage / wall destruction / `Explosion_Start` are seams. Wired the three `Unit_Move` bullet-impact points (mid-flight wall/building/mountain hit; arrival detonation; the now-unblocked `degrades` self-`Unit_Damage(1)`). **`attack-close` regated `compared: 67 → 0` (FULL match):** the attacker fires at tick 61 → bullet flies → impacts the defender at tick 67 (200→188 hp, GUARD→HUNT) → the defender retaliates and hits the attacker back (200→188) — all 401 frames bit-identical to the oracle. (No RNG-spread divergence: the combatants are stationary, so they never draw the render-only `wobble` that desyncs `guard` — see `sim-rng-stream-unpinned-wobble`.) `UnitCombatTests` (+1: mapMakeExplosion radius/scaling/retaliation). Doc `Algorithms/UnitFire.md` updated. 239 tests green, clean build zero warnings.
- Combat-integration slice — **wired `Script_Unit_RemoveFog` (`0x28`) live** (`UnitScriptFunctions.removeFog` → `state.unitRemoveFog`, the long-standing fog SEAM). Held off earlier because revealing an enemy shifts `Unit_FindBestTarget`'s `seenByHouses` visibility mid-combat and diverged the attack goldens before combat was ported; now that `attack-close` matches the oracle for the full 400 ticks, flipping fog on keeps **all four scenario goldens green** — fog + combat-AI visibility finally come up together. No code beyond the one-native flip. 239 tests green, clean build zero warnings.
- Combat-integration slice — **second combat golden `attack-rocket` (Launcher duel), exercising the missile / `notAccurate`-scatter spawn path.** Two stationary Launchers (turreted, fireDistance 9) five tiles apart fire rockets at each other. New `attack-rocket.ini` + oracle golden (`Scripts/gen-scenario-goldens.sh`, scenario 95, 401 frames). Matches the oracle through tick 68: setup → aim → fire → the `Rocket` (type 19, `notAccurate`) spawning bit-identical, its scatter draws aligned. **Strong confirmation of `sim-rng-stream-unpinned-wobble`:** with *both* units stationary (no `wobble`), the `random256` stream stays aligned and even both GUARD units' `IdleAction` twitches match byte-for-byte — the desync `guard` hit comes specifically from its *moving* trike's render-only wobble. Gated `compared: 69`: it diverges at the **rocket unit's own in-flight EMC script** (the projectile draws `Tools_Random_256` at tick 69, ctx `u12`) — the rocket-script natives are the next gap, separate from the firing path. 239 tests green (5 scenario specs), clean build zero warnings.
- Tooling — **`Scripts/check.sh`** (the per-round verification: build + tests → a concise BUILD/TESTS/VERDICT summary that names the specific warnings, failing tests, and scenario divergences; `--full` for the clean-build zero-warnings audit, `--filter <Suite>` for the inner loop) and **`Scripts/build-oracle.sh`** (rebuild + ad-hoc re-sign the OpenDUNE oracle, wrapping the `.shim` + stale-signature gotchas). Both wrap this repo's env quirks (repo-local `TMPDIR`, `xcrun`, `--disable-sandbox`). CLAUDE.md "Running things" now points at `Scripts/` as the per-round entry point with a standing instruction to fold any new repeating action into them; CurrentState pointer added.
- `scenariolab` — added a **Speed control (1×–10×)** to the toolbar: a `speed` picker (`ContentView`) bound to `ScenarioLabModel.speed`, which drives `ScenarioScene.ticksPerFrame`. The scene's `update(_:)` now runs `tick()` that many times per rendered frame (rendering once afterwards), fast-forwarding the simulation without redrawing each tick — useful for watching long combat/movement scenarios play out. App-only/visual (no sim-logic change); `scenariolab` builds clean, full suite still 239 green.
- Combat — **fixed a missile-homing bug in `GameLoop_Unit`.** A flying winger non-normal-unit (a missile) re-aims each tick while `fireDelay != 0`; OpenDUNE (`unit.c:203`) re-aims at `currentDestination` *unless* it is chasing an **aircraft** (a winger-type `targetAttack`), in which case it tracks the moving target. Ours re-aimed at *any* unit `targetAttack`, so a rocket fired at a ground unit flew straight at the target (orient 64) instead of toward its scattered `currentDestination`. Now gated on the target being a winger (`Simulation.swift`). Dispatch-traced the Rocket (type 19) script first to rule it out — it just `Delay(1200)`s (no unported native), so this was a movement-loop bug, not a script gap. Cuts `attack-rocket`'s tick-69 divergence from 7 orientation units (64 vs 57) to **1** (58 vs 57); the residual is a sub-tile difference in `Tile_MoveByRandom`'s `center: false` scatter (the path `getRandomTile`'s golden never exercised) — flagged for a focused golden, not chased (stochastic scatter under the parity bar). `attack-rocket` stays `compared: 69`. 239 tests green, clean build zero warnings.
- Correction to the previous entry — the `attack-rocket` tick-69 residual is **not** a `Tile_MoveByRandom(center:false)` bug (that math checks out). Back-computing the scatter shows our `newDistance` resolves to `0x18` = the byte the oracle draws ~3 positions later, i.e. our `Script_Unit_Fire`/`Unit_CreateBullet` **missile** path makes ~3 extra `Tools_Random_256` draws the oracle doesn't. The fix is to find + remove those draws (diff our random256 sequence vs `--parity-random-trace` around the tick-66 fire), not to golden `moveByRandom`. Re-flagged in CurrentState.
- Closing the `attack-rocket` tick-69 residual investigation — **it is stochastic spread, not a bug** (correcting the two prior entries' "extra draws in the missile path" lead, which had the direction wrong). Instrumented our `random256` draw stream: over 70 ticks our engine draws only `13 c2 f6 55` vs the oracle's 13 bytes — *fewer*, not more. The defender launcher (`u23`, a GUARD unit) reaches `Script_Unit_IdleAction` on a slightly different cadence, so its idle-twitch RNG-draw count diverges; the twitches rotate the *turret* (level 1), so the body-orient snapshots still match through tick 68, but the diverged stream makes `u22`'s `notAccurate` rocket scatter consume different bytes → the 1-unit sub-tile `currentDestination`. Same class as `sim-rng-stream-unpinned-wobble` (idle/wobble RNG we don't byte-chase). `attack-rocket` stays `compared: 69`; not chased further. The missile-homing fix it surfaced stands. 239 tests green.
- Structures subsystem — first primitive: **`Structure_UntargetMe`** (`structure.c`) in `GameState+Lifecycle.swift`. Mirrors the ported `Unit_UntargetMe`: clears the structure's own two-way script-var-4 link, then zeroes any unit `targetMove`/`targetAttack`/`variables[4]` and any team `target` pointing at it (minus the turret-`variables[2]` + team-membership bits, which are unit-only). Needed by `Structure_Damage` (the `Map_MakeExplosion` structure SEAM). `GameStateLifecycleTests` (+1: scrubs unit targets + var4 link + team target). 240 tests green, clean build zero warnings.
- Structures subsystem — **`Structure_Remove`** (`structure.c:1305`, the map-clear + free teardown, analog of `Unit_Remove`) in `GameState+Lifecycle.swift`: clears `hasStructure` occupancy on each of the structure's layout tiles, frees the slot (`Structure_Free`, already ported), and scrubs references (`Structure_UntargetMe`). The destruction animation (`Animation_Start`/`Animation_Stop_ByTile`) and the House AI rebuild queue (`ai_structureRebuild`) are seams (render / AI, no headless consumer). `GameStateLifecycleTests` (+1: tile occupancy cleared, unit refs scrubbed, slot freed + dropped from the find array). 241 tests green, clean build zero warnings.
- Structures subsystem — **`Structure_Damage` + `Structure_Destroy`, wired into `Map_MakeExplosion`** (so a bullet/explosion now damages a building). `GameState.structureDamage` (`structure.c:1037`): HP drain; on 0 HP → `Structure_Destroy` + `Structure_UntargetMe` (returns destroyed); the score tally + sound + the survivor `range != 0` `Map_MakeExplosion(IMPACT_LARGE)` are seams. `GameState.structureDestroy` (`structure.c:979`): mark destroyed (`variables[0]=1`, un-`allocated`), reset script, remove linked units (or destroy a construction-yard's linked structure), apply the House credit penalty (storage-share loss + enemy build-cost refund) + windtrap decrement. **Seam:** the `Script_Load` of the structure death script (needs `BUILD.EMC` + the structure `ScriptInfo`, a Simulation concern) — so a destroyed structure stays unallocated-but-unremoved here until the script layer drives `structureRemove`. `UnitImpact.mapMakeExplosion`'s structure SEAM now calls `structureDamage`. `GameStateLifecycleTests` (+3: non-lethal drain / no-op guards, lethal destroy + enemy refund + ref-scrub, windtrap decrement). 244 tests green, clean build zero warnings.
- Tooling — `Scripts/log-history.sh`: append a dated bullet to `History/YYYY-MM.md` (workflow step 6), creating the month file with its header when new; flattens to one line, strips a pre-formatted `-`/date prefix, takes text as args or stdin, `--date` override. Wired into CLAUDE.md "Running things".
- docs: handoff refresh — CurrentState corrected for restart (branch `main` not `phase2-world-stat-tables`; test count 239→244; structures-subsystem + missile-homing + tooling added to Recently completed; self-review flagged OVERDUE at 93 commits).
- Periodic self-review (overdue, 94 commits since selfreview-phase1): reread History/2026-05.md + Insights; extracted one recurring lesson — RNG-stream-desync misdiagnosis (attack-rocket tick-69 cost three wrong diagnoses) — into a standing CLAUDE.md "What not to do" rule (align the RNG draw stream via --parity-random-trace before calling a scenario-golden divergence a bug; gate at the deterministic horizon). Re-tagged baseline selfreview-phase3, reset the count; CurrentState tracker updated.
- Structures subsystem — the structure script system + death-path closure (the big next foundation). Bridged BUILD.EMC → a structure ScriptInfo and ran structure EMC scripts via a new GameLoop_Structure (Simulation.swift): StructureTickCursors (degrade/structure/script/palace; only the script path is live, the rest seams), a StructureScriptRunner (op-14 dispatch over g_scriptFunctionsStructure, fixed 3-opcode batch, the 1.07 'i!=3 aborts remaining structures' quirk), and StructureScriptFunctions (SetState/GetState/RemoveFogAroundTile/the two var-4 scrubs/Explode/Destroy; FindUnitByType/turret/Fire/RefineSpice deferred → clean-halt). Key faithfulness point: the death-script Script_Load lives in GameLoop_Structure's not-loaded else-branch (Structure_Destroy only Script_Resets at the World layer), so a building damaged to 0 HP reloads its death branch (Explode→Delay→Destroy→Structure_Remove) — confirmed by decoding BUILD.EMC's shared @0 subroutine. Added GameState.structureRemoveFog (Structure_RemoveFog) + StructureTickCursors. Tests StructureScriptTests (3: healthy idle, full death-path removal, SetState DETECT resolution). Doc Algorithms/StructureScript.md. 247 tests, clean build zero warnings.
- Structures subsystem — turret-firing natives (Script_Structure_FindTargetUnit 0x08 / RotateTurret 0x09 / GetDirection 0x0A / Fire 0x0B) in StructureScriptFunctions, wired into StructureScriptRunner dispatch. FindTargetUnit is 1.07-faithful (distanceCurrent never updates → returns the LAST in-range seen non-allied unit, not the closest; the original swapped-assignment no-op). RotateTurret steps the turret groundTileID toward the target via the ICM base-turret sprite (group 23/24 through the iconMap). Fire reuses the ported Unit_CreateBullet (rocket turret ≥0x300 → missile/launcher delay, else bullet/tank delay), stamping originEncoded. A gun turret now acquires → aims → fires at a seen enemy end-to-end via GameLoop_Structure. StructureScriptTests (+4: FindTargetUnit last-pick/ignores, GetDirection, Fire spawn, the full turret-fires integration over ticks). Doc Algorithms/StructureScript.md (turret-firing section). 251 tests, clean build zero warnings.
- Structures subsystem — Script_Structure_RefineSpice (0x15): a refinery converts a linked harvester's spice into the owner House's credits. harvesterStep = (hitpoints*256/maxHitpoints)*3/256 per call (a damaged refinery refines slower), clamped to the harvester's remaining amount; 7 credits/unit (enemy refineries get a ±RNG bonus); script.delay=6 throttle; an emptied harvester drops inTransport; no linked unit → SetState(IDLE). The g_scenario allied/enemy harvested tally is a SEAM (scenario score). Ported in StructureScriptFunctions + StructureScriptRunner dispatch. StructureScriptTests (+3: player exact credits/amount/delay, empties→inTransport cleared, enemy RNG-bonus bounds). Doc Algorithms/StructureScript.md. Next deploy slice (FindUnitByType 0x03 / unit-unload 0x07 / Unit_EnterStructure) needs Structure_FindFreePosition + Unit_SetPosition (+ Unit_CallUnitByType). 254 tests, clean build zero warnings.
- Structures subsystem — deploy cluster: Unit_SetPosition + Structure_FindFreePosition + the unit-unload native (Script_Structure_Unknown0C5A, 0x07). Unit_SetPosition (UnitCombat.unitSetPosition, unit.c:1107) places an off-map unit (centred) at a tile, failing+re-flagging off-map if occupied, else clears destination/targets, refreshes visibility on an unveiled tile, sets the default action (AI/harvester/saboteur→actionAI else player[3]), and stamps the map. Structure_FindFreePosition (StructureScriptFunctions, structure.c:1101) picks a free non-wall/mountain/occupied tile from the ring around a structure (random start; spice-nearest for a harvester). unloadLinkedUnit (0x07) deploys the structure's linked unit — a winger lifts off the structure's own tile, a ground unit deploys to a free adjacent tile — then unlinks it, shifts the next queued unit in (or SetState IDLE), clears var-4. Wired into StructureScriptRunner dispatch. StructureScriptTests (+3: unitSetPosition place/occupied, findFreePosition ring/full, unload deploy+unlink). Doc Algorithms/StructureScript.md. Next: FindUnitByType (0x03, needs Unit_CallUnitByType) + Unit_EnterStructure to close the harvester loop. 257 tests, clean build zero warnings.
- Structures subsystem — Unit_EnterStructure (unit.c:2177) + Unit_Hide + Structure_GetStructuresBuilt (all GameState/World), closing the harvester economy loop. A unit arriving inside a structure: allied (harvester→refinery / unit→repair) → structure goes READY/BUSY (busyStateIsIncoming), a repair pad heals+times the unit, and the unit links into the structure's chain and stays hidden (Unit_Hide: off-map, script reset, untargeted, dropped from visibility counts); saboteur → Structure_Damage(500)+remove; enemy unit → capture a low-HP enemy structure (houseID swap + Structure_GetStructuresBuilt rebuild) else damage it. Seams: g_unitSelected deselect (render) + House_CalculatePowerAndCredit (House subsystem); 1.07-enhanced takeover untarget/unveil off. **Wired the Unit_Move SEAM** (UnitMovement.swift) so a unit arriving on a structure tile now enters it. StructureScriptTests (+4: enter-refinery link/hide, saboteur detonate, dead-structure remove, and the harvester→refinery→credits integration over GameLoop_Structure ticks). 261 tests, clean build zero warnings.
- Structures subsystem — Script_Structure_FindUnitByType (0x03) + Unit_CallUnitByType: the last BUILD.EMC native. Unit_CallUnitByType (UnitCombat, unit.c:2017) finds the last idle (linkedID 0xFF, not moving) unit of a type/house and orders it to a target (var-4 set); with createCarryall + none free + type==CARRYALL it spawns a fresh off-map scenario carryall (validateStrictIfZero bypass). FindUnitByType (StructureScriptFunctions, structure.c:195) summons a carryall to collect a READY structure's linked unit — a player harvester with no last-target + a free deploy spot is left to walk out (returns 0), else links the carryall via the structure's var-4. Wired into StructureScriptRunner dispatch. **All natives BUILD.EMC actually calls are now ported** — a structure runs its full script with no clean-halts; the refinery cycle (enter→refine→summon carryall/walk out→unload) runs end to end. StructureScriptTests (+3: summon idle carryall, player-harvester walk-out, create-carryall). Remaining structure work = the BUILD/REPAIR/factory production state machine + House_CalculatePowerAndCredit (House subsystem) + the carryall flight AI + the oracle [STRUCTURES] golden. 264 tests, clean build zero warnings.
- Parity harness — structure + house golden (oracle Scen_DumpState) + a structure scenario, and FOUND+FIXED a real bug. Extended the oracle scenario mode (parity.c): Scen_Init loads BUILD.EMC; new Scen_LoadStructure (Structure_Create placement, mirroring Scenario_Load_Structure); Scen_DumpUnits→Scen_DumpState dumps {tick,houses,structures,units} (reusing Parity_DumpHouses/DumpStructures); Parity_RunScenario parses [STRUCTURES]. New attack-structure.ini (a Harkonnen tank attacks an Ordos windtrap) + gen-scenario-goldens.sh entry; rebuilt+re-signed the oracle, regenerated all 6 goldens in the new format. ScenarioGoldenTests now compares structures (hp/state/linkedID) per tick. **The new golden immediately caught a real bug:** structures store the tile CORNER, not the centred sub-tile (Structure_Place: position &= 0xFF00) — our loadStructure used the centred Tile32.unpack, mispositioning every structure 128px and shifting unit-vs-structure aim (the tank fired ~10 ticks late). Fixed in ScenarioLoader.loadStructure; attack-structure now matches the oracle the FULL 400 ticks (units + structures, inc. the bullet-impact Structure_Damage 200→175). Insight world-structure-corner-position. Investigation of the better harness (Tier-2a structure decision-trace — oracle hooks already exist; Tier-3i event-trace) recorded in ScenarioHarness.md/ParityHarness.md. 264 tests, clean build zero warnings.
- Tooling — extracted the recurring investigation commands into Scripts/, and refined the golden generator. New: odfn.sh (print an OpenDUNE C function body + file:line — the single most re-typed command when porting), emc.sh (disassemble an EMC script: one type N / the shared region via --linear / all), golden.sh (a scenario golden JSONL as a per-tick 'what changed' timeline — replaces the python one-liners). Added a permanent --linear whole-program mode to assetgen emc-disasm (refactored Emc.disassemble into a from/to range overload) so the shared subroutines below the lowest type entry — a structure's death/turret/refine common code, reached only via Jump — are disassemblable (previously needed a temp tool hack). Refined gen-scenario-goldens.sh: DRY'd the oracle build through build-oracle.sh + added --only <name> (regenerate one scenario instead of all 6). CLAUDE.md 'Running things' lists the new helpers. 264 tests, clean build zero warnings.
- House subsystem — started: House_CalculatePowerAndCredit (house.c:470, GameState+Lifecycle). Recomputes a house's powerUsage/powerProduction/creditsStorage by summing over its structures — consumers add powerUsage, power plants produce -powerUsage scaled by hitpoints (1.07: ≤ half HP → half output, else -powerUsage*hp/maxHP), storage adds creditsStorage. Player low-power GUI warning + the g_playerCreditsNoSilo reset are seams. Closed the two Unit_EnterStructure takeover SEAMs (the captured/losing houses now recompute power+credit). HouseEconomyTests (3: sum producers/consumers/storage + other-house exclusion, damaged-plant proportional output, idempotent recompute). 267 tests, clean build zero warnings.
- House subsystem — GameLoop_House core (house.c:51): the House phase is now live (was an order-preserving stub). Added HouseTickCursors (6 cadences) + GameState.playerCreditsNoSilo; Simulation.gameLoopHouse advances the cursors and runs the core economy — tickHouse (every 900: clamp credits to creditsStorage, the player keeping a no-silo allowance; House_CalculatePowerAndCredit recompute; decrement timerUnit/Sandworm/StructureAttack) and tickPowerMaintenance (every 10800: deduct powerUsage/32+1 from credits). Seams: starport frigate delivery, scenario reinforcements, the house missile, starport stock, House_EnsureHarvesterAvailable, Structure_CalculateHitpointsMax, harvestersIncoming spawns, all GUI/sound. No effect on the scenario goldens (houses aren't allocated there → houseFind is empty → no-op, matching the oracle). HouseLoopTests (4: clamp+recompute, power maintenance, attack-timer decrement, paused freezes the economy). 271 tests, clean build zero warnings.
- Test apps — surfaced the structure + economy features across the apps. DuneIIScenarios/scenariolab: ScenarioBuilder + runner now take BUILD.EMC (structureScript) so structures run their scripts; added two scenario kinds — attackStructure (a tank destroys a pre-weakened windtrap → the death branch Explode→Destroy→Remove + soldiers runs live) and turretDefense (a gun-turret script acquires→aims→fires at an approaching enemy); fixed placeStructure to corner-position structures (insight world-structure-corner-position); ScenarioBuilderTests +2 (building damaged/destroyed, turret fires). mapview: loads UNIT.EMC + BUILD.EMC + allocates houses + Scen-style unit prepare + viewport, so the scenario runs LIVE; MapScene now rebuilds unit sprites each tick (units/bullets/soldiers follow the sim) and re-blits on structure changes (structureRemove now sets mapDirty). duneii-headless: replaced the stub with a real deterministic headless demo driver (loads scripts from Resources, runs attackStructure + turretDefense via ScenarioBuilder, prints the events — e.g. building destroyed+removed at tick 106). rendertest: asset-only, no change. 273 tests, clean build zero warnings.
- docs: session-reset handoff — rewrote CurrentState.md's stale Active task (it still described Phase-2-era stat-table work) into a concise current frontier (Phase 3 late-stage: structures + house economy done; remaining = production state machine / teams-AI / carryall AI; the verification frontier = Tier-2a structure decision-trace), preserved the env-quirk + Scripts notes, trimmed 32 foundational Phase-0/1/2 entries from Recently completed (left a History breadcrumb), refreshed the queue; 160→87 lines. Updated SimulationLoop.md (Structure + House phases are live, only Team is a stub). 273 tests, all green.
- world+sim: Structure_CalculateHitpointsMax — power ratio rescales each structure's max HP (floored at half), bleeds 1 HP when the cap drops below current HP; wired into tickHouse (GameState+Lifecycle.swift, Simulation.gameLoopHouse), HouseEconomyTests +3 (276).
- world+sim: structureTickStructure — the GameLoop_Structure tickStructure body goes live (structure-repair + factory production progress/completion + onHold + repair-pad unit-repair countdown); restructured gameLoopStructure to run it on the structure cursor + the faithful slab/wall skip; added GameState.campaignID (default 1). Upgrade branch + AI maintenance/auto-place remain seams. StructureProductionTests +10 (286).
- world: structureTickStructure upgrade branch + Structure_IsUpgradable (structure.c:1102) — upgrade progress (buildCredits/40/tick, upgradeTimeLeft step, level bump, Ordos Heavy-Vehicle 1→3 free, campaign-gated re-arm). Closes the tickStructure body (AI maintenance/auto-place + GUI remain seams). StructureProductionTests +7 (293).
- insight: build-gamestate-layout-stale-module — adding a GameState stored property tripped a stale-swiftmodule false failure in an unrelated test (SearchSpice); clean-build before trusting the red.
- world+sim: GameLoop_Team goes live — the team script subsystem bring-up (team.c:22). TeamScriptRunner over a bridged TEAM.EMC + the g_scriptFunctionsTeam dispatch (getters + delays + no-ops live; the recruit/target/order brain natives clean-halt as seams); single random-period cursor GameState.teamLoopTick (5-12 ticks), isAIActive gate, per-team delay, the 1.07 abort-rest quirk. New TeamScriptFunctions + Simulation.teamScriptInfo. TeamLoopTests +5 (298). Doc Algorithms/TeamScript.md.
- apps: surface the tickStructure economy in the test apps — 3 new ScenarioKinds (factoryProduce/repairBuilding/upgradeBuilding) exercise the build/repair/upgrade state machine live; the builder sets the player no-silo allowance + settles the structure (suspends the placement-animation script so it doesn't clobber the production state). duneii-headless prints each demo's economy progression; scenariolab lists them via allCases. ScenarioBuilderTests +3 (301).
- world+sim: team brain natives — recruit + target. Script_Team_AddClosestUnit (0x03), GetAverageDistance (0x04), FindBestTarget (0x06) in TeamScriptFunctions (script/team.c); new World primitives Unit_AddToTeam + Tile_GetTileInDirectionOf + Map_IsValidPosition (GameState+Lifecycle). TeamScriptRunner now carries the UnitScriptRunner so FindBestTarget reuses its TargetFinder. TeamScriptFunctionsTests +7. 308 tests. Order-issuing natives + Load are the next slice.
- parity: house-economy golden — a new [HOUSES] scenario section activates a house (Scen_LoadHouse/House_Allocate in parity.c; loadScenario+houseAllocate on our side) + both House_CalculatePowerAndCredit after load. economy.ini (Ordos windtrap+silo) gives a full 60-tick house-aggregate match (credits 2000→clamp 1000→power-maint 999, power 100/5, storage 1000) — validates House_CalculatePowerAndCredit + the credit clamp + power upkeep cross-engine. ScenarioGoldenTests compares houses per tick (HouseGolden); 7 scenarios. Oracle rebuilt+re-signed. 308 tests.
- parity: Tier-2a structure decision-trace harness. Wired --parity-script-trace into the oracle scenario mode (parity.c); our StructureScriptRunner emits the identical per-opcode line (pc/op/param/delay/SP/FP/return/current) via an injected StructureScriptTracer + ScriptTraceLine.decode (DuneIISimulation/Script/ScriptTrace.swift). StructureTraceTests diffs line-by-line — attack-structure windtrap (index 0) matches the oracle's 24-opcode trace exactly (placement-anim subroutine FP flex + Delay settle). Proves opcode-identical EMC execution. Fixture attack-structure-struct0-trace.txt + gen entry. 309 tests.
- scenariolab: show unit-death/building-destruction (explosion burst + lingering scorch in ScenarioScene.detectDeaths/spawnDeathMarker) and a 'scenario finished' banner that auto-pauses, driven by new ScenarioWorld.outcome() per-kind endpoints (DuneIIScenarios/ScenarioOutcome.swift). +4 ScenarioOutcomeTests; 313 tests.
- Explosion subsystem (DuneIIWorld Explosion.swift + GameState+Explosion.swift, faithful port of src/explosion.c): the 20-entry command-table VM + 32-slot pool + explosionStart/explosionTick; wired into mapMakeExplosion (UnitImpact.swift, RNG-free → golden-neutral) and gated per-tick via Simulation(tickExplosions:) — off for goldens (matches the oracle harness), on for scenariolab. scenariolab now renders real impact/death/building-destruction explosion animations + smoke on .isSmoking units (ScenarioImageBuilder.effectSprites), replacing the synthetic markers; finished banner kept. +9 tests (ExplosionTests, ScenarioExplosionTests); 320 total.
- Team subsystem — the order-issuing + script-switch brain natives complete the g_scriptFunctionsTeam table: Unknown0543 (0x05, move strayed members to a random tile near the centroid else Guard), Unknown0788 (0x07, order members to Attack the team target at a random firing position), Load/Load2 (0x08/0x09, switch the team action script via interpreter.load on the live engine). moveOrGuardMembers/issueAttackOrders take the unit-action layer (UnitActions/UnitScriptFunctions) explicitly; wired into TeamScriptRunner.dispatch. +5 TeamScriptFunctionsTests; 325 tests. Next: team creation (scenario [TEAMS] + Team_Create/Add).
- scenariolab fixes (reported): units now die — ported the unit DIE-branch natives Script_Unit_ExplosionSingle (0x0E) + Script_Unit_Die (0x0F, Unit_Remove + resets the running engine + saboteur blast) and made Script_Unit_DisplayDestroyedText (0x27) a GUI no-op (it was clean-halting the death branch before Die, so 0-HP units sat in ACTION_DIE forever); fixed damaged-unit smoke to OpenDUNE's draw (frame 180+(spriteOffset&3) with 183→181, 14px above the unit). UnitImpact.die/explosionSingle; ScenarioImageBuilder.effectSprites. +4 tests (UnitCombatTests death natives + ScenarioExplosionTests unit-death end-to-end); 329. Goldens unaffected (death natives fire only on death, outside the compared window).
- Troopers: walk animation + death (reported). Ported tickUnknown5 (Simulation.gameLoopUnit, unit.c:239 — advance spriteOffset for moving foot units / smoke / ornithopter / harvester, throttled by animationSpeed/5; RNG-free) so infantry walk-cycle + smoke animate; ported the unit DIE-branch natives the trooper path needs — Script_Unit_StartAnimation (0x04, UnitScriptFunctions; state effects + return 1, the overlay corpse is a render SEAM) + General_VoicePlay (0x26) as an audio no-op — so troopers reach Unit_Die and are removed. New 'trooper' scenario golden: a foot trooper walks 400 ticks, full match vs the oracle incl. the new spriteOffset dump field (added to parity.c Scen_DumpState + UnitState) — verifying the walk animation tick-for-tick; all goldens now also compare spriteOffset (tanks=0). +6 tests (TrooperTests ×2, trooper golden, fixed 2 stale UnitScriptRunnerTests). 331 tests; oracle rebuilt + goldens regenerated.
- Verified movement start-speed accounts for terrain: Unit_StartMovement (our UnitMovement.startMovement) is a faithful port reading the landscape of the tile being ENTERED (Tile_MoveByOrientation → landscapeType → LandscapeInfo.movementSpeed[movementType], minus the half-HP 25% penalty) — so terrain is taken into account at the start of every step (incl. the first), not only mid-move. New MovementTerrainTests (+2) pin it: a swap test proves the entered tile (not the current tile) drives the start speed (wheeled sand 160 > rock 64); the moving/move-trike/trooper goldens already confirm it end-to-end over real terrain. scenariolab: after detecting a finished scenario, keep ticking a 5s grace period (ScenarioScene.finishedAt/finishGraceSeconds) so death/destruction explosions + animations play out, then auto-pause. 333 tests.
- Team creation: ported Team_Create (GameState.teamCreate, team.c:75 — allocate + set house/action(=actionStart)/movementType/min-max + Script_Reset/Load to a passed-in scriptPC + delay=0; World-layer, the caller resolves offsets[teamActionType] since the script program is Simulation-side) + the [TEAMS] scenario section (ScenarioLoader.loadTeam, via a new teamScriptOffsets param) + the MovementType.named/TeamActionType.named string parsers (g_table_movementTypeName/teamActionName; INI 'Winged'→.winger). The whole g_scriptFunctionsTeam table + team creation are now ported. +4 TeamCreateTests; 337 tests.
- Structure production triggers: ported Structure_SetRepairingState (structure.c:1735) + Structure_SetUpgradingState (structure.c:1691) — start/stop/toggle self-repair / upgrade by flipping .repairing/.upgrading/.onHold (the tickStructure branches act on these); Widget/GUI text are seams, g_dune2_enhanced pinned false. Refactored the repairBuilding/upgradeBuilding ScenarioKinds to call the real setters instead of inserting flags by hand. +8 StructureStateTests; 345 tests.
- Harvester loop natives: ported Script_Unit_Harvest (0x2A, unit.c:1652 — gather random 0/1 spice on a spice tile, set inTransport, ~1/32 deplete the tile; 2 Random256 draws), Script_General_SearchSpice (0x29, general.c:325 — nearest harvestable spice within radius via Map_SearchSpice → encoded tile), Script_Unit_GoToClosestStructure (0x33, unit.c:1798 — Move to the nearest idle/unlinked/un-busy structure of a type, e.g. an empty refinery). harvest/searchSpice on UnitMovement (has map), goToClosestStructure on UnitScriptFunctions (takes the action layer). +6 HarvesterTests; 351 tests.
- Common unit-script natives: ported MoveToTarget (0x16, unit.c:427 — fine homing onto targetMove: far = aim + distance/turn-scaled speed, close = ±16/axis step, arrived <32 returns 1 else re-runs via a script-PC rewind + delay), SetActionDefault (0x0A, switch to actionsPlayer[3]), SetSprite (0x13, spriteOffset = -(value&0xFF) — the death frames), Blink (0x0B, blinkCounter=32), and DisplayText (0x02) as a GUI no-op. moveToTarget on UnitMovement; the rest on UnitScriptFunctions. +7 UnitNativesTests; 358 tests.
- More unit-script natives: ExplosionMultiple (0x12, unit.c:553 — death-hand 8-blast: 1 at the unit + 7 random-offset, LCG dmg), FindIdle (0x18, general.c:400 — an idle structure of a type/the given encoded structure, for the house), GetTargetPriority (0x1D, unit.c:96 — TargetFinder.targetPriority over the encoded unit/structure), IsValidDestination (0x2F, unit.c:1694 — can the carrier head to a tile/structure, temp-relocating its passenger to test occupancy). explosionMultiple on UnitMovement, findIdle on UnitScriptFunctions, targetPriority on TargetFinder, isValidDestination on UnitCombat. Retargeted the UnitScriptRunnerTests 'unported native' marker 0x12→0x14 (porting the marker opcode had broken+crashed them). +4 UnitNativesTests; 362 tests.
- Carryall transport natives: MoveToStructure (0x1E, unit.c:1376 — link the carrier to + Move toward an idle/unlinked structure: the passenger's origin if still idle, else nearest of a type), Pickup (0x22, unit.c:225 — grab a unit from a READY structure or off the ground, routing it to its refinery/repair pad; harvester spice-field bookkeeping), TransportDeliver (0x14, unit.c:123 — drop cargo into a starport busy->ready / refinery+repair via Unit_EnterStructure / onto the ground via Unit_SetPosition). All on UnitCombat. Retargeted the UnitScriptRunnerTests unported-marker 0x14->0x37 (per insight). +5 CarryallTests; 367 tests.
- Sandworm + link-validity natives: Sandworm_GetBestTarget (0x36, unit.c:1886 → new TargetFinder.sandwormFindBestTarget + sandwormGetTargetPriority, unit.c:1020/987 — prey on revealed sand weighted by movementType/motion/distance), Unknown2BD5 (0x37, unit.c:1909 — validate the unit's var4 link, clear it if not mutual+same-house), General_Unknown0288 (0x3B, general.c:181 — is an encoded index a stale/empty reference). Retargeted the UnitScriptRunnerTests unported-marker 0x37->0x09 (MCVDeploy, blocked on Structure_Create). +3 SandwormNativesTests; 370 tests. Unit native table ~61/64 live (remaining: 09 MCVDeploy, 21 RandomSoldier, 23 CallUnitByType).
- Finished the last 3 unit-script SEAMs → the whole g_scriptFunctionsUnit table (64/64) is now live: RandomSoldier (0x21, unit.c:48 — spawnChance-gated SOLDIER spray near the unit), CallUnitByType (0x23, unit.c:1512 — summon a same-house carryall to a pickup-able unit, reusing Unit_CallUnitByType), and MCVDeploy (0x09, unit.c:1846) via a new structure-placement chain on UnitCombat: structureCreate (structure.c:373) + structurePlace (structure.c:442, general-building case; the BUILD.EMC script-load is deferred to GameLoop_Structure's auto-load) + structureIsValidBuildLocation (structure.c:734; footprint landscape isValidForStructure/2 + occupancy). Structure_BuildObject (GUI) + wall/slab placement specials + fog remain seams. Retargeted the UnitScriptRunnerTests unported-marker to 0x40 (past the 64-entry table → a stable non-native marker). +5 MCVDeployTests; 375 tests. All three EMC native tables (unit/team/structure) are now complete.
- sim: verify carryall winger flight end-to-end (Unit_Move/MovementTick/GameLoop_Unit winger branches + SetDestinationDirect/MoveToTarget natives) — +1 CarryallTests flight test.
- sim: wire the area-effect combat SEAMs in Unit_Move/Unit_Damage — sonic-blast area damage (Unit_Damage/Structure_Damage over the wave tile, sonicProtection immunity), the death-hand 17-point blast (offset tables → mapMakeExplosion), saboteur arrival detonation, the range!=0 IMPACT crater, and the sand-burst on empty sand; +6 AreaEffectCombatTests, goldens unchanged.
- sim: slice 4 — deviator gas + spice-bloom detonation. Map_DeviateArea (deviator-missile arrival → start gas explosion + Unit_Deviate every unit in radius 32) reusing the relocated Unit_Deviate (now on UnitMovement, UnitCombat delegates); Map_FillCircleWithSpice (new MapPrimitives primitive, radius-edge half-skip RNG); Map_Bloom_ExplodeSpice wired into Unit_Move's isSpiceBloom arrival (remove the unit, revert the tile via mapBaseTileID, fire SPICE_BLOOM_TREMOR, spread spice) + the now-unblocked harvester-death spice spill. Map_Bloom_ExplodeSpecial left unported (unreachable in 1.07 — isSpecialBloom never set). +5 MapEffectTests; goldens unchanged.
- sim: slice 5 — GameLoop_House tails. Ported Map_FindLocationTile (MapPrimitives; edge/air/visible/base spawn tiles, LCG-driven), Unit_CreateWrapper + House_EnsureHarvesterAvailable (UnitCombat), and wired the tickHouse playerCreditsNoSilo reset, EnsureHarvester + harvester-incoming spawn, the starport stock bump (tickStarportAvailability, one LCG draw), and the starport frigate delivery (tickStarport). Added GameState.starportAvailable[27] + minimapPosition. Reinforcements ([REINFORCEMENTS] data not loaded) + the palace house-missile (slice 7) remain documented seams. +7 GameLoopHouseTailsTests; clean --full build green (the GameState-layout stale-swiftmodule trap — insight build-gamestate-layout-stale-module); goldens unchanged.
- sim: slice 6 — Structure_BuildObject headless state-setup. Ported the concrete-objectType build path (UnitCombat.structureBuildObject): stop repair, cancel a differing build, create the product off-map (unit, or a structure for a construction yard), link it, set countDown=buildTime<<8, flip the factory BUSY — unblocks AI production triggers. Ported Structure_CancelBuild (free the queued product + proportional credit refund). The player factory-window GUI (0xFFFD/0xFFFE/0xFFFF sentinels, Structure_GetBuildable + available flags, starport stock, placement hints) is a documented Phase-6 seam. +6 BuildObjectTests; goldens unchanged.
- sim: slice 2 — end-to-end team AI exercised through the live loop. New TeamLoopTests case wires the bridged TEAM.EMC + teamScriptOffsets (Team_Create) + an isAIActive house + recruitable units into a Simulation and runs the four-phase loop: the 'normal' brain recruits (AddClosestUnit), targets the seen enemy (FindBestTarget), and orders an attack (Unknown0788), observed across the run. The cross-engine per-tick oracle golden is NOT added: the team AI is heavily stochastic (random loop cursor + the recruits' unpinned idle-wobble draws) and isAIActive only flips on a vision trigger, so a byte-exact golden would gate at a tiny deterministic horizon — chasing it conflicts with the 'don't chase RNG-order parity' rule. The oracle [TEAMS]/TEAM.EMC harness is scoped as future work. +1 test; 401 green; clean --full build.
- sim/parity: ROOT-CAUSED the guard/attack-rocket golden divergence — GameLoop_Team skipped its cursor re-arm (a Tools_Random_256 draw) when no TEAM.EMC was bridged, while OpenDUNE re-arms it unconditionally every fire. Made the cursor draw unconditional (gate only the team-script execution on the runner). Trace-aligned via the oracle --parity-random-trace (the ctx=NULL t1 0x13 draw was the team cursor, not setup/wobble). Both guard and attack-rocket are now FULL 400-tick matches (compared 6→0, 69→0). Corrected insight sim-rng-stream-unpinned-wobble + the CLAUDE.md 'what not to do' rule: a fully-seeded divergence is a real draw-count bug, not unpinnable spread. Updated TeamLoopTests (the team cursor now draws even without a script). 401 tests, clean --full build.
- sim/parity: added an RNG draw-stream golden — RngTraceSink (opt-in, Sendable-via-Mutex, zero-overhead-when-off) records every Random256/RandomLCG draw; gen-scenario-goldens.sh now also dumps the oracle's --parity-random-trace/--parity-lcg-trace as fixtures; ScenarioGoldenTests asserts our per-tick draw stream is byte-identical to the oracle's, reporting the first divergent draw (value + oracle tick/ctx/idx). It immediately caught a 2nd real bug: animationTick draws Random256 (.pause % 4) but the oracle's parity harness never ticks animations — economy/attack-structure made 6-12 extra draws. Fixed by gating animationTick behind a new Simulation.tickAnimations flag (default off for the golden/oracle path; scenariolab/mapview set it on), mirroring tickExplosions. +RngTraceSinkTests; updated the untick-visual-subsystems insight; 404 tests, clean --full build.
- sim/parity: cross-engine TEAM golden — closes Phase 3's last item. parity.c gains Scen_LoadTeam ([TEAMS] → Team_Create + flag the team's house isAIActive) + loads TEAM.EMC; our ScenarioLoader.loadTeam now also sets isAIActive. Added 'team' to the scenario unit dump (Scen_DumpState + our UnitState snapshot) so AI recruiting is directly visible. New teams.ini (an Ordos Normal-brain team + 3 recruitable tanks); the golden matches the oracle full 400 ticks on BOTH the unit state (team field 0→1 as Team_AddClosestUnit recruits) AND the RNG draw stream — proving the team loop + brain run identically cross-engine. Targeting is fog-gated off (seenByHouses 0, matching the harness); the full recruit→target→order chain is covered by TeamLoopTests. Oracle rebuilt; all 9 goldens regenerated. 404 tests, clean --full build.
- Phase 4 — defined the `FrameInfo` sim→render seam (`DuneIIContracts/FrameInfo.swift` + `SpriteLayer.swift`) + `Simulation.makeFrameInfo()` (`DuneIISimulation/FrameInfo+Make.swift`): a presentation-neutral per-frame snapshot (terrain tiles incl. baked-in structures, units with `UnitSprites`-resolved body/turret layers + effective house, structures, houses, transient effects = explosions + smoke, viewport in sub-tile units). `UnitSpriteLayer` is now a typealias of the canonical Contracts `SpriteLayer`. Doc `Architecture/FrameInfo.md`. +9 `FrameInfoTests`. 413 tests, clean --full build.
- Phase 4 — the renderer's FrameInfo-consumer core (`DuneIIRenderer`, headless-testable): `Renderer` protocol + `NullRenderer` (records last frame), `GlobalSprite` (the canonical `Sprites_Init` load-order global-index→sheet+frame mapping, deduping the apps' copies), `WorldSpriteSource` + `SpriteFrame` (the asset abstraction), and `FrameComposer` (pure: terrain ground layer → indexed buffer + units/effects → placed, flipped, house-tinted, z-ordered `ComposedSprite`s). Colorization deferred to the SpriteKit leaf. Doc `Architecture/Renderer.md`. +5 `FrameComposerTests` (RendererTests gains a DuneIIContracts dep). 418 tests, clean --full build.
- Phase 4 — the `SpriteKitRenderer` visual leaf (`DuneIIRenderer`) + migrated `mapview` to drive it via `FrameInfo`: colorizes the `FrameComposer`'s terrain buffer (recomposited on tile change, recoloured on palette-cycle tick) into one nearest-filtered terrain node + each `ComposedSprite` (house-recoloured via `HouseRemap`, index-0 transparent) rebuilt per frame, in a camera scene. `mapview`'s `MapScene` now advances the sim, snapshots `makeFrameInfo()`, and renders it (deleting the `GameState`-reaching `MapImageBuilder`; new value-type `MapSpriteSource` over `AssetStore`) — proving the seam end-to-end on real SCEN INIs. Documented gaps: sandworm shimmer + tile overlays. Doc `Architecture/Renderer.md`. Clean --full build, 418 tests; mapview launches + renders (sandbox blocks screenshot).
- Phase 4 — fixed mapview render slowness (grew O(tick) over time). `PaletteAnimator` gained an incremental `stepTick`/`CycleState` (O(1) per tick) and `animatedPalette` now shares it; `SpriteKitRenderer` advances the cycling palette one tick at a time instead of replaying tick 0→N every frame, and recolours the 1024×1024 terrain only when its tiles or the wind-trap colour (223) actually change — not every frame (was a full megapixel colorize per frame, unconditionally). +1 `RendererTests` proving `stepTick` ≡ `animatedPalette`. 419 tests, clean --full build.
- Fixed destroyed buildings staying on the map (#5): `structureRemove` (`GameState+Lifecycle`) now starts the destruction/collapse animation — `Animation_Start(g_table_animation_structure[0], …)` per `structure.c:1305` — which overwrites the building's stamped ground tiles with the rubble frames and aborts back to the base landscape, so the building disappears (it previously left its `groundTileID` stamped forever). Headless/golden runs don't tick animations (matching the oracle parity harness), so the 9 scenario goldens are unchanged; the visual apps play the collapse. Extended `GameStateLifecycleTests.structureRemove` to assert the anim starts.
- Fixed the windtrap power-light starting purple (#4): the animated palette indices (223 windtrap, 255 selection, 239 repair) are the raw IBM.PAL magenta placeholders until cycling shifts them; OpenDUNE runs GUI_PaletteAnimate during the load/fade so they've converged before the map shows, but our renderer drew from tick 0. Added `PaletteAnimator.seedAnimatedColours` (223→entry 12, 255/239→entry 15, the GUI_PaletteAnimate initial references) and seed it in `SpriteKitRenderer.init`/reseed. +1 RendererTests.
- Diagnosed carryall movement/rotation (#1/#2) as sim-faithful via new behavioral tests: `CarryallTests.wingerMovesSubTile` (a winger's per-movement-tick position delta stays < a full tile — it moves in sub-tile steps, not tile jumps) + `wingerRotatesShortestPath` (it turns the shortest way in BOTH directions: clockwise toward East, counter-clockwise toward West — not 'always one direction'). The sim matches OpenDUNE; the visual stepiness/orientation is render-side. +2 SimulationTests.
- Rendering perf: rebuilt `SpriteKitRenderer` to prerender + cache everything (the per-frame `FrameComposer.terrainBuffer` + megapixel `IndexedImage.cgImage` were the hotspots). Terrain is now layered — the static landscape is composited+colorized ONCE into one background node; only dynamic cells (structure-animation tile-id changes + wind-trap-light index-223 cells) get small 16×16 overlay nodes, re-textured only when their tile id or the wind colour changes. Tile textures cached by (tileId,windColour), sprite textures by (spriteIndex,house) — colorized once, flip via node xScale; sprite nodes pooled. `ComposedSprite` gains `spriteIndex` (the cache key). Steady-state per-frame does ~no CoreGraphics. 422 tests.
- Fixed an arithmetic-overflow crash in long mapview runs: `unitAllocate` (`GameState+Pools`) incremented `houses[].unitCount` with non-wrapping `+= 1`, which traps once the count has wrapped to 65535 — but the matching `unitFree` decrement already uses wrapping `&-=` (a long game can drift the count past its live-unit total via transient units, and OpenDUNE's `h->unitCount++` is a plain uint16 that wraps, never traps). Made the increment `&+= 1` to match (both unitAllocate + unitRecount). +1 GameStateTests (allocate at .max wraps to 0, no trap).
- Added a bullet-accounting golden (`missile-duel`) — the cross-engine validation for the unitCount fix: an Atreides + an Ordos Launcher each ordered to attack the other trade notAccurate rockets; each rocket is a unit (Unit_Allocate → firing house's unitCount++) freed on impact (unitCount--), so each house's per-tick unitCount oscillates 1↔3 as 4 bullets spawn + land — a FULL 400-tick match incl. the new per-tick house `unitCount` comparison + the RNG/scatter stream. Extended the parity harness: optional 7th UNITS field = a seenByHouses bitmask (oracle `Scen_LoadUnit` + Swift `loadUnit`, backward-compatible); `HouseGolden`/`houseSnapshot` now compare `unitCount`; `Spec` gained an optional 2nd attack order. (Turrets can't fire in the headless oracle — no sprite-rotation GFX — and the player house crashes House_CalculatePowerAndCredit's radar path, so a non-player missile duel exercises the identical bullet path.) 423 tests, 10 scenario goldens.
- Rocket/unit mirroring (#6): verified the flip data is faithful end-to-end (new `UnitSpriteTests.rocketDirections` — `UnitSprites` resolves the W-half mirror via values_32A4 for all 8 orientations; the flow makeFrameInfo→FrameComposer→renderer carries `flipped`). Since the data was correct, the suspect was the SpriteKit `node.xScale = -1` transform; changed `SpriteKitRenderer` to **bake the horizontal mirror into the cached texture** (pre-mirrored pixel buffer, cache keyed by `(spriteIndex,house,flipped)`) instead of an xScale transform — pre-rendered + immune to any transform quirk. +2 tests (`mirrorRows`). 425 tests, clean --full build. Needs on-screen confirmation in mapview.
- Fixed slabs + walls not rendering: `loadStructure` allocated them but never stamped the map, and — being special-slot structures not in the find array — `GameLoop_Structure` never runs a script to stamp them (normal buildings stamp via their BUILD.EMC `SetState`). Ported `Structure_Place`'s slab/wall case to the World layer (`GameState+WallSlab.swift`): slabs paint `tileIDs.builtSlab` over their footprint, walls paint the WALLS tile + `Structure_ConnectWall` (the 256-entry neighbour-bitmask table + the 4 cardinal `g_table_mapDiff` offsets + recursive re-connect), and neither leaves a persistent structure object (`Structure_Place` frees them). `loadStructure` now branches slab/wall types to placement + returns. +4 WallSlabTests. 429 tests, goldens unchanged.
- Fixed structures (incl. the harvester docked on a refinery) ignoring house colours: the terrain layer drew every map tile with the plain palette — only unit sprites got `HouseRemap` — so non-Harkonnen buildings + their refinery docking-harvester animation rendered in the base colour. OpenDUNE draws each tile via `GFX_DrawTile(groundTileID, …, t->houseID)` (the 0x90-block shift, = our `HouseRemap.tile`). `FrameInfo.Tile` now carries `houseID` (= `map[tile].houseID`); `FrameComposer.terrainBuffer` house-remaps owned tiles in the static background and `SpriteKitRenderer`'s dynamic-cell `tileTexture` remaps animating structure tiles (keyed by `(tileId,houseID,windColour)`, dirty-tracked on houseID too) — Harkonnen/terrain is identity. +1 RendererTests. 430 tests.
- Verified unit sprite direction end-to-end vs OpenDUNE + found/fixed a slither divergence. New `UnitSpriteTests.everyUnitEightDirections` re-derives `viewport.c`'s sprite formula (values_32A4/32C4/334A + the displayMode switch) independently and asserts `UnitSprites.info` matches for EVERY unit type × 8 directions — caught that our `.unit/.rocket` case was missing OpenDUNE's `if (movementType == SLITHER) break` (sandworm/sonic-blast should draw their base frame with no directional offset; they're blurTile-skipped in render, so latent). Confirmed the rocket chain is bit-identical to OpenDUNE: groundSpriteID/displayMode are golden-verified, the global-index→SHP mapping matches `Sprites_Init` (UNITS2@111/UNITS1@151/UNITS@238), so a south rocket draws the exact same SHP frame OpenDUNE draws. 431 tests.
- Added an OpenDUNE 'draw a unit in 8 facings' bitmap tool (`--parity-draw=<type>` in parity.c → off-screen GFX_Init+Sprites_Init+GUI_DrawSprite, no SDL, PPM out; `Scripts/draw-unit.sh` wraps it + PPM→PNG) to settle the rocket-direction question. Verdict: our rocket render is BIT-IDENTICAL to OpenDUNE — both draw a south-flying rocket (orientation8 4) with sprite 262 = the rocket's frame-4. The render shows WHY: `DISPLAYMODE_ROCKET` frames are N,NNE,NE,ENE,E (a 22.5° quarter-turn, NO down frame), so values_32A4 maps the 'south' bucket to the East/horizontal frame — a south rocket points horizontal in OpenDUNE itself. A Combat-Tank control (`DISPLAYMODE_UNIT`, N,NE,E,SE,S half-turn) draws a proper down-facing south. So the 'wrong' south rocket is faithful 1.07 behaviour, not our bug.
- render/air-units: fix air-unit (winger) sprite drawing — UnitSprites.info now branches to viewport.c's separate AIR pass (values_32E4/values_3304 16-orient/values_33AE) instead of the ground values_32A4, so southern facings flip VERTICALLY; south-flying rockets now point down. Threaded a flippedV mirror across the seam (SpriteLayer→ComposedSprite→SpriteKitRenderer baked V-mirror) and proved it against the OpenDUNE bitmap draw tool (parity.c --parity-draw air path). UnitSpriteTests.everyUnitEightDirections now covers both passes; rocketDirections asserts south=N-frame+V-flip.
- render/frameinfo: draw the harvester harvesting overlay (viewport.c:546 third layer, spriteOffset%3 animated, house-neutral) via UnitSpriteInfo.overlay threaded through FrameInfo.Unit/FrameComposer (closes reported bug #3); z-order air units (wingers) above ground units + explosions to match viewport.c's separate air pass (FrameComposer.ZOrder.airBody/airTurret); stop drawing isNotOnMap (in-transport/hidden) units in makeFrameInfo so a carried/picked-up harvester no longer shows as a phantom before the carryall delivers it. +6 tests (UnitSpriteTests/FrameComposerTests/FrameInfoTests), 437 total.
- render: composite tile overlays + fog of war into FrameComposer.cell / terrainBuffer (ground → opaque overlay tile e.g. walls → black veil for cells whose overlay == FrameInfo.veiledTileIndex). Fog is a SpriteKitRenderer.showFog toggle (default off = OpenDUNE debug-scenario 'show everything' view; on blacks out veiled cells per viewport.c:390); mapview gets a Fog toolbar toggle (rebuildTerrain). FrameInfo carries veiledTileIndex (g_veiledTileID); the renderer's per-cell identity + tile cache now key on (ground, overlay, house, wind, fog). Our fog is binary (no partial-fog edge sprites). +3 tests, 440 total.
