# History — 2026-06-01

- duneii player hints (`GUI_DisplayHint` family): a transient bottom-of-map banner (`GameModel.notice`/`refreshHints`, auto-cleared ~3s) on construction-complete (a player factory's product becomes ready — edge-triggered per factory), low power (production < usage — edge-triggered), and insufficient funds (a build/starport order refused for cost — wired into `startBuild`/`orderFromStarport`). All derived each frame from the player economy + factory state, no new sim events; presentation-only → neutrality (552 green, sim untouched) + manual verify. Overlay banner in ContentView. Doc DuneiiClient.md (Hints).
- Fix 'AI fog of war doesn't work' (the AI attacked anyway): enabling the toggle after a scenario was loaded (or relying on the default-off load) left the base placed with stock `seenByHouses=0xFF`, and the live toggle only affected *future* objects. New `GameState.reapplyPlayerVisibility()` re-applies the current flag to existing player objects (on ⇒ re-hide: reset to the player bit + clear `housesFoundPlayer`; off ⇒ restore 0xFF); duneii's `aiFogOfWar` didSet now calls it, so the toggle works mid-game with no reload. Confirmed set-before-load already worked (the load-time `unitUpdateMap` of far enemies in fog doesn't trigger `aiFogReveal`). +2 tests (`AIFogTests` load-flow + reapply), 554, clean --full. Doc AIFogOfWar.md + panel text.
- duneii keyboard action shortcuts for all actions + r=Return: `GameModel.issueAction(ActionType)` issues an action only if it's in the selected unit's `actionsPlayer` (so a=Attack is ignored for a harvester, r=Return for a tank); a/m/h arm a targeted click, r(eturn)/e(retreat)/g(uard)/d(eploy)/b(sabotage)/x(destruct) apply at once, s=Stop universal. Fixes the old retreat-armed-as-targeted bug (Retreat is selectionType .unit → immediate). Immediate actions + the inspector buttons now apply to the whole drag-group (`issue` iterates selectedUnits). Inspector button labels show the letters (`ActionType.shortcut`). duneii builds, 555 tests.
- Fix the fog over-reveal parity bug (root cause of 'I see all enemy troopers with fog on' + 'AI fog of war does nothing'): `Unit_RemoveFog` (`unit.c:1217`) reveals the player's fog ONLY for player-allied units — our `GameState.unitRemoveFog` was missing the `House_AreAllied` check, so every enemy unit self-revealed the player's fog around itself (→ player sees all enemies; `Unit_UpdateMap` then counts the now-unveiled enemy as player contact → `aiFogReveal` fires for every house instantly). Added the allied + off-map/0xFFFF/radius-0 guards. Found+verified by a new cross-engine `fog` scenario golden: added `veil` (isUnveiled) to the `[DUMPTILES]` dump (parity.c + ScenarioGoldenTests), a `fog.ini` (Harkonnen base + Ordos soldiers at increasing distances) — our `veil` now matches the oracle (before: we unveiled tiles 5–10 away the oracle kept fogged). Gated at tick 6 (a residual: the guard soldier adjacent to the base picks a different target encoding than the oracle — a stock targeting wobble, unrelated to fog). Re-recorded the scena001 fog render golden (correctly shows less revealed); fixed `StructureScriptTests.turretFiresAtEnemy` to put its 'seen' enemy on an unveiled tile (a fogged enemy is correctly un-seen). Insight `sim-unit-removefog-allied-only`. 555 tests, clean --full build.
- Wire two sim features that weren't exposed in duneii: (1) PAUSE — `GameModel.paused` → `GameState.paused` (the two-clock pause; `Simulation.tick` no-ops while paused), with a toolbar play/pause button + the space key + a speed-picker disable; (2) WIN/LOSE — `GameModel` reads `GameState.gameEndState` (latched each tick by `evaluateLevelEnd`) and shows a centred Victory/Defeat banner + auto-pauses; both reset on scenario/save load (`finishLoad` syncs `paused`/`gameEnd` from the new state, so a restored save keeps its pause). duneii-only wiring (no sim change) → neutrality (555 green). Doc DuneiiClient.md.
- Presentation/cosmetics batch — audio: (1) distance attenuation — `AudioSink.setListener` + `EngineAudioSink` fades a positioned `SoundEvent` from full (≤2 tiles) to ~0.12 (≥18 tiles); `GameModel.advance` now plays the full event (with position, was just the SoundID) + sets the listener from the viewport. (2) Per-house announcement voices — `registerHouseVoices` loads the player-house `%c`-prefixed VOCs (`HCONST`/`AWARNING`/…, prefix from `HouseInfo.prefixChar`), played on the client hint events: construction-complete → `%cCONST`, base-under-attack (player-building HP drop, rate-limited) → `%cWARNING`. duneii/audio presentation only (no sim change) → neutrality (555 green). Doc DuneiiClient.md (Audio); FeatureParity §350/§206. (Fog soft-edges confirmed already done.)
- Crater overlay on explosion (cosmetic, the last §257 seam): `Explosion_Func_TileDamage` tail (explosion.c:49) — the World explosion VM now records the impact tile (`GameState.pendingCraters`), and `Simulation.drainCraters` stamps the sand/rock crater overlay (grow an existing crater else a random 1-of-2 via `Random_256`), deducts spice (`Map_ChangeSpiceAmount(-1)`), and pops a bloom underneath. Gated to `tickExplosions` (off for goldens) so the RNG draw is parity-neutral. Also verified destruction-rubble (§231) already renders via the death animation's ground-tile rubble cycle → both rows ✅. +1 test (`ExplosionTests.craterRecording`), 556, clean --full build. Docs Explosion.md + FeatureParity §231/§257.
- feat(duneii): in-game music — AVMIDIPlayer + system DLS bank (Tier B) playing the extracted MIDI songs; MusicDirector transcribes OpenDUNE g_table_musics + per-house win/lose IDs, GameModel drives it host-side (scenario start→map theme, under-attack→attack theme, win/lose→stinger, pause); no XMI decoder needed (.NN suffix = OpenDUNE song index); host-side, sim-untouched ⇒ flag-off neutrality golden, +7 MusicDirectorTests; docs Architecture/Music.md (Code/Frameworks/DuneIIAudio/MusicPlayer.swift, MusicDirector.swift, Code/Apps/duneii/GameModel.swift)
- fix(duneii): base-under-attack alert now fires only on real explosion impact to a player structure (port Structure_HouseUnderAttack in GameState+Lifecycle, called from Map_MakeExplosion, gated by timerStructureAttack), replacing the host's cumulative-HP heuristic in GameModel that false-fired on degradation/power/placement; voice+message routed through the sim's new pendingFeedback queue (id 48). +8 HouseUnderAttackTests.
- test(duneii): cross-engine house-under-attack scenario golden — a Harkonnen tank hits an Ordos windtrap; on impact Map_MakeExplosion → Structure_HouseUnderAttack flips the Ordos house's doneFullScaleAttack 0→1 (tick 82), tick-for-tick vs the oracle. Extended parity.c Parity_DumpHouses with timerStructureAttack+doneFullScaleAttack + an opt-in [BASIC] PlayerHuman; added a checkUnderAttack-gated optional house comparison so existing goldens are byte-untouched. Negative-tested (disabling the call fails the golden at tick 82). The player-human feedback path SIGSEGVs headless (no strings) so it stays a unit-tested UI seam.
- fix(duneii): the player's palace no longer auto-launches its house missile at scenario start — set flags.human on the Brain=Human house in ScenarioLoader.loadHouses (mirrors Scenario_Load_House, scenario.c:74) + on the duneii app's chosen player house. Without it the palace special-weapon gate (!human && isAIActive) fired the player's own rocket on the palace's first tick. +ScenarioLoaderTests flags.human assertions, +PalaceTests end-to-end repro (loaded human palace doesn't fire; clearing human fires). Reverted the unused [BASIC] PlayerHuman harness knob (parity.c) — a cross-engine golden is impossible (the oracle SIGSEGVs on any ticked player palace + on flying any house missile headless), so the bar is unit tests + golden neutrality. 572 tests, clean --full.
- feat(duneii): 'Play indefinitely' toggle (Debug panel) disables the win/lose evaluation so the game never ends. New golden-neutral GameState.disableLevelEnd flag (default false) gates evaluateLevelEnd; GameModel.playIndefinitely applies it to the live sim + every load (finishLoad) and clears any latched outcome when turned on. +1 LevelEndTests (the toggle keeps gameEndState .playing past a met condition; control still wins). Debug/UI seam ⇒ flag-off neutrality golden (all scenario/render goldens byte-identical, default off) + unit coverage. 573 tests, clean --full.
- fix(sim): sandworm 'swallow' eat animation now plays — filled the Map_MakeExplosion(ui.explosionType=13, hitpoints 0) seam in UnitCombat.fire (script/unit.c sandworm case), so eating a unit shows the EXPLOSION_SANDWORM_SWALLOW gulp (sprites 218-222) + the already-wired WORMET3P voice. Visual-only (hitpoints 0 ⇒ RNG-free ⇒ golden-neutral). +1 SandwormNativesTests (eat starts the swallow explosion + emits voice 63 + decrements amount). New scenariolab 'sandwormEating' scenario (DuneIIScenarios: ScenarioKind + builder + outcome) so the eat is visually testable. 574 tests, clean --full.
- feat(duneii): spoken 'unit destroyed' announcement — implemented the Sound_Output_Feedback death cue (unit.c:1558). Sim: UnitImpact.damage appends the feedback index to pendingFeedback (saboteur→20; else, unless noMessageOnDeath/not-alive, houseID+14 for the player's own loss or campaign>3, else 13=enemy). RNG-free ⇒ golden-neutral. Host: new FeedbackVoice table (feedback→%c-prefixed g_table_voices fragment sequence) + GameModel.playDeathFeedback plays the fragments in order scheduled by clip duration, rate-limited to one announcement at a time. +1 UnitCombatTests (enemy→13, player→14, saboteur→20, sandworm/noMessageOnDeath→silent). 575 tests, clean --full.
- feat(duneii): wire all remaining gameplay sound announcements. Sim emits (golden-neutral, RNG-free, not dumped): structure-destroyed feedback 21-24 + CRUMBLE Voice_PlayAtTile(44) (structureDamage/structureDestroy); threat warnings 1-12/37 + battle music (Unit_HouseUnitCount_Add player-alert block ported, with directional enemy-approaching via the player CY); unit/harvester deployed 30-35/68-73 (deploy native); starport frigate 38; spice bloom 36; AI palace house-missile 39; carryall drop Voice(24)→DROPEQ2P; radar on/off 28/29 (House_UpdateRadarState core ported, player-only); animation + script VoicePlay natives (unit 0x26 / structure 0x0E). Host: FeedbackVoice now data-driven over g_feedback sequences + g_table_voices fragments (registered per house, played in order, rate-limited, battle music on threat indices); VoiceTable +24/44/54 (DROPEQ2P/CRUMBLE/EXTINY). +5 FeedbackAnnouncementTests; all goldens byte-identical. 580 tests, clean --full.
- feat(duneii): minimap obeys radar availability + STATIC.WSA tuning animation + debug toggle. By default the minimap is blank until the player's radar is up (House.radarActivated — outpost+power, now exposed in FrameInfo.House); a 'Minimap (force on)' Debug toggle (GameModel.forceMinimap) overrides. On a radar on/off transition the host plays the decoded STATIC.WSA noise frames (forward=online, backward=dark) via GameModel.updateRadar; the announcer voice (feedback 28/29) was already wired. Also compute House.structuresBuilt at scenario load (Game_Prepare) so the radar + build prerequisites work from turn 1 — golden-neutral (not dumped, no RNG). +2 tests (FrameInfoTests radar exposure, ScenarioLoaderTests structuresBuilt-at-load). 581 tests, clean --full.
- fix(duneii): radar tuning animation direction — match Dune II's House_UpdateRadarState (WSA_DisplayFrame(activate ? frameCount-frame : frame)): turning radar ON plays the STATIC.WSA reel REVERSED (last→0), turning OFF plays it FORWARD (0→last). Was inverted. 581 tests.
- fix(render): omit not-yet-placed (isNotOnMap) structures from FrameInfo — Structure_Create parks them off-map at (0,0), so the structures loop in FrameInfo+Make.swift was leaking a phantom cyan blip into the minimap's top-left corner; added the same .isNotOnMap guard the units loop already has (FrameInfo+Make.swift:73) + FrameInfoTests.hiddenStructureOmitted neutrality test.
- feat(duneii): campaign-level selector in the scenario toolbar (GameModel.campaignLevel→state.campaignID, applied live + on load) and a full greyed-out build menu — new Simulation.buildOptions(forStructure:) returns every item with BuildBlocker reasons (missing prerequisite structures / campaign level / factory upgrade); the panel greys locked items with a 'Requires: …' tooltip; explains why Heavy Factory was unbuildable (campaignID was pinned to the default 1). Golden bar: buildOptions' available subset is asserted == the cross-engine-verified buildables (Structure_GetBuildable) + blocker-content tests (StructureBuildTests, +3); StructureBuild.swift, GameModel.swift, Panels.swift, ContentView.swift, BuildGUI.md.
- fix(duneii): campaignLevel didSet self-assignment caused infinite setter recursion (stack overflow) when changing the Campaign picker — under @Observable, writing the property in its own didSet re-enters the setter; clamp at call sites with UInt8(clamping:) instead. GameModel.swift; insight swift-observable-didset-self-assign.
- feat(duneii): single scenario-chooser popover — house tabs + scenarios grouped by campaign level, replacing the separate scenario menu + campaign picker; picking a mission sets state.campaignID from the scenario's level (new testable ScenarioID parser in DuneIIWorld: SCEN<House><NNN>.INI → house/mission/campaign, Dune II's 1-then-3-per-level grouping). ScenarioCatalog.swift, ScenarioPicker.swift, GameModel/ContentView/Panels; ScenarioCatalogTests +3 (588).
- feat(duneii): add a Settings window (⌘,) with Sound effects + Music toggles (new EngineAudioSink.enabled master SFX gate + GameModel.soundEnabled; Music moved out of the Debug panel), and Save/Load to the File menu (⌘S/⌘O via CommandGroup, kept in the toolbar too — shared presentSaveGame/presentLoadGame helpers). Presentation/preferences seam, no OpenDUNE oracle: golden bar is flag-off neutrality (defaults on, sim untouched; the music toggle is already covered by MusicDirectorTests, the SFX gate isn't headlessly observable since the engine isn't running on CI) + manual verification. Settings.swift, App.swift, ContentView.swift, GameModel.swift, EngineAudioSink.swift, Panels.swift.
- fix(duneii): minimap corner blob — clip the unit/structure dots to the playable mapArea (the same clip the base terrain uses). Root cause: units can faithfully sit in the unplayable border (the Atreides palace Fremen wave scatters via Tile_MoveByRandom into tile (0,0); OpenDUNE creates it anyway, Unit_Create wrapped in g_validateStrictIfZero++), so the minimap plotted a stray corner blip. Sim is faithful — fix is presentation-only. Tests: MinimapBlobTests (+2) load real scenarios, run, and assert the clip — SCENO020 reproduces the border Fremen spawn + that it's clipped, SCENA020 (requested) never plots a corner blip. Minimap.swift, MinimapBlobTests.swift.
- fix(duneii): Victory/Defeat banner subtitle was invisible — .secondary on the dark panel rendered near-black; use .white.opacity(0.85). ContentView.swift (visual-only, manual verification).
- feat(duneii): authentic AdLib FM music backend via SwiftOPL3 (git dep, from v1.0) — new `ADLMusicPlayer` synthesises the Westwood `.ADL` files on an emulated OPL3 chip (`OPL3Chip` + `WestwoodADL.ADLPlayer`) streamed through an `AVAudioSourceNode` (Mutex-guarded, nonisolated render path); a `MusicEngine` protocol + `MusicBackend` enum let `MusicDirector` swap `.adlib`(default)/`.midi` live, exposed as a Settings picker (`GameModel.musicBackend`, persisted). Selection table unchanged; `AudioTests/ADLMusicTests` (+4: asset mapping, real-data OPL3 non-silent render, backend-switch). 594 tests, clean --full. Docs: Music.md/Music.OPL2.md (marked implemented), insight swift-audio-render-thread-mutex.
- fix(duneii): ADL music crashed on launch (audio IO thread `_dispatch_assert_queue_fail`) — the `AVAudioSourceNode` render block had captured the `@MainActor` `self`, so AVFAudio asserted main-actor isolation off the audio thread. Render path now captures only a `Sendable` `RenderBox` (the `Mutex` state + a `@Sendable` notifier), built in a `nonisolated` factory; the closure is nonisolated `@Sendable`. Insight `swift-audio-render-thread-mutex` corrected.
- fix(duneii): ADL music glitched/dropped out — OPL3 synthesis ran inside the AVAudioSourceNode render callback (missed the real-time deadline). Reworked `ADLMusicPlayer` to producer/consumer: a background synthesis thread runs the chip+driver into a ring buffer, the audio callback only copies out (or emits silence on underrun). Measured OPL3 throughput ~1.9× real-time in debug vs ~0.17× release ⇒ the host must be run optimized (`swift run -c release duneii`); documented in Music.OPL2.md + the insight.
- docs: add Architecture/Parallelization.md — plan for tick speedup. (1) a `SPEEDUP` build mode (env-driven `.define`/`#if !SPEEDUP`, built `-c release`) that (2) strips all non-essential debug output (the `RngTraceSink` hooks in Random256/RandomLCG, the script decision-trace decode in the runners) to zero footprint; plus the determinism-bounded parallelization analysis (shared-RNG order is the parity ceiling → wins are SPEEDUP + single-thread copy/alloc cuts + whole-sim parallelism + off-path makeFrameInfo, not naive parallel-for over the ordered tick) and a mandatory identical-hash stress test for any parallel change.
- chore(deps): update SwiftOLP3 to the optimised synth (track `branch: main`, commit 0cc22ab — no tag past v1.0 yet). OPL3 throughput improved ~2.8× in debug (1.86×→0.67× real-time) and ~4× in release (0.17×→0.04×); debug playback now sustains real-time. API unchanged (builds clean, ADLMusicTests green); refreshed the throughput notes in Music.OPL2.md, DuneIIAudio/CLAUDE.md, CurrentState, and the insight. 594 tests green.
- fix(duneii): the Upgrade button is now offered only at full health — `StructureActions.canUpgrade` (GameModel.swift) gained `s.o.hitpoints == StructureInfo[type].o.hitpoints`, mirroring OpenDUNE's factory-window gate `Structure_IsUpgradable(s) && si->o.hitpoints == s->o.hitpoints` (structure.c:1466); previously a damaged building could be upgraded, diverging from the original. Core `structureSetUpgradingState` was already faithful (no HP re-check); this is the deferred GUI gate. Presentation seam — flag-off neutral (app-only, no sim/golden change); 594 tests green.
- feat(duneii): allow starting construction underfunded — removed the UI credit gate on the factory/construction build menu (GameModel.startBuild no longer blocks on cost; Panels build button no longer .disabled on low credits, cost just shows red). The core build is billed incrementally and auto-pauses (.onHold) when the house runs out mid-build (structureTickStructure / structure.c:266), so starting underfunded matches the original. Starport CHOAM orders keep their upfront affordability check. Presentation seam — app-only, goldens unchanged; 594 tests green.
- fix(duneii): minimap now tracks the live map, not the starting one — `minimapBase` was built once at scenario load, so the radar terrain froze at the initial state (built structures baking into the ground tiles, walls, craters, harvested spice never showed). `GameModel.refreshMinimapBase` now rebuilds the cached 64×64 base from the current frame's tiles, gated by a cheap rolling hash so it only re-extracts/re-allocates when the tiles actually change (not every tick). App-only presentation; goldens unchanged; 594 tests green.
- fix(duneii): the radar/minimap now obeys fog of war — when the 'Fog of war' toggle (showFog) is on, `Minimap.baseImage` leaves unexplored (`!isUnveiled`) cells black, and `MinimapView` hides unit/structure blips on still-veiled tiles via `FrameComposer.isHiddenByFog` (the same masking the main map uses). `refreshMinimapBase` folds fog + per-tile reveal into its rebuild hash and the showFog didSet refreshes immediately; gated by showFog so the default (no fog) is unchanged. App-only presentation; goldens unchanged; 594 tests green.
- feat(duneii): building keyboard shortcuts — when a player building is selected, r=repair (toggle), u=upgrade (toggle), s=stop the in-progress repair/upgrade (GameScene.keyDown branches on GameModel.isBuildingSelected; new stopBuildingActivity only sends the toggle when the matching flag is set, so it can only stop). r/s keep their unit meanings (return/stop) when no building is selected; u was unbound. Repair/Upgrade buttons now show (R)/(U) + tooltips. App-only; 594 tests green.
- fix(duneii): map no longer spans under the toolbar — dropped `.ignoresSafeArea()` on the MapSpriteView (it had drawn the map up into the title-bar safe area, behind the toolbar). The map now fills the content area below the toolbar. App-only layout; 594 tests green, clean --full.
- feat(duneii): right-click the minimap to order the selected unit(s) — `MinimapMouseView` now handles rightMouseDown/Dragged, and `MinimapView` converts the point to a tile and calls `rightClickTile` (the same default move/attack/harvest order as right-clicking the big map). Left click still recentres the camera. App-only input; 594 tests green.
- fix(render): sandworm shimmer no longer drags the fog edge into its silhouette when fog of war is on — the blur displaces terrain from the right, so a worm at a fog boundary pulled the dithered veil pixels into the worm shape (a checkered swirl). `ShimmerEffect.patch/patchRGBA` gained an optional `veiled` predicate; `SpriteKitRenderer.updateBlurs` passes it only when showFog is on, leaving a worm pixel transparent when its display tile or displaced-sample tile is veiled (worm shows nothing over fog, like other units; no fog dragged in at the edge). Fog-off path passes nil ⇒ byte-identical (worm render golden unchanged). +1 ShimmerEffectTests.fogSuppression; 595 tests, clean --full.
- fix(duneii): add the missing 'squish' sound when infantry is crushed by a tracked/heavy unit. The sim chain was already correct (Unit_Move sets variables[1]=1 + ACTION_DIE; Script_Unit_StartAnimation adds 2 → the unitScript1/2 rows 2/3 that carry ANIMATION_PLAY_VOICE 35; animationTick emits SoundID(35)) — but the host VoiceTable didn't map voice 35, so it was a silent no-op. Added 35→g_table_voices[28]→SQUISH2.VOC (verified in VOC.PAK; voiceMapping[35]=28 per src/table/sound.c). Golden: new WorldTests UnitDeathAnimationTests proves the squished-death rows (2,3) emit voice 35 and the normal rows (0,1) are silent, for both infantry display modes. Crushability decision already covered (UnitMovementDecisionTests); crush trigger is a 1:1 port of unit.c:1332. 596 tests, clean --full.
- feat(duneii): build-progress bar under a building's health bar — while a factory/construction-yard is producing (or a repair pad repairing), a white bar shows its readiness directly below the health bar, same width/height + left-to-right fill rule (0%→0 width, 100%→full). New `FrameInfo.Structure.buildProgress` (`Double?`) computed in makeFrameInfo as (buildTime - countDown>>8)/buildTime for a BUSY factory with a queued object (nil otherwise; refinery excluded as a non-factory; shown even while on-hold). GameScene draws it from a white `buildBars` pool one barHeight below the health bar, gated by the same health-overlay toggle (on by default). Golden: FrameInfoTests.buildProgress (+1) — 0 at start, ~0.5 half, >0.9 near done, nil when idle/no-queue. Renderer/HUD overlay like the health bar (no render golden); 597 tests, clean --full.
- feat(duneii): better selection cursor — a selected building now shows a white square outline that pulsates (alpha 1↔0, 3 s period, via a repeating SKAction); a selected unit shows the original selection-box sprite MOUSE.SHP frame 6 (g_sprites[6]), decoded through the palette (index 0 transparent) into a nearest-filtered SKTexture, centred on the unit. selectionBoxes() gained an isStructure flag so GameScene picks the right node (separate pulsating-square + MOUSE/006-sprite pools on the selection layer). Presentation/UI seam — host SpriteKit overlay, no sim/FrameInfo/golden change; full suite neutral (597 tests, clean --full). Manual verification (GUI).
- fix(duneii): no more silent gaps in the AdLib music rollover. The ADL producer thread used to STOP producing when a non-looping (ambient/attack) track hit its end, then wait for the main-actor rollover (notify→onFinished→advanceAmbient→play); since it renders ~1s ahead, the ring drained and the track went SILENT until that round-trip ran — seconds if the main thread was busy (high game speed). Now it fires the end-of-track notify ONCE but keeps producing — the Westwood channels are repeating, so the track loops seamlessly until the next track's command supersedes it. Music never falls silent waiting for the rollover. Host-side audio (no sim/golden change); 597 tests, clean --full. (MIDI backend still has AVMIDIPlayer's inherent brief load gap on track change — unchanged.)
- feat(duneii): middle-mouse click + drag on the minimap recentres/follows the camera too — `MinimapMouseView` now handles otherMouseDown/Dragged (buttonNumber 2) the same as a left click (recenter on the clicked world point, follow while dragging), mirroring the big map's middle-button pan. Active only while the radar is up + settled, like the left/right handlers. App-only input; 597 tests green.
