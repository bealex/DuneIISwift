# History — 2026-06-06

- Fix iPhone pinch failing when a finger starts on a unit/building: enable SKView.isMultipleTouchEnabled in GameScene.didMove (iOS) — it defaults false, so the view tracked only the first finger and dropped a second finger landing afterward (the deliberate one-finger-on-a-unit-then-pinch case); pinch previously only worked when both fingers landed together.
- Sidebar/HUD rework + edge-touch filter: ignore iPhone touches that originate within 15pt of a view edge (GameScene.edgeMargin — drops system edge-swipes/grips); remove the house name from the sidebar header (mission number stays); move credits/power out of the header into a new shared MapStatsOverlay shown top-leading on the map with a double drop-shadow + allowsHitTesting(false), wired into both the iOS and macOS ContentViews.
- Mentat full-screen on iOS + audio settings in Options: present MentatView via fullScreenCover in a NavigationStack (Done button) on iOS, popover on macOS, MentatView fills on iOS; extracted AudioSettingsRows (sound/music/engine) from SettingsView and added it as an Audio section in the Options form on both platforms (replacing the macOS-only SettingsLink). Verified game pauses on open: showOptions/showMentat onChange → beginUIPause/endUIPause → applyPause sets simulation.state.paused.
- Remove the mission-number label from the sidebar (deleted GameSidebar.header entirely; the minimap is now the top element). Sidebar header content (house name, credits/power, mission) is fully gone — credits/power live in MapStatsOverlay.
- Fix Mentat house 'strange titles': in the Atreides mentat file the house topics' parsed description lead-in is a planet label (Caladan: / Name: Unknown), not the house name, and MentatView promoted it to the bold heading. MentatView now uses the canonical topic.name as the heading for .houses (lead-in demoted to a grey sub-line when it differs); also made MentatHelp.parseDescription handle the '?' (no-description) separator per GUI_Mentat_ShowHelp so header/string-index topics yield empty title/body instead of 'No file?NO Desc'. +1 test (parseDescription '?').
- Mentat icons are now the original WSA pictures, not our sprites: per GUI_Mentat_Loop each topic shows the *.WSA named in its description (in MENTAT.PAK). Added SpriteImageProvider.wsaImage(name:assets:) — first WSA frame → CGImage via Minimap.rgbaImage, cached (hits+misses); MentatView.thumbnail now renders that picture (scaledToFit), falling back to the sprite/symbol only when the WSA is absent.
- Fix radar-off minimap rendering to match OpenDUNE: the offline radar (no outpost / no power, not force-enabled) is no longer a black screen — it fills the playable area with palette colour 12 and plots the player's own structures (cyan), with the unplayable border black and the viewport rectangle still drawn (GUI_Widget_Viewport_DrawTile radar-off branch; no terrain/enemies/fog). Presentation seam (SwiftUI Canvas, no render-golden harness) — manual verification.
- Mentat detail picture 4x larger: the topic's WSA image is now size 256 (was 64) and moved above the heading so it reads as the prominent Mentat picture rather than an inline thumbnail; the list-row icons stay small.
- Fix iOS minimap tap-to-center: the radar sits inside the sidebar ScrollView, so its normal-priority DragGesture lost to the scroll pan and taps never recentred. Switched to highPriorityGesture (DragGesture minimumDistance 0) so a tap centers the map immediately and a drag scrubs, winning over the ScrollView.
- Mentat article re-layout: topic-list sidebar 20% wider (210->252); detail now an HStack of the large image (256, unchanged) with title+stats to its right, and the description spanning full width beneath both; widened the macOS popover (660x460 -> 760x520) to fit the new title/stats column.
- Fix iOS placement: a tap on the placeholder no longer nudges it. placementHover now fires only once a touch passes the 10pt drag threshold (touchDidPan), so a tap's tiny jitter doesn't move the footprint — the building is placed at the placeholder's current position; a real drag still repositions it.
- Fix no music on iOS (two gaps): (1) configure an active AVAudioSession (.playback) in duneii-ios App.init before GameModel builds its audio engines — iOS routes AVAudioEngine output only through an active session; (2) bundle the music — build-ios.sh now stages Resources/Audio/Music into the app's Audio/Music/ (folder reference in project.yml, git-ignored) since the sandboxed app can't read the repo path GameModel.musicURL falls back to on macOS.
- Fix 'noise on every level start': the scenario [MAP] Field spice fills were detonating audibly. OpenDUNE runs Scenario_Load_Map_Field with g_validateStrictIfZero raised (Game_Prepare, opendune.c:1410), so Map_Bloom_ExplodeSpice skips the Unit_Remove/tile-revert/EXPLOSION_SPICE_BLOOM_TREMOR and does only Map_FillCircleWithSpice. We drain the fields on the first tick with the flag at 0, so each field spawned the spice-bloom-tremor explosion → its sand-burst voice (EXSAND, voiceID 40) → the load noise. applyScenarioSpiceFields now raises state.validateStrictIfZero around the fill. Parity-neutral (goldens green); updated SpiceFieldTests to assert the no-revert spice-fill.
- Start the camera on the player's base: finishLoad now centres the viewport on the player's Construction Yard (else the centroid of their structures, else their units, else the playable-area centre) instead of the map midpoint — applies to both a fresh scenario and a save load. New GameModel.playerBaseCenter(_:).
- Render winger drop shadows (carryall/ornithopter/frigate): ShadowMapping ports g_paletteMapping1 (the 0xC/0x55 darkening LUT, opendune.c:959) and ShadowEffect.patch darkens the terrain/baked-building under the body silhouette at (+1,+3) on a new SpriteKitRenderer shadow layer (z3, under unit sprites) — fixes the ornithopter crash-site-over-building looking weird (shadow was absent). FrameInfo.Unit.hasShadow from ObjectInfo.hasShadow; ShadowEffectTests (+4) + scena005-air-shadow-t0 render golden + airUnitCastsShadow integration test. 621 tests.
- Fix the ornithopter/carryall crash wreck: our setAnimation handler was a stub that placed the wreck unconditionally with the base id. Now a faithful Explosion_Func_SetAnimation (explosion.c:175) — no wreck when a structure is on the crash tile (the 'crash site over a building looks weird' bug), a random 0/1 variant, and +2 over non-sand terrain. Deferred to Simulation.drainCrashAnimations (needs Map_GetLandscapeType/Random_256) via state.pendingCrashAnimations, like craters. TrackAndCrashTests (+2: structure-skip, sand/rock variant). 623 tests; scenario goldens byte-identical (explosions off the parity path).
- Update SwiftOPL3 (SwiftOLP3) dependency 956d440 → 935140c (latest main): upstream added experimental OPL_FLOAT/OPL_SIMD block-SIMD float DSP engines behind compile flags (off in our build), plus lint/format tooling + a reformat. Our default integer DSP path is unchanged — clean --full build, 623 tests green, ADLMusicTests pass (OPL3 renders non-silent PCM, MIDI/AdLib backend switch intact).
- Double-click a unit selects all same-type units (both clients). New pure InputController.sameTypeGroup (testable, mirrors dominantGroup) + GameModel.doubleClickSelectSameType (enumerates player-owned/on-map/normal units of the clicked type, whole map; falls back to single-select for ground/structure/enemy/winger/worm). macOS: NSEvent.clickCount>=2; iOS: same-tile double-tap within 0.35s. No-oracle input seam => flag-off neutrality (sim untouched, all goldens byte-identical) + InputTests sameTypeGroup; the client wiring is manual-verification owed (no ClientTests target). 624 tests; iOS cross-compile clean.
- Move the Mentat/Options/Save/Load buttons off the sidebar bottom bar into a new floating MapControlsOverlay at the map top-right (both clients). New DuneIIClient/MapControlsOverlay.swift owns showOptions/showMentat + its own SpriteImageProvider; Options/Mentat present as a macOS popover (arrowEdge .bottom) / iOS full-screen cover, freezing the game while open. GameSidebar lost its bottomBar/sidebarButton + onSave/onLoad init params; both ContentViews add .overlay(alignment:.topTrailing){MapControlsOverlay(...)} and pass save/load there. Presentation-only ⇒ neutrality (sim untouched, 624 tests green); macOS app + iOS sources build clean; manual-verification owed (no ClientTests).
- Right-click a player building (no units selected) opens a context popover with its state, actions (repair/upgrade/super-weapon, or the in-progress build's pause/resume/place/stop) and build items (factory/CY buildable list). New DuneIIClient/BuildingContextMenu.swift (BuildingMenuAnchor public + BuildingContextMenu, reusing ActionIcon + the model's structureActions/buildOptions/buildProgress + startBuild/repair/upgrade/launch). GameModel.rightClickOpensBuildingMenu selects the building + sets buildingMenu{slot,point}; GameScene maps the right-click/long-press to a SwiftUI map-overlay point (macOS y-flip, iOS top-left) and tries the menu before rightClickTile (units-selected right-click still orders units). Both ContentViews add a BuildingMenuAnchor overlay. Presentation/input seam ⇒ neutrality (sim untouched, 624 tests green); macOS+iOS build clean; popover anchoring is manual-verification owed.
- Add a scrollable decorative border margin around the map (both clients). Viewport.clamp now clamps to the playable area outset by Viewport.borderPx (50 game-pixels), so the camera scrolls ~50px past the map edge; GameScene.updateBorder fills that ring with four tiled SKSpriteNode strips on a borderLayer (z0.5, above terrain-border cells, below units, never over the play area), textured with the red/gold diagonal hazard stripe from SCREEN.CPS — an 8x8 tileable patch at (241,100) via new AssetStore.borderTile() + IndexedImage. Rebuilt per scenario area change. Client/presentation-only ⇒ neutrality (sim untouched, 624 tests green); macOS+iOS build clean. Doc Algorithms/MapBounds.md; manual-verification owed (GUI). Verified the (241,100) 8x8 patch tiles into a seamless hazard field by cropping SCREEN.png.
- Right-click into the fog of war is always Move, never Attack: GameModel.isEnemy now requires the target tile to be isUnveiled (you can't target what you haven't discovered — faithful: attack needs a revealed tile). Client/input seam ⇒ neutrality (sim untouched).
- Map border: shrink to 25px (Viewport.borderPx 50→25), add a dune-gold solid fallback so the ring is never black if SCREEN.CPS is absent, and add a new ClientTests target (the long-wanted one) with BorderRenderTests rendering the real GameScene headlessly — proves the border ring builds (4 strips 562x25/25x512) with a bright, non-black stripe texture. 625 tests; macOS+iOS build clean. (The user's 'fully black / no border' was a stale build: the strips + clamp-outset weren't in the running binary.)
- Restyle the floating map-control buttons (Mentat/Options/Save/Load): neutral + translucent — white glyphs on a faint dark disc with a thin white outline (no green accent, .plain not .borderedProminent), cluster at 0.85 opacity. Presentation-only.
- Fix the map border rendering black: GameScene.load() rebuilds the renderer and removes scene children not in a keep-list, which orphaned borderLayer every scenario load — the strips existed (textured) but their parent wasn't in the scene, so nothing drew (a black margin). Added borderLayer to the keep-list. Hardened ClientTests/BorderRenderTests to assert the strips are attached to the scene (scene != nil), which the existence-only check had missed. Verified by a headless edge capture: the red/gold SCREEN.CPS hazard stripe now renders along the scrolled edges. 625 tests, macOS+iOS clean.
- Fix the macOS scenario-picker (and other List/Form) popovers being too short: gamePopover used .frame(maxHeight:), under which a SwiftUI List (no intrinsic height) collapses to a few rows. macOS now gets a definite height (frame(width:height:)) so the content fills it; iOS keeps the shrink-to-fit + scroll for small landscape screens. Presentation-only.
- Replace the map border's hazard-stripe tiling with a 9-patch frame from SCREEN.CPS: the rounded gold window-frame bar under the menu buttons (y23, h13, ~8px caps). AssetStore.borderFrame() rebuilds it symmetric (cap|middle|mirrored-cap) + a transposed straight bevel for the sides; GameScene.updateBorder draws top/bottom via SKSpriteNode.centerRect (caps fixed at corners, middle stretched) and left/right as the stretched vertical bevel. borderPx 25→14 to match the frame thickness. Verified by headless edge capture. 625 tests, macOS+iOS clean.
- Change the map border from the SCREEN.CPS gold frame to a concrete-slab tile border, 16px (one tile) thick: AssetStore.concreteTile() returns the CONCRETE_SLAB ground tile (TileIDs.builtSlab) pixels; GameScene.updateBorder tiles it around the 4-strip ring (borderPx 14→16). Verified by headless edge capture. 625 tests, macOS+iOS clean.
- Map-control buttons → Liquid Glass: controlButton uses .buttonStyle(.glass).buttonBorderShape(.circle) (was a dark-disc .plain), dropped the cluster's Capsule backing pane + opacity, and nudged the Save glyph up 2pt (iconOffsetY). macOS+iOS both target 26 so .glass needs no gating. Presentation-only.
- Fix the building right-click popover anchoring from the window instead of the cursor: BuildingMenuAnchor used Color.clear.frame(1).position(point), but .position returns a full-overlay-sized view so the popover anchored to the whole overlay. Now a GeometryReader + .popover(attachmentAnchor: .point(click/overlaySize)) so it emanates from the clicked map point. Presentation-only.
- Building right-click popup: add .focusEffectDisabled() so its action/build buttons don't show macOS focus rings. Presentation-only.
- Building popup: remove the accent focus ring on the build-list rows — .buttonStyle(.borderless) draws an accent focus ring (green here) on the auto-focused first row; switched to .plain + .focusable(false). (.focusEffectDisabled stays on the root.) Presentation-only.
- Selection inspector title: baseline-align the ×N multi-select badge with the unit name (HStack .firstTextBaseline) and drop the title's .lineHeight(.tight) minification so it uses full line height. Presentation-only.
- Extract shared building UI (SelectionTitle/StructureActionBar/BuildOptionRow/BuildProgressBar/FactoryBuildList/StarportOrderList) into Code/Frameworks/DuneIIClient/BuildingControls.swift; the right-click building popup (BuildingContextMenu) and GameSidebar now render the identical views, so the popup matches the sidebar for buildings by construction. Presentation-only seam — no OpenDUNE oracle; verified via clean build + iOS cross-compile + manual UI check.
- Persist selected game options to UserDefaults via a new Prefs helper (Code/Frameworks/DuneIIClient/Preferences.swift): sound/music toggles, the debug toggles (fog, AI fog, unit limit, play-indefinitely, all-economies, health bars, force-minimap), game speed, and zoom now survive launches — loaded as each GameModel property's default + saved in its didSet (audio enable + zoom applied explicitly in init since didSet doesn't fire for init defaults). Presentation/preference seam — no OpenDUNE oracle; covered by ClientTests/PrefsTests round-trip + default-fallback tests.
- Right-click on a player building now shows full info immediately: GameModel.rightClickOpensBuildingMenu recomputes the popup's selection-derived state (refreshBuild + refreshStructureActions) synchronously instead of waiting for the next throttled frame, so a not-previously-selected building no longer opens its popup showing the prior selection's actions/build list. Presentation seam — covered by ClientTests/BuildingMenuInfoTests (CY popup has its build list populated with no tick).
- Context-sensitive map cursor (macOS): with a unit selected the cursor shows the authentic Dune II target reticle (MOUSE.SHP frame 5) over an attackable enemy and the move pointer (frame 0) over everything else — and always move over fog of war. GameModel.unitOrderIsAttack mirrors rightClickTile's resolution (harvester=move, fogged tile=move via isEnemy); GameScene picks/caches the NSCursor in mapCursorKind/applyMapCursor + cursorUpdate, keeping the armed-order/placement/missile crosshair precedence. Presentation seam — decision logic covered by ClientTests/CursorActionTests (nil unselected, move over empty/own/fog).
- Make Scripts/build-ios.sh self-contained: prepend /opt/homebrew/bin:/usr/local/bin to PATH so xcodegen/xcbeautify resolve when run from a non-interactive shell (agent/CI) that didn't source the user's profile — previously it died 'xcodegen is required' though installed, needing a manual PATH export every deploy. Device mode already auto-detects the first connected iPhone/iPad, so 'Scripts/build-ios.sh device' now just works.
- Harden Scripts/build-ios.sh device deploy further: auto-detect the connected iPhone via a JSON file ('devicectl -j /dev/stdout | python' returned empty over the pipe, breaking detection), and build for 'generic/platform=iOS' + install via devicectl instead of a device-targeted build (a device-targeted xcodebuild hangs indefinitely preparing a wireless-only device). Install now errors clearly if the phone is locked/unreachable.
- feat(fog): add `fullSightMovementReveal` (GameState+client toggle, default engine-off/client-on) — a moving player unit now lifts the player's fog over its full `fogUncoverRadius` every tile (`GameState+Lifecycle.swift unitUpdateMap`), matching the original game's centred sight disc instead of OpenDUNE's lagging radius-1 trail; off = byte-identical to the faithful path (scenario goldens neutral). Tests: MovementFogRevealTests (5).
- Remove the accent focus ring on the building popup's build-item rows: the shared FactoryBuildList button used .borderless (which draws a focus ring on the popover's first control); switched to .buttonStyle(.plain) + .focusable(false), the known no-ring recipe.
- Fix attack cursor/order for multi-unit selections: GameModel.isEnemy compared the target to a single selected unit's house (controller.selection.unitSlot, nil for a group), so the attack cursor and attack order never engaged with >1 unit selected; now compare to state.playerHouseID. Also show the attack reticle whenever the Attack order is armed (a-shortcut / sidebar button), and the move pointer for the other armed orders, in GameScene.mapCursorKind (placement/missile keep the crosshair).
- Radar tuning animation now runs at a constant wall-clock rate independent of game speed: the STATIC.WSA on/off transition step moved out of the sim-tick-gated refreshDerived (which slowed/stalled it at <1x speed and pause) into GameModel.advanceRadarTuning, called every render frame from GameScene.update. Detection of the radar on/off flip stays sim-driven.
- Hide the Upgrade button when upgrading is impossible at the current campaign level: StructureActions gains an 'upgradable' flag (state.structureIsUpgradable(slot) || already upgrading); the shared StructureActionBar only shows the Upgrade icon when true (vs. just disabling it for the not-actionable-yet case). Covered by ClientTests BuildingMenuInfoTests (a windtrap is never upgradable).
