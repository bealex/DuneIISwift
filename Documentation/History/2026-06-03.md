# History — 2026-06-03

- fix(ai): the live duneii game no longer pins enemy houses isAIActive at scenario load — that was a parity-harness shortcut (parity.c Scen_LoadTeam pins it because the harness doesn't tick fog). New loadScenario(activateTeamHousesAI:) param (default true keeps the goldens/oracle-matched); GameModel.load passes false so each AI house wakes only on first enemy contact (Unit_HouseUnitCount_Add via the fog path unitUpdateMap/mapUnveilTile), exactly like OpenDUNE's real game. Previously the AI ramped its economy + launched house missiles from tick 0, assaulting the base far too early. Flag-off neutrality: scenario goldens use the default pin → byte-identical; +1 TeamCreateTests (game-mode leaves [TEAMS] houses dormant).
- feat(client): starport CHOAM order is now a cart with delivery progress + visible stock (all three were UI-surfacing gaps; the sim already enforced stock + delivery). GameModel: starportStock→[StarportItem] (carries live stock), new starportCart/cartAdd/cartRemove/canAddToCart/cartTotalCost/sendStarportOrder/clearStarportCart (charge-on-send: batches one .starportOrder per unit, equivalent to OpenDUNE's charge-on-＋/refund-on-cancel), and starportDelivery (frigate-arrival countdown from House.starportTimeLeft/starportLinkedID). Sidebar: −/＋ steppers per row with 'N left'/'Sold out', a cart total + Order/Clear, and a 'Frigate inbound…' progress bar. Sim untouched ⇒ goldens byte-identical; real behavior covered by StarportOrderTests/GameLoopHouseTailsTests. 611 tests green.
- fix(render): a missile crater left on a tile no longer renders over a building placed there. structureUpdateMap (GameState+Animation) now clears a real (unveiled) overlayTileID on each structure footprint tile, matching OpenDUNE's Structure_UpdateMap (if (Tile_IsUnveiled(t->overlayTileID)) t->overlayTileID = 0); the fog-veil overlay run is left intact. We previously stamped only groundTileID, so a pre-existing crater overlay composited on top of the turret/structure. Sim-level map-state fix ⇒ scenario+render goldens byte-identical (OpenDUNE clears too, so faithful); +2 WorldTests (StructureUpdateMapTests: crater cleared, fog veil preserved). 613 tests green.
- feat(client): slim the duneii toolbar to just the scenario chooser — removed the Debug (ladybug), Play/Pause, Speed, and Save/Load toolbar items (ContentView). Game speed moved to the sidebar Options popover (segmented 0.5×–4× picker); Save/Load stay as the sidebar lower buttons + File menu; pause is the space bar. New auto-pause: refactored GameModel pause into userPaused + a uiPauseCount (paused is now private(set), effective = userPaused || uiPauseCount>0; beginUIPause/endUIPause), and freeze the game while a save/load dialog (presentSaveGame/Load), the Options popover, or the Mentat sheet is open (onChange begin/endUIPause), resuming to the player's own pause. No-oracle UI seam ⇒ flag-off neutrality (sim untouched, 613 tests green, clean --full); manual-verification owed (no ClientTests target).
- feat(client): rich Mentat help browser pulled from the game PAKs. New MentatHelp decoder (DuneIIFormats) parses MENTAT<HOUSE>.ENG (ENGLISH.PAK) — the IFF FORM/MENT NAME topic list + the char-pair-compressed descriptions (String_DecompressAndTranslate port), splitting each into wsa/title/attributes/body, with per-topic campaign gating. AssetStore.mentatTopics(houseLetter:) loads the player house's file (cached). New MentatView (replaces MentatSheet, now a popover not a sheet): a Structures/Vehicles/Special/Houses master-detail browser filtered to the scenario's campaign level, each topic showing the original description + sprite (name→type map) + our cost/armor/power/damage/range stats; opens on the selected unit/building when there is one. Mentat button no longer needs a selection. Tests: FormatsTests/MentatHelpTests (+2, real MENTATH.ENG). No-oracle UI seam ⇒ flag-off neutrality (615 tests green, clean --full); Mentat UI manual-verification owed. Format doc Documentation/Formats/MentatHelp.md.
- feat(client): remove the duneii window toolbar entirely and move the scenario (INI) chooser into the sidebar Options popover. ContentView no longer declares a .toolbar or showScenarioPicker; the map fills the window beside the sidebar. OptionsPopover gains a 'Scenario' row (current title + the existing ScenarioPicker in a popover) above the speed picker. All controls now live in the sidebar (scenario+speed+debug in Options, Save/Load+Mentat lower buttons, space=pause). No-oracle UI seam ⇒ flag-off neutrality (615 tests green, clean --full); manual-verification owed (open Options → Scenario picks a mission).
- feat(ios): add the duneii-ios client + build/deploy tooling. Made DuneIIClient iOS-clean (gated Minimap's unconditional import AppKit + the MinimapMouse NSView behind #if os(macOS), added a SwiftUI DragGesture radar path; gated OptionsPopover's macOS-only SettingsLink) and added GameScene touch input (#if os(iOS): tap=select/place/armed-order, drag=pan, pinch=zoom, long-press=order). New Apps/duneii-ios (App.swift @main + ContentView.swift: SpriteView map + shared GameSidebar, Documents quicksave) bundling the install PAKs under GameData/. XcodeGen project.yml (bundle id com.lonelybytes.duneii, team REDACTED_TEAM, automatic signing, landscape). Scripts/build-ios.sh (sim|device|archive) and Scripts/check-ios.sh (iOS-SDK cross-compile type-check — xcodebuild's package resolution is sandbox-blocked). Verified: macOS clean+615 green; DuneIIClient + app sources cross-compile for iOS with no errors. Insight build-ios-crosscompile-typecheck; README Apps/duneii-ios. Device signing/TestFlight run on the user's Mac (no signing identity here).
- feat(ios): target a specific device for deploy — build-ios.sh 'device' mode now takes a device name-substring/UDID (or DUNEII_DEVICE), resolved via xctrace; new Scripts/deploy-iphone17.sh wrapper for 'a specific device' (iPhone 17 Pro Max, UDID REDACTED_UDID, iOS 26.5). Deploy is pending two host prereqs documented in CurrentState: xcodegen (user adding to sandbox) + a signing identity (Apple ID for team REDACTED_TEAM in Xcode ▸ Accounts; currently 0 valid identities). README + CurrentState resume note updated.
