# 2026-04

## 2026-04-19

- Wrote `Documentation/Plans/01.Initial.md` — authoritative phase plan (P0 through P8, with P1 owning every file format decoder).
- Wrote `Documentation/Architecture/Overview.md` — module graph, threading model, asset-load + tick flows.
- Wrote `Documentation/Formats/PAK.md` and implemented `Formats.Pak.Archive` + `Formats.Pak.Encoder`. Real `DUNE.PAK` opens and round-trips; typed `DecodeError`s cover corruption modes.
- Implemented `Codec.Format80` (LZ77-style) and `Codec.Format40` (XOR delta). Synthetic tests cover every opcode.
- Implemented `Formats.Palette` (6-bit VGA, bit-replicated to 8-bit).
- Implemented `Formats.Cps.Image` — full-screen 320×200 images with optional embedded partial palette. Decode works against real `MAPMACH.CPS`.
  - Fix: removed an incorrect guard in `Codec.Format80` that rejected legal forward-overlap copies (short/long absolute copy). Added to the insights log.
- Implemented `Formats.Shp.FrameSet` — modern + legacy offset tables, Format80 + row-RLE expansion, real `MOUSE.SHP`/`UNITS.SHP` decode. Captured SHP offset-table quirks in the insights log.
- Implemented `Formats.Icn.TileSet` — IFF FORM walker (SINF/SSET/RTBL/ RPAL), 4-bit packed sub-palette tiles. Real `ICON.ICN` decodes.
- Implemented `Formats.Wsa.Animation` — Format80 → Format40 pipeline against a running display buffer. Continuation WSAs (`offsets[0] == 0`) emit the running display and continue.
- Implemented `Formats.Fnt.Font` — 4-bit packed glyphs, per-glyph `unusedLines`/`usedLines`. Real `NEW8P.FNT` 'A' verified.
- Implemented `Formats.Voc.Sound` — Creative Voice decoder (types 0/1/2) with the classic 1_000_000 / (256 − divisor) rate formula.
- Added `assetgen` executable + `AssetExport` library target. Ran over the full install: 1959 files produced under `Resources/` (Palettes, Screens, Sprites, Animations, Tiles, Fonts, Audio/{Voc,Music,Drivers}, Scenarios, Scripts, Tables, Strings).
- Introduced `Documentation/History/` (monthly changelog) and `Documentation/Insights/` (distilled non-obvious findings), and rewrote `CLAUDE.md` to require TDD + history/insight updates per feature.
- First TDD-workflow features land: `Formats.IconMap` (terrain tile group indirection per OpenDUNE's `ICM_ICONGROUP_*`) and `Formats.Ini.Document` (case-insensitive, order-preserving scenario/region INI reader). 74 tests green; two new insights captured (iconmap double indirection, INI case handling).

## 2026-04-22

- **Harvester continuous-harvest loop fixed + fullness bar.** `harvestSpiceStep` sets `inTransport = true` on the very first pickup (port of OpenDUNE's `Script_Unit_Harvest` "has cargo" flag). `tickHarvesting` had `!inTransport` gates on the harvest pass + all three AI branches, so a harvester ordered onto spice picked up exactly one unit and then froze. New `Scheduler.isHarvesterDocked(harvesterIndex:structures:units:)` walks the refinery → harvester `linkedID` chain as the authoritative anti-reharvest check; the `!inTransport` gates are replaced with `!isDocked`. Harvester fullness bar: small cyan bar above every harvester marker showing `amount / 100`, hidden when empty (task #41 precursor). New `HarvesterContinuousLoopTests` suite (6 tests) covers: continuous harvest past the first pickup, full → RETURN with `inTransport=true`, docked harvester skipped, and the three `isHarvesterDocked` cases. **844 / 85 / zero warnings.**

- **Movement keeps `targetMove` through `move-halt` + `move-track` log + regression tests.** Ground units whose route step is blocked by a transient obstacle (another unit crossing, a path clipping a structure corner) used to clear both `route` and `targetMove` — leaving hunters wedged in concave building angles with no recovery, and occasionally stranding user-ordered moves. Now `tickMovement` clears only the route + `currentDestination` on a blocked step, keeping `targetMove` live so (a) the next tick's fallback slide immediately drives the unit around the block and (b) the script's `CalculateRoute` re-fills the route on its next dispatch. (An earlier attempt that ran `Pathfinder.findRoute` inline inside `tickMovement` itself was reverted — it pre-empted the script's `CalculateRoute`, which is also where `setSpeed` runs, and left user-ordered moves stuck in `ACTION_MOVE` at `speed=0`.) New `MovementSelfHealTests` suite pins the new invariants (6 tests). New `move-track` tracer emits one compact `tile / pos / orient / goal` line per moving unit per tick so jagged movement is auditable from logs. **838 / 84 / zero warnings.**

- **Harvester harvest-pin widens to any non-HARVEST idle action.** Prior pin only caught `STOP`; `H`-shortcut harvests could land in `GUARD` or other defaults (per `Script_Unit_SetActionDefault`) and strand the harvester. Pin now flips any non-HARVEST / non-RETURN idle harvester back to HARVEST so the existing seek-spice / seek-refinery branches reclaim it. MOVE with a live `targetMove` still skips. Test updated to reflect realistic invariant. **832 / 83 / zero warnings.**

- **Pathfind around other units.** Port of OpenDUNE's `Unit_GetTileEnterScore` occupancy rule (`src/unit.c:2335`): any other ground unit on a tile = impassable. Applied in both `Scheduler.isTilePassable` (per-tick route / fallback gate) and `makeCalculateRouteUnit`'s `scoreFn` (route build). Mover's own tile skipped via `excludingUnit: Int?`; projectiles + wingers don't block. Two naive-movement tests updated to target tiles instead of units (unit-encoded targets now redirect to an adjacent cell via `nearestPassableNeighbor`). **832 / 83 / zero warnings.**

- **Scene fills window — drop aspectFit.** `scaleMode = .resizeFill`; sidebar pinned right via new computed `sidebarX` / `mapAreaWidth` / `mapAreaHeight`; view-area `Self.mapSize` uses replaced with dynamic values (click classifier, HUD positions, sidebar layout, camera clamp, viewport overlay, zoom centre). `didChangeSize(_:)` re-applies camera + rebuilds sidebar / HUD on resize, logs under `scene`. Content-coord math (64×tileSize) unchanged. **832 / 83 / zero warnings.**

- **Attacker flips to default action when target dies.** `Scheduler.tickAttackHold` now detects a freed `targetAttack` pool slot via a new `Self.isTargetAlive(encoded:host:)` helper and reverts to `UnitInfo.actionsPlayer[3]` (GUARD for vehicles, STOP for harvesters) — matches `Script_Unit_SetActionDefault` semantics. Logs under `attack-hold` as `target-dead: action N→M`. `targetAttack == 0` still skips (ATTACK-for-approach case). **832 / 83 / zero warnings.**

- **Corpse transparency + per-house palette + muzzle flash alpha actually dimmed.** Previous corpse rendered in a black square (ICN `.opaque` loaded the tile → index 0 = black) and with Harkonnen-default colour (no house remap applied). New `corpseTexture(houseID:)` on `ScenarioScene` builds a per-house `SKTexture` via `Formats.Icn.TileSet.pixels(forTile:houseID:)` with `.index0Transparent`. Per-house cache keeps the overhead to one CGImage per house. Also moved the `IMPACT_SMALL` alpha cap into `syncExplosions`'s per-tick recompute (`min(0.3, frames/30 × 0.3)`) — prior marker-time assignment was overwritten every frame.

- **Two regressions: placement + corpse.** (a) `ScenarioRuntime.leftClick` short-circuited with `.structureSelected(selIdx)` before reaching the build-panel commit branch — auto-selected CY from load blocked every placement click. Gated the shortcut on `buildController.placementType == nil`. (b) `Explosions.makeExplosion`'s `stopAtPosition` wiped the infantry corpse whenever a follow-up bullet arrived at the dead target's tile. `stopAtPosition` now skips `corpseInfantry` entries. Added headless `dump explosions` verb + enriched corpse-spawn log (slot + activeAfter) — both bugs were diagnosed through them.

- **Attack muzzle flash — visible fire indicator.** `Simulation.Functions.makeFireUnit` (slot 0x08 — `Script_Unit_Fire`) now spawns an `IMPACT_SMALL` explosion at the shooter's tile after `createBullet` succeeds. Damage is `0` (cosmetic only); `unitOriginEncoded` tracks the shooter. OpenDUNE plays a voice cue (`Voice_PlayAtTile(ui->bulletSound)`) at the same point; we render a visual instead so the player sees every fire attempt even when the bullet sprite is too small / too fast to catch. Background diagnostic (via headless `rclick`-attack-then-tick) showed the earlier "no attack animation" report is primarily a range / approach-timing issue — tricks only fire at 3 tiles, so enemies often aren't close enough yet. Extended `FireTests.fireSuccess` to pin the new `IMPACT_SMALL` spawn. No test-count delta (existing suite captured it). **832 green / 83 suites / zero warnings** on clean build.

- **Spice-bloom walk-into detonation.** Port of OpenDUNE's `Map_Bloom_ExplodeSpice` + `Map_FillCircleWithSpice` (`src/map.c:669` / `:697`). New `Simulation.Bloom.explodeSpice(packed:unitIndex:sandTileID:host:rng:)`: spawns an `EXPLOSION_SPICE_BLOOM_TREMOR` at the tile, resets `groundTileID` back to sand via the new `Host.groundTileOverride` closure, walks a radius-5 circle applying `spiceMap.apply(+1)` to every non-thick non-bloom cell (edge cells accepted 50%, one RNG byte per edge), applies an extra `+1` at the centre (matches OpenDUNE's tail — bare→thin→thick at ground zero), and frees the walking unit. New `Scheduler.tickBloomDetonation()` runs after `tickMovement` and sweeps non-sandworm non-projectile units standing on `LandscapeType.bloomField` tiles; fires per-unit detonation. Gated on `scheduler.bloomSandTileID != 0` — default disables the pass for tests that don't care. `ScenarioRuntime.load` sets it to `resolver.landscapeTileID` + wires the `groundTileOverride` closure. 6 new tests in `BloomTests`. **832 green / 83 suites / zero warnings** on clean build. Deferred: `Map_Bloom_ExplodeSpecial` + bloom from damage (explosion-callback path).

- **Action shortcut keys (A/M/H/R).** `UnitCommandController` gains a `stagedAction: StagedAction?` field + a `stage(action:pool:)` method: with a friendly unit selected, pressing A/M/H/R stages attack / move / harvest / return; the next left-click on the map resolves the stage (and clears it) instead of running the select/deselect branch. Harvest + Return gate on `type == 16` (HARVESTER); Attack on empty tiles falls back to `orderMove`, on enemy units to `orderAttack`, on enemy structures to `orderAttackStructure`. `ScenarioRuntime` picks up two new controller actions — `.orderHarvest` (moves there + pins `actionID=harvest`) and `.orderReturn` (finds nearest refinery via `findFreeRefinery` → `findNearestRefinery` fallback and sets `actionID=returnAction`); existing tile-free-for-pathfinding stays in effect. Scene binds keycodes 0/46/4/15 (A/M/H/R) and shows `· STAGE X (click target)` in the HUD. `Escape` also clears the stage. Headless `stage <name>` verb exposes it to harness + tests. 11 new tests in `ActionShortcutTests`. **826 green / 82 suites / zero warnings** on clean build.

- **Slice 9: scene repaint on `SpiceMap.apply`.** Previously, harvesters drained spice via `SpiceMap.apply` each tick but the tile's `groundTileID` never changed — visually a drained tile looked full forever. New `Scripting.Host.spiceLevelDidChange: ((UInt16, SpiceMap.Level) -> Void)?` callback. `Scheduler.tickHarvesting` wraps the existing `changeSpice` closure: captures `before`, runs `apply`, and fires the notifier when `before != after`. `ScenarioRuntime.load` installs a closure that maps `Level → groundTileID` via the resolver's landscape iconGroup (offsets 0/49/65 for bare/thin/thick — port of `Map_ChangeSpiceAmount` at `src/map.c:786`) and rewrites `tileGridRef.tiles[cellIdx].groundTileID`. `ScenarioScene.syncGroundTiles` already picks up `groundTileID` changes per tick — no scene-side plumbing needed. Edge fixup (`Map_FixupSpiceEdges`) deferred. 3 new tests in `SpiceRepaintTests`. **815 green / 81 suites / zero warnings** on clean build. Design: `Algorithms/SpiceRepaint.md`.

- **`ScenarioScene` tile rendering: per-house palette remap via shared cache.** Ports the same `(tileID, houseID) → CGImage` path from `ScreenshotRenderer` into the live SpriteKit scene so the game window matches the golden output. `ScenarioScene.tileTextures` (base palette) still backs terrain cells; a new lazy `houseRemappedTextures` dict stores per-structure-house-remapped `SKTexture`s. `renderedGroundTileIDs` was replaced by `renderedGroundKeys: [UInt32]` packing `(tileID << 8) | ownerHouseOr0` so ownership transitions trigger repaints alongside tile-ID changes. `addGroundTiles` stamps default-palette textures (because `Core.Map.Cell` doesn't carry `houseID`) and records `UInt32.max` sentinels; `build()` then calls `syncGroundTiles()` right after `runtime.load` so scenario-placed structures render in the owner's house colours on the very first frame. No test-count change — renderer output is already validated by `ScreenshotTests`.

- **Structure ICN tiles get per-house palette remap (`ScreenshotRenderer`).** Analysed OpenDUNE's `GFX_DrawTile` (`src/gfx.c:210`): for each rendered map tile it looks up a 16-entry sub-palette via `rpal[rtbl[tileID] * 16 ..]` and remaps every byte where `(c & 0xF0) == 0x90` by `(houseID << 4)` (full 0x90..0x9F band — the narrower `<= 0x96` check is a port-only ENHANCEMENT). Our existing `Palette.applyHouseColors` shared with SHP sprites used a narrower `0x90..0x98` range. New `Formats.Icn.TileSet.pixels(forTile:houseID:)` applies the wider vanilla-D2 rule on the sub-palette before the 4-bit nibble expansion. `AssetLoader` exposes the raw `loadIcnTileSet()`. `ScreenshotRenderer` routes every tile whose cell carries `hasStructure && houseID != 0` through a lazy `(tileID, houseID) → CGImage` cache; terrain cells keep the default atlas. Verified via tile-dump: CYARD tile 292 carries 0x90/0x92 in its sub-palette (→ 0xA0/0xA2 for Atreides); windtrap vane tile 307 has the same. Tiles that render as pink-not-house-colour (windtrap body 304..306) don't carry any 0x9X indices — that's a vanilla-D2 base-palette quirk, not a remap failure. New headless `dump icntile <id>` / `dump iconmap <rawGroup>` commands added to make palette / frame investigations tractable. **812 green / 80 suites / zero warnings** on clean build.

- **Screenshot golden-image test suite (5 tests) + shared `ScreenshotRenderer`.** Pulled the headless screenshot composite into a reusable `DuneIIRendering.ScreenshotRenderer` that renders ground tiles + structure house-colour outlines + unit sprites (UnitSpriteAtlas lookup + per-house palette remap + orientation flip) + selection halos (green / red ellipse for units, rectangle for structures) into a `CGImage` / PNG. `duneii-headless`'s `screenshot` command routes through it so the on-disk PNG and the in-test render match byte-for-byte. New `ScreenshotTests` suite in `DuneIICoreTests/` loads mission 1 via `ScenarioRuntime`, exercises 5 deterministic flows at tick 0, compares pixel-exact against PNG goldens in `Fixtures/Screenshots/`: (1) initial scenario load — terrain + CYARD + spawned units; (2) unit-halo — click tile (29,23) selects the player trike and draws a green halo; (3) structure-halo — click the CYARD anchor draws a green footprint outline; (4) slab placement — commit a 1×1 slab east of the CYARD; (5) windtrap placement — commit a 2×2 windtrap west of the CYARD. Env `DUNEII_REGENERATE_GOLDENS=1` rewrites fixtures; a missing golden on a normal run is a test failure (no silent creation). On mismatch, the current render lands next to the golden as `<name>.actual.png` for manual diffing. Also fixed a latent gap: `ScenarioRuntime.load` was only flagging `hasStructure=true` on scenario-placed structure footprints without stamping iconGroup tiles, so any consumer reading from `tileGrid` (minimap, screenshot) saw sand under the CYARD. Now paints iconGroup tiles the same way `stampPlacement` does for runtime-placed structures (OpenDUNE `Structure_UpdateMap` offset). **812 green / 80 suites / zero warnings** on clean build.

- **Headless screenshot command (`screenshot x y w h path`).** `duneii-headless` gained a `screenshot` verb that renders an arbitrary tile rectangle from the live `runtime.tileGrid` to a PNG at 16 px / tile. Each cell's `groundTileID` is composited from `assets.loadIcn()` into a `CGContext(premultipliedLast)`; the buffer is read back + handed to the existing `PNGWriter` so error surface + directory creation stay consistent. Unit / structure overlays not drawn — the ground stamps already carry scenario + placed structures, which is the payload needed for visual-regression tests on terrain / placement / palette. Smoke-tested: `load SCENA001.INI` → `screenshot 20 20 24 16 …/mission1-center.png` emits a 384×256 RGBA PNG with the expected rock / sand / spice layout. Logged under the `screenshot` tracer.

- **Post-playtest bug-fix batch (5 issues).** User-reported issues from the first hands-on play of the player-built structure flow. (1) **Sidebar click Y offset**: `sidebarSlotIndex(atY:)`'s analytic formula had a `-1` that offset the click region one full row below the visible highlight (user had to click below the row they wanted). Replaced with direct iteration over visible row frames. (2) **Speed multiplier for debug**: new `speedMultiplier` ivar on `ScenarioScene` (1× / 2× / 4× / 8× / 16×, cycled by `,` and `.` keys); HUD appends `· N×` when non-default. (3) **Structure iconGroup frame pick**: `stampPlacement` + `ScenarioWorld` were picking the *last* `w × h` tiles of an iconGroup, which for multi-frame groups (refinery, windtrap) lands on the damaged / with-harvester / rotated variant. Switched to OpenDUNE's `Structure_UpdateMap` offset (`src/structure.c:1796`) — skip `2 × layoutSize` and take the next `layoutSize` for the "finished" frame. Short groups still fall back to tail. (4) **Structure house-color outline offset**: `syncStructures` centred its `SKShapeNode(rectOf:)` at `anchor + (dims-1) × 128` — the *centre of the top-left tile*, not the footprint midpoint — shifting every outline half a tile up-left. Corrected to `anchor + dims × 128`. (5) **Harvester stuck in STOP after refinery spawn**: OpenDUNE's `actionsPlayer[3] = stop` for harvesters, so `Script_Unit_SetActionDefault` leaves a freshly-spawned player harvester idle. `tickHarvesting` now pins a genuinely-idle harvester (`STOP + not in transport + no move + amount<100`) back to `HARVEST` so the existing seek-spice branch reclaims it. Logs under `harvest-tick` (`harvest-pin harvester=N STOP → HARVEST (idle)`). No existing tests broke — **807 green / 79 suites / zero warnings**.

- **Carryall pickup slice 8c: arrival detection + drop-off.** Closes the ferry loop. `Simulation.Units.dropCarryall(carryallIndex:units:structures:)` detaches the ferried harvester (snaps its position to the carryall's current tile, clears `inTransport` on both sides, frees the carryall slot). `Scheduler.tickCarryallFerry()` — new pass running between `tickMovement` and the tickUnits/tickStructures/tickHarvesting trio — walks the unit pool, picks up every in-transport CARRYALL whose `linkedID != 0xFF` AND `targetMove == 0` (the arrival signal `tickMovement` writes on `Arrived via targetMove fallback`), and invokes `dropCarryall`. The dropped harvester then re-enters tickHarvesting's RETURN-action branch on the same tick + docks via the existing flow. Logs under `carryall` — `carryall-drop slot=N harvester=M at=(px,py)`. Deferred vs. OpenDUNE: carryall doesn't fly off to a map-edge return trip — we free the slot on drop so the same carryall can't be reused for another ferry yet (next ferry allocates a fresh one). 5 new tests in `CarryallPickupTests` (3 pure helper + 2 scheduler). **807 green / 79 suites / zero warnings** on clean build. Design: `Algorithms/CarryallPickup.md` §8c. This closes the carryall pickup loop (task #1 of Next-up from yesterday).

- **Carryall pickup slice 8b: spawn a carryall when all refineries are busy.** Builds on 8a's `findFreeRefinery` preference. New `Simulation.Units.callCarryall(harvesterIndex:destinationRefineryIndex:units:structures:)` ports the tail of OpenDUNE's `Unit_CallUnitByType` (`src/unit.c:2131`) — allocates a CARRYALL (type 0) in its per-type pool range, writes `inTransport=true`, `linkedID=harvesterIndex`, `targetMove=EncodedIndex.structure(refineryIdx).raw`, and positions it at the harvester's current tile. Harvester also flips `inTransport=true` so the ferry isn't interrupted. `Scheduler.tickHarvesting`'s full-harvester branch now picks one of three paths: (1) free refinery found → foot route (8a); (2) all refineries busy AND house owns ≥ 2 refineries → carryall ferry; (3) single refinery (busy) → foot fallback. New `Simulation.Scheduler.countRefineries(houseID:structures:)` helper gates the ferry on the ≥ 2 count — no point ferrying to the same refinery. Logs under new `carryall` tracer for spawn; `harvest-tick` reports `(free)` / `(carryall ferry)` / `(only-option)` per path. 4 new tests (3 callCarryall unit tests + 2 scheduler integration tests); one slice-8a test superseded (2-busy-fallback assertion now reflects carryall behaviour, not on-foot). **802 green / 79 suites / zero warnings** on clean build. Design: `Algorithms/CarryallPickup.md` §8b. Deferred to 8c: fly-to-pickup phase, drop-off on arrival, carryall return-to-origin.

- **Carryall pickup slice 8a: full harvester prefers a free refinery.** Pure-sim groundwork toward the carryall ferry (task #1 of Next-up). `Simulation.Scheduler.findFreeRefinery(forHarvester:structures:)` walks the structure pool like `findNearestRefinery` but filters `linkedID == 0xFF` — picks the nearest refinery that has no harvester currently docked. `tickHarvesting`'s full-harvester branch now tries `findFreeRefinery` first and falls back to `findNearestRefinery` when every refinery is already busy; logs the decision under `harvest-tick` with `target=free` vs `target=busy-fallback`. Observable: with two refineries + two harvesters, harvesters parallelise instead of queueing at the closer refinery. The carryall itself ships in 8b. 9 new tests in `CarryallPickupTests`. **798 green / 79 suites / zero warnings.** Design: `Algorithms/CarryallPickup.md`.

- **Sidebar minimap — radar render between BUILD rows and INFO panel.** New pure-Foundation `DuneIIRendering.Minimap` produces a 64×64 RGBA buffer from `(tileGrid, landscapeAt, UnitPool, StructurePool, houseColor)` — terrain colour per tile first (sand / rock / mountain / spice / thick-spice / slab / wall / bloom), then one unit pixel per non-projectile slot (projectile types 18..24 skipped to avoid per-tick flicker), then structure footprints paint over units so 3×2 refineries don't get holes. Slabs + walls read as terrain — no overlay. `ScenarioScene` builds the `SKTexture` once per tick in `refreshMinimap()` via `CGImageFactory.makeRGBAImage` + nearest-neighbour filtering; node pinned at a 120×120 pt square above the info panel (baseY=204). Manual-verification: run `swift run duneii`, confirm a colour map appears in the sidebar gap, units / structures show as house-colour dots, and the render updates every tick. 12 new tests in `MinimapTests`. **789 green / 78 suites / zero warnings.** Design: `Algorithms/Minimap.md`.

- **Task 9: Pathfinder + scheduler fallback redirect to passable neighbour.** Previously, any unit whose `targetMove` pointed at an impassable tile (player's CYARD for enemy hunt-action units, walls, mountains for vehicles) halted at spawn because neither the pathfinder nor the scheduler's targetMove-fallback path could make progress. Two fixes: (1) `Simulation.Pathfinder.findRoute` pre-resolves an impassable `dst` via the new `resolveReachable(src:dst:score:)` helper — it samples `score(dst, 0)` and, if impassable, returns the nearest passable 8-neighbour measured by squared distance from `src`. Keeps `findRoute`'s interface unchanged so call sites don't need to know. (2) `Scheduler.tickMovement`'s targetMove-fallback gained `nearestPassableNeighbor(of:from:movementType:)` with the same shape — redirects the move goal to the passable adjacent tile instead of halting. Mission 1's three Ordos HUNT-action soldiers now actually advance toward the player base each tick instead of standing frozen at spawn. 3 new tests in `PathfinderTests` pin the resolver behaviour. **777 green / 77 suites / zero warnings.**

- **Task 8: Keyboard shortcuts — Escape deselects, Tab cycles player units.** `ScenarioScene` now overrides `keyDown(with:)` + sets `acceptsFirstResponder = true` + calls `view.window?.makeFirstResponder(self)` at scene build so keystrokes land. Two bindings: `Escape` (key code 53) routes to `ScenarioRuntime.deselect()` — clears unit + structure + placement selections in one call; `Tab` (key code 48) routes to `ScenarioRuntime.cycleToNextPlayerUnit()` which walks `findArray` skipping projectiles + non-player units and wraps around. Both refresh the halo + sidebar afterwards. `Simulation.Scheduler.isProjectileType` promoted from internal → public so the runtime can filter. Logs both actions under the `selection` tracer. 2 new tests (`deselectClearsAll`, `cycleSelection`). **774 green / 77 suites / zero warnings.**

- **Task 7: Right-click attack on enemy structures (slice 3 of attack work).** New `Simulation.Units.orderAttackStructure(poolIndex:targetStructureIndex:units:structures:)` composes `Unit_SetAction(ACTION_ATTACK)` + `Unit_SetTarget(EncodedIndex.structure(idx))` — same shape as the unit-attack slice but for building targets. Non-turret attackers get `targetMove = encoded` + cleared route so the chassis chases; turreted units stay put and rotate. `UnitCommandController.handle` gained a `structures:` parameter + `enemyStructureAtTile` scan; right-click routing now promotes a move order to `orderAttackStructure` when the click lands on a non-player structure tile. New `.orderAttackStructure(attacker:targetStructureIndex:)` controller action; new `.orderAttackStructure(attacker:targetStructureIndex:ok:)` `ClickOutcome`. Runtime + scene + harness all updated to handle the new case. Logs under `unit-cmd` + `attack` tracers. 4 new tests (3 in `UnitOrderAttackTests`, 1 in `ScenarioRuntimeTests`). **772 green / 77 suites / zero warnings.**

- **Task 6: Spawn harvester on first refinery (no carryall ferry).** Simplified port of OpenDUNE's `House_EnsureHarvesterAvailable` (`src/house.c:298`). When `ScenarioRuntime.commitPlacement` lands a REFINERY (type 12) for the player, a new helper `ensureHarvesterAvailable(houseID:refineryTileX:refineryTileY:host:)` checks whether the house already owns a harvester anywhere in the pool; if not, it spawns one at the refinery's south-exit tile in `actionID = HARVEST` so slice-7's auto-seek-spice cycle picks it up on the next `tickHarvesting` pass. Shortcuts OpenDUNE's carryall ferry (which spawns a CARRYALL off-map, has it pick up a freshly-created HARVESTER, fly to the refinery, drop it off) — we spawn directly on the map. Every spawn + skip logs under the new `harvester-spawn` tracer. 1 new test (`refineryPlacementSpawnsHarvester` — builds 4 slabs, windtrap, refinery; asserts exactly one Atreides harvester exists in HARVEST action). **768 green / 77 suites / zero warnings.**

- **Selection UI task 5: Ground tile sync after placement.** The scene's `addGroundTiles` stamped the SKSpriteNode textures once at scene-build from `ScenarioWorld`; runtime placements updated `tileGrid` but not the scene tiles, so a newly-placed slab or windtrap stayed invisible. Fixed two layers: (1) `ScenarioRuntime.stampPlacement` now paints the fully-built iconGroup tiles for non-slab / non-wall structures into the runtime tileGrid (mirrors `ScenarioWorld`'s scenario-spawn stamping via `StructureInfo.iconGroupRawValue` + `IconMap.tileIds(in:)`), sets walls to `wallTileID+1`, keeps slabs at `builtSlabTileID`. (2) New `ScenarioScene.syncGroundTiles` pass compares each cell's live `groundTileID` against a cached `renderedGroundTileIDs` array and re-textures only the changed tiles — runs every scheduler tick inside `update(_:)` and eagerly on `placementCommitted` so the player sees their building immediately. 1 new test (`structureStampsIconGroupTiles` asserts the windtrap's 4 footprint tiles carry non-default iconGroup IDs + hasStructure + houseID). **767 green / 77 suites / zero warnings.**

- **Selection UI task 4: Structure selection halo.** `ScenarioScene.refreshSelectionHalo` now draws a halo around a selected *structure* in addition to the existing unit halo. Footprint dimensions come from `StructureInfo.layout.dimensions`; the outline matches the structure's visible footprint rectangle (e.g. 1×1 slab, 2×2 CYARD, 3×2 refinery). Friendly structures draw in green, enemy structures in red — matching the halo colour rule already applied to enemy units. No sim-layer changes; test count unchanged.

- **Selection UI task 3: Info panel in the sidebar.** New 200px info panel anchored at the bottom of the right sidebar, rendered by `ScenarioScene.renderInfoPanel` inside the same container-teardown cycle as the build sidebar. For a selected unit: full name (Trike, Soldier, Carryall, etc.), house, HP bar (green>66%, yellow>33%, red<33%), action (Move/Attack/Guard/…), and a hint line — friendly units show "R-click: move / attack"; enemy units show "Enemy — info only". For a selected structure: full name (Windtrap, Refinery, Construction Yard, …), house, HP bar, state (Idle/Busy/Ready), and hint — friendly yard shows "sidebar: build / R-click: rally", friendly non-yard shows "owned", enemies show "info only". Empty selection shows "click a unit or building" placeholder. Auto-updates every scheduler tick via `refreshBuildSidebar`. No sim-layer changes. Test count unchanged (766); all visual.

- **Selection UI task 2: Structure selection in runtime.** New `ScenarioRuntime.selectedStructureIndex: Int?` set whenever a left-click lands on any structure footprint (friendly, enemy, yard, non-yard). Unit selection now clears it (mutually exclusive); empty-tile deselect leaves it alone. New `.structureSelected(Int)` `ClickOutcome` case; scene's `handleOutcome` refreshes the sidebar + halo when it fires. Scheduler-side slot frees are swept each tick via new `validateSelections()`. Harness gained a `dump selection` command printing `unit=N unitFriendly=B structure=N yard=N`. Log under the new `selection` tracer. 2 new tests (`clickStructureSetsSelection`, `clickUnitClearsStructureSelection`) + 1 updated to expect the new outcome. **766 green / 77 suites / zero warnings.**

- **Selection UI task 1: Enemy unit selection (info-only).** `UnitCommandController` gained `isFriendlySelection: Bool`. Left-click on ANY unit tile (friendly or enemy) selects it; the flag tells the scene whether to render action affordances. Right-click only acts when `isFriendlySelection == true` — enemy selections are info-only (no move/attack order). Previous `.selectUnit(poolIndex:)` case widened to `.selectUnit(poolIndex:isFriendly:)`. `ScenarioRuntime` logs the new `friendly=true/false` flag under the `unit-cmd` tracer. 1 new test (`rightClickEnemySelectionInert`) + renamed existing test (`clickEnemyDoesNotSelect` → `clickEnemySelectsInfoOnly`). Foundation for the info panel + the "building select shows info, no actions for enemies" pass. **764 green / 77 suites / zero warnings.**

- **Movement port: Unit_SetSpeed + subpixel accumulator + infantry walk cycle.** Replaces the `step = max(4, speed/4)` shortcut with OpenDUNE's real movement model (`src/unit.c:98, 1902`). New `Simulation.Units.setSpeed(poolIndex:speedPercent:units:)` ports `Unit_SetSpeed`: multiplies the 0..255 percent by per-type `movingSpeedFactor / 256`, splits the result into a tile-hop clamp (`speed`) + subpixel accumulator step (`speedPerTick`), resets `speedRemainder`. New UnitSlot fields `speedPerTick`, `speedRemainder`, `movingSpeed`. `Scheduler.tickMovement` rewritten around the accumulator: per tick, `remainder += speedPerTick`; when the high byte is non-zero, fire one `Pos32.moved` step with distance `min(speed*16, distToGoal+16)`. Overshoot detection (post-move distance >= pre-move → snap to goal) mirrors `src/unit.c:1419`. All three entry points now route through `setSpeed`: `makeSetSpeedUnit`, `makeStopUnit`, `CalculateRoute`'s landscape-speed slice (moved to run BEFORE the orientation-gate so a fresh route sets speed even when the MOVE script's "already moving" wait blocks subsequent CalcRoute calls). `makeCalculateRouteUnit`, `Units.createUnit`, `Units.createBullet` all call setSpeed for wingers / bullets so their accumulators are populated.

- **Infantry sprite fix: values_32C4 + values_334A port.** Our previous resolver used `values_32A4` (the 5-direction vehicle table) for infantry, so soldiers always showed a static pose. New `Orientation.infantryBucket` (`[(0,false), (1,false), (1,false), (1,false), (2,false), (1,true), (1,true), (1,true)]`) matches `values_32C4` at `src/gui/viewport.c:496` — three direction buckets (N, side, S) with E-side mirrored for W. New `Orientation.infantry3FramePhase = [0, 1, 0, 2]` ports `values_334A` for 3-frame walk cycles. `UnitSpriteAtlas.resolveFrame` gained a `spriteOffset:` parameter threaded through from the scene; infantry indices now compute as `groundSpriteID + bucket*stride + phase`. New `Scheduler.tickSpriteOffsets` advances the walk-cycle counter once per 5 ticks for moving infantry only (mirrors `src/unit.c:243..244`).

- **Mission-1 bug hunt pass 1.** Four concrete fixes surfaced by the headless harness plus regression tests. (1) **Scenario-spawned structures now stamp `hasStructure=true` onto the runtime tileGrid at load**, so the pathfinder + passability gate see them as impassable. The plain `Map.Generator` used by `WorldSnapshot.init(scenario:)` doesn't apply structure placements (that's ScenarioWorld's job, used only for rendering); before this fix a TRIKE ordered south-east of the CYARD routed *through* it. (2) **Runtime-placed slabs + structures now stamp the tileGrid live** — `ScenarioRuntime.stampPlacement(type:tileX:tileY:)` writes `groundTileID = builtSlabTileID` + `houseID` for slabs (types 0/1), or `hasStructure = true` + `houseID` otherwise. Subsequent `isValidBuildLocation` / pathfinder calls see the placement. (3) **CYARD flips back to IDLE after a successful placement**: the yard's state, objectType, countDown reset so the next sidebar click doesn't re-open placement for the same type. (4) **Closures baked over live state**: `makeTileEnterScorer` and `makeLandscapeLookup` now capture a `TileGridRef` class box, not a snapshot of `[Tile]`, so post-load mutations propagate to the scheduler's scripts. `WorldSnapshot.Tile` init is now `public` to let the runtime build fresh tiles on stamping. End-to-end: mission-1 Atreides can now lay 4 slabs adjacent to the CYARD → build a WINDTRAP fully un-degraded (validity goes from `-4` to `1`) → see REFINERY unlock in the buildable list. **Mouse clicks verified working via the runtime path** (unit select, deselect, move, attack, yard select, sidebar, placement commit). Headless REPL → runtime calls → same code path the scene uses. 14 new install-gated tests in `ScenarioRuntimeTests` (load counts, CYARD tile stamp on load, click routing all branches, full slab+windtrap build cycle, pathfinder-around-CYARD, harvest pass with no harvester, yard-select no-op, tick-refreshes-yardState). **763 green / 77 suites / zero warnings on clean build.**

## 2026-04-21

- **Headless harness + `ScenarioRuntime` extraction.** Pulled all non-visual game state out of `ScenarioScene` into a new `DuneIIRendering.ScenarioRuntime` class — it owns `AssetLoader`, `Scripting.Host`, `Simulation.Scheduler`, `BuildPanelController`, `UnitCommandController`, `tileGrid`, `tickCounter`. Exposes intent methods `load(scenarioName:)`, `tick(_:)`, `leftClick(tileX:tileY:)`, `rightClick(tileX:tileY:)`, `sidebarClick(row:)`, `selectYard(index:)`, plus a `ClickOutcome` enum describing what happened. Scene becomes a thin SpriteKit renderer that forwards mouse events and reacts to outcomes for visual refresh. Added new SwiftPM target `duneii-headless` — an executable that drives the runtime via stdin with commands (`load`, `tick`, `click`, `rclick`, `sidebar`, `dump <units|structures|houses|tile|build|spice|scene>`, `validity`, `enqueue`, `build`, `yard`, `quit`), prints command outcomes on stdout, and routes the `Log` facade to stderr via `FileMemoir(handle: FileHandle.standardError)` so a caller can redirect `2>run.log` while parsing stdout. First use of the harness immediately caught a bug: the `BuildPanelController`'s surface state (`yardState`, `countDown`, `queuedType`) was stale in the runtime because `tick()` wasn't calling `refreshBuildState()`. Scene was unaffected (scene's per-frame refresh hid it) but the harness's tick-then-read pattern flushed it out. Fixed: `runtime.tick()` now calls `refreshBuildState()` at the end. Design: runtime extraction is the basis for future mission-1 bug diagnosis. Docs + more bugs to follow.

- **Harvester auto-seek spice (slice 7 of spice income).** New transition in `Scheduler.tickHarvesting`'s AI pre-pass: a HARVEST-action harvester with `amount < 100`, `!inTransport`, no active move (targetMove==0, route[0]==0xFF, currentDestination==0), and standing on a non-spice tile now auto-issues `Units.orderMove` toward the nearest `thin`/`thick` spice tile. Closes the cycle after `undockHarvester` — harvesters now fully self-manage: fill → return → dock → drain → undock → seek-spice → move → fill → … without any human nudge. New `Scheduler.findNearestSpiceTile(from:map:)` squared-distance scan over all 4096 cells (cheap at cadence 3). Returns nil on a drained map — silent no-op. Logs: `harvest-cycle seek-spice harvester=N from=(x,y) → tile=(x,y)` under `harvest-tick`. 5 new tests in `SpiceSeekTests` (nil when map empty, nearest-wins selection, idle-off-spice triggers move, active-move not overridden, on-spice skips seek because harvest pass handles it). **749 green / 76 suites / zero warnings on clean build.** Design extended in `Algorithms/HarvesterSpiceDeposit.md` §"Slice 7".

- **Placement UX + movement passability gate (two user-facing bug fixes).** (1) Building placement from a READY CYARD used to fail silently when the player clicked an invalid tile — no ghost preview, no feedback, and `placementType` reset on any rejection so the player had to re-click the sidebar between every attempt. New hover-driven ghost footprint in `ScenarioScene`: tracks mouse via `NSTrackingArea` + `mouseMoved` override; coloured green (valid) / yellow (degraded, slab deficit) / red (invalid) at the hovered tile. HUD widened to "PLACING TYPE (click map)" while in placement mode. Transient red toast above the map when a commit rejects, auto-clearing after ~24 ticks (~2 s). Placement now persists across rejections so the player can try tile after tile without re-clicking the sidebar. `commitPlacement` success exits placement mode and hides the ghost. New `placementValidity(type:tileX:tileY:)` helper refactors the same gates used by `commitPlacement`. (2) Vehicles previously walked straight through rock, walls, and structures — `tickMovement`'s fallback path (when a unit has `targetMove` but no pathfinder route) slid directly toward the tile without landscape or structure checks, and the route-step path didn't re-validate either. New `Scheduler.isTilePassable(tileX:tileY:movementType:)` helper combines a live structure-pool footprint check + `LandscapeInfo.movementSpeed[movementType] != 0` via `host.landscapeAt`. Winger and slither bypass (they fly / slither over everything); projectiles bypass (they should detonate wherever the script sends them). Gate wired at both `tickMovement` paths: route-step (`route[0]` → next tile) and fallback (raw `targetMove` slide). On fail: clears route + currentDestination + targetMove and logs `move-halt u\(idx) impassable tile=...` under the `move` tracer. 7 new tests in `MovementPassabilityTests` (fallback into mountain halts, route-step into structure halts, winger over mountain, sand passable, `isTilePassable` landscape matrix: mountain blocks vehicles but foot climbs, rock passable for all ground, wall blocks all, off-map fails). **744 green / 75 suites / zero warnings on clean build.**

- **Harvester AI loop (slice 6 of spice income).** Two additions inside `Scheduler.tickHarvesting`: (6a) auto-undock once `refineSpiceStep` empties a harvester — the refine pass records exhausted pairs, a second loop calls `Structures.undockHarvester` at the south-of-footprint exit tile and flips `actionID = HARVEST` so the next pass resumes; (6b) pre-pass transitions full HARVEST-action harvesters (`amount >= 100`, `!inTransport`) via a new `Scheduler.findNearestRefinery` squared-distance search, calls `Units.orderMove` toward the refinery, flips `actionID = RETURN`; when RETURN harvesters land on a same-house refinery footprint (via new `Scheduler.refineryAt` footprint check), calls `Structures.dockHarvester` and flips back to HARVEST. End-to-end: a full harvester seeks the nearest refinery, arrives, docks, drains ~34 passes worth of spice, earns ~700 credits, auto-undocks, and is ready to harvest again — all without a live EMC. Logs everything under `harvest-tick` (`full`, `arrived ... DOCK`, `released ... action=HARVEST tile=(...)`). 5 new tests in `SpiceAILoopTests` (seek, dock, auto-undock, full cycle, nearest-refinery prefers same-house). **737 green / 74 suites / zero warnings on clean build.** Design extended in `Algorithms/HarvesterSpiceDeposit.md` §"Slice 6".

- **Scheduler + scene wiring for harvesting (slice 5 of spice income).** `Scripting.Host` gained `spiceMap: Simulation.SpiceMap?`. `Simulation.Scheduler.init` gained `harvestRNG: (() -> UInt8)?`. New public `Scheduler.tickHarvesting()` drives the per-tick pass — iterates HARVESTERs in `ACTION_HARVEST` with `inTransport=false` and calls `Units.harvestSpiceStep`; iterates REFINERY slots with `linkedID != 0xFF` and calls `Structures.refineSpiceStep`. Flushes the mutated SpiceMap back to the host. Invoked from `tick()` once every `Scheduler.harvestCadenceTicks` (3 scheduler ticks ≈ 250 ms at 12 Hz, rough match for OpenDUNE's `script.delay=6` at ~30 Hz). Gated off when either `spiceMap` or `harvestRNG` is nil — existing tests untouched. `ScenarioScene.setUpScheduler` seeds SpiceMap from the snapshot via `TileResolver.landscapeType` (logs thick/thin counts under `scene`), wires `harvestRNG` to the scripting `RandomSource.tools.next` so the `Tools_Random_256` stream is shared with existing script functions. Logs: `harvest-tick` summary (counter + harvested + refined-pairs) when anything happens. 5 new tests in `SpiceTickHarvestTests` (HARVEST-action picks up spice, docked pair accrues credits, non-HARVEST skipped, cadence gate fires only every 3rd tick, auto-disable without rng/spiceMap). **732 green / 73 suites / zero warnings on clean build.** Design: `Algorithms/HarvesterSpiceDeposit.md` §"Slice 5".

- **Runtime spice map (slice 4 of spice income).** New `Simulation.SpiceMap` value type — 64×64 per-tile `Level` enum {notSand, bare, thin, thick} with `apply(delta:at:) -> Level` porting OpenDUNE's `Map_ChangeSpiceAmount` transitions (`src/map.c:771..797`): THICK→THIN→BARE on drain, BARE→THIN→THICK on add, gated so THICK+pos / BARE+neg / notSand are no-ops. `init(landscapeAt:)` seeds from a closure so tests pass stubs without building a `TileResolver`. `landscapeByte(at:)` bridges to the `UInt8`-shape `harvestSpiceStep`'s `landscapeAt` closure expects. Every level transition logs under the `spicemap` tracer with before→after + delta. `Map_FixupSpiceEdges` (visual neighbour-tile smoothing) stays on the scene side. 7 new tests in `SpiceMapTests` (drain chain, add chain, zero-delta no-op, notSand inert, closure-based init, landscapeByte mapping, end-to-end with harvestSpiceStep driving THICK→THIN→BARE + next-call reject). **727 green / 72 suites / zero warnings on clean build.** Design extended in `Algorithms/HarvesterSpiceDeposit.md` §"Slice 4".

- **Harvester on-spice pickup (slice 3 of spice income) + Package.swift CLAUDE.md excludes.** New `Simulation.Units.harvestSpiceStep(harvesterIndex:units:landscapeAt:changeSpice:rng:) -> UInt16` ports OpenDUNE's `Script_Unit_Harvest` (`src/script/unit.c:1640..1669`) as a pure function. Per call: gate on `amount<100` + tile landscape in {spice, thickSpice}; `amount += rng() & 1`; `inTransport=true`; clamp 100; `(rng() & 0x1F) != 0` → return 1 (no drain), else call `changeSpice(packed, -1)` and return 0. Map state stays outside the sim — tile reader / writer / RNG injected via closures (lets tests pin sequences + lets the scene's runtime map grid own storage when wired later). Every step logs under the `harvest` tracer. Also fixed Package.swift: the 5 per-module CLAUDE.md files added earlier today produced 5 "unhandled resource" warnings on clean build; added `exclude: ["CLAUDE.md"]` to each target. 10 new tests in `HarvestSpiceStepTests` (jitter 0/1, drain-tick triggers and RNG scripting, 5 gate rejections: cap, off-spice, wrong type, out-of-range, off-map position; accumulation-over-time pins a 400-tick run to amount=100). **720 green / 71 suites / zero warnings on clean build.** Design extended in `Algorithms/HarvesterSpiceDeposit.md` §"Slice 3".

- **Harvester dock / undock primitives (slice 2 of spice income) + logs across recent slices.** Two new pure-sim functions on `Simulation.Structures`: `dockHarvester(refineryIndex:harvesterIndex:structures:units:) -> Bool` ports the allied-unit branch of OpenDUNE's `Unit_EnterStructure` (`src/unit.c:2177..2225`) — chain-links harvester.linkedID ← prior refinery head, refinery.linkedID ← harvester pool index, harvester.inTransport ← true, refinery.state ← READY (busyStateIsIncoming). `undockHarvester(refineryIndex:exitTile:structures:units:) -> Int?` ports `Script_Structure_FindAndLeaveUnit`'s unlink dance (`src/script/structure.c:273..283`) — pops the head harvester off the chain, positions it at `exitTile` (tile×256+128 centred), clears `inTransport`, and flips refinery to IDLE when the chain empties. Neither touches `actionID` — post-unload behaviour is the caller's decision, keeps these helpers reusable. Every step emits `Log.info` under the `dock` tracer. Also retrofitted logs on `refineSpiceStep` (`spice` tracer — per-tick before→after credits + amount, UNLOAD_COMPLETE on empty), `startConstruction` / `cancelConstruction` / `completeConstruction` (`construction` tracer), `createUnit` (`unit-create`), `setRallyPoint` + rally-fire on completion (`rally` tracer). Memory updated: new convention "add logs after each step" in feedback. 10 new tests in `HarvesterDockTests` (dock happy path, chain-link 2nd harvester, 3 rejection paths, undock single, undock chained, empty-chain-returns-nil, off-map-rejected, end-to-end 3-tick cycle). **710 green / 70 suites / zero warnings on clean build.** Design extended in `Algorithms/HarvesterSpiceDeposit.md` §"Slice 2".

- **Harvester → refinery spice deposit (slice 1 — pure-sim refine step).** First slice of the P4→P5 economy bridge. New `Simulation.Structures.refineSpiceStep(refineryIndex:harvesterIndex:structures:units:houses:playerHouseID:enemyJitterByte:)` ports the per-tick math in OpenDUNE's `Script_Structure_RefineSpice` (`src/script/structure.c:105..153`) as a pure function: computes `harvesterStep = (refinery.hp * 256 / hpMax) * 3 / 256` clamped by `harvester.amount`, flat `creditsStep = 7` for player harvesters (+optional −1..+2 jitter for enemies via a closure), credits the owning house `creditsStep × harvesterStep`, decrements amount, clears `inTransport` when amount reaches zero. Caller passes both pool indices explicitly — no dock/undock AI, no linkedID management, no script wiring; those follow in later slices. HP scaling is OpenDUNE-faithful int math: full-HP refinery drains 3/tick, half-HP 1/tick, ≤33% HP 0/tick. Credits saturate at `UInt16.max` (`HousePool.credits` is UInt16). 13 new tests in `HarvesterSpiceDepositTests` (happy path, drain-to-zero, zero-amount no-op, half-HP step, low-HP stops, 3 rejection paths — wrong refinery type / wrong unit type / cross-house, out-of-range, enemy jitter min/max, player ignores jitter, saturation). **700 green / 69 suites / zero warnings on clean build.** Design: `Algorithms/HarvesterSpiceDeposit.md`. Deferred to later slices: dock/undock script wiring, spice-tile mutation, carryall pickup path, credit roll-up animation.

- **Rally-point click on factories.** New player-facing feature: right-clicking a map tile with a player-owned factory (LIGHT_VEHICLE / HEAVY_VEHICLE / HIGH_TECH / WOR / BARRACKS) selected stamps a rally tile on that factory. Each subsequent unit completing from the factory spawns at `factorySpawnTile` and immediately receives `Simulation.Units.orderMove(rally)`, so it walks off on its own. New `StructureSlot.rallyPointPacked: UInt16` (`0xFFFF` sentinel = unset); new `Simulation.Structures.setRallyPoint(yardIndex:tile:pool:)` — validates factory kind + on-map bounds, `tile: nil` clears. `completeConstruction` reads the rally on success and fires the move order after the spawn. Scene wiring: `rightMouseDown` branches — unit-command first (selection → move/attack), then rally-point when a factory is selected and nothing's highlighted. Yellow diamond `SKShapeNode` marker at the rally tile; rebuilt when the selected yard changes or the rally is updated. This feature does not exist in OpenDUNE — our own layer on top of the existing spawn path. 11 new tests in `FactoryRallyPointTests` (default sentinel, allocate/setter, six rejection paths, completion with/without rally, rally persists across two builds). **687 green / 68 suites / zero warnings on clean build.** Design: `Algorithms/FactoryRallyPoint.md`.

- **Repo structure: per-module CLAUDE.md files + drop strict-TDD workflow.** Each SwiftPM target now carries its own `CLAUDE.md` — `Code/Core/Sources/{DuneIICore,AssetExport,DuneIIRendering,assetgen,duneii}/CLAUDE.md` — covering that module's layout, conventions, and key entry points. Root `CLAUDE.md`'s "Layout" section points at them. Feature workflow relaxed: implementation now precedes tests (both still ship in the same commit; the coverage bar is unchanged). "Write the failing test first" step removed; "what counts as tested" retained. No code changes.

- **Wire EMC-driven SetSpeed for ground units (GetInfo 0x0B misport + `Unit_StartMovement` SetSpeed port).** Two-part fix so player-ordered MOVE orders no longer crawl at 4 px/tick. (1) `Script_Unit_GetInfo` subcase `0x0B` was reading `slot.targetMove` instead of `slot.currentDestinationX/Y` — OpenDUNE's `src/script/unit.c:968` checks `currentDestination`, a different field (pixel-level per-step goal vs. ultimate goal). On tick 1 of a fresh MOVE, `targetMove != 0` while `currentDestination == (0,0)`, so our misport returned 1 ("already moving") and the MOVE handler at UNIT.EMC word 637 looped forever on the "already moving" wait — `Script_Unit_CalculateRoute` was never reached. (2) Our `makeCalculateRouteUnit` intentionally skipped OpenDUNE's `Unit_StartMovement` call (`src/unit.c:1345 → 1105`), which is where `Unit_SetSpeed` reads `g_table_landscapeInfo[landscape].movementSpeed[movementType]` and writes `unit->speed`. Even if the script *had* reached CalcRoute, speed would have stayed zero. Fix ports the landscape-speed slice of `Unit_StartMovement` (`unit.c:1088..1105`) directly into our CalcRoute's step-consume path: landscape lookup via a new `Scripting.Host.landscapeAt` closure, `movementSpeed[movementType]` indexing, HP<half 1/4 slowdown for non-winger units, `byScenario` scaling. `ScenarioScene.setUpScheduler` wires the closure from the snapshot tile grid via `TileResolver.landscapeType`. 4 new tests (1 on GetInfo 0x0B, 3 on CalcRoute: landscape-set, HP<half reduction, nil-closure regression guard). 675 green / 67 suites / zero warnings on clean build. Design: `Algorithms/EmcDrivenSetSpeed.md`. Insight: `scripting-unit-getinfo-0b-is-currentdestination.md`. Simplifications: we write `slot.speed` directly, bypassing `Unit_SetSpeed`'s `movingSpeedFactor` / gameSpeed / speed-vs-speedPerTick split — a sub-pixel-accurate accumulator is queued for the tick-parity golden harness.

- **P5 slice 6d: HUD credits label.** First user-visible read-out of the credits state that's been live in `HouseSlot.credits` since slice 6a. New `Simulation.House.credits(for:in:)` pure-sim helper (returns `UInt16?`; `nil` distinguishes "no such house" from "0 credits"). `ScenarioScene.creditsLabel` is an `SKLabelNode` pinned at the top of the right-hand sidebar (white, 14 pt, centered above the BUILD header); refreshed every tick from `refreshHud()` already on the per-tick path. Defaults to "Credits: —" when the player house slot isn't allocated (defensive). Mission 1 Atreides starts at "Credits: 1000" (`SCENA001.INI` `[Atreides] Credits=1000`) and ticks down as BUSY yards drain (`buildCredits/buildTime` per tick). 5 new tests in `HouseCreditsLookupTests` (non-zero, zero-not-nil, unallocated, freed-still-readable per `House_Free` quirk, out-of-range). 671 green / 67 suites / zero warnings on clean build (was 666). Design: `Algorithms/HudCreditsLabel.md`. Deferred: silo capacity readout (needs `creditsStorage` to be live), digit roll-up animation, quota / score display.

- **Right-click-attack on enemy units (slice 2).** Builds on the slice-1 unit-command bridge. New `Simulation.Units.orderAttack(poolIndex:targetUnitIndex:units:)` composes `Unit_SetAction(ACTION_ATTACK)` + `Unit_SetTarget` (`src/unit.c:497, 1131`) into a single pure-sim write: sets `targetAttack = EncodedIndex.unit(targetUnitIndex)`, `actionID = attack` (0), zeros `currentDestination{X,Y}` so the scheduler reloads the engine at the ATTACK entry-point next tick. Non-turret attackers (TROOPER, TRIKE, QUAD, DEVASTATOR) also get `targetMove = targetAttack` + `route[0] = 0xFF` so the chassis chases; turreted units (TANK, SIEGE_TANK) leave move state alone and rotate the turret in place — matches `Unit_SetTarget`'s `!hasTurret` arm at `unit.c:1161`. Rejection paths: out-of-range, unallocated, freed, self-attack — pool untouched on `false`. Controller side: new `Action.orderAttack(attackerIndex:targetIndex:)` case + `enemyUnitAtTile` scan; the right-click handler now discriminates between empty / friendly / enemy under the click, promoting a move order to an attack order when the click lands on a non-player unit. Slice-1 right-click-on-empty / right-click-on-friendly semantics preserved (regression-guarded). `ScenarioScene.applyCommandAction` gets the new case; logs `unit-order-attack unit=N target=M ok=true` via the `unit-cmd` tracer. 14 new tests (8 in `UnitOrderAttackTests`: turreted vs non-turreted happy paths + 6 rejection / overwrite cases; 6 added to `UnitCommandControllerTests` for the right-click branches). 666 green / 66 suites / zero warnings on clean build (was 652). Design: `Algorithms/UnitOrderAttack.md`. Deferred: enemy-structure attack, target blink + voice cue, Fremen↔Atreides alliance check.

- **Fix: sprite "blink" on off-axis route-following — lock orientation to `route[0] * 32`.** User report: units moving mostly north with a small westward offset flashed the NW-mirror sprite between N-sprite frames. Root cause: `Scheduler.tickMovement` recomputed `orientationCurrent` every tick from the continuous pos32 delta `(goal − position)`, which straddled the N↔NW octant boundary (byte ≈ 240) when the unit was a few pixels off the tile centerline. The sprite atlas mirrors octants 5..7 with `xScale < 0`, so the boundary crossing toggled `flipHorizontal` — visibly a blink. Fix: while following a route step, `orientationCurrent = route[0] * 32` (canonical octant midpoint — the same alignment `Script_Unit_CalculateRoute` performs when filling the route). Continuous recompute retained only for the `targetMove`-fallback path (rare: first tick after `orderMove`, and carryall `SetDestinationDirect`). Defensive guard: fall back to continuous direction if `currentDestination` is set but `route[0] == 0xFF` (possible on save restore). 1 new test in `NaiveMovementTests`: `routeOrientationStableOffAxis` pins a Trike 6 px west of a tile centerline, gives it `route[0] = 0`, ticks 4 times, asserts `orientationCurrent == 0` throughout. 653 green / 65 suites / zero warnings on clean build. Insight: `simulation-route-orientation-locked-to-step.md`.

- **Movement-tracing log labels: `move`, `route`, `sel`.** Follow-up to the movement triage. Added structured per-unit logs so the next time motion misbehaves the trace is already in the log file. (1) `Simulation.Units.orderMove` logs at info with (unit, type, house, current pos, target tile, encoded raw, prior action + targetMove). (2) `Scheduler.tickMovement` gained `move-step` (picking a route step, debug), `move-abort` (invalid targetMove, debug), `move-arrived` (route-exhaust or fallback arrival, info), `move-pop` (route step consumed, debug), and `move-tick` (per-tick compact state trace, verbose — gated on `DUNEII_LOG_VERBOSE=1`). (3) `Script_Unit_CalculateRoute` logs `route` events — `src==dst` early exit (debug), no-route (info), route filled with step count + steps list (info), partial truncate (debug). (4) `ScenarioScene` emits `sel-tick` (debug, per-tick) for the currently-selected unit only — keeps the trace narrow when diagnosing one player-issued order. Default log level stays `.debug`, so everything except `move-tick` lands in `Logs/run-*.log` without flags. No test or behaviour changes; 652 green / 65 suites / zero warnings on clean build.

- **Fix: units moved wrong + "blinked" — scheduler was loading wrong script entry + IdleAction over-rotated body + unported SetActionDefault.** Three bugs uncovered by the unit-command slice's player-ordered moves. (1) **Scheduler entry-point**: `Simulation.Scheduler.tickUnits` was calling `unitVM.load(typeID: slot.actionID)`, landing the PC at `UNIT.EMC`'s `entryPoints[action]` — a completely different unit type's entry (action=1 → ornithopter's entry, etc.). OpenDUNE loads `entryPoints[u->o.type]` and sets `variables[0] = action` separately (`src/unit.c:520..521`). Fixed: scheduler now passes `slot.type` as `typeID` and overrides `unitEngines[idx].variables[0] = UInt16(actionID)` after load. One existing test (`schedulerLoadsEntryPointOnActionChange`) rewritten to match reality — now asserts `variables[0] == actionID` + engine doesn't halt for valid types. Renamed to `schedulerLoadsEntryPointByType`. (2) **Script_Unit_SetActionDefault (slot 0x0A) ported**. Reads `UnitInfo.actionsPlayer[3]` (typically `guard_` for combat units) and writes it to `slot.actionID` + clears `currentDestination`. Without this, the MOVE script couldn't transition back to GUARD on arrival — the engine looped on a NoOp. Wired into `FunctionTables.swift`; 2 new tests (happy path + no-current-object). (3) **IdleAction over-rotating body**. Our port wrote the randomised orientation to `orientationCurrent` 100% of the time; OpenDUNE picks `i = (byte & 1) == 0 ? 1 : 0` and writes to `orientation[i]` — turret when even. For non-turret units (trike, infantry) the `i == 1` case is a no-op, so body orientation only changes ~50% of calls. Fixed to match; 1 new test. Total: 652 tests / 65 suites / zero warnings on clean build (was 649). Insight: `scripting-unit-entry-point-by-type-not-action.md` — the subtle "by type not action" gotcha for any future unit VM loader. Visual verification pending from the user.

- **Unit selection + move orders (slice 1).** First input-layer slice — the player can now command built units. `Simulation.Units.orderMove(poolIndex:tileX:tileY:units:)` is a pure-sim bridge: writes `targetMove` as a tile-encoded index, sets `actionID = ActionID.move` (1), clears `currentDestination{X,Y}` + `route[0]`. The scheduler's per-slot `loadedUnitAction` delta-check (`Scheduler.swift:253`) reloads the engine at the ACTION_MOVE entry on the next tick — no explicit `Script_Reset` / `Script_Load` needed. New `DuneIIRendering.UnitCommandController` is a pure state machine (parallel to `BuildPanelController`): `selectedUnitIndex` + `handle(click:pool:playerHouseID:)` with `Click.leftMapTile` / `rightMapTile` and `Action.{none, selectUnit, deselect, orderMove}`. Friendly-only selection; left-click empty terrain with a selection deselects; right-click with a selection issues a move order; selection persists across orders; stale selection (freed slot) auto-clears on next click. `ScenarioScene` wiring: `mouseDown` routes map clicks to the command controller first; `.selectUnit` consumes, `.deselect` / `.none` fall through so yard-select + build-panel still run. New `rightMouseDown(with:)` override forwards to the controller for move orders. Green selection halo is an `SKShapeNode` circle parented to the selected unit's marker (naturally tracks the unit as it walks). Per-tick `validateSelectionHalo()` drops halo + selection when the selected unit dies. 18 new tests in `UnitOrderMoveTests` + `UnitCommandControllerTests` (7 orderMove paths including scheduler integration; 11 controller state transitions). 649 green / 65 suites / zero warnings on clean build. Design: `Algorithms/UnitSelectionAndOrders.md`. Insight: `simulation-action-id-drives-script-reload.md` — writing `slot.actionID` is enough to re-enter the script. Deferred: drag-select, shift-additive, right-click-attack on enemies, right-click-harvest on spice, rally-point click on factories.

- **Boot flow: skip MainMenu + Mentat; fix map-click scene-cycle bug.** User-reported: in `swift run duneii` clicks on the scenario map were cycling through mentat → shaded-mentat → map instead of selecting/placing. Two fixes, no sim changes. (1) `ScenarioScene.mouseDown`'s `.none` branch previously called `coordinator?.route(to: .mainMenu)` as a pre-slice-3 fallback — replaced with `break`. Unclassified clicks (empty sidebar row, map tile with no yard, off-map) now do nothing, as they should during play. (2) `GameController.start()` + `advance(from:)` now go Intro → Scenario directly, skipping `MainMenuScene` (MENTATA.CPS) + `MentatScene` (tinted mentat). The two scene types still exist but aren't in the boot chain — they'll be revisited once P7 campaign glue needs them. Moved the `DUNE0.XMI` jukebox kickoff to scenario entry so background music still fires. No new tests (routing is GUI-level; manual-verification only). 631 green / 63 suites / zero warnings on clean build. Also wrote `Documentation/Algorithms/UnitSelectionAndOrders.md` — design for the next slice (left-click select friendly unit, right-click order move), sized for one session.

- **P5 slice 6c — factory credit drain + refund (symmetric with CY).** Extends slice 6b's drain / refund paths to the 5 factory yards. `Simulation.UnitInfo.buildCredits` added (27 rows from `src/table/unitinfo.c`: Carryall=800, MCV=900, SOLDIER=60, Trike=150, Siege Tank=600, etc.; projectiles 18..26 = 0). Added with default 0 to the memberwise init; 17 non-zero rows updated. New private `Simulation.Structures.producedCost(slot:) -> (UInt16, UInt16)?` helper dispatches by yard kind — CY → StructureInfo, factory → UnitInfo; both `tickConstruction` and `cancelConstruction` route through it so the drain + refund math is shared between structure and unit production. BARRACKS + SOLDIER = 60/32 = 1 credit/tick; LV + TRIKE = 150/40 = 3 credits/tick; factory pauses identical to CY when house can't pay. Cancel refund at half-built BARRACKS + SOLDIER = 30 credits (16 × 60 / 32). 5 new tests. 631 green / 63 suites / zero warnings on clean build. Deferred: `buildCostRemainder` fractional accumulator, storage cap enforcement, HUD label. Design: `Algorithms/CreditDrainFactory.md`.

- **P5 slice 6b — credit drain during BUSY + cancel refund (CY path).** `Simulation.Structures.tickConstruction` and `cancelConstruction` gained required `houses: inout HousePool` parameters. On every BUSY construction yard with a live `objectType`, each tick deducts `costPerTick = buildCredits / buildTime` from the owning house's credits (`StructureInfo` lookup via `objectType`; 300/48 = 6 credits/tick for WINDTRAP). If the house can't pay, the yard stays BUSY and `countDown` doesn't advance — effectively pausing construction until credits arrive. `cancelConstruction` now refunds credits proportional to progress: `refund = ticksSpent × buildCredits / buildTime` — matches OpenDUNE's `Structure_CancelBuild` formula. Factory drain + factory refund are deferred to slice 6c (requires `UnitInfo.buildCredits`, 27 rows). `Scripting.Host` grew a `houses: HousePool` field so the scheduler + scene can reach live house state; `Scheduler.tick` passes it to `tickConstruction`; `ScenarioScene.cancelConstructionOnYard` passes it to `cancelConstruction`. `ScenarioScene.setUpScheduler` initialises `host.houses` from `snapshot.houses`. 6 new tests (drain happy path, pause on no-credits, refund fresh/half/ready/objectType-unset). 626 green / 63 suites / zero warnings on clean build. Deferred: factory drain, `buildCostRemainder` fractional accumulator, storage cap, HUD. Design: `Algorithms/CreditDrainAndRefund.md`.

- **P5 slice 6a — HouseSlot credit plumbing.** Pure-sim groundwork for the HUD/economy slices. `Simulation.HouseSlot` grew three `UInt16` fields — `credits`, `creditsStorage`, `creditsQuota` — all defaulted to 0. Plumbed through both `WorldSnapshot` init paths: scenario path copies `credits` + `creditsQuota` from `Scenario.HouseLayout` (clamped from `Int`); save path copies all three from `Formats.Save.Players.Slot`. `creditsStorage` stays 0 on the scenario path (derived from refinery count at runtime — not yet wired). No drain, no UI, no cancel refund — those land with slice 6b and 6c. 3 new tests in `SimulationWorldSnapshotTests` (defaults, scenario seed, save round-trip). 620 green / 63 suites / zero warnings on clean build. Design: `Algorithms/HouseCredits.md`.

- **P5 slice 5c — cancel, queue-swap, rally tile.** Closes the last 5b UX gaps. `Simulation.Structures.cancelConstruction(yardIndex:pool:)` resets a BUSY / READY yard to IDLE (state + objectType + countDown all cleared; OpenDUNE credit-refund deferred with `House` economy). `Simulation.Structures.factorySpawnTile(yardType:anchorX:anchorY:)` returns `(x, y+h)` (south of footprint) with anchor fallback on out-of-bounds; `completeConstruction` now routes through it so units spawn below the factory rather than on top of it. `BuildPanelController` grew a `.cancelConstruction(type:)` action: BUSY + click on queuedType → cancel; BUSY + click on different type → `.none` (no mid-build swap, matching OpenDUNE); READY + click on different type → `.enqueue(type:)` (queue-swap — replaces the ready item with a fresh build). `ScenarioScene.mouseDown` dispatches the new action to a `cancelConstructionOnYard` helper. 11 new tests + 2 existing tests updated for new semantics (BUSY + queued-type now returns `.cancelConstruction` not `.none`; BARRACKS SOLDIER spawn now at (5, 7) not (5, 5)). 617 green / 63 suites / zero warnings on clean build. Deferred: credit refund on cancel, collision-aware spawn tile (bumps another unit), click-to-rally. Design: `Algorithms/BuildPanelTightening.md`.

- **P5 slice 5b-build — `UnitInfo.buildTime` + unit spawn on factory completion. The factory loop now closes end-to-end.** `Simulation.UnitInfo.buildTime: UInt16` added (27 values from `src/table/unitinfo.c`: Carryall=64, Siege Tank=96, Trike=40, Soldier=32, etc.; projectiles 18+ all zero). `Simulation.Structures.startConstruction` now dispatches the countdown source by yard kind — CY uses produced structure's `buildTime`; factory uses produced unit's `buildTime`. New `Simulation.Units.createUnit(type:houseID:tileX:tileY:pool:)` — narrow port of `Unit_Create` covering the factory-spawn path (allocateForType + seed hitpoints + centered pos32 + seenByHouses=0xFF + winger speed=255). New `Simulation.Structures.completeConstruction(yardIndex:pool:unitPool:)` ports the READY→spawn tail of OpenDUNE's factory flow: spawns the queued unit at the yard's anchor tile (rally-point heuristic deferred), flips yard IDLE, resets objectType + countDown. On unit-pool-full failure the yard is left READY so the player can retry. CY completion still routes through `commitPlacement` (click-map-to-place). `ScenarioScene.mouseDown` routes READY factory `.enterPlacement` clicks to a new `completeFactoryProduction(type:)` helper that invokes `completeConstruction` and refreshes the sidebar. 11 new tests in `FactoryBuildableTests` (UnitInfo.buildTime pinned; LV+TRIKE / BARRACKS+SOLDIER unit-buildTime countdown; CY+WINDTRAP unchanged; Units.createUnit happy/reject paths; completeConstruction happy path + BUSY no-op + CY nil + REFINERY nil). 607 green / 63 suites / zero warnings on clean build. Deferred: rally-point / south-of-factory exit tile, credit drain, `Structure_CancelBuild`, `Unit_Create`'s Script_Load + fog + MCV deploy. Design: `Algorithms/FactoryUnitSpawn.md`.

- **P5 slice 5b — factory UI: yard switching on map click + sidebar dispatches CY↔factory.** Clicking a player-owned construction yard or factory on the map switches `selectedYardIndex`; sidebar re-populates with that yard's buildable. Factory sidebar rows render unit icons (from `UnitSpriteAtlas.texture(at:houseID:)`) + abbreviated unit labels; CY rows stay as before (ICN iconGroup). `Simulation.Structures.selectableYardAt(tileX:tileY:pool:playerHouseID:)` is a pure helper returning the first player-owned CY/factory whose footprint covers a tile (STARPORT excluded — deferred). `startConstruction` relaxed to accept the 5 factory yards (3/4/5/7/10) in addition to CYARD (8); countdown source dispatches by yard kind (CY → produced structure's buildTime; factory → yard's own buildTime as placeholder until `UnitInfo.buildTime` lands in slice 5b-build). New `Simulation.UnitInfo.buildableUnitTypes(from:)` mirrors the structure-side helper. `ScenarioScene` tracks a `YardKind` enum (structure / unit) and dispatches `refreshBuildSidebar` to the right buildable query; `renderSidebar` branches sprite lookup. READY factory clicks intercepted in scene: clear `placementType` and log "unit spawn deferred" instead of entering map-placement (which would misfire `Structures.create` with a unit-type ID as a structure-type). 13 new tests in `FactoryBuildableTests` (selectableYardAt — CY / LV hit, REFINERY / STARPORT miss, enemy miss, outside miss; `startConstruction` on LV/BARRACKS/REFINERY; `buildableUnitTypes` bitmask). 596 green / 63 suites / zero warnings on clean build. Deferred to slice 5b-build: `UnitInfo.buildTime` + correct factory countdown, unit spawn on completion. Design: `Algorithms/BuildPanelFactoryUI.md`.

- **P5 slice 5a — factory buildable units (pure sim; no UI yet).** Pure-sim half of the factory panel work: extends `Structure_GetBuildable` to the 5 factory cases (LIGHT_VEHICLE / HEAVY_VEHICLE / HIGH_TECH / WOR_TROOPER / BARRACKS). `Simulation.UnitInfo` gained three fields — `availableHouse: UInt8`, `structuresRequired: UInt32`, `upgradeLevelRequired: UInt8` — with `flagAll` / 0 / 0 defaults. 17 of 27 rows updated where OpenDUNE values differ from defaults (Thopter=62/IX/1, Launcher=59/0/2, Siege Tank=ALL/0/3, Devastator=57/IX/0, Sonic Tank=58/IX/0, Sandworm=FREMEN, Death Hand=HARKONNEN, etc.). `Simulation.StructureInfo.buildableUnits: [UInt8]` added with `[0xFF × 8]` default; 5 factory rows populated with their production lists (LV=[TRIKE, QUAD], HV=[SIEGE, LAUNCHER, HARVESTER, TANK, DEVASTATOR, DEVIATOR, MCV, SONIC], HT=[CARRYALL, ORNITHOPTER], WOR=[TROOPER, TROOPERS], BARRACKS=[SOLDIER, INFANTRY]). New `Simulation.Structures.buildableUnitsFromFactory(factoryType:factoryHouseID:factoryUpgradeLevel:structuresBuilt:)` returns a UNIT-type bitmask. Two Ordos quirks ported verbatim: LV's TRIKE becomes RAIDER_TRIKE; HV's SIEGE_TANK `upgradeLevelRequired` drops from 3 to 2. STARPORT returns 0 (deferred — OpenDUNE uses `(uint32)-1` backed by `g_starportAvailable`). CY / non-factory types also return 0. Explicit memberwise init on both `UnitInfo` and `StructureInfo` so default-valued fields don't force all 27/19 rows to spell them out. 22 new tests in `FactoryBuildableTests` (per-field data pins, per-factory buildableUnits arrays pinned, LV/HV/BARRACKS + Ordos specialty gates, IX-prereq gate, non-factory returns 0). 583 green / 63 suites / zero warnings on clean build. Design: `Algorithms/FactoryBuildable.md`. Slice 5b (sidebar UI for factory yards + yard-select click) is the next step.

- **P5 slice 4d-ui — build panel surfaces BUSY/READY + progress bar + gates clicks.** The player now experiences construction: clicking a sidebar slot on an IDLE yard calls `Simulation.Structures.startConstruction`; the yard goes BUSY; the sidebar shows a yellow progress bar on the queued slot that fills over ~4 seconds for a WINDTRAP; the slot border turns green at READY; clicking READY → enters placement mode; map-clicks commit as before (slice 3/4a-c gates unchanged). `BuildPanelController` grew four fields (`yardState`, `queuedType`, `countDown`, `buildTime`), a `progress: Double?` computed property, a `refreshYardState(_:queuedType:countDown:buildTime:)` method, and a new `.enqueue(type:)` action. `handle(click:)` branches on yardState: IDLE/nil → `.enqueue`; BUSY → `.none`; READY + matching queuedType → `.enterPlacement`; READY + different type → `.none` (no queue-swap in 4d-ui; 4e territory). `ScenarioScene.refreshBuildSidebar` now also updates controller yard state from pool data; `renderSidebar` draws progress bar + READY highlight; `update(_:)` calls refresh after each scheduler tick so progress animates at 12 Hz. `enqueueConstruction(type:)` routes the new action to the sim. Three slice-3 tests removed (`sidebar click enters placement`, `re-pick`, the original `map commits`) — superseded by the 4d-ui state-driven path; one (`outside no-op`) adapted to use the READY → enter path. 7 new tests in `BuildPanelTests` (IDLE → enqueue, nil state → enqueue, BUSY → no-op, READY on queued → enterPlacement, READY on different type → no-op, READY → placement → commit round-trip, progress math at 0/half/full). 561 green / 62 suites / zero warnings on clean build. Deferred: cancel on BUSY, queue-swap on READY, per-house credit drain, map-click yard switch. Design: `Algorithms/BuildPanelProgress.md`.

- **P5 slice 4d-sim — construction countdown state machine.** Pure-sim half of slice 4d: yards flip BUSY, tick drains countdown, flip to READY at zero. `StructureInfo.buildTime: UInt16` added + populated for all 19 rows (WINDTRAP=48, REFINERY=80, PALACE=130, etc. — per OpenDUNE `src/table/structureinfo.c`). New `Simulation.StructureState` enum mirrors OpenDUNE's `STRUCTURE_STATE_*` constants (`detect=-2`, `justBuilt=-1`, `idle=0`, `busy=1`, `ready=2`); existing raw `Int16` comparisons keep working. `Simulation.Structures.startConstruction(yardIndex:objectType:pool:)` ports the tail of `Structure_BuildObject` — flips CY state to BUSY, sets `objectType` + `countDown = buildTime << 8`. Rejects non-yard slots, out-of-range `objectType`, already-BUSY yards. `Simulation.Structures.tickConstruction(pool:)` ports the `GameLoop_Structure` countdown decrement — each BUSY yard drops `countDown` by 256 per tick and flips to READY when it reaches zero. `Simulation.Structures.defaultBuildSpeed = 256` constant. `Scheduler.tick()` gains a `Structures.tickConstruction` pass between `tickMovement` and `tickUnits`. Deferred to slice 4d-ui: sidebar progress indicator + gating map-click commits on READY state. Deferred further: credit drain, game-speed scaling, `Structure_CancelBuild`, onHold, upgrade path, starport flow, pre-allocated placeholder structures (OpenDUNE pattern we're skipping in favour of allocate-at-commit). 14 new tests in `StructureConstructionTests` (buildTime table parity, state constants, startConstruction happy/rejection paths, tickConstruction BUSY→READY transitions, full 48-tick WINDTRAP build end-to-end). 557 green / 62 suites / zero warnings on clean build. Design: `Algorithms/StructureConstruction.md`.

- **P5 slice 4c — HP degradation on placement + adjacent-to-player-base gate.** Closes the two remaining checks in `Structure_IsValidBuildLocation` + `Structure_Place`. `StructureSlot.degrades: Bool` added + plumbed from `Formats.Save.ObjectFlags.degrades` (bit `0x0400`) in `WorldSnapshot`'s save init. `Structures.create` gained `tilesWithoutSlab: Int = 0` parameter; when > 0 applies the OpenDUNE HP math `hitpoints -= (hitpointsMax/2) * tilesWithoutSlab / footprintCount` and sets `degrades = true`. `StructureLayout.adjacentOffsets` ports `g_table_structure_layoutTilesAround` (clockwise ring: N+NE → E → S+SE+SW → W → NW; 8..16 entries per layout). `Structures.isValidBuildLocation` gained `playerHouseID: UInt8?` + `tileHouseIDAt: ((Int, Int) -> UInt8)?` parameters; when both non-nil and structure isn't CY, runs the adjacency gate — new placements must have at least one surrounding tile containing a player-owned pool structure OR a concrete-slab / wall tile with `houseID == playerHouseID`. Private `Structures.structureOwnerAt(pool:tileX:tileY:)` helper. `ScenarioScene.commitPlacement` wires `tileHouseIDAt` from snapshot tiles + `playerHouseID` from the scene constant; passes `-validity` as `tilesWithoutSlab` on degraded placements. 13 new tests (4 HP-degradation cases in `StructureCreateTests`; 9 adjacency + layout-table cases in `BuildValidationTests`, including CYARD skips gate, enemy doesn't count, concrete-slab-player fallback). 543 green / 61 suites / zero warnings on clean build. Deferred: per-tick HP decay consuming the `degrades` flag, live landscape updates on placement (`LST_STRUCTURE` / `LST_CONCRETE_SLAB` stamping), construction countdown state machine. Design: `Algorithms/BuildValidationAdjacency.md`.

- **P5 slice 4b (landscape) — landscape gate + slab-prereq count + `isValidForStructure2` + `notOnConcrete`.** `Simulation.LandscapeInfo` grew `isValidForStructure2: Bool` (narrower than `isValidForStructure`; true only for ENTIRELY_ROCK / MOSTLY_ROCK / DESTROYED_WALL). Fixed three mis-ports in the existing table uncovered while cross-checking: `5 MOSTLY_ROCK` and `13 DESTROYED_WALL` had `isValidForStructure=false` (should be true); `4 ENTIRELY_ROCK`, `5 MOSTLY_ROCK`, `9 THICK_SPICE` had `letUnitWobble=false` (should be true). `Simulation.StructureInfo` grew `notOnConcrete: Bool` (defaulted false; true only for CONSTRUCTION_YARD); explicit memberwise init added so the 18 non-CY rows don't need to pass the flag. `Simulation.Structures.isValidBuildLocation` now takes an optional `landscapeAt: ((Int, Int) -> LandscapeType)?` closure; when non-nil, every footprint tile is validated against the structure's landscape gate (isValidForStructure for non-CY; isValidForStructure2 for CY) and tiles not already on concrete contribute to `neededSlabs`. Return is `0` / `1` / `-neededSlabs`. `ScenarioScene` now stashes `tileGrid` at scene build, and `commitPlacement` constructs a landscape closure from `tileGrid` + `TileResolver`. Negative validity logs "commit degraded slabs_needed=N" but still succeeds; slice 4c applies HP degradation. 10 new tests in `BuildValidationTests` (table invariants, landscape-gate sand/rock/concrete/mixed, CYARD's concrete-is-invalid quirk, nil closure preserving slice-4a semantics). 530 green / 61 suites / zero warnings on clean build. Deferred: HP degradation on create, adjacent-to-player-structure gate, landscape updates from placement. Design: `Algorithms/BuildValidationLandscape.md`.

- **P5 slice 4a — `sortPriority` ordering + minimal `Structure_IsValidBuildLocation`.** `Simulation.StructureInfo` grew `sortPriority: UInt16` populated for all 19 rows from `src/table/structureinfo.c` (CYARD=0, SLAB_1x1=2, …, HOUSE_OF_IX=34). New `buildableTypesByPriority(from:)` helper sorts by that field, used by `ScenarioScene.refreshBuildSidebar` so the panel matches OpenDUNE's factory-window order. `StructureLayout.footprintOffsets` materialises the `(dx, dy)` offsets for each layout. New `Simulation.Structures.footprintTiles(type:anchorX:anchorY:)` + partial `Simulation.Structures.isValidBuildLocation(tileX:tileY:type:structures:units:)` port: bounds check + no-overlap with existing pool structures (walks findArray, expands each to its footprint) + no-unit-on-tile. Commit path in `ScenarioScene.commitPlacement` now validates before calling `Structures.create`; invalid clicks log under `build-panel` tracer and reset placement state. 18 new tests in `BuildValidationTests` (sortPriority table, priority vs type-ID ordering, footprint extents, in-bounds validity, overlap / out-of-bounds / unit-overlap rejection). 520 green / 61 suites / zero warnings on clean build. Deferred to slice 4b: landscape-type gate (`isValidForStructure` / `isValidForStructure2`), slab-prereq degradation (`-neededSlabs`), "adjacent to player base" rule, `notOnConcrete` flag. Design: `Documentation/Algorithms/BuildValidation.md`.

- **P5 slice 3 — build panel on `ScenarioScene` + click → `Structure_Create` (instant placement; no validation).** First user-visible P5 slice. `Simulation.StructureInfo.buildableTypes(from: UInt32) -> [UInt8]` decodes the slice-1 bitmask into an ordered list (ascending type-ID; `sortPriority` is slice 4). New `DuneIIRendering.BuildPanelController` is a pure state machine (`Click → Action`, no SpriteKit) holding `selectedYardIndex`, `placementType`, and `availableTypes`. `ScenarioScene` widened from 1024×1024 to 1152×1024 to host a 128 pt right-hand sidebar; slice nodes render per buildable type with the last ICN tile of the structure's iconGroup + short Menlo label. `mouseDown` translates scene points to controller clicks, dispatches to `Structures.create` on commit, refreshes the sidebar. Auto-selects the player's first construction yard on scene load; "click outside sidebar while not placing" preserves the pre-slice "route to main menu" fallback. 13 new tests in `BuildPanelTests` (bitmask → type list; controller state transitions through empty / picking / placing / re-pick / outside). 502 green / 60 suites / zero warnings on clean build. **Cannot visually verify from the sandbox** — manual-verification checklist lives in `Documentation/Algorithms/BuildPanel.md` §8. Deferred to slice 4: `sortPriority` ordering, `Structure_IsValidBuildLocation`, countdown + credit drain, ghost preview at cursor, yard-switching via map click, escape/right-click cancel.

- **P5 slice 2 — `Structure_Create` + `Structure_Allocate` (non-slab / non-wall tail of `Structure_Place`).** Pure-sim allocator that lets a construction yard (or scenario loader) spawn a new structure at a tile. `Simulation.StructureSlot` grew three fields — `hitpointsMax: UInt16`, `upgradeLevel: UInt8`, `objectType: UInt16` — all plumbed through both `WorldSnapshot.init` paths from `Formats.Save.Structures.Slot` (save) / `StructureInfo.table[type].hitpoints` (scenario). `Simulation.Structures.allocate(at:type:houseID:pool:)` routes slabs/walls to reserved slots 81/80/79 and normal structures to the first free in `[0, 79)` when given `invalidIndex`, or to a caller-specified slot when free. `Simulation.Structures.create(type:houseID:position:pool:)` seeds `state=JUSTBUILT`, `hitpoints=hitpointsMax=StructureInfo.hitpoints`, `linkedID=0xFF`, `objectType=0xFFFF`, `countDown=0`, `upgradeLevel=0` (1 for Harkonnen + LIGHT_VEHICLE), and tile-aligns `positionX/Y` via `& 0xFF00`. Deferred: `Structure_BuildObject` (queue seed), AI auto-upgrade loop + `Structure_IsUpgradable`, wall/slab placement, `Structure_IsValidBuildLocation`, fog/power/seenByHouses side effects, turret `rotationSpriteDiff` from iconMap, construction countdown state machine. 17 new tests in `StructureCreateTests` (allocator range, reserved routing, full-pool nil, field seed invariants, HK upgrade pin, tile align, findArray registration, `_SAVE001.DAT` round-trip). 489 green / 59 suites / zero warnings on clean build. Design: `Documentation/Algorithms/StructureCreate.md`.

- **P5 kickoff slice 1 — `Structure_GetBuildable` (yard case) + `Structure_GetStructuresBuilt`.** Pure-sim data port that unlocks the build panel. `Simulation.StructureInfo` grew four fields (`availableCampaign: UInt16`, `availableHouse: UInt8` bitmask, `structuresRequired: UInt32` bitmask, `upgradeLevelRequired: UInt8`) populated for every row in the 19-entry table. `Simulation.House` grew three static bitmask constants (`flagAll`, `flagBarracksHouses`, `flagWorHouses`) plus `flag(for:)`. New `Simulation.Structures` namespace with `structuresBuilt(houseID:pool:)` (ignores slabs + walls, filters by house) and `buildableStructuresFromYard(yardHouseID:yardUpgradeLevel:structuresBuilt:campaignID:playerHouseID:)` — port of the `STRUCTURE_CONSTRUCTION_YARD` case of `Structure_GetBuildable`, including both verbatim quirks: Harkonnen WOR exception (clears BARRACKS prereq + pins availableCampaign=2 from c≥1) and non-Harkonnen LIGHT_VEHICLE pin (availableCampaign=2 hides LV at mission 1). Campaign gate ports C's signed-int promotion: `Int(availableCampaign) - 1` so ROCKET_TURRET's `availableCampaign = 0` → threshold `-1` passes the gate (the upgrade-level requirement is the real lock). Factory / Starport cases deferred to next slice. 18 new tests in `StructureBuildableTests`. 472 green / 58 suites / zero warnings on clean build. Design: `Documentation/Algorithms/StructureBuildable.md`. Insight: `simulation-getbuildable-signed-int-campaign-gate.md`.

- **Worktree adaptation.** User moved project from `DuneIIRemaster` / `DuneIIRemake` to `DuneIISwift` and added git. Updated hardcoded log path in `Code/Core/Sources/duneii/main.swift` (`/Users/alex/.../DuneIIRemake/Logs` → `/Users/alex/.../DuneIISwift/Logs`). Removed stale `resources: [.copy("Fixtures")]` declaration in `Package.swift` (no test reads from a Fixtures dir; declaration was breaking fresh clones). Tightened `TestInstall.locate()` to also require `contentsOfDirectory` readability so install-gated tests skip cleanly when the worktree sandbox can't reach the main repo's `Repositories/` (previously `fileExists` returned true but reads failed with POSIX `Operation not permitted`, surfacing 45 false failures).

- **Team AI live hookup + log filter + INI robustness.** User confirmed motion after the prior fixes. Closing the last big P4 gap — enemy coordination via teams. `Scenario` now parses the `[TEAMS]` INI section into `TeamSpawn` records (`house, action, movementType, minMembers, maxMembers`); new `Scenario.TeamAction` enum (Normal/Staging/Flee/Kamikaze/Guard) with `typeID` mapping to OpenDUNE's `TeamActionType`. `WorldSnapshot.init(scenario:)` exposes a new `teams: TeamPool` field and allocates one slot per scenario team spawn. `ScenarioScene.setUpScheduler` loads `TEAM.EMC` (15 entry points in vanilla) + wires `teamTable(host:source:)` + passes a real `teamVM` to the Scheduler; previously `teamVM` fell back to the unit VM (safe no-op). Fixed two scenario-loader bugs uncovered while testing mission 2: generated structure rows can be as short as `GEN1388=Ordos,Concrete` (2 fields; HP defaults to 256), and `Concrete`/`Spice Silo` string aliases now parse as `slab1x1` / `silo`. Log noise reduced via a new `FilteringMemoir` wrapper in `duneii/main.swift` — default `minLevelShown = .debug` drops VM per-opcode spam from the file; set `DUNEII_LOG_VERBOSE=1` before launch to widen. 7 new tests (TeamAction parse + typeID, synthetic [TEAMS] parse, missing/malformed rows, mission-2 spawn round-trip, mission-1 no-teams invariant). 454 tests green across 57 suites; zero warnings on clean build. Mission 1 still has no teams (not an enemy-wave mission), but missions 2+ will now drive `TEAM.EMC` with real team state.

- **Two motion-fix landed: `variables[0] = typeID` on script load + `seenByHouses = 0xFF` on scenario spawn.** Parsed the second diagnostic log. The scripts were running and calling host functions (slot 0x28 RemoveFog, slot 0x00 GetInfo, slot 0x01 SetAction, slot 0x3A SetTarget), but **never** calling IdleAction (0x31), SetDestination (0x05), or FindBestTarget (0x1C). Root cause on inspection of `VM.load`: we reset the engine and set `pc` but didn't set `variables[0] = typeID` — OpenDUNE's `Script_Load` does. Every action script starts with `pushVariable(0); push2(N); binary(2)` checking "is this the N-th action?". With our `variables[0] = 0`, every unit took the `var[0] == 0` branch (ACTION_ATTACK) regardless of what action the scheduler thought it loaded. Fixed by adding `engine.variables[0] = UInt16(truncatingIfNeeded: typeID)` in `VM.load`. Second issue uncovered while tracing: scenario-spawned units had `seenByHouses = 0` (unset) — so every `TargetAcquisition.targetUnitPriority` call returned 0 on the `(target.seenByHouses & (1 << attacker.houseID)) == 0` gate. That explained why FindBestTarget never returned a target even though we wired it. Mission 1 has no fog; set `slot.seenByHouses = 0xFF` on every scenario spawn so targets are mutually visible. Added a `FindBestTarget` log site so the next run confirms real target resolution. 447 tests green / 56 suites / zero warnings.

- **Diagnostic instrumentation + UNIT.EMC ACTION_GUARD trace.** User reported units still not visibly moving after the NoOp-default fix. Analysed the log: everything wires correctly (17 units spawn, each loads its correct action entry point in UNIT.EMC — pc=1545 for GUARD, 1758 for AMBUSH, 2029 for HUNT — no halts). Disassembled the ACTION_GUARD handler at pc=964: it clears destination + target, then branches on `var[4]` — on fresh spawn `var[4]=0`, which jumps to pc=1015. pc=1015 calls GetInfo(0) → push 128 → binary compare → FindStructure (slot 0x25, **unwired** → NoOp returns 0) → CallUnitByType (slot 0x23, unwired → NoOp) → popVariable(4) stores the NoOp's 0 return into var[4] → next tick branches the same way → infinite loop of "do nothing useful". The NoOp default keeps scripts running but the conditionals that rely on real return values never enter the IdleAction path (pc=991, only reachable via pc=985 which requires var[4] to be set by FindStructure's real return). Added instrumentation so the next run reveals exactly what's happening: `Log.debug` inside `makeIdleActionUnit` (fires only when IdleAction runs), `makeSetActionUnit` (logs prior → new action when different), `makeSetDestinationUnit` (logs every call with encoded target), VM FUNCTION opcode gets a `Log.verbose` tagging each slot call, and `ScenarioScene.update` now emits a once-per-60-ticks sample of the first four unit slots (type/house/action/orient/pos/dest). 447 tests green across 56 suites; zero warnings on clean build. Fix for actual motion requires porting `FindStructure` (slot 0x25) so GUARD's var[4] flips correctly — that's the next real action item.

## 2026-04-20

- **Logging infrastructure — memoirs-ios with per-run file rotation.** New `DuneIICore/Logging/Logger.swift` facade (`Log.debug`, `Log.info`, `Log.warning`, `Log.error`, `Log.verbose`) wraps any `Memoirs.Memoir` and is guarded at compile time by `#if DEBUG` — release builds compile every call to a no-op, the `@autoclosure` message never evaluates, the memoir storage is never touched. New `DuneIIRendering/Logging/FileMemoir.swift` writes line-formatted items to a `FileHandle` through a serial dispatch queue (`[HH:mm:ss.SSS] LVL [tracer] [file:line func] message`) so any thread can log safely. New `DuneIIRendering/Logging/LogRotator.swift` picks a fresh `Logs/run-YYYY-MM-DD_HH-MM-SS.log` filename under `/Users/alex/Programming/LonelyBytes/DuneIIRemake/Logs/` and trims older files so at most 10 remain. `duneii/main.swift` installs the rotator + `FileMemoir` in `applicationDidFinishLaunching` (still inside `#if DEBUG` so release wipes the whole setup). Added memoirs-ios 2.1.2 as a SwiftPM dependency. Initial log sites added across the hot paths: install / asset-loader open, WorldSnapshot spawn init (per-unit type/house/action/position + per-scenario counts), Scheduler's unit action-change + entry-point load, unit halts, bullet arrival + detonation, `Script_Unit_Fire` bullet creation, `applyUnitDamage` on zero-HP, `makeExplosion` type + position + damage, `ScenarioScene.build` + EMC program sizes. Logger uses `nonisolated(unsafe) var memoir` + `NSLock` so calls work from test runners, SpriteKit update ticks, and background queues without hopping actors. 5 new tests in `LoggingTests` (FileMemoir writes + appends, LogRotator creates + trims, Log facade routes through the installed memoir). 447 tests green across 56 suites; zero warnings on clean build. Logs show up at `Logs/run-*.log` next time you `swift run duneii`.
- **Motion bug — second pass. Units actually fidget now.** Two gaps remained after the previous "loaded entry-point per action" fix: (1) `Simulation.WorldSnapshot.init(scenario:resolver:)` was hardcoding `slot.actionID = 0` (ACTION_ATTACK) for every spawn, ignoring the INI's 6th `UNITS=` field — so the scheduler loaded every unit at the `ATTACK` entry point regardless of whether the scenario said `Guard` or `Hunt`, and `Script_Unit_IdleAction` never fired. New `Scenario.UnitAction.typeID` extension maps each case to OpenDUNE's `ActionType` numeric ID (guard_=3, hunt=11, …); WorldSnapshot now seeds `slot.actionID = spawn.action.typeID`. Winger-class units (CARRYALL, ORNITHOPTER, FRIGATE, missiles) also get `speed = 255` at spawn per OpenDUNE's `Unit_Create` winger branch. (2) Even with the right entry point loaded, the EMC programs call per-type functions we haven't ported yet (e.g. `Script_Unit_SetActionDefault`, `Script_Unit_Rotate`), and the `VM.step` FUNCTION opcode halted the engine on any nil slot — one unwired call near the start of `ACTION_GUARD`'s script and the unit was stuck forever. Changed `Scripting.Functions.unitTable` / `structureTable` / `teamTable` to default every slot to `noOperation` (return 0, no side-effects) instead of leaving nil; ported slots overwrite the default. Unwired opcodes now degrade to "silently return 0" so the rest of the script keeps executing and reaches wired slots like `IdleAction`. New install-gated integration test runs UNIT.EMC for 30 ticks against real mission 1 spawns and asserts that at least one unit updates state, with the halt rate staying under 90%. Updated the teamTable factory test to reflect the new all-non-nil shape. 442 tests green across 55 suites; zero warnings on clean build. Running `swift run duneii` now shows units actually fidgeting in place (idle-animation path fires).
- **Visual fidelity pass — motion bug, per-house palette remap, structure rendering.** Fixed three issues the user flagged in mission 1 after running `swift run duneii`. **Motion**: the `Scheduler` was starting every unit/structure/team engine at `pc = 0` regardless of action, so EMC scripts never reached their `ACTION_GUARD` entry point where `IdleAction` fires. New per-slot `loadedUnitAction` / `loadedStructureType` / `loadedTeamAction` tracking detects action changes each tick and calls `VM.load(engine:typeID:)` to jump to the correct entry point in `UNIT.EMC` / `BUILD.EMC` / `TEAM.EMC`. `VM.load` made a no-op when `program.entryPoints.isEmpty` so synthetic test programs retain their seeded engine state. **Palette**: `Formats.Palette.applyHouseColors(_:houseID:)` ports `GUI_Widget_Viewport_GetSprite_HousePalette` (`src/gui/viewport.c:300`) — sub-palette bytes in `[0x90, 0x98]` get `houseID << 4` added. `CGImageFactory.makeImage(indices:width:height:subPalette:palette:mode:)` is the new two-stage paletted path; `AssetLoader.loadShp(named:houseID:)` gets a house parameter that routes frames with embedded mini-palettes through the remap. `UnitSpriteAtlas` refactored from `struct` → `final class` with a lazy `[UInt8: [SKTexture?]]` per-house cache; `texture(at:houseID:)` builds the requested house's variant on first miss. `ScenarioScene.syncUnits` drops the `colorBlendFactor = 0.35` tint and picks per-house textures instead. **Structures**: `Simulation.StructureInfo.iconGroupRawValue(for:)` maps every structure type (0..18) to its `Formats.IconMap.Group` raw value. `ScenarioWorld.init` grew an optional `iconMap:` parameter; when passed, non-slab non-wall structures paint the last `(width × height)` tiles of their iconGroup across the footprint (the fully-built tail of the group). `ScenarioScene.syncStructures` drops the 55%-alpha coloured rectangle overlay — the baseline tile stamp now shows the real structure art — and replaces it with a thin house-coloured outline for ownership readability. 7 new tests (1 scheduler entry-point load + 6 palette/structure). 438 green / 55 suites / zero warnings. `swift run duneii` now shows real buildings, per-house unit colours, and visibly fidgeting idle units.
- **P4 Phase 4 ninth slice — Team AI dynamic behaviour (`AddClosestUnit` + scheduler team-tick).** Two core helpers on `Simulation.Units` close the team-membership loop: `addToTeam(unitIndex:teamIndex:host:)` (writes `unit.team = teamIdx + 1`, bumps `team.members`, returns remaining capacity) and `removeFromTeam(unitIndex:host:)` (clears `unit.team`, decrements `team.members`, returns remaining; defensive clear when `u.team` references a dead slot). Team slot 0x03 `AddClosestUnit` ported — walks `host.units.next(&query)` filtered by `houseID`, skips non-byScenario + SABOTEUR + mismatched `movementType`, prefers the nearest `team == 0` (unaligned) unit and falls back to stealing from teams whose `members > minMembers`. `Scheduler` grew a `teamVM: Scripting.VM` + `teamEngines: [Scripting.Engine]` (capacity 16) + a `tickTeams` pass that walks `teams.findArray`, sets `currentObject = .team(idx)`, and dispatches the team engine with a 5-opcode budget. `Scheduler.init(...)` defaults `teamVM` to the unit VM when not supplied (safe no-op until real TEAM.EMC wiring). 7 new tests: addToTeam/removeFromTeam happy path, remove no-op on team==0, slot 0x03 pulls unaligned + nearest wins, slot 0x03 skips SABOTEUR + mismatched movement, slot 0x03 returns 0 when full, scheduler visits teams in findArray order decrementing their `engine.delay`. Updated teamTable factory test to reflect the new slot. 431 tests green across 54 suites; zero warnings on clean build. Teams can now recruit live units via EMC and have their scripts ticked — wiring the real `TEAM.EMC` in `ScenarioScene` would now produce coordinated enemy waves.
- **P4 Phase 4 eighth slice — Team AI infrastructure.** The biggest remaining P4 gameplay gap — coordinated enemy waves driven by team AI — is now scaffolded end-to-end for the read-only script slots. New `Simulation.TeamPool` (16 slots, matching OpenDUNE's `TEAM_INDEX_MAX`) + `TeamSlot` value type with the fields our wired slots read: `index, members, minMembers, maxMembers, movementType, action, actionStart, houseID, positionX/Y, targetTile, target`. New `Simulation.TeamAction` enum (5 values: `normal, staging, flee, kamikaze, guard_`). `UnitSlot` grew `team: UInt8` (OpenDUNE's "team index + 1" encoding, 0 = no team); `WorldSnapshot` plumbs it from save records. `Scripting.Host` gained `teams: Simulation.TeamPool` + a new `ObjectRef.team(poolIndex:)` case so scripts can address a team as the `currentObject`. Ported seven team script slots: **0x01 `Team_DisplayText`** (skips drawing when team is player, matching OpenDUNE's hostile-only behaviour); **0x02 `Team_GetMembers`** (`t->members`); **0x06 `Team_FindBestTarget`** (walks the team's member units, calls `TargetAcquisition.findBestTargetEncoded` with mode 4 for KAMIKAZE else 0, locks `target + targetTile` on first hit); **0x08 `Team_Load`** (swaps `action` to peek(1)); **0x09 `Team_Load2`** (resets `action` to `actionStart`); **0x0C `Team_GetVariable6`** (`t->minMembers` — name is OpenDUNE's misnomer); **0x0D `Team_GetTarget`** (`t->target`). New `teamTable(host:source:)` factory mirroring OpenDUNE's `g_scriptFunctionsTeam` — wires slots 0x00 `Delay`, 0x0A `DelayRandom`, 0x0E `NoOp` alongside the seven team-specific slots. Deferred to a follow-up slice: slot 0x03 `Team_AddClosestUnit` (needs `Unit_AddToTeam`/`RemoveFromTeam` + steal-from-weaker-team logic), 0x04 `GetAverageDistance`, 0x05 `Unknown0543` (Unit_SetAction chain), 0x07 `Unknown0788`, 0x0B `DisplayModalMessage` (GUI), `TEAM` save chunk decoder, and the scheduler team-tick pass. 11 new tests in `TeamAITests` — TeamPool allocate/free, GetMembers (with + without current team), 0x0C/0x0D pure reads, DisplayText player-skip vs enemy-write, Load's action swap, Load2's actionStart reset, FindBestTarget full flow with a team member + live enemy, teamTable factory spot-check. 424 tests green across 54 suites; zero warnings on clean build.
- **P4 Phase 4 seventh slice — sandworm targeting + `ExplosionMultiple` + `Tile_MoveByRandom`.** Three small but independent P4 items ticked off. New `Pos32.movedRandomly(from:distance:center:random:)` ports `Tile_MoveByRandom` (`src/tile.c:306`); takes a `() -> UInt8` closure so it doesn't have to depend on any particular RNG type (caller wraps their `ToolsRandom256` stream). Consumes exactly 2 bytes per call — one for distance-halving, one for orientation. `Simulation.TargetAcquisition` gained `sandwormTargetPriority(attacker:target:host:)` + `sandwormFindBestTarget(attackerIndex:host:)` — ports of `Unit_Sandworm_GetTargetPriority` / `Unit_Sandworm_FindBestTarget` (`src/unit.c:987`, `:1020`). Movement-weighted priorities: wheeled = 5000, tracked/harvester = 1000, foot = 100, air/other = 0. Moving or mid-fire units score ×4; within-2-tiles targets score ×2. `>=` tie-break picks later-allocated. Deferred: the `isSandAtPosition` gate (needs landscape-type plumb from `Host`, added when sandworms matter for mission 5+). Two new script slots wired in `unitTable`: **0x12 `ExplosionMultiple`** spawns 1 central DEATH_HAND at 25-50 damage + 7 scattered at 75-150 damage via `movedRandomly(radius:, center: false)`, matching `src/script/unit.c:553` exactly (one explosion sometimes dedups via `stopAtPosition` when two scatter points hash to the same tile — preserved behaviour). **0x36 `Sandworm_GetBestTarget`** — thin wrapper returning the encoded unit index of the best worm prey or 0. 11 new tests (`SandwormAndMultiExplosionTests`): `movedRandomly` guards + RNG-byte consumption pin, sandworm priority across all five movement types + moving-bonus + unallocated + air-skip, FindBestTarget picks-highest + returns-nil-when-no-prey, slot 0x36 VM integration, slot 0x12 VM integration (1..8 explosions, all DEATH_HAND). 413 tests green across 53 suites; zero warnings on clean build.
- **P4 Phase 4 sixth slice — sim-to-visual sync on `ScenarioScene`.** The sim has been alive for days but the scene didn't show it. `Scheduler.tick()` gained a `tickExplosions` pre-pass that decrements every active `ExplosionSlot.remainingFrames` and auto-frees on zero — matches OpenDUNE's `Explosion_Tick` shape in a single-frame approximation (no command stream yet). `ScenarioScene.syncVisualsFromPool` is now lazy: markers are created on first observation of an `isUsed` slot, so bullets spawned mid-combat into slots 12..15 get proper sprite markers via the existing `UnitSpriteAtlas` path (bullet + sonic-blast use `displayMode: .singleFrame` → groundSpriteID direct). Pre-tick `buildStructureMarkers` / `buildUnitMarkers` removed; everything flows through `syncUnits`, `syncStructures`, `syncExplosions`. Explosion visuals: one `SKShapeNode` circle per active pool slot, coloured by `ExplosionType` (IMPACT_SMALL yellow, IMPACT_* orange, DEATH_HAND red, DEVIATOR_GAS green, SANDWORM_SWALLOW brown), alpha fades as `remainingFrames → 0`, auto-removed when the scheduler frees the slot. `Host` gained an `explosions: Simulation.ExplosionPool` parameter on `ScenarioScene`'s scheduler setup; `playerHouseID` wired from the scenario's Atreides anchor so `House.areAllied` picks the right enemy set. 2 new `Scheduler` tests (3-tick countdown → free on zero; no-op on empty pool). 402 tests green across 52 suites; zero warnings on clean build. `swift run duneii` now shows live combat — tanks fire, bullets fly across the map, explosions pop, structures take damage and disappear.
- **P4 Phase 4 fifth slice — `explodeOnDeath` + structure combat (slots 0x08/0x09/0x0A/0x0B).** `Simulation.Explosions.applyUnitDamage` now spawns `UnitInfo.explosionType` on zero-HP for `explodeOnDeath` units — TANK/SIEGE_TANK/DEVASTATOR visibly detonate with IMPACT_MEDIUM, LAUNCHER with IMPACT_EXPLODE; infantry/carryalls/harvesters still just vanish (explodeOnDeath=true + explosionType=nil → no visual). Added the four structure-combat script wrappers. **Slot 0x08 `FindTargetUnitStructure`**: walks `host.units`, filters by `House.areAllied` (reused from first combat slice) + `seenByHouses` (ornithopter exception: always visible), picks closest non-allied within `peek(1) * 256` range (3× for ornithopters). **Slot 0x09 `RotateTurret`**: 8-step turret rotation toward the encoded target — computes needed octant via `Orientation.to8(Pos32.direction(...))`, rotates one step per call, returns 0 when aligned and 1 while rotating. **Slot 0x0A `GetDirection`**: returns `rotationSpriteDiff << 5` for invalid target, else `octant(direction) << 5`. **Slot 0x0B `FireStructure`**: reads `engine.variables[2]` as the target (set by the structure's own EMC), picks `BULLET` / `MISSILE_TURRET` based on structure type + distance (ROCKET_TURRET ≥ 0x300 pos32 → missile), spawns via `Simulation.Units.createBullet` at structure centre (`positionX+0x80, positionY+0x80`), stamps `originEncoded = EncodedIndex.structure`, returns `UnitInfo[TANK/LAUNCHER].fireDelay` so the EMC engine can schedule the next shot. `StructureSlot` gained `rotationSpriteDiff: UInt8` (0..7 octant); `WorldSnapshot` plumbs it from save records. 3 new tests for `explodeOnDeath` (TANK → IMPACT_MEDIUM, INFANTRY → no visual, HARVESTER with nil explosionType → no visual). 11 new tests for structure combat in `StructureCombatTests` — FindTargetUnit (nearest wins / allied skip / out-of-range skip / ornithopter 3× range), RotateTurret (N→NE→E progression over three calls / zero-encoded no-op), GetDirection (invalid passthrough / valid octant), Fire (TURRET→BULLET w/ damage=20 + delay=80, ROCKET_TURRET far target→MISSILE_TURRET w/ damage=30 + delay=120, variables[2]=0 no-op). Updated the "unported slot" spot-check in EmcHostBatch67Tests to reflect the wiring. 400 tests green across 52 suites; zero warnings on clean build. Turrets and rocket-turrets now defend their bases end-to-end.
- **P2 slice 2.1 — intro WSA + audio (Jukebox + Voice).** Three new pieces of the presentation stack land, closing the remaining P2 scope. New `DuneIIRendering/Audio/Jukebox.swift` — `@MainActor` `AVMIDIPlayer` wrapper that loads XMI from the install, converts track 0 via `Formats.Xmi.Song.Track.toStandardMidiFile()`, plays via `AVMIDIPlayer(data:soundBankURL:)` with nil soundBank = system DLS default (user can drop a custom `.sf2` into `~/Library/Audio/Sounds/Banks/`; no redistributable font bundled). `play(named:)` replaces the current song on each call; `stop()` + `isPlaying` for lifecycle. New `DuneIIRendering/Audio/Voice.swift` — `@MainActor` VOC player built on `AVAudioEngine` + `AVAudioPlayerNode`. `Voice.makeBuffer(for:)` converts `Formats.Voc.Sound` (8-bit unsigned mono PCM at native kHz) to an `AVAudioPCMBuffer` with the `byte→float = (byte - 128) / 128.0` remap so 128 lands at 0.0. Engine auto-connects the player to `mainMixerNode` with `format: nil` so the mixer handles rate conversion. New `DuneIIRendering/Scene/IntroScene.swift` — swap-texture WSA playback at 15 fps (≈ vanilla 70/5 Hz). Each frame becomes an `SKTexture` with nearest filtering; the scene's `update(_:)` accumulates elapsed time and advances `SKSpriteNode.texture`. Mouse click or end-of-animation routes to `.mainMenu`. New `Route.intro` case. `GameController.start()` routes to `.intro` when `INTRO.WSA` exists, else straight to main menu. `GameController` now owns a `Jukebox` and optional `Voice` (`try?`-initialised so audio-init failure doesn't prevent window boot); `route(to: .mainMenu)` kicks off `DUNE0.XMI` playback so users hear Dune II's title music. New `AssetLoader` helpers: `loadWsa(named:)` → `WsaFrames { frames: [CGImage], width, height }` using the WSA's embedded palette when `hasPalette`; `loadXmiAsSMF(named:)` → `Data?` ready for `AVMIDIPlayer`; `loadVoc(named:)` → `Formats.Voc.Sound?`. 6 new tests in `AudioIntroTests` (all install-gated): `INTRO.WSA` decodes to 320×200 frames, `DUNE0.XMI` produces an SMF that starts with the `MThd` header, any `.VOC` decodes with non-zero rate + non-empty samples, `Jukebox.play(named:)` runs without throwing, `Voice.makeBuffer` pins the `u8 → float` remap (0 → -1.0, 128 → 0.0) and rejects empty / zero-rate inputs. 386 tests green across 51 suites; zero warnings on clean build. P2 is effectively done — deferred-to-P5 items (mentat briefing text from MESSAGE.ENG, scenario pan/zoom, scenario picker UI) need gameplay-flow code that belongs with the full HUD.
- P4 Phase 4 fourth slice — bullet flight + arrival detonation closes the end-to-end kill loop. `UnitInfo` grew `explosionType: UInt16?` per type, with all 27 values ported from `src/table/unitinfo.c` (TANK/SIEGE_TANK/DEVASTATOR → IMPACT_MEDIUM=1, LAUNCHER/DEVIATOR → IMPACT_EXPLODE=3, BULLET/infantry → IMPACT_SMALL=0, MISSILE_ROCKET/TURRET → IMPACT_EXPLODE, MISSILE_DEVIATOR → DEVIATOR_GAS=7, MISSILE_TROOPER → MINI_ROCKET=18, MISSILE_HOUSE → DEATH_HAND=11, SANDWORM → SANDWORM_SWALLOW=13; carryall/harvester/MCV/sonic-blast/frigate → nil). `Simulation.Units.createBullet` now stamps `speed = 255` on every projectile (matches OpenDUNE's `Unit_Create` winger path) so `Scheduler.tickMovement`'s `step = max(4, speed/4) = 63` can cross the map in a handful of ticks. `Scheduler.tickMovement` grew a bullet-arrival hook: when a unit of type 18..24 arrives within `arrivalThreshold` of its `currentDestination`, it detonates via `Simulation.Explosions.makeExplosion` using `UnitInfo.explosionType` for the type, `slot.hitpoints` for damage, and `slot.originEncoded` for attacker credit, then frees the bullet slot — this shortcuts OpenDUNE's EMC-driven `BULLET.EMC` → `Script_Unit_ExplosionSingle` call chain until bullet scripts are wired. New `Scheduler.isProjectileType(_:)` helper encapsulates the 18..24 gate. 4 new tests in `BulletFlightTests`: bullet advances per tick with speed=255, bullet detonates on arrival and spawns IMPACT_SMALL + damages co-located enemy, MISSILE_ROCKET detonation uses IMPACT_EXPLODE type, SONIC_BLAST bullet with nil explosionType still frees its slot (visual pool skipped). 380 tests green across 50 suites; zero warnings on clean build. The end-to-end combat loop now works: TANK calls `Script_Unit_FindBestTarget` (slot 0x1C) → picks an enemy → `Script_Unit_SetTarget` → `Script_Unit_Fire` (slot 0x08) → bullet allocated in slots 12..15 → scheduler advances it toward the target → on arrival `makeExplosion` damages the target + queues visual + frees the bullet. Future slices need EMC bullet scripts for non-linear flight, `explodeOnDeath` death chain, and AI retaliation.
- P4 Phase 4 third slice — explosion pool + damage + `Script_Unit_ExplosionSingle` (slot 0x0E). New `Simulation.ExplosionPool` (32-slot fixed array of `ExplosionSlot { isActive, type, positionX/Y, houseID, remainingFrames }`), plumbed through `Scripting.Host.explosions`. `Simulation.ExplosionType` enum ports OpenDUNE's 20-entry `ExplosionType` (`src/explosion.h:13`) with raw-UInt16 matching the game data. New `Simulation.Explosions` namespace: `applyUnitDamage` (simplified port of `Unit_Damage` — HP reduction + free-on-zero; bullets/missiles invulnerable via the `18...24` type gate since we haven't plumbed `isNormalUnit` yet); `applyStructureDamage` (simplified port of `Structure_Damage` — same shape, `damage == 0` early-out); `makeExplosion` (port of `Map_MakeExplosion` for the damage path: radius scan with `distance >> 4` tile metric, `hitpoints >> (distance >> 2)` falloff, `reactionDistance` doubled for `DEATH_HAND`; SANDWORM+SWALLOW exemption; FRIGATE invulnerable; structure-at-point damage with the `IMPACT_LARGE → SMOKE_PLUME` half-HP cosmetic downgrade; pool entry with `stopAtPosition` dedup on the same tile). New `Scripting.Functions.makeExplosionSingleUnit` wires slot 0x0E via `UnitInfo.hitpoints` as the AoE damage radius and `EncodedIndex.unit(shooter.index)` as the attacker credit. Deferred this slice (Explosion.md §7): `Script_Unit_ExplosionMultiple` (slot 0x12 — needs `Tile_MoveByRandom`), explosion command-stream interpreter (animation — presentation layer), `explodeOnDeath` + `ACTION_DIE` death chain, AI retaliation (`Unit_SetTarget` + `ACTION_HUNT`), wall decay (`Map_UpdateWall`), infantry→trooper halving, harvester spice spill, score updates on structure destruction. 14 new tests in `ExplosionTests`: pool fill-to-cap + overflow nil, `stopAtPosition` match-by-packed-tile, unit damage wound vs death with slot-freed assertion, bullet invulnerability at type 23, structure damage death + damage=0 no-op, `makeExplosion` visual-only (hitpoints=0) queues pool without touching HP, center-unit full damage, out-of-radius no damage, SANDWORM_SWALLOW spares sandworms, structure-at-point damage inside a 3×2 REFINERY footprint, invalid-type (≥ 20) no-op, slot 0x0E kills a co-located QUAD using the TANK's 200 max HP. Algorithm doc: `Explosion.md`. 376 tests green across 49 suites; zero warnings on clean build.
- P4 Phase 4 second slice — `Script_Unit_Fire` + bullet plumbing. `UnitInfo` grew four more fields ported from `src/table/unitinfo.c`: `indexStart` / `indexEnd` (per-type pool slice — bullets sit at 12..15, sandworms 16..17, vehicles 22..101, carryall/thopter 0..10, saboteur 20..21, frigate 11..11), `bulletType` (the unit spawned when firing — `nil` for non-firing types), and `firesTwice` (double-tap units). `UnitSlot` grew `fireDelay: UInt8` (runtime cooldown counter) and `fireTwiceFlip: Bool` (double-tap flip-flop). New `UnitPool.allocateForType(type:houseID:)` walks a type's `indexStart..indexEnd` for the first unused slot — faithful port of `Unit_Allocate` with `UNIT_INDEX_INVALID`. Rejects invalid types and `houseID >= 6`; OpenDUNE's `unitCount >= unitCountMax` gate is deferred (needs `HouseSlot.unitCount`). New `Simulation.Units.createBullet(position:type:houseID:damage:target:host:)` ports `Unit_CreateBullet` (`src/unit.c:1954`) for MISSILE_* (18..22) and BULLET / SONIC_BLAST (23..24) — missiles spawn at the shooter's position, bullets step forward via two `Tile_MoveByDirection` calls so they don't hit the shooter; both set `currentDestination` to the target tile, stamp `hitpoints = damage`, and seed `fireDelay` for missiles (`ui.fireDistance & 0xFF`, doubled vs winger targets). `Pos32.moved(_:orientation:distance:)` ports `Tile_MoveByDirection` with the full 256-entry `_stepX` table inlined (derive `_stepY[i] = _stepX[(i + 64) & 0xFF]`). `Scripting.Functions.makeFireUnit(host:)` wires slot 0x08: validates target, self-cancels own-tile aim (sandworm exception), gates on `fireDelay == 0` / range (`fireDistance << 8`) / orientation diff < 8 (skipped for sandworm and winger targets), subs MISSILE_TROOPER for long-range troopers (distance > 512), calls `createBullet`, stamps the bullet's `originEncoded`, resets shooter's `fireDelay = ui.fireDelay * 2` (or 5 on the firesTwice "flip = true" shot). Deferred here: sandworm eat branch, `Tools_AdjustToGameSpeed` scaling, RNG jitter on reload, `Unit_Deviation_Decrease`, voice / fog side-effects. `Scheduler.tick()` grew a `tickFireCooldowns` pre-pass that decrements every unit's `fireDelay` by 1 each tick. `WorldSnapshot` plumbs `fireDelay` + `fireTwiceFlip` from save records. 14 new tests in `FireTests`: allocator range (TANK fills 22..101, BULLET fills 12..15 then returns nil on the 5th, invalid type / houseID → nil), `createBullet` (invalid target → nil, BULLET spawns in 12..15 with `targetAttack` + `currentDestination` + offset position, MISSILE_ROCKET spawns at shooter with `fireDelay = 8`), slot 0x08 Fire (no target → 0, cooldown gate, out-of-range gate, off-orientation gate, successful shot spawns bullet + resets `fireDelay = 160`, SIEGE_TANK firesTwice: first = flip + `fireDelay = 5`, second = unflip + `fireDelay = 180`), scheduler tick decrements `fireDelay` 3→2→1→0 and floors at 0. Plus: updated a pre-existing "slot 0x08 stays nil" spot-check in `EmcHostBatch67Tests` to reflect the wiring. 362 tests green across 48 suites; zero warnings on clean build. Algorithm doc: `Fire.md`.
- P4 Phase 4 first slice — target acquisition. New `Simulation.TargetAcquisition` namespace ports four OpenDUNE functions: `Unit_GetTargetUnitPriority` (`src/unit.c:743`), `Unit_GetTargetStructurePriority` (`src/unit.c:2562`), `Unit_FindBestTargetUnit` (`src/unit.c:923`), `Unit_FindBestTargetStructure` (`src/unit.c:2275`), and the dispatcher `Unit_FindBestTargetEncoded` (`src/unit.c:2396`). `Simulation.UnitInfo` grows four `ObjectInfo`-level fields (`priority: Bool`, `targetAir: Bool`, `priorityBuild: UInt16`, `priorityTarget: UInt16`) with per-row values extracted from `src/table/unitinfo.c`; `Simulation.StructureInfo` grows `priorityBuild` + `priorityTarget` from `src/table/structureinfo.c` plus the `layoutTileDiff` center-offset table. New `Simulation.House` namespace holds `House.areAllied(a, b, playerHouseID:)` — faithful port of `House_AreAllied` (`src/house.c:353`) with the Fremen↔Atreides exception and the "non-player houses implicitly ally" rule. `Scripting.Host` gains three optional fields — `playerHouseID: UInt8?`, `isValidPosition`, `isPositionUnveiled` — plumbing the `g_playerHouseID` / `Map_IsValidPosition` / `Map_IsPositionUnveiled` globals without forcing map state on every caller (nil defaults to "always valid" / "always unveiled", matching `g_debugScenario`). `Scripting.EncodedIndex.tile(packed:)` factory added, mirroring `Tools_Index_Encode(_, IT_TILE)`'s bit layout. `Pos32.distanceRoundedUp` ports `Tile_GetDistanceRoundedUp` (`src/tile.c:103`). Two new host functions — `makeFindBestTargetUnit` (slot 0x1C) and `makeGetTargetPriorityUnit` (slot 0x1D) — wired into `unitTable`. `FindBestTargetUnit` preserves OpenDUNE's single side-effect on the read chain: stamps `attacker.originEncoded` with the current tile on first call. Deviators skip structure candidates in the dispatcher (`attacker.type == 8`). 27 tests in a new `TargetAcquisitionTests` suite covering `areAllied` (invalid/same/Fremen/non-player/nil-player), `distanceRoundedUp` math pin, `targetUnitPriority` across seven gates (self / unallocated / unseen / allied / priority-off / winger-targetAir / off-map / TANK-vs-QUAD math pin), `targetStructurePriority` (allied / REFINERY-vs-TANK math pin), `findBestTargetUnit` (empty pool / mode 0 picks highest / mode 1 distance gate / origin-stamp side-effect), `findBestTargetStructure` (slab+wall skip / `>=` tie-break picks later-allocated), `findBestTargetEncoded` (mode 4 prefers structure / returns 0 when nothing / deviator skips structures), and VM integration via slots 0x1C (returns encoded unit index) + 0x1D (priority pin + tile/invalid → 0). Algorithm doc: `Documentation/Algorithms/TargetAcquisition.md`. 348 tests green across 47 suites. Also: cleaned three pre-existing `var host → let host` warnings in `SimulationSchedulerTests`. Zero warnings on clean build of library + tests.
- `Formats.Emc.Program` — byte-level decoder for compiled game scripts. Handles the 19-opcode instruction stream with 1- or 2-word widths driven by the 0x8000 / 0x4000 / 0x2000 flag bits, the TEXT chunk string pool, and the ORDR entry-point table. Insight captured: `format-emc-variable-instruction-width.md`.
- `Formats.Xmi.Song` — Miles XMIDI decoder with an SMF converter. Parses the nested `FORM XDIR` / `CAT XMID` / `FORM XMID` tree, decodes the EVNT event stream (including XMI's delay-before-event and note-on-with-duration semantics), and emits playable Format-0 SMF. Insight captured: `format-xmi-delay-and-duration.md`.
- `assetgen` now routes `.XMI` through `Formats.Xmi` → SMF and `.EMC` through `Formats.Emc` → raw + JSON disassembly. Empty XMI catalog slots (no note events) are filtered from output.
- Wrote `Documentation/Plans/02.P1-Complete.md` — P1 "format foundation" is closed. 90 tests green; every on-disk format in the 1.07 install has a typed decoder.
- Wrote `Documentation/Plans/03.P2-Rendering.md` — P2 roadmap, split into slice 1 (pure-Swift semantic models) and slice 2 (Mac Catalyst app + SpriteKit scenes).
- P2 slice 1 first pieces: `Core.Scenario` (typed model over `Formats.Ini.Document` — `House`, `UnitType`, `StructureType`, `UnitAction`, `UnitSpawn`, `StructureSpawn`, `HouseLayout`, `MapField`, `Briefing`) decoded from `SCENA?00?.INI` including the `GEN`-prefix slab/wall rows.
- P2 slice 1 second pieces: `Core.Map.Map` (64×64 grid with per-cell ground/overlay IDs, spice, structure flag, fog bitmask), `Core.Map.LandscapeType` enum, and `Core.Map.TileResolver` that classifies tiles via OpenDUNE's 81-entry `_landscapeSpriteMap` lookup. 100 tests green across 17 suites.
- P3 kickoff: `Core.ScenarioWorld` — typed world snapshot that pairs a `Scenario` with a freshly stamped `Map`. Handles structure placement, `GEN`-keyed slab / 2×2 slab / wall tiles, `units(at:)`, `structure(at:)`, and `isBlocked(at:)`. 105 tests green across 18 suites; new algorithm doc `ScenarioWorld.md`.
- `Core.RNG.ToolsRandom256` and `Core.RNG.BorlandLCG` — bit-for-bit ports of OpenDUNE's two PRNGs. Pinned regression baselines for the first five outputs from seed 0 / seed 1. Algorithm doc `RNG.md` added. 111 tests green across 19 suites.
- Reflowed every `.md` file under the repo (41 files) to remove hard line wraps within paragraphs; wrote `.tools/reflow_md.swift` as the reusable tool. Nested list items are now preserved (fixed a bug where the script folded sub-bullets into the parent's continuation text). Added a project rule to `CLAUDE.md` forbidding paragraph hard-wraps in future Markdown edits.
- `Core.Map.Generator` — deterministic 64×64 terrain generator seeded by `Scenario.MapField.seed`. Bit-for-bit port of OpenDUNE `Map_CreateLandscape`: 273-byte noise pool → blob/anti-blob spread via the 21-entry `around[]` table → 16×16 anchor stamp → 4-pixel bilinear fill via the parity `_offsetTable` → 9-neighbour smoothing → terrain thresholding → spice sprinkling via `Tile_MoveByRandom` + `Map_AddSpiceOnTile` → sprite-index assignment via the 4-bit neighbour-equality mask → final ICN tile lookup through the LANDSCAPE icon group. Six tests covering determinism, distinct seeds differing, range invariant (every output sprite ∈ `[landscape, landscape+80]`), terrain-type coverage, a per-cell pin and a full-grid checksum pin for seed `0x16BD`. Algorithm doc: `Documentation/Algorithms/MapGenerator.md`. Insight captured: `format-tile-packxy-uint16-bounds.md`. Closes the §3 "non-trivial map generator" risk in `01.Initial.md`. 117 tests green across 20 suites.
- Scenario map now renders full landscape. `ScenarioWorld.init(scenario:resolver:)` was starting from `Map.empty()` and only stamping slabs / walls / structure flags on top, which meant every non-stamped cell (the other 4000+) displayed tile 0 as a flat mono-texture. Changed to `Map.Generator.generate(seed: scenario.mapField.seed, resolver: resolver)` as the baseline; `applyMapField` + structure stamps run on top as before. The generator was already ported (phase P2 slice 1) — it just wasn't wired into the scenario path. `ScenarioWorldTests.makeSyntheticIconMap` grew to an 81-entry landscape group (was 32) since the generator indexes `tileId(.landscape, 0..80)`. Palette chain audited against OpenDUNE's SDL2 backend: `p * 0x41 >> 4` in `video_sdl2.c:732` is mathematically equivalent to our bit-replicated `(v<<2) | (v>>4)` at `AssetExport.PalettedImage.render` — both produce identical 0..63 → 0..255 mapping. IBM.PAL is the in-game palette per `src/opendune.c:313` and is what `AssetLoader` loads. No palette-side changes needed. 321 tests still green, zero warnings.
- Unit SHP sprites on the scenario scene. `Simulation.UnitInfo` grew `groundSpriteID: UInt16` and `displayMode: DisplayMode` (new enum: `unit` / `ornithopter` / `infantry3` / `infantry4` / `rocket` / `singleFrame`) for all 27 entries, pulled from `src/table/unitinfo.c`. New `Orientation` helper with `to8(_:)` (port of `Orientation_256ToOrientation8`: `((o+16)/32) & 7`) + the 8-entry `octantFrame` table (port of `values_32A4` at `src/gui/viewport.c:334`) — 5 distinct frames across 8 directions, the W-side three mirroring the E-side three. New `DuneIIRendering.UnitSpriteAtlas` loads `UNITS2.SHP` / `UNITS1.SHP` / `UNITS.SHP` into a single 355-slot `[SKTexture?]` mirroring OpenDUNE's `g_sprites` numbering from `src/sprites.c:487..494` — indices 111..354 populated, 0..110 and 355+ left `nil`. `UnitSpriteAtlas.resolveFrame(info:orientation:)` (nonisolated pure computation) composes `groundSpriteID + octant offset` for `.unit` / `.rocket` / `.ornithopter`, strides by 3 for `.infantry3`, by 4 for `.infantry4`, ignores orientation for `.slither` / `.singleFrame`. `ScenarioScene` replaces unit `SKShapeNode` circles with `SKSpriteNode`s driven by the atlas: each tick, `syncVisualsFromPool` recomputes frame + horizontal flip from the slot's `orientationCurrent`, tints with house colour (blend 0.35 until per-house palette remap lands), scales the native 16×16-ish SHP art up to tileSize. Fallback: 8×8 white square for out-of-atlas sprite IDs. 11 new tests: `OrientationTests` (3) cover cardinal octants + boundary rounding + octantFrame mirroring; `UnitInfoSpriteTests` (2) pin groundSpriteIDs and displayMode; `UnitSpriteAtlasTests` (5) cover frame resolution for unit / slither / single-frame / infantry3, plus an install-gated atlas-load test that asserts UNITS2/UNITS1/UNITS ranges populate. 321 tests green across 46 suites; zero warnings on clean build.
- Warning cleanup + no-warning policy + `Script_Unit_IdleAction`. `Pathfinder.swift:43` had a `var direction` that was never mutated — changed to `let`. `CLAUDE.md` feature-workflow step 5 grew a **Zero warnings** clause: after a clean rebuild (`swift package clean && swift build`), every `warning:` line is treated as a failure. Don't silence with `_ =` or ignore pragmas — fix the root cause. `Script_Unit_IdleAction` (slot 0x31) ported from `src/script/unit.c:1748` — foot / tracked / wheeled units fidget while idle: foot units occasionally nudge `spriteOffset = tools.next() & 0x3F` (~20% chance via `range(0, 10) > 8`), and all three ground movement types have a ~30% chance (`range ≤ 2`) of rotating to `orientationCurrent = Int8(bitPattern: tools.next())`. Winger / harvester / slither units skip the whole routine. Mirrors OpenDUNE's `Tools_Random_256()` ordering comment (advance-then-use). `makeIdleActionUnit(source:host:)` factory takes the same `RandomSource` the rest of the unit functions share. Wired into `FunctionTables.unitTable` slot 0x31. 3 new tests in `IdleActionTests` — skip on non-ground movement (sweeps winger / harvester / slither), tank rotation on a seed where `range(0,10) ≤ 2`, foot sprite-offset nudge on a seed where `range(0,10) > 8`. 311 tests green across 43 suites. Clean build reports zero warnings.
- P4 Phase 3 — pathfinder + route-follower. `Simulation.LandscapeInfo` ports all 15 landscape-type rows (`movementSpeed[6]` array per row matching OpenDUNE's `src/table/landscapeinfo.c`, plus `letUnitWobble` / `isValidForStructure` / `isSand` / `canBecomeSpice`). `Simulation.Pathfinder` is a faithful port of `Script_Unit_Pathfinder` + `Pathfinder_Connect` + `Pathfinder_Smoothen` from `src/script/unit.c:1012..1286` plus the tile-packed helpers `packedTile(x:y:)`, `packedDistance(from:to:)`, `directionPacked(from:to:)` (the 16-entry `returnValues` table from `src/tile.c:193`). Route buffer = sequence of 3-bit directions (0..7) terminated by `0xFF`, matching `u->route[14]` byte-for-byte. `UnitSlot.route` (14 entries) + `UnitSlot.currentDestinationX/Y` fields added; save path plumbs `route` verbatim. `Scripting.Host.tileEnterScore` is a new optional closure (`(packed, orient8, movementType) -> Int32`) — when nil, `CalculateRoute` treats all tiles as walkable (cost 128). `ScenarioScene.makeTileEnterScorer` wires a real map-backed scorer (TileResolver landscape lookup + LandscapeInfo movementSpeed + diagonal tax + invert to cost). `Script_Unit_CalculateRoute` (slot 0x0C) is a full port of `src/script/unit.c:1296` — runs the pathfinder when `route[0] == 0xFF`, copies up to 14 steps, truncates mid-route when close to `dst`, consumes one step on success. `Script_Unit_MoveToTarget` (slot 0x16) ports the minimal "arrived within threshold" path — full OpenDUNE closes-in-smoothly logic is deferred. Scheduler's `tickMovement` replaces `tickNaiveMovement` with a route-aware mover using `currentDestination` as the stable per-step target (bugfix vs. Phase 2: recomputed-each-tick sub-goals shifted when units crossed tile boundaries, so arrival was never detected). Threshold 16 px Manhattan. 7 new tests in `PathfinderTests`: packedTile / packedDistance / directionPacked cardinals / open-map routing / impassable / wall-routing / scheduler route-follower. Plan: `09.P4-Phase3.md` with updated P4 remaining list (projectile pool, explosion pool, FindBestTarget, IdleAction, Harvest, RefineSpice, playerHouseID, fog grid, team AI, tick-parity harness — est. 4-7 more sessions for full P4). 308 tests green across 42 suites.
- P4 Phase 2 — data tables + naive movement + sim-to-visual sync. `Simulation.UnitInfo` ports all 27 entries from `src/table/unitinfo.c` (trimmed to the fields our wired host functions + movement need: hitpoints / fireDistance / fireDelay / damage / movementType / hasTurret / explodeOnDeath / movingSpeedFactor / turningSpeed / actionsPlayer[4] / actionAI). New `Simulation.MovementType` enum + `Simulation.ActionID` namespace. `Simulation.StructureInfo` ports all 19 entries (hitpoints / buildCredits / fogUncoverRadius / layout) with a new `StructureLayout` enum (1x1..3x3) exposing `dimensions`. `Script_Unit_GetInfo` now covers subcases 0x00 (hitpoints ratio `hp * 256 / maxHP`), 0x02 (`fireDistance << 8`), 0x0B (has-destination), 0x0D (`explodeOnDeath`), 0x10 (turret → falls back to body), 0x12 (always-0 in vanilla). `makeSetSpeedUnit` now applies the OpenDUNE `byScenario` check (`speed * 192 / 256` for non-scenario units); `UnitSlot.byScenario` flag plumbed through both WorldSnapshot init paths. Two new mutators + slot wirings: `makeSetDestinationUnit` (slot 0x05) + `makeSetDestinationDirectUnit` (slot 0x19) writing `targetMove`, the latter also facing via `Pos32.direction`. `Simulation.Scheduler.tick()` grew a `tickNaiveMovement()` pre-script pass: for every allocated unit with `targetMove != 0`, step `max(4, speed/4)` px toward the target, snap on arrival (|dx|+|dy| ≤ 32 px), clear `targetMove`, face direction of travel. Explicitly NOT OpenDUNE-faithful — placeholder until pathfinding lands (swaps cleanly). `ScenarioScene` now drives marker layout off the live `scheduler.host` pools (keyed by pool index) and runs `syncVisualsFromPool()` after every tick — marker positions track pool pos32, freed slots hide. Structure markers honour real footprints via `StructureInfo.layout.dimensions` (3×2 refineries, not 2×2). 14 new tests: `UnitInfoTests` (7) covers table size + canonical unit/structure lookups + GetInfo 0x00/0x02/0x0D subcases; `NaiveMovementTests` (5) covers SetDestination valid/invalid, tick advances, arrival, stays-put; `EmcHostBatch67Tests` +2 for SetSpeed byScenario / non-scenario. Plan: `08.P4-Phase2.md` updates the P4 remaining list (pathfinding, projectiles, explosions, FindBestTarget, IdleAction, Harvest, RefineSpice, playerHouseID, fog grid, team AI, tick-parity harness). 301 tests green across 41 suites.
- P4 Phase 1 — host-function wiring + live script execution. `Scripting.Functions.unitTable(host:source:)` / `structureTable(host:source:)` return 64-slot `[VM.Function?]` arrays mirroring OpenDUNE's `g_scriptFunctionsUnit` / `g_scriptFunctionsStructure` tables (`src/script/script.c:33,64`) — 28/64 and 11/25 slots wired, the rest left `nil` with per-slot comments citing the blocker (pathfinding / projectile pool / UnitInfo / etc.). New pool fields: `Simulation.UnitSlot` gained `positionX/Y`, `hitpoints`, `seenByHouses`, `speed`, `spriteOffset`, `blinkCounter`, `inTransport`; `StructureSlot` gained `positionX/Y`, `hitpoints`. All plumbed through both `WorldSnapshot` init paths (save → `ObjectHeader.positionX/Y` / etc., scenario → `Pos32.centered(at: spawn.position)` + `spawn.hitPoints`). `Code/Core/Sources/DuneIICore/Map/Tile.swift` exports a pure-Swift `Pos32` tile32-coordinate value type (OpenDUNE's 256-px-per-tile scale) with `centered(at:)`, `distance` (longest + shortest/2 metric — port of `Tile_GetDistance`), `direction(from:to:)` (full port of `Tile_GetDirection` with the 32-entry directions table and 4-quadrant offsets from `src/tile.c:342`), and `of(_:host:)` encoded-index resolver. `Scripting.Host` gained a `voiceLog: [VoicePlay]` sink paralleling `textLog`. Twelve new host-function factories: batch 6 (`makeGetDistanceToTile`, `makeGetDistanceToObject`, `makeVoicePlay`) + batch 7 (`makeSetOrientationUnit`, `makeSetSpeedUnit`, `makeStopUnit`, `makeBlinkUnit`, `makeDieUnit`, `makeSetSpriteUnit`, `makeSetTargetUnit` with `Pos32.direction` aim, `makeIsInTransportUnit`, `makeDestroyStructure`). `ScenarioScene.setUpScheduler` now wires the real tables; scripts no longer halt on the first `SCRIPT_FUNCTION`. 18 new tests (4 `TileMathTests` covering centre / distance / cardinal directions / pool resolution; 14 `EmcHostBatch67Tests` covering every new function plus the unit/structure-table slot assignment smoke test). Plan: `07.P4-Phase1.md` documents coverage, blockers per unported slot, and a 6-10-session estimate for remaining P4. 287 tests green across 39 suites.
- Window aspect lock + fullscreen support. `GameWindow.contentAspectRatio = NSSize(width: 320, height: 200)` locks all user-driven resizes (drag, zoom, programmatic `setFrame`) to the native Dune II ratio. Defaults cleaned up: `nativeSize` (320×200), `defaultScale = 4` (1280×800 at launch), `minScale = 2` (640×400 floor via `contentMinSize`). `collectionBehavior` now includes `.fullScreenPrimary` so the titlebar green button enters / exits macOS fullscreen, with SpriteKit's `aspectFit` scaleMode letterboxing scenes inside the full-screen SKView. New `AppMenu.install()` builds a minimal programmatic menu bar — Application (About, Hide / Hide Others / Show All, Quit ⌘Q), View (Enter Full Screen ⌃⌘F), Window (Minimize ⌘M, Zoom, Close ⌘W) — so keyboard shortcuts work without a nib. `duneii/main.swift` calls `AppMenu.install()` at launch before discovering the install. No test impact; code path is UI-only, visually verified by the user.
- P3 scenario walker complete + P4 tick loop kickoff. `ScenarioScene` now renders three layers: the existing 64×64 ground grid (ICN tiles → SKTextures) plus two marker passes — `HouseColors.color(for:)` rectangles (32×32, 55% alpha, white stroke) for every non-`GEN` structure spawn, and circles (r=6.4 px) for every unit spawn. `HouseColors` is a new renderer-side helper with the canonical per-house hues (Atreides blue / Harkonnen red / Ordos green / Fremen yellow / Sardaukar purple / Mercenary orange). P4 kickoff: `Scenario.{House,UnitType,StructureType}.typeID` extension properties port OpenDUNE's `HOUSE_*`/`UNIT_*`/`STRUCTURE_*` numeric IDs (`src/house.h` / `src/unit.h` / `src/structure.h`). `Simulation.WorldSnapshot.init(scenario:resolver:)` builds live pools from scenario spawns (map baseline via `Map.Generator` with the scenario seed; houses at `house.typeID`; units at slot `i` for each spawn in declaration order; structures at slot `i` skipping `GEN*` rows — those stay as map stamps). `ScenarioScene.update(_:)` drives a `Simulation.Scheduler.tick()` every 5 frames (≈12 Hz at 60 fps, matching OpenDUNE's main-loop cadence); scheduler is built with `UNIT.EMC` / `BUILD.EMC` loaded from the install and empty function tables (scripts halt on first `SCRIPT_FUNCTION` — expected for now). `Formats.Emc.Program.empty` is a new canonical zero-instruction fallback. HUD label shows `Tick N · units M · structures K` live. Minor API additions: public `Scenario.init()`, `HouseLayout.init(…)`, `UnitSpawn.init(…)`, `StructureSpawn.init(…)` so tests can compose scenarios without going through an INI. 2 new tests: `RenderingInstallationTests.scenarioRoster` (real SCENA001.INI has non-empty unit + structure lists; every position fits in 64×64; every house resolves a colour) and `SimulationWorldSnapshotTests.scenarioSnapshotPoolAllocation` (install-free synthetic Atreides/Harkonnen scenario → asserts pool slots, orientation, GEN-slab filtering). Plan doc: `06.P3-Complete-P4-Kickoff.md` with manual-verification checklist. 269 tests green across 37 suites.
- P2 slice 2 nav fix — `MentatScene.mouseDown` was calling `coordinator?.route(to: .mainMenu)` which trapped users in a MainMenu ↔ Mentat cycle. Changed to `coordinator?.advance(from: self)` so the `GameController.advance(from:)` logic routes forward (Mentat → Scenario) as intended. Caught by user on first visual walkthrough of `05.P2-S2-P3.md` §4 checklist; Nav 1/2/3 and scenario visuals now all verified on a real session. Build incremental < 3 s; no test impact.
- P2 slice 2 + P3 scaffold — first runnable `duneii` executable. New `DuneIIRendering` library target and `duneii` executable target in the existing SwiftPM package. `Installation` discovers `Repositories/patched_107_unofficial/`, builds a case-insensitive `[name → (pak, archive)]` catalog with first-PAK-wins semantics matching OpenDUNE's `File_Open`. `AssetLoader` caches `IBM.PAL` + `ICON.MAP` + `TileResolver` on init and exposes lazy `loadCps` → `CGImage`, `loadShp`/`loadIcn` → `[CGImage]`, and `loadScenario` → `Scenario?`. `CGImageFactory` wraps `AssetExport.PalettedImage.render` into in-memory `CGImage`s (premultiplied RGBA8, optional index-0 transparency). Three `SKScene` subclasses: `MainMenuScene` (MENTATA.CPS full-screen, click → mentat), `MentatScene` (first available MENTAT?.CPS tinted, click → main menu), `ScenarioScene` (P3 — loads SCENA001.INI via AssetLoader, builds a `ScenarioWorld`, stamps 64×64 16×16 tile sprites as `SKSpriteNode`s with nearest-neighbour filtering; click → main menu). `GameController` is the `SceneCoordinator`; `GameWindow` is the AppKit host (1280×800 resizable). `duneii/main.swift` bootstraps `NSApplication` with a critical `NSAlert` fatal-path when the install isn't discoverable. Six real-install-gated tests (`RenderingInstallationTests`) verifying PAK catalog, case-insensitive lookup, scenario prefix search, palette + iconmap caching, CPS → CGImage native 320×200, `SCENA001.INI` scenario decode, and ≥100 16×16 ICN tiles. **Deviation from Initial plan**: AppKit host instead of Mac Catalyst (xcodegen isn't installed; hand-authoring a Catalyst `project.pbxproj` was more fragile than SwiftPM). SpriteKit rendering is identical under either host — migration path is a project-regeneration step, not a logic rewrite. Ergonomic `Simulation.WorldSnapshot.init(loading:resolver:)` lands alongside, running `Map.Generator` from the save's `mapSeed` internally (install-gated test in `SimulationWorldSnapshotTests`). Closes P2 slice 1 formally (`04.P2-S1-Complete.md`) and opens a slice-2 + P3 scaffold plan (`05.P2-S2-P3.md`) with an explicit manual-verification checklist since visual correctness can't be asserted from CI. 267 tests green across 37 suites. `swift build` clean in < 5 s.
- EMC host batch 5 — further `Script_General_*` generics that need no new pool state. Five new factories on `Scripting.Functions`: `makeDelayRandom(source:)` (closes over `RandomSource` — now carrying both `BorlandLCG` and `ToolsRandom256`; returns `(Tools_Random_256() * peek(1)) / 256 / 5` and writes `engine.delay`), `makeGetIndexType(host:)` (returns `IT_NONE`/`TILE`/`UNIT`/`STRUCTURE` as `0`/`1`/`2`/`3`, `0xFFFF` when `Tools_Index_IsValid` fails), `makeDecodeIndex(host:)` (returns `EncodedIndex.decoded` — pool index for unit/structure, `Tile_PackXY` for tile — or `0xFFFF` invalid), `makeGetLinkedUnitType(host:)` (reads `currentObject.linkedID` from either unit or structure, returns the linked unit's `type` or `0xFFFF` when unlinked / linked-slot freed), `makeFindIdle(host:)` (two-mode: encoded-structure-index → `1` if same-house + IDLE else `0`; IT_UNIT / IT_TILE → `0`; otherwise `PoolQuery`-iterates structure pool filtered by current house + peek(1) as type, returning first IDLE match encoded or `0`). `RandomSource` gained a `tools: RNG.ToolsRandom256` field and a `init(lcgSeed:toolsSeed:)` constructor; the default `init(seed:)` seeds both streams from the same `UInt16`. 13 tests in a new `Scripting host batch 5 (further generics)` suite: DelayRandom computed against a preview of the same seeded `ToolsRandom256`, DelayRandom stream advancement across two calls; GetIndexType kind sweep (unit/structure/tile/none) and freed-slot `0xFFFF`; DecodeIndex kind sweep including tile-pack `Tile_PackXY(3, 5) == 323`; GetLinkedUnitType linked/unlinked/structure-origin; FindIdle positive/negative in both modes plus the IT_UNIT/IT_TILE short-circuit. Algorithm doc extended: `EmcHostFunctions.md` §10. 260 tests green across 36 suites.
- EMC host batch 4 — structure-specific generics. Two new factories on `Scripting.Functions` port the smallest entries from `g_scriptFunctionsStructure`: `makeGetStateStructure` (slot 0x0D — returns `s->state` reinterpret-cast to `UInt16`, so `-1` becomes `0xFFFF`) and `makeSetStateStructure` (slot 0x04 — writes `peek(1)` to `s->state`; the `-2` DETECT sentinel resolves to IDLE/READY/BUSY based on `linkedID == 0xFF` / `countDown == 0` / else). `Structure_UpdateMap` side-effect deferred — this layer owns state writes only. `Simulation.StructureSlot` grew two fields: `state: Int16` and `countDown: UInt16`. `Simulation.WorldSnapshot` plumbs both through from `Formats.Save.Structures.Slot` verbatim for soft-range records; reserved slots (79-81) bypass this path since they never carry structure state in vanilla saves. A private `currentStructure(host:)` helper mirrors `currentUnit(host:)`, accepting any `isUsed` slot (including the reserved trio, defensive). 8 tests in a new `Scripting host batch 4 (structure-specific)` suite: GetState with signed round-trip (both `-1 → 0xFFFF` and `2 → 2`), GetState no-current-structure returns 0, SetState explicit non-DETECT write, SetState DETECT resolves to IDLE / READY / BUSY, SetState no-current no-op, and `_SAVE001.DAT` WorldSnapshot round-trip asserting `state` + `countDown` equal the save record for every non-reserved allocated structure. Algorithm doc extended: `EmcHostFunctions.md` §9. 247 tests green across 35 suites.
- EMC host batch 3 — unit-specific generics. Three new `Scripting.Functions.makeXxxUnit(host:)` factories port the per-unit entries from OpenDUNE's `g_scriptFunctionsUnit`: `makeGetInfoUnit` (slot 0x00 — ten of twenty `GetInfo` subcases covered: `targetMove` with live `Tools_Index_IsValid`, `index`, `orientationCurrent`, `targetAttack`, `originEncoded`, `type`, `EncodedIndex.unit(index)`, `houseID`; subcases that need `g_table_unitInfo` or `g_playerHouseID` return `0`, matching OpenDUNE's `default` branch), `makeSetActionUnit` (slot 0x01 — writes `peek(1) & 0xFF` to the current unit's `actionID`, no-op without `currentObject`), `makeGetAmountUnit` (slot 0x20 — returns own `amount` when `linkedID == 0xFF`, otherwise the linked unit's `amount`, falling back to own `amount` if the linkedID is dangling). `Simulation.UnitSlot` gains five fields: `actionID`, `amount`, `targetAttack`, `targetMove`, `originEncoded`. `Simulation.WorldSnapshot` plumbs them through from the save records verbatim. Live `Tools_Index_IsValid` lives as a private `isValid(encoded:host:)` helper on `Functions` — tile always valid, none never, unit/structure require `isUsed` + (for units) `isAllocated`. 9 tests in a new `Scripting host batch 3 (unit-specific)` suite: GetInfo subcase sweep over nine values with pinned slot state, GetInfo no-current-object returns 0, GetInfo 0x01 returns targetMove when encoded index resolves, SetAction writes with mask, SetAction no-op without current, GetAmount unlinked, GetAmount linked, GetAmount no-current, and `_SAVE001.DAT` WorldSnapshot round-trip asserting all five new fields equal the save record. Algorithm doc extended: `EmcHostFunctions.md` §8. 239 tests green across 34 suites.
- `Simulation.Scheduler` — per-tick driver for EMC script dispatch. On each `tick()`: walks the unit pool in `findArray` order, sets `host.currentObject = .unit(poolIndex:)`, and either decrements `engine.delay` or runs up to `unitOpcodesPerTick` (= 7, matching OpenDUNE's `SCRIPT_UNIT_OPCODES_PER_TICK + 2`) `VM.step(_:)` calls; then the structure pool with budget 3 and a skip set `{SLAB_1x1=0, SLAB_2x2=1, WALL=14}` matching OpenDUNE's explicit `continue` in `Structure_Tick`. After both walks, clears `host.currentObject` to nil. Per-slot engines stored on the scheduler as parallel `[Scripting.Engine]` arrays (capacity 102 / 82) keyed by pool index, avoiding further bloat of the `UnitSlot` / `StructureSlot` value types. Host-function `delay` writes end the step loop early via the `remaining > 0 && engine.delay == 0` guard, preserving OpenDUNE's "one tick per requested delay" behaviour. 6 tests: delay countdown 3→2→1→0 over three ticks with pc unchanged then dispatch on tick 4; opcode budget caps pc advancement at `unitOpcodesPerTick × wordsPerInsn`; mid-loop `Script_General_Delay` exits after exactly two opcodes despite budget = 7; multi-unit walk matches findArray insertion order; `host.currentObject` cleared after tick; structure slab/wall skip verified by checking their engine.delay is unchanged while a real structure's decrements. Algorithm doc: `TickScheduler.md` (contract, dispatch pseudocode, walk order, scope exclusions). 230 tests green across 33 suites.
- `Scripting.EncodedIndex` + `Scripting.Host` + second batch of EMC host functions. `EncodedIndex` is a value-type port of OpenDUNE's `Tools_Index_*` 16-bit encoding (top two bits pick `Kind` — `.none`/`.tile`/`.unit`/`.structure`; `decoded` returns pool index for unit/structure or `Tile_PackXY(x,y)` for tile; `unit(_:)` / `structure(_:)` factories apply the `0x4000` / `0x8000` mask). `Host` is a reference-typed context carrying `UnitPool`, `StructurePool`, an optional `currentObject: ObjectRef` (unit or structure pool index), the EMC program's `texts` table, and a `textLog: [DisplayedText]` sink; it exposes `currentHouseID`, `houseID(of:)`, and `unitSlot(for:)` convenience queries that encapsulate `Tools_Index_IsValid` against live pool state. `Simulation.UnitSlot` grows one minimal field — `orientationCurrent: Int8` (matches `u->orientation[0].current`) — plumbed through `WorldSnapshot` from the save record's `orientation[0].current`. Five new `Scripting.Functions.makeXxx(host:)` factories, all following the existing `peek`-never-`pop` convention: `makeDisplayText` (appends `texts[peek(1)]` + three args to `textLog`, drops out-of-range indices silently), `makeUnitCount` (PoolQuery iteration filtered by current house + peek(1) type), `makeGetOrientation` (returns `orientationCurrent` or `128` sentinel for invalid/freed units), `makeIsEnemy` (`1` if target house differs from current, `0` otherwise including invalid — matches OpenDUNE), `makeIsFriendly` (`1` friendly, `0xFFFF` enemy, `0` invalid — pre-checks `used` then delegates house comparison to `makeIsEnemy`). 13 tests across a new `Scripting host batch 2 (context-aware)` suite (EncodedIndex kinds × 4; DisplayText append with correct texts[] lookup and three args; UnitCount filters both house and type and returns 0 with no current object; GetOrientation returns the pool slot's orientation for valid / returns 128 for invalid / returns 128 for freed slot; IsEnemy same-house vs. different-house vs. invalid; IsFriendly friendly/enemy/invalid). Algorithm doc extended: `EmcHostFunctions.md` §7 (batch-2 contract + `Host` shape + `UnitSlot.orientationCurrent` rationale). 224 tests green across 32 suites.
- `Simulation.WorldSnapshot` — value-type bridge from `Formats.Save.Game` into live pools plus a dense 4096-cell tile grid. Single `throws` initializer walks the save's record arrays in file order: allocates `HousePool` slots from `PLYR` records (copying `starportLinkedID`); allocates `UnitPool` slots from `UNIT` records via `allocate(at:type:houseID:)` and restores `linkedID`; routes `BLDG` records through `StructurePool.allocate(at:type:houseID:)` for the soft `[0, 78]` range and `allocateReserved(at:type:)` for the wall/slab aggregates `[79, 81]` (defensive — real 1.07 saves never carry reserved indices, but OpenDUNE-produced saves could). Tile grid synthesises one `WorldSnapshot.Tile` per baseline cell (carrying baseline `groundTileID` + `overlayTileID` with fog and ownership flags defaulted), then replaces the tile at each `tileMap.entries[i].cellIndex` with the decoded sparse `Tile`. `WorldSnapshot.Tile.objectRef` preserves the off-by-one pool-plus-one semantic from §11.2 verbatim. Cross-chunk invariants enforced by `LoadError`: `.duplicateHouseIndex`, `.unitIndexOutOfRange`, `.duplicateUnitIndex`, `.structureIndexOutOfRange`, `.duplicateStructureIndex`. Baseline cell-count is not re-validated here — `Map.init` already asserts `cells.count == 4096`. 6 tests (pinned single-house / single-unit / single-structure synthetic with pool populations asserted, sparse-tile override layering at cells 10 and 4095 with unchanged baseline elsewhere, duplicate-house rejection, out-of-range unit index, out-of-range structure index, real-data end-to-end load of `_SAVE001.DAT` with every unit / structure record verified against its pool slot and every sparse tile entry verified against the dense grid). Architecture doc: `Documentation/Architecture/WorldSnapshot.md` (contract, `Tile` field table, failure modes, explicit scope exclusions — EMC resumption, fog reconstruction, tile-object cross-link validation). 211 tests green across 31 suites.
- `Formats.Save.Game` — top-level aggregator that closes save-format coverage for vanilla 1.07. Pure composition over the existing body decoders: `Container.decode` → index lookups → route `INFO` / `PLYR` / `UNIT` / `BLDG` / `MAP ` through their respective `Save.*` decoders; decode `NAME` as NUL-terminated ASCII into the `description` field; surface `TEAM` and `ODUN` as optional raw `Data` blobs (both always `nil` in vanilla saves). Every failure maps to a distinct `DecodeError` case that wraps the underlying error verbatim: `.container(_)`, `.info(_)`, `.player(_)`, `.units(_)`, `.structures(_)`, `.tileMap(_)`, plus two aggregator-level cases `.missingRequiredChunk(tag:)` and `.nameNotAscii`. Unknown chunks are tolerated (matches OpenDUNE's `Load_Main` warn-and-skip behaviour). 7 tests (real-data end-to-end on `_SAVE001.DAT` asserting description is non-empty, version `0x0290`, human house present, units+structures+sparse tiles non-empty, TEAM/ODUN both nil; synthetic minimal-valid composition round-tripping description + counts; `missingRequiredChunk` surfaces the specific absent tag; malformed container propagates `.container`; non-ASCII `NAME` rejected; legacy version in `INFO` propagates `.info(legacyVersion)`; unknown chunks tolerated with odd-length pad). Format doc extended: `SAVE.md` §12 (contract, failure-mode table, explicit "what Game does NOT do" — no pool allocation, no map reconstruction, no script resumption). 205 tests green across 30 suites. **Save-format porting complete** — every chunk in a vanilla 1.07 `_SAVE00?.DAT` now round-trips into typed Swift values.
- `Formats.Save.TileMap` — body decoder for the `MAP ` chunk (note the trailing space in the 4CC). **Sparse**, not a fixed 4096-tile dump: each 6-byte record is `u16 LE cellIndex` + 4 packed tile bytes, emitted only for tiles that differ from the map-seed-generated baseline or carry dynamic state (unveiled, structure, unit, animation, explosion). 9-bit `groundTileID` spans bytes 0 and 1 (low byte + bit 0 of byte 1); 7-bit `overlayTileID` sits in bits 1-7 of byte 1; byte 2 packs 3-bit `houseID` + five boolean flag bits (`isUnveiled` / `hasUnit` / `hasStructure` / `hasAnimation` / `hasExplosion`); byte 3 is the off-by-one pool reference (`0` = none, `n` = Unit/Structure pool index `n - 1`). 9 tests (record-size constant, pinned single-tile round-trip exercising every packed field, 9-bit `groundTileID` spanning bytes correctly, 7-bit `overlayTileID`, all boolean flag bits set, misaligned-body rejection, `cellIndex ≥ 0x1000` rejection, empty body allows zero entries, real-data decode of `_SAVE001.DAT`'s `MAP ` chunk asserting `count % 6 == 0`, ascending `cellIndex` ordering, and per-record value ranges `groundTileID < 512`, `overlayTileID < 128`, `houseID < 8`, `cellIndex < 0x1000`). Format doc extended: `SAVE.md` §11. Insight: `format-save-map-is-sparse-not-fixed.md` — expected chunk size is `sparseCount × 6`, not `4096 × tileSize`; reconstructing the full grid needs `Core.Map.Generator` output as the baseline + the sparse overrides layered on top. 198 tests green across 29 suites.
- `Formats.Save.Structures` — body decoder for the `BLDG` chunk. Packs allocated Structures into 88-byte records: the shared 71-byte `ObjectHeader` from §9 plus a 17-byte structure tail (`s_saveStructure` in `src/saveload/structure.c`) with `creatorHouseID`, `rotationSpriteDiff`, `SLD_EMPTY(UINT8)` pad, `objectType` (currently-producing type), `upgradeLevel` + `upgradeTimeLeft`, `countDown`, `buildCostRemainder` (fractional credits-per-tick accumulator), a signed `state` (`-1` = invalid/idle), and `hitpointsMax`. Reuses `ObjectHeader` / `ScriptState` / `ObjectFlags` unchanged — no new shared infrastructure. Misaligned bodies surface `DecodeError.misalignedBody(length:)`. The pool's three reserved aggregate slots (indices 79-81, for walls/slabs) never appear in the chunk — OpenDUNE's `Structure_Save` walks `Structure_Find`, which only yields allocated slots from `[0, 78]`. 6 tests (slot-size constant, pinned two-structure synthetic, signed-`state` decode for `-1`, misaligned-body rejection, empty-body zero slots, real-data decode of `_SAVE001.DAT`'s `BLDG` asserting multiple-of-88, `flags.used && !flags.isUnit` on every slot, `type ≤ 44`, `houseID ≤ 5`, `index ≤ 81`, and `hitpoints ≤ hitpointsMax`). Format doc extended: `SAVE.md` §10. 189 tests green across 28 suites.
- `Formats.Save.Units` + shared `ObjectHeader` — body decoder for the `UNIT` chunk (one 128-byte record per allocated Unit). Each record layers three blocks ported byte-for-byte from OpenDUNE: a 71-byte `ObjectHeader` (`g_saveObject` in `src/saveload/object.c`) with an `ObjectFlags` `u32 LE` bitfield covering 17 named bits (`used`, `allocated`, `isNotOnMap`, `isSmoking`, `fireTwiceFlip`, `animationFlip`, `bulletIsBig`, `isWobbling`, `inTransport`, `byScenario`, `degrades`, `isHighlighted`, `isDirty`, `repairing`, `onHold`, `isUnit`, `upgrading`) plus `index` / `type` / `linkedID` / `houseID` / `seenByHouses` / `positionX,Y` / `hitpoints`; a nested 55-byte `ScriptState` (`g_saveScriptEngine`) with `delay`, `scriptOffset` (word-offset into entry-point code, callback-encoded as `u32` on disk), `returnValue`, `framePointer` / `stackPointer`, `variables[5]` and `stack[15]`, and `isSubroutine`; and a 57-byte unit tail (`s_saveUnit`) with the `SLD_EMPTY(UINT16)` pad, `currentDestinationX,Y`, `originEncoded`, `actionID` / `nextActionID`, narrowed-to-`u8` `fireDelay`, `distanceToDestination`, `targetAttack` / `targetMove`, `amount`, `deviated`, two `targetLast` / `targetPreLast` tile pairs, two 3-byte `dir24` orientation tracks, the speed quintet + `spriteOffset` + `blinkCounter` + `team`, `timer`, and a 14-byte `route` path buffer. `ObjectHeader` + `ScriptState` + `ObjectFlags` live in a shared `SaveObjectHeader.swift` so `BLDG` can reuse them. Chunk length must be a multiple of 128; misalignment surfaces as `DecodeError.misalignedBody(length:)`. 6 tests (slot-size constant, pinned two-unit synthetic with flags / script / orientation / route round-trips, misaligned-body rejection, empty-body allows zero slots, real-data decode of `_SAVE001.DAT` asserting multiple-of-128, every slot has `used` + `isUnit` set, `type ≤ 27`, `houseID ≤ 5`, `index ≤ 101`, `hitpoints ≤ 2000`, and a separate assertion that `_SAVE001.DAT` does NOT carry `ODUN`). Format doc extended: `Documentation/Formats/SAVE.md` §9. Insight: `format-save-odun-repairs-narrowed-fields.md` — `ODUN` is not "extra fields"; it patches values the `UNIT` record truncated (`fireDelay` u16→u8 narrowing; `deviatedHouse` byte that disambiguates "Ordos did it" when absent). 183 tests green across 27 suites.
- `Formats.Save.Player` — body decoder for the `PLYR` chunk (one record per allocated House). Flat port of OpenDUNE's `s_saveHouse` table (`src/saveload/house.c`): each 66-byte record is 21 × `u16 LE` (index, harvestersIncoming, flags word, four unit-count counters, six economy / power counters, palace x/y, one `SLD_EMPTY` pad word, three alert cooldowns, two starport fields) + 1 × `u32 LE` (`structuresBuilt` bitmask) + `u16[10]` (`aiStructureRebuild` queue). Chunk body size is `slotCount × 66`, with misaligned bodies surfaced as `DecodeError.misalignedBody(length:)`. `HouseFlags` is a value type with computed accessors for the five documented bits (`used`, `human`, `doneFullScaleAttack`, `isAIActive`, `radarActivated`) plus `rawWord` for round-tripping; bits `0x20…0x8000` are ignored. `humanSlot` projection returns the first slot with `flags.human` set, matching OpenDUNE's `House_Load` path that wires up `g_playerHouseID` via the flag, not the index. 6 tests (slot-size constant, pinned two-slot synthetic, human-house-by-flag, misaligned-body rejection, empty-body allows zero slots, real-data decode of `_SAVE001.DAT` asserting chunk is a multiple of 66 bytes, at least one human slot, and every slot has `flags.used` set with a valid 0…5 `index`). Real saves are sparser than the 6-house pool cap: `_SAVE001.DAT` allocates only 2 (player + single enemy) → 132-byte chunk. Format doc extended: `Documentation/Formats/SAVE.md` §8. 177 tests green across 26 suites.
- `Formats.Save.Info` — body decoder for the `INFO` chunk in modern (`0x0290`) saves. Flat port of OpenDUNE's `s_saveInfo` table (`src/saveload/info.c`) plus the nested `g_saveScenario` table (`src/saveload/scenario.c`): 2-byte `u16 LE` version, then a 228-byte scenario block (score, win/lose flags, `mapSeed`, `mapScale`, `timeOut`, three 14-byte `picture*` filenames, six counter u16s, sixteen 10-byte `Reinforcement` records), then 100 bytes of top-level UI / campaign state (`playerCreditsNoSilo`, minimap/selection positions, `selectionType`, `structureActive*` triplet, `unit*` indices, `activeAction`, `strategicRegionBits`, `scenarioID`, `campaignID`, two `hintsShown` dwords, `scenarioElapsedTicks`, duplicate `playerCreditsNoSilo`, `starportAvailable[27]`, `houseMissileCountdown`, `unitHouseMissileIndex`, `structureIndex`). Modern body size is exactly 330 bytes (version + 328-byte payload). Legacy-version saves are rejected with a dedicated `DecodeError.legacyVersion(version:)` rather than mis-aligning every subsequent field. 5 tests (pinned synthetic round-trip over every field, duplicate-field second-wins, legacy-version rejection, truncated-chunk rejection, real-data decode of `_SAVE001.DAT` asserting `scenarioID ∈ 1…22`, `campaignID ∈ 0…9`, non-zero map seed, printable-ASCII briefing filename, starport stock sanity). Format doc extended: `Documentation/Formats/SAVE.md` §7. Insight: `format-save-info-duplicate-credits.md` — OpenDUNE's `s_saveInfo` references `g_playerCreditsNoSilo` twice; the second write wins and there is no distinct "reserve credits" field. 171 tests green across 25 suites.
- `Formats.Save.Container` — top-level IFF-style walker for `_SAVE00?.DAT`. Asserts the `FORM` + size + `SCEN` header (where `SCEN` is the IFF form-type, not a chunk with a length), then indexes every `{ tag:4cc, length:u32be, body, pad? }` chunk into a `[String: Data]` keyed by tag. 4CC tags preserve whitespace verbatim — critical for `MAP ` (trailing space). The convenience `version` accessor reads the `u16 LE` at the head of `INFO` (0x0290 for modern 1.07). Per-chunk decoders are deliberately deferred: this commit only delivers the container walker and chunk index. 8 tests — synthetic two-chunk round-trip, `MAP ` vs `MAP` key distinction, odd-length pad handling, `notForm` / `notScen` / `chunkLengthPastEnd` / truncated-header failure modes, plus a real-data pass over `_SAVE001.DAT` that asserts the vanilla-1.07 chunk set (`NAME`, `INFO`, `PLYR`, `UNIT`, `BLDG`, `MAP `). Format doc: `Documentation/Formats/SAVE.md`. Insight: `format-save-odun-is-opendune-only.md` — the `ODUN` chunk referenced by OpenDUNE's writer never appears in original Dune II saves, and `TEAM` is conditional on the scenario having AI teams. 166 tests green across 24 suites.
- `Simulation.PoolQuery` + `UnitPool.next(_:)` / `StructurePool.next(_:)` — filter-and-resume iteration over the pool find-arrays, mirroring OpenDUNE's `Unit_Find` / `Structure_Find`. Value-type `PoolQuery { houseID, type, position }` with `nil` meaning "any" for both filters; the opaque `position` cursor persists across calls so callers can `break` out and resume. `StructurePool.next` walks `findArray` first then yields the three reserved aggregate slots (`indexWall`, `indexSlab2x2`, `indexSlab1x1`) as a tail trio, but only when the reserved slot's `isUsed` flag is set — matching OpenDUNE's `Structure_Find` tail walk. Exhausted queries return `nil` idempotently (no crash on continuation). 7 tests (no-filter walk order, houseID filter, combined houseID+type filter, exhausted-query idempotency, walk-after-free, structure reserved-tail walk with partial allocation, structure tail filter). Architecture doc extended with a §6 "Filtered iteration — `PoolQuery`" section. 158 tests green across 23 suites.
- `Scripting.Functions` + `Scripting.RandomSource` — first batch of generic host callbacks wired into the EMC VM. `Engine` grows a `delay: UInt16` field (observable output, not yet consulted by `step()`); `Scripting.popForTest` is promoted to three public stack primitives — `push`, `pop`, and `peek` — with `peek` being the intended default. Three host functions land: `noOperation` (returns 0), `delay` (`engine.delay = peek(1) / 5`), and a `makeRandomRange(source:)` factory that closes over a shared `RandomSource` (reference-typed wrapper around `RNG.BorlandLCG`) so multiple call sites draw from OpenDUNE's single global LCG stream. All three functions use `peek`, never `pop` — the EMC compiler emits a `STACK_REWIND` after every call. 6 tests (reset-delay, no-op preserves stack, delay divides by 5 and truncates sub-5 requests to 0, random-range bounds, shared-LCG state between draws). Existing EMC VM test updated to use `Scripting.pop` in place of `popForTest`. Algorithm doc: `Documentation/Algorithms/EmcHostFunctions.md`. Insight: `scripting-host-fn-peek-not-pop.md`. 151 tests green across 23 suites.
- `Simulation.UnitPool` (102 slots), `Simulation.StructurePool` (82 hard / 79 soft + reserved 79/80/81 aggregates for walls and slabs), `Simulation.HousePool` (6 slots) — fixed-capacity allocators with a parallel insertion-ordered `findArray` cache mirroring OpenDUNE's `g_*FindArray`. Slot value types (`UnitSlot`, `StructureSlot`, `HouseSlot`) are deliberately minimal — just enough fields (`isUsed`, `isAllocated`, `index`, `type`, `houseID`, `linkedID` / `starportLinkedID`) to support the allocator round-trip; gameplay fields land alongside the systems that need them in P4. `allocate(at:)` and `allocate(in:)` honour the OpenDUNE rejection rules (in-use slot → nil, out-of-range → nil); `free(at:)` is a no-op for unused slots and uses ordered remove on `findArray` to preserve insertion order. Three pool quirks faithfully ported: (a) `StructurePool.allocateReserved` always succeeds and never touches `findArray`; (b) `StructurePool.allocate(in:)` only walks the soft range `[0, 78]`; (c) `HousePool.free` removes from `findArray` but leaves `slots[i].isUsed == true`, mirroring OpenDUNE's `House_Free` omission. 16 tests covering empty init, allocate-at, allocate-in-range, allocate-twice rejection, free-preserves-order, free-of-unused no-op, the structure reserved-slot path, and the house "freed-but-still-used" round-trip. Architecture doc: `Documentation/Architecture/Pools.md`. Insight: `simulation-house-free-leaves-used.md`. 145 tests green across 22 suites.
- `Scripting.Engine` + `Scripting.VM` — stack-based EMC virtual machine. `Engine` is a `Sendable` value type with the canonical `pc / returnValue / framePointer / stackPointer / variables[5] / stack[15] / isSubroutine / halted` layout from OpenDUNE's `ScriptEngine`. `VM` pairs a `Formats.Emc.Program` with a 64-slot host-function table and exposes `step(_:)` that executes exactly one opcode, mirroring `Script_Run` byte-for-byte across all 19 opcodes (JUMP, SETRETURNVALUE, PUSH_RETURN_OR_LOCATION, PUSH/PUSH2, PUSH_VARIABLE / LOCAL_VARIABLE / PARAMETER, the four POP_* variants, STACK_REWIND/FORWARD, FUNCTION dispatch, JUMP_NE, the three UNARY ops, the 18 BINARY ops, RETURN). Halt semantics replicate OpenDUNE's `script = NULL` via a sticky `engine.halted = true` and a peek-before-pop guard on multi-pop opcodes. 12 tests covering reset state, push/pop, BINARY arithmetic, UNARY operators, JUMP_NE branching, SETRETURNVALUE round-trip, stack alloc/dealloc, FUNCTION dispatch, and three halt paths (unknown BINARY parameter, nil function slot, RETURN with empty stack), plus a 5-instruction pinned program. Algorithm doc: `Documentation/Algorithms/EmcVM.md`. Insight: `scripting-emc-saved-location-plus-one.md` documenting the `pc + 1` save-location quirk. 129 tests green across 21 suites.
