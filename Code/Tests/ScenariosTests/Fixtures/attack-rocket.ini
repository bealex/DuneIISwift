; Rocket-attack golden: two Launchers (turreted, fireDistance 9) five tiles apart. The commanded one is
; ordered to attack; both are within range, so each aims its turret and fires notAccurate rockets without
; moving — and being mutually in range, neither falls idle (no IdleAction twitch). This exercises the
; missile / notAccurate-scatter branch of Unit_CreateBullet (the rocket scatters via RNG), which the
; point-blank `attack-close` bullet never touches. Both stationary ⇒ no wobble ⇒ the RNG stream stays aligned.
[BASIC]
MapScale=1
[MAP]
Seed=4660
[UNITS]
ID000=Harkonnen,Launcher,256,1040,0,Guard
ID001=Ordos,Launcher,256,1045,0,Guard
