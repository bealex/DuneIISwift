; Bootstrap scenario for the behavioural golden harness.
; Shared verbatim by our comparison tests and the OpenDUNE oracle.
; Terrain comes from [MAP] Seed via Map_CreateLandscape (the real generator — natural partial
; sand/rock transition tiles). Units are placed here; their move/attack orders are issued afterwards
; as simulated user commands (so the same file serves every scenario kind).

[BASIC]
MapScale=1

[MAP]
Seed=4660

[UNITS]
ID000=Harkonnen,Tank,256,1040,0,Guard
