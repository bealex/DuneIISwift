; House-economy golden: an Ordos base (windtrap + spice silo) with starting credits, no units, no combat.
; Activates the house via the new [HOUSES] section so GameLoop_House runs and the per-tick dump carries
; the house aggregate. Validates House_CalculatePowerAndCredit (powerProduction from the windtrap, power
; usage + creditsStorage from the silo) and the non-player credit clamp (credits start above storage and
; are clamped down to it on the first House tick). Harvester-free (no refinery) so the oracle's House loop
; stays deterministic + RNG-neutral. Exercises this session's House subsystem cross-engine.
[BASIC]
MapScale=1
[MAP]
Seed=4660
[HOUSES]
Ordos=2000
[STRUCTURES]
ID000=Ordos,Windtrap,256,1042
ID001=Ordos,Spice Silo,256,1046
