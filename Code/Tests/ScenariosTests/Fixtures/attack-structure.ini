; Attack-a-structure golden: a Harkonnen tank ordered to attack an Ordos windtrap.
; Both combatants are stationary (no idle wobble), so the structure's HP drain + state are deterministic
; until the death soldier-spawn RNG — the Swift side compares structures (hp/state/alive) per tick and
; gates `compared` at the death horizon. Exercises Structure_Damage / Structure_Destroy + the death script.
[BASIC]
MapScale=1
[MAP]
Seed=4660
[UNITS]
ID000=Harkonnen,Tank,256,1040,0,Guard
[STRUCTURES]
ID000=Ordos,Windtrap,256,1042
