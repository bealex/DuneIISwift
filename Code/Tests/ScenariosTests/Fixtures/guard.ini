; Scenariolab-like guard golden: a Harkonnen tank guards while an Ordos trike approaches from afar.
; The approach (mover movement + the guard sitting) is deterministic and matches per-tick; gate before
; the guard reacts to the mover (target acquisition / fire are combat, not chased byte-exact).
[BASIC]
MapScale=1
[MAP]
Seed=4660
[UNITS]
ID000=Harkonnen,Tank,256,1100,0,Guard
ID001=Ordos,Trike,256,2600,0,Guard
