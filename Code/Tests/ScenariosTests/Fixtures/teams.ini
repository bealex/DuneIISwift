; Team-AI golden: an Ordos team running the `Normal` brain recruits nearby same-house tanks. Activated via
; the new [TEAMS] section (parity.c Scen_LoadTeam → Team_Create, + the team's house flagged isAIActive so
; GameLoop_Team runs it). No enemy + the harness leaves units unseen (seenByHouses 0, fog isn't ticked), so
; the team recruits but doesn't acquire a target — both engines behave identically. Proves the team loop +
; Team_AddClosestUnit cross-engine; the full recruit→target→order chain is covered by TeamLoopTests
; (which sets visibility). Tracked tanks, clustered, so recruiting order is deterministic.
[BASIC]
MapScale=1
[MAP]
Seed=4660
[UNITS]
ID000=Ordos,Tank,256,1300,0,Guard
ID001=Ordos,Tank,256,1301,0,Guard
ID002=Ordos,Tank,256,1364,0,Guard
[TEAMS]
ID000=Ordos,Normal,Tracked,2,4
